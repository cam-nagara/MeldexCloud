/* Generated by build_cloud_static.py. Preserve source order. */

/* === gb-cloud-mobile-explorer.js === */
;
(function () {
  if (window.__MeldexCloudMobileExplorerInstalled) return;
  window.__MeldexCloudMobileExplorerInstalled = true;

  const CREATE_ITEMS = [
    ['フォルダ', 'folder', 'folderPlus'],
    ['ノート', 'page', 'filePlus'],
    ['シナリオ', 'scriptnote', 'bookPlus'],
    ['シート', 'database', 'tableProperties'],
    ['ボード', 'board', 'presentation'],
    ['スマートシート', 'smart-db', 'database'],
    ['カレンダー', 'calendar', 'calendarPlus'],
  ];
  const CONTAINER_TYPES = new Set(['folder', 'database']);
  const MODE_STORAGE_KEY = 'meldex-cloud-mobile-explorer-mode';
  const LAYOUT_STORAGE_KEY = 'folder-layout';
  const LAYOUT_ITEMS = [
    ['リスト', 'list', 'list'],
    ['グリッド', 'grid', 'grid3x3'],
    ['ウォーターフォール', 'waterfall', 'layoutGrid'],
    ['横並び', 'hflow', 'galleryHorizontal'],
  ];
  let _mode = 'tree';
  let _layout = _normalizeLayout(_readLayoutStorage());
  let _currentFolder = null;
  let _currentItems = [];
  let _listSeq = 0;
  let _treeClickBridgeInstalled = false;
  let _createOverlay = null;
  let _itemMenuOverlay = null;

  function _isEnabled() {
    return !!window.MeldexCloudMobile?.shouldUseSidebarDrawer?.()
      || document.body?.dataset?.cloudMobile === '1';
  }

  function _sidebar() {
    return document.getElementById('sidebar');
  }

  function _iconHtml(name, size, fallback) {
    return typeof lucide === 'function'
      ? lucide(name, size || 18)
      : `<span aria-hidden="true">${fallback || ''}</span>`;
  }

  function _normalizePath(path) {
    return String(path || '').replace(/\\/g, '/').replace(/\/+$/, '');
  }

  function _readLayoutStorage() {
    try { return localStorage.getItem(LAYOUT_STORAGE_KEY) || 'waterfall'; } catch {}
    return 'waterfall';
  }

  function _normalizeLayout(mode) {
    const value = String(mode || '').trim();
    return LAYOUT_ITEMS.some(item => item[1] === value) ? value : 'waterfall';
  }

  function _e2eIdPart(value) {
    return String(value || 'item')
      .replace(/\\/g, '/')
      .replace(/[^a-zA-Z0-9_-]+/g, '-')
      .replace(/^-+|-+$/g, '')
      .slice(0, 80) || 'item';
  }

  function _displayName(target) {
    const name = String(target?.name || '').trim();
    if (name) return name;
    const path = String(target?.path || '').replace(/\\/g, '/').replace(/\/+$/, '');
    return path.split('/').filter(Boolean).pop() || 'フォルダ';
  }

  function _pathHint(path) {
    const clean = String(path || '').replace(/\\/g, '/');
    const parts = clean.split('/').filter(Boolean);
    if (parts.length <= 2) return clean || 'ソースフォルダ';
    return parts.slice(-2).join(' / ');
  }

  function _treeNodeCanActAsFolder(data) {
    const type = String(data?.type || '').toLowerCase();
    return !!data?.path && (data._isRoot || CONTAINER_TYPES.has(type));
  }

  function _isTreeRowControlTarget(target) {
    return !!target?.closest?.('.tree-toggle, .tree-hover-btn, button, a, input, textarea, select, [contenteditable="true"]');
  }

  function _targetFromData(data) {
    return {
      path: data.path || '',
      name: data.name || data.label || String(data.path || '').split(/[\\/]/).pop() || 'フォルダ',
    };
  }

  function _isWriteBlocked() {
    const dataset = document.body?.dataset || {};
    return dataset.cloudReadonly === '1' || dataset.cloudQuotaBlocked === '1';
  }

  function _writeBlockedMessage() {
    return document.body?.dataset?.cloudQuotaBlocked === '1'
      ? '保存容量を確認してください'
      : 'この保存先は閲覧のみです';
  }

  function _isLockedPath(path) {
    try {
      return !!(path && typeof isItemLocked === 'function' && isItemLocked(path));
    } catch {
      return false;
    }
  }

  function _canCreateIn(target) {
    return !!target?.path && !_isWriteBlocked() && !_isLockedPath(target.path);
  }

  function _canMutateItem(item) {
    return !!item?.path && !_isWriteBlocked() && !_isLockedPath(item.path);
  }

  function _activeTreeNodeData() {
    const selectors = [
      '#sidebar .tree-node-row.active',
      '#sidebar .tree-node-row.selected',
      '#sidebar .tree-node-row[data-active-workspace="true"]',
    ];
    for (const selector of selectors) {
      const row = document.querySelector(selector);
      const node = row?.closest?.('.tree-node');
      if (node?._nodeData) return { node, data: node._nodeData };
    }
    try {
      if (typeof treeSelection !== 'undefined' && treeSelection.lastClicked?._nodeData) {
        return { node: treeSelection.lastClicked, data: treeSelection.lastClicked._nodeData };
      }
    } catch {}
    const firstRoot = document.querySelector('#outliner-tree > .tree-node');
    return firstRoot?._nodeData ? { node: firstRoot, data: firstRoot._nodeData } : null;
  }

  function _openContainerInMainPanel(item, options = {}) {
    const path = item?.path || '';
    if (!path) return false;
    const type = String(item?.type || 'folder').toLowerCase();
    const name = item.name || item.label || String(path).split(/[\\/]/).pop() || 'フォルダ';
    if (options.closeDrawer !== false) window.MeldexCloudMobile?.suppressNextSidebarOpen?.();
    if (type === 'database' && typeof window.selectDatabase === 'function') {
      Promise.resolve(window.selectDatabase(path, null, { fromExplorer: true, skipHighlight: true })).catch(() => {});
    } else if (typeof window.openFolder === 'function') {
      // gb-folder.js は編集禁止のため、fromExplorer の一体化リダイレクトを
      // skipMobileExplorer で明示的にバイパスし、デスクトップ同様メインパネルへ表示する。
      Promise.resolve(window.openFolder(name, path, { fromExplorer: true, skipMobileExplorer: true })).catch(() => {});
    } else {
      return false;
    }
    if (options.closeDrawer !== false) window.MeldexCloudMobile?.closeSidebar?.();
    return true;
  }

  function selectedFolderTarget() {
    const active = _activeTreeNodeData();
    if (_treeNodeCanActAsFolder(active?.data)) return _targetFromData(active.data);
    let parent = active?.node?.parentElement?.closest?.('.tree-node') || null;
    while (parent) {
      if (_treeNodeCanActAsFolder(parent._nodeData)) return _targetFromData(parent._nodeData);
      parent = parent.parentElement?.closest?.('.tree-node') || null;
    }
    try {
      if (typeof _folderPath !== 'undefined' && _folderPath) {
        return {
          path: _folderPath,
          name: String(_folderPath).split(/[\\/]/).pop() || 'フォルダ',
        };
      }
    } catch {}
    const firstRoot = document.querySelector('#outliner-tree > .tree-node');
    return firstRoot?._nodeData?.path ? _targetFromData(firstRoot._nodeData) : null;
  }

  function currentFolderTarget() {
    return _currentFolder?.path ? { ..._currentFolder } : null;
  }

  function _findTreeNodeByPath(path) {
    const normalized = _normalizePath(path);
    if (!normalized) return null;
    return Array.from(document.querySelectorAll('#sidebar .tree-node')).find((node) => {
      return _normalizePath(node._nodeData?.path || node.dataset.path || '') === normalized;
    }) || null;
  }

  function _selectTreePath(path) {
    if (!path) return;
    try {
      if (typeof highlightOutlinerNode === 'function') highlightOutlinerNode(path, { noScroll: true });
    } catch {}
    const node = _findTreeNodeByPath(path);
    if (!node) return;
    try {
      if (typeof treeSelection !== 'undefined') {
        treeSelection.clear();
        treeSelection.add(node);
        treeSelection.lastClicked = node;
      }
    } catch {}
    document.querySelectorAll('#sidebar .tree-node-row.active').forEach(row => row.classList.remove('active'));
    node.querySelector(':scope > .tree-node-row')?.classList.add('active');
  }

  function _ensureListPane(sidebar) {
    let pane = sidebar.querySelector('#cloud-mobile-explorer-list-pane');
    if (pane) return pane;
    pane = document.createElement('section');
    pane.id = 'cloud-mobile-explorer-list-pane';
    pane.className = 'cloud-mobile-explorer-list-pane';
    pane.setAttribute('aria-label', 'フォルダ一覧');
    pane.innerHTML = [
      '<div class="cloud-mobile-explorer-list-header">',
      '  <button type="button" class="cloud-mobile-explorer-up" aria-label="上のフォルダへ"></button>',
      '  <div class="cloud-mobile-explorer-heading">',
      '    <strong class="cloud-mobile-explorer-title">フォルダ</strong>',
      '    <span class="cloud-mobile-explorer-path"></span>',
      '  </div>',
      '  <button type="button" class="cloud-mobile-explorer-refresh" aria-label="一覧を更新"></button>',
      '</div>',
      '<div class="cloud-mobile-explorer-layout-switch" role="group" aria-label="フォルダ一覧の表示形式"></div>',
      '<div class="cloud-mobile-explorer-list" role="list"></div>',
    ].join('');
    const upButton = pane.querySelector('.cloud-mobile-explorer-up');
    const refreshButton = pane.querySelector('.cloud-mobile-explorer-refresh');
    upButton.dataset.e2eId = 'cloud-mobile-explorer-up';
    refreshButton.dataset.e2eId = 'cloud-mobile-explorer-refresh';
    upButton.innerHTML = _iconHtml('chevronLeft', 20, '<');
    refreshButton.innerHTML = _iconHtml('refreshCw', 18, '↻');
    _fillLayoutSwitch(pane.querySelector('.cloud-mobile-explorer-layout-switch'));
    upButton.addEventListener('click', (event) => {
      event.preventDefault();
      event.stopPropagation();
      _openParentFolder();
    });
    refreshButton.addEventListener('click', (event) => {
      event.preventDefault();
      event.stopPropagation();
      renderCurrent({ force: true });
    });
    const treeScroll = sidebar.querySelector('#tree-scroll-container');
    if (treeScroll) treeScroll.insertAdjacentElement('afterend', pane);
    else sidebar.appendChild(pane);
    return pane;
  }

  function _fillLayoutSwitch(switcher) {
    if (!switcher || switcher.dataset.ready === '1') return;
    switcher.dataset.ready = '1';
    LAYOUT_ITEMS.forEach(([label, key, icon]) => {
      const button = document.createElement('button');
      button.type = 'button';
      button.className = 'cloud-mobile-explorer-layout-button';
      button.dataset.layout = key;
      button.dataset.e2eId = `cloud-mobile-explorer-layout-${key}`;
      button.setAttribute('aria-label', label + '表示');
      button.innerHTML = `<span aria-hidden="true">${_iconHtml(icon, 17, '')}</span><strong></strong>`;
      button.querySelector('strong').textContent = label;
      button.addEventListener('click', (event) => {
        event.preventDefault();
        event.stopPropagation();
        setLayout(key);
      });
      switcher.appendChild(button);
    });
    _syncLayoutButtons();
  }

  function _ensureFab(sidebar) {
    let button = sidebar.querySelector('#cloud-mobile-explorer-create-button');
    if (button) return button;
    button = document.createElement('button');
    button.id = 'cloud-mobile-explorer-create-button';
    button.type = 'button';
    button.className = 'cloud-mobile-explorer-fab';
    button.title = '新規作成';
    button.setAttribute('aria-label', '新規作成');
    button.innerHTML = _iconHtml('plus', 24, '+');
    button.addEventListener('click', (event) => {
      event.preventDefault();
      event.stopPropagation();
      openCreateSheet(button);
    });
    sidebar.appendChild(button);
    return button;
  }

  function _syncCreateButtonState() {
    const sidebar = _sidebar();
    const button = sidebar?.querySelector('#cloud-mobile-explorer-create-button');
    if (!button) return;
    const target = _createTarget();
    const blocked = _isWriteBlocked();
    const disabled = blocked || !target?.path || _isLockedPath(target?.path);
    button.hidden = blocked;
    button.disabled = disabled;
    button.setAttribute('aria-disabled', disabled ? 'true' : 'false');
    button.title = blocked
      ? _writeBlockedMessage()
      : disabled
        ? '作成先フォルダを選択してください'
        : '新規作成';
  }

  function ensure() {
    if (!_isEnabled()) return false;
    const sidebar = _sidebar();
    if (!sidebar) return false;
    sidebar.dataset.cloudMobileExplorer = '1';
    _ensureListPane(sidebar);
    _ensureFab(sidebar);
    _installTreeClickBridge();
    _syncCreateButtonState();
    return true;
  }

  function _syncLayoutButtons() {
    document.querySelectorAll('#sidebar .cloud-mobile-explorer-layout-button').forEach((button) => {
      button.setAttribute('aria-pressed', button.dataset.layout === _layout ? 'true' : 'false');
    });
    const select = document.getElementById('folder-layout-select');
    if (select) select.value = _layout;
  }

  function syncLayoutFromFolder(mode) {
    _layout = _normalizeLayout(mode);
    _syncLayoutButtons();
    const sidebar = _sidebar();
    const list = sidebar?.querySelector('#cloud-mobile-explorer-list-pane .cloud-mobile-explorer-list');
    if (list) list.dataset.layout = _layout;
    if (_mode === 'list' && _currentItems.length) {
      const pane = sidebar?.querySelector('#cloud-mobile-explorer-list-pane');
      if (pane) _renderItems(pane, _currentItems);
    }
  }

  function _syncFolderPanelLayoutState() {
    try {
      if (typeof _folderLayout !== 'undefined') _folderLayout = _layout;
    } catch {}
    const select = document.getElementById('folder-layout-select');
    if (select) select.value = _layout;
  }

  function setLayout(mode, options = {}) {
    _layout = _normalizeLayout(mode);
    try { localStorage.setItem(LAYOUT_STORAGE_KEY, _layout); } catch {}
    _syncLayoutButtons();
    if (options.syncFolderPanel !== false) _syncFolderPanelLayoutState();
    const sidebar = _sidebar();
    const pane = sidebar?.querySelector('#cloud-mobile-explorer-list-pane');
    if (pane && _mode === 'list') {
      if (_currentItems.length) _renderItems(pane, _currentItems);
      else renderCurrent({ force: false });
    }
    return true;
  }

  function setMode(mode, options = {}) {
    // フォルダツリー/フォルダ一覧の一体化UIは廃止済み。'list' 指定は引き続き
    // 受け付けるが常に 'tree' へ強制する（ドロワーはツリー専用）。
    // 呼び出し口互換のため引数・関数自体は残す（gb-folder.js の
    // _openFolderInMobileExplorer など編集禁止の外部コードが呼び出す）。
    if (!ensure()) return false;
    void mode;
    _mode = 'tree';
    try { localStorage.setItem(MODE_STORAGE_KEY, _mode); } catch {}
    const sidebar = _sidebar();
    if (sidebar) sidebar.dataset.cloudMobileExplorerMode = _mode;
    return true;
  }

  async function _fetchFolderItems(path, options = {}) {
    if (typeof apiFetch !== 'function') throw new Error('フォルダ一覧を取得できません');
    const fetchOptions = options.force === true ? { skipBrowseCache: true, cache: 'reload' } : undefined;
    let items = await apiFetch('/browse?path=' + encodeURIComponent(path) + '&detail=true&all_files=true', fetchOptions);
    if (!Array.isArray(items)) items = Array.isArray(items?.items) ? items.items : [];
    if (typeof isOutlinerDeletePendingPath === 'function') {
      items = items.filter(item => !isOutlinerDeletePendingPath(item?.path));
    }
    return items;
  }

  function _sortItemsForMobile(items) {
    return [...items].sort((a, b) => {
      const af = String(a?.type || '').toLowerCase() === 'folder' ? 0 : 1;
      const bf = String(b?.type || '').toLowerCase() === 'folder' ? 0 : 1;
      if (af !== bf) return af - bf;
      return String(a?.name || '').localeCompare(String(b?.name || ''), 'ja');
    });
  }

  function _itemIcon(item) {
    const type = String(item?.type || '').toLowerCase();
    if (type === 'folder') return 'folder';
    if (type === 'database') return 'table2';
    if (type === 'smart-db') return 'database';
    if (type === 'page') return 'fileText';
    if (type === 'scriptnote' || type === 'scenario') return 'bookOpenText';
    if (type === 'board') return 'presentation';
    if (type === 'calendar') return 'calendarDays';
    if (type === 'image') return 'image';
    if (type === 'video') return 'film';
    if (type === 'audio') return 'volume2';
    if (type === 'csv') return 'table';
    if (type === 'html') return 'code2';
    return 'file';
  }

  function _itemTypeLabel(item) {
    const type = String(item?.type || '').toLowerCase();
    if (typeof FILE_TYPE_LABELS !== 'undefined' && FILE_TYPE_LABELS[type]) return FILE_TYPE_LABELS[type];
    const labels = {
      folder: 'フォルダ',
      database: 'シート',
      'smart-db': 'スマートシート',
      page: 'ノート',
      scriptnote: 'シナリオ',
      scenario: 'シナリオ',
      board: 'ボード',
      calendar: 'カレンダー',
      image: '画像',
      video: '動画',
      audio: '音声',
      csv: 'CSV',
      html: 'HTML',
    };
    return labels[type] || 'ファイル';
  }

  function _setListState(pane, state, message) {
    const list = pane.querySelector('.cloud-mobile-explorer-list');
    if (!list) return;
    list.dataset.layout = _layout;
    list.dataset.state = state;
    list.replaceChildren();
    const empty = document.createElement('div');
    empty.className = 'cloud-mobile-explorer-state';
    empty.textContent = message;
    list.appendChild(empty);
  }

  function _syncListHeader(pane, target, count) {
    pane.querySelector('.cloud-mobile-explorer-title').textContent = _displayName(target);
    const suffix = Number.isFinite(count) ? `・${count}項目` : '';
    pane.querySelector('.cloud-mobile-explorer-path').textContent = _pathHint(target?.path || '') + suffix;
    const up = pane.querySelector('.cloud-mobile-explorer-up');
    if (up) up.disabled = !_parentTargetForCurrent();
  }

  async function renderCurrent(options = {}) {
    // 一覧モードのフォルダパネル一体化UIは廃止済み。呼び出し口が残っていても
    // 何もしない（ファイル自体は互換のため残置）。
    if (_mode !== 'list') return false;
    if (!ensure()) return false;
    const sidebar = _sidebar();
    const pane = sidebar?.querySelector('#cloud-mobile-explorer-list-pane');
    if (!pane) return false;
    if (!_currentFolder?.path || options.force) {
      const target = selectedFolderTarget();
      if (target?.path) _currentFolder = target;
    }
    _syncCreateButtonState();
    if (!_currentFolder?.path) {
      _currentItems = [];
      _syncListHeader(pane, { name: 'フォルダ', path: '' }, 0);
      _setListState(pane, 'empty', '表示するフォルダを選択してください');
      return false;
    }
    const seq = ++_listSeq;
    _syncListHeader(pane, _currentFolder, NaN);
    _setListState(pane, 'loading', '読み込み中...');
    try {
      const items = _sortItemsForMobile(await _fetchFolderItems(_currentFolder.path, { force: options.force === true }));
      if (seq !== _listSeq) return false;
      _currentItems = items;
      _syncListHeader(pane, _currentFolder, items.length);
      _renderItems(pane, items);
      return true;
    } catch (error) {
      if (seq !== _listSeq) return false;
      _currentItems = [];
      _setListState(pane, 'error', '読み込みに失敗しました');
      if (typeof showStatus === 'function') showStatus((error && error.message) || 'フォルダ一覧の読み込みに失敗しました', true);
      return false;
    }
  }

  function _renderItems(pane, items) {
    const list = pane.querySelector('.cloud-mobile-explorer-list');
    list.replaceChildren();
    list.dataset.layout = _layout;
    list.dataset.state = items.length ? 'ready' : 'empty';
    if (!items.length) {
      const empty = document.createElement('div');
      empty.className = 'cloud-mobile-explorer-empty';
      const title = document.createElement('strong');
      title.textContent = 'このフォルダは空です';
      const detail = document.createElement('span');
      detail.textContent = _isWriteBlocked()
        ? _writeBlockedMessage()
        : '右下の「＋」から作成できます。';
      empty.append(title, detail);
      list.appendChild(empty);
      return;
    }
    const fragment = document.createDocumentFragment();
    items.forEach(item => fragment.appendChild(_itemRow(item)));
    list.appendChild(fragment);
  }

  function _fillItemVisual(el, item) {
    if (!el) return;
    const type = String(item?.type || '').toLowerCase();
    const canThumb = (type === 'image' || type === 'video') && item?.path && _layout !== 'list';
    if (!canThumb) {
      el.innerHTML = _iconHtml(_itemIcon(item), _layout === 'list' ? 20 : 28, '');
      return;
    }
    const img = document.createElement('img');
    const generatedThumbnail = type === 'video';
    const rawUrl = typeof _folderItemRawUrl === 'function'
      ? _folderItemRawUrl(item)
      : '/api/file-raw?path=' + encodeURIComponent(item.path);
    const thumbnailUrl = typeof _folderItemThumbnailUrl === 'function'
      ? _folderItemThumbnailUrl(item, 384)
      : '/api/thumbnail?path=' + encodeURIComponent(item.path) + '&size=384';
    img.loading = 'lazy';
    img.alt = '';
    img.src = generatedThumbnail ? thumbnailUrl : rawUrl;
    img.onerror = () => {
      if (!generatedThumbnail && !img.dataset.thumbFallback) {
        img.dataset.thumbFallback = '1';
        img.src = thumbnailUrl;
        return;
      }
      const fallback = document.createElement('span');
      fallback.className = 'cloud-mobile-explorer-row-fallback-icon';
      fallback.innerHTML = _iconHtml(_itemIcon(item), 28, '');
      img.replaceWith(fallback);
    };
    el.replaceChildren(img);
  }

  function _itemRow(item) {
    if (typeof _applyCachedBrowseItemType === 'function') _applyCachedBrowseItemType(item);
    const type = String(item?.type || '').toLowerCase();
    const isFolder = type === 'folder';
    const itemE2EId = _e2eIdPart(item?.path || item?.name || item?.label);
    const row = document.createElement('div');
    row.className = 'cloud-mobile-explorer-row';
    row.dataset.itemType = type;
    row.setAttribute('role', 'listitem');
    const openButton = document.createElement('button');
    openButton.type = 'button';
    openButton.className = 'cloud-mobile-explorer-row-open';
    openButton.dataset.e2eId = `cloud-mobile-explorer-open-${itemE2EId}`;
    openButton.setAttribute('aria-label', (item?.name || item?.label || '無題') + 'を開く');
    openButton.innerHTML = [
      '<span class="cloud-mobile-explorer-row-icon" aria-hidden="true"></span>',
      '<span class="cloud-mobile-explorer-row-main">',
      '  <span class="cloud-mobile-explorer-row-title"></span>',
      '  <span class="cloud-mobile-explorer-row-meta"></span>',
      '</span>',
    ].join('');
    _fillItemVisual(openButton.querySelector('.cloud-mobile-explorer-row-icon'), item);
    openButton.querySelector('.cloud-mobile-explorer-row-title').textContent = item?.name || item?.label || '無題';
    openButton.querySelector('.cloud-mobile-explorer-row-meta').textContent = _itemTypeLabel(item);
    openButton.addEventListener('click', async (event) => {
      event.preventDefault();
      event.stopPropagation();
      try {
        if (isFolder) await _openFolderInExplorer(item);
        else await _openFileFromExplorer(item);
      } catch (error) {
        if (typeof showStatus === 'function') showStatus(error?.message || '項目を開けませんでした', true);
      }
    });
    row.appendChild(openButton);
    const action = document.createElement('button');
    action.type = 'button';
    action.className = 'cloud-mobile-explorer-row-menu';
    if (_canMutateItem(item)) {
      action.dataset.e2eId = `cloud-mobile-explorer-menu-${itemE2EId}`;
      action.setAttribute('aria-label', (item?.name || item?.label || '無題') + 'の操作');
      action.innerHTML = _iconHtml('moreHorizontal', 20, '…');
      action.addEventListener('click', (event) => {
        event.preventDefault();
        event.stopPropagation();
        _openItemMenu(item);
      });
    } else {
      action.dataset.e2eId = `cloud-mobile-explorer-placeholder-${itemE2EId}`;
      action.classList.add('cloud-mobile-explorer-row-menu-placeholder');
      action.setAttribute('aria-hidden', 'true');
      action.tabIndex = -1;
      action.innerHTML = isFolder ? _iconHtml('chevronRight', 18, '>') : '';
    }
    row.appendChild(action);
    return row;
  }

  function _mobileTypeResolutionPaneSnapshot() {
    return typeof _captureBrowseItemPaneSnapshot === 'function'
      ? _captureBrowseItemPaneSnapshot('', { requirePath: false })
      : null;
  }

  async function handleResolvedItemType(payload) {
    const item = payload?.item;
    const type = String(payload?.type || item?.type || '').toLowerCase();
    if (!item?.path || !['database', 'calendar'].includes(type)) return true;
    let opened = false;
    if (typeof _openResolvedBrowseItemType === 'function') {
      opened = await _openResolvedBrowseItemType(item, type, { paneSnapshot: payload?.paneSnapshot || null });
    } else if (type === 'database' && typeof selectDatabase === 'function') {
      await selectDatabase(item.path, payload?.paneSnapshot?.paneContext || null, { fromExplorer: true, skipHighlight: true });
      opened = true;
    } else if (type === 'calendar' && typeof openCalendarFile === 'function') {
      await openCalendarFile(item.name, item.path, {
        fromExplorer: true,
        skipHighlight: true,
        paneContext: payload?.paneSnapshot?.paneContext || null,
      });
      opened = true;
    }
    if (opened) _scheduleSidebarClose(120);
    return true;
  }

  function _scheduleMobileFolderTypeResolution(item, visiblePromise, node) {
    if (typeof _scheduleBrowseItemTypeResolution !== 'function'
        || typeof _browseItemNeedsTypeCheck !== 'function'
        || !_browseItemNeedsTypeCheck(item)) return Promise.resolve({ skipped: 'not-needed' });
    const targetPath = _normalizePath(item.path);
    return _scheduleBrowseItemTypeResolution(node || null, item, visiblePromise, {
      paneSnapshot: _mobileTypeResolutionPaneSnapshot(),
      isStillActive: () => _normalizePath(_currentFolder?.path) === targetPath,
      onResolved: handleResolvedItemType,
    });
  }

  async function _openFolderInExplorer(item) {
    if (!item?.path) return false;
    _currentFolder = { path: item.path, name: item.name || item.label || 'フォルダ' };
    _selectTreePath(item.path);
    const visiblePromise = renderCurrent({ force: false });
    _scheduleMobileFolderTypeResolution(item, visiblePromise, _findTreeNodeByPath(item.path));
    return visiblePromise;
  }

  async function _openFileFromExplorer(item) {
    if (!item?.path) return false;
    if (typeof openFolderItem === 'function') await openFolderItem(item);
    else if (typeof openNative === 'function') openNative(item.path);
    _scheduleSidebarClose(120);
    return true;
  }

  function _scheduleSidebarClose(delay) {
    const generation = window.MeldexCloudMobile?.getSidebarOpenGeneration?.();
    setTimeout(() => window.MeldexCloudMobile?.closeSidebar?.({ openGeneration: generation }), delay);
  }

  function _parentTargetForCurrent() {
    if (!_currentFolder?.path) return null;
    const node = _findTreeNodeByPath(_currentFolder.path);
    const parent = node?.parentElement?.closest?.('.tree-node') || null;
    if (parent?._nodeData?.path) return _targetFromData(parent._nodeData);
    const raw = String(_currentFolder.path).replace(/\\/g, '/').replace(/\/+$/, '');
    const idx = raw.lastIndexOf('/');
    if (idx <= 0) return null;
    const parentPath = raw.slice(0, idx);
    return { path: parentPath, name: parentPath.split('/').pop() || 'フォルダ' };
  }

  function _openParentFolder() {
    const parent = _parentTargetForCurrent();
    if (!parent?.path) return false;
    _currentFolder = parent;
    _selectTreePath(parent.path);
    renderCurrent({ force: false });
    return true;
  }

  function selectFolderFromTree(item, options = {}) {
    if (!item?.path) return false;
    _currentFolder = {
      path: item.path,
      name: item.name || item.label || 'フォルダ',
    };
    if (options.syncSelection !== false) _selectTreePath(item.path);
    _syncCreateButtonState();
    // フォルダツリーとフォルダパネルの一体化は廃止。フォルダ/シート行の選択は
    // 常にデスクトップ同様メインパネルで開く（openPanel:false の場合のみ抑制）。
    if (options.openPanel !== false) {
      _openContainerInMainPanel(item, { closeDrawer: options.closeDrawer });
    }
    return true;
  }

  function _createTarget() {
    if (_mode === 'list' && _currentFolder?.path) return _currentFolder;
    return selectedFolderTarget();
  }

  async function _refreshAfterMutation() {
    const jobs = [];
    if (typeof loadOutliner === 'function') jobs.push(Promise.resolve().then(() => loadOutliner()));
    if (typeof renderHomeFolderTree === 'function') jobs.push(Promise.resolve().then(() => renderHomeFolderTree()));
    if (typeof renderWorkspaceSidebar === 'function') jobs.push(Promise.resolve().then(() => renderWorkspaceSidebar()));
    await Promise.allSettled(jobs);
    if (_mode === 'list') await renderCurrent({ force: true });
    else _syncCreateButtonState();
  }

  async function _renameItem(item, newName) {
    const oldPath = item?.path || '';
    const oldName = item?.name || item?.label || oldPath.split(/[\\/]/).pop() || '無題';
    const name = String(newName || '').trim();
    if (!oldPath || !name || name === oldName) return;
    const type = String(item?.type || 'page').toLowerCase();
    const res = await apiPost('/outliner/rename', { old_path: oldPath, new_name: name, type });
    if (!res?.new_path) throw new Error('名前の変更に失敗しました');
    if (typeof _renameTreeNode === 'function') _renameTreeNode(oldPath, res.new_path, res.new_name || name, res.file_id);
    if (typeof renameAppPathReferences === 'function') {
      renameAppPathReferences(oldPath, res.new_path, { label: res.new_name || name, fileId: res.file_id, type });
    }
    if (_currentFolder?.path === oldPath) {
      _currentFolder = { path: res.new_path, name: res.new_name || name };
    }
    await _refreshAfterMutation();
    if (typeof showStatus === 'function') showStatus('名前を変更しました: ' + (res.new_name || name));
  }

  async function _deleteItem(item) {
    const path = item?.path || '';
    if (!path) return;
    const name = item?.name || item?.label || path.split(/[\\/]/).pop() || 'この項目';
    const confirmMessage = '「' + name + '」を削除しますか？';
    const impactTarget = [{ path, kind: item?.type === 'folder' ? 'folder' : 'file' }];
    let confirmed;
    if (typeof MeldexDeleteImpactWarning !== 'undefined' && typeof cfConfirm === 'function') {
      confirmed = await MeldexDeleteImpactWarning.confirmDeleteWithImpact(impactTarget, confirmMessage);
    } else if (typeof cfConfirm === 'function') {
      confirmed = await cfConfirm(confirmMessage);
    } else {
      confirmed = window.confirm(confirmMessage);
    }
    if (!confirmed) return;
    const currentKey = _normalizePath(_currentFolder?.path || '');
    const deleteKey = _normalizePath(path);
    if (currentKey && (currentKey === deleteKey || currentKey.startsWith(deleteKey + '/'))) {
      _currentFolder = _parentTargetForCurrent() || _currentFolder;
    }
    if (typeof deleteOutlinerItemsWithHistory === 'function') {
      const result = await deleteOutlinerItemsWithHistory([{
        path,
        name,
        type: item?.type || 'page',
      }], {
        confirmation: confirmed,
        label: '削除: ' + name,
        refresh: _refreshAfterMutation,
      });
      await _refreshAfterMutation();
      if (result?.failedCount > 0 && typeof showStatus === 'function') showStatus('削除に失敗しました', true);
      return;
    }
    await apiPost('/outliner/delete', {
      path,
      kind: impactTarget[0].kind,
      ...(window.MeldexDeleteImpactWarning?.confirmationPayload?.(confirmed) || {}),
    });
    await _refreshAfterMutation();
    if (typeof showStatus === 'function') showStatus('削除しました: ' + name);
  }

  function closeItemMenu() {
    if (_itemMenuOverlay?.parentNode) _itemMenuOverlay.remove();
    _itemMenuOverlay = null;
  }

  function _makeSheetButton(label, icon, className, handler) {
    const button = document.createElement('button');
    button.type = 'button';
    button.className = className || 'cloud-mobile-explorer-action-item';
    const iconEl = document.createElement('span');
    iconEl.setAttribute('aria-hidden', 'true');
    iconEl.innerHTML = _iconHtml(icon, 19, '');
    const labelEl = document.createElement('strong');
    labelEl.textContent = label;
    button.append(iconEl, labelEl);
    button.addEventListener('click', handler);
    return button;
  }

  function _openItemMenu(item) {
    if (!_canMutateItem(item)) return;
    closeItemMenu();
    const name = item?.name || item?.label || '無題';
    const overlay = document.createElement('div');
    overlay.className = 'cloud-mobile-explorer-item-overlay';
    overlay.setAttribute('role', 'presentation');
    const sheet = document.createElement('div');
    sheet.className = 'cloud-mobile-explorer-action-sheet';
    sheet.setAttribute('role', 'dialog');
    sheet.setAttribute('aria-modal', 'true');
    sheet.setAttribute('aria-label', name + 'の操作');
    const header = document.createElement('div');
    header.className = 'cloud-mobile-explorer-action-header';
    const title = document.createElement('strong');
    title.textContent = name;
    const meta = document.createElement('span');
    meta.textContent = _itemTypeLabel(item);
    header.append(title, meta);
    sheet.appendChild(header);
    sheet.appendChild(_makeSheetButton('開く', String(item?.type || '').toLowerCase() === 'folder' ? 'folderOpen' : 'externalLink', 'cloud-mobile-explorer-action-item', () => {
      closeItemMenu();
      if (String(item?.type || '').toLowerCase() === 'folder') _openFolderInExplorer(item);
      else _openFileFromExplorer(item);
    }));
    sheet.appendChild(_makeSheetButton('名前を変更', 'pencil', 'cloud-mobile-explorer-action-item', () => {
      closeItemMenu();
      _openRenameSheet(item);
    }));
    sheet.appendChild(_makeSheetButton('削除', 'trash2', 'cloud-mobile-explorer-action-item cloud-mobile-explorer-action-danger', async () => {
      closeItemMenu();
      try {
        await _deleteItem(item);
      } catch (error) {
        if (typeof showStatus === 'function') showStatus(error?.message || '削除に失敗しました', true);
      }
    }));
    sheet.appendChild(_makeSheetButton('キャンセル', 'x', 'cloud-mobile-explorer-action-item', closeItemMenu));
    overlay.appendChild(sheet);
    overlay.addEventListener('click', (event) => {
      if (event.target === overlay) closeItemMenu();
    });
    document.body.appendChild(overlay);
    _itemMenuOverlay = overlay;
    requestAnimationFrame(() => sheet.querySelector('button')?.focus?.({ preventScroll: true }));
  }

  function _openRenameSheet(item) {
    closeItemMenu();
    const oldName = item?.name || item?.label || '無題';
    const overlay = document.createElement('div');
    overlay.className = 'cloud-mobile-explorer-item-overlay';
    overlay.setAttribute('role', 'presentation');
    const sheet = document.createElement('form');
    sheet.className = 'cloud-mobile-explorer-action-sheet cloud-mobile-explorer-rename-sheet';
    sheet.setAttribute('role', 'dialog');
    sheet.setAttribute('aria-modal', 'true');
    sheet.setAttribute('aria-label', '名前を変更');
    const header = document.createElement('div');
    header.className = 'cloud-mobile-explorer-action-header';
    const title = document.createElement('strong');
    title.textContent = '名前を変更';
    const meta = document.createElement('span');
    meta.textContent = oldName;
    header.append(title, meta);
    const input = document.createElement('input');
    input.type = 'text';
    input.value = oldName;
    input.autocomplete = 'off';
    input.className = 'cloud-mobile-explorer-rename-input';
    const actions = document.createElement('div');
    actions.className = 'cloud-mobile-explorer-rename-actions';
    const cancel = document.createElement('button');
    cancel.type = 'button';
    cancel.textContent = 'キャンセル';
    const save = document.createElement('button');
    save.type = 'submit';
    save.textContent = '変更';
    save.className = 'primary';
    actions.append(cancel, save);
    sheet.append(header, input, actions);
    cancel.addEventListener('click', closeItemMenu);
    sheet.addEventListener('submit', async (event) => {
      event.preventDefault();
      save.disabled = true;
      try {
        await _renameItem(item, input.value);
        closeItemMenu();
      } catch (error) {
        save.disabled = false;
        if (typeof showStatus === 'function') showStatus(error?.message || '名前の変更に失敗しました', true);
      }
    });
    overlay.appendChild(sheet);
    overlay.addEventListener('click', (event) => {
      if (event.target === overlay) closeItemMenu();
    });
    document.body.appendChild(overlay);
    _itemMenuOverlay = overlay;
    setTimeout(() => {
      input.focus({ preventScroll: true });
      input.select();
    }, 0);
  }

  function openCreateSheet(anchor) {
    if (!ensure()) return false;
    const target = _createTarget();
    if (!target?.path) {
      if (typeof showStatus === 'function') showStatus('作成先フォルダを選択してください', true);
      return true;
    }
    if (!_canCreateIn(target)) {
      if (typeof showStatus === 'function') showStatus(_writeBlockedMessage(), true);
      return true;
    }
    closeCreateSheet();
    const overlay = document.createElement('div');
    overlay.className = 'cloud-mobile-explorer-create-overlay';
    overlay.setAttribute('role', 'presentation');
    const sheet = document.createElement('div');
    sheet.className = 'cloud-mobile-explorer-create-sheet';
    sheet.setAttribute('role', 'dialog');
    sheet.setAttribute('aria-modal', 'true');
    sheet.setAttribute('aria-label', '新規作成');
    const header = document.createElement('div');
    header.className = 'cloud-mobile-explorer-create-header';
    header.innerHTML = [
      '<div>',
      '  <strong>新規作成</strong>',
      '  <span></span>',
      '</div>',
      '<button type="button" aria-label="閉じる"></button>',
    ].join('');
    header.querySelector('span').textContent = '作成先: ' + _displayName(target);
    header.querySelector('button').innerHTML = _iconHtml('x', 20, '×');
    header.querySelector('button').addEventListener('click', closeCreateSheet);
    sheet.appendChild(header);
    const grid = document.createElement('div');
    grid.className = 'cloud-mobile-explorer-create-grid';
    const items = window.MeldexCloudBootstrap?.filterPhase1CreateItems?.(CREATE_ITEMS) || CREATE_ITEMS;
    items.forEach(([label, type, icon]) => {
      const button = document.createElement('button');
      button.type = 'button';
      button.className = 'cloud-mobile-explorer-create-option';
      button.innerHTML = `<span aria-hidden="true">${_iconHtml(icon, 20, '')}</span><strong></strong>`;
      button.querySelector('strong').textContent = label;
      button.addEventListener('click', async () => {
        closeCreateSheet();
        if (typeof addItemAt === 'function') {
          await addItemAt(target.path, type);
          if (type === 'folder') {
            // addItemAt() はフォルダ作成時は自動で開かないため、作成直後に
            // ツリーで選択状態になった新規フォルダをメインパネルへ明示的に開く。
            const created = selectedFolderTarget();
            if (created?.path && created.path !== target.path) {
              _openContainerInMainPanel(created, { closeDrawer: true });
            } else {
              _scheduleSidebarClose(80);
            }
          } else {
            _scheduleSidebarClose(80);
          }
        }
      });
      grid.appendChild(button);
    });
    sheet.appendChild(grid);
    overlay.appendChild(sheet);
    overlay.addEventListener('click', (event) => {
      if (event.target === overlay) closeCreateSheet();
    });
    document.body.appendChild(overlay);
    _createOverlay = overlay;
    const first = sheet.querySelector('button.cloud-mobile-explorer-create-option');
    setTimeout(() => first?.focus?.({ preventScroll: true }), 0);
    return true;
  }

  function closeCreateSheet() {
    if (_createOverlay?.parentNode) _createOverlay.remove();
    _createOverlay = null;
  }

  function _installTreeClickBridge() {
    if (_treeClickBridgeInstalled) return;
    _treeClickBridgeInstalled = true;
    const handleTreeClick = (event) => {
      const sidebar = _sidebar();
      if (!sidebar?.classList?.contains('cloud-mobile-tree-screen-open')) return;
      if (_mode !== 'tree') return;
      if (_isTreeRowControlTarget(event.target)) return;
      const row = event.target?.closest?.('.tree-node-row');
      const node = row?.closest?.('.tree-node') || null;
      if (!_treeNodeCanActAsFolder(node?._nodeData)) return;
      event.preventDefault();
      event.stopPropagation();
      selectFolderFromTree(node._nodeData);
      const toggle = row.querySelector('.tree-toggle');
      if (toggle && toggle.dataset.expanded !== 'true') toggle.click();
      _scheduleMobileFolderTypeResolution(node._nodeData, Promise.resolve(true), node);
    };
    document.addEventListener('click', handleTreeClick, true);
    document.addEventListener('keydown', (event) => {
      if (event.key === 'Escape') {
        closeCreateSheet();
        closeItemMenu();
      }
    });
  }

  function _bind() {
    // 一体化UI廃止に伴い、過去に保存された 'list' モードがあっても常にツリーで開く。
    _mode = 'tree';
    try { localStorage.setItem(MODE_STORAGE_KEY, _mode); } catch {}
    _layout = _normalizeLayout(_readLayoutStorage());
    ensure();
  }

  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', _bind, { once: true });
  else _bind();

  window.MeldexCloudMobileExplorer = {
    ensure,
    setMode,
    getMode: () => _mode,
    setLayout,
    getLayout: () => _layout,
    syncLayoutFromFolder,
    renderCurrent,
    selectFolderFromTree,
    openCreateSheet,
    closeCreateSheet,
    closeItemMenu,
    selectedFolderTarget,
    currentFolderTarget,
    handleResolvedItemType,
  };
})();

;
