---
type: settings-db
property_types:
  ふりがな:
    type: text
  一人称:
    type: text
  生年月日_西暦:
    type: date
  基準年月日_西暦:
    type: date
  年齢:
    type: formula
    formula: if(or(empty(prop("生年月日_西暦")), empty(prop("基準年月日_西暦"))), "", year(prop("基準年月日_西暦"))
      - year(prop("生年月日_西暦")) - if(or(month(prop("基準年月日_西暦")) < month(prop("生年月日_西暦")),
      and(month(prop("基準年月日_西暦")) == month(prop("生年月日_西暦")), day(prop("基準年月日_西暦"))
      < day(prop("生年月日_西暦")))), 1, 0))
  性別:
    type: select
    options:
    - 男
    - 女
    - その他
  身長cm:
    type: number
  体型:
    type: select
    options:
    - 痩せ型
    - 細身
    - 標準
    - がっしり
    - 筋肉質
    - ぽっちゃり
  カップ:
    type: select
    options:
    - 未設定
    - なし
    - A
    - B
    - C
    - D
    - E
    - F
    - G
    - H
  体重kg:
    type: formula
    formula: if(empty(prop("身長cm")), "", round(prop("身長cm") * prop("身長cm") * if(prop("体型")
      == "痩せ型", 18.5, if(prop("体型") == "細身", 19.5, if(prop("体型") == "標準", 21.5, if(prop("体型")
      == "がっしり", 23.5, if(prop("体型") == "筋肉質", 25, 22))))) / 10000 * 10) / 10)
  アンダーcm:
    type: formula
    formula: if(empty(prop("身長cm")), "", round(prop("身長cm") * if(prop("性別") == "男",
      0.53, 0.43) * if(prop("体型") == "痩せ型", 0.94, if(prop("体型") == "細身", 0.97, if(prop("体型")
      == "標準", 1, if(prop("体型") == "がっしり", 1.06, if(prop("体型") == "筋肉質", 1.08, 1.1)))))
      * 10) / 10)
  バストcm:
    type: formula
    formula: if(empty(prop("身長cm")), "", round((prop("身長cm") * if(prop("性別") == "男",
      0.53, 0.43) * if(prop("体型") == "痩せ型", 0.94, if(prop("体型") == "細身", 0.97, if(prop("体型")
      == "標準", 1, if(prop("体型") == "がっしり", 1.06, if(prop("体型") == "筋肉質", 1.08, 1.1)))))
      + if(prop("カップ") == "A", 10, if(prop("カップ") == "B", 12.5, if(prop("カップ") ==
      "C", 15, if(prop("カップ") == "D", 17.5, if(prop("カップ") == "E", 20, if(prop("カップ")
      == "F", 22.5, if(prop("カップ") == "G", 25, if(prop("カップ") == "H", 27.5, 0)))))))))
      * 10) / 10)
  ウエストcm:
    type: formula
    formula: if(empty(prop("身長cm")), "", round(prop("身長cm") * if(prop("性別") == "男",
      0.44, 0.37) * if(prop("体型") == "痩せ型", 0.96, if(prop("体型") == "細身", 0.98, if(prop("体型")
      == "標準", 1, if(prop("体型") == "がっしり", 1.08, if(prop("体型") == "筋肉質", 1.12, 1.15)))))
      * 10) / 10)
  ヒップcm:
    type: formula
    formula: if(empty(prop("身長cm")), "", round(prop("身長cm") * if(prop("性別") == "男",
      0.51, 0.54) * if(prop("体型") == "痩せ型", 0.96, if(prop("体型") == "細身", 0.98, if(prop("体型")
      == "標準", 1, if(prop("体型") == "がっしり", 1.06, if(prop("体型") == "筋肉質", 1.08, 1.1)))))
      * 10) / 10)
  髪の色:
    type: text
  髪型:
    type: text
  瞳の色:
    type: text
  肌の色:
    type: text
  外見:
    type: long-text
  特徴的な傷・装飾:
    type: long-text
  くせ・しぐさ:
    type: long-text
  固有の小道具:
    type: long-text
  性格:
    type: long-text
  性格核:
    type: long-text
  矛盾:
    type: long-text
  嘘:
    type: long-text
  WANT:
    type: long-text
  NEED:
    type: long-text
  弱点:
    type: long-text
  恐れ:
    type: long-text
  口調・口癖:
    type: long-text
  語彙レベル:
    type: select
    options:
    - 低い
    - 普通
    - 高い
    - 専門的
    - 古風
  他キャラへの呼称:
    type: long-text
  職業:
    type: text
  正体:
    type: long-text
  役割:
    type: text
  キャラアークタイプ:
    type: select
    options:
    - 正の変化
    - 負の変化
    - フラット
    - 負の不変
    - 幻滅
  現在状態:
    type: select
    options:
    - 生存
    - 憑依中
    - 死亡直後
    - 死霊
    - 怨霊
    - 不明
  秘密:
    type: long-text
  能力:
    type: long-text
  物語的機能:
    type: long-text
  動機:
    type: long-text
  概要:
    type: long-text
  登場話:
    type: number
  初登場:
    type: text
  特徴:
    type: long-text
  人間関係:
    type: long-text
  過去の因縁:
    type: long-text
  備考:
    type: long-text
modified: '2026-05-07T16:52:02'
modified_by: anonymous
modified_by_kind: human
modified_by_model: gpt-5
---

# キャラ表

死霊探偵の主要キャラクターを、推理上必要な属性・秘密・関係性に絞って管理するシートです。キャラテンプレート由来の身体数式列は残し、未使用の外部リレーション列や本企画に不要な名前派生列は削除済みです。
