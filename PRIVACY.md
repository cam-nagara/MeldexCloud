# Meldex（メルデックス） プライバシーポリシー

最終更新日: 2026-08-29

配布者: cam-nagara / Meldex 開発者  
公式情報: https://cam-nagara.github.io/MeldexCloud/
問い合わせ: Meldex 内のフィードバック、または Meldex BETA 配布ページの送信フォーム

## 作品・データの帰属

ユーザーが Meldex で作成・編集した作品・データはユーザー自身に帰属します。Meldex 開発者は、ユーザーの作品・データに対して所有権を主張しません。

## 保存場所

作品・データは原則としてユーザーのローカル PC 上、またはユーザー自身が指定したクラウドストレージ（Dropbox など）に保存されます。Meldex 開発者のサーバには保存されません。

## API キー・管理者鍵の保管

デスクトップ版の LLM API キーは、OS の認証情報ストアを使って保存します。Cloud / モバイル版で暗号化APIキーCloud保存を使う場合、API キーはユーザーのパスフレーズから PBKDF2-SHA256 で導出した鍵で AES-GCM 暗号化され、ユーザー自身の Dropbox 内に保存されます。パスフレーズは保存されません。

ブラウザ内に一時的に保存する API キーは IndexedDB を使います。IndexedDB が利用できない環境では、平文の localStorage へ自動保存せず、必要な時に再入力する動作になります。

管理者鍵（ナレッジ層や監査対象 JSON の改竄検知に使う鍵）は、保存先ごとの salt を使って AES-GCM 暗号化し、ブラウザの IndexedDB に保存します。管理者鍵は Dropbox には保存されません。復旧UIからバックアップを出力した場合、そのファイルには管理者鍵本体が含まれるため、Dropbox / OneDrive の共有フォルダやメールには置かず、パスワード管理ツールやオフライン媒体で保管してください。

## テレメトリとクラッシュレポート

Meldex は、ユーザーが明示的に同意した場合に限り、クラッシュレポートと匿名利用統計を送信することがあります。送信対象は、スタックトレース、OS バージョン、Meldex バージョン、直前操作の概要、機能利用頻度、パフォーマンスメトリクスです。

作品本文、ファイル名、ユーザー識別情報、LLM API キーは送信対象に含めません。ユーザーが同意するまで、テレメトリは送信されません。同意は設定画面から後で変更できます。

## LLM 連携時の外部送信

ユーザーがチャット機能などの LLM 連携を使う場合、質問本文、ユーザーが選択した関連コンテキスト、添付したファイル内容は、ユーザーが設定した API キーに基づき OpenAI、Anthropic、Google Gemini などの外部 LLM プロバイダへ送信されます。Meldex 開発者はその送信内容を取得・閲覧・保存しません。ユーザー本人の API キーで直接送信され、Meldex 開発者のサーバを経由しません。

各プロバイダのデータ取り扱いは、各社のポリシーに従います。2026-05-17 時点で確認した主な方針は以下です。

| プロバイダ | Meldex から送信されるもの | データ取り扱いの概要 | 公式情報 |
|---|---|---|---|
| OpenAI API | チャット本文、選択コンテキスト、添付内容 | API 経由の入力・出力は、ユーザーが明示的に共有へ同意しない限り、モデルの訓練・改善に使われない方針です。安全監視等のため一定期間ログが保持される場合があります。 | https://developers.openai.com/api/docs/guides/your-data |
| Anthropic API | チャット本文、選択コンテキスト、添付内容 | API / Team / Enterprise などの商用条件では、ユーザーが明示的にモデル改善へ提供しない限り、プロンプトや出力を生成モデルの訓練に使わない方針です。 | https://privacy.claude.com/en/articles/7996885-how-do-you-use-personal-data-in-model-training |
| Google Gemini API | チャット本文、選択コンテキスト、添付内容 | 無料枠では入力・出力が Google のサービス改善や機械学習技術の改善に使われ、人間のレビュー対象になる場合があります。Cloud Billing が有効な有料扱いの Gemini API では、入力・出力を製品改善に使わない方針です。 | https://ai.google.dev/gemini-api/terms?hl=ja |
| Google Vertex AI | チャット本文、選択コンテキスト、添付内容 | 事前の許可または指示なしに、管理対象モデルの訓練・微調整には使われない方針です。 | https://cloud.google.com/vertex-ai/generative-ai/docs/vertex-ai-zero-data-retention |

LLM 連携を使いたくない場合は、設定画面で API キーを未入力にするか削除してください。API キーが未設定のプロバイダには送信されません。

## 第三者ライブラリ

Meldex は第三者ライブラリとフォントを同梱します。ライセンス情報は `THIRD-PARTY.md` と `CREDITS.md` を参照してください。

## Dropbox などの同期サービス

ユーザーが Dropbox などのクラウドストレージ内に Meldex のソースフォルダを置いた場合、作品・データ、ゴミ箱（`_trash/`）配下のファイル、設定ファイルがそのサービスの同期対象になります。Dropbox 接続中は、同じ Dropbox アカウントで使う端末間のプロフィール同期のため、ユーザー名、ユーザーアイコン、アイコン背景色が Dropbox 内の `MeldexSettings/_meldex/profiles.v1.json` に保存されることがあります。

Cloud / モバイル版の共有フォルダで誰が書き込めるかは、最終的には Dropbox 側の共有権限で制御されます。Meldex 側でも閲覧専用ユーザーの編集UIを無効化し、管理者向け共有データでは Dropbox の現在アカウントと保存先メタデータの管理者IDを照合しますが、Dropbox の共有権限設定が最終的な防御線です。

クラウドストレージ側で削除・巻き戻しを行った場合、Meldex 側でも影響を受けることがあります。

## 変更

本ポリシーは、機能追加や配布方法の変更に応じて更新される場合があります。
