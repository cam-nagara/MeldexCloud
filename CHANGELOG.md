
# シナリオエディタ チェンジログ

## v0.7.157 (2026-08-06)

- 修正: フォルダツリーの項目をダブルクリックで開いていたテスト用の共通操作部品が、v0.7.150の既定値変更（クリックで開く）に追随しておらず、ツリー経由で画面を開く多数の自動テストが「表示待ち」で失敗していた問題を修正しました（表示中の画面や操作そのものへの影響はありません）。
- 内部: リリース時の品質確認の仕組み（既知の未解消問題を期限付きで管理する台帳）を今回の公開作業に合わせて全面的に整理し、実ブラウザ・通常テストの両方で正しく機能することを確認しました。
- 内部: リリース時の品質確認に、新規に失敗したテストをその場で単独再実行して「一時的な揺らぎか、本物の失敗か」を自動判定する仕組みを追加しました（検査環境の一時的な不調で公開作業が止まり続ける問題への恒久対策。判定結果はすべて記録に残ります）。
- 内部: 配布ページ（public-index.html）のスタイル定義を外部ファイル（public-index.css）へ分離しました（1ファイル1000行以内のルールに抵触していたため。表示・挙動に変更はありません）。

## v0.7.156 (2026-08-05)

- 改善: 設定 → ユーザー・共同作業 → プロフィールで「クラウド版で設定したプロフィールが見つかりました」と表示されたとき、候補を選ぶと「どちらの名前とアイコンを使いますか？」の2択が出るようになりました。クラウド版の設定をこの端末でも使うか、この端末の設定をクラウド版・他の端末にも反映するかを選べます。どちらを選んでも、以後は同じ1つのプロフィールに統合され、片方で名前やアイコンを変えるともう片方にも反映されます（従来は候補を選ぶと常にクラウド版の名前・アイコンでこの端末側が上書きされていました）。

## v0.7.155 (2026-08-05)

- 変更: 制作管理の「かんたん割当」を廃止し、「再計算」（管理操作パネルでは「予定を組み直す」）へ一本化のうえ「割当再計算」に改名しました。名称はツールバー・管理操作・ダイアログ・固定メニュー・案内文で統一しています。
- 改善: 割当再計算は、スタッフが1人も登録されていない場合でも、現在のユーザーへ仮の勤務時間（平日9:00〜18:00・12:00〜13:00休憩）で割り当てられるようになりました（旧かんたん割当のゼロ設定フォールバックを吸収。仮の勤務時間を使ったことはプレビューに表示されます）。
- 補足: 旧かんたん割当が指定件数まで中身のない仮タスクを自動作成していた動きは廃止しました。タスクの作成は作品設定に基づく「タスクを一括作成」「タスク構成を更新」で行い、割当再計算は存在するタスクの割り当てだけを行います。

## v0.7.154 (2026-08-05)

- 修正: ノート・シナリオ・シート・ボード・タイマー・クイックメモの単独クラウドアプリと、ビューワー系の画面で、保存先フォルダの管理機能に必要な内部部品が読み込まれておらず、保存先フォルダの追加が常に失敗し、一覧が古い記憶のまま更新されないことがあった問題を修正しました。
- 修正: 共有ワークスペースの共有一覧の保存場所を、デスクトップ版とクラウド版で同じ場所（Dropbox内の管理領域）へ統一しました。旧い保存場所は読み取り互換として非破壊で残します。共有一覧は全端末がこのバージョン以降へ更新されると最新化されます。
- 修正: 「このフォルダを共有ワークスペースにする」が、通常の接続状態では必ず失敗していた問題を修正しました。作成はユーザーが確認した場合のみ許可し、同時作成は上書きせず競合として通知します。
- 修正: 設定画面のソースフォルダ共有バッジの判定が旧い保存場所しか見ておらず、新しい環境で共有状態が表示されない問題を修正しました。
- 修正: Dropboxが生成する競合コピーの検出が、共有ソースフォルダ設定・プロフィールの新しい保存場所（管理領域）を対象にしていなかった問題を修正しました。
- 改善: 旧い保存場所への「効かない書き込み」経路を閉鎖し、共有設定の書き込みは管理領域経由のみに統一しました。関連する回帰テスト・検証範囲の登録も拡充しています。

## v0.7.153 (2026-08-05)

### fix: 記録済みの既知バグ一括修正（スケジュール・シート・フォルダツリー・CSV・右レール）＋WebClipperアイコン更新

- スケジュール: タスクの予定を月表示のセルへドロップ・終日帯へドロップ・複数選択してまとめて移動した時も、作業予定日時へ正しく書き戻すようにした（従来はエラーで元の位置に戻っていた）。タスクの予定には終日の概念が無いため、終日帯へのドロップは時刻を保ったまま日付だけを移動する。担当者や作業区間が変わっていた場合の競合確認は週表示のドラッグと同じ扱い
- スケジュール: タスクリストが未セットアップ案内の表示をきっかけに永久に表示されなくなることがある不具合を修正した（案内カードが一覧の表示場所を壊していた真因を解消）
- シート: エントリを開いた時のプロパティ一覧で、ロールアップ・数式（計算列）が計算結果を表示するようにした（従来は変換前の古い値がそのまま見えていた）。計算に必要な情報が無い画面では、古い値ではなく「計算列」表示へ安全に切り替わる
- シート: 別のシートへ切り替えた時、前のシートの絞り込み条件が持ち越されることがある問題を修正した（保存済みの表示設定があるシートは従来どおりその設定を使う。同じシートの再表示では絞り込みを維持）
- フォルダツリー: 読み込み中のフォルダで右矢印キーを連打した時、2回目の入力が無視される問題を修正した（読み込み完了後に最初の子項目へ自動で移動する）
- フォルダツリー: サムネイルサイズ「大」が高解像度ディスプレイでわずかにぼやける問題を修正した（サムネイルの生成上限を引き上げ）
- CSV: CSVタブへ切り替えた直後に取り消しを実行すると、読み込み完了時に取り消しの効果が消えることがある問題を修正した
- 右レール: 画面の再描画と重なった時にアイコンボタンのクリックが反応しないことがある構造的な弱点を解消した
- Web Clipper: Chrome拡張のアイコンを最新のMeldexアイコンに差し替えた（拡張バージョン 1.2.1）
- 内部: フォルダツリーの「開く操作」の既定値変更（v0.7.150）に追随していなかった実ブラウザテスト2件と、かんたん割当・タスク一括作成ダイアログのTab移動検証テストを現行仕様へ更新（Tab移動の実動作は正常で、テスト側の期待が古かったことを実機で確認）。タスクのドラッグ書き戻しと空状態カードの実ブラウザテスト2件を新規追加
- 表示バージョンを `0.7.153` に更新した。

検証: 新規回帰テスト（シート切替絞り込み2件・計算列表示7件）と関連ユニットテスト群（制作管理375件・シート/履歴/ドック系ほか）、新規micro E2E 2件と一括作成E2E（PC/モバイル）の緑化確認を実行。既存の失敗はすべて変更前からの既知失敗であることを比較確認済み。

## v0.7.152 (2026-08-05)

### refactor: タスクリスト画面のプログラムを責務単位で3分割（1500行超の解消・動作変更なし）

- スケジュールのタスクリスト画面を担うプログラムファイル（1509行）を、既存の分割読み込み機構で3つのパートへ分割した（①共有ヘルパー・状態管理・シート一覧読込・リスト選択 ②リスト切替バー・タスク構成更新・かんたん割当 ③ワークスペース構築・カレンダー画面への配線）。3パートの連結結果は分割前と完全一致し、画面の動作・読み込み順は一切変わらない
- このファイルを直接読み込んでいた自動テスト9箇所を、分割ファイルを自動でたどる読み込み方式へ更新した
- 表示バージョンを `0.7.152` に更新した。

検証: 制作管理系ユニットテスト8モジュール110件すべてグリーン。タスクリスト関連の実ブラウザE2Eは8ケース中2件PASS、失敗6件はすべて分割前の状態でも同一失敗することを確認済み（分割とは無関係の既存問題として記録）。

## v0.7.151 (2026-08-05)

### fix: 設定のテーマサブタブが切り替わらない問題を修正（テーマ詳細設定をサブタブへ全面統合）＋設定ダイアログ全タブの余白整理

- 設定 → テーマの5つのサブタブ（テーマ／テーマカラー／フォント／状態・装飾／アプリ別）が、どれを押しても同じ内容のままだった問題を修正。各サブタブが自分の設定項目だけを表示するようにした
- 「テーマ詳細設定を開く」ボタンで開いていた別ダイアログを廃止し、基本の面・文字・操作状態・装飾・アプリ別・テーマカラーの全設定をサブタブへ統合（基本の面→テーマカラー、文字→フォント、操作状態と装飾→状態・装飾）
- 履歴・引き継ぎタブの2つのサブタブ（履歴・復元／引き継ぎ・初期化）も同じ原因で内容が切り替わっていなかったため、履歴系と引き継ぎ・全設定リセットに分割した
- 設定ダイアログの2列レイアウトを改善し、サブタブで項目を絞った時に空白のマスができる問題を解消（項目が縦に詰まって並ぶ方式へ変更）。保存先・接続・導入などのタブで項目の並びも整理
- 項目間の余白が二重に加算されて不揃いになっていた箇所と、Dropbox未接続時に空のカード領域が余白として残る問題を修正
- テーマサブタブの切替回帰テスト（実ブラウザE2E）を追加

## v0.7.150 (2026-08-05)

### feat: UI改善8件（シートのカード表示・リンク色統一・フォルダツリー設定・タスクリスト混在表示ほか）

- シート: リレーション・複数リレーション・複数ソースリレーションの値と、ロールアップ「値ごとの一覧」の参照先名を、セル内でカード（角丸の札）として表示するようにした。クリックで編集・ダブルクリックで参照先へ移動の操作は従来どおり。合計などの集計値はカード化しない（参照ではないため）。行揃え表示の高さ対応も維持
- 全体: リンクの表示を「リンク色（青系）・通常時は下線なし・カーソルを乗せると下線」に統一した。テーマ変数 `--link-fg` を新設し、全プリセットテーマに背景と調和する青を定義。ノート本文・シナリオ・スマートシート・別ウィンドウ表示にも反映
- フォルダツリー: サムネイルのサイズを設定（表示・起動タブ）で小/中/大から選べるようにした（初期値は中=現行サイズ）。項目を開く操作の初期値を「クリックで開く」に、サムネイルの表示方法の初期値を「全体を枠内に収める」に変更（いずれも明示的に保存済みの設定はそのまま尊重）
- 設定: 保存先・フォルダタブのソースフォルダ一覧をドラッグで並べ替えられるようにし、保存すると並び順がフォルダツリーの表示順にも反映されるようにした。「削除」ボタンと確認ダイアログの文言を「登録解除」に改名（フォルダの中身は削除されないことを明確化）
- スケジュール: 「外部同期」ボタンのアイコンを「更新」と同一の絵柄から、カレンダー＋同期矢印の専用アイコン（calendar-sync）に変更した
- タスクリスト: 作品タブへ切り替えると一覧が表示されない不具合を修正した。真因は、画面準備チェックの最中に読み込み途中の埋め込みシートの表示場所を空にしてしまい読み込みが中断されることで、併せて作品タブ間で絞り込み条件が持ち越される穴と、絞り込み適用がタブ切替まで反映されない穴も修正した
- タスクリスト: 「すべて」タブを作品ごとの縦積みから、全作品のタスクを1つの表に混在表示する形へ再設計した。先頭に「作品」列、表上部に担当者・状況・作品の絞り込みと検索、列見出しクリックで作品横断の並べ替え（目標作業時間・作業予定日時など）。行クリックで詳細サイドバーから編集でき、「作品タブで開く」で該当作品タブへ移動できる。既存の統合取得API（Desktop/Cloud同等）を利用し、シートデータへの新しい書き込み経路は追加していない
- ユーザーマニュアル（シート・フォルダツリー・設定・カレンダー）を上記の変更に合わせて更新した
- 表示バージョンを `0.7.150` に更新した。

検証: シートmicro E2E（リレーション/ロールアップ5件）・タスクリスト新規micro E2E 2件（2回連続PASS）・サムネイル系shell micro 4件・制作管理/シート/テーマ/設定系の関連ユニットテスト群、実シートでのカード表示と行揃えのスクリーンショット確認、隔離インスタンスでの既定値・設定UI・リンク計算スタイル確認を実行。

## v0.7.149 (2026-08-05)

### fix: ノート行ハンドルの縦書き対応と、タッチ操作直後にハンドルが縮む問題を修正（v0.7.148の徹底チェックで発見）

- 縦書きモードで、行ハンドルが全行とも画面左上の同一点に重なって区別できなくなっていたのを修正した。縦書きでは行が縦の列になるため、横書きの規則の鏡映しとして「本文エリア上端のガター・行（列）の左端」へ配置する。
- タッチ操作で行をタップした直後に、ブラウザが発行する合成マウスイベントによってタッチ用の大きいハンドル（44px）が通常サイズ（20px）へ即座に縮んでしまう問題を修正した。タップ後の短時間はマウスイベントを無視し、その後の実マウス操作では通常どおり切り替わる。
- 上記2件を微細挙動E2E（全行種ハンドル検証ケース）の恒久チェック項目として追加した。
- 表示バージョンを `0.7.149` に更新した。

検証: 拡張した行ハンドルE2E（縦書きの上端ガター配置・タッチ合成イベント耐性を含む）と関連micro E2E 計8ケース、行ハンドル回帰ユニットテスト28件、実ブラウザでの縦書き・タッチエミュレーション・UI倍率125%の動作確認、バージョン整合性チェックを実行。

## v0.7.148 (2026-08-05)

### improve: ノートエディタの行ハンドルの位置を全行で固定し、行ハイライト枠の表示をハンドル操作時のみに変更

- 行ハンドル（⠿）の表示位置を、すべての行で本文エリア左端のガター・行の上端に固定した。これまでは行のインデント（入れ子リストの深さ等）に合わせて右へずれ、行の中ではマウスの高さに追従していた。
- 行のハイライト枠は、行ハンドルにカーソルを合わせている間と、ハンドルの操作中（ドラッグ・ハンドルから開いたメニューの表示中）だけ表示するようにした。行の文字列や空行にカーソルを乗せただけでは表示しない。タッチ操作でもハンドルに触れた時点で表示する同じ規則とした。
- 表示バージョンを `0.7.148` に更新した。

検証: 行ハンドル・行ハイライトの微細挙動E2E（全17行種の位置固定・ハンドルホバー時のみハイライト・ドラッグ並べ替え・Ctrl移動との一致・境界移動・右クリック行種変更・タッチハンドル・行移動ショートカット計8ケース）、行ハンドル回帰ユニットテスト28件、隔離インスタンスでの実ブラウザ目視確認、バージョン整合性チェックを実行。

## v0.7.147 (2026-08-05)

### fix: 制作管理シートが行の直接追加などで内部の集約保存形式へ切り替わり、一覧・検索・自動計算が行を見失う問題を修正

- クラウド版で、制作管理のシート（作品リスト・作業内容リスト・タスクリストなど）へ行の直接追加（「+」）・CSV取込・列設定の保存を行うと、そのシートが内部の集約保存形式へ暗黙に切り替わり、以後、制作管理の一覧・検索・重複チェック・目標作業時間の自動計算・予定の組み直しが追加した行を見失ったり、制作管理側の更新（タスク生成・再計算の書き戻し）がシートの表示へ反映されなくなる問題を修正した（v0.6.120以降で発生。修正計画: `app/docs/production-sheet-store-contamination-fix-plan-2026-08-05.md`）。
- 既にこの状態になっているシートは、制作管理を開く・行を追加する・列設定を保存する、のいずれかの時点で自動修復される。集約保存側にしか無かった行はシート内のファイルとして復元し、途中で同名の行が二重に作られていた場合も両方の行を名前を変えて保全する（データは破棄しない）。
- 自動修復が済むまでの間も、制作管理の一覧・重複チェックが集約保存側の行を読み合わせるようにし、重複作成・編集不能・計算誤りを防ぐようにした。
- デスクトップ版でも、クラウド側から同期されたシートに残る内部の保存方式指定を制作管理の初期化・シート確保のたびに取り除き、同じ見失いが起きないようにした。
- 制作管理以外の通常シートの保存方式（集約保存形式）は従来どおり変更していない。
- 表示バージョンを `0.7.147` に更新した。

検証: ストア汚染の防止・自動修復・過渡期読み合わせの新規回帰テスト（クラウド7件・デスクトップ8件）、制作管理クラウド回帰・エンジンパリティ・CSV取込・シートアーカイブ・同期フックの既存回帰、結合JavaScript構文チェック、バージョン整合性チェックを実行。

## v0.7.146 (2026-08-05)

- fix: クラウド版のCSV取込（既存シートへの追記）で、日付列など新しい列の型が取込先シートに反映されず、内部作業用の隠しファイルがシート内に残ることがある問題を修正した（v0.7.084の取込安全化で入った不具合。既存データは壊れない）。あわせて、取込先シートがまだ無い状態での追記取込が失敗する経路も修正した。
- test: 制作管理のクラウド回帰テストを現行実装へ追随させた（シフト取込時のスタッフ台帳連携の再現、予定の組み直しエンジンの読み込み、CSV取込・ファイル保存の新契約への期待値更新）。

## v0.7.145 (2026-08-05)

- fix: 公開リポジトリの衛生対応として、テストデータ4ファイルにハードコードされていた開発者の実名入りDropboxパスを架空の名前（Example User / Sample-Project）へ置換した（テストの検証内容は不変。実名混入を検査する公開前ゲートが緑に復帰）。アイコン再生成スクリプトの入力フォルダ既定値から実環境パスを除去し、引数または環境変数 MELDEX_ICON_ARTWORK_DIR で指定する方式へ変更した。制作管理UIサンプル内の担当者名の例も架空名へ変更した。

## v0.7.144 (2026-08-05)

- feat: 制作管理を全面再設計した（列整理・計算修正・Desktop/Cloud完全同等化・操作整理）。
  - タスクリストの列を「ユーザーが設定する列」中心の約12列へ整理した。予定・目標時間などの計算結果は読み取り専用の自動列になり、直接編集できない（値はアプリが更新する）。内部管理用の列（階層パス、作成キー、ページソート値、作業予定の分割区間など）は列一覧から消え、内部データへ移行した。どこにも使われていなかった列（評価、カテゴリ、総合基準作業時間、依存関係列、面積比など16列）と「自動シフト調整設定」シートを廃止した（既存の値は移行記録へ非破壊で退避。シート契約は12→11）。
  - 目標作業時間の計算を修正した: 設計どおり「基準時間×作業内容倍率×作業規模倍率×対象数」で計算する（従来は対象数が掛かっていなかった）。テンプレートからの作成やカレンダーへのドロップ挿入でも同じ式を通す。タスクの分類（作業対象・内容・規模・対象数）を後から変更すると目標時間が自動で追従する（作業中・実績あり・固定中の行は保護）。優先度が割当順に反映されるようになった（締切優先・同一ページの工程順は維持）。再計算に「シフト時間内に収める」設定を追加した。
  - クラウド版の制作管理がデスクトップ版と完全同等になった: 本格的な再計算（工程順・担当者候補・休憩時間・保護設定を考慮）、かんたん割当、タスクの固定/解除、タスク名の自動更新をクラウドでも利用できる。両者が同一結果を返すことを自動比較テストで保証する。従来の簡易割当（工程順・候補・休憩を無視する即時割当）は廃止し、予定計算を一本化した。
  - 管理操作を整理した: 「制作管理を始める」ボタンは廃止し、未セットアップ時のみ開始の案内を表示する。「担当者と時間を割り当て」と「再計算」は「予定を組み直す」に統合し、必ず変更内容のプレビューを確認してから適用する（「未割当のタスクだけ」の絞り込み付き）。「メンバーを追加」はスタッフ管理シートを開く導線に変更した。タスク詳細の予定・目標時間は読み取り専用表示になり、保護設定（再計算ロック・担当者固定・シフト固定）をタスク詳細で切り替えられる。
  - カレンダー上のタスク予定をドラッグで移動、カード端のドラッグで所要時間を調節できるようになった（変更した予定は自動で固定され、再計算でも動かない）。タスクの「対象色」がカレンダーの予定の色に反映される。状況を「作業中」「完了」へ変えると開始日時・完了日時が自動記録される。
- feat: カレンダーの外部連携を強化した。
  - Google/Microsoftカレンダーに約5分ごとの自動双方向同期を追加した（変更分だけを取得する差分方式。デスクトップ・クラウド両対応。手動の「取得」「送信」も従来どおり）。
  - クラウド版でGoogleカレンダー・Microsoftカレンダー・Google ToDoとの連携が利用可能になった（ブラウザ内で完結する認証。設定画面の案内に従い自分のアプリ登録が必要）。iPhoneの「照会カレンダー」用の購読URLを作成できる（閲覧専用。URLを知っている人は誰でも閲覧できる公開リンクである旨を画面に明示。リンクの自動作成にはDropboxへの再接続が必要な場合がある）。制作管理の「外部カレンダーへ送信」もクラウド版でGoogle送信に対応した。認証情報は端末内の保護された保存領域に置く。
- feat: シート共通機能に「計算列」を追加した。シートの列を読み取り専用（アプリが値を更新する列）として宣言でき、直接編集・一括編集・貼り付け・列の削除や型変更から保護される。列見出しに鍵アイコンと説明を表示する。
- 旧バージョンとの互換性: この版以降で開いた制作管理データは内部形式が新しくなるため、旧バージョン（v0.7.143以前）で開くと、削除済みの列が復活して見えたり、再計算・タスク名の自動更新が正しく動かない。チームで利用している場合は全員が同時にこの版以降へ更新することを強く推奨する。移行は非破壊で、削除列の値は移行記録として各データ内に退避される。
- 既知の制限: カレンダー上のタスク予定は、月表示へのドロップ・終日帯へのドロップ・複数選択でのまとめて移動が未対応（従来どおり移動できない）。Googleのブラウザ内認証は実アカウントでの最終確認前。実ブラウザでの操作E2E追加は次回作業へ引き継ぎ。

## v0.7.143 (2026-08-04)

- 修正: 単独アプリのビルドが、アイコンを旧デザインで再生成して新アイコンを上書きしてしまう問題を修正した（ビルド時のアイコン生成を新しいデザイン原画ベースの方式へ差し替え。原画フォルダが無い環境ではリポジトリ内の現行アイコンをそのまま使う）。あわせて同梱の単独アプリ5種（ノート/シナリオ/シート/タイマー/ボード）を再ビルドし、アプリ本体のアイコンも新デザインへ更新した。

## v0.7.142 (2026-08-04)

- 変更: シートの列タイプ「セレクト」のアイコンを、タグ型と紛らわしかったタグ形からリスト形（四角2つ＋線）へ変更した。単一・複数選択の両方で同じアイコンを使い、列見出し・列タイプメニュー・列タイプの設定ダイアログに反映される（共通タグのアイコンは従来どおり）。

## v0.7.141 (2026-08-04)

- 改善: シートのセレクト型・マルチセレクト型で、セルの＋ボタンの候補一覧に、選択肢として未登録でも列内で実際に使われている値をすべて表示し、その中から選択できるようにした（型変更前に入力した値や外部から書き込まれた値が対象。リレーション型は従来から参照先シートの全エントリを表示）。行ごとに候補が変わる列（制作管理のページ範囲など）は対象外。セレクト型の候補一覧が長い場合はスクロール表示になる。
- 改善: 列タイプの設定ダイアログの「選択肢」と「選択肢の色」に、列内で実際に使われている値をすべて表示し、一つ一つに色を設定できるようにした。設定した色はセル・候補一覧・グループ見出し・カンバンへ反映される。
- 変更: Meldex本体と単独アプリ7種（ノート/クイックメモ/ボード/シナリオ/シート/タイマー/ビューワー）のアイコンを新デザインへ差し替えた（ウィンドウ・タスクバー・ファビコン・PWA・配布ページの全サイズを一括再生成。再生成スクリプトを追加し、今後はデザイン原画から生成する方式に統一）。

## v0.7.140 (2026-08-04)

- 修正: フォルダツリーの「最近使った項目」「お気に入り」の文字とアイコンが本体の行より一回り小さかった問題を修正し、行の高さも含めて本体の行と統一しました。同じ表示部品を使うスマートシート一覧の行も同じサイズに揃えています（タッチ環境の行内メニューボタンは押しやすい大きさを維持）。

## v0.7.139 (2026-08-04)

- 修正: フォルダツリーのフィルタで、画像と動画が混在する大きいフォルダの表示が壊れる問題を修正しました（タイプをオフにすると関係ないファイルまで消える・別フォルダの内容がその位置に表示されたように見える・フォルダごと消える）。原因は3件（タイプ一覧が描画時点で固定される・仮想表示の空フォルダ誤判定・絞り込みで表示中の行が消えた時のスクロール無補正）で、あわせて名前検索と併用した時に大きいフォルダ内の検索結果が表示されない問題も修正しました。
- 修正: ビューワーで画像を表示中にフォルダツリーの検索欄をクリックすると、画像と操作ボタンが消えてファイル名だけの表示になる問題を修正しました。
- 修正: 複数のタブでビューワーを使い分けて片方のタブを閉じると、残ったタブに以前再生した動画が表示されることがある問題を修正しました（タブへ戻った時に表示内容を照合し、食い違っていれば開き直します）。
- 修正: ビューワーで画像をドラッグすると小さなカードがカーソルに付いてきてパンが始まらない問題を修正し、ドラッグで即座にパンするようにしました（ボードへのカード化ドラッグはCtrl+ドラッグに変更）。ズーム中のパンは画像の端が見える範囲に制限されます。
- 修正: 「OSタグを再同期」が15秒でタイムアウトし、情報タブに画像のOSタグが表示されない問題を修正しました（バックグラウンド処理化と、OSへの読み書きに見切り時間を導入。タグの名前変更・削除で全対象画像の同期処理が応答を止めていた問題も同時に解消）。
- 修正: ビューワーのフィットボタンの初期アイコンが全画面ボタンと同じ表示になっていた問題を修正しました。
- 改善: フォルダツリーから画像・動画を開く動作を高速化しました。表示中のビューワーを開き直さず表示だけを差し替える方式にし、従来のように毎回ビューワー全体を再読み込みしません。
- 改善: 動画もビューワーで開くようにし、画像と同じツールバーと機能（回転・上下左右反転・ズーム・パン・前後のファイルへの移動）を使えるようにしました。動画で使えない「見開き」「マンガ（右→左）」は選択できない表示になります。
- 改善: ビューワーのホイールズームをカーソル位置中心にしました。マウスホイールの動作（拡大・縮小/前後のファイルへ移動）は設定ダイアログで選択できます。
- 改善: ビューワーのツールバーを自動表示にしました（パネル下端にカーソルを近づけると表示、離れると非表示。タッチ操作の端末では常時表示）。フィット切替ボタンのアイコンはフィットの種類ごとに変わります。ビューワー表示中はステータスバー右側にショートカット一覧を表示します。
- 改善: フォルダツリーのフィルタ設定を、検索バー下のボタン羅列からポップアップメニューへ変更しました。検索バーのフィルタボタン1クリックで開きます。
- 改善: フォルダツリーの項目をワンクリックで開くかダブルクリックで開くかを、設定ダイアログで選択できるようにしました。
- 改善: サムネイルの表示方法（枠いっぱいに表示/全体を枠内に収める）を設定ダイアログで選択できるようにしました。フォルダツリーとシートの画像サムネイルに適用されます。
- 改善: リンクの右クリックメニューに「フォルダツリーに表示」を追加しました（ノート本文・シートのセル・リレーション・チャット・バックリンク・ボードのカード等、全箇所のリンクが対象）。
- 改善: フロートパネルの「メインパネルで開く」を押した時、フロートパネルを自動的に閉じるようにしました。
- 検証: 実ブラウザE2E 32項目（フィルタ回帰・ビューワー操作・動画・設定・高速化の実測）が全て合格。関連unittest（フォルダツリー・パネル・ビューワー・OSタグ計93件）も合格しました。

## v0.7.138 (2026-08-04)

- 修正: デスクトップ版とスマホ・タブレット（クラウド版）で、共有ソースフォルダの登録情報が別々の場所に保存され、初回引き継ぎ以降はお互いの追加・削除が反映されなくなっていた問題を修正しました。デスクトップ版もクラウド版と同じ保存場所（Dropbox内の管理領域）を使い、旧い保存場所の内容は初回書き込み時に非破壊で取り込みます。旧バージョンのデスクトップ版が残した登録情報も引き続き読み取れます。
- 修正: クラウド版の初回セットアップに出る「デスクトップ版で使っているソースフォルダが見つかりました」の判定が旧い保存場所しか確認していなかったため、新しい保存場所を優先して確認するようにしました。

## v0.7.137 (2026-08-04)

- 修正: ウィンドウ幅の変更でビューワーなどの非アクティブ画面が再生成・再読み込みされる問題を修正し、全パネルの表示状態を保持するようにしました。PDFはウィンドウ変更後も同じ表示のまま表示領域へ再フィットし、ページ・ズーム・回転・反転・注釈状態を維持します。
- 修正: 旧形式の注釈（ストローク・矩形・付箋・しっぽ）が画像・PDFの回転・反転・ズーム・パンへ追従するようにしました。読み込みだけではファイルを書き換えず、編集・保存時のみ新形式の座標情報を付加します。
- 改善: 単独ビューワーとクイックメモの色選択を本体と同じ共通カラーパレットへ統一し、同一PCの各アプリでカスタム色を共有できます。Windowsでの同時保存はロックと再試行で保護します。
- 改善: 反転・回転アイコン、下端ツールバーの高さ、右クリック・長押しメニューをMeldex共通UIへ揃えました。親画面と子ビューワーの注釈状態（表示・透明度・色・保存結果）を完全同期し、保存失敗は共通通知に表示します。
- 検証: 座標変換の実ブラウザ検証（回転・反転でサブピクセル一致）を含むunittest 114件と、ビューワーの実ブラウザE2E 4件が合格しました。実端末回転・実Dropbox二端末は公開前の手動確認として残ります。

## v0.7.136 (2026-08-04)

- 修正: Ctrl+↑/↓の行移動が同じ階層内に制限されていた回帰を修正し、リスト境界をまたぐ移動（同一リスト内の入れ替え・入れ子端の一段上げ・同種リストへの合流・通常行との相互変換）を復旧しました。ドラッグ移動も同じ結果になります。
- 修正: 行種変更後にキャレットがノート先頭へ戻る問題を修正し、元の位置と選択範囲を維持するようにしました（コールアウト・コード・表・区切り線への変換を含む）。
- 改善: 本文・見出し・リスト・コールアウト・引用・コード・表・区切り線の全17種類の行で行ハンドルを表示し、ハンドルにカーソルがある間は行全体をハイライトします。書式設定ポップアップは最下段1行にコピー・切り取り・貼り付け・閉じるを右寄せで統合しました。
- 改善: チャットの発言者名・日時も本文と続けて選択でき、Ctrl+Cで発言者・時刻付きの整形テキストとしてコピーできます。
- 検証: ノートの実ブラウザE2E 8件（行移動・全行種ハンドル・キャレット保持・ポップアップ・チャット選択）と関連unittest 67件が合格しました。

## v0.7.135 (2026-08-04)

- 修正: メインパネルのサイズ変更時に、調整済みの列幅が古い保存値で巻き戻ることがある問題を修正しました。ギャラリーが「カードに表示する列」で選択していない画像列を表示する問題も修正しました。
- 改善: テーブルの「セル表示」メニュー（旧「折返し」）で値の最大行数と画像サムネ数を設定できるようにし、長文列は最大268px程度で折り返します。未調整のシートは初回表示時に一度だけ列幅を自動調整します。
- 改善: フィルタ・並べ替え・手動順序をビューごとに保存し、列ヘッダーのフィルタとツールバーの統合フィルタダイアログを双方向に連携しました。「シートテンプレート」「シート設定」はツールバー右側へ移動しました。
- 改善: エントリの「列一覧」（旧「列」）で文字列を通常のドラッグで選択し、Ctrl+Cや選択付近の「コピー」からコピーできるようにしました。レイアウト編集の並べ替えは専用ハンドルからのみ行います。
- 検証: 新規契約テスト34件を含む関連369件と、シートの実ブラウザE2E 6件が合格しました。マニュアルを新しい配置・用語へ同期しました。

## v0.7.134 (2026-08-04)

- 改善: Xブックマークを含む9種類のインポート（Notion・Obsidian・Evernote・Eagle・ENEX・PureRef・Xアカウント投稿・Notion同期）に、再起動やスリープに強いバックエンド定期実行を追加しました。設定画面を開いていなくても予定どおり実行され、停止中に逃した予定は次回起動時に対象ごと1回だけ追いつき実行します。
- 改善: 手動・定期を問わず重いインポートは共通の待ち行列で順番に実行し、ステータスバーに進捗率・待機件数を表示します。画面移動や再読み込み後も進捗表示が復元され、安全な単位でキャンセルできます。
- 安全性: 定期実行はすべて初期状態で無効です。Xアカウント投稿の定期実行は、API利用量・料金の確認に同意しない限り登録できません。時刻は永続化した基準時刻から算出し、夏時間・月末・うるう年・時計変更でも重複や取りこぼしが起きません。
- 検証: 周期計算・追いつき実行・ジョブ状態遷移・旧設定の互換読み込みの新規53件を含む関連133件と、設定画面の実ブラウザE2E 2件が合格しました。

## v0.7.133 (2026-08-04)

- 改善: WebClipperの保存先を本体と同じ「一覧から選択」で指定できるようにしました。登録済みフォルダの階層移動・新規フォルダ作成に対応し、ポップアップ・右クリック保存・画像収集・範囲キャプチャの全入口で同じ保存先を共有します。
- 改善: タグ入力を色付きタグチップへ刷新しました（候補一覧・追加・削除・重複防止・IME・タッチ対応）。新しいタグは保存時に本体のタグ辞書へ安全に登録されます。
- 改善: Meldexが起動していなくても保存できるようになりました。保存時に専用のバックグラウンドサービスを自動起動し、最終利用から5分後に自動終了します。本体起動中は追加プロセスを作りません。
- 安全性: 起動ブリッジは固定命令のみを受け付け、許可した拡張機能ID・Origin・専用ヘッダー・保存先境界を検証します。保存本文・画像は従来どおりローカルHTTPのみで送信します。
- 検証: 新規テスト63件を含む関連95件と、拡張機能ポップアップの実ブラウザE2E（PC・スマートフォン幅）が合格しました。実Chrome/Edgeでのホスト登録から未起動時保存までの実機一連確認は公開前の手動確認として残ります。

## v0.7.132 (2026-08-04)

- 修正: チャットのファイル操作で、チームDropboxの登録フォルダが常に「アクセス範囲外」(403)になる問題の真因を修正しました。フォルダ登録の名前空間種別が保存経路3箇所で欠落し、誤ったローカルパスへ解決されていました。
- 安全性: 矛盾した登録を読み込んだ場合は実在するチームスペース側を優先して警告を記録し、チーム共有情報の書き込みが存在しない幽霊フォルダを実体化しないようにしました。一度正しく登録した名前空間種別は、無関係な保存操作で巻き戻りません。
- 検証: 修正前のソースで失敗し修正後に成功する登録往復の回帰テストを追加し、スコープ・MCP・CLIチャット関連の490件が合格しました。

## v0.7.131 (2026-08-04)

- 修正: `.mel-board`、`.mel-scenario`、`.mel-timer`、`.mel-sheet`と旧固有形式の保存・閲覧・編集・複製・移動・版管理等で、保存フォルダへ `_meldex`、`.meldex`、`_events`、`自動タグ辞書`、`_Meldex_team.json`、隣接競合コピーなどのMeldex内部付随物を新規作成しないようにしました。
- 改善: 注釈、タグ、ロック、版、競合時の退避、プロフィール、チーム／ワークスペース情報を、PC・ブラウザ・Dropbox個人／共有の型付きMeldex管理領域へ統合しました。旧付随物は読取専用で取り込み、ID・内容ハッシュ・スキーマ・revision・バックアップを照合できた場合だけ移行します。
- 安全性: 文書IDの差替え、保存先判定不能時の個人領域への誤保存、オフライン送信待ちの保存失敗を成功扱いする経路を防止しました。旧クライアントが付随物を再生成した共有領域は読取専用にし、ownerの再移行確認まで書込を止めます。
- 検証: 8形式×28操作の224通りについて暗黙の付随物作成がないことを決定的に検証し、旧パス書込許可リストを0件にしました。移行、競合、CAS、オフライン、権限、版、プロフィール、チーム、Desktop API、Cloud静的版の関連テストと、PC・スマートフォン・タブレット縦横の対象実ブラウザE2Eを実施しました。Dropboxは偽APIで競合・障害を検証しており、実運用Dropboxアカウントは変更していません。

## v0.7.130 (2026-08-04)

- 修正: Windows単独アプリからMeldex本体や別アプリを開く経路を、配布物に存在する実行先の自己登録と署名付き移送へ接続しました。Viewerも単独配布へ追加し、存在しない移送先や古い登録を成功扱いしません。
- 修正: タイマーのカレンダー連携をローカルSQLiteとCloudスナップショットへ接続しました。オフライン時は世代付きキャッシュを使い、404、ストレージ復旧、別タブ更新、未来時刻があっても古い結果へ戻りません。
- 修正: 注釈をMeldex本体と単独版で共有できるワークスペース別管理領域へ統合しました。旧形式を非破壊で移行し、別Vault隔離、同時編集、移動・改名の巻戻し、削除後の再出現防止、版と書き出しを共通契約にしました。
- 改善: ノートのDOCX出力、シナリオのファイルスタイル、シートの表示設定を共通実装へ移し、ボードの表示先を実際のメイン／新しい画面／対応副画面へ接続しました。
- 安全性: Windows単独アプリの登録情報は所有者・アクセス権・heartbeatを検証し、ブラウザーのワークスペース識別を安全に比較できない場合は保存を停止します。能力監査も未読込コードの文字列ではなく、実読込順・実DOM・配布物を検査します。
- 検証: 変更直結418件、注釈・保存・Windows／タイマー基盤の独立レビュー計190件、PC／タブレット／スマートフォンの実ブラウザ6件が合格しました。全量5,483件の初回で新規赤15件を検出し、14件は共通化後の正本・強化契約へテストを追随、1件は注釈取得上限の実装回帰を修正しました。再実行は既知54件、新規赤0件、skip 3件、タイムアウト0件で機械ゲートに合格しています。署名済みEXE、実Dropbox 2端末、実File System Accessピッカーは公開前の手動ゲートとして分離し、今回の検証では実運用データを変更していません。

## v0.7.129 (2026-08-04)

- 改善: フォルダツリーのサムネイル表示を初期状態でONにしました。設定でOFFにした状態は次回起動後も維持され、OFF時は従来どおりコンパクトな行と形式アイコンで表示します。
- 修正: ファイル名に `#` を含むPNGで、画像自体は正常でもフォルダツリーにサムネイルが表示されない問題を修正しました。拡張子の大文字小文字を問わず、端末上にあるPNGを表示できます。
- 改善: 設定説明へPNG対応と、Dropboxでオンラインのみにあるファイルは自動取得しないことを明記しました。サムネイルを生成できないCloud静的版では、この設定を表示しません。
- 検証: 設定の未保存・ON・OFF・再読込、PNG／大文字拡張子／`#` を含むファイル名、実PNGのサムネイルAPI、64×44px表示と50px行、OFF時の22px行とAPI要求なし、大量項目の遅延取得を、関連ユニットテストと実ブラウザE2Eで確認しました。

## v0.7.128 (2026-08-03)

- 修正: 150件以上の大量項目を持つフォルダ内で、さらに子フォルダやシートを展開すると全項目がDOMへ戻り、急に重くなる問題を修正しました。入れ子の子孫も同じ仮想スクロールへ接続し、表示範囲付近だけを描画します。入れ子の階層線、固定親、並べ替え、移動、選択、サムネイル行高も論理上の親子関係へ追従し、画面幅を変更した時もPCの22px行とスマートフォン・タブレットの44px行を再計算します。
- 改善: 上端に固定される親階層行を、元の位置へ戻るボタンと閉じるボタンに分けました。Enter／Spaceで操作でき、スマートフォン・タブレット・タッチ端末では44px以上、マウス操作のPCでは従来どおり22pxの密度を維持します。
- 修正: PDFをダブルクリック、Enter、右クリック／長押しメニューの「開く」から開いた時、DesktopではOSアプリへ渡され、Cloudでは開けない場合がある問題を修正しました。拡張子の大文字小文字にかかわらずMeldex内蔵ビューワーで開きます。
- 修正: シートのエントリ行が初回表示時だけドラッグ可能になっていた問題を修正しました。エントリは選択できますが、フォルダツリー内のファイル移動ドラッグは開始しません。
- 検証: 関連ユニット／静的契約／JavaScript構文／バンドル一致検査と、Desktop・390pxスマートフォン・834pxタブレットの実ブラウザで、大量項目の入れ子展開、固定親のマウス・キーボード操作、PDFの共通起動経路、エントリのドラッグ抑止を確認しました。

## v0.7.127 (2026-08-03)

- 修正: ボードや各アプリのリンクから「右サイドバーで開く」を選んだ時、ノート、シナリオ、ボード、シート、フォーム等を、それぞれ正しいアプリの「サブパネル」で閲覧・編集できるようにしました。ボードの初期化、同じファイルを複数画面で開いた時の所有状態、外部URLと存在しないファイルの判定も修正しました。
- 修正: サブパネルとフロートパネルで、閉じる、戻る、進む、別ファイルへ置換、「メインパネルで開く」の前に編集内容を保存します。保存に失敗した場合は画面遷移を中止し、内容を失わないようにしました。「メインパネルで開く」は実際に作成した新しいタブを正確に選択します。
- 修正: フロートパネルとサブパネルから、オプション、ヒストリー、チャット等の右サイドバー補助操作を、表示操作だけでなく内部経路からも開けないようにしました。メインパネルでは従来どおり利用できます。
- 改善: スマートフォンでもプレビューではなく実際の対象アプリをサブパネルとして開き、編集・保存・メインパネル昇格を行えるようにしました。390px幅で「メインパネルで開く」が欠けない2段ヘッダーと44px以上の操作領域に整えました。
- 修正: クイックメモを含む確認ダイアログで、Tab／Shift+Tabと矢印キーによる選択がダイアログ内を循環し、Escape後に呼出元へフォーカスが戻るようにしました。
- 検証: 関連ユニット／静的契約テスト、Desktop実ブラウザー、Cloud StaticのPC・834pxタブレット・390pxスマートフォン、ローカル配信クイックメモで、表示・編集・保存・再試行・メイン昇格・キーボード操作を確認しました。全ユニットテストの一括実行は20分でタイムアウトしたため、既知の長時間テスト負債として継続管理します。

## v0.7.126 (2026-08-03)

- 修正: Xアカウントの全ポスト保存が約60分を超えた時、サーバー側では保存が続いているのに、画面だけが「処理が長時間続いています」と失敗表示になる問題を修正しました。時間だけでは失敗扱いにせず、設定画面やページを閉じて開き直しても同じ処理の進捗確認を再開します。
- 改善: ポストごとに取得件数と保存件数を更新し、最後のメディア参照整理も進捗として表示します。途中でX APIエラーになった場合も、すでに保存できた新規・更新件数を残して案内します。
- 修正: 認証切れの401では更新トークンを一度だけ使って同じ取得を再試行し、再試行も認証エラーの場合だけXの再接続を求めるようにしました。クレジット・利用上限、アプリ権限、非公開アカウント、レート制限、X側障害、通信障害は、それぞれ原因と次の操作を分けて表示します。
- 安全性: 実行前に「最大3,200件・X APIの従量課金対象」であることを常時表示し、保存開始前にも確認します。料金は変動するため、X Developer Consoleを正本として案内します。
- 検証: X保存・OAuth・ジョブ再開・設定画面・ルーター認証の関連テスト46件、JavaScript／Python構文検査、実ブラウザでの保存先プレビューと課金確認表示に合格しました。従量課金を避けるため、実X APIへの保存実行は行っていません。

## v0.7.125 (2026-08-03)

- 修正: 同じノートが古い保存先情報と現在の文書情報に分かれて管理され、競合を解決・保留しても、残った古い競合が次の操作で再表示される根本原因を修正しました。同一文書の保存待ち・競合状態・表示中パネルを一つに統合します。
- 安全性: ノート、CSV、ボード、シナリオ、詳細パネル、スマートフォン画面、各単独アプリの保存を、読込時の版と一致した場合だけ既存ファイルへ反映するよう統一しました。版を持たない全量上書きは拒否し、新規作成と利用者が明示した上書きだけを区別して許可します。
- 修正: 保存中に続けて編集した内容が、最初の保存前の古い版を使って再送され、自分自身の保存と競合する問題を修正しました。直前の保存成功で得た最新版を次の保存へ引き継ぎます。
- 修正: ファイルの改名・移動後も同じ文書として保存待ちと競合状態を引き継ぐようにしました。固有形式の文書IDを保存ごとに作り直す問題と、複製した文書が同じIDを共有する問題も修正しました。
- 修正: CSVを切り替えた時、前のCSVの待機中設定保存が次のCSVの内容や版情報を参照し得る問題を修正しました。前の保存を確定してから表示内容を切り替えます。
- 改善: Cloudで競合を「後回し」にした状態を、同じDropboxワークスペースの端末間で共有するようにしました。別のアカウントやワークスペースの競合とは混同しません。
- 安全性: チャット／CLIのファイル読込と書込にも版照合を追加しました。長文を途中までしか取得していない場合は版情報を返さず、未取得の末尾を全量書込で消せないようにしました。
- 検証: 関連ユニットテスト349件、JavaScript／Python構文検査、Desktop版と390pxスマートフォン幅のChrome競合テスト、版の保存・比較・復元テストに合格しました。実DropboxのDesktop／Cloud／スマートフォン三者試験と生成済み配布物への反映は、ビルド・実環境確認の別ゲートとして継続します。

## v0.7.124 (2026-08-03)

- 修正: ノートの競合確認で「保留」を選んだ後も内部状態が確認中のまま残り、別の場所を操作するたびに「他のタブ/パネルで変更されています」が再表示される問題を修正しました。保留後は保存を止めた状態を維持しつつ、非モーダルの「確認する」から改めて競合を確認できます。
- 修正: 同じノートをメインパネルと詳細パネルなどで開いた時、未編集側の本文表示が古いまま保存基準だけ更新され、その後のフォーカス移動で古い本文を保存し得る問題を修正しました。競合解決後は未編集ホストの本文・保存基準・版情報を同時に揃えます。
- 修正: 競合確認中に遅れて届いた取得結果が新しい競合を誤って解決する問題と、最新版の描画に失敗した時に編集中の本文や保存基準を失う問題を修正しました。競合は確認開始時と同じ世代で、読込と描画が完了した場合だけ解決します。
- 修正: CSV、ボード、シナリオ、詳細パネル内ノートで、競合保留中の通常保存、通常の画面再表示による誤解除、存在確認を省略した強制上書き、読込失敗前の競合解除を防ぎました。
- 修正: 見出しへのリンクをコピーした時に、新しく付けたアンカーIDがノートへ保存されない問題を修正しました。見出しの表示名を「見出し１」〜「見出し６」に統一し、行種ドロップダウンの見本へ本文・見出しテーマを正確に反映しました。
- 検証: 保存・競合・行種・行移動・見出しリンク・Cloud単独版の関連テスト196件とNode内87件、PC／スマートフォン幅のChrome実ブラウザ競合テストを合格しました。実Dropbox複数端末、安定文書ID、revisionなし全量PUT拒否、配布生成物への反映は引き続き完了ゲートとして管理します。

## v0.7.123 (2026-08-03)

- 追加: シナリオの「タイプ」と「キャラ」を分離しました。タイプは本文・タイプ列・区切り・カスタム列を含む完全な書式と機能を持ち、キャラは名前色と対応タイプだけを持ちます。キャラ名を残したまま、対応タイプの見た目をまとめて変更できます。
- 改善: タイプ管理を上段「タイプ」、下段「キャラ」の独立した一覧に刷新しました。タイプの完全な書式・機能設定、キャラの名前色・対応タイプ、複数キャラの一括設定、並べ替え、複製、DB読込、使用中項目の安全な置換削除を同じ画面で操作できます。
- 改善: タイプ列の候補を「全キャラ → キャラと紐づいていないタイプ →（なし）」のフラットな一覧へ統一しました。未登録名の入力は、対応タイプ未設定の新規キャラとして登録されます。
- 互換性: シナリオ保存形式をスキーマ3へ更新し、行の参照を安定IDで保持するようにしました。旧スキーマ2は元ファイルを変更せずに読み込み、本文・表示名・順序・従来の見た目を保ったまま移行します。旧版向けの `role` 名も保存するため、旧版はシナリオ本文を引き続き読み込めますが、新しいキャラ／タイプ対応の編集には本版以降が必要です。
- 改善: テンプレートはタイプの書式とエディタ設定だけを保存・適用し、作品固有のキャラ名、名前色、対応タイプを置き換えなくなりました。
- 修正: フィルタ、行セット、コピー／貼り付け、Undo／Redo、履歴、B-MANGA送信、CLIP STUDIO出力を共通の役割解決へ統一し、キャラ経由と直接タイプ指定で書式・区切り判定が食い違わないようにしました。
- 修正: 徹底チェックで検出した、タイプの自動配色が反映されない問題、パレット変更で複数タイプが同じ色になる問題、キャラ名前色の2回目以降を直前状態へ戻せない問題、書式設定中のUndo後に変更が保存されない問題、欠損IDの補完で既存IDと重複し保存不能になる問題を修正しました。
- 検証: スキーマ2→3移行、300行指定サンプルの本文・表示名・順序保持、7キャラ／6タイプ分類、候補・名前変更・削除置換・テンプレート・周辺出力の回帰テスト、JavaScript／Python構文検査、PC・スマートフォン縦・タブレット縦横の実ブラウザ操作を確認しました。

## v0.7.122 (2026-08-03)

- 改善: ノート、シナリオ、ボード、シート、タイマー、クイックメモ、Viewerの単独アプリで、現在の環境に応じて利用できる機能と代替の開き方を判定するようになりました。公開、バックリンク、ファイル情報などMeldex本体が必要な操作は、同じファイルを引き継いで本体へ移動します。
- 改善: 単独アプリの内部リンクを「現在の画面」「新しいウィンドウ」「Meldex本体」から選べるようにし、編集中は保存または破棄を確認してから移動するようになりました。
- 修正: Windows単独アプリで、別画面が先に更新したファイルを古い内容で上書きできる問題を修正しました。保存は版の照合、プロセス間ロック、アトミック置換で保護されます。
- 改善: ボードのテーマ・ファイルスタイル・一括取込、タイマーのカレンダー、クイックメモのCloud保存を各環境で揃えました。途中失敗時の部分更新を残さず、クイックメモの既存識別情報や将来追加された保存項目も保持します。
- 改善: Cloudでは画像、動画、音声、PDF、HTMLをCloud版本体のViewerで開くことを明示しました。単独Viewerが存在しない環境でも無反応になりません。
- 安全性: 安全でない形式の自動リンクを拒否し、Cloudのシフト反映失敗時は本文、カレンダー、作成フォルダ、新規スタッフをまとめて元へ戻すようにしました。

## v0.7.121 (2026-08-03)

- 修正: フォルダツリーで、一度もクリックしていない項目を右クリックメニューの「開く」やEnterで開こうとしても反応しないことがある問題を修正しました（壊れて読み込めないファイルなども、エラー案内が出るか正しく開かれるようになります）。
- 変更: OSの関連付けアプリで開く形式（.clip など）は、ダブルクリック・Enter・メニューの「開く」でも関連付けアプリで開くようになりました。
- 修正: 大量項目のフォルダで、矢印キーの上下移動が画面に見えている範囲で止まったり、無関係な行へ飛んだりする問題を修正しました。
- 修正: 検索やドラッグ操作の後に、サムネイル表示の行の高さが一瞬戻り、表示位置がずれる問題を修正しました。
- 修正: 「フォルダツリーを更新」で、直近に失敗したサムネイル取得も再試行されるようになりました。
- 修正: 大量項目のフォルダで、スクロールを挟んだShift+クリックの範囲選択が単一選択になってしまう問題を修正しました。

## v0.7.120 (2026-08-02)

- 追加: フォルダツリーにファイルのサムネイルを表示できるようになりました（表示設定でON、初期設定はOFF）。画面に見えている範囲だけを取得するため、大きなフォルダでも重くなりません。サムネイルが無いファイルは、Windowsに登録されたファイル形式のアイコン（例: .blend や .zip の関連付けアイコン）で表示されます。
- 改善: フォルダツリーの新しい操作方法（単クリック=選択、ダブルクリック=開く、F2=名前変更、範囲選択、固定親階層行など）をユーザーマニュアルに反映しました。

## v0.7.119 (2026-08-02)

- 改善: フォルダツリーが数千件の項目でも軽快に動くようになりました。画面に見える範囲だけを描画する方式に変わり、2,400件のフォルダで描画時間が約500分の1、表示の内部行数が数十行に抑えられます。
- 追加: 深い階層をスクロールしても、展開元のフォルダがツリー上端に固定表示され、その場で枝を閉じられるようになりました（最大3階層）。閉じた後はその親に選択が戻ります。
- 追加: フォルダ・シートの右クリックメニューに「この階層を閉じる」、ツリーの空白右クリックに「すべて閉じる」を追加しました。
- 追加: ツリーの親子関係が分かる縦線を表示するようになりました（表示のみで、クリック操作はありません）。

## v0.7.118 (2026-08-02)

- 変更: フォルダツリーの範囲選択（ドラッグでまとめて選択）は、空白部分からドラッグした時だけ始まるようになりました。項目の行からドラッグした場合は常にその項目の移動操作になります（未選択の項目はその項目だけ、選択済みの項目はまとめて移動）。
- 修正: Ctrlを押しながらフォルダツリー内へドラッグ＆ドロップすると、項目が誤って開いてしまう問題を修正しました。

## v0.7.117 (2026-08-02)

- 変更: フォルダツリーの操作を刷新しました。単クリックは項目の選択だけを行い、開くのはダブルクリックまたはEnterです。名前の変更はF2キーか右クリックメニューから行います。矢印キーは標準的なツリー操作（↑↓=選択の移動、←=閉じる/親へ、→=開く/最初の子へ）になりました。
- 変更: 「最近使った項目」とお気に入りも同じ操作（単クリック=選択、ダブルクリック=開く）に統一しました。
- 準備: フォルダツリーの軽量サムネイルとWindows形式アイコンの取得機能を内部に追加しました（画面への表示は今後のバージョンで有効になります）。

## v0.7.116 (2026-08-02)

- 修正: カスタムカラーを短時間に続けて追加・削除した時、他の画面で追加した色や直前の編集が消えることがある問題を修正しました。
- 修正: カスタムカラーの共有が一時的な通信失敗のあと復帰しなくなる問題を修正しました（30秒後の次の操作で自動的に再接続します）。
- 修正: ビューワーで注釈モードがオフの時でも、既存の注釈の線や図形がページ送りや画像の移動操作を妨げることがある問題を修正しました。
- 追加: 単独ビューワーでも注釈（描画・付箋・しっぽ）を保存できるようになりました。右クリックメニュー・Aキー・右サイドバーから利用できます。
- 補足: v0.7.115以降の注釈を旧バージョンで表示すると位置がずれることがあります（データは壊れません）。詳しくはマニュアル「旧版にロールバックする手順」を参照してください。

## v0.7.115 (2026-08-02)

- 追加: ビューワーの注釈（描画・付箋・しっぽ）が画像そのものに固定されるようになりました。ズーム・移動・回転・左右／上下反転・見開き表示・PDFのページに正確に追従します。既存の注釈はそのまま読み込め、編集して保存した時点で新しい保存形式へ切り替わります。
- 追加: Meldex本体の右下の注釈ボタンから、ビューワーで表示中の画像やPDFの注釈を直接オン・オフできるようになりました。
- 修正: フロートパネルやサブパネルの中でボードを開いた時、カードの右クリックメニューから「右サイドバーで開く」が制限を素通りしていた問題を修正しました。
- 修正: サブパネルから「メインパネルで開く」を使った時に、パネル内で変えた表示状態が引き継がれない問題を修正しました。
- 修正: スマートフォン幅への切り替え直後の画面遷移で、保存の確認が行われないことがある問題を修正しました。
- 修正: ダイアログの中から開いたカラーパレットなどのポップアップで、Tabキーがダイアログに奪われる問題を修正しました。
- 改善: ビューワーとタイマーの画面でもダイアログのキーボード操作（Tab循環・矢印キー）が使えるようになりました。

## v0.7.114 (2026-08-02)

- 修正: ウィンドウサイズを変えるとビューワーの画像・ツールバー・表示状態が消えて読み込み直しになる問題を修正しました。サイズ変更では画面を作り直さず、表示中の内容・ページ・ズーム・回転などをそのまま維持します。
- 改善: ビューワーのツールバーと右クリックメニューをMeldex共通のデザイン（アイコン・高さ・タッチ操作領域・画面端補正）へ統一しました。タッチ／ペンの長押しでも同じメニューが開きます。
- 変更: ビューワーからタグ表示と、ツールバーの注釈ボタンを撤去しました。注釈は右下の共通ボタン（単独ビューワーでは右クリックメニュー・Aキー・右サイドバー）から開きます。

## v0.7.113 (2026-08-02)

- 追加: フロートパネルとサブパネルの中では、右サイドバーの補助機能（オプション・ビューワー・バージョン管理・チャット・タイマー・ヒストリー・注釈・タグ・別のサブパネル）を開く操作を表示・実行の両方で制限しました。メインパネルからは従来どおり使えます。
- 改善: フロートパネルでも、閉じる・戻る／進む・表示の置き換えの前に保存を確認するようにしました。保存できなかった場合は操作を中止し、編集内容を保持したままお知らせします。
- 改善: シナリオをフロートパネルやサブパネルで編集した場合も、閉じる・置き換え・「メインパネルで開く」の前に未保存の編集を確定して保存するようにしました。
- 改善: フロートパネルとサブパネルをユーザーマニュアルで別ページに分け、サブパネルの使い方とダイアログのキーボード操作の説明を追加しました。

## v0.7.112 (2026-08-02)

- 追加: 右サイドバーに新しい「サブパネル」を追加しました。リンクのメニューから「右サイドバーで開く」を選ぶと、ノート・シナリオ・ボード・シートなどが対応アプリの画面のまま右サイドバーに一時表示され、そのまま閲覧・編集できます。戻る／進むで表示履歴を行き来できます。スマートフォンでは右サイドドロワーに同じ内容が表示されます。
- 追加: フロートパネルとサブパネルのヘッダーに「メインパネルで開く」ボタンを追加しました。表示中のファイルを必ず新しいタブとしてメインパネルへ開きます。
- 追加: カスタムカラーを、Meldex本体と同じPC上の単独アプリすべてで共有するようにしました。カラーパレットからファイルへの書き出しと読み込み（追加または置き換え）もできます。
- 改善: リンクの開き先判定を全アプリ共通の仕組みへ統一し、「サイドバーで開く」の表記を「右サイドバーで開く」へ統一しました。

## v0.7.111 (2026-08-01)

- 修正: ノートの行ハンドルが、行の文字の上にカーソルがある時しか表示されなかった問題を修正しました。左余白の行ハンドル位置にカーソルを乗せるだけで、その位置の行のハンドルが表示されます。
- 修正: ノートの右クリックメニューにある「行種変更」で行の種類を選んでも反映されなかった問題を修正しました。
- 修正: ノートの行ハンドルを左クリックで押している間に右クリックすると、行の選択が消えてメニューが表示される問題を修正しました。あわせて、行ハンドルを右クリックするとその行の右クリックメニューが開くようになりました。
- 変更: ノートの行の並べ替えショートカットを Ctrl+↑ / Ctrl+↓ に変更しました。従来の Alt+Shift+↑↓ はWindowsの言語切替操作と衝突して動かないことがあったためです。

## v0.7.110 (2026-08-01)

- 改善: 浮動ウィンドウの内部名を「フロートパネル」へ正式に統一しました。旧名のまま残っていた画面部品・保存キーを新名称へ移行し、保存済みの位置・サイズ・最大化状態は初回起動時に自動で引き継がれます。
- 追加: すべてのダイアログでキーボード操作を保証する共通基盤を追加しました。Tab／Shift+Tabでダイアログ内を循環し、矢印キーでボタン間を移動できます。ダイアログの外へフォーカスが抜けることもなくなります。Meldex本体に加え、ノート・シナリオ・シート・ボード・タイマー・クイックメモの単独アプリでも同じ操作が使えます。

## v0.7.109 (2026-08-01)

### fix: ノート計画の徹底チェックで検出した重大バグ群を修正

- **保存の並走時に編集内容が消える2つの経路を修正した。** 進行中の保存が競合で失敗した際、後から重ねた編集の保存要求が「成功」と誤認されて下書きまで消される問題と、詳細パネルの保存が競合保留を「成功」と飲み込む問題（いずれも再現テスト付きで遮断）。
- **ノートを素早く切り替えた直後、前のノートの自動保存が新しいノートへ誤った内容を保存し得る競合窓を塞いだ**（切替時のタイマー停止＋保存時のパス照合の二重防御）。
- **競合解消で「相手の変更を読み込む」「別名で保存」を選んだ後、破棄したはずの自分の編集が次回起動の下書き復元から復活し、相手の内容を上書きし得る問題を修正した。**
- **実質移動しない行ドラッグの後、通常の入力がCtrl+Zで丸ごと消える取り消し破損を修正した。**
- **番号付きリストの途中の行を別の行種へ変換すると残りの番号が壊れる問題、スラッシュメニューで検索語（h1等）を入力して選ぶとその文字が本文に残る問題を修正した。**
- **同じ内容の競合コピーを自動で片付ける機能が、識別子未付与のファイルで一度も機能していなかった問題を修正した**（照合が毎回異なる結果になる非決定性を解消）。競合コピー処理の安全網（削除直前の再照合）とCloud側のエラー分類の堅牢化も実施。
- そのほか、誤った競合表示・保留状態の固着・通知の重複・日本語入力中の下書き保証の後退・自動スクロール中のドロップ位置ずれ・見出しインデントの追従など中位11件を修正した。

検証: 全修正に「修正前は失敗する」再現テストを付与（負の対照）。統合373件超全緑、実ブラウザE2E（競合ダイアログ・スラッシュメニュー・行移動・リスト変換・単独ノート版）合格。

## v0.7.108 (2026-08-01)

### feat: Dropbox保存経路の統合と単独ノート版への機能展開（ノート計画 工程2-D・12）

- **Cloud・スマートフォンでのDropbox保存が競合した際、その文書を自動的に「競合を保留中」へ移し、自動保存の再試行を止めるようにした**（従来は競合検知の仕組みが構造化されておらず、共通の競合処理に接続されていなかった）。同じ内容の保存し直しでは競合コピーを作らず現在の版へ収束し、内容が完全に一致する既存の競合コピーは自動で解消する。
- **Cloud版の競合通知の「後回し」を、時間経過で再表示される一時しのぎから、その競合を確認するまで再通知しない方式へ変更した**（非モーダルの確認導線は残る）。
- ローカル保存とDropbox保存の版管理トークンを別物として扱い、取り違えを実行時に検出する保護を入れた。
- **単独ノート版へ、行種メニュー・行移動・右クリック行種変更・ツールバーのテーマ見本メニューを展開した。** スマホ幅のあふれメニューからメニュー系ボタンを押すと開いた瞬間に閉じてしまう不具合も修正した。
- ノートのマニュアルへ行種変更・行頭記法・行移動・見出しリンク・競合の保留を追記し、ショートカット一覧の漏れ（コールアウトほか）を補完した。

検証: 保存契約37件・競合コピー・分類マニフェスト・関連回帰199件中198件緑（1件は無関係の既存不整合）。単独ノート版の実ブラウザ検証（PC/タブレット/スマホ全ケース）合格。

## v0.7.107 (2026-08-01)

### feat: 全編集面の保存を共通契約へ・入力の軽量化・右クリック行種変更（ノート計画 工程2-C・3・9）

- **ボード・シナリオ・エントリ自由記述・ボードノート・モバイル編集の保存に、読み込み時点との照合（楽観的排他制御）を導入した。** 従来はこれらの保存が無条件の全量上書きで、別の場所からの変更を黙って消し得た。競合時は共通の「競合を保留中」表示と解決導線に入る。CSVは既存の保存キューを維持しつつ競合時だけ共通導線へ接続した。
- **長いノートでの文字入力を軽量化した。** 毎キー実行されていた全本文の変換・整形・目次再構築を入力イベントから外し、5,000行のノートでも入力処理は行数に依存しない（実測で性能基準の数千分の一）。日本語入力の変換中は下書き退避も待機し、確定後に1回だけ処理する。
- **右クリック・長押しメニューへ「行種変更」を追加した。** クリック位置の行を明示して、共通の行種メニュー（見出し1〜6・本文を含む15種）へ到達できる。見出しの右クリック・ハンドルからは「この見出しへのリンクをコピー」も使える。
- 既存ファイルへの照合なし全量上書きをサーバー側で警告記録するようにした（全経路の載せ替え確認後に拒否へ切り替える段階導入）。

検証: 保存契約34件・書込分類5件・入力性能検証・回帰ベースライン25件+関連回帰100件超全緑。右クリック行種変更・見出しリンクコピー・キャレット保持を実ブラウザで検証。

## v0.7.106 (2026-08-01)

### feat: 競合の「保留」を本物の保留に・行移動とツールバー行種選択を刷新（ノート計画 工程2-A/2-B・7〜8・10）

- **競合パネルを「保留」で閉じた後、他の場所を触ったり時間が経つだけで何度も再表示される無効ループを根治した。** 保留中はその文書のネットワーク保存を止めて端末内の下書き保護だけを続け、「競合を保留中」の表示から「確認する」を選んだ時だけ最新版を取得してパネルを1回開く。再読込・再起動後も保留状態を復元し、パネルを強制表示しない。
- **同じ内容の保存が競合として扱われる偽競合をサーバー側で無害化した。** 正規化後の内容が同じなら書き直さずに成功を返し、本当に内容が異なる時だけ競合として報告する（競合応答には照合用の情報を含め、競合後のフォーカスはユーザーが移ろうとした先へ戻す）。
- **行のドラッグハンドルを修復した。** 押した瞬間に対象ブロックをハイライトし、クリックで行種メニュー、ドラッグで挿入位置を明示、移動直後に確実に保存する（従来は再読込で移動が消えることがあった）。**Alt+Shift+↑/↓の行移動はフォーカス中の編集場所で動くようになり**、詳細パネルやエンティティ自由記述でも使える。
- **ツールバーの行種選択を、テーマの見た目見本付きメニューへ刷新した。** 見出し1〜6・本文が実際にどう表示されるかを確認しながら選べ、テーマ変更にも再読込なしで追従する。
- スラッシュメニューの選択状態の支援技術への通知を正しい方式（メニュー側の現在項目通知）へ修正した。

検証: 保存コーディネーター20件・回帰/契約ベースライン56件・関連既存回帰約270件全緑。競合保留・行移動・ツールバー選択・スラッシュメニューを実ブラウザE2Eで検証（PC/スマホ）。

## v0.7.105 (2026-08-01)

### feat: ノートの保存直列化・行種変換の刷新・見出しリンク作成（ノート計画 工程1/4〜6/11）

- **未変更のノートから他の場所へフォーカスを移しただけで保存が実行される問題を解消した**（保存0件を実機検証）。保存は文書単位で直列化され、自動保存と切り替え時保存が同じ処理へ合流して並走しない。詳細パネル内のノートも同じ文書として扱い、片方の保存結果へもう片方が追従する。
- **スラッシュメニューを刷新した。** 見出し1〜6・本文を含む15種類の行種を「現在行の変換」として実行する（従来は見出し1〜3のみで、カーソル行の前へ別の行を挿入していた）。検索・キーボード操作・タッチ対応の共通メニューで、今後ハンドル・右クリック・ツールバーからも同じメニューを使う。
- **行頭の記法変換を拡張した**: 「・」+スペース→箇条書き、数字+スペース→番号付きリスト（開始番号を保持）、h1〜h6+スペース→見出し。チェックリストのショートカットは本物のチェック項目を作るようになり、コールアウトのショートカットも追加した。日本語入力の変換中は誤発火しない。
- **リンク挿入に「このノートの見出し」を追加し、見出しへのリンクを作成できるようにした。** リンク先は本文に依存しない安定した識別子を使い、**前方にある同名見出しを改名しただけで既存リンクが壊れる不具合も修正した**。
- **単独ノート版でリンク挿入機能が丸ごと読み込まれていなかった問題を修正した**（Ctrl+K・右クリックのリンク挿入が動作していなかった）。

検証: 回帰・契約ベースライン52件+保存コーディネーター15件+関連既存回帰約180件全緑。スラッシュ変換・行頭記法・チェックリスト・見出しリンクを実ブラウザで操作検証。空行変換のキャレット消失など実装中に発見した3件も修正済み。

## v0.7.104 (2026-08-01)

### fix: フェーズB徹底チェックの残修正（移行の信頼性・検査の網羅性）

- **Cloud版で旧管理フォルダの削除に失敗しても「移行完了」と表示される問題を修正した。** 失敗を記録して次回接続時に再試行する（PC版と同じ動作へ統一）。あわせてCloud版の移行にも、保存後の内容一致確認を追加した。
- **移行バックアップ・競合バックアップの30日保持が実際には削除されず無期限だった問題を修正した。** デスクトップ・単独ボード・Cloud・ブラウザローカルの4経路すべてで、期限切れ分の掃除が起動・接続時のバックグラウンド保守として動く。
- 内部フォルダへの直接書込を検出する静的検査の見逃し2パターン（特殊な関数登録の書き方・除外リスト判定の誤発火）を改善し、検出器自体の単体テストを追加した。

検証: 関連6ファイル160件+周辺回帰101件全緑。

## v0.7.103 (2026-08-01)

### fix: フェーズB徹底チェックで検出した重大バグ群を修正

- **Dropbox接続時の内部スクリプト結合が壊れており、ファイル操作・注釈・版・ロックなど約50機能が無音で動かなくなる問題を修正した。** Cloud配布物側にも同じ破損が及んでいたことを実測で確認し、結合結果の構文を機械検査するテストを追加して再発を防止した。
- **PCとCloud版が同じファイルの編集中ロックを互いに検知できず、同時編集できてしまう問題を修正した。** ロックのパス形式不一致が原因で、実データ形式での双方向の衝突検証をテストへ追加した。
- **エクスプローラー等でファイルをコピーすると、コピー先で付けた注釈が元ファイルへ混入する問題を修正した。** 独立コピーを書き込み時に検出して自動で識別を分岐し、監査記録を残す。
- **起動直後の自動移行と注釈の操作が重なると、削除したはずの注釈が復活し得る問題を修正した**（移行がライブ操作を上書きしないことを楽観的排他制御で保証）。
- **移動・改名の直後にそのファイルを削除すると被参照警告が0件になる縁ケースを修正した**（資産IDでの照会を追加。画面側の完全対応は継続課題として記録）。**大きなフォルダの削除確認が表示まで最大8秒待たされる問題も、確認を即時表示して警告を非同期で差し替える方式へ変更して解消した。**
- 参照書き換えの後追い処理が失敗してもどこにも記録されず、リンク切れに気づけない問題へ失敗ログを追加した。

検証: 修正ごとの再現ケース(負の対照含む)+関連回帰約250件全緑。バンドル6系統の構文検査合格。

## v0.7.102 (2026-08-01)

### docs: ファイル参照整合性・削除警告・全ファイルバックリンクのマニュアル反映と品質ゲート

v0.7.097〜101で実装した「ファイルを移動・改名してもリンク切れしない」「参照されているファイルを削除しようとすると警告する」「フォルダで選んだどのファイルでもバックリンク（参照元一覧）を見られる」という3つの機能について、実装済みの挙動が今後の変更で壊れないよう検査を固定し、マニュアルへ反映した。

- **バックリンクのユーザーマニュアルを更新**: 画像・PDF・音声・動画等すべてのファイル種類で使えること、複数ファイル選択時のグループ表示、参照の索引が追いついていない場合に「参照0件」と決めつけない表示になることを説明した。
- **削除時の被参照警告のマニュアルを新設**: 参照されているファイルを削除しようとすると件数と代表的な参照元が表示されること、この警告は削除自体を止めるものではないこと、単独アプリの一部画面ではまだ未対応であることを明記した。
- **移動・改名時のリンク追従のマニュアルを新設**: Meldex内の操作だけでなく、Windowsエクスプローラーでの移動やDropbox上の他端末での移動にも追従すること、同名の別ファイルへ誤って繋がらないことなど、追従の限界も含めて説明した。
- 開発向け: 実装済みの挙動を記録していた回帰検査のうち、実際に解消された項目（参照の索引不足を隠さず報告する・削除系の主要操作が警告を表示する等）を、今後も維持されるべき検査へ格上げした。

検証: 契約テスト30件全緑（新規2件・更新2件を含む）、バージョン整合テスト緑。

## v0.7.101 (2026-08-01)

### feat: 削除前の被参照警告と、Meldex外のファイル移動への参照追従

- **ファイル・フォルダ・シートエントリを削除する際、他のファイルから参照されている場合は確認ダイアログに「N件のファイルから参照されています」の警告と参照元の内訳を表示するようにした。** フォルダは配下をまとめて集計し、参照の索引が不完全な場合はその旨も明示する（フォルダツリー・フォルダ表示・シート・ゴミ箱・Cloudモバイルの主要11経路。単独アプリの削除経路は今後対応）。
- **Explorer等のMeldex外でのファイル移動・コピー・削除や、Dropbox経由の他端末の変更にも、参照の索引が追従するようにした。** 「削除して同名の別ファイルを置いた」場合に、以前のファイルへの参照を新しいファイルへ誤って繋げないことを保証する。
- 内部実装: 削除影響の照会API新設、既存タグ復旧基盤の差分照合への参照追従の統合。

検証: 削除影響15件+外部追従9件+関連回帰70件超全緑。

## v0.7.100 (2026-08-01)

### feat: ファイル操作の参照追従を一元化し、全ファイルのバックリンク表示を追加

- **移動・改名・削除・復元・複製・アーカイブの主要11経路を、参照書き換え・参照索引・資産IDの追従を一貫して行う共通処理へ載せ替えた。** 実装過程で、フォルダ移動時に配下ファイルの参照元記録が孤立する・削除してもフォルダ分の記録が残る・復元と複製が索引し直されない、という3つの既存の取りこぼしを発見し解消した。
- **Cloud版のファイル移動時の本文書き換えが固定19種の参照キーしか見ていなかった問題を修正し、デスクトップ版と同じ47キーの統一台帳へ揃えた**（言語間の一致を機械照合テストで固定）。
- **フォルダパネルで選んだあらゆるファイル（画像・PDF・CSV等を含む）のバックリンク（参照元一覧）を、右パネルのバックリンクタブで表示できるようにした。** 参照元ごとの出現数と開く導線付き。索引が不完全な場合は「一部のファイルを解析できていません」と明示し、不完全な0件を確定した0件のように見せない。
- **前に開いたノートのバックリンクが、別のファイルを選んだ後も表示され続ける取り違えを解消した。** 選択対象の管理を単一の仕組みへ集約した。
- 一括削除時の内部処理を1接続へ集約し、大量削除の負荷を軽減した。

検証: 操作一貫性12件・言語間照合9件・バックリンクAPI 11件・選択追従12件+関連回帰490件超全緑。実ブラウザE2E（バックリンクパネルのPC/スマホ）合格。

## v0.7.099 (2026-08-01)

### fix: ファイル移動時にリンクが書き換えられない参照キーの非対称を解消

- **ファイル移動・改名時の参照書き換えと、バックリンク索引が、それぞれ別の参照キー一覧を使っていた問題を解消した。** 「索引には出るのに移動すると本文が書き換えられずリンク切れになる」キーが16種、「移動には追従するのに索引に出ない」キーが26種存在した。参照キー台帳を一本化し、両方向の非対称を解消した。
- **一覧形式で並ぶ参照（画像リストなど）が索引から漏れていた問題も修正した。**
- 参照の抽出・書き換えロジックを共通コーデックへ一元化した（動作は等価。今後の削除前警告・全ファイルバックリンクの土台）。

検証: コーデック26件（非対称解消のbefore/after回帰含む）+関連既存10ファイル176件全緑。

## v0.7.098 (2026-08-01)

### fix: Windows単独ボード版でも旧内部フォルダの自動移行が動くようにした

- **Windows単独ボード版の起動時・保存場所を開いた際に、旧版が保存場所へ作っていた内部フォルダ（ボード注釈など）の自動移行が実行されるようにした。** v0.7.095で実装した移行の仕組みが単独ボード版だけ接続されていなかった。移行はバックグラウンドで実行され、操作を妨げない。

検証: 単独ボード14件+移行エンジン16件全緑（起動時経路・保存場所選択経路・旧付随物なしの場合の無動作を確認）。

## v0.7.097 (2026-08-01)

### feat: ファイル参照の索引基盤を強化（参照グラフ・破損検出・パス正規化）

- **ファイル間参照の索引へ安定した資産IDを結び付け、「どのファイルがどのファイルを参照しているか」を照会できる参照グラフ基盤を追加した。** 今後の削除前警告・全ファイルバックリンク表示の土台となる。
- **壊れたファイルが「リンク0件」と黙って報告される問題を修正した。** 解析できなかったファイルは「不完全」として区別して記録し、索引がどこまで完全かを照会できるようにした。
- **表記の異なる同一パス（文字の正規化差）が別ファイル扱いになる問題を修正した。** パス解決の正規化を統一し、同一ファイルへ収束させる。
- 完全に同一のリンクが同じファイル内に複数回ある場合、2件目以降が索引から消えていた問題も修正した。

検証: 参照グラフ21件、参照整合性契約28件、資産ID・バックリンク・移動追従の周辺回帰9モジュール全緑。

## v0.7.096 (2026-08-01)

### docs: 保存場所の管理データ一元化についてマニュアルを整備、旧直接書込を防ぐ検査を追加

- **保存場所へ内部フォルダを作らなくなったこと（v0.7.094〜095の変更）を、ユーザー向けマニュアルへ反映した。** 注釈・ロックの新しい保存先、旧データの自動移行、30日間の移行バックアップ保管、ボード画像の埋め込み既定化とリンクを選ぶ方法・5MB超の確認手順を説明した（ファイル関連Q&A、よくある質問、旧版へのロールバック手順、別PCへのデータ移行、既知の不具合、ボードマニュアルを更新）。
- **今後、保存場所へ `_meldex` 等の内部フォルダへ書き込むコードが紛れ込んだ場合に検出する静的検査を追加した。** 共通の管理領域・移行の仕組み・意図的に残している書込（理由・対応予定つき）以外は機械的に検出して失敗させる。
- 内部実装: 今回の監査で、これまで方針が決まっていなかった書込（LLM APIキー保管、ベータ機能のログ、サンプル投入フラグ等）と、Windows単独ボード版で旧データの自動移行が起動経路に組み込まれていない問題を新たに発見した（対応は別途実施予定）。

検証: 新規の静的検査11件、既存の書込集合検査13件、関連回帰（共通ストレージ層78件・移行関連16件）全緑。

## v0.7.095 (2026-08-01)

### feat: 既存の内部フォルダを保存場所から安全に自動移行

- **ボードの注釈、Cloud版の注釈・閲覧ロック・編集ロックについて、既存データを保存場所から新しい管理領域へ自動的に移行するようにした。** 起動時（単独ボード、Cloud接続）にバックグラウンドで1回実行し、内容を照合できた分だけ、旧データを30日間の復旧用バックアップへ退避してから削除する。破損・権限不足・容量不足・同時編集・不明なファイルの混在では削除せず、次回起動時に安全に再試行する。
- **PC本体（デスクトップ版）が、Cloud版で新たに確保された編集中ロックも参照できるようにした。** 従来はCloud版側だけがPC本体のロックを見えており、逆方向は見えていなかった非対称を解消した。
- 内部実装: 共通管理ストレージへ移行専用の区分を追加し、進捗記録・冪等な再実行・中断からの安全な再開を実装した。

検証: 移行関連の新規テスト16件、ロック併読の新規テスト3件、共通ストレージ層・注釈・単独アプリの既存回帰135件全緑。

## v0.7.094 (2026-08-01)

### feat: 保存場所へ内部フォルダを作らない管理データ一元化（ローカル/Cloud）

- **ボードの注釈を、ユーザーの保存場所に作られる内部フォルダから、環境ごとのMeldex管理領域へ移した**（Windows単独ボード・ブラウザ・Cloudの各環境）。既存の注釈はそのまま読める（旧データの自動移行は次段階で実装）。
- **タグパネルの表示やタグ候補の参照など、閲覧するだけの操作で保存場所へ辞書フォルダや管理ファイルが新規作成される問題を解消した。** 実際に書き込みが必要な操作（タグ付与・辞書編集）の時だけ作成する。
- **Cloud版の閲覧ロック・編集ロック・注釈も管理領域へ移し、共有フォルダへの内部構造の露出を減らした。** 旧い場所のデータは読み取りで引き続き参照する。
- **ボードのカード画像・背景画像は埋め込み保存が既定になった**（5MB超は保存前にサイズを案内して埋め込み/リンクを選択）。既存のリンク画像は変換しない。
- 内部実装: 1000行を超過していたCloudファイル操作モジュールを責務別の4ファイルへ分割した。

検証: 書込集合検査13件（閲覧操作の書き込みゼロを機械検証）、Cloud移設13件、関連回帰約330件（ボード・タグ・注釈・ロック・単独アプリ）全緑。

## v0.7.093 (2026-08-01)

### feat: 固有形式ファイルへ安定した文書IDを追加

- **ボード、シナリオ、タイマー、スマートシート定義の4つの固有形式へ、保存時に文書ID（追加型メタデータ）を付与するようにした。** ファイルを開くだけでは書き込まず、名前変更・移動ではIDを維持し、複製・名前を付けて保存では新しいIDを発行する。デスクトップ本体、Windows単独版、ブラウザローカル、Cloud版の全保存経路で同じ規則を適用した。
- **旧版との互換性を維持した。** 既存フィールドは一切変更せず、旧版で開いて再保存してもIDを含むメタデータが失われないことを、実際の各保存処理を動かして検証した。
- 保存場所へ内部フォルダを作らない管理データ一元化（注釈・タグ・ロック等の移設）の土台となる変更。あわせて共通管理ストレージ層（デスクトップ／ブラウザ／Dropbox個人／共有ワークスペース）の未配線の基盤も同梱した。

検証: 文書ID 29件、単独ボード11件、共通ストレージ層100件(Python78+ブラウザ22)、関連回帰約200件(制作管理・セキュリティ・SQLite・単独アプリ・ファイルロック・検索索引ほか)全緑。分割スクリプトの再バンドルと構文検査済み。

## v0.7.092 (2026-08-01)

### fix: 徹底チェックで見つかった設定消失・書き込み競合の残存経路を修正

- **データ保護設定の保存・起動時の保護処理・バックアップ後の記録更新が、設定ファイルが破損して復旧もできない状態だと、他の全設定（保存先など）を消してファイル全体を上書きし得た問題を修正した。** 保存操作は理由を表示して中止し、起動時などの背景処理は記録の書き込みだけをスキップして保護処理自体（バックアップ・掃除）は続行する。実際に破損状態を作って全消失を再現したうえで遮断を確認した。
- **デスクトップ版のチーム情報・アイコン一覧の書き込みを再試行付きの安全な置換へ統一し、プロフィールに変更が無い起動では共有Dropbox上のチーム情報を書き換えないようにした。** Cloud版には対応済みだった「実変更がある時だけ書く」方式をデスクトップ版へも適用し、置換失敗時に直接上書きへ切り替わる危険なフォールバックを撤去した。
- 保存先切替時の検索索引の再構築が、データベース混雑などのエラーで気づかれないまま停止し得た問題を修正した（エラーを記録して自然に見えるようにした）。
- クイックメモ、単独アプリのタグ、設定の書き出し・取り込み前バックアップの3経路が、設定ファイル破損時のバックアップからの自動復旧の恩恵を受けられていなかったのを統一した。

検証: 破損状態での保存中止（409・ファイル無変更）、背景経路の記録スキップ、無変更同期の書き込みゼロを実行ベースで検証。関連回帰テスト約150件全緑。

## v0.7.091 (2026-08-01)

### fix: Dropbox上の管理ファイル書き込みを同期処理との競合に強くした

- **Dropbox配下の管理ファイル（編集中ロック、カレンダー共有スナップショット、OS標準タグ同期、Notion同期、競合コピー解消、共有ワークスペース情報）の書き換えを、共通の再試行付き安全置換へ統一した。** Dropbox同期やウイルススキャンがファイルを掴んでいる瞬間に操作全体が失敗する問題（過去にフォルダ移動失敗の実害を確認）への対策を、対応済みだった編集中ロック以外の全対象へ横展開した。再試行の有無・回数がファイルごとにばらばらだった状態を解消した。
- **起動時のプロフィール同期で、名前・アイコンなどに変更が無い場合は共有Dropbox上のチーム情報ファイルを書き換えないようにした。** 従来は閲覧するだけの起動でも一定時間の経過を理由に書き込みが発生し、Dropbox競合コピー多発の再発候補になっていた。
- 共有ワークスペース情報ファイルの書き込みを一時ファイル経由の安全な置換に変更した（従来は直接上書きで、途中失敗時に破損し得た）。

検証: 共通ヘルパー新規7件、適用各箇所の再試行回帰、プロフィール無変更時の書き込みゼロと変更時の書き込み実行のNode実行検証を追加。関連既存テスト計210件超が全緑。

## v0.7.090 (2026-08-01)

### fix: 自動タグ付け設定の保存も安全な設定書き込みへ統一

- **自動タグ付け設定の保存を、v0.7.089で導入した安全な設定書き込み（直列化・バックアップ・自動復旧・再試行）へ統一した。** 従来は独自の書き込みと独自のロックを使っており、他の設定保存と重なった際に更新が取りこぼされる可能性に加え、設定ファイルが一時的に読めない瞬間に保存すると他の設定（保存先など）を消してファイル全体を上書きし得る経路が残っていた。
- 設定ファイルが破損していて自動復旧もできない場合、自動タグ付け設定の保存は中止してエラーを返す（壊れた土台でファイル全体を上書きしない）。読み込み側は既定設定で動作を継続する。

検証: 迂回書き込みの機械検査を8ファイルへ拡大し全緑。自動タグ付け関連の既存回帰（内容レーティング、ジョブ中止、タグリセット、辞書）全緑。

## v0.7.089 (2026-08-01)

### fix: 設定ファイルの書き込みを安全化（破損による保存先消失の防止）

- **Meldexの設定ファイルが、複数の保存処理が重なった際に壊れて全機能が使えなくなることがある問題を修正した。** 書き込みを一時ファイル経由の安全な置換へ変更し、同時書き込みを直列化した。初回セットアップ直後に保存先設定が消えて全操作がエラーになる実害が確認されていた。
- **書き込み前に直前の正常な設定を自動でバックアップし、設定ファイルの破損を検出した場合はバックアップから自動復旧するようにした。** 復旧できない場合のみ明示的なエラーとして扱う。
- Dropbox同期やウイルススキャンがファイルを掴んでいる瞬間の書き込み失敗には、短い再試行で対応する。
- 設定ファイルへ直接書き込んでいた7系統（保存、同意、データ保護、設定インポート、AI利用予算、共同作業、デバッグ）をすべて新しい安全な書き込みへ統一した。調査で見つかった自動タグ付け設定の1系統は別途対応する。

検証: 新規テスト12件（並行書き込みの破損再現、バックアップ復旧、アトミック性、迂回書き込みの機械検査）、影響範囲の既存回帰299件、Python構文・分割読み込みの完全ロード確認に合格。

## v0.7.088 (2026-08-01)

### fix: 起動後の検索索引処理がフォルダ読込・保存を35秒待たせる問題を修正

- **起動後のバックグラウンド検索索引（意味検索用の埋め込み計算）が、フォルダ読込やシート表示設定の保存を最大35秒待たせて「通信に失敗しました」になる問題を修正した。** 埋め込み計算を16件ずつの小さな単位に分割し、各単位の後で画面操作からの要求へ処理を譲るようにした。従来は数千件たまった計算を譲らずに連続実行していた。
- **埋め込み計算が使うCPUスレッド数を「論理コア数の半分・最大4」に制限し、索引処理中も画面操作の応答性を保つようにした。** 検索時の1件だけの埋め込み計算は影響を受けない（実測13〜15ms）。
- 単一ファイルの増分更新経路では、SQLiteのセーブポイント保護と両立するよう、処理を譲る際に途中確定（コミット）を行わない。

検証: 検索索引32件（新規8件: 譲りの検証、セーブポイント保護、フォアグラウンド活動中のブロックと解除後の完了を実スレッドで決定的に検証、スレッド数計算）、埋め込み診断6件、バックグラウンドI/O 14件、関連消費側56件、Python構文、実モデル手動確認（24論理コア環境でスレッド4適用・36チャンク索引完走）に合格。

## v0.7.087 (2026-07-31)

### change: 私用の日本語タグ辞書を同梱プリセットから分離

- **大規模な日本語タグ辞書をMeldex本体・Cloud・サンプル・通常UIから撤去し、利用者のPCにある単一の読取専用CSVをローカル画像AIの推論時だけ参照する方式へ変更した。** CSVはユーザーデータ領域のSQLite完全一致索引へ遅延変換され、ソースフォルダ、ホーム、ワークスペースごとの全件コピーは作らない。
- **通常のタグ辞書で同じ英語別名を編集している場合は利用者の正式名を優先し、私用辞書から一致したタグだけを通常タグとして保存する。** 私用プリセットの名称、識別子、ファイルパスは設定、タグパネル、ジョブ結果、公開APIへ出さない。
- 旧版で導入済みだった大規模コピーをバックアップ後に縮退し、未使用の自動生成タグだけを削除した。標準等と共有するタグ、編集済みタグ、付与済みタグは保持し、廃止したプリセット所属だけを除去した。再実行時の変更0件と、既存の付与IDが全件残ることを確認した。
- 同梱CSV、翻訳シード、固有ライセンスファイル、公開説明、固有E2Eフィクスチャを削除し、Desktop／Cloud配布検査へ私用データと固有UI文字列の混入ブロックを追加した。

検証: 私用レジストリ、SHA-256整合、SQLite索引再利用、英語タグの日本語解決、通常辞書優先、移行dry-run／バックアップ／冪等性／付与保持、標準プリセット、タグ管理API、配布衛生検査を自動確認。

## v0.7.086 (2026-07-31)

### fix: CLIチャットのファイル操作で「現在の対象」の既定範囲が効かない問題を修正

- **CLIチャット経由のMeldexファイル操作（読み取り・作成・編集など）で、AIが作業フォルダ指定を省略すると「アクセス範囲外」と誤判定されることがある問題を修正した。** チャット開始時に選んだ対象フォルダ・作業フォルダの情報がツール実行側まで届いていなかったため、AIが毎回明示しない限り既定の参照範囲に反映されなかった。今回から、チャットに設定した現在の対象がそのまま既定範囲として引き継がれる。
- 対象は Claude Code / Codex / Gemini CLI の全プロバイダと、クロスレビュー・ワークスペースCLI連携。『』などの全角記号を含むフォルダ名・ファイル名でも劣化なく引き継がれることを回帰テストで固定した。
- これにより読み取り・書き込みの既定範囲が通常チャットと同じ基準（現在の作業フォルダ優先）に統一され、作業フォルダ設定時の書き込み既定範囲は従来より安全側になった。AIが範囲を広げたい場合の明示指定（参照範囲の指定）は従来どおり有効。

## v0.7.085 (2026-07-31)

### feat: ボードの情報・整列・表示・注釈・貼り付け連携を改善

- **ボードのオプションパネルへ、フォルダパネルと共通の「情報」タブを追加した。** ボード上で選択した画像やリンク先ファイルの情報を表示し、ファイルに対する共通タグ操作を利用できる。ボードカード自体へのタグ付けは撤去し、カード追加・選択時にタグ集計が走らない構造へ戻した。
- **「自動整列（サイズ）」を追加し、カードの幅と高さを揃えてから自動整列するようにした。** 不要な「面積を揃える」を廃止し、画像カードには表示サイズを元画像サイズへ戻す操作を追加した。
- **カードや画像の上から開始した注釈ストロークと塗りつぶしを、その対象の移動・回転へ追従させた。** ボード全体を回転した場合も付箋のしっぽが正しい角度を保つよう、座標変換と角度計算を統一した。
- **画像、HTML、各種リンクカードを、メインパネル、フロートパネル、右サイドバーから選んで開けるようにした。** ダブルクリック時の既定表示先を設定可能にし、初期値をフロートパネルとした。メインパネルで開いた後は戻るショートカットとマウスの戻るボタンでボードへ戻れる。通常のクリックやドラッグだけではフロートパネルを開かない。
- **リンクカードのファイル種別アイコンを背景色に左右されないテーマ色へ統一し、右端へリンク先を開くボタンを追加した。**
- **シートの表、ギャラリー、カンバン、タイムライン、ツリー、フォルダツリーから、エントリをボードへD&Dしてリンクカードを作れるようにした。** 新旧シート形式の論理パスを保ち、表の複数選択は一回のD&Dで全件を追加する。
- **ボードの貼り付けをMeldex内外で共通化し、テキスト、URL、HTML、画像、動画、音声、PDF、一般ファイル、フォルダ、シートエントリを内容に合うカードへ変換するようにした。** 入力欄の通常貼り付けは維持し、複数項目は一回のUndo単位で差分追加する。大量ファイルは3件ずつ処理し、既存カード全体の再描画や常時ポインター監視を追加していない。

検証: ボード関連ユニット73件、JavaScript構文・読込順・分割スクリプト整合、Desktop／390pxの実ブラウザD&D・貼り付けE2E、Cloud staticのDesktop／390px、単独ボードのDesktop／820px／390pxに合格。シートエントリ、テキスト、URL、OS画像の保存値、Undo単位、差分描画、危険URL除外、別ボード切替時の中止経路を確認した。

## v0.7.084 (2026-07-31)

### feat: CSV編集、ボード集中表示、単独アプリ保存保護を統合

- **CSVをテキスト・数値・Meldex数式に限定した本格編集モードへ刷新した。** RFC 4180相当の引用符・セル内改行・空行・末尾空セル、UTF-8 BOM／UTF-8／CP932、矩形編集、行列操作、検索置換、複数列フィルター、複数キーソート、固定列、Undo／Redo、10,000行超の仮想描画に対応した。
- **CSVから新しいMeldexシートを作る共通変換を、本体・Windows単独版・Cloud/PWAへ追加した。** 見出し・項目名列・列タイプ・数式を事前確認し、同名時は別名作成、明示した時だけ既存シートへ追加する。全行検証、ETag競合保護、途中失敗時のロールバックを行い、元CSVは変更しない。
- CSVの列タイプ・表示設定を`.meldex/csv/`へ保存し、CSVまたは親フォルダーの名前変更・移動・複製・削除・復元へローカル／Cloudの両方で追従させた。既存CSVのBOM、区切り文字、改行形式も維持する。
- **ボードの上端・下端・右端を初期状態で自動的に隠し、端へのポインター移動、タッチハンドル、端スワイプで一時表示する集中表示を追加した。** 一時表示で座標・ズーム・パンを動かさず、D&D、IME、リサイズ、メニュー操作中は収納しない。空ボードにはカード追加と画像D&Dの短いガイドを表示する。
- **Windows版7アプリとCloud/PWA版6アプリで、共通ツールチップ、右サイドバー、自動保存状態、未保存終了保護を統一した。** 未命名ファイルは端末下書きを継続保存しつつ最終保存先を促し、保存失敗時は閉じない。Windowsのネイティブ終了、ブラウザーの終了警告、PWA再起動復元へ同じ終了契約を接続した。

検証: JS／Python構文、対象回帰97件、10,001行CSV実ブラウザー、単独アプリDesktop／tablet／mobile、Cloud/PWA 6アプリ・オフライン復元・ETag競合・静的979資産に合格。全体回帰は4,303件を完走し、今回対象の新規回帰0件（残存は着手前と同じ既存8失敗・1エラー）。

## v0.7.083 (2026-07-31)

### fix: 連続したエントリ削除の失敗と待ち時間表示を修正

- **単独削除・一括削除・エントリ追加の取り消し・一括削除のやり直しを、シートごとに1件ずつ処理する共通削除キューへ統合した。** 連続操作でもSQLiteへの削除が並列にならず、同じエントリの重複要求と、削除中の保存・改名を止める。
- **削除を確定すると対象行をすぐに非表示にし、下部へ `削除中…（残りN件）` を表示するようにした。** すべての要求が終わった時だけ正本を1回再読込し、失敗した行だけ元の位置・選択状態へ戻す。処理中に閉じたタブや破棄された埋め込みシートは開き直さない。
- **SQLiteが最大30秒待つ一方で画面が15秒で通信失敗にしていたため、実際は削除済みでも失敗表示になる競合を修正した。** 削除通信を60秒・共通通信エラー通知なしにし、応答だけ失われた場合は `削除結果を確認中…` へ切り替え、同じ操作IDの再送と正本照合で成功へ収束させる。
- 削除APIへ安定ID照合、同一操作IDの成功結果再返却、シート単位の直列化を追加した。Dropboxなどの一時ロックは短く再試行し、ゴミ箱移動または復元情報の確定前に失敗した場合は元ファイルとSQLiteを戻す。確定後のタグ・一覧索引・監査の失敗は削除失敗へ誤変換せず警告として返す。
- **徹底チェックで、IDあり／なしの重複要求、表示フィルター中の正本照合、再読込時の表示例外、一括削除Undoの部分失敗を追加修正した。** 重複通信やフィルターによる成功誤判定を防ぎ、削除サイクル終了後は保存・改名の停止状態を必ず解除する。一括削除の復元に一部失敗した場合は成功済み項目を再度復元せず、失敗分だけを再試行できる。SQLite削除は1回最大30秒の待機を外側で4回繰り返して最悪約120秒になり得たため、削除時だけ1回3秒の短い待機へ変更した。SQLiteだけのエントリが確定前に失敗した場合は、先行して更新したカレンダー連動も元へ戻す。

検証: 削除コーディネーター、安定ID競合、同一操作再送、SQLite／ファイル一時ロック、途中失敗ロールバック、表示フィルター中の正本照合、通常／埋め込みシート、閉じたパネル、Undo／Redoの部分失敗再試行に合格。Chrome実ブラウザE2Eでデスクトップ／390pxタッチ幅の連続・一括削除、最大同時通信1、即時非表示、残件更新、最終再読込1回、失敗行復元、応答消失後の成功収束を確認した。

## v0.7.082 (2026-07-31)

### fix: 固定列の範囲とアクティブ枠を修正

- **列メニューで「固定する」を選ぶと、現在の表示順を保ったまま、表の左端から選んだ列までを連続して固定するようにした。** 作品名列を途中へ移動した場合や、固定後に列を並べ替えた場合も、固定範囲の途中へスクロールする列が入り込まない。
- **固定中の列で、アクティブセルを示すハイライト枠の上辺・左辺などが隠れる問題を修正した。** 枠をセルの内側4辺へ描画し、固定列の重なり順と全体フォーカス表示に上書きされないようにした。
- 固定範囲の途中で「固定しない」を選ぶと、その列から右側をまとめて解除する。旧版で保存された飛び飛びの固定設定も、最も右の固定列を終端とする連続範囲として表示する。

検証: 関連自動回帰108件、Chrome実ブラウザE2E（デスクトップ／タッチ対応タブレットの固定・途中解除・列D&D・横スクロール・アクティブ枠、キーボードセル移動、ツリービュー）、分割JavaScript構文、生成bundle整合、差分整合に合格。

## v0.7.081 (2026-07-31)

### fix: 最近の更新内容の横断整合性を修正

- **ボード単独アプリのカード追加・移動・ライン追加などの変更を、端末下書き保存へ接続した。** 未接続でボードを編集した後に再読込しても内容を復元でき、接続後の自動送信へ引き継げるようにした。
- **最近追加された単独アプリUIとCloud初回起動処理へ、回帰テストの前提を同期した。** 端末下書き初期化と、Dropbox未接続時の背景読込を無通知にする契約をテストでも正しく扱う。
- UI追加後のチェックボックス監査マニフェストを再生成し、未分類0件の状態へ戻した。

検証: 単独アプリ／共通タグ／Quick Memo 143件、通常シート／埋込スケジュール125件、タグ／資産ID／復旧188件、Cloud／フォルダ／チャット／CLI 272件に合格。スマートフォン6アプリ、タブレット、配布ページ、Cloudファイル操作、ノートとボードのオフライン復元を実ブラウザで確認した。

## v0.7.080 (2026-07-31)

### fix: 公開Cloudの初回起動を高速化

- **公開Cloud本体の324本のclassic scriptを、Cloud静的ビルド時だけ8本の順序付きbundleへまとめた。** 実行時設定を含む読込は合計9本となり、ソースHTMLとデスクトップ版の読込構造、inline script境界、既存の分割スクリプト、単独アプリ向け個別資産は維持した。
- **生成bundleへ内容SHA-256の指紋を付け、Service Workerのimmutable cacheと公開manifestへ接続した。** 元スクリプトの参照順・収録漏れ・重複、split-loader残存、実ファイルとURL指紋の不一致、古い生成bundleの残留をCloudビルド検査で失敗扱いにした。
- **Dropbox未接続の初回画面で、背景のワークスペース／チャット対象準備が「操作する権限がありません」を表示する問題を修正した。** 背景読込だけを無通知にし、設定画面やユーザー操作からの実エラー通知は維持した。
- キャッシュなし、各JavaScript要求へ250msの遅延を加えた決定的E2Eを追加し、390×844、820×1180、1400×900で「操作可能になるまで10秒以内」、横はみ出し0、起動エラー0を継続検査できるようにした。

検証: Cloud静的ビルドと配布完全性、生成bundleのJavaScript構文、主要Cloud E2E 4件、ワークスペース回帰3件、遅延付き初回起動E2E 3端末に合格。遅延付き計測はスマートフォン4.023秒、タブレット3.748秒、デスクトップ3.816秒（変更前の公開v0.7.071は約103.4秒）。GitHub Pages本番のキャッシュなし実測は公開反映後に実施する。

## v0.7.079 (2026-07-30)

### feat: 単独アプリのUI・タグ・端末保存を全環境で統一

- **Windows単独版7種とCloud/PWA版6種のUI契約をMeldex本体へ揃えた。** 各アプリの専用アイコンとLucide操作アイコン、最上段の編集可能タイトル、共通タグ辞書、対象タグ行、同期状態を統一した。Viewerも専用アイコン、Lucide操作、画像タグ行へ対応した。
- **スマートフォンでは主作業領域を優先し、オプション/右サイドバーを初期状態で閉じるドロワーへ変更した。** 左下のMeldexフォルダ用フロートボタンを廃止し、全アプリのハンバーガーメニューへ「Meldexファイル」を配置した。ボードのカード/ライン追加、シナリオのページ/コマ列切替など中核操作を44px以上の操作面で維持した。
- **シート単独アプリを通常シート専用として固定した。** `.mel-sheet` を通常シート、`.smart-db.json` をスマートシートとして明確に分離し、探索・タイトル変更・メニュー・アイコンへスマートシート契約が混入しないようにした。
- **Quick MemoのPWA/Cloud UIを同一化し、無反応になっていたメニュー、履歴、タグ、入力モード、描画、録音、Undo/Redoを再接続した。** 常設接続行、重複タイトル欄、青い保存ボタンを撤去し、タグ行とモード固有ツールをコンパクトな一行へ整理した。タグは共通辞書の安定IDと色を保持する。
- **全編集アプリへIndexedDBの端末内下書きと順序付き送信待ちキューを導入した。** 文書ID、保存先、base revision/ETag、アカウント/ルート文脈、操作IDを保存し、接続/認証復帰後に順次再送する。競合、権限、ロック、容量、保存先変更では自動上書きを止め、remote acknowledgement前にキューを消さない。保存先表示と「端末に保存済み・同期待ち」も分離した。
- 可視操作の監査正本 `standalone-operation-audit.json` と全環境監査ノートを追加し、旧HTML/新JSの混在、廃止済みフロートボタン、横あふれ、44px未満の操作面を配布検査で失敗扱いにした。

検証: `test_standalone_apps.py` 30件、`test_meldex_standalone_distribution.py` 16件、Quick Memo回帰18件、Desktop/Tablet/Mobileの表面操作E2E全件、Cloud静的961ファイルの6アプリ起動・Dropbox操作・ETag競合保護・端末下書き/送信待ち再読込E2E、JavaScript構文検査に合格。実iOS/AndroidのPrivate Browsing、実Dropbox複数端末、完成Windows EXEは公開前の手動リリースゲートとして未実施。

## v0.7.078 (2026-07-30)

### fix: スケジュール埋め込みシートと列設定の保存整合性を修正

- **スケジュール内の作品リストなどでエントリ名やセルを編集した際、HTTP 404が出る、画面だけ編集前へ戻る、通常のシート画面へ切り替わる問題を根本修正した。** 埋め込みシートの所有コンテキストと非同期処理の世代を固定し、再読込、エラー復旧、Undo/Redoが通常シートの選択DB、表示、履歴、DOM、フォーカスへ脱落しないようにした。
- **エントリの安定ID、改名前後のパス対応、ID単位の保存キューを共通化した。** 名前セル、右クリック、検索・置換、自動改名、Undo/Redo、制作管理、Dropboxのすべての改名入口を同じcoordinatorへ接続し、保留中のセル保存も実行直前に最新パスへ解決する。応答喪失時は同じ操作IDで保存結果を照合し、保存済みの変更を画面だけ巻き戻さない。
- **列ヘッダー改名をローカル／Dropbox共通の原子的なスキーマ変更へ統合した。** 安定した列IDとスキーマrevisionを使い、値、型設定、数式、並び替え、検証条件、カレンダー割当、同一・別シートのリレーション、マルチソースリレーション、ロールアップ、カスケード、双方向参照を一括で追従させる。
- **セレクト／マルチセレクトの候補名と採用状況ステータスの変更を、使用中セル値の移行と一体化した。** 使用中候補の削除で孤児値を黙って作らず、保存または復旧に失敗した場合も一覧・セル・履歴を確定状態へ戻す。共通タグはタグIDを維持し、タグ作成と割当の部分失敗を補償する。
- リレーションの候補作成・双方向反映・カスケード更新、ロールアップ再計算、URLと通常セル保存を同じ安定ID契約へ接続した。数値、ステータス、ユーザー、セレクト、共通タグ、リレーション、集計、列メニューの候補UIもペイン単位に隔離し、通常シートと埋め込みシートが互いの編集中UIを閉じない。
- 正本保存後の監査ログやDropboxミラー更新失敗を保存失敗として返す経路、分割スクリプトの重複関数によって埋め込み対応のコンテキスト解決が上書きされる経路、空シートの列名重複、数式の空白付き列参照、候補移行の復旧失敗が隠れる経路も徹底チェックで修正した。

検証: 新規同一性・スキーマ整合性契約23件を含む関連自動回帰404件＋サブテスト28件、Chrome決定的E2E 12件（セル保存、型別エディター、全プロパティ型、リレーション、ロールアップ、候補色、制作タスクリストのDesktop／Mobile、列表示順）、Python／分割JavaScript構文、差分整合に合格。実利用データとDropbox正本は変更せず、リポジトリ内フィクスチャとモック通信で検証した。制作管理ワークスペース全体の追加E2Eでは、本修正対象の埋め込み切替・各管理シート確認後、別機能「かんたん割当」のTabフォーカストラップに既存問題を検出し、別P2として記録した。Desktop／Cloudのソースコピーは同期済み。完成済みEXE、配布用Python一式、公開環境変数、HTTPS公開URLを要する最終リリースゲートは未実施。

## v0.7.077 (2026-07-30)

### fix: フォルダ・タグ・シート・チャット・CLIの操作を統一

- **フォルダパネルで複数ファイルを選んだ情報タブは、種類、フォルダ、日時、ファイルサイズ、埋め込み情報、タグなど、全選択ファイルで値が同一の項目だけを読み取り専用で表示するようにした。** 詳細取得の失敗時は未確認値を共通扱いせず、選択変更後に古い応答が戻っても現在の表示を上書きしない。単一選択・複数選択のフォルダリンクには、Desktop版で同じフォルダをOSのExplorerから開く操作を追加した。
- **ポップアップ外周の明るいグレーを共通トークンへ集約した。** フォルダメニュー、コンテキストメニュー、書式設定、カラーパレット、列フィルター、コマンドパレット、検索・置換、ツールチップ、単独版メニューなどを同じ境界色へ接続した。検索・置換バーは入力面とボタン面、フォーカスリング、押下状態を区別し、検索語・置換語入力欄のツールチップを撤去した。
- **タグツリーの各グループへ、現在のフォルダまたは選択ファイルに付いているタグ種類数とグループ内総数を `付与済み/総数` で表示し、分子をアクセント色にした。** グループごとの目のボタンで、メインパネルのファイルカードとシートに表示するタグをソースフォルダ単位で切り替えられる。件数更新ではツリー行を作り直さず、非同期取得中・失敗時に未確認値を確定値として表示しない。
- **タグ表示数をフォルダカードとシートで分離した。** フォルダは表示ポップアップ、シートは折り返し設定から個別に設定し、タグツリーから旧共通設定を撤去した。情報タブのタグ文字サイズはファイルカードと同じ小型タグ表示へ統一した。
- **シートのリレーション候補値へ常時到達できる「…」操作を追加し、「＋」と重ならない配置へ修正した。** 候補値の削除メニューをキーボードでも操作でき、スマートフォンでは両方に44pxの操作面を確保した。候補値を編集した直後のロールアップも通常描画と同じ値別改行・リレーション行揃えを使い、古い非同期再計算結果を破棄する。
- **通常LLM、チーム、CLIの会話を複数発言にまたがってドラッグ選択し、選択した本文だけを発言日時・発言者名付きのテキストとしてコピーできるようにした。** 中間の発言は全文、先頭・末尾は実際の選択部分だけを含め、ボタン、内部思考、ツール表示、非表示要素は混ぜない。開発用エントリでも本番と同じチャット描画スクリプトを読み込むようにし、開発時だけ会話履歴コピーが欠落する不整合も解消した。
- **Claude Code / Codex CLIの認証切れを専用の `cli_auth_required` として扱い、Meldexが実際に使用するCLIパス、バージョン、認証状態の確認、可視ターミナルでの再ログイン導線を追加した。** owner権限かつ同じPCからの要求だけを許可し、状態確認・ログインの双方でLLM APIキーを子プロセスから除去する。CLI失敗時にMeldexの保存済みAPIキーや従量課金APIへ自動切替せず、ログイン後も質問を自動再送信しない。
- エクスプローラー、タグ、シート、オプションパネル、チャット、CLI連携の各ユーザーマニュアルを新しい操作へ同期し、公開配布ページのフォールバック版表記も0.7.077へ揃えた。

検証: 最終差分を含む関連自動回帰195件、Chrome決定的E2E 9件（シート、タグのPC／タブレット／スマートフォン、会話履歴コピーのPC／スマートフォン、ポップアップ）、Python／分割JavaScript構文、セキュリティ静的ゲート、差分整合に合格。全体回帰4,233件で検出した42件は再分類し、17件の対象内修正・静的契約更新と1件の一時的な揺れを解消した。残る24件は `app/Meldex`・`app/MeldexCloud` の配布用コピー未同期であり、配布ビルドを実行する次工程で解消する。Claude Code／Codex CLIの状態確認とバージョン取得は、APIキーを除去した環境で終了コード0。モデル問い合わせ・質問送信・ログイン画面起動は未実施。実iOS／Android、実Mac、実Dropbox共同編集、実際の期限切れ認証更新は `manual-required`。

## v0.7.076 (2026-07-30)

### feat: タグツリー高速化・ファイル移動追従・復旧基盤を追加

- **同じソースフォルダ、ホームフォルダ、ワークスペース内で表示フォルダや選択ファイルを切り替えても、タグ辞書とタグツリー全体を読み直さないようにした。** タグツリーの構造、開閉状態、検索、選択中ファイルのチェック状態を分離し、グループ開閉と絞り込みは表示中の差分だけを更新する。8,609件の辞書、深い階層、連続開閉、PC・タブレット・スマートフォンで性能予算を検証した。
- **タグ割り当てをファイルの場所ではなく安定したファイルIDへ接続し、Explorer、Finder、CLIP STUDIO PAINT、BlenderなどMeldex外の移動・コピー・削除へ追従できる復旧基盤を追加した。** DesktopはOSのファイル実体ID、CloudはDropbox IDを優先し、移動は同じファイル、コピーは別ファイル、同じ場所への別ファイル配置はタグを引き継がない。旧パス形式は再開可能に移行し、互換投影とロールバックを当面維持する。
- **設定 → データベースへ「タグのメンテナンス」を追加した。** 明示実行の差分照合、確認候補、移動・コピー・削除・復元履歴、失敗した追従の再試行、タグ索引の確認・再構築、任意の外部変更監視を提供する。通常起動、フォルダ切替、タグ操作では全走査、内容ハッシュ、復旧台帳全件読込、Dropbox追加要求を行わない。
- **JPEG、PNG、APNG、WebPへ任意の復旧IDを安全に追加し、タグ情報JSONと一緒に別のソースフォルダ、ワークスペース、利用者へ受け渡せるようにした。** 通常のタグ付けではファイルを書き換えず、明示実行時も原本バックアップ、原子的置換、既存メタデータ・画像内容の再検証を行う。署名付き、読取専用、破損、未対応形式は変更せず、他のすべてのファイル形式も通常タグと復旧台帳を利用できる。
- **タグまたはタググループの追加欄から、現在の辞書と外部タグカタログをまとめて検索できるようにした。** 外部カタログ全件をタグツリーへ展開せず、2文字以上またはIME確定後に候補だけを遅延検索する。選んだタグ／グループと必要な祖先だけを原子的に実体化し、ページ送り、同名別ID、説明ツールチップ、スマートフォン長押し、オフライン、閲覧者、競合へ対応した。Cloud版は私用CSVを同梱せず、Dropboxの文字別検索シャードを必要時に1個だけ読む。
- NAS切断やクラウドプレースホルダーの未取得時は、未確認ファイルを削除扱いにせず差分照合を一時停止し、接続回復後に同じ場所から再開する。Windowsのタグ辞書原子的置換は、高頻度の並行読込中も短い共有解除を拾える再試行へ強化した。
- タグと設定のユーザーマニュアルへ、候補検索、外部タグカタログ、Meldex外の移動後の復旧、対応形式、復旧ID、別利用者へのタグ受け渡し、Desktop／Cloudの能力差を追記した。

検証: Phase 0〜7の統合回帰253件、Cloud公開準備18件、タグ候補／タグメンテナンスのChrome実ブラウザE2E 6件、Cloud単独アプリ6種のスマートフォン・タブレット・Desktop配布、10,000件外部候補検索、200世代Windows CAS並行読込5回、Python／JavaScript構文、差分整合、秘密情報・私用パス検査に合格。実Dropbox共同編集、実iPad Safari、実Mac Safari／Finder、実Android、実CLIP STUDIO PAINT、実Blender、実NAS／クラウドプレースホルダーは `manual-required` として実機確認待ち。

### fix: Ctrl＋Fとメインパネルの検索・置換を統一

- **メインパネル編集中の Ctrl＋F が、表示中のノート、シナリオ、シート、ボード、フォルダパネルそれぞれの「検索と置換」と同じUIを開くようにした。** 検索専用表示へ切り替わったり、繰り返し押下で閉じたりせず、検索入力へフォーカスする。
- **フォルダパネルの検索・置換を、フォルダツリー上部ではなくメインパネル右上ツールバー直下へ表示するようにした。** ポップアップはツールバー右端へ揃え、UI倍率、狭いパネル、画面サイズ変更に合わせて幅と位置を補正し、画面外への見切れを防ぐ。
- Escapeまたは閉じる操作でポップアップをフォルダツリー内の元の位置へ戻し、ツリー全文検索の範囲・置換表示を復元する。虫眼鏡から開いた場合は虫眼鏡へ、編集中のCtrl＋Fから開いた場合は元の編集位置へフォーカスを戻す。表示中のパネルが切り替わった場合も位置監視とメインパネル専用状態を破棄する。

検証: 関連静的回帰27件、Chrome実ブラウザE2E（デスクトップ、横向きタブレット、スマートフォン、ノート、シート、ボード、シナリオ）、JavaScript結合構文、差分整合に合格。フォルダツリー回帰は既存のテスト抽出不整合1件を除く32件が合格。

## v0.7.075 (2026-07-29)

### feat: ツリービューの列ヘッダー操作をテーブルと統一

- **ツリービューのヘッダーで、列境界のドラッグ／キーボードによる幅変更、データ列のドラッグ並べ替え、横端自動スクロールに対応した。** 階層列は常に先頭へ残し、移動・非表示を禁止する。列幅・列順・固定・非表示・フィルター・並び替えは保存済みビューごとに独立して保持する。
- **ヘッダーの「…」、右クリック、長押しから、テーブルビューと共通の列メニューを開けるようにした。** 列名、種類、挿入、フィルター、並び替え、幅、固定、表示、条件付きカラー、折り返し、編集制限、削除を提供し、階層と競合するグループ化はツリービューでは表示しない。
- 列ソートは同じ親を持つ行の間だけへ適用し、親子関係と検索時の祖先表示を維持する。列ソート中も親子変更は許可し、行の前後移動には「階層の並び順」へ戻す案内を表示する。
- 固定列、列フィルター、折り返し、条件付きカラーをツリー行へ反映し、列幅変更中はヘッダー・表示中の仮想行・固定境界を同じグリッド定義で更新する。列名変更・削除時はツリー設定と列表示設定の参照も追従する。
- ツリーヘッダーを独立モジュールへ分離し、デスクトップ版、Cloud静的版、シート単独版で同じ実装を読み込むようにした。

検証: ツリー関連単体テスト12件、9,001件仮想描画を含むChrome E2E、シート単独版のdesktop／tablet／mobile検証、既存テーブルの折り返し・長押し・列D&D・固定・列フィルター回帰、Python／JavaScript構文、差分整合・秘密情報検査に合格。

## v0.7.074 (2026-07-29)

### fix: タグツリーの構造・並べ替え・検索を仕上げ

- **右レールのタグパネル直下へ「タグツリー」「自動タグ付け」の順でタブを配置し、重複していた内側の「タグ」セクションを廃止した。** タグツリーの見出し、グループ名とタグ名の文字サイズ、ボタンの透明・枠なし表示を統一した。
- タグとタググループをドラッグ＆ドロップで並べ替え・階層移動できるようにし、タグ辞書を1回の原子的な保存で更新するようにした。タグ辞書シート側のツリー編集もタグパネルへ通知し、ソースフォルダ切替と保存が重なった場合は別の辞書へ結果を混在させない。
- 並べ替えボタンを廃止し、各階層の **＋** から「タグを追加」「サブグループを追加」を選ぶ形へ集約した。タグツリーはファイルの選択状態に関係なく辞書の全タグを辞書順で表示し、検索欄をツリー直前へ移動した。
- 日本語IMEの変換中はタグ検索欄を再生成せず、確定後にだけ絞り込むようにした。右レールの重複した検索ボタンと検索コンポーネントは既存レイアウトからも除去した。
- ファイルのデータ量を示すUI表記を「ファイルサイズ」に統一した。自動タグ付けが失敗なしで正常終了した右下の進捗パネルは、完了結果を表示した後に自動で閉じるようにした。

検証: タグ・固定レール・自動タグ付け・版管理の関連自動回帰157件、Chrome実ブラウザE2E 7件、Python／JavaScript構文、差分整合、秘密情報検査に合格。

### feat: タグパネルを自動タグ付けとタグツリーに分離

- タグパネルを **自動タグ付け** と **タグツリー** の2タブに分離した。AI・モデル・実行プリセット・同梱プリセットの導入と、タグ検索・辞書編集・グループ管理が同じ長い画面へ混在しない。
- タグツリーの各タグへチェックボックスを追加し、単独で選択しているファイルの付与済みタグだけをオン表示する。チェックの切替でタグを追加・解除でき、複数ファイルまたはフォルダ選択時は誤操作を防ぐため無効にする。
- ファイル選択時はタグ辞書やツリーDOMを作り直さず、表示中のチェック状態だけを更新する。8,000件を超える辞書も初期250件の段階描画と入力待ち検索にし、自動タグ付けタブでは非表示ツリー用のファイル別タグを取得しない。
- 保存失敗時のチェック復元、古いファイル取得結果の破棄、別ファイルの保存待ち中に届く現在ファイルの更新、自動タグ付け完了後の再取得に対応した。自動タグ付けを利用できない環境ではタグツリーだけを表示する。
- タブは共通のパネル内タブ外観へ統一し、灰色の塗りつぶしや白い外枠を使わず、選択中のタブだけをアクセント色と下線で示す。

### feat: タグの一括リセットと自動タグだけの置換に対応

- タグパネルから、単独ファイル、複数選択、フォルダ配下の全ファイルを対象に、手動・自動を含む全タグをバックグラウンドで一括リセットできるようにした。深い階層を含めて対象を検索し、進捗、完了件数、失敗項目、中止操作を共通のタグ処理パネルへ表示する。
- 自動タグ付けの「既存タグの扱い」へ「自動タグのみ置換」を追加した。手動タグを保持したまま、以前の自動タグだけを今回の判定結果へ置き換える。既存データはすべて手動タグとして安全側へ移行し、手動で再追加したタグも自動タグから手動タグへ昇格する。
- タグ割り当て保存形式をv4へ更新し、自動タグの由来情報をデスクトップ版とDropbox版で共有する。同じタグの重複作成を防ぎ、別端末の独立変更は由来情報込みで統合し、競合時の巻き戻し・復旧データにも由来差分を含める。
- ファイル選択を切り替えた際、一括リセットボタンの有効状態だけを再描画なしで追随させるようにした。大量のタグ割り当てを扱う保存処理では競合検査用データの二重保持を避け、数十万件規模でも余分なメモリ消費を抑える。

### feat: シートのツリービューと列フィルタを追加

- **シートアプリへ汎用ツリービューを追加した。** 親項目、並び順、表示名、種類、色、折りたたみ状態に使う列をビューごとに設定でき、任意の深さの階層表示、検索、展開／折りたたみ、同階層内の並べ替え、ドラッグによる親変更、行名の直接編集に対応する。
- **自動タグ辞書へ「タグ階層」ツリービューを初期搭載した。** タググループとタグの階層を視覚的に編集でき、既存ユーザーが作成したビューは上書きしない。版権作品名・キャラクター名のプリセットは標準搭載せず、利用条件を確認したデータをユーザーが読み込む方針を画面マニュアルへ明記した。
- **各列のヘッダーメニューへ値フィルタを追加した。** 値の検索、すべて表示／すべてクリア、個別チェック、条件フィルタを同じポップアップで操作でき、複数列はAND、同じ列の複数値はORとして全表示形式へ適用する。フィルタ中の列にはフィルタアイコンを表示し、設定を保存ビューごとに保持する。
- 検索・置換パネルの前へ／次へで該当セルまで確実にスクロールし、対象文字列そのものをハイライトするようにした。ツリービューでも検索結果の祖先を表示して該当行を開く。

### fix: タグ表示・自動タグ付け・フォルダ操作を仕上げ

- **情報タブ、タグパネル、フォルダパネル、シートのタグ表示を共通タグカードへ統一した。** 色は状態ドットではなく文字色で示し、削除ボタンはカーソルを重ねた時だけ表示する。タグはタググループ順に並べ、既定10件まで表示し、省略数のツールチップには全タグを表示する。表示上限は設定可能で再起動後も保持する。
- 自動タグ付け完了後にフォルダパネルとシートのタグを即時更新し、ファイル選択でタグパネル全体を再生成しないようにした。AI、モデル、複数プリセット、処理デバイスなどの設定は変更時に保存し、選択切替や再起動後も復元する。
- 複数選択と深い階層のフォルダを正しいソースフォルダへ解決し、実行済みの自動タグ付けだけをスキップできるようにした。手動タグは処理対象に残し、既存と同一のタグは重複作成しない。
- Windows版の画像AI実行環境をONNX Runtime DirectMLへ統一し、GPU対応状況と利用中の実行方式を実態どおり判定するようにした。ローカルWDモデルでは水着などの`questionable`判定を対象外にせず、明確な`explicit`判定だけを成人向け設定の対象とする。
- フォルダツリーの最近使った項目でフォルダをシートと誤判定しないようにし、フォルダツリー／パネルのメニューへコピー、切り取り、貼り付け、削除を追加した。Xブックマーク取込では投稿者アイコンも保存して専用画像列へ表示する。
- ツリービューで列を改名・削除した際に参照設定も追従させ、ビュー切替時の一時状態混在、設定ポップアップの多重表示、ブラウザ保存領域を利用できない時のタグ表示上限の巻き戻りを修正した。

検証: 関連自動・回帰検査325件、Chrome実ブラウザE2E 5件、差分整合・秘密情報検査に合格。

## v0.7.073 (2026-07-29)

### fix: タグ辞書とローカル自動タグ付けを安定化

- **タググループの並べ替えを1回の原子的な保存へ集約した。** 大規模なタグ辞書をNAS上で扱う場合も、兄弟グループごとの全件保存と連続再読込を行わない。保存結果をその場で全タグ表示へ反映し、再読込に失敗した場合は直前の表示を維持する。
- ソースフォルダ切替中の並べ替え競合を防ぎ、開始時と同じタグ辞書を表示している場合だけ保存結果またはロールバックを反映する。デスクトップ版、単独アプリ、Cloud静的版の同じ操作経路を揃えた。
- **ローカル画像AIで明確な成人向け画像を解析する場合、「成人向け」グループ配下だけを専用の感度で補完するようにした。** 通常タグや`questionable`判定のしきい値は変えず、「成人向け画像も端末内だけで解析する」がオフの場合は従来どおり対象外にする。CLI AIへはこの補完を適用しない。
- 自動タグ付け対象がパスを包むオブジェクトとして渡された場合も実パスを共通処理で取り出し、`[object Object]`を対象名や実行パスとして扱わないようにした。不正な対象では安全な未選択表示に戻す。

検証: 関連回帰120件、Chrome実ブラウザ微細挙動（PC・タブレット・スマートフォン）、Python／JavaScript構文、差分整合、秘密情報検査に合格。

## v0.7.072 (2026-07-28)

### fix: 自動タグ付けの複数選択・大量処理・Codex CLI応答回収を修正

- **フォルダパネルで複数選択した全項目を、タグパネルの自動タグ付けへそのまま渡すようにした。** 対象表示も「選択項目: 48件」のように実件数を示し、選択したフォルダは配下を含めて1つのバックグラウンドジョブで処理する。
- プレビュー表示が複数選択を最後の1項目だけで上書きしないよう、チェックボックス、Ctrl／Shift選択、ラッソ、選択解除からタグパネルまでの対象同期を統一した。
- **Codex CLIをJSON出力で実行し、標準出力が空でも最終回答ファイルからタグJSONを回収するようにした。** CLI認証のまま動作し、保存済みLLM APIキーへの自動切替は行わない。
- **保存バッチを5,000件から最大20,000件まで段階的に拡大し、数十万件でタグ割当JSONの全体再書き込みが二次的に増えないようにした。** 1回の保存が大きくなりすぎて中止操作を長時間待たせない上限を設け、中止時と後半の認証・権限・設定エラー時も、保存済み・失敗・未処理件数と失敗理由を保持して表示する。読めない場所も総数・完了数・失敗数へ一貫して計上する。
- `_`で始まる一般ファイルと深い階層も再帰対象へ含めつつ、`.meldex`、`_chat`、自動タグ辞書などMeldex内部データだけを除外するようにした。
- CLI実行時はOpenAI／Anthropic／GeminiのAPIキーに加え、Bedrock、Vertex AI、Azureの従量課金資格情報と切替設定も子プロセスから除外する。旧標準からの更新や独自タグ追加後も標準を利用できるよう候補上限を2,000件とし、それを超えるプリセット構成は実行前に件数と解決方法を表示する。
- **ローカル画像AIとCLIの推論中にも中止を反映するようにした。** ONNX Runtimeの実行停止とCLI子プロセスの終了を伝播し、完了済みタグだけを保存して、中止対象を失敗件数へ誤計上しない。
- **画像AI／CLIを実行できないCloud・クイックメモ・単独アプリでは、自動タグ付けのボタン、複数選択バー、フォルダメニューを最初から表示しないようにした。** 統一タグ辞書の閲覧・編集・CSV操作と通常タグ付けは、引き続きすべての実行環境で利用できる。
- **旧版で26件だけ入っていた「標準」を、同梱CSVの版ごとに一度だけ1,000件へ更新するようにした。** ユーザー独自タグ、色、説明、自動付与設定を保持し、更新後に意図して削除したタグは復元しない。書き込み不能なNASでは既存辞書の表示を続け、更新保留を案内する。
- 辞書とファイル別タグ割り当ての同時更新を、スレッドだけでなくMeldex本体・単独アプリなどの別プロセス間でもソースフォルダ単位に直列化した。Cloud側の変更は端末時計やDropboxの更新時刻がずれていても内容署名とシート世代から検出し、保存直前にも再照合して独立した変更を三方向統合する。
- 辞書とファイル別タグのJSONは、Windowsの原子的置換で実際に押し出された直前世代を復旧領域へ保持し、Dropbox側はrevision条件付き更新と三方向統合で保存する。保存前後に競合が入った場合は自分の変更だけを巻き戻し、巻戻し不能時は復旧意図と「保存された可能性」を記録して編集を停止する。
- Cloud版とデスクトップ版が同じタグ項目を同時に変えた場合は、片方を黙って上書きせず、通常のタグ追加・削除、OSタグ同期、自動タグ付けを含む全タグ変更を停止してタグパネルへ理由を表示する。「クラウド版を採用」「デスクトップ版を採用」の選択前に、同じ読込世代の両版を競合バックアップへ保存する。
- 競合解消には競合IDを必須とし、古い画面からの重複送信が新しいCloud更新を上書きしないようにした。保存直前のファイル削除・移動はその1件だけを失敗として記録し、残りの大量処理を継続する。

検証: 自動タグ辞書、別プロセス同時更新、Cloud三方向統合・競合解消、複数プリセット、4階層230件、100,000件の一括保存、実5,001件の段階保存、中止・致命エラー部分保存、Cloud／単独アプリ同期、CLI回収の関連回帰、Cloud静的版、Python／JavaScript構文検査に合格。実SwinV2 v3と実Codex CLI 0.145.0の双方で、画像入力からバックグラウンドジョブ、辞書解決、タグ保存まで確認した。Chrome実ブラウザのPC・390pxスマートフォン、フォルダ一括、通常タグ編集を確認した。

### fix: リリース前の徹底チェックで検出した回帰を修正

- ページ構成生成へ極端に大きな件数が渡された場合、展開用データを作る前に上限超過として拒否するようにした。パネル数`0`も既定値へ置き換えず入力エラーとして扱い、不正入力による過大なメモリ消費を防ぐ。
- スタッフ登録画面の保存場所変更ダイアログを、タイトルバーから再びドラッグ移動できるようにした。
- 検索用DBの明示指定と関連アイデア検索用DBを分離し、先に実行したテストや別機能の状態が後続検索へ残らないようにした。
- 単独アプリのローカルAPIへ、サーバーごとに独立したセッションCookieと一度だけ使える起動トークンを導入した。接続先、送信元、JSON形式、UTF-8、本文サイズ、重複した本文長、分割転送を検証し、別Webページや別の単独アプリからの変更要求を拒否する。
- 複数のソースフォルダでタグ辞書を切り替えた際、取得元とキャッシュを混同せず、表示中の辞書だけを編集するようにした。
- 自動タグ付けを中止・失敗した際は作業用一覧を残さず削除し、失敗通知でも完了・失敗・未処理件数を正しく表示するようにした。
- スマートフォンのタグパネルで、自動タグ付けのAI、モデル、プリセット、実行ボタンを44px以上のタップ領域にし、縦長の実行セクション内もスクロールしてすべての操作へ到達できるようにした。
- CLI履歴の自動取り込みは、データベース接続を閉じるまで処理中として追跡し、終了時の一時ファイル削除と競合しないようにした。

### fix: Cloud公開ゲートを現行の配布資産へ同期

- Cloud静的ビルドの必須設定にクイックメモの配布URLを追加し、未設定・HTTP・異なる資産名を公開前に機械的に拒否するようにした。
- GitHub Pagesワークフローと配布手順を、本体、6種類のWindows単独アプリ、サンプルを含む現行18資産へ統一した。
- スマートフォンの初回起動・ホーム追加ダイアログがUI倍率の適用時に画面外へはみ出さないよう、表示幅と最大高を倍率込みで補正した。

### feat: タグ辞書と自動タグプリセットを一元化

- **タグパネルをタグ管理の入口へ統一した。** 正式タグ、別名、ホワイトリスト、任意階層のタググループ、プリセット所属、CSV取込／書出、同梱プリセット導入を同じパネルから操作できる。設定画面は既定AI・モデル・処理条件の設定に絞り、タグパネルでは実行時のAI・モデル・複数プリセットを選べる。
- **プリセットをタグ辞書の独立コピーではなく、1つの正式タグに持たせる複数の所属情報へ変更した。** 複数プリセットに完全一致する正式名があっても1件へ統合し、既存タグの内部ID、グループ、色、説明、並び順、ホワイトリスト設定を保持したまま別名とプリセット所属だけを追加する。
- **写真・イラスト・商品・人物・自然・構図・色・感情などを横断する標準プリセット1,000タグを同梱した。** 2026年7月28日時点でAdobe Stock 27件、Shutterstock 39件、iStock 106件、Unsplash 15件、Magnific／Freepik 15件、合計202件の明示公開された有限カテゴリを全件取り込み、同一概念は1タグへ統合して公式英語表記を別名に保持した。各素材へ投稿者が任意入力する公開語彙の定まらないキーワードは「サイトの全タグ」として列挙できないため対象外とし、汎用語彙で補完した。タグパネルでは所属プリセットを複数選択して辞書を絞り込める。
- **ローカル画像AIの出力を日本語の正式タグへ解決する処理を改善した。** 大規模辞書をCLI AIへ渡さず、端末内の画像判定結果だけへ適用する。
- 数千件のCSVと同梱プリセットはバックグラウンドで検証・進捗表示し、SQLiteの単一トランザクションで一括反映する。別名競合、重複、存在しない親、階層循環があれば辞書全体を変更せず失敗させる。
- 旧来のタグツリー複製型プリセットとAPIを廃止し、ファイル別タグ割り当てだけを保持する保存形式v3へ整理した。

検証: タグ辞書・API・タグ階層・既存タグ保持・競合時ロールバック・Eagle CSV・8千タグ導入の関連回帰47件、共通タグ等の関連回帰98件、Python／JavaScript構文検査、CSV再現生成・9列構造検査に合格。Chrome実ブラウザでデスクトップと390×844スマートフォンの検索、複数プリセット絞り込み、同梱プリセット導入、追加、検索、メニュー、長押し、複数選択、フォルダ自動タグ付けを確認した。

### feat: 全チャット履歴を読み取り専用シートで一覧化

- **チャットの履歴タブに「すべての履歴をシートで表示」を追加した。** サイドバーは最終更新日時が新しい50件を維持し、中央の動的シートでは選択中のソースフォルダまたはワークスペースにある全履歴を表示する。
- **タイトル、作成日時、最終更新日時、対象ファイル、プロバイダー、モデル、メッセージ数、履歴ファイルの保存場所を、列ソート・複合フィルタ・表示列切り替え・仮想スクロール付きで一覧できるようにした。** 行からチャットを開き、対象ファイル列から元ファイルを直接開ける。
- `_chat/llm/` のMarkdownを唯一の正本として維持し、読み取り専用シートを開く・並べ替える・絞り込む操作では履歴を複製・変更・削除しない。壊れた履歴は正常な一覧を止めず、読み込めなかった件数を表示する。
- 新規チャットの作成日時をISO日時で保存し、旧履歴の年月日だけの値は存在しない時刻を補わず表示する。通信失敗時は前回取得分を保持し、ツールバーの再読み込みから再試行できる。
- ローカル版とDropbox版の取得列を統一し、Dropbox版は同時6件までの制限付き並列読込で全件取得を高速化した。Dropbox版でもワークスペースIDを登録済みフォルダへ正しく解決し、表示設定はソースフォルダまたはワークスペースごとに保持する。

検証: 関連API・Dropbox・ワークスペース・ルーター・UI回帰105件、E2E定義監査9件、Python／JavaScript構文検査に合格。Chrome実ブラウザで1400×900、1024×768タッチ、390×844タッチを確認し、55件対50件、旧日付、日付一致・複合フィルタ、列切り替え、ソース切り替え、壊れた履歴、通信失敗からの再試行、755件の仮想スクロール、チャット／対象ファイル遷移、履歴Markdown無変更を検証した。

### feat: 自動タグ付けを大量・バックグラウンド処理へ拡張

- **ファイル数の上限をなくし、数十万件規模を想定した逐次列挙・バックグラウンド処理へ変更した。** 対象検索、推論、タグ保存を小分けに進め、進捗、成功／失敗件数、安全な中止、失敗したファイルと理由をMeldex上で確認できる。
- **フォルダパネルとフォルダツリーのポップアップメニューから、選択フォルダの深い階層を含む全ファイルへ自動タグ付けできるようにした。** 複数選択、重複する親子フォルダ、読めないサブフォルダ、一部ファイルの失敗があっても処理全体を継続する。
- **1回の実行で複数の自動タグプリセットを選択できるようにした。** 正式名、複数の英語別名、ホワイトリスト、タググループ、任意階層を自動タグ辞書シートで一元管理し、設定画面とタグパネルの双方から選択できる。
- **Eagleのタグプリセットを参考に、BOM付きUTF-8のMeldex用サンプルCSVを追加した。** 日本語の正式タグへ複数の英語検出タグを対応付け、プリセットと階層グループも同時に取り込める。
- CLI AIは1件ずつ外部CLIの応答を待つ少数ファイル向けであることを設定画面・タグパネルの説明へ明記し、大量処理には端末内のローカル画像AIを案内するようにした。端末内推論はCPUを占有しすぎないよう実行スレッド数を制限し、複数ジョブは順番待ちで安全に処理する。
- オプションパネルの従来の「エディタ」タブを、現在の内容に合わせて「情報」へ変更した。

検証: 自動タグ付け18件、関連回帰60件、情報タブのフロントエンド契約1件、Python／JavaScript構文検査、フォルダ操作の実Chrome micro E2Eに合格。隔離した実ブラウザ環境で、深い階層を含む3画像をフォルダメニューから投入し、3件の検出、バックグラウンド進捗、部分失敗、ファイル別の失敗理由、モデル・複数プリセット選択欄、CLI大量処理の注意説明、「情報」タブを確認した。

## v0.7.071 (2026-07-27)

### feat: フォルダパネルの動画・画像情報表示を強化

- **動画ファイルを画像と同じカード領域へ静止画サムネイルで表示するようにした。** サムネイルを生成できない形式は、従来の種類アイコンへ安全に戻す。
- **画像サイズと★評価をサムネイル上から下のメタ情報欄へ移し、タグも同じ欄へ表示できるようにした。** リスト表示ではファイル名の下へまとめ、タグは最大3件と残件数を表示する。
- **ツールバーの「表示」へ「画像サイズ」「評価」「タグ」を追加した。** 各項目を個別に表示／非表示にでき、設定は既存の表示設定と同じく保存・復元する。
- タグはフォルダ単位でまとめて取得し、PC・タブレット・スマートフォンでカードの重なりや横はみ出しが起きないようにした。

検証: 関連回帰41件、画面内コントロール検査124件、Chrome実ブラウザE2E、Python／JavaScript構文検査に合格。1280×900、768×1024、390×844で動画サムネイルと画像サイズ・評価・タグの配置、表示切替、横はみ出しなしを確認した。

### fix: チャット内のMeldexリンクを正しく開く

- **ファイル名だけのリンクを、チャットで現在対象にしているフォルダから解決するようにした。** 深い階層のシートやフォルダノートも、短いリンク名から開ける。
- 登録済みソースフォルダ内の絶対パスはOS側ではなくMeldex内で開き、存在しないリンクでは空タブを作らず分かりやすい通知を表示するようにした。
- 同名候補が複数ある場合は現在の対象フォルダを優先し、一意に決まらない別項目を勝手に開かない。

検証: 関連回帰116件、Chrome実ブラウザE2E、Python／JavaScript構文検査に合格。完全相対パス、深い階層の短いリンク名、登録済みルート内の絶対パス、登録外ローカルパス、Web URL、存在しないリンクを確認した。

### fix: 公開ビルドの依存関係を修正

- `sentence-transformers` と互換性のない `huggingface-hub` 1.x の指定を修正し、Windows公開ビルドで依存関係を解決できるようにした。
- GitHub Actionsで外部コマンドが失敗した時点で公開処理を停止し、後続処理へ進まないようにした。

検証: 依存関係のクリーン解決、バージョン整合、公開配線テストを実行。

## v0.7.070 (2026-07-27)

### feat: 追加した同一ファイル・同一画像を自動で警告

- **ドラッグ＆ドロップ、貼り付け、アップロード、Webクリップ、Eagle／PureRefなどから追加した外部ファイルを、管理中の全ローカルフォルダと照合するようにした。** 1バイトも違わないファイルと、形式・メタデータが異なっても表示画素が同じ画像を保存後に警告する。
- **警告から重複検出ダイアログを開き、既存ファイルを初期選択したうえで「すべて残す」か、残す1件を選んでほかをゴミ箱へ移せるようにした。** 確定直前に内容を再確認し、変更済みのファイルは自動で移動しない。
- **手動の重複画像スキャンをバックグラウンドジョブ化した。** 15秒のHTTPタイムアウトを廃止し、進捗表示と安全な中止に対応。永続フィンガープリントキャッシュとBK木を使い、毎回の全画像再ハッシュと全組み合わせ比較を避ける。

検証: 追加・起動・中止・再起動復帰・Cloud切替・保持／ごみ箱移動の徹底チェック、関連回帰33件、実Chrome E2E、Python／JavaScript構文検査に合格。実NASフォルダを330件まで読み取り専用で確認し、15秒を超えても継続すること、中断後に310件分のキャッシュから再開できることを確認した。

### feat: WD v3とシート型タグ辞書による自動タグ付けを追加

- **設定 → AI・Discordへ「自動タグ付け」を追加し、既定AI、WD EVA02 Large v3 / WD SwinV2 v3、Codex CLI / Claude Code / Gemini CLI、処理デバイスを選べるようにした。** モデルごとに特徴、得意分野、必要容量、公式配布元、入手・設定手順を表示し、端末内モデルは画面から取得できる。
- **画像はONNX Runtimeで端末内推論でき、成人向け画像の解析は明示的に有効にしたローカルWDモデルだけへ限定した。** CLIの安全制限は変更せず、CLI失敗時に従量課金APIへ自動で切り替えない。
- **自動タグ辞書の正本を標準Meldexシートにした。** 正式名、別名、自動付与ホワイトリスト、タググループ、任意階層、色、説明、並び順、折りたたみをシートで管理し、設定・オプションパネル・タグパネルから同じ辞書を開ける。
- **Eagleの `group,color,tag` CSVとMeldex拡張CSVの取り込み・書き出しをタグパネルへ追加した。** ファイル／フォルダのオプションパネルでは、その実行だけに使うAIとモデルを選択できる。
- Web Clipper、クイックメモ、ファイル／フォルダの自動タグ候補を同じ辞書の別名・ホワイトリストへ統一した。
- Cloud版ではローカル専用APIを呼ばず、設定画面にデスクトップ版で準備・実行する旨を表示するようにした。既存タグの確認と手動編集は引き続き利用できる。

検証: 自動タグ辞書・Eagle CSV・共通タグ・クイックメモ51件、API・ルーター・Web Clipper70件、設定・分割資産27件、依存ロック1件、Python／JavaScript構文検査に合格。公式WD SwinV2 v3モデルを実取得し、ONNX Runtime 1.28のCPU推論で画像から7タグを検出した。実ブラウザでPC、820pxタブレット、390pxスマートフォンの設定・辞書・オプション・タグパネルを確認し、警告・エラー0件、横はみ出し0件。

### feat: 画像の埋め込み情報とWeb Clipperシート表示を強化

- **フォルダパネルの画像サムネイルへ画像サイズを`幅 × 高さ px`で表示し、1〜5個の★で評価を設定できるようにした。** リスト表示でも画像サイズをファイル名の右へ表示する。
- **オプションパネルのエディタタブへ画像サイズ、★評価、編集可能なメモ、元ページ／画像URLを表示するようにした。** EXIF、XMP／IPTC、PNGテキスト／コメント、カラープロファイル、Office文書プロパティなど、取得できた埋め込み情報をグループ別に確認できる。評価とメモは画質を変えずに書き戻せる画像形式だけ画像ファイル自体へ保存し、再圧縮が必要な形式は読み取り専用にした。
- **新規Web Clipperシートの初期並び順を「追加日時・降順」にした。** 既存の保存済みビューと並び順は上書きしない。
- **画像セルを含むシートの下半分が未描画の空白になる問題を根本修正した。** Chromiumで可変高の表行に`content-visibility`を適用した際の行高誤推定を廃止し、大規模シートはJS仮想行レンダラーで軽量化する。仮想行はパネル自身のサイズ変更も監視し、サイドバーやペインの開閉後に表示範囲を再計算する。

検証: 画像メタデータ、Web Clipper保存・初期並び順、フォルダ／エディタUI、シート行描画の関連回帰テスト67件、Python／JavaScript構文検査、実Pixiv保存画像のEXIF／XMP読取と実ブラウザ上のWeb Clipperシート寸法測定に合格。

### feat: Web Clipperの画像をファイルとして直接保存

- **画像のドラッグ保存、ページ画像の一括保存、表示範囲画像、範囲画像を、シートのエントリではなく指定フォルダ直下の画像ファイルとして保存するようにした。** 記事・簡易記事・ページ全体などのページ内容クリップは、引き続きシートへ保存する。
- **元ページURL、画像URL、タグ、メモ、画像寸法、保存日時を画像ファイル自体へ埋め込むようにした。** JPEG、PNG／APNG、WebP、GIF、SVG、AVIFに対応し、BMPは画質を保ったPNGへ変換する。
- 既存の画像メタデータを維持しつつMeldex管理情報だけを更新し、再保存時の重複、巨大XMP、外部宣言を拒否する。Web Clipper拡張機能を `1.1.5` に更新した。

検証: Web Clipper関連回帰テスト210件・11サブテスト、画像形式別の画素・フレーム維持、メタデータ再埋め込み、PC／モバイルの決定的操作テスト、Python／JavaScript／JSON構文検査に合格。同期元と読み込み用フォルダ18ファイルのSHA-256一致も確認した。

### fix: シートの行・列ドラッグ、列固定、リレーション連動を安定化

- **行のドラッグ並べ替えを復旧し、並び替えが「マニュアル」のときだけ受け付けるようにした。** それ以外の並び替え中は警告して順序を維持し、ドラッグした行を選択状態にしない。マニュアル中のセル編集でも行位置を維持する。
- **リレーション候補のステータスをセルから変更でき、ロールアップの表示対象と件数がリレーションのステータスフィルタへ連動するようにした。**
- **エントリ名列が先頭以外でも固定できるようにし、複数の固定列、ヘッダー、通常行、集計行、最下部の「＋ 新規」行で固定位置を統一した。** 固定列間の隙間と、横スクロール時の列の透けを解消し、固定列の合計幅が狭いパネルを超える場合も列同士が重ならないようにした。
- **列のドラッグ並べ替え後にヘッダーとセル内容がずれる問題と、固定ヘッダーが他の列と一緒にスクロールする問題を修正した。**

検証: コミット対象だけを展開した関連回帰テスト100件、行・列D&D／名称変更後の位置維持／列幅変更／複数列固定を確認するChrome E2E（デスクトップ・タブレット・390×844タッチ環境）と、リレーションステータス／ロールアップ連動のChrome E2Eに合格。

### fix: Dropboxアカウント連携とCloud単独画面を統合

- **Dropboxのメンバー個人ホームとチーム共有領域を明示的に分離した。** アカウント設定・プロフィール・ソースフォルダ台帳は個人ホームへ統一し、チーム共有フォルダを選んだ操作だけチーム名前空間を使う。
- **デスクトップ版で登録済みのDropbox内ソースフォルダを、Cloud版へ自動反映するようにした。** 旧設定のローカルパスは初回読込時に共有台帳へ追加し、個人ホーム外のチームフォルダも別名前空間として区別する。旧台帳・プロフィールは削除せず、必要な場合だけ個人ホームへコピーする。
- **Note、Scenario、Board、Sheet、Timer、クイックメモを1つのMeldex Cloud PWAへ統合した。** Cloud本体でのDropbox接続を6画面で共有し、別画面の接続・解除を即時反映する。個別manifest・個別Service Worker・個別インストール案内はCloud配布物から除外し、共通manifestのショートカットから各画面を開く。
- DropboxトークンはIndexedDBだけに保持し、画面間通知には接続状態・アカウントID・保存時刻だけを使用する。

検証: Dropbox名前空間、デスクトップ自動共有、プロフィール、共有ワークスペース、単独画面配布、公開準備の関連テスト252件・41サブテストに合格。Cloud静的版917ファイルのビルド、PC・820pxタブレット・390pxスマートフォンの決定的Chrome E2Eで、チームPath-Root、個人ホーム台帳、44px操作領域、横あふれなし、共通manifestを確認した。

### fix: Web ClipperのPixiv保存とページ画像の一括選択を安定化

- **画像の保存先として、ホームフォルダに加えて登録済みのソースフォルダとワークスペースを選べるようにした。** 保存先ポップアップから新規フォルダを作成でき、ポップアップ外を押すと閉じる。選択ボタンはアイコン表示へ整理した。
- **Pixivの画像CDNへ必要な参照元情報を付けて取得するようにし、プレビューと保存が拒否される問題を修正した。** 保存先ルート外への移動、隠しフォルダ、保護対象は引き続き拒否する。
- **一括保存ダイアログへ最大幅・最大高さと詳細な数値入力を追加し、条件外の画像を自動で選択解除するようにした。** 表示中／全体の選択・解除、Shift範囲選択、ドラッグ複数選択、サムネイルサイズ変更にも対応した。
- ダイアログ操作中に背面ページがスクロールする問題と、画像検出メッセージが二重表示されて文字が回転し続ける問題を修正した。
- Web Clipper拡張機能を `1.1.4` に更新した。

検証: Web Clipper関連回帰テスト199件・2サブテスト、PC／モバイルの決定的操作テスト、同期済み拡張機能の実ブラウザ検証に合格。Pixiv画像CDNから約3.1MBのPNGを取得できること、同期元と同期先18ファイルのSHA-256一致も確認した。

## v0.7.069 (2026-07-27)

### fix: ビューワー・動画・フォルダ・チャットの操作を改善

- **ビューワー内でマウスの戻る／進むボタンを押すと、前／次の項目へ移動するようにした。** 押下時だけ移動し、マウスを離した時や補助クリックで二重に移動しない。
- **ローカル版、Dropbox版、Cloud版の動画を静止画サムネイルで表示し、動画を開いた直後に再生を開始するようにした。** フォルダカード、スマホ一覧、ボード、右サイドバーを共通対応し、サムネイル生成に失敗した形式は従来の種類アイコンへ安全に戻す。
- **フォルダパネルの画像メニュー「別の方法で開く」へ「サイドバーで開く」を追加した。** チャット入力欄上の「一覧から選択」は、既存操作と読み上げ名を維持したフォルダツリーアイコンへ統一した。
- **フォルダツリーでフォルダを選ぶたびに全体を再描画して縦に揺れる問題を修正した。** 同じフォルダタブ内の移動ではタブ情報と内容だけを更新し、ツリーDOM、スクロール位置、画面上の位置を維持する。
- 表示バージョンを `0.7.069` に更新した。

検証: 徹底チェック（ユーザー操作、呼び出しチェーン、状態境界、失敗時フォールバック）、Python／JavaScript構文検査、関連回帰159件・2サブテスト、ビューワーのPC／モバイル決定的Chrome E2E、フォルダツリーのPC／820pxタブレット／390pxスマートフォン決定的Chrome E2Eに合格。Windows動画とDropbox動画の実サムネイル生成、動画自動再生、チャットアイコン、横あふれなしも確認した。

## v0.7.068 (2026-07-27)

### feat: クイックメモの入力切替・描画・履歴・過去メモを刷新

- **テキスト、ペン、音声をドロップダウンで切り替える構成へ統一した。** Meldex Memo、過去メモ、ユーザー、新規、保存を一行へまとめ、テキスト書式は本文を選択している間だけ選択位置の下へ表示する。
- **自動タグ付けのボタンとクライアント送信を封印した。** 既存の保存データとサーバー機能は削除せず、再開できる状態を維持する。
- **ペン、塗り、消しゴム、消去をLucideアイコンへ統一した。** ペンの色、1〜100%の不透明度、線幅を調節・端末保存でき、不透明度の初期値を100%にした。塗りはクリック位置から閉じた領域だけを走査し、画面端へつながる開領域では中止する。
- **ペンの描画面を残り画面全体へ広げ、DPR、画面サイズ、回転後も描画を復元するようにした。** テキストとペンへUndo/Redoを追加し、IME入力、貼り付け、音声挿入、書式、ストローク、塗り、消去を適切な履歴単位で扱う。
- **保存済みと送信待ちを統合した過去メモ一覧を追加した。** タイトル・本文検索、タグ絞り込み、50件単位の追加読込、同一メモとしての再編集に対応した。ローカル版は許可ルート内のクイックメモだけを開き、Cloud版はDropbox上のメモを一覧・再編集する。
- Windows単体版とCloud版の配布資産、Service Worker、操作マニュアルを更新し、表示バージョンを `0.7.068` に更新した。

検証: JavaScript／Python構文検査、対象差分検査、単体・静的回帰・版整合テスト108件、Windows/ローカル版6条件・Cloud版3条件の決定的Chrome E2Eに合格。PC・820pxタブレット・390pxスマートフォンで、選択書式、Undo/Redo、ペン設定、開領域の塗り拒否、一覧再編集、スマホ共有、44px操作領域、横あふれなし、ヘッダー5要素を確認した。Cloud静的版920ファイルと、フロントエンド資産34ファイルを含む約10.9MBのWindows単体exeの生成にも成功した。

## v0.7.067 (2026-07-27)

### fix: シート候補操作・辞書出力・候補色の関連経路を追加補修

- **候補値の変更・削除・Undo／Redoで使う正規化済みエントリパスを、一括編集、リレーション、リンク列、カレンダー、カンバンを含む全関連経路へ統一した。** 画面上だけ変更されて保存先を特定できない状態を防ぎ、削除メニューの名称も「候補値を削除」へ統一した。
- **同じ単語が複数のシート／DBに存在する場合も、異なる読みとステータスを失わず1件へ統合するようにした。** Cloud版も汎用ピボット生成を経由せず、辞書に必要な列だけを読む専用収集経路へ統一した。
- **保存APIの失敗時にHTTP本文の具体的な理由を表示し、辞書出力完了時にルビなし件数と重複除外件数を表示するようにした。**
- **候補色をグループ見出しとカンバン見出しの実背景へ反映し、背景色に応じた白／黒の文字色を適用した。** Desktop／CloudのPC・タブレット・390pxスマートフォンで候補設定と辞書ダイアログを再確認し、PC・タブレットでは見出しの実描画も確認した。
- **セル編集のTab／Shift+Tab保存・横移動を決定的E2Eへ追加し、直前の非同期保存による遅延フォーカス復元が次の編集やTab移動を上書きしないようにした。** IME変換中のキー処理と編集開始条件を含む操作説明もマニュアルへ反映した。
- 表示バージョンを `0.7.067` に更新した。

検証: 辞書候補の統合、Cloud専用収集、候補パス、IME、候補色、辞書UI、タグアイコンの関連回帰テスト、JavaScript／Python構文検査、Desktop／Cloud静的版の6画面条件における決定的Chrome E2Eに合格。詳細結果は品質監査記録へ保存した。

## v0.7.066 (2026-07-26)

### fix: シートの日本語入力・候補操作と辞書出力を安定化

- **セルを選択しただけでは文字入力を開始せず、Enter／F2／ダブルクリックから編集するように統一した。** 日本語変換中のEnter、Space、矢印、Escapeをシート側が奪わず、変換確定後の次のEnterで保存する。Enter保存後は同じセルを維持し、Tab／Shift+Tabは保存後に横移動する。
- **候補値の変更・削除・ステータス変更・Undo／Redoが、正規化したエントリパスを使うようにした。** 保存失敗時は楽観表示を戻して再取得し、HTTPエラーの理由を表示する。
- **セレクト／複数選択の候補ごとに、列設定とセル候補欄の双方から共通カラーパレットで背景色を設定できるようにした。** 色はセル、候補欄、グループ見出し、カンバンへ反映し、透明を選ぶとテーマ既定色へ戻る。候補名を変えても旧候補の色設定は別候補へ転用しない。
- **辞書出力を、各ルビ候補自身のステータスで絞り込めるようにした。** 採用だけを初期選択し、すべて選択／解除、独自ステータス、複数ルビ、重複除外、ルビなし件数、BOM付きGoogle日本語入力形式へ対応した。専用のSQLite／Markdown／Cloud収集経路とキャッシュを追加し、実データではコールド約1.3秒、キャッシュ時約0.05秒で生成できた。
- **右レールのタグパネルを、共有UI定義の単一タグアイコンへ統一した。** 四角アイコンへフォールバックする経路を解消した。
- 表示バージョンを `0.7.066` に更新した。

検証: 候補パス・IME境界、SQLite／新旧Markdown／Cloud辞書、候補色、辞書ダイアログ、タグアイコンの単体・回帰テストと決定的Chrome E2Eに合格。Desktop／Cloud静的版のPC・タブレット・390pxスマートフォンでの画面確認結果は品質監査記録へ保存した。

## v0.7.065 (2026-07-26)

### feat: Web Clipperへ画像のドラッグ保存とページ画像一括保存を追加

- **Webページ上の画像をドラッグすると、画面下部へMeldexの保存先を表示するようにした。** 最近使った最大5件のフォルダへドロップするとすぐ保存でき、**保存先を選ぶ**へドロップするとMeldexホーム内のフォルダ、タグ、メモを確認してから保存できる。従来の画像ホバーボタンは復活させず、Meldex以外への通常のドラッグ操作も維持した。
- **拡張機能のポップアップと右クリックメニューへ、ページ内の画像をまとめて保存する機能を追加した。** `img`、`picture/srcset`、遅延読込属性、高解像度画像へのリンク、CSS背景画像を検出し、形式・最小幅・最小高さで絞り込める。スクロール後に読み込まれた画像の自動検出にも対応するが、自動スクロールやページ上のボタン操作は行わない。
- **画像1枚をMeldexの1エントリとして保存し、元ページURL、画像URL、画像幅・画像高さを記録するようにした。** 保存先はMeldexホーム配下の表示可能なフォルダに限定し、隠しフォルダ、保護対象、ホーム外への経路を拒否する。
- 一括保存は検出最大500件、1回最大200件、同時2件に制限した。51件以上では確認を挟み、中止時は保存済みを残し、一部失敗時は失敗分だけ再試行できる。
- Web Clipper拡張機能を `1.1.0`、Meldexの表示バージョンを `0.7.065` に更新した。

検証: Web Clipperの画像候補抽出、保存先境界、拡張機能からのアクセス制御、画像メタデータ、ドラッグ／一括保存UIの回帰テスト、Python／JavaScript／JSON構文検査に合格。PC・スマートフォン相当の実ブラウザで、保存先表示、絞り込み、複数選択、保存進捗、中止、失敗分の再試行を確認した。

## v0.7.064 (2026-07-26)

### fix: Codex CLI既定と具体的なエラー案内を修正

- **「CLI既定（推奨）」でMeldexが固定モデルを強制せず、Codex CLI自身の既定モデルを使うようにした。** 明示候補はGPT-5.6 Sol／Terra／Lunaへ更新し、思考の深さ`max`も丸めずそのまま渡す。
- **Codex CLI 0.145.0未満を送信前に検出し、更新方法を表示するようにした。** 設定APIへバージョン・必要バージョン・互換性・案内文を追加し、独自ラッパーで版を確認できない場合は警告付きで実行できる。
- **Codex JSONLのネストしたエラーを解析し、モデル非対応、設定不整合、空回答、非ゼロ終了を具体的な日本語案内へ変換するようにした。** stderrやWindowsのプロセス終了ログを回答本文へ混ぜず、安全化した詳細だけを折りたたんで表示する。
- 更新要求やモデル非対応をチャット履歴へ保存し、失敗したCLIセッションを破棄して再送時に引き継がないようにした。CLI失敗時の従量課金APIフォールバックとAPIキー環境変数の継承は行わない。
- 表示バージョンを `0.7.064` に更新した。

検証: CLIチャット関連テスト127件、チャット回帰5件、分割資産17件、micro登録8件、Python／JavaScript／JSON構文検査、`git diff --check`に合格。PC・390pxスマートフォンの決定的Chrome E2Eで更新案内、折りたたみ詳細、履歴復元、失敗セッションを引き継がない再送を確認した。Codex CLI 0.145.0へ更新後、`--model`なし・APIキー環境変数なしの実CLIとMeldex SSEの双方で正常回答を確認した。

## v0.7.063 (2026-07-26)

### feat: 作品の見開き・カラーページ設定とタスク構成更新を追加

- **作品設定の「見開きページ」「カラーページ」に、ページ数に応じた `p0001` 形式の候補を表示するようにした。** ページ数が未入力でも `p0001`〜`p0099` を表示し、見開きは「開始ページの位置」（左ページ／右ページ）に合わせて2ページ単位の候補を表示する。
- **見開きを1つの制作単位としてタスク生成するようにした。** たとえば左ページ開始の6ページで `p0002-p0003` と `p0004-p0005` を指定すると、`p0001 / p0002-p0003 / p0004-p0005 / p0006` の4単位になる。カラーページは選択・保存・表示だけに使い、タスク数・割り当て・再計算時間には影響させない。
- **作品別タスクリストへ「タスク構成を更新」を追加した。** 更新前に作成・アーカイブ・保護・変更なしの件数をプレビューし、未着手で予定・実績・固定のない旧タスクだけをアーカイブして新しい構成へ移す。進行中・予定済み・実績あり・固定済みのタスクは残し、プレビュー後に変更があれば適用を拒否する。
- ページ数や開始位置を変更して既存選択が範囲外になった場合、値を自動削除せず警告を表示するようにした。
- 制作管理12シート142列と全体スタッフ正本12列、合計154列について、割り当て・再計算・生成・補助表示での利用有無と使い方をCSV監査表へ整理した。
- 表示バージョンを `0.7.063` に更新した。

検証: 新規・関連テスト、Desktop／CloudのJavaScript構文検査、Python構文検査、PC・390pxスマートフォンの実ブラウザ操作に合格。左右開始、空欄時99候補、旧見開き表記、見開き単位生成、コマ単位への再切替、保護対象、状態指紋、失敗時ロールバック、154列の完全一致、範囲外警告を確認した。

## v0.7.062 (2026-07-26)

### fix: メインパネルのタブ操作と、URL・パスの長文表示を改善

- **メインパネルのタブをドラッグ＆ドロップで並べ替えられない問題を修正した。** 固定レイアウトでは同じメインパネル内の並べ替えだけを許可し、別パネルへの移動やウィンドウ外への切り離しとは分離した。並べ替え後も選択中のタブを維持し、順序を保存して次回表示へ復元する。
- **メインパネルの各タブへ、カーソルを合わせたときとキーボードでフォーカスしたときに「…」ボタンを表示するようにした。** 「…」、右クリック、長押し、スマートフォン／タブレットの既存タブ操作ボタンは同じメニューを開き、Escapeや外側操作で閉じて元の操作位置へフォーカスを戻す。
- **Meldex内の長いURL・パスを、末尾のファイル名や最終セグメントが見える中略表示へ統一した。** 入力値、保存値、リンク先は完全なまま保持し、入力欄へフォーカスすると全文を編集できる。ファイル比較、ホームフォルダ、スタッフ管理シートの保存先、Dropbox接続設定、単体版アプリにも反映した。
- **マルチソースリレーションの列設定で、参照シートの長いパスが末尾まで見えない問題を修正した。** 「～内のシートを表示中」は入力行の右端ではなく、シートパス欄の直下へ表示する。
- 表示バージョンを `0.7.062` に更新した。

検証: 関連単体テスト42件、JavaScript構文検査、PC・390pxスマートフォン・820pxタブレットの決定的Chrome E2E、マルチソース設定とURLセルの実ブラウザ検査に合格。タブの中間／先頭／末尾／同位置移動、アクティブ維持、保存・再適用、メニューの画面内配置とキーボード操作、長いパスの完全値保持を確認した。

## v0.7.061 (2026-07-26)

### fix: チャットの対象選択、CLI初期値、前回状態の復元を改善

- **メインパネルで開いているファイルが対象欄に表示されていても、送信時に「対象フォルダまたはファイルを選択してください」と止まる問題を修正した。** 参照する対象とチャットの保存先を分離し、対象を指定しない通常のAIチャットも送信・保存できるようにした。
- 対象パスの右端へ「対象を解除」と「一覧から選択」を追加した。対象を解除した状態は維持され、一覧からフォルダまたはファイルを選び直せる。メインパネルへ追従中は、タブを切り替えた際も現在の対象へ更新する。
- CLIが利用できるPCでは、ユーザーがプロバイダをまだ選んでいない初回に限り、Codex CLI、Claude Code、Gemini CLIの順で利用可能なCLIを初期値にするようにした。保存済みの選択は変更しない。
- チャットパネルを開き直した際に、前回の「ワークスペース／AI／履歴」タブ、履歴内の表示、AIチャット、保存先ごとのチームチャットルーム、対象の選択状態を復元するようにした。
- 欠損したAIチャット履歴を開いた後も古い復元情報や対象が残る問題、対象なし履歴を開いた際に直前の対象が残る問題、CLI応答中に対象変更ボタンの見た目が有効なままになる問題を修正した。
- 表示バージョンを `0.7.061` に更新した。

検証: 関連回帰テスト、JavaScript構文検査、PC・タブレット・スマートフォンの決定的Chrome E2Eで、対象の解除／再選択、対象なし保存、CLI初期選択、タブとチャットルームの反復復元、狭い画面での44pxタップ領域を確認。

## v0.7.060 (2026-07-26)

### feat: シートの行選択、ビュー切替、ロールアップ、マルチソースリレーションを改善

- **セルがアクティブで編集中でないとき、`Ctrl+A`で表示対象の全行を選択し、`Ctrl+D`で行選択だけを解除できるようにした。** フィルタ中・仮想スクロール・分割表示・単独シートにも対応し、入力欄の通常の全選択は妨げない。非表示になった表のセルが以前アクティブだった場合もキーを奪わない。表の空白部分をクリックした場合は行選択を解除する。
- **シートのステータスバーへ主要ショートカットを表示し、ビュー切替をツールバーと表の間の独立した帯へ移した。** キー設定の変更にも追従し、狭い表示では改行せず横へスクロールできる。
- **ロールアップへ既定の「文字列」表示を追加した。** 参照先の表示値を順番どおり複数行で表示し、同じリレーション列を使う文字列ロールアップ同士は、参照名も含めて行位置を揃える。件数・合計・平均など従来の集計も引き続き選べる。
- **マルチソースリレーションで、現在のシートを参照している別シートのリレーション列をソースにできるようにした。** 手動候補と自動収集、同じシート内の複数参照列、`シート名/列名`・`シート名/ラベル`表示、旧保存形式の読込、参照切れ警告に対応した。
- 単一・複数リレーションのアイコンを同じリンクアイコンへ統一した。アクティブなリレーションセルの「＋」は、ホバー表示と候補画面を開けることを実ブラウザで再確認した。
- CSVへ切り替えた際に、直前のシートの保存ビュータブや全行選択のショートカット案内が残る問題を修正し、CSV専用の表示と案内へ切り替えるようにした。
- 表示バージョンを `0.7.060` に更新した。

検証: 行選択、ビュー帯／ステータスバー、ロールアップ行合わせ、逆向きマルチソース、リレーションの「＋」を決定的Chrome E2Eで確認。PC・スマートフォンの画面証跡、関連回帰テスト、デスクトップ／Cloud静的配布物の同期検査も実施した。

## v0.7.059 (2026-07-26)

### feat: パネル・シート・ビューワー・タグ・ZIP操作を改善

- **メインパネルで開いたエントリを「戻る／進む」の履歴へ追加した。** 元のシートの表示形式、フィルタ、並べ替え、スクロール位置、選択行も復元し、履歴はタブごとに分離した。
- **シートのエントリをメインパネル、右サイドバー、フロートパネルのどこでも同じように編集できるようにした。** プロパティと本文の編集、自動保存、文字列のコピー、ノート／チャット作成、リンク操作、Xポストの個別再インポートに対応した。
- 左レール下部のボタン配置を固定し、フォルダパネルの「表示」と「フィルタ」を分離した。タブの「＋」メニューはメインパネルで開ける項目だけに整理し、「作業パネル」の表記を「メインパネル」へ統一した。右レールのタグアイコンも修正した。
- **Xブックマークの同期を常に差分同期に統一した。** ポストIDを複数の保存情報から復元して既存行を更新し、同じポストの連番行が増えないようにした。個別再インポートと、内容を確認してから既存の重複を統合する機能も追加した。
- **シートの画像セルから開いたビューワーでは、左右移動とスライドショーが同じセル内だけでループするようにした。** 上下移動は、フィルタと並べ替え後の表示順に従って同じ画像列の前後の行へ移動する。ウィンドウやパネルをリサイズしても、表示中の画像とズーム・回転・再生状態を維持するようにした。
- **画像ファイルのOSタグとMeldexの共通タグを双方向で同期できるようにした。** Windowsでタグを書き込める画像だけを対象とし、未対応形式や取得失敗でもMeldex側のタグは失わない。
- **複数項目のZIP名を、最初に選んだ項目の名前に「_他」を付ける形式へ変更した。** ZIPのダブルクリックでは解凍やエクスプローラー起動を行わず、読み取り専用の仮想フォルダとして安全に中身を閲覧できるようにした。
- 表示バージョンを `0.7.059` に更新した。

検証: 新規・関連Pythonテスト121件、主要なシート／シェル／ビューワー微細E2E、JavaScript構文検査、分割バンドル生成・同期検査、Windows実画像でのOSタグ読み書き、デスクトップ／モバイル／フロートパネルの実ブラウザ操作に合格。メインパネルの連続リサイズと画像状態維持、履歴のタブ間分離、ZIPの危険パス・暗号化・異常サイズ拒否も確認した。

### fix: リレーション候補の追加で参照先がリンク切れになり、ロールアップが更新されない不具合を修正

- 作品フォルダー内の参照先シートが相対パスで保存され、複数のソースフォルダーがある環境で別の場所へ解決される問題を修正した。新規保存時は絶対パスへ正規化し、既に保存済みの相対パスも正しい作品フォルダー基準で読み込む。
- リレーションセルの候補ポップアップから新しい候補を追加した際、候補がセルへ反映されず、ロールアップも古い値のままになる問題を修正した。候補追加後に参照先とロールアップのキャッシュをまとめて更新する。
- 候補を1件追加するだけで全ソースフォルダーのメディア参照を再走査し、処理が長時間停止する問題を解消した。存在しない参照先への誤作成も防止した。

検証: 対象自動テスト18件すべて合格。実ブラウザで新規候補が約1.5秒でセルへ反映され、ロールアップ件数が再読み込みなしで0から1へ更新されることを確認した。

### fix: シート再読み込み時の保存競合と、候補入力・初回列設定の不具合を修正

- ツールバーの再読み込みが進行中のセル・列・ビュー設定の保存を追い越し、保存競合の警告が出る問題を修正した。同じ行の複数セル保存を直列化し、最新の更新番号を行内の全セルへ同期してから再読み込みする。
- セルの「＋」から開く候補ポップアップの入力欄で、Backspace／Deleteを押すと閉じる、左右キーでキャレットを動かせない問題を修正した。リレーション、複数リレーション、セレクト、複数セレクト、共通タグ、ユーザー選択など、共通の候補入力を使う列へ反映した。
- シートを開いた直後に初めてリレーション／複数リレーション／ロールアップを設定すると、設定が即座に表示へ反映されない場合がある問題を修正した。表示中のシートを明示して設定保存・参照先読込・再描画を行う。

検証: 対象回帰テスト43件、JavaScript構文検査、実Edgeによるネイティブキー入力と保存競合E2Eに合格。デスクトップ・タブレット・スマートフォンの3画面幅でポップアップが見切れないことも確認した。

## v0.7.058 (2026-07-25)

### fix: シートの列を「切り詰め」にすると列幅が広がり、列幅も変えられなくなる不具合を修正

- **シートの列を「切り詰め」にしたとき、その列が本文まるごと1行に収まる幅まで広がってしまい、さらに列幅の変更もできなくなる不具合を修正した。** 切り詰めても列幅は一切変わらず、はみ出した文字だけが末尾「…」で省略されるようにした。列幅のドラッグ変更も通常どおり効く。
- **シート全体を「切り詰め」にしたときに、関係のない列まで幅が縮んでしまう問題も併せて解消した。** 以前の対処は表全体の幅を詰める方式だったため、見出しの文字数で広がっていた列が指定幅まで縮んでいた。今回は切り詰めた本文だけを幅の計算から外す方式に変えたので、どの列の幅も変わらない。
- エントリ名列を「切り詰め」にした場合も同じように列幅を保つようにした。
- 表示バージョンを `0.7.058` に更新した。

検証: 実データ（サンプル作品のキャラ表・52列・最長74文字のセル）で実ブラウザ検証し、列単位の切り詰めで全52列とテーブル幅が一切変わらないこと、末尾が「…」で省略されること、切り詰め中に列幅のドラッグ変更が効くこと（100px→230px）、シート設定に従うへ戻すと元の幅に復帰すること、シート全体の切り詰めでも全列幅が変わらないこと、折り返しへ戻せることを確認（11項目すべて合格・JSエラー0件）。回帰検知として、切り詰めの4つの表示規則すべてに幅計算の除外指定が入っていることを検査する静的テストを追加し、指定を外すと実際に失敗することを確認した。列の折り返し設定のE2Eには、長文を流し込んでも列幅が変わらないことの確認を追加した。シート表示設定の静的テスト17件・シート関連API回帰26件・列折り返しE2E 1件すべて合格。

## v0.7.057 (2026-07-25)

### スキルをCLIチャットでも使えるようにし、スキルの管理画面を追加

- **チャットの「スキル」を、CLIチャット（Claude Code / Codex / Gemini CLI）でも使えるようにした。** フォルダごとにまとめた専門的な指示（スキル）を、これまでは通常のチャットだけが参照していた。今回からは、どのCLIを選んでいても、依頼内容がスキルの説明やトリガーに合うときだけ、そのスキルの本文を自動で読み込んで参照するようにした。
- **スキルを一覧・作成・編集・削除できる管理画面を追加した。** 設定 → LLM →「スキル」から開ける。名前・説明・トリガー・本文を入力してスキルを作れる。これまでスキルは画面から見ることも作ることもできず、ファイルを直接置くしかなかった。削除したスキルは完全には消さず、ゴミ箱へ移動する。

## v0.7.056 (2026-07-25)

### 取り消し・やり直しがパネルを取り違える問題を修正

- **別のタブへ切り替えてから取り消し・やり直しをすると、別のシートの内容が混ざって表示されることがあった問題を修正。** タブを切り替えても取り消しの対象が追随せず、最後にフォルダツリー等から直接開いたシートに固定されたままになっていた。タブを切り替えるたびに取り消しの対象を切り替え後のタブへ追随させるようにした。
- **ヒストリーパネルの「すべて」フィルタから、いま画面に出ていない別のシートの操作を巻き戻すと、そのシートを開かずに適用しようとしていた問題を修正。** 対象のシートのタブへ自動で切り替えてから適用するようにした。対象のシートが既に閉じられている場合は、適用せずに通知するようにした。
- **シートの表示設定（並び順や列幅など）の取り消しが、対象のシートを開き直した直後は反映されないことがあった問題を修正。**
- **ヒストリーパネルの「すべて」フィルタから、別のボード・CSVの古い操作を巻き戻したとき、切り替え直後の読み込み中に適用されてしまうと内容が混ざる可能性があった問題を修正。** 対象の読み込みが完了するのを待ってから適用するようにした。読み込みが完了しない場合は適用せずに通知する。

## v0.7.055 (2026-07-25)

### Claude Opus 5 への対応と、シート・Webクリップまわりの不具合修正

- **チャットのモデルにClaude Opus 5を追加した。** LLM設定のモデル一覧とCLIチャット（Claude Code）のモデル一覧の両方から選べる。思考の深さは「低い」から「最大」まで全段階を使える。
- **エントリの「サイドバーで開く」でサイドバーに何も表示されなかった問題を修正。** サイドバーの表示区画を切り替えても画面が描き直されていなかったため、内容が画面に出ない場所へ書き込まれていた。表示できないときはフロートパネルへ切り替える動作も残している。
- **Webクリップのエントリノート内で画像が表示されなかった問題を修正。** クリップ本文に保存された画像の指定（保存フォルダからの相対指定）を、ノートの場所を基準に解決するようにした。以前のバージョンで保存したクリップの画像も表示される。
- **エントリを開くと、既定パネルの設定に関係なくフロートパネルまで一緒に開いてしまう問題を修正。** 開いているパネルに追従させる処理が、閉じているフロートパネルまで開いてしまっていた。既にフロートパネルを開いている場合はこれまでどおり選択中のエントリに追従する。
- **フロートパネル・サイドバー・スマホのドロワーで、箇条書きの記号が本文の外に出て枠に接触していた問題を修正。** これらの本文だけ箇条書きの左余白が設定されておらず、記号が枠まではみ出していた。メインパネルと同じ余白に揃えた。
- **サイドバーのビューワーで、内容が区画の左右にはみ出す問題を修正。** 案内文を中央に置くための中央寄せが、実際の内容を表示するときにも効いていた。
- **「メインパネルで開く」で新しいタブを増やさず、開いているタブでエントリを表示するようにした。** 同じシートから何度もエントリを開いてもタブが増えない。固定したタブとロック中のパネルはこれまでどおり新しいタブで開く。

## v0.7.054 (2026-07-25)

### シートとパネルまわりのUI不具合をまとめて修正

- **シートのセルを編集中に、文字と文字の間をクリックしても入力が確定しなくなった。** カーソルの位置を動かすだけになる。日本語入力の変換中にクリックしたときも確定しない。同じ直し方を、列見出しの名前変更・ビュー名の編集・エントリ名列の見出しの変更にも適用した。
- **シートを開いているパネルにスケジュールを開くと、シートのツールバーが残ったまま二重に表示される問題を修正。** 画面を切り替えるときに前の画面のツールバーを片付けるようにした。
- **「エントリ名列を固定」して表を右へスクロールしたとき、エントリ名列だけ左にずれる問題を修正。** 見出しと本文とで固定位置の基準が食い違っていた。行頭の操作アイコンの列幅は、指で操作する画面での拡幅も含めて1か所で管理するようにした。
- **「サポートに送信」ダイアログが極端に小さく表示される問題を修正。**
- **パネルの境界にカーソルを合わせたときの色が、シートのセルのメニューや選択肢リストの上に重なって見える問題を修正。** 境界の色は境界線そのものだけに出るようにした。
- **シート設定の「エントリの既定の開き方」を「エントリの開き方」に変更し、選択肢が切れないようドロップダウンの幅を広げた。**
- **画面右端に並ぶアイコンにカーソルを合わせたとき、説明が表示されるようにした。**
- **シートの行頭のチェックボックスの右端が切れることがある問題を修正。**

## v0.7.053 (2026-07-25)

### シート: リレーション（リンク列）のリンク切れを列見出しの警告アイコンで表示

- **リレーション列の参照先シートが見つからないとき、これまで出ていた警告メッセージ（「項目が見つかりません」などのポップアップと赤い通知）を出さないようにした。** 代わりに、その列の見出しに小さな警告アイコン（△）を表示し、カーソルを合わせると「参照先シート『◯◯』が見つかりません」と説明が出るようにした。
- **スケジュールの「作品設定」など、シートをベースにした画面すべてで有効。** シート全般の基本機能として、削除・移動された参照先シートを指すリレーション列があると、その見出しに警告アイコンが出るようにした。
- シート本体の読み込みに失敗したときのエラー表示は、これまでどおり表示される（リンク切れの警告だけをアイコンに置き換えた）。

## v0.7.052 (2026-07-25)

### シート: セルの「＋（候補値を追加）」と候補値ステータスの一括変更を改善

- セルを選択している間も、そのセルの「＋」ボタンが表示され、クリックして候補値を追加できるようにしました（選択すると「＋」が消えて押せなくなっていた問題を修正）。
- 値がすでに入っているセルや、テキスト列などでも「＋」で候補値を複数入力できるようにしました。候補値が2つ以上あるセルでは、採用／案／ボツなどのステータスの印を自動で表示して区別できます。
- 複数のセルを選択したときに表示されるメニューから、候補値のステータスをまとめて変更できるようにしました（選択したすべてのセルの候補値が1つのときのみ）。取り消し・やり直しにも対応します。

## v0.7.051 (2026-07-25)

### シート: エントリ名列もヘッダーのドラッグ&ドロップで並べ替えできるように

- **エントリ名列を、他の列と同じようにヘッダーをドラッグして好きな位置へ並べ替えられるようにした。** これまではエントリ名列だけ常に先頭固定で、動かすことができなかった。
- **行の先頭にある「＋（エントリ追加）」「ドラッグハンドル（並べ替え用のつまみ）」「選択チェックボックス」を、エントリ名列から独立した専用の列に分けた。** エントリ名列を動かしても、これらは常に行の一番左に残る。
- **横スクロールしても画面に残る「固定表示」は、エントリ名列が先頭にある間だけ有効にした。** 列の間や末尾へ動かすと固定は解除され、先頭に戻すと固定表示に戻る。

## v0.7.050 (2026-07-25)

### シート: ビュー名編集・複数セル選択・列タイプまわりのUI改善

- **ビュータブの名前部分をダブルクリックすると、その場でビュー名を編集できるようにした。** これまではダブルクリックしてもビューが切り替わるだけで編集が始まらないことがあった。スケジュール・スタッフ管理シートなど、シートをベースにした画面すべてに反映される。
- **Shift＋矢印キーなどで複数のセルを選んだとき、選択した範囲すべてに色が付くようにした。** これまで一部の画面（単独のシートアプリ・クラウド版）では、最後のセルしか色が付いていなかった。
- **列名などの入力欄で、Enterキーを押すと確定して編集を抜けるようにした。** これまではEnterを押しても入力欄にカーソルが残ったままだった。
- **エントリを開いたときの列一覧で、各列名の前に列タイプのアイコンを表示するようにした。** また、候補値を追加する「＋」ボタンを、値の下ではなく値の並びの末尾に置くようにした。
- **列タイプを選ぶメニューの各項目に、その列タイプのアイコンを表示するようにした。**
- **新しい列タイプ「カラー」を追加した。** 横長の色見本で表示し、共通のカラーパレットから色を選べる。
- **セレクト・マルチセレクトの選択肢の色を、セル編集時の選択肢メニューからも設定できるようにした。** これまでは列の設定画面からのみ設定できた。
- **辞書ファイルを書き出すとき、ステータスを選んで出力対象を絞り込めるようにした。** 出したいステータスだけにチェックを付けて書き出せる。
- **右側のパネルの「タグ」タブのアイコンが表示されていなかったのを修正した。**

## v0.7.049 (2026-07-24)

### シート: 編集中に「保存に失敗しています」が繰り返し出る問題を根本解決

- **シートのセルや列を編集していると、保存が15秒でタイムアウトして「保存に失敗しています」「通信に失敗しました」が繰り返し表示されることがあったのを解消した。** 原因が複数重なっていたため、まとめて対処した。
- **裏で動く検索用の索引づくりと保存が内部の保存先を奪い合い、保存だけが最大15秒待たされていた**のを、保存を優先して先に通すようにした（ノートの保存では既に入っていた仕組みをシートの保存にも広げた）。
- **セルを1つ保存するたびに、シート全体の「更新前の控え」を作り直す重い処理**が走って常にもたついていたのを、連続して編集したときはまとめて1回だけ作るようにした。
- **保存の待ち時間の上限を、保存先が実際に必要とする時間に合わせて延ばし**、保存できているのに画面だけ「失敗」と出る誤表示をなくした。
- **複数のセルをまとめて貼り付け・削除したときに保存が一度に殺到して渋滞していた**のを、少しずつ流して平準化した。
- **シートの保存先ファイル（クラウド同期フォルダ上）への書き込みを軽くして**、保存の遅れを減らした。

## v0.7.048 (2026-07-24)

### シート: リレーションの参照先変更を即時反映＋エントリ名の同名チェック

- **リレーション列の参照先シートを変えた直後、値が内部ID（ent_… ）のまま表示され、再読み込みするまで名前にならなかったのを修正。** 参照先を変えたら、新しい参照先の名前解決を読み込み終えてから表示し直すようにした（再読み込み不要）。
- **エントリ名を、同じシートに既にある別のエントリと同じ名前へ変えようとしたとき、その場で「同じ名前のエントリが既にあります」と知らせるようにした。** これまでは変更が保存側で拒否されるまで一瞬その名前で表示され、拒否後に元のエントリが再読み込みまで見えなくなることがあった。

## v0.7.047 (2026-07-24)

### シート: 変更が「再読み込み」を押さなくてもすぐ反映されるように

- **エントリ名を変えたとき、その場ですぐ画面に反映するようにした。** これまではサーバーへの保存が終わるまで古い名前のままで、反映されていないように見えていた（再読み込みを押す原因になっていた）。保存に失敗したときだけ元の名前に戻す。
- **列の種類を「作成日時」「更新日時」に変えたとき、新しく作ったエントリでも日時が表示されるようにした。** これまでは一度も編集していないエントリだと「更新日時」が空欄のままだった。作成した時点の日時を入れるようにして、すぐに表示されるようにした。
- **選択肢（セレクト）の項目や色を編集したまま設定パネルを閉じても、編集が消えないようにした。** これまでは保存が確定する前にパネルを閉じたり別の列に切り替えたりすると、最後の編集が保存されず失われることがあった。パネルから離れる時点で保存を確定するようにした。

## v0.7.046 (2026-07-24)

### シートのエントリ操作・列追加・フロートパネルのUI改善

- **エントリ名のセルをクリックしたとき、フロートパネルが開かないようにした。** クリックはセルの選択のみ。ダブルクリックまたは選択後にEnterでエントリ名の編集を開始し、編集中にEnterで確定する。他のセルと同じ操作体系に統一した。
- **エントリ名のセルにマウスを乗せたとき、「開く」ボタンを表示するようにした。** クリックすると、シートごとの「既定の開き先」設定に従ってメインパネル・フロートパネル・サイドバーのいずれかで開く。
- **エントリの「…」メニューの項目を整理した。** 「詳細を開く」を廃止し、「メインパネルで開く」「フロートパネルで開く」「サイドバーで開く」の3項目に差し替えた。
- **シートの設定ダイアログに「既定の開き先」を追加した。** エントリを開くときの表示先を、メインパネル・フロートパネル・サイドバーの3つから選べる（初期値はメインパネル）。
- **ヘッダー右端の「列を追加（＋）」をクリックしたとき、列タイプの選択肢を表示するようにした。** これまではテキスト列が即追加されていたが、「左に列を挿入」と同じ列タイプ一覧から選べるようになった。追加する列の名前は選んだ列タイプ名が初期値になる。
- **日時の列タイプにサブメニューを追加した。** 「通常」「作成日時」「更新日時」の3種類を選べる。作成日時・更新日時はそのまま列名の初期値にもなる。
- **「サブパネル」の表示名を「フロートパネル」に変更した。** メニュー項目、パネルタイトル、エラーメッセージなど、ユーザーに見えるすべての箇所で統一。
- **フロートパネルがダイアログの前面に表示され、操作できてしまう問題を修正。** ダイアログが開いている間はフロートパネルを背面に移動し、操作を受け付けないようにした。
- **フロートパネルの外枠を、常時アクセントカラーで表示するようにした。**

## v0.7.045 (2026-07-24)

### シート: 列名・列タイプの変更を即座に反映し、元に戻せるように

- **シートの列名を変えたとき、Enter を押した時点ですぐに画面へ反映するようにした。** これまではサーバーへの保存が終わるまで表示が変わらず、反映されていないように見えていた（更新ボタンを何度も押す原因になっていた）。ヘッダーを直接編集した場合も、列のオプションパネルで変えた場合も、すぐに反映される。
- **列名を変えた直後に更新を繰り返すと、その列の種類（列タイプ）がテキストに変わってしまう不具合を修正。** 列名の変更と列タイプの引き継ぎが別々のタイミングで保存されていたため、その隙間に再読み込みが入ると列タイプが失われることがあった。列名の変更と同時に列タイプも引き継ぐようにして、種類が失われないようにした。
- **列名の変更を、元に戻す・やり直す（Ctrl+Z / Ctrl+Y）に対応させた。** これまでは列名を変えても元に戻せなかった。
- **列タイプの変更も、オプションパネルで種類を変えた時点ですぐに画面へ反映するようにした。** これまではパネルで種類を変えても表がすぐには変わらなかった。
- **列タイプの変更を、元に戻す・やり直すに対応させた。** テキスト⇔数値などの種類変更を Ctrl+Z で戻せる（別シートを参照する「リレーション」で相互参照を設定・解除した場合は、参照先シート側の設定は自動では戻らないため、その旨を知らせる）。

## v0.7.044 (2026-07-24)

### スケジュール: シフトとスタッフ管理シートの連携

- **シフト表を取り込むと、担当者をスタッフ管理シートへ自動で追加するようにした。** これまで取り込んだ担当者はスタッフ一覧に載らず別管理になっていた。取り込み時に、正本のスタッフ管理シートにいない名前を追加し、結果に「○名を追加」と表示する。既にいる人は変更しない（正本の保存場所を設定していないときは何もしない）。
- **取り込んだ名前が既存スタッフと紛らわしいときに知らせるようにした。** 「山田 太郎」と「山田太郎」のように空白違いで別人扱いになりそうなとき、取り込み結果に「表記ゆれの可能性」と表示する（自動では統合しない）。
- **担当者と時間の割り当てで、スタッフ管理シートの「休日」を尊重するようにした。** これまではシフト表にシフトがあれば休日でも割り当てていたが、正本で休日に設定した日は、シフトがあっても割り当てないようにした（均等配分は従来どおり）。
- **手動でシフトを追加するときのユーザー欄に、スタッフ管理シートの候補を表示するようにした。** 入力の補助として候補が出るが、一覧にない名前も従来どおり自由に入力できる。

## v0.7.043 (2026-07-24)

### スケジュール・シート・全体表示のUI改善

- **長いURLやパスが欄に収まらないとき、末尾のファイル名・フォルダ名が見えるように途中を省略して表示するようにした。** これまでは末尾が切れてしまい、何を指しているのか分からなくなっていた。ソースフォルダの一覧、フォルダの選択中表示、スタッフ管理シートの保存場所、クラウドの競合ファイル表示などに適用。マウスを乗せると全文を確認できる。
- **スケジュールのカレンダーで、複数の予定を選んだ状態で1つをドラッグすると、選んだ予定すべてが同じだけ移動するようにした。** 1件だけ選んでいるときや、これまでの操作は変わらない。自動で作られる予定（シフト・勤怠・工程タスク）は移動対象から外し、動かせないものがあればスキップして件数を知らせる。取り消し（Ctrl+Z）で一括して元に戻せる。
- **スケジュール上部に残っていた「スタッフ」タブを削除した。** スタッフの一覧・追加・編集は、設定画面や管理操作の「メンバー」から開く「スタッフ管理シート」に一本化されている。
- **スタッフ管理シートの保存場所を変えるダイアログに「一覧から選択」ボタンを付けた。** フォルダツリーから選べるようになり、列タイプの設定ダイアログなどと同じ操作に揃えた。
- **シートのリレーション（別シートの参照）で、入力した名前が参照先に無いとき、その場で新しいエントリとして追加できるようにした。** 検索欄に打った文字が候補に無ければ「『◯◯』を新規作成」が出て、押すと参照先シートにエントリを作り、そのまま設定する。集計（ロールアップ）はこの参照の更新に自動で追随する。

## v0.7.042 (2026-07-24)

### シートの列タイプ: ロールアップがテキスト列に見える問題ほかを修正

- **ロールアップ列に変えてもテキスト列のままに見える問題を修正。** リレーション列を指定し終えるまでの間、セルが元のテキスト値をそのまま表示していたため、テキスト列と見分けが付かなかった。設定が終わっていない間は「未設定」と表示し、何を指定すればよいかをツールチップで案内するようにした。数式列も同じ状態（数式が未入力）で同様に扱う。
- **ロールアップの設定画面で、選べない理由が分かるようにした。** シートにリレーション列が1つも無い場合は「リレーション列がありません」と表示して選択を無効化し、先にリレーション列を作れば集計できることを添えた。「参照先の列」も、リレーション列を選ぶ前は「読み込み中...」ではなく「リレーション列を選ぶと表示されます」と表示する。
- **「列タイプの設定」ダイアログの「一覧から選択」ボタンのアイコンが潰れていた問題を修正。** ダイアログ内ではボタンの左右余白が優先され、アイコンが幅2pxまで縮んで別物に見えていた。オプションパネル側と同じ表示に揃えた。
- **表示していないシートの列タイプを保存すると、そのシートの他の列の設定が消えることがある問題を修正。** シート情報を読み込んでいない状態では手元にある分だけを送っていたため、フォルダノート側の設定が丸ごと置き換わっていた。保存前に現在の設定を取得して差分を重ねるようにした（削除済みの列は取り直した値から除いて、削除が取り消されないようにしている）。
- **列タイプの保存に失敗したとき、画面に何も出ない問題を修正。** 他の人が編集中などで保存できなかった場合に警告ログだけで終わっていたため、ステータス表示で知らせるようにした。

## v0.7.041 (2026-07-23)

### 基本UIの説明文をツールチップへ集約（Meldex全体の共通ルール化）

- ダイアログ・オプション/設定パネル・フォームの各フィールドに長い説明文を直接載せず、ラベル横の小さな「?」（ヘルプアイコン）に集約して、説明はホバー/フォーカス/長押しのツールチップで見せる方式に統一した。共通ヘルパー `fieldHelp()` と `.gb-field-help` / `.gb-check-help-row` を追加し、`app/AGENTS.md`「UI共通ルール」と UI監査入口ドキュメントに恒久ルールとして明記した。
- きっかけとなった「列タイプの設定」ダイアログを全面整理し、各設定項目の説明（リレーション/双方向/カスケード、カレンダー連動、数式、選択肢の色、初期値、テンプレート等）をすべてツールチップへ移した。UIに漏れていた内部の仕様参照（「Phase 1 §5.2」）も除去した。
- 設定ダイアログ本体（ホームフォルダ・保存先・スマホ接続・LLM APIキー・ローカルLLM・CLIチャット・ナレッジ・Web Clipper・拡張機能ほか）を整理。あわせて内部用語・コード名のUI露出（AES-GCM / localStorage / localhost 等）を、意味の伝わる言い回しへ置き換えた。
- 主要アプリの各ダイアログ・オプションパネルにも横断適用: シート（エントリ名テンプレート・カレンダー連携・スマートシート・テンプレート編集・ステータス連動ほか）、シナリオ（タイプ管理・役割オプション・ルビ・CLIP STUDIO送信）、ボード（カード構造・ステータス管理）、制作管理・カレンダー（かんたん割当・一括作成・分類名編集・同期ほか）、チャット/管理者鍵/ワークスペース連携。
- 同意・プライバシーに関わる開示（外部AIへの送信、暗号化保存 など）はツールチップに隠さず、要点の1文を可視のまま残し、詳細だけをツールチップへ送った。設定変更が必要な操作の妨げにならないよう配慮した。

## v0.7.040 (2026-07-23)

### マニュアル追記: CLIチャットのモデル選択・思考の深さの制限事項

- CLIチャット連携マニュアルに「モデル・思考の深さの制限事項」の節を追加した。モデル一覧が代表候補であることと一覧に無いモデルの自由入力方法、速度の調整の考え方（軽いモデル×浅い思考）、Ultracode が Claude Code 専用で利用枠の消費が大きいこと、Codex CLI の「最大」の扱い、Gemini CLI が思考の深さ非対応（APIキー方式の Gemini なら対応）であることをまとめた。

## v0.7.039 (2026-07-23)

### MeldexWebClipperで一般サイトの動画も保存できるようにした

- ブラウザ拡張「MeldexWebClipper」で記事を保存する際、ページ内の`&lt;video&gt;`の直リンク動画（mp4/webm/mov）を画像と同様にダウンロードして保存するようにした。従来は画像しか収集しておらず、動画は本文中から静かに抜け落ちていた。
- 保存した動画は既存の「画像」列にアイテムとして混在し（画像/動画をXブックマークと同じ扱いで区別）、シートの「画像」列から再生表示できる。ノート本文中の該当箇所も動画プレイヤーとして開けるようマーカーを付与した。
- `blob:`配信の動画（X/Twitter等、JS制御のストリーミング）や`.m3u8`/`.mpd`のようなプレイリスト形式は、単一ファイルとして保存できないため対象外とし、静かにスキップする（偽のリンクは作らない）。Xの動画は既存の「Xブックマーク」機能（公式X連携）で従来どおり正しく取得できる。
- 保存設定の「記事内の画像も保存」チェックボックスの表記を「記事内の画像・動画も保存」に変更し、同じ設定が画像・動画の両方を制御することを明確にした。

## v0.7.038 (2026-07-23)

### リレーション列「一覧から選択」を全ワークスペース横断のフォルダツリーに刷新＋参照先シート候補の絞り込みを拡張

- リレーション・マルチリレーション・マルチソースリレーション・日時列のカレンダー連動で使う「一覧から選択」を、保存先選択などで使っている共通のフォルダ選択画面に統一。ホームフォルダ・すべてのソースフォルダ・すべてのワークスペースを横断し、フォルダとシートだけを表示するフォルダツリーから選べるようにした（以前はシートが1件でも入っているフォルダしかツリーに現れず、ホームフォルダのシートも一切出ていなかった）。
- 検索欄でシート名から直接絞り込めるようにした。開いた時点で編集中のシートの位置まで自動的にフォルダを開いた状態で表示する。
- 「参照先シート」欄の候補は、「作品フォルダに設定」済みならその中だけに絞り、未設定でも編集中のシートと同じワークスペース・ソースフォルダ・ホームフォルダの中だけに絞るようにした（以前は作品フォルダ未設定時にすべてのシートが候補に出ていた）。絞り込みが効いている範囲は入力欄の下に表示する。
- シート一覧の走査にホームフォルダを含め、深い階層のシートも拾うようにした。
- 「一覧から選択」ボタンのアイコンをフォルダツリーを表す見た目に変更。
- 再発防止として、ホームフォルダ横断・重複排除・深い階層の検出を確認する自動テストを新設。実機ブラウザでリレーション・マルチリレーション・マルチソースリレーション・ロールアップ・双方向リレーションの一連の操作を確認済み。

## v0.7.037 (2026-07-23)

### AIチャットのモデル選択と「思考の深さ」を拡充（Claude Codeは低〜Ultracodeに対応）

- CLI型プロバイダ（Claude Code / Codex CLI / Gemini CLI）でも、チャット画面のモデル欄から複数の候補を直接選べるようにした。Claude Codeは「CLI既定」に加えて Fable 5 / Opus 4.8 / Sonnet 5 / Haiku 4.5 を選択でき、選んだモデルはそのまま会話に反映される（設定画面の保存値は、まだ選んだことがない端末での初期値として尊重）。
- 「思考の深さ」を オフ/低/中/高/最高/最大 の6段階へ拡張し、Claude Code選択時はさらに「Ultracode」を選べるようにした（複数のエージェントを並列実行する公式の高負荷モード。トークン消費が大きい点に注意）。Codex CLIも思考の深さに対応し、Gemini CLIは公式に指定手段がないため従来どおり非対応の注記を表示する。
- 現行のClaudeモデル（Fable 5 / Opus 4.8 / Sonnet 5 など）で「思考の深さ」を指定すると通信エラーになり、黙って思考オフへ切り替わっていた不具合を修正。あわせて、これらのモデルでは無効になった詳細パラメータ（温度など）を送らないようにし、設定画面に注記を追加した。ブラウザから直接通信するクラウド版の同経路と、クロスレビュー機能に潜んでいた同型の問題も修正。
- 設定 > LLM > CLIチャットのモデル欄で、候補一覧が現在の入力値で絞り込まれてほぼ表示されなかった問題を修正（未設定時は空欄になり、クリックで全候補が出る）。
- モデル未指定時の既定を現行世代（Sonnet 5 相当）へ更新。
- マニュアル（LLM設定・CLIチャット連携）を新機能に合わせて更新。

## v0.7.036 (2026-07-23)

### 「操作に失敗しました」通知の多発を抑制し、CLIP STUDIO連携の状態表示がエラー扱いになる不具合を修正

- 同じ種類の失敗が短時間に繰り返し起きた場合、右下の「操作に失敗しました」通知を1件にまとめるようにした（表示中は自動消去までの時間を延長し、消えた直後の同種のみ15秒間抑制。別の種類の失敗は今までどおり即座に表示する）。起動直後などに複数の画面部品が同時に同じ失敗へ遭遇すると、そのたびに新しい通知が積み上がっていた。
- CLIP STUDIO連携（SEPサーバー）の状態表示で、接続成功などエラーでない通知にも「エラー: 」が付いて見える不具合を修正。ステータスバーの警告欄が内容を問わず「エラー: 」を前置していたため、本当にエラーの時だけ付くようにした。
- 再発防止として、通知の重複抑制と状態表示の分類を実コード実行で検証する自動テストを新設（修正前のコードでは失敗することを確認済み）。

## v0.7.035 (2026-07-23)

### チャットのAIタブと付箋の種類フィルタがウィンドウリサイズで勝手に戻る不具合を修正

- 右サイドバーのチャットで「AI」タブを開いていても、ウィンドウサイズを変えると「ワークスペース」タブへ強制的に切り替わってしまう不具合を修正。リサイズのたびに画面レイアウト全体を組み直す処理が「初回用の初期化」を毎回実行し、タブ選択を既定値へ戻していたことが原因（パネルの開閉・分割・ドッキングなどでも同じ現象が起きていた）。
- 同じ原因で、付箋タブの種類フィルタがリサイズのたびに「付箋」へ戻ってしまう同族の不具合も修正。
- 再発防止として「AIタブ選択→リサイズ→維持」「付箋フィルタ変更→リサイズ→維持」の自動テストを新設（修正を外すと実際に失敗することを確認済み）。

## v0.7.034 (2026-07-23)

### シートUIの不具合修正・機能追加＋用語統一（9件）

**不具合修正**

- 列ヘッダーのリネームが反映されない不具合を修正。サーバー保存は成功していたが、描画に使う列一覧の配列を更新し忘れていたため旧名が画面に残っていた（開き直すと反映されていた）。
- 「一覧から選択」ポップアップが背後に隠れて押しても反応しないように見える不具合を修正。クラウド/モバイルの全画面オーバーレイより前面に出るよう重なり順を修正した。
- AIチャットで思考中に連続送信すると2通目以降がエラー（WinError 206）で止まる不具合を修正。会話履歴の蓄積で起動コマンドが長くなりすぎてWindowsが起動を拒否していた。プロンプトを標準入力経由に統一し、長すぎる場合の分かりやすい案内も追加した。
- 列タイプの設定ダイアログなどの左右余白が消えていたレイアウト崩れを修正（列タイプ設定・条件付きカラー・各フィルタ・テンプレート作成/編集）。シートアプリの全ダイアログを監査し、共通ルール準拠を確認した。

**機能追加・改善**

- 列ヘッダーメニューの「左に列を挿入」「右に列を挿入」をトップに配置し、挿入する列タイプをその場で選べるようにした。新しい列の初期名は選んだ列タイプ名になる。
- 「折り返し/切り詰め」を「折り返し設定」に改名し、親メニューを閉じずにサブメニューとして開くようにした。エントリ名列でも折り返し設定が可能になった。
- 列タイプの並び順で、ロールアップをリレーションの直後に配置した。
- WebClipperで画像ホバー時に出ていた「Meldexに保存」ボタンを廃止した（画像単体の保存は右クリックメニューから行える）。

**用語整理**

- シートアプリのユーザー向け表示から「プロパティ」を一掃し、文脈に応じて「列」「列タイプ」に統一（約120箇所）。用語仕様書も同方針に更新した。

**徹底チェックでの追加修正**

- 同じシートを分割ビューの2ペインで開いているとき、片方で列名を変更するともう片方が列名だけ変わって中身が空に見える不具合を修正（もう片方の表示データも新しい列名へ移行するようにした）。
- エントリ名列で折り返し設定を「シート設定に従う」にしたとき、実際にはシート全体の折り返し/切り詰め設定に従っていなかったのを修正（通常の列と同じ挙動に統一）。

## v0.7.033 (2026-07-23)

### feat: クイックメモを他の単独アプリ5種と同じ扱いに（Windows単体版＋クラウドDropbox保存）

クイックメモは配布ページのグリッドには載っていたが、Windows単体版のダウンロード先が存在せず、クラウド版はDropboxに保存されずブラウザ内保存のままだった。他5アプリ（ノート/シナリオ/ボード/シート/タイマー）と同じ入口に揃えた。

- Windows単体版: 専用ランチャー`MeldexQuickMemo.py`（既存の`quick_memo_server.py`をephemeralポートで起動しwebview表示）と、それをビルドする`build_quick_memo_exe.py`を追加。ファイル編集アプリではないため`build_standalone_apps.py`の共通枠には乗せず、ボード版exeと同じ「専用ランチャー+専用ビルドスクリプト」方式にした。
- クラウド版: `quick-memo-standalone.html`を新規作成し、`cloud_standalone_apps.py`の`STANDALONE_APPS`に登録。`apps/quick-memo/`として他5アプリと同じPWA基盤（マニフェスト・専用sw.js・検証ゲート）で生成されるようにした。
- クラウド保存: `quick-memo.js`にクラウドモード判定を追加し、標準エンドポイント（`/file`・`/outliner/add`・`/db-metadata`・`/entity/create`・`/value`）だけで「クイックメモ」シートへ保存する経路を実装。新規メモは`/entity/create`、2回目以降の自動保存は既存パスへの`/file`上書きでfrontmatterを丸ごと書き直す。タグ候補はシートの「タグ」列の選択肢を使用（共通タグカタログは対象外）。自動タグ付けは既存候補との単純一致に簡略化。音声書き起こしはWeb Speech APIが使える環境ではそのまま動作し、サーバー文字起こしへのフォールバックのみ従来どおり利用不可の案内を表示する。一覧機能はクラウド版では非表示（デスクトップ専用のまま）。Dropbox未接続時は接続バナー（コード方式の手動認証）を表示する。
- 配布ページ: `public-standalone-apps.js`のクイックメモの`cloudUrl`を`apps/quick-memo/`、アイコンを専用アイコンに変更。表示名も他アプリと同じ「Meldex 」接頭辞に統一。
- リリースワークフローに`MeldexQuickMemo.zip`のビルド・アップロード手順を追加。

実ビルド検証で発見・修正した問題:
- Windows単体版のPyInstaller除外リスト漏れにより`sentence-transformers`系（torch/transformers/sklearn等）が誤同梱されビルドが肥大化する問題（`build_exe.py`と同じ除外リストを追加）。その対応で`starlette`を誤って除外しfastapiのimportが壊れる問題も同時に修正。
- クラウド保存で、新規メモの`/entity/create`成功直後に`/value`（本文書き込み）が失敗すると、再試行のたびに空のエントリが重複作成される問題（`saveMemoCloud()`にフォールバック書き込みを追加し、決定的なパスへの直接上書きで自己修復するよう修正。ブラウザでのモック再現検証で確認済み）。

検証: `test_meldex_standalone_distribution.py`（6アプリ構成に更新、全緑）、`test_meldex_quick_memo.py`・`test_meldex_batch205_quick_memo_regressions.py`（既存のまま全緑）、`test_meldex_standalone_profile.py`・`test_standalone_apps.py`（共有ファイル変更の無影響確認、全緑）。実ビルド（Windows exe起動確認・クラウド静的ビルドのブラウザ実機確認）とあわせ詳細は `_verify/2026-07-23_quick-memo-standalone-parity/README.md` を参照。

## v0.7.032 (2026-07-23)

### fix: シートの参照先候補が作品フォルダで絞り込まれても無言のままだった問題を修正（R3）

参照先シートのテキスト欄候補は、既存の「作品フォルダに設定」機能により作品フォルダ配下だけへ絞り込む実装がすでにあったが、絞り込みが効いているかどうかがユーザーから見えず、「あるはずのシートが出ない」という行き止まり感につながっていた。絞り込みが効いている間は入力欄の下に「◯◯（作品フォルダ）内のシートを表示中」という一言を添えるようにした。作品フォルダ未設定、または編集中のシートが配下に無い場合は表示せず、従来どおり全件を出す。

あわせて、「一覧から選択」のフォルダツリーポップアップに検索欄という選択以外の操作があるにもかかわらず閉じるボタンが無かったため、`attachMeldexDropdownCloseButton()` で右下に閉じるボタンを追加した（`app/CLAUDE.md`「ドロップダウンの閉じ方とキーボード操作」に準拠）。

検証: 作品フォルダを設定した状態・未設定の状態それぞれで候補の絞り込みとヒント表示を実機ブラウザで確認。ツリーポップアップの閉じるボタンの表示とクリックでの正常終了も確認。

## v0.7.031 (2026-07-23)

### fix: シナリオで前の文字列（textBefore）付きの型を使うとルビの位置がズレる問題を修正（R2）

横書きシナリオで、行頭に「（」等の前置文字を出す型を使うと、ルビ付き文字の位置が本来の位置からずれて表示される不具合を修正した。主因（継承されたtext-indentがルビ用spanの中身だけをずらす）は先行修正済みだったが、以下3件の副次要因が残っていた。

- 前置文字の占有幅を`::before`の固定1em幅で見積もっていたため、実際の文字幅（1文字/2文字以上/半角記号で異なる）とズレると1行目と折り返し行で本文の開始位置が食い違っていた
- ぶら下げインデントがtext-indentによる1行目限定の処理だったため、そもそも上記のズレが起きる構造だった
- 型の「インデント」設定と前の文字列を併用すると、インデントの直接指定がスタイルシートの前置文字幅予約を上書きし、text-indentだけが残ってさらに1emずれていた

対応として、`data-before`付きのテキスト列をtext-indentに頼らないflexレイアウト（前置文字と本文を横並びのflexアイテムにする）へ作り替えた。前置文字は内容なりの幅を占め、本文は残り幅で折り返す独立ボックスになるため、1行目・折り返し行のどちらも本文の開始位置が自然に揃い、型のインデント設定とも競合しなくなる。型に縦方向中央寄せ（data-valign）が設定されている場合は、そちらが要求するdisplay:block構造を優先し、旧方式のまま維持する（縦書きモードは元々この構造を使っていないため対象外）。

あわせて、後の文字列（textAfter、例:「）」）がルビの重なり判定に一切参加していなかったため、最終文字のルビが閉じ括弧に被る問題も修正した。後の文字列の占有幅を「動かせない境界」として同じ行の重なり解決（resolveRubyOverlaps）に参加させ、必要に応じてルビが縮小・再配置されるようにした。

検証: 前の文字列（1文字/2文字以上/半角記号）×1行目/折り返し行×型インデント併用有無×横書き/縦書きの組み合わせを実機ブラウザで確認（DOM上の矩形比較）。後の文字列との重なりも実機で確認（現実的なルビ長では重なりゼロで解決、極端に長いルビでは既存の縮小下限0.6倍が働くため残存重なりが出ることがあるが、これは重なり解決アルゴリズム自体の既存の設計上の制約であり本修正の対象外）。新規E2Eケース5件（前の文字列の1文字/2文字以上/半角記号/型インデント併用/縦書き）を追加し全件合格。既存のscriptnote micro E2Eスイートで回帰がないことを確認。

## v0.7.030 (2026-07-23)

### fix: 単独ウィンドウ表示のボードで「オプションを閉じる」が効かなかった問題を修正

`?single=1` で別ウィンドウ表示したボードで、オプションパネルを開いた状態からもう一度ボタンを押しても閉じられず、開く方向にしか動かなかった（単独版ボードアプリでは既に正しくトグルしていた）。原因は本体側のボタン処理が「閉じている時だけ開く」条件で覆われており、既に開いている状態でのクリックを素通りさせていたこと。この条件を外し、単独版と同じ「現在の表示状態を反転する」処理に統一した。

検証: 隔離検証インスタンスで `?single=1` 表示のボードを開き、開く→閉じる→開くの3回クリックを実機ブラウザで確認（`#rp-detail` の表示切り替わりをDOM上で確認）。`run_frontend_e2e_board_micro.ps1` を実行し、既存失敗1件（`targeted-board-micro-bulk-import-dialog-contracts`、画像再指定ボタンの識別子不足によるE2E側の既知不備）が本修正と無関係な前から存在するベースライン失敗であることを、本修正を一時的に取り消した状態での再実行で確認した。

## v0.7.029 (2026-07-22)

### fix: シナリオのタイプ選択メニューに「リンクを設定」が実装されていなかった（row-popup既存赤2件）

シナリオmicro E2Eの既存赤として残っていた `targeted-scriptnote-micro-row-popup-controls` / `targeted-mobile-scriptnote-micro-row-popup-controls`（2026-07-21発見）を修正した。原因はテストの期待値陳腐化ではなく実装漏れで、行のタイプ選択メニュー用CSS（`.sn2-role-link`）だけが先に用意され、対応するボタン生成コードが存在しない状態だった。

タイプ選択メニュー（役割ボタンクリックで開くポップアップ）の末尾に「リンクを設定」ボタンを追加した。クリックするとメニューを閉じ、共通のリンク挿入モーダルを開いて、選択結果を行本文の末尾へ手動リンクとして挿入する（既存の選択範囲からのリンク挿入と同じ挿入ロジック `_insertLinkResultIntoText` を再利用）。モバイル向けにタップ領域44pxのCSSも合わせて追加した。

検証: 関連する静的回帰テスト35件は全緑（`test_meldex_scriptnote_micro_coverage.py` / `test_meldex_batch128_scriptnote_regressions.py` / `test_meldex_batch154_tools_css_regressions.py`）。**対象2ケースの実機E2Eは、セッション中の同時実行負荷（Chrome 78プロセス・Python 9プロセス）により3回連続でシナリオ表示自体がタイムアウトし未確認**（無関係な既知合格ケースでも同一のタイムアウトを再現しており、環境要因と判断）。次回セッションで `powershell -ExecutionPolicy Bypass -File app/tests/run_frontend_e2e_case_batch.ps1 -Browser chrome -CaseList "targeted-scriptnote-micro-row-popup-controls,targeted-mobile-scriptnote-micro-row-popup-controls"` の実機確認を行うこと。

## v0.7.028 (2026-07-22)

### fix: E2E蓄積赤29件の解消（実バグ3件の修正 + テスト期待値の追随 + 検査部品の識別子整備）

2026-07-01以来蓄積していたシェル系微細挙動テストの既存失敗29件（+シナリオ側の類似1件）を全件分類し、全緑へ戻した。内訳は「実バグ3件・テスト期待値の陳腐化/テスト自体の不備が大半・負荷起因のゆらぎ数件」。

実バグの修正:
- 開発用画面（Meldex-dev.html）のスクリプト一覧に自動実行スケジューラと画像ドロップ切替の2本が漏れており、開発用画面ではNotion同期の自動実行設定が丸ごと表示されない状態だった。本番用との読み込み一覧の同期を検査する回帰テストを新設し、再発を機械的に防ぐ
- チャットのMarkdownリンク等でツールチップを抑止する除外リストが、旧来のtitle表示経路から迂回されて効かない構造だった。共有ツールチップ側に適格判定を公開し、旧経路も除外を尊重するようにした
- ダイアログのボトムシート化を解除した後、リサイズハンドルが二度と復元されないことがあった（シート化時の後片付けがハンドルだけ除去し「導入済み」の印を残すため、以後どの機構も再導入しない）。後片付けで印も併せて解除するようにした
- 自動実行スケジューラとカラーパレットのプリセット色見本にUI監査用の安定識別子が無く、品質ゲートを通過できなかった（識別子とアクセシビリティラベルを付与）

テスト側の追随・堅牢化（アプリの意図的な機能変更に未追随だったもの）:
- DM相手の取得元一元化（スタッフ管理シート）、Notion自動同期の共通スケジューラ化、閉じるアイコンの20px統一、設定「接続・共有サーバー」タブへの移設、一括操作バーのパネル内フロート化、ツール名「スケジュール」への統一、スマホドロワー化、へそれぞれ追随
- メニュー表示直後の先頭項目フォーカス背景（アクセシビリティ由来）を透明チェックの許容対象へ（既存の同種修正と同じパターンを5箇所へ展開）
- 単一画像ボード作成テストのモックが404を模していなかった不備、サブパネル親リンクの非同期生成待ち不足、モバイルシート開閉アニメーションの幾何条件待ち、ズーム下の座標差によるクリック判定の偽陽性、を修正
- 重量ケースの時間予算を実測に合わせて調整し、ケース単位の経過時間ログを常設（今後のタイムアウト調査を1回のログで判別可能に）

## v0.7.027 (2026-07-22)

### fix: シートから戻った後もエントリの「戻る」履歴が正しく積まれ、スクロール位置と対象行が復元されるように

シートでエントリを開いて「戻る」で復帰した後、もう一度別のエントリを開くと、その復帰用履歴が表示中のタブではなく不可視の旧共有履歴へ記録され、以後の「戻る」が古い位置へ着地する問題を修正した（初回だけ正常に動き、2回目以降で壊れる）。原因は、旧2分割ビュー時代のフォールバックが返す実在しないペインID（'main'）が、アクティブペインの解決を短絡評価で覆い隠していたことで、履歴解決の関所（`_resolveNavHistoryPaneId`）で実在しないIDを無指定扱いへ降格する防御を追加した。制作管理の埋め込みシートからエントリを開いた場合の履歴も同じ防御で表示中タブへ積まれる。

あわせて、履歴に保存するスクロール位置が常に0で記録され、復元も素通りしていた問題を修正した。原因はコンテナ探索がクラス名（`.pivot-view` 等）のみで、本体画面のコンテナ（IDのみ）が一度もヒットしなかったこと。ID優先（`#pivot-view` → `.pivot-view`）の探索へ変更し、「戻る」でシートに復帰した際にスクロール位置と対象行のハイライトが実際に復元されるようになった。埋め込みシート（クラス識別）との取り違え防止も兼ねる。復元の実行タイミングも描画フレーム待ち単独からタイムアウトフォールバック併用（既存 `_nextFrame` と同パターン）へ変更し、フレームが生成されない状況（バックグラウンドタブ等）でも復元が走るようにした。

- 回帰テスト: `test_meldex_pane_tab_nav_history.py` に静的ガード2件 + Node実行の挙動検証2件を新設（実在しないペインIDの降格・GBLayout不在時のlegacy維持・スクロールコンテナのID優先とクラスフォールバック）
- 実ブラウザE2E: `targeted-shell-micro-sheet-nav-second-cycle` を新設（「戻る」後の2周目往復で履歴がタブへ積まれ、対象行ハイライトが復元されることを実機検証。既存の1周目ケースでは検出不能だった）
- 調査記録: `app/docs/pane-context-paneid-fallback_plan_2026-07-22.md`

## v0.7.026 (2026-07-21)

### fix: メインパネルの戻る/進むをタブごとに独立させ、シート画像クリックからも戻れるように

メインパネルの戻る/進むが「タブが複数ある場合」や「シートの画像列の画像を表示している状態からの戻る」でうまく対応できていなかった問題を解消した。戻る/進む履歴はタブ単位（ブラウザのタブと同じ方式）になり、戻る/進むでタブが切り替わることは無くなった。

- 戻る/進む履歴をペイン単位からタブ単位へ移行。タブ切替後に「戻る」を押すと、今見ているタブの中身が別タブの履歴で上書きされる不具合を根治した
- タブ切替・パネルセットのグループ切替・ペイン間タブ移動のすべてで、戻る/進むボタンの表示が今アクティブなタブの履歴へ即座に追随するようにした
- シートの画像列セルをクリックした際、シートの表示状態（スクロール位置）を履歴へ積んでから画像を開くようにした。戻るでシートの元の位置へ復元でき、タブの種別表示も画像/シートで正しく切り替わる（副次的に、シートの動画アイテムも正しい再生表示で開けるようになった）
- タブを閉じるとその履歴も一緒に破棄され、戻る/進むボタン表示が直ちに追随する
- 旧バージョンが保存したレイアウト（ペイン単位の履歴）は、読み込み時にアクティブタブへ自動移行される（追加のみの変更でスキーマ互換を維持）。起動前サニタイズもタブ単位履歴の削除済みパス剪定に対応
- 検証: 静的回帰テスト9件・サーバ側サニタイズテスト2件を新設して合格。実Chromeでの新規E2E（タブ別履歴の分離／シート画像セルの往復／タブを閉じた際の履歴破棄）に3回連続合格。全体unittest（3659件）を実行し、自変更起因の失敗は文字列アサート1件のみで即修正、残りは無関係の既知差分と個別確認

## v0.7.025 (2026-07-21)

### feat: スマホ版の左右サイドバーをドロワー化し、フォルダツリー/フォルダパネルの一体化を解除

スマホ版のフォルダツリー（左）とリンク/詳細プレビュー（右）を、画面の一部だけを覆うドロワーに統一した。ドロワーの外側には隙間が残り、半透明の背景越しにメインパネルの内容がうっすら見える。あわせて、スマホ版で一体化されていたフォルダツリー/フォルダパネルを分離し、フォルダ行のタップでデスクトップ版と同じくメインパネルにフォルダの中身が開くようにした。

- 左ドロワー（フォルダツリー）: 幅 min(85vw, 400px)。閉じるハンドルはドロワー右端（隙間側）に配置し、タッチ領域44px以上を確保。隙間のタップでも閉じられる
- 右ドロワー（リンク/エントリのプレビュー）: 左の鏡像に変更。閉じるハンドルを右端→左端へ移動し、左側に隙間を確保
- フォルダツリーのヘッダーにあった「ツリー/一覧」切替を撤去し、ドロワーはツリー専用にした。フォルダ・シートの行をタップするとメインパネルへ直接開き、ドロワーは自動で閉じる
- スワイプで閉じる・画面左端スワイプで開く・Escで閉じる等の既存操作は従来どおり
- 検証: 静的回帰テストを新実装へ更新し合格。実ChromeのモバイルE2E（ドロワー幅と隙間・ハンドル位置・バックドロップ・フォルダタップ→メインパネル遷移+自動クローズ・右ドロワーの鏡像配置、を一気通貫で確認）に合格。モバイル微細挙動E2E全21件中20件合格（残り1件は変更前から失敗する無関係の既存不具合であることをベースライン比較で確認）

## v0.7.024 (2026-07-21)

### fix: ツリー側のリネームでも、シナリオ／スマートシートの内部タイトルが追従するように

フォルダツリーでファイルをリネームしても、シナリオとスマートシート／ダッシュボードの内部に保持されたタイトルが更新されず、次に開いた時や保存し直した時に古いタイトルへ戻ってしまう問題を解消した。ファイル名とタイトルが全ファイル種別で双方向に一致するようになった。

- サーバ側: リネームAPIが、シナリオ・旧シナリオ・スマートシート（ダッシュボード含む）の内部タイトル相当フィールド（title/name）を新しいファイル名へ書き換えるようにした。ノートの見出し書換と同じタイミング・同じ安全機構に相乗り。JSON解析に失敗するファイルは書換だけをスキップし、リネーム自体は成功させる。フィールド自体を持たないファイルへの新規追加はしない（旧版互換維持）
- フロント側: ツリーでリネームした際、既に開いているシナリオエディタの表示タイトル・タブラベル・内部データを追従させ、開いているスマートシートの表示タイトルも同様に追従させるようにした
- 未保存の編集を保持したままツリー側リネームが来ても未保存状態は維持され、その後保存すると新タイトルと未保存編集の両方が正しく保存される
- ノート（見出し書換済み）・シート（フォルダ名ダブルクリック）・ボード（内部タイトルという概念を持たない）は従来どおり一致する。タイマーは内部フィールドが画面表示に使われていないことを確認し対象外とした
- 検証: サーバ側の内部タイトル書換テスト18件・フロント実装の静的固定テスト5件を新設して合格。実Chromeでの新規E2E（未保存編集を保持したままツリー相当のリネーム→タイトル・タブ・内部状態が追従→保存で両方永続化→復帰リネームも追従、を一気通貫で確認）に3回連続合格。隣接する既存回帰テスト182件を実行し、無関係な既知のビルド成果物同期差分を除き全て合格

## v0.7.023 (2026-07-21)

### feat: バージョン管理パネルがメインパネルの表示ファイルに追従するように

パネルメニュー・コマンドパレット・右レール・モバイル編集バーなど、対象ファイルを指定せずに開いたバージョン管理タブが、メインパネルで表示中のファイルへ自動的に追従するようにした。

- 対象未指定でバージョン管理タブを開くと「追従タブ」になり、メインパネルの表示ファイルが変わるたびに自動でバージョン一覧が切り替わる。追従は (a) ノート内リンク等で同一タブ内のファイルを切り替えた時 (b) ペイン内のタブを切り替えた時 (c) 別のペインをアクティブにした時、いずれでも即座に反映される
- 各アイテムを右クリック等でファイルを明示して開いたバージョン管理タブは従来どおりピン留めされ、メインパネルの表示切替に連動しない
- メインパネルに対象が無い（何も開いていない）状態では、追従タブの表示を空にせず直前の表示を維持する
- 保存済みレイアウトから復元される既存のバージョン管理タブは、この変更後も従来どおりピン留め扱いのまま開く（互換性維持）
- 検証: 静的回帰テスト9件を新設して合格（既存3件のピン留め文字列アサートも新シグネチャへ更新）。実Chromeでの新規E2E（対象未指定で開く→追従表示→同一タブ内切替/タブ切替/ペイン切替の3経路すべてで追従→右クリック相当の明示オープンはピン留めされ連動しない→対象喪失時も直前表示を維持、を一気通貫で確認）に6回連続合格。隣接するペイン・バージョン管理関連の既存回帰テスト29ファイル325件を実行し、無関係な既知の差分5件を除き全て合格

## v0.7.022 (2026-07-21)

### feat: エントリのプロパティ一覧を開閉・列幅設定できるように

エントリを開いた時に上部へ表示されるプロパティ一覧（フルページ版・サブパネル版・モバイルドロワー共通）に、開閉トグルと列幅設定を追加した。

- プロパティ一覧の見出し行に開閉シェブロンを追加。クリックで一覧本体（並び替えツールバーを含む）を折りたたみ／展開できる
- 開閉状態はシート（エントリの親フォルダ）単位で保存され、別のエントリを開いた時やフルページ版/サブパネル版を切り替えた時にも保持される
- 見出し行右端の列幅設定ボタンから、プロパティカードの列幅（既定300px、150〜600pxの範囲）を数値入力で調整できる。変更は入力と同時にプレビューされ、確定でシート単位に保存される。「既定に戻す」でワンクリックで300pxに戻せる
- 列幅に応じてパネル幅に合わせた複数列表示になる（従来はサブパネル版がCSS未対応で実質1列固定だった欠落を解消し、フルページ版の初期列幅も260px→300pxへ統一）
- 検証: 静的回帰テスト2件を新設して合格。実Chromeでの新規E2E（サブパネルでの折りたたみ→サブパネルを閉じて別エントリで開き直しても保持→列幅変更のプレビュー/保存/既定復帰→フルページ版でも反映、を一気通貫で確認）と、隣接する既存サブパネルE2E2件の回帰確認に合格

## v0.7.021 (2026-07-21)

### feat: シートのセル折り返し/切り詰めを列ごとに設定できるように

シートの「折り返し/切り詰め」表示設定が、これまでシート全体で1つだけだったのに対し、列ごとに個別設定できるようにした。

- 列ヘッダーメニューに「折り返し/切り詰め」項目を追加。「シート設定に従う（既定）」「折り返し」「切り詰め」の3択+最大行数を列ごとに指定できる
- 列ごとの設定はシート全体設定より優先される。設定していない列は従来どおりシート全体設定に従う
- 列に個別設定がある場合、ツールバーの折返しボタンの説明に「（一部の列は個別設定）」と表示する
- 列ごとの設定は他のシート表示設定と同様に保存され、シートを開き直しても保持される
- テーブル表示（ピボット）限定の機能。ギャラリー/カンバンのカード表示・タイムライン表示・シート単独版は対象外（従来どおりシート全体設定のみ）
- 検証: 静的回帰テスト2件・サーバ側保存往復テスト1件を新設して合格。実Chromeでの新規E2E（列メニューから切り詰め設定→対象列のみ反映→シート設定に従うへ復帰→再設定して再読み込み後も保持、を確認）と、隣接する列メニュー関連E2E4件の回帰確認に合格

## v0.7.020 (2026-07-21)

### feat: チャットのモデル・思考の深さ設定を強化（CLIのモデル指定が実際に効くように）

AIチャットのCLI連携（Claude Code / Gemini CLI）で、設定したモデル名が実際にCLIへ渡されるようにした。あわせて、思考の深さ設定がどのプロバイダで使われるかを画面上で分かるようにした。

- 設定 > LLM > CLIチャットのモデル欄に、Claude Code（sonnet / opus / haiku）、Gemini CLI（gemini-2.5-pro等）の代表的なモデル名を候補表示（datalist）した。自由入力も引き続き可能。欄を空欄のままにすると、従来どおりCLI側の既定モデルが使われる
- Claude Code / Gemini CLIのモデル欄に値を入れると、実行時に `--model` としてCLIへ渡されるようにした（Codex CLIは従来から対応済み）。渡す値は許可文字パターンで検証し、CLIへ別フラグとして誤認識される値は注入しないようにした
- Codex CLIの既定モデルを `gpt-5.6` に更新した
- チャット入力欄下の「LLM設定」ポップアップに、「思考の深さ」がプロバイダによって使われるかどうかの表示を追加した。対応: Claude（anthropic）／ ChatGPT（openai）／ Gemini。非対応: ローカルLLM、CLI系（Codex CLI／Claude Code／Gemini CLI）ではドロップダウンを無効化し「このプロバイダでは思考の深さ設定は使われません」と案内する。既存の保存値・動作は変更していない
- 同ポップアップに「応答の賢さは思考の深さとモデルで、速度は軽いモデルの選択で調整できます」の案内を追加した
- Claudeのモデル一覧（フォールバック表示）を現行世代へ更新した
- 検証: CLIモデル注入の判定・置換・不正値の拒否を固定する単体テストを新設（test_meldex_cli_chat_model_args.py、29件）して合格。既存のCLIチャット回帰テスト（48件）、モデルカタログテスト（6件）、関連フロントエンド回帰テストを更新のうえ合格。実ブラウザE2Eでプロバイダ切替時の思考の深さ無効化表示を確認

## v0.7.019 (2026-07-21)

### feat: AIチャット「次にできること」を非表示にできる設定を追加

AIチャット欄に出る「次にできること」の提案パネルを、使わない場合はオフにできるようにした。

- チャット入力欄下の「LLM設定」ポップアップに「『次にできること』の提案を表示」のチェックボックスを追加した（既定はオン。従来どおりの表示のまま変わらない）
- オフにすると、既に表示されていた提案パネルもその場で消える。オンに戻すと、次の更新（最大2.5秒以内、または設定を閉じた直後）で再び表示される
- 個別の提案を「今は不要」で消す既存の記憶とは別の設定のため、互いに影響しない
- 検証: トグルのDOMと保存キー、パネル側の早期終了実装を固定する回帰テストを追加し合格。実ブラウザE2E（チャット推薦パネルのシナリオ）にオフ→非表示→オン→再表示のケースを追加して合格

## v0.7.018 (2026-07-21)

### change: チャットのプロバイダ表示「GPT」を「ChatGPT」に改名

AIチャットのプロバイダ選択、ナレッジ自動抽出設定の「使うAI」、設定のAPIキー欄で「GPT」と表示していたものを、一般に通じる製品名「ChatGPT」へ統一した。

- 表示ラベルのみの変更。内部の識別子・保存済みの設定値（openai）は変わらないため、既存の設定はそのまま引き継がれる
- 検証: 表示ラベルと内部識別子を固定する回帰テストを新設（test_meldex_chat_provider_labels.py）し合格

## v0.7.017 (2026-07-21)

### fix: 右サイドバー等の入力中に Ctrl+A がメインパネルを全選択する問題を修正

チャットの入力欄をドッキングで右サイドバー（#right-panel）の外に移動している場合、メインでシナリオを開いていると、チャット欄への入力中に Ctrl+A を押すとメインのシナリオ側が全行選択されてしまい、チャット欄自体は選択されなかった。

- ショートカットのスコープ判定（`_resolveShortcutScope`）で、シナリオ用スコープ（scenario）を有効にする条件に「編集中の要素がシナリオエディタ本体（`.gb-se-root`）の内側にあること」のチェックを追加。ノート用スコープには既にあった包含チェックと同型に揃えた
- シナリオのセル内での Ctrl+A（セル内容の全選択）、シナリオのタイトル入力欄での Ctrl+A（ブラウザ標準の選択）は従来どおり動作する
- 検証: 実ブラウザE2Eでチャットをドッキング配置しシナリオを開いた状態を再現し、修正前は再現・修正後は解消することを確認（`targeted-shell-micro-ctrl-a-scope-docked-chat`）。関連する既存の回帰テストも実行し合格

## v0.7.016 (2026-07-21)

### feat: 共有ワークスペース — フォルダ1つ渡すだけのチーム共有（設定に新ボタン）

設定の「保存先・フォルダ」タブ「Dropbox」欄に、フォルダ単位のチーム共有を追加した。従来の共有と違い、**渡した共有フォルダの中だけ**が相手と共有され、自分の他のフォルダは相手から見えない。

- **このフォルダを共有ワークスペースにする**: 選んだフォルダ全体を1つの共有ソースフォルダとして登録し、自分も参加する
- **共有ワークスペースに参加**: 招待された共有フォルダを選ぶと、その中のソースフォルダが自分のフォルダツリーに現れる
- **参加中の一覧と離脱**: 離脱してもDropbox上のフォルダの中身は消えない
- デスクトップ版はDropboxアプリの同期フォルダ経由、ブラウザ版はDropbox接続で動作する（ブラウザ版への反映は次回のクラウド公開時）

公開前の徹底チェック（複数視点の敵対的検証・実コード実行での再現）で、次を修正してからのリリース:

- 共有状態の読み取りが一時的に失敗しただけで「未登録」と誤判定し、**他のメンバーの登録済み一覧を上書きして消してしまう**恐れ → 「無い」と確認できた時だけ新規作成し、読み取れない場合は上書きせず中止するよう安全化
- 記号・空白だけが違う別フォルダ（例: Team A と Team-A）を両方共有すると内部の識別が衝突し、「片方を離脱すると両方消える」「フォルダツリーの開き先が入れ替わる」恐れ → 識別を一意化して解消
- 全ソースが削除済みのフォルダを共有ワークスペースにすると、成功表示なのに何も共有されない空振り → 登録し直すよう修正
- Dropbox全体（同期フォルダそのもの）を誤って共有ワークスペースにできてしまう問題 → 明示的に拒否
- 同時操作で参加記録が失われうる書き込み競合、およびリンク（ジャンクション）経由で共有情報が同期フォルダの外へ書かれる穴 → 直列化・封じ込めを追加

## v0.7.015 (2026-07-21)

### change: Dropboxをフォルダ単位で共有する仕組みの内部準備（画面にはまだ出ません）

「共有フォルダを1つ渡せば、その中のワークスペードとソースフォルダだけをメンバーが共有できる」ようにするための土台を追加した。今回の変更だけでは**画面上の見た目・操作は一切変わらない**（参加・共有開始のボタンはまだ無い）。次のアップデートで、この土台の上に実際に使える画面を追加する予定。

- 共有フォルダの中に、その場所からの相対位置で共有情報を安全に読み書きする仕組みを追加
- 参加中の共有フォルダのソースフォルダが、フォルダツリーの読み込みに正しく合流するよう内部配線（デスクトップ版・ブラウザ版の両方）。**参加している共有フォルダが無い場合、動作はこれまでと完全に同じ**
- 実装過程の点検で、複数の共有フォルダに同名フォルダがあると誤って別の共有フォルダの中身を開いてしまう恐れ、および共有フォルダの実際の場所が既存の「他の端末とも共有する」操作を通じて個人の設定へ紛れ込む恐れを発見し、あわせて解消した（現時点ではどちらも到達する画面が無いため実害はない）

## v0.7.014 (2026-07-21)

### change: バージョン管理パネルの保存ボタンを1つに集約し、名前を分かりやすく変更

バージョン管理パネルに「スナップショットを作成」ボタンと、ファイルバージョン欄の中の「＋保存」ボタンがあり、ファイルを開いているときはどちらも同じ「その時点を保存する」動作だったため紛らわしかった。

- パネル上部のボタンを **「スナップショットを作成」→「現バージョンを保存」** に名前変更した（動作は同じ）
- ファイルバージョン欄の中にあった重複する「＋保存」ボタンを**削除**した。保存は上部の「現バージョンを保存」に集約された
- フォルダを選んでいるときに出る「＋フォルダ版を保存」はこれまでどおり残している

## v0.7.013 (2026-07-21)

### fix: フォルダの移動・保存が「保存に失敗しました」で止まる問題を根本解消

検索用の索引づくりが裏で走っている間、フォルダツリーの移動や保存が約15秒待たされて「保存に失敗しています」と表示されることがあった。原因は、検索索引の作り直し・整理・最適化と、日常の保存・移動が、同じ内部の保存先（データベース）を奪い合っていたこと。

- 検索索引の保存先を、作品データ本体とは**別のファイルに分離**した。これにより、裏で索引の整理・最適化が長く走っていても、フォルダの移動・作成・名前変更・削除・保存がそれに邪魔されなくなった
- 検索の使い勝手・結果は変わらない
- 分離にともない、**アップデート後の初回起動時に検索索引の作り直しが1回だけ自動で走る**（バックグラウンドで実行され、操作の妨げにはならない）
- 旧バージョンへ戻しても作品データには影響しない（検索索引は必要に応じて自動で作り直される）

## v0.7.012 (2026-07-21)

### change: 設定の「保存先・フォルダ」を分かりやすく整理し、接続系を新しいタブへ

設定画面の「保存先・フォルダ」に、フォルダの設定と、保存の仕組み・スマホ接続がまとめて並んでいて分かりにくかった。整理して、日常使う項目だけを残した。

- 「保存先・フォルダ」タブは **ホームフォルダ / ソースフォルダ / Dropbox** の3つだけに整理した
- 「スマホ・タブレットからの接続」と「保存の仕組み・共有サーバー（このPC / Dropbox / Meldex共有サーバーの切り替え）」は、新しい **「接続・共有サーバー」** タブへまとめて移した
- 「Dropboxで共有」の見出しは「Dropbox」に統一した
- 機能自体は変わらず、置き場所（タブ）だけが変わっている

## v0.7.011 (2026-07-21)

### fix: 「自分を同期」でユーザーが「anonymous（名無し）」と二重に登録される問題を修正

ワークスペースの作成時にまだ表示名を設定していないと、作成者が「anonymous」という名無しの既定名で登録されていた。その後に表示名を設定して「自分を同期」を押すと、同じ本人が「anonymous」と実名の2行に分かれてメンバー一覧に並んでしまっていた。

本人が実名で同期したときに、残っていた「anonymous」の作成者エントリを実名へ引き継ぐようにした（作成者などの権限も保持）。すでに実名の行がある場合は1行に統合し、強い方の権限を残す。これにより名無しの重複行が生じなくなった。

- 既存の重複を解消したい場合は、実名を設定した状態で「自分を同期」を押すと anonymous 行が実名へ統合される
- 表示名が未設定のまま（anonymous）で同期した場合は従来どおりの動作
- 共有ワークスペースで、管理者でないメンバーが同期した場合は、名前の統合だけ行い管理者権限は引き継がない（他人の作成者権限を勝手に得られないようにするため）

## v0.7.010 (2026-07-21)

### fix: 取り込み・同期が「タイムアウトしました」と誤って失敗表示になる問題を修正

Xアカウント保存・Xブックマーク保存・Notion同期・拡張機能のインストール・Evernote(ENEX)取り込み・PureRef取り込みで、件数や画像が多いと処理が待ち時間の上限（最長5分。機能によっては15秒）を超え、実際には保存できているのに画面だけが「タイムアウトしました」と赤いエラーを出すことがあった。

これらを裏で続けて実行する方式に変更し、処理中は「取得中… ○件保存済み」のように進捗が表示されるようにした。画面が待ちきれてエラーになることがなくなり、途中経過も分かるようになった。

- 対象: Xアカウント保存 / Xブックマーク保存（手動・自動）/ Notion同期（手動・自動）/ 拡張機能インストール / Evernote(ENEX)取り込み / PureRef取り込み（取り込みダイアログ・フォルダからの変換の両方）
- すでに裏で実行していた「外部取り込みのセット実行」は従来どおり

## v0.7.009 (2026-07-21)

### fix: 検索の索引づくりの最中に保存が「保存に失敗しました」と出る問題を修正

保管庫が大きい環境で、検索の索引（本文検索・意味検索）を裏で作り直している最中に、シナリオやノートの保存が「保存に失敗しました（15秒でタイムアウト）」と表示されることがあった。実際にはファイル自体は書き込めていることが多いが、保存の後処理（編集履歴・関連リンクの記録）が索引づくりの裏側処理とデータベースの書き込み順番を取り合い、最大15秒級で待たされて重なると、画面上の待ち時間の上限（15秒）を超えて失敗表示になっていた（読み取りは並行して動けるため、開くのは速いのに保存だけ遅い、という症状になる）。

- 保存・取込の各操作が、裏で走る索引の作り直しに「道を譲る」よう明示した。これにより索引づくりは保存が来た時点でいったんデータベースの書き込みを手放し、保存が先に完了できるようになる（フォルダの新規作成・名前変更・移動では既に同じ仕組みが入っていたが、ファイル保存経路が抜けていた）。
- 対象: ファイル保存（シナリオ・ノート等）、CSV取込、Excel取込、Excelからのシナリオ取込。

## v0.7.008 (2026-07-21)

### fix: シート・レイアウト・シナリオの実機報告不具合をまとめて修正 + 用語整理

実際の使用で見つかった不具合と用語の不統一をまとめて解消した。

**シート（データ消失に見える重大3件）**
- ＋ボタンでのエントリ追加が「作成に失敗」の赤字を出して行が消える問題を修正。作成通信の打ち切り時間を延長し、失敗時はサーバー側で実際に作成済みでないか確認してから撤去するようにした。サーバーが別名を付けた際の並び順のズレ、連続追加時の名前衝突も解消。
- 列名変更が Enter で確定せず数秒かかる問題を修正。Enter で即確定し、列名変更はサーバー側で全エントリを1トランザクションでまとめて処理する経路（`PUT /api/db-property/rename`）を新設（従来は1マスずつ通信し都度シート全体のスナップショットを保存していた）。旧マークダウン形式シートは従来の逐次経路へフォールバック。
- エントリ名列のヘッダーポップアップからのリネームがシート名を変えてしまう問題を修正。列名の変更に置き換え、表示名をビュー設定（`entityColumnLabel`）に保存できるようにした。制作管理シートは固定名のまま。

**レイアウト**
- 「右サイドバーを閉じる」が付随的な `revealPane` で開き直される問題を修正。固定レールにユーザー明示クローズ（`_userCollapsed`）を持たせ、ユーザー操作起点（`userIntent`）の reveal のみ再展開する。
- フォルダツリーを閉じると右サイドバーの幅が変わる問題を修正。内側の水平 split を panelset 越しに取得し、毎回現在比率から右サイドバーのピクセル幅を維持する方式に作り直した（`_savedInnerRatio` 廃止で折りたたみ中のドラッグ調整も保持）。

**シナリオ**
- 前の文字列（`textBefore`）を持つタイプの行で、横書きルビが本文字より約1文字分右にズレる問題を修正。`.sn2-text span[data-ruby]` に `text-indent:0` を追加し、継承インデントが本文字だけを左へずらす副作用を打ち消した。

**参照先シート**
- 「参照先シートフォルダのパス」→「参照先シート」、「シート一覧から選択」→「一覧から選択」に改名。候補は作品フォルダ内のシートに絞り込み、シート一覧のキャッシュを開くたびに最新化。「一覧から選択」を全ワークスペース/ソース/ホーム横断のフォルダツリー（現在地展開・シートのみ選択可）に刷新。

**用語・アイコン**
- 列タイプの「リレーション（単一）」→「リレーション」。ロールアップのアイコンを Lucide `workflow` に変更（数式 Σ との重複を解消）。「プロパティ型」→「列タイプ」（UI・サーバーメッセージ・E2E・マニュアル）。「シートフォルダ」→「シート」（画面表示・マニュアル。AI 向けツール説明は操作の取り違え防止のため据え置き）。用語統一表に追記。

**クイックメモ（配布ページ登録）**
- 配布ページの単独アプリ一覧にクイックメモを追加し、「ホームに追加」を既存 PWA（`quick-memo.html`）で利用できるようにした。Windows 単体版のビルドとクラウド版の Dropbox 保存（クイックメモは他アプリとストレージ方式が異なるため新規実装が必要）は後続作業。

## v0.7.007 (2026-07-21)

### fix: Dropbox共有連携の追加不具合を修正（徹底チェックで発見）

v0.7.006 の後、ユーザー操作を一つずつ最深部まで追跡する徹底チェックを行い、見つかった不具合をまとめて修正した。

- **プロフィールの切り替え**: 設定の「ユーザー」で別のプロフィールに切り替えたとき、切り替えが反映されないうえ、選んだ相手の名前とアイコンをこの端末の内容で上書きしてしまう問題を修正した。他の端末やブラウザ版で使っているプロフィールが書き換わることはなくなる
- **フォルダの削除（ツリー）**: フォルダツリーで右クリックしてソースフォルダを削除しても、他の端末と共有しているフォルダが次に開いたときに元に戻ってしまう問題を修正した。右クリックからの「名前を変更」「パスを変更」も共有設定に正しく反映されるようにした
- **フォルダの削除（確認）**: 他の端末で追加されたフォルダ（「場所を確認」と表示されるもの）を一覧から外すとき、他の端末やブラウザ版からも見えなくなることをお知らせする確認画面を追加した。共有状態がまだ読み込めていないときも念のため確認する
- **設定画面の編集内容**: フォルダの名前を書き換えたり行を削除した直後に、別のフォルダの「この端末のみ」や「場所を確認」を操作すると、編集した内容が知らせなく元に戻り、そのあと「保存」を押しても保存されない問題を修正した
- **共有バッジの表示**: 「フォルダを指定…」でDropboxのフォルダを設定した直後や「場所を確認」を終えた直後に、フォルダ一覧のバッジが古いまま更新されない問題を修正した。設定を開き直さなくてもそのまま共有の操作に進める
- **共有設定の保存**: Dropboxや他のソフトが共有設定のファイルを使用中のとき、保存が失敗したまま一時ファイルが残り次回以降の保存まで妨げられることがある問題を修正した

### 既知の制限

- 他の端末で共有から外したフォルダが、すでにこの端末の一覧に取り込まれている場合、その端末の一覧からは自動では消えない。不要な場合は設定の一覧から手動で外す
- 一度共有から外したフォルダを同じ内容のまま再び共有することはできない。フォルダの表示名を変えてから保存し直す

## v0.7.006 (2026-07-20)

### fix: Dropbox共有連携の重大な不具合を修正（公開前の総点検で発見）

v0.7.005 で追加したDropbox共有連携について、多角的な監査と実機検証で見つかった問題をまとめて修正した。

- 「全設定を初期化」を実行すると、他のPCやブラウザ版Meldexに登録してある共有ソースフォルダの一覧まで一斉に消えてしまう問題を修正
- ブラウザ版Meldexで追加した共有ソースフォルダをデスクトップ版で削除しても、次回読み込みで復活してしまう問題を修正
- 「この端末のみ」と表示されたソースフォルダを共有に切り替える操作がどこにも無かったため、バッジをクリックして共有できるようにした。Dropboxの中に無いフォルダには、移動手順を具体的に案内する
- Dropboxアプリが自動で見つからないときに手動でフォルダを指定しても「見つかりません」の表示が残り続ける問題を修正。指定中のフォルダと「指定し直す…」を常に表示するようにした
- ソースフォルダ一覧の「場所を確認」バッジが、表示されるのにクリックしても反応しなかった問題を修正
- 共有ソースフォルダを一覧から外すときに確認を表示するようにした（他の端末からも見えなくなるため）
- 同じ画面で「端末間で共有されます」と「この端末のみ」が同時に表示される矛盾を解消
- 名前とアイコンの引き継ぎが一度起きると、以後どの端末でも自動で揃わなくなる問題を修正。設定の「ユーザー」から別のプロフィールに切り替え直せるようにした
- 共有設定ファイルを他の人も書き換えられる状況で、こちらのPCの別の場所を読み書きできてしまう抜け道を塞いだ。共有設定の書き込みには管理者権限を必要とし、同時書き込みでファイルが壊れないようにした
- Dropboxアプリの一時的な読み取り失敗時に、他の端末の登録を消してしまう可能性を解消

## v0.7.005 (2026-07-20)

### feat: デスクトップ版とクラウド版のDropbox共有連携・名前とアイコンの端末間共通化

デスクトップ版がPC上のDropboxアプリの同期フォルダを自動で見つけ、共有ソースフォルダの設定をDropboxに接続しなくても直接読み書きできるようにした。あわせて、設定画面の共有状態表示、クラウド版の初回セットアップ、ユーザー名・アイコンの端末間共通化、単独アプリのユーザー表示を整備した。

- 設定 → 全般タブに「Dropboxで共有」状態カードを追加。Dropboxアプリの検出状況と共有ソースフォルダの件数を表示し、フォルダ一覧の各行には共有状態のバッジ（共有中 / この端末のみ / 場所を確認）が付く。今の保存先がDropbox内にある場合は「他の端末とも共有する」から共有に加えられる
- クラウド版の初回セットアップでDropboxに接続した際、デスクトップ版で共有登録済みのフォルダが見つかった場合は「このフォルダを引き継いで開始」から、保存先を入力し直さずそのまま始められるようにした
- ユーザー名とユーザーアイコンの端末間共通化が、Dropboxに接続していないデスクトップ版でも働くようにした。どのプロフィールを使うか自動で決められない場合は、設定 → ユーザー・共同作業 → プロフィールの案内から候補を選ぶか、今の設定のまま新しく登録できる
- 単独アプリ（ノート・シナリオ・シート・ボード・タイマーのクラウド版）にユーザー名・アイコンのバッジを追加し、その場でユーザー名とアイコンを編集できるようにした
- Dropboxが作る「競合コピー」ファイルの検知範囲に、共有ソースフォルダの設定ファイルも含めた（検知して知らせるところまでで、自動での統合はしない）

## v0.7.004 (2026-07-20)

### refactor: クラウド静的版のYAML-liteフロントマター読み書きを共有ヘルパーへ統合

`docs/cloud-frontmatter-lite-consolidation-plan-2026-07-20.md`。制作管理
（`gb-production-management.part01/02.js`）とスタッフ管理シートのクラウド静的版twin
（`gb-staff-registry-cloud-twin.js`）に完全に同一実装で重複していたYAML-liteフロント
マター読み書き・書き込み権限エラー判定を、新規共有ファイル `gb-cloud-frontmatter-lite.js`
（`window.MeldexCloudFrontmatterLite`）へ切り出し、両者を委譲に置き換えた。

- Python `meldex_frontmatter.write_frontmatter` と相互互換な「1行1キー、値は
  JSON.stringify」形式の読み書きを1箇所に統合。`gb-data-access-dropbox-expanded.js` /
  `gb-data-access-dropbox-chat.js` のブロックスカラー対応版 `_yamlLiteObject` 系は
  別方式のため対象外。
- `Meldex.html` / `Meldex-dev.html` に `gb-cloud-frontmatter-lite.js` の読み込みを
  `gb-production-management.js` の直前へ追加。
- ユーザーから見える動作の変更はなし（内部実装の重複解消のみ）。

## v0.7.003 (2026-07-20)

### feat: 単独アプリ5種のスマホ幅ツールバーを優先ボタン+あふれメニュー化

`docs/standalone-mobile-toolbar_plan_2026-07-20.md`。単独版ノート/シナリオ/シート/
ボードのスマホ幅（≤820px）ツールバーを、従来の「横スクロールで全ボタン到達」から
「よく使う操作だけ常時表示＋残りを『その他』ボトムシートへ畳む」構成へ置き換えた。

- **新規 `standalone-mobile-toolbar.js` / `standalone-mobile-toolbar.css`**:
  4アプリ共通の優先ボタン+あふれメニュー化モジュール（`window.MeldexStandaloneMobileToolbar`
  のみ公開）。方式は「元ボタンを隠して代理クリック」（物理移動しない）。ノートの
  ツールバーはイベント委譲、シナリオ・ボードのツールバーは本体共用の生成コード
  （`gb-tool-scriptnote.js` / `gb-board-presets.js`）に依存するため、どちらも無改変の
  まま外側から `setup()` で後付けする。あふれメニュー（ボトムシート）は画面下部固定。
  `<select>` は「ラベル+現在値」行→タップで選択肢展開→選択で値設定+change発火。
  開いた時点で active/pressed/disabled/バッジをミラーする。
- **タイマー**は対象ツールバーが無いため未対応（画面崩れの発生なしを確認済み）。
- 優先ボタン: ノート=目次・取り消し・やり直し・太字・検索置換 / シナリオ=取り消し・
  やり直し・横書き・縦書き・フィルタ・検索 / シート=ビュー切替select・取り消し・
  やり直し・フィルタ・並び替え / ボード=メニュー・タイトル・取り消し・やり直し・
  カード追加・ライン追加。ボードの「オプションを開く」は計画案では優先候補だったが、
  `gb-layout.part01.css` の `.gb-toolbar-option-panel-btn` 非表示規則により単独版では
  幅を問わず常時非表示（既存の仕様）と判明したため対象から除外した。
- 4 HTML（note/scenario/sheet/board）へ新規2ファイルを読み込み配線。
  `build_cloud_static.py` / `cloud_standalone_apps.py` の資産一覧・per-app Service
  Workerキャッシュ一覧はHTMLの `<script>`/`<link>` を自動走査するため追加登録は不要。
- E2E: `meldex_standalone_surface_micro.py` へ4新規ケース（note/scenario/sheet
  mobile-toolbar個別ケース + board分は既存 `board-standalone-controls` ケースへ追加）。
  優先ボタンのみ表示+その他ボタン・横あふれ無し・あふれメニューの内容・代理クリック
  の実挙動（斜体適用・見出しselect適用・折返しトグル・消しゴムツール切替）・
  Escape閉じ+フォーカス復帰・デスクトップでは「その他」非表示、を検証。

## v0.7.002 (2026-07-20)

### feat: スタッフ管理シートのクラウド静的版twin実装（/staff-registry/* 配線とデスクトップ案内の置き換え）

`docs/user-account-unification-plan-2026-07-18.md`。v0.7.000で明記した
「既知の未解消ギャップ（クラウド静的版）」を解消し、正本「スタッフ管理
シート」のサーバーAPI（`meldex_api_staff_registry.py` /
`meldex_staff_registry_service.py`）と同じ意味論をDropboxブラウザ完結版
（サーバー無し）でも実動作させた。

- **新規 `gb-staff-registry-cloud-twin.js`**: config GET/PUT・ensure・
  list・upsert の5ルートを `__MeldexPwaDataAccessExtensions` ハンドラとして
  実装（`gb-production-management.part01.js` の `_pmInstallCloudHandler` と
  同じ配線パターン）。正本シートは通常のフォルダ型シート（フォルダノート +
  1スタッフ=1個別`.md`）のままとし、`gb-data-access-dropbox-expanded.js` の
  「storage: sqlite 集約ストア」方式のsettings-dbは使わない（デスクトップ版と
  同じVaultを共有する必要があるため）。フォルダノート・エントリの
  YAML-liteフロントマターはPythonの `meldex_frontmatter` が読める形式
  （1行1キー、値はJSON.stringify）で書く。
- **設計上の差異（意図的）**: デスクトップ版は複数の「ソースフォルダ」を
  横断して正本を自動発見・既定作成できるが、クラウド静的版は1つのDropbox
  接続 = 1つのワークスペースルートしか持たないため、自動発見・既定作成は
  ワークスペース直下だけを対象にする（`gb-production-management.js` が
  制作管理フォルダを直下に作るのと同じ前提）。設定の保存先は
  `editor-config.json` ではなく、既存の `/ui-config` twinと同じ
  localStorageキー（`meldex-cloud-ui-config`）の `staff_registry` サブキー
  を使う。
- **`_pmCloudAddStaff`（制作管理「メンバーを追加」）から旧フォールバックを
  撤去**: 「メンバーの追加はデスクトップ版で実行してください」という案内へ
  すり替えていたtry/catchを削除し、`window.MeldexUserRegistry.upsertStaff`
  の成功/失敗（ユーザー重複時の409等）をそのまま呼び出し元へ伝播させる
  ようにした。
- 設定画面「ユーザー」タブのスタッフ一覧・起動時のfill-only自動登録
  （`_primeStaffRegistrySelfUpsert`）・ユーザーピッカーの正本反映・
  再計算前の重複チェックは、いずれもtwinの実装だけで動作するようになった
  （呼び出し側のコード変更は不要）。
- 新規回帰テスト `test_meldex_staff_registry_cloud_twin.py`（2件）:
  twin単体でのconfig/ensure/list/upsert・fill-only・一意制約409・
  自己修復・閲覧専用縮退・保存先変更の一連の挙動と、twinが書いた
  `.md`テキストをPython側 `meldex_frontmatter.parse_frontmatter` で
  パースするクロス言語互換ケース。加えて、制作管理の「メンバーを追加」が
  実際にこのtwinへ到達し、409等の実エラーがそのまま伝播することを確認する
  統合ケース。

## v0.7.001 (2026-07-20)

### chore: アカウント一元管理の旧コード掃討とマニュアル整備（Phase 5・計画完了）

`docs/user-account-unification-plan-2026-07-18.md` Phase 5（クリーンアップ）。
Phase 1〜4（v0.6.208〜v0.7.000）で実装した正本「スタッフ管理シート」への
移行を受け、残置参照の掃討とドキュメント整備を行った。破壊的変更は含まない。

- **候補ユーザー一覧の重複実装をさらに2箇所解消**: §5.8で棚卸しされた6箇所とは
  別に、シート一括編集の user/multi-user 型エディタ（`gb-db-bulk-edit.js`）と
  チームチャットのDM相手選択モーダル（`gb-right-panel-chat.part01.part02.js`）
  が `/team`・`/auth/users` を個別にマージしていたのを発見し、
  `MeldexUserPicker.getCandidates()` / `MeldexUserRegistry.listStaff()` へ
  統一した（DMのワークスペース内スコープ限定は既存挙動を維持）。
- Phase 4完了前の先送り文言（「制作管理フル統合はPhase 4」等）が完了後も
  残っていた箇所を4件訂正（`gb-production-management-recalc.js` /
  `gb-user-picker.js` / `meldex_collab_workspace_service.py` /
  `Meldex.part02.part01.py`）。
- マニュアル・仕様文書を更新: `user-property-type-spec.md`（ピッカー仕様を
  正本ベースに全面書き直し）、
  `production-management-user-manual-after-option-panel-2026-06-01.md`
  （スタッフ管理・スキル設定の導線変更を反映）、用語統一表（「スタッフ管理
  シート」登録）、旧版ロールバック手順（v0.7.000境界の注意を追記）。
- `multi-user-cloud-deployment-plan.md` は編集統治（§0.4）に従い、直接編集
  せず随伴レビューファイル
  `multi-user-cloud-deployment-plan-review-2026-07-20-claude.md` を作成
  （ユーザー承認待ち）。
- 新規回帰テスト `test_meldex_staff_registry_phase5.py`。

## v0.7.000 (2026-07-20)

### feat!: 制作管理のスタッフ管理を正本シートへ完全統合（ユーザーアカウント一元管理 Phase 4・メジャー/マイナー境界リリース）

`docs/user-account-unification-plan-2026-07-18.md` Phase 4。Phase 1〜3
（v0.6.208〜v0.6.210）で並走導入した正本「スタッフ管理シート」へ、制作管理の
スタッフ管理を完全統合した。**破壊的変更を含むため、このバージョンから
メジャー/マイナー番号を v0.7.000 へ進める。**

#### ファイル形式（スキーマ）が変わった項目【重要】

- **制作管理のシート契約が 13→12 に変更**: 制作管理ルートごとの
  「スタッフリスト」シートを廃止した。スタッフ情報は全体で1枚の正本
  「スタッフ管理シート」（置き場所は設定で変更可）に一元化される。
- **スケジュール/スケジュール アーカイブ の「スタッフリスト」列（relation）を
  廃止し、「担当者」（user）へ一本化**。既存の値は自動移行される（値が
  入っていれば「担当者」へコピーし、「担当者」に既存値があれば上書きしない）。
- **スタッフの「担当できる作業」（スキル）列を廃止**。担当可否は作業内容
  リスト側の既存「担当者候補」列（複数選択のユーザー）で設定する形に変わった
  （「担当者候補が空 = 全スタッフが担当可」）。

#### 旧版（v0.6.x以前）で開けなくなる/挙動が変わる条件

- **旧版のMeldexで、この変更以降に作られたワークスペースを開くと、制作管理の
  「スタッフ」タブ・自動割り当てが空/未設定に見える。** 旧版は正本シートを
  認識せず、廃止済みの「スタッフリスト」を再生成しようとするため、共有
  フォルダを新旧混在で使うと編集場所が分裂する（新版は正本へ、旧版は
  再生成した旧シートへ書く）。**チーム全員が同時にこのバージョン以降へ更新
  することを強く推奨する。**
- 新版が生成した正本シート・移行済みマーカー付きの旧「スタッフリスト」
  フォルダ自体は、旧版から見ても壊れたファイルではない（旧版は単に無視する
  か、通常のシートとして扱う）。**旧版で新版のデータを開いてもデータの破損・
  消失は起きない**（読み込み専用の後方互換を確認済み。下記「旧版互換の検証」
  参照）。
- 旧版へロールバックした場合、旧版はスタッフ情報を制作管理ルートごとの
  「スタッフリスト」（移行前の状態のまま温存されている）から読むため、
  従前どおり動作する。ただし正本シートで行った編集（Phase 4以降の変更）は
  旧版には反映されない。

#### 一回限りの移行（新規 `meldex_staff_registry_migration.py`）

制作管理を開く（`/init`）と、既存の制作管理ルートに残るレガシー
「スタッフリスト」を検出して自動的に正本へ移行する。

- **コピーであり移動ではない**: 旧フォルダは削除・リネームせず温存する
  （フォルダノートに `migrated_to_staff_registry` マーカーと告知文だけを追記
  し、以後は新版から読まれなくなる）。
- ワークスペースメンバー・ソースフォルダのチーム登録簿（`_Meldex_team.json`）
  からも、正本に無い行を追加する（表示名・権限、チーム登録簿は名前のみ・
  アバターは移行しない）。
- 複数ソースからフィールドが競合した場合は「最終更新が新しい方を採用」し、
  破棄した値は正本シートの備考へ `【移行】休日: "..."（○○を破棄）` の形式で
  記録する。あわせて正本シートの隣に `移行レポート_<日付>.md` を生成する。
- 冪等（何度実行しても安全）。既に移行済みの場合・レガシーフォルダが無い
  場合・正本の場所が未解決の場合は何もしない。

#### その他の変更

- 自動割り当て（`meldex_production_recalculate.py`）・外部カレンダー送信
  （`meldex_production_external_sync.py`）・制作管理の初期化・識別移行
  （`meldex_production_identity.py`）・JSミラー
  （`gb-production-management.part01/02.js`、
  `gb-production-management-schema-migration.js`）を正本参照へ切替。
- 制作管理タスク詳細の「担当者」ドロップダウン
  （`gb-tool-calendar-production-sidebar.js`）とスタッフタブ
  （`gb-tool-calendar-production-task-view.js`）を正本参照へ切替。
- オプションパネル版の制作管理（`gb-tool-calendar-production-panel.js`）の
  「メンバー」タブは、独自の一覧・編集UIを廃止し、正本シートを開く導線のみに
  差し替えた。
- 「メンバーを追加」ダイアログ（`gb-production-management-recalc.js`）から
  「担当できるタスク」欄を削除。
- `meldex_staff_registry_service.py`: 正本の自動解決を共通化する
  `ensure_or_discover_registry_root()` を新設（設定タブ・ピッカー・制作管理の
  各所で重複していたロジックを集約）。「ユーザーを選択（未連携も可）」の
  運用を維持するため、`upsert_staff()` はユーザーIDが空でも表示名だけで行を
  作成できるよう修正した。
- **実サーバー配線バグの修正（隔離ブラウザ実機検証で発見）**:
  `Meldex.part03.py` の制作管理ルーターへ渡す ctx に `config_file` /
  `save_config` / `load_outliner_roots` が欠けており、
  `ensure_or_discover_registry_root()` が実運用では常に解決失敗していた
  （unitテストは自前のctxにこれらを含めていたため検出できなかった）。この
  ため、ワークスペースメンバーの正本への同期・レガシースタッフリスト移行の
  両方が**修正前は実際には一度も動作していなかった**。3属性を追加して修正し、
  ソース文字列で固定する回帰テストを追加した。
- **既知の未解消ギャップ（クラウド静的版）**: `/staff-registry/*` の
  Dropboxブラウザ完結（クラウド静的）版twinルートは未実装のまま。クラウド
  静的版からの「メンバーを追加」は「デスクトップ版でご利用ください」という
  案内で失敗するようフォールバックした（既存の他機能と同じ方針）。フル対応
  は別途のフォローアップ課題とする。

#### 旧版互換の検証

- `test_meldex_staff_registry_migration.py`（新規12件）: 移行エンジン単体
  （コピー・冪等性・レガシーフォルダ温存・latest-modified-wins・空値が
  非空値に勝てないこと・スキル転置・fill-only）。
- `test_meldex_staff_registry_migration_integration.py`（新規5件）:
  `/api/production-management/init` からの自動移行トリガーと、
  スケジュール退役列の値移行。
- 旧版互換E2E: `test_meldex_legacy_compatibility.py` に、v0.6.x相当の読み込み
  ロジック（`_load_staff` 型の読み取り）で新版データを開いても例外にならず
  空扱いで安全に読み過ごせることを確認するケースを追加（実機での旧版exe起動
  までは環境上実施できず、旧版の該当読み込みコードパスを直接再現して検証。
  制約は本体の完了報告に明記）。

## v0.6.211 (2026-07-20)

### feat(debug-list): アプリ別デバッグリスト展開（第1期・B-MANGA導入）

`docs/per-app-debuglist-plan-2026-07-18.md` 第1期。Meldexのデバッグリスト
（登録→進捗管理→ダッシュボード）を、Meldexと関係の無い他の開発中アプリでも
同様に運用できるようにした。初回対象アプリはB-MANGA。第2期（AI自動修正
ワーカーの複数アプリ対応）はユーザー判断待ちのため未着手。

- **`ensure_debug_management_workspace()` のパラメータ化**
  （`meldex_settings_db_service.part01.py`）: `app_name` / `source_root`
  キーワード引数を追加。`app_name` があれば機能チェックリストのフォルダ名が
  `{app_name}機能チェックリスト`、smart-db表示名が
  `デバッグリスト（{app_name}）` になる（`id: "debug-management"` は不変）。
  両方省略した従来呼び出しは完全に後方互換。
- **smart-db sourcesのルート名相対化**: `_debug_source_prefix()` を拡張し、
  vault相対解決に失敗した場合でも `source_root` があれば
  `{source_rootのフォルダ名}/{debug_rootの相対パス}`（例:
  `B-MANGA開発/デバッグ`）で解決するようにした。絶対パスを埋め込まないため、
  共有メンバーの各マシンでソースフォルダ登録さえされていれば同じsmart-db
  ファイルが解決される。
- **ensure APIの拡張**（`meldex_api_settings_db.py`）: `POST
  /api/settings-db/debug/ensure` が `source_folder` / `app_name` を受理する
  ようになった。`source_folder` は登録済みルートかを検証（未登録は403）。
  既定vaultルートへ `app_name` 付きで実行すると400で拒否し、既存Meldex
  ワークスペースの誤改名を防ぐガードを追加した。
- **トレイのバグ報告に報告先選択を追加**（`tray.py`、任意対応）: バグ報告
  ウィンドウに「報告先」ドロップダウンを追加。editor-config.jsonの
  `bug_report_targets` またはソースフォルダ一覧から候補を自動生成し、選択に
  応じてquick-reportへ `source_folder` を付与する。事前probeを軽量な
  `/exists` エンドポイントへ変更。Meldex以外の報告先はAPI失敗時にローカル
  ファイルへフォールバックせずエラー表示のみとする。
- **B-MANGA導入の運用手順**を `automatic-debug-solution-operations-manual.md`
  に追加し、`デバッグリスト_設計意図.md` の陳腐化していた「`/exists`
  未実装」記述を実装済みに訂正した。
- 検証: `test_meldex_debug_automation.py` に app_name/source_root 生成・
  後方互換・再ensureマージの回帰3件を追加。新設
  `test_meldex_per_app_debuglist.py` でensure APIのsource_folder登録/未登録
  /既定vaultガードの回帰6件を追加。`test_meldex_batch31_services_regressions.py`
  にトレイの報告先候補生成・quick-report送信の回帰5件を追加。既存の
  デバッグリスト関連テストは全緑（無変更呼び出しの後方互換を個別確認）。
  実フォルダ操作を伴うB-MANGA運用手順そのものは隔離検証環境で完走確認済み
  （詳細は `per-app-debuglist-plan-2026-07-18.md` 実行ログ参照）。

## v0.6.210 (2026-07-19)

### feat(account): ピッカー統一（ユーザーアカウント一元管理 Phase 3）

`docs/user-account-unification-plan-2026-07-18.md` Phase 3。候補ユーザー一覧の
実装が6箇所に重複していた問題へ、新設 `gb-user-picker.js`（`MeldexUserPicker`）
で候補取得を一本化した。候補 = 正本「スタッフ管理シート」+ ワークスペース
メンバーのマージ（制作管理フル統合前のフォールバック）。`/auth/users` への
参照はここで無くなった。

- **新設**: `gb-user-picker.js` — `getCandidates()`（重複排除・離脱者除外の
  候補解決）+ `open()`（既存 `_showUserDropdown` を母体に一般化した共通ポップ
  アップ。検索・複数選択・自由入力・UI共通ルール準拠の閉じるボタンに対応）。
- **候補取得を統一した5箇所**: 汎用user/multi-userセル（`gb-db-value-editors.
  part03.js`）、旧カレンダー（`gb-calendar.part02.js`）、スケジュールツール
  （`gb-tool-calendar-options.js`）、制作管理「メンバーを追加」
  （`gb-production-management-recalc.js`）、制作管理オプションパネル
  （`gb-tool-calendar-production-panel.js`）。
- **ToDo担当者のコンボ化**: 自由テキスト入力のみだった欄に候補選択ボタンを
  追加（`MeldexUserPicker.open({ allowFreeText: true })`）。既存の自由入力値・
  保存経路は無変更。
- **意図的に対象外**: 制作管理タスク担当者サイドバー
  （`gb-tool-calendar-production-sidebar.js` の managed-select）。ここは
  正本ではなく制作管理が実際に書き込む先（スタッフリスト）を表示する必要が
  あり、Phase 1〜3の「制作管理には触れない」方針により現状維持。Phase 4
  （§5.9手順7-8）で正本参照へ切り替える。
- E2Eヘルパー `chooseUser()`（`gb-e2e-actions-sheet-micro.js`）を
  `/team/sync`・`/team`・`/auth/users` 直叩きから registry ベースの
  セットアップへ書き換えた。
- 検証: 新規回帰テスト `test_meldex_user_picker_phase3.py`
  （candidatesの重複排除・離脱者除外のnode実行検証を含む）、
  `test_meldex_production_management_frontend.py` の候補一覧アサート更新。
  隔離ブラウザで、正本シートに登録したユーザーが実際の汎用シートのuser型
  セルのドロップダウンに現れること、`MeldexUserPicker.open()` の候補選択・
  自由入力・確定が実際に機能することを確認した。

## v0.6.209 (2026-07-19)

### feat(account): 検出との統合（ユーザーアカウント一元管理 Phase 2）

`docs/user-account-unification-plan-2026-07-18.md` Phase 2。Phase 1で導入した
正本「スタッフ管理シート」へ、既存の検出系から fill-only（既存行は一切上書き
しない）で行を追加する dual-write 期間の並走統合を行った。レガシーのスタッフ
リスト書き込みは変更せず残す。

- **起動時 self-upsert**: `gb-app.part02.js` の `init()` に
  `_primeStaffRegistrySelfUpsert()` を追加し、起動のたびに現在ユーザーを
  fill-only で正本へ登録する（未設定なら無ダイアログ自動作成、計画書§5.1の
  「起動時同期」トリガー）。
- **ワークスペース連携フック**: `meldex_collab_workspace_service.py` に
  `_sync_workspace_member_to_staff_registry` を新設し、ワークスペースの
  作成・更新・メンバー追加のたびに正本へ fill-only upsert する（既存の
  `_sync_workspace_member_to_production_staff` は無変更のまま並走）。
- Meldex.part02.part01.py のワークスペースルーターctxに
  `resolve_path`/`parse_frontmatter`/`write_frontmatter` を追加（`get_db` は
  意図的に含めない — 含めると既存の制作管理スタッフリストへの無条件同期まで
  有効化してしまい、Phase 1〜3で触らないと定めた制作管理側の挙動を変えてしまう
  ため）。
- **実機検証で発見・修正した回帰**: `gb-user-registry.js` の `<script>` タグが
  起動処理（`await init();`）を呼ぶインラインスクリプトより後に置かれており、
  `init()` 実行時点で `window.MeldexUserRegistry` が未定義のままとなって
  起動時 self-upsert が常に何もせず終了していた。`Meldex.html` / `Meldex-dev.html`
  の読み込み順を修正し、回帰テスト（`test_user_registry_script_loads_before_startup_init_is_invoked`）
  で固定した。
- 検証: 新規回帰テスト`test_meldex_staff_registry_phase2.py`、
  `test_meldex_collab_workspaces.py` に fill-only 検証2件を追加
  （既存行の表示名・権限が上書きされないこと、正本未設定時は何も作成しないこと）。
  隔離ブラウザ検証で、設定を開かなくても起動だけで正本にユーザーが登録される
  こと、シートで編集した値がページ再読込（起動処理の再実行）を経ても保持される
  ことを確認した。

## v0.6.208 (2026-07-19)

### feat(account): スタッフ管理シートの基盤導入（ユーザーアカウント一元管理 Phase 1）

`docs/user-account-unification-plan-2026-07-18.md` Phase 1。ユーザーアカウント
情報が5系統（ソースフォルダ検出登録簿・ワークスペースメンバー・制作管理スタッフ
リスト・旧ログイン`_users.json`・localStorage現在ユーザー）に分裂している問題へ、
正本となる「スタッフ管理シート」を新設して並走導入した。既存の5系統・6箇所の
候補ユーザー一覧実装・制作管理には一切触れていない（既存挙動は不変）。

- **新設（Python）**: `meldex_staff_registry_schema.py`（必須列12・権限select
  「管理者/メンバー/閲覧」・マーカー`meldex_registry: staff-v1`の定義）、
  `meldex_staff_registry_service.py`（正本の設定読み書き・フォルダノート
  自己修復・スタッフ行の取得/作成・fill-only更新・一意制約チェック）、
  `meldex_api_staff_registry.py`（`/api/staff-registry/config`・`/ensure`・
  `/list`・`/upsert`）。
- **新設（JS）**: `gb-staff-registry-schema.js`（Python版と契約テストで一致検査）、
  `gb-user-registry.js`（`MeldexUserRegistry`。apiFetch統一のためデスクトップ/
  クラウド静的で同じ実装が動く）。
- **列保護**: `gb-db-core.js`に`getSchemaProtectionLevel(dbPath, propName)`を
  新設し、`setPropertyType`/`_deleteColumn`/`_deleteColumnNoConfirm`/
  `renameDbProperty`（`gb-db-property-types.part01.part01.js`）を経由化。
  正本シートは置き場所が可変なため、制作管理（固定パス判定）と異なりフォルダ
  ノートのマーカーで必須列のみを保護し、管理者追加列は自由に削除・改名・型変更
  できる。バックエンド側も`meldex_api_database.part01.py`/`part02.py`に対称の
  拒否ガードを追加した。
- **自動作成・自動発見**: 初回アクセス時に無ダイアログでソースフォルダ直下へ
  自動作成し、現在ユーザーを1行目として登録する。設定未設定でもマーカー付き
  フォルダノートを浅い階層で見つけたら自動採用する。
- **設定ダイアログ**: 「ユーザー」パネルへ「スタッフ」セクションを新設
  （アイコン+表示名+権限バッジの一覧+正本シートを開くリンク+保存場所表示・変更）。
  死んでいた旧チーム一覧コード（`loadUserListForSettings`/
  `_renderTeamMemberRows`、表示先`#settings-user-list`が既に存在せず実質無効化
  していた）を削除し、`_renderStaffRegistrySettings`へ統合した。
- 検証: 新規回帰テスト`test_meldex_staff_registry_schema_contract.py`・
  `test_meldex_staff_registry_service.py`・`test_meldex_staff_registry_api.py`・
  `test_meldex_staff_registry_backend_guards.py`、`test_meldex_workspace_frontend.py`
  更新、既存の`test_meldex_batch76_db_props_regressions.py`等の回帰を維持。

## v0.6.207 (2026-07-19)

### fix(history): カレンダー表示シートを開いた後の取り消し・やり直し誤動作を修正

v0.6.206で発見・記録した実バグを修正した。共通 Undo/Redo ルーター
（`_meldexUndoRedoContext()`, `gb-history.part02.js`）の系統(B)判定
（`isCalendarDb()`）は `state.pivotData.calendar_db` の値だけを見ており、
カレンダー表示モードのシートを閉じて別のツール（CSV等）へ切り替えても
この値はクリアされないため残留していた。その結果、カレンダー表示モードの
シートを一度でも開いた後は、別ツールでの編集中もCtrl+Z・取り消しボタンが
誤って系統(B)（`_calUndo`/`_calRedo`）へルーティングされる不具合があった。

- **修正**: `_meldexCalendarSheetActive()` を新設し、`isCalendarDb()` に加えて
  `state.view` がシート表示系のビューモード（pivot/gallery/kanban/timeline/
  chart/graph/form/smart-db/calendar/tasks/shifts）であることも要求するよう
  厳密化した。`state.view` はツール切替のたびに `showView()` が確実に更新する
  ため、残留した `state.pivotData` だけでは誤ルーティングしなくなった。
  カレンダー表示モードのシートが実際にアクティブな場合の系統(B)ルーティングは
  従来どおり機能する（非退行）。
- ボードは `state.view === 'board'` の判定が系統(B)判定より先に評価されるため
  元々影響を受けていなかった（無変更）。

検証: `test_meldex_batch212_history_board_calendar_phase34.py`（新規回帰1件＋
既存1件を修正後の挙動へ更新）・`test_meldex_schedule_history_integration.py`・
`test_meldex_batch105_history_regressions.py` を実行し全緑。隔離インスタンスで
カレンダー表示モードのシートを開く→CSVへ切替→セル編集→Ctrl+Zとツールバーの
取り消しボタンがCSV編集の取り消しとして機能し、系統(B)が発火しないことを確認。
カレンダー表示モードのシートへ戻った時の取り消しも従来どおり機能することを確認。

## v0.6.206 (2026-07-19)

### test(history): 取り消し・やり直しボタン計画の受け入れ検証・仕上げ（フェーズ6）

取り消し・やり直しボタン計画の最終フェーズ。メイン画面8ツールバー＋単独アプリ3種の
受け入れ確認5項目を実ブラウザで最終確認し、フェーズ1〜2実装時に持ち越されていた
CSV・カレンダー・ボードの完全な往復検証（実データ作成含む）を実施した。

- **メイン画面**: 隔離インスタンス（`_verify/2026-07-19_undo-redo-toolbar-phase6/`）で
  スケジュール（ToDo追加）・ボード（カード追加）・CSV（セル編集）の3系統について、
  実データ作成→Ctrl+Z→ツールバーのやり直しボタンの往復を確認した。
- **実バグ1件を新規発見（別途 `AGENT_INBOX.md` に記録・本リリースでは未修正）**:
  カレンダー表示モードのシート（系統(B)）を一度開いた後、別のツール（CSV等）へ
  切り替えても `state.pivotData` がクリアされず `isCalendarDb()` が真のまま残留し、
  共通ルーターがCtrl+Z/取り消しボタンを誤って系統(B)側へルーティングしてしまう
  既存の状態管理ギャップを発見した。ボードは `state.view === 'board'` の判定が
  先に評価されるため影響を受けない。フェーズ5・6の変更起因ではなく、対象範囲外
  のため本リリースでは修正していない（詳細調査・修正は別タスク）。
- **単独アプリ3種の展開（ノート・シート・ボード）**: 詳細はv0.6.205のフェーズ5を参照。

検証: 関連ユニットテスト（history/shortcuts/standalone/board系）を個別実行し全緑。
実ブラウザで単独3アプリ＋メイン画面のカレンダー/ボード/CSVの往復を確認。

## v0.6.205 (2026-07-19)

### fix(history): 取り消し・やり直しボタンの単独アプリ3種（ノート/シート/ボード）展開

取り消し・やり直しボタン計画のフェーズ5。単独起動アプリのうちノート/シート/
ボードの3つへ、メイン画面と同じ取り消し・やり直しの挙動を展開した。

- **共通基盤の修正（実バグ1件）**: `renderHistoryList()` がボタンの有効/無効
  更新（`updateUndoRedoButtonStates()`）を履歴パネルDOM（`#rp-history-list`）の
  存在チェックより後に呼んでいたため、履歴パネルを持たない単独アプリでは
  ボタンの disabled 状態が一切更新されない不具合があった。DOM存在チェックより
  前に呼ぶよう順序を修正（`gb-history.part02.js`）。
- **シート単独版**: 本体と同じ共通履歴基盤（`gb-history.js`）を新たに同梱し、
  `#sheet-db-toolbar` に「元に戻す」「やり直し」ボタンを追加。本体の
  `gb-db-*.js` 一式（Phase 4で同梱済み）が内部で呼ぶ `historyPush` 等が
  未定義のまま一部の編集操作（候補値削除等）で例外を起こしうる潜在バグも
  同時に解消された。Ctrl+Z/Ctrl+Y（`bindShortcuts()`への追加、フォーカスが
  入力欄/自由記述欄にある間はブラウザ既定へ委譲する判定つき）とツールバー
  ボタン、両方とも `meldexUndo()`/`meldexRedo()` 経由で同じ `db:<パス>`
  スコープの共通履歴を操作する。
- **ボード単独版**: 取り消し・やり直しボタン自体とその場での動作（自己完結
  スタック `_bdUndoStack`/`_bdRedoStack` へのフォールバック）はフェーズ3-3で
  既に機能していたが、**Ctrl+Z/Ctrl+Yキーボードショートカットが無反応**という
  実バグを発見・修正した（`gb-shortcuts.js` の中央ハンドラが呼ぶ共通ルーター
  `meldexUndo()`/`meldexRedo()` が単独版に定義されておらず、静かに無視されて
  いた）。またボタンの有効/無効状態を更新する `updateUndoRedoButtonStates()`
  も同様に未定義だったため、スタックが空でもボタンが常に有効表示のままだった。
  `board-standalone-stubs.js` へ両方の薄いフォールバック実装（`bdUndo`/`bdRedo`
  への委譲、`_bdUndoStack`/`_bdRedoStack` の長さからの disabled 判定）を追加し、
  実ブラウザでカード追加→Ctrl+Z→Ctrl+Yの往復とボタン有効/無効表示を確認。
- **ノート単独版・シナリオ単独版**: 既存実装（`rtCmd('undo'/'redo')` によるブラウザ
  標準undo、`data-scenario-action="undo"/"redo"` によるエディタ内蔵undo）が
  そのまま機能することを実ブラウザで確認。追加のコード変更なし。

## v0.6.204 (2026-07-19)

### feat(standalone): タイマー残差分の確認と単独アプリ横断の仕上げ

単独アプリ本体パリティ計画 Phase 5（最終フェーズ）。タイマー単独版の残差分を
確認し、ノート単独版のルビ往復保存回帰テストを復活、5アプリ横断でスマホ幅の
安全策を展開した。

- **ノート単独版のルビ往復保存回帰テストを復活**（`test_meldex_note_ruby_roundtrip.py`
  新規）。Phase 3で独自Markdown変換モジュールと共に削除されたルビ往復テストを、
  今度は本体の実エンジン（`gb-editor.js` の `mdToHtml`/`htmlToMd`）を実ソースから
  抽出してNode実行する方式で再構築。`{base|reading}` ↔ `data-ruby` span/貼り付け
  `<ruby><rt>` の往復保存が今後も壊れていないことを継続的に検証する。
- **スマホ幅の安全策を5アプリへ展開**: 計画書が想定する「優先ボタン+あふれ
  メニュー」化はコード基盤ゼロだったため（`standalone-shell.css` 自身に
  「Phase 5で行う」と明記済み）、今回は暫定策として、既存のnote単独版
  `#page-rt-toolbar` の横スクロール+44pxタップ対象パターンを、従来@media規則が
  皆無だったシナリオの `#se-toolbar`、シートの新規 `#sheet-db-toolbar`/
  `.smart-db-toolbar` へ展開。ボタンが隠れる行き止まりを解消した（本格的な
  優先ボタン選定は後続タスクとして `AGENT_INBOX.md` に記録）。
- **タイマー単独版の確認**: 本体パリティはほぼ達成済みと確認。カレンダー連携
  （advanced設定内の `/cal/*`）はWindows単独版のローカルサーバーに未実装で
  あることが判明（計画時の想定と逆）。クラッシュはしないが機能もしないため、
  環境別の実機確認と対応方針決定を後続タスクとして記録。
- **受け入れ条件の全アプリ横断確認**: 本体と同一動作・オプションパネル・
  相互互換・行き止まりUIゼロ・テスト緑の5アプリ横断状況を確認（詳細は
  `AGENT_INBOX.md`「単独アプリ5種の本体機能パリティ」完了節を参照）。
- これで単独アプリ本体パリティ計画（Phase 0〜5）が完了。

## v0.6.203 (2026-07-19)

### feat(standalone): シート単独版の本体パリティ（実データ表示・編集の同梱）

単独アプリ本体パリティ計画 Phase 4。シート単独版は「本体とは別の簡易実装」
（定義編集フォーム＋簡易CSV表のみで、実データの表示・編集そのものが無い）
だったため差分が最大だったが、本体シートエンジン（`gb-database.js` +
`gb-db-*.js` 一式・約35ファイル）をそのまま同梱する方式へ全面的に拡張した。

- **フォルダを直接シートとして開く（新機能）**: メニュー「フォルダをシートとして
  開く...」から、本体と同じ形式（フォルダノート + `type: settings-entry` の
  エントリファイル群）のフォルダを開くと、本体の `selectDatabase()` が
  そのまま実データを描画する。セル編集・行追加・列追加・型変更・並び替え・
  複数条件フィルタ・列幅/固定・集計行・ビュー切替（表/ギャラリー/カンバン/
  タイムライン/チャート/グラフ/フォーム）は本体と同一の実装で動作する。
- **`.mel-sheet`（スマートシート定義）の実データプレビュー**: 既存の
  ソース・フィルタ編集フォームは維持しつつ、本体の `selectSmartDb()` /
  `renderSmartDbTable()` で実際に一致するエントリを表示するプレビューを追加。
  定義を保存しなくても、今の条件で何が一致するかその場で確認できる。
- **Windows単独版のローカルサーバーに実データAPIを追加**:
  `meldex_standalone_sheet_service.py`（新規）が `/pivot`・`/db-metadata`・
  `/value`（追加・更新・削除）・`/entity/create`・`/smart-db`・
  `/outliner-roots` を実装し、本体と同じ「1エントリ=1ファイル」形式の
  frontmatterを直接読み書きする。Cloud/PWA版は本体のCloudデータアクセス層
  （`gb-data-access-dropbox-expanded.part01.js`）が同じ経路を既に実装済みの
  ため追加実装なしで動作する。
- **本体の「シート表示設定」レイヤーを単独版へ移植**:
  `sheet-standalone-view-config.js`（新規）。ビューモード・列の表示/固定/幅・
  並び替え・複数条件フィルタ・集計行・ステータス一覧などの永続化ロジックは
  本体と同一（`gb-database.js`/`gb-db-views.js` 等25ファイルが前提とする
  内部APIのため、単独版でも同梱が必須）。
- **オプションパネル**: プロパティ設定（列名・型・新規エントリ初期値）・
  テーマタブが本体と同じ挙動で操作できる。「公開」「バックリンク」は
  保管庫全体の機能に依存するため単独版では隠す。
- 既存の `.mel-sheet`/CSV編集フォーム・関数（`normalizeSheet` 等）は
  そのまま維持（回帰テスト済み）。フォルダ選択はCloud/PWA版が
  `standalone-workspace-tree.js` のフォルダ選択ダイアログ、Windows版は
  ネイティブのフォルダ選択ダイアログ（`tkinter.filedialog.askdirectory`）
  を使う。
- 既知の簡略化（後続タスクとして記録）: 値の楽観的排他制御（リビジョン
  照合）は実施しない、フォルダのリネーム追従は未対応、取り消し・やり直しは
  別プロジェクトの対象。詳細は `AGENT_INBOX.md` を参照。

## v0.6.202 (2026-07-19)

### feat(standalone): ノート単独版の本体パリティ（本体エディタ資産への置き換え）

単独アプリ本体パリティ計画 Phase 3。ノート単独版は「本体とは別の簡易実装」
（独自contenteditable + 独自Markdown変換）だったため差分が最大だったが、
本体ノートエディタ資産をそのまま同梱する方式へ全面的に置き換えた。

- **エディタエンジンの置き換え**: 独自実装の `note-standalone-markdown.js`
  （Markdown⇔HTML変換）を撤去し、本体の `mdToHtml`/`htmlToMd`（`gb-editor.js`）を
  そのまま使用する。ルビ・表・コールアウト・チェックリスト・引用・折りたたみ
  ブロック等、本体ノートが対応する記法はすべて単独版でも同一の変換結果になる
  （変換ロジックが完全に同一のため、往復保存の差分が構造的に生まれない）。
  往復保存の無劣化確認は実ブラウザ操作（ルビ入りノートの作成→保存→再読込→
  内容一致確認）で実施し、本体との相互互換（本体作成ノートを単独版で開いて
  保存→本体で再読込）も確認した。
- **編集ツールバー**: 本体と同じ `#page-rt-toolbar` 構成（太字/斜体/下線/
  取り消し線・見出し選択・箇条書き/番号付き/引用・コールアウト・表・水平線・
  元に戻す/やり直し・目次表示切替・縦書き切替・見出しインデント表示・検索）を
  新設。ボタンの選択範囲保持は本体の `#page-rt-toolbar` 用 mousedown ハンドラ
  （`gb-editor.js`）にそのまま乗るため、単独版側での追加配線は不要だった。
- **表操作・スラッシュコマンド・ブロックドラッグ・見出し折りたたみ・Markdown
  記法ショートカット・ファイル内検索/置換・選択時書式ポップアップ**:
  `gb-note-enhance.js` / `gb-format-popup.js` / `gb-text-selection-format.js` /
  `gb-editor-preview.js` / `gb-shortcuts.js` を同梱するだけで有効化された
  （いずれも `#page-content` 等のIDに対して自己配線される設計のため）。
  キーボードショートカット（Ctrl+B/I/U、見出し1-6、リスト、引用、字下げ等）も
  `gb-shortcuts.js` の共通ディスパッチャに `state.view = 'page'` を合わせることで
  同時に有効化した。
- **Ctrl+K リンク挿入の安全性**: 単独版はCtrl+K内部リンク検索（要判断#4により
  対象外）を同梱しないため、`gb-shortcuts.js` はURL未検証のフォールバック
  プロンプトを使う。単独版側で `window.showLinkInsertModal` を安全な実装
  （スキームのホワイトリスト検証込み）へ差し替え、キーボード経由でもメニュー
  （「リンクを挿入」）と同じ安全性を確保した。
- **オプションパネル**: `_syncDetailPanel(label, path, 'page', {})` を配線し、
  本体と同じ「エディタ」（ファイル情報）「テーマ」（ファイル別スタイル。見出し/
  本文/リンク/引用/区切り線/選択・カーソル色）タブが使えるようになった。
  ファイル情報タブの作成日時・更新日時・サイズは、単独版サーバーに
  `/file-meta` 相当のエンドポイントが無いため未対応（ファイル名・パスは表示）。
  AGENT_INBOXに記録し見送り。
- **エクスポート**: 「Markdownとして保存」「画像（PNG）として保存」
  「Markdownをコピー」を追加（すべてクライアント側で完結、`gb-export-save.js`/
  `gb-export-image.js` を利用）。「Wordとして保存」（docx）は本体で
  `/export/note?format=docx`（`python-docx` 使用）というサーバー機能に
  依存しており、単独版の軽量サーバー（`meldex_standalone_runtime.py`）には
  未実装、Cloud/PWA静的版はそもそもサーバーが無いため搭載できない。
  今回は見送り、AGENT_INBOXへ記録（Windows版のみ新規エンドポイント追加で
  対応可能）。
- **Cloud画像参照の保全**: Cloud単独版はDropbox経由で読み込んだ画像をデータURL
  へ差し替えて表示するが、保存時にそのまま`htmlToMd`へ渡すと巨大なdata URLが
  本文に書き込まれてしまう。保存直前にクローンDOM上で元のサーバー参照へ戻す
  処理を維持し、本体と同じ軽量な `![alt|path=...](...)` 参照が保存されることを
  確認した。
- **旧実装の撤去**: `note-standalone-markdown.js` と専用テスト
  （`test_note_standalone_markdown.py` / `test_note_standalone_markdown_edges.py`）
  を削除。`test_standalone_apps.py` / `test_meldex_standalone_distribution.py` の
  参照箇所を更新した。
- **本体側の暗黙のDOM前提を実機発見・単独版側で吸収**: `gb-editor.js` は
  本体Meldex.htmlに常在する `#rt-toolbar`（エンティティ用ツールバーの複製元）
  と `#entity-freetext`（エンティティ自由記述欄）を、存在確認なしに参照する
  箇所がある（`document.getElementById('rt-toolbar').addEventListener(...)`、
  `setupEditableDropHandler(document.getElementById('entity-freetext'))`）。
  単独版にはエンティティ編集機能が無くこれらの要素が存在しないため、実機確認で
  未捕捉例外により初期化処理の残りが停止し、リッチテキストツールバーの選択範囲
  保持（`rtCaptureSelectionFromToolbar`）が配線されない不具合を発見した。
  本体側コードは変更せず、単独版のみに空のプレースホルダー要素
  （`hidden` 属性付き）を追加して吸収した。`setupEditableDropHandler` は
  隣接する `setupEditableDragSource`/`bindNoteEditorContextMenu` と異なり
  null ガードが無い非対称な実装になっているため、本体側の恒久対応（ガード追加）
  をAGENT_INBOXへ記録した。
- **オプションパネルのタブ描画ガード発見・単独版側で吸収**: 共有関数
  `_syncDetailPanel`（`gb-detail-panel.js`）は、`#rp-detail` がパネルシステム
  （`.gb-pane-content`）配下に無い場合、本体の旧・独立詳細パネル向け
  localStorage フラグ `detail-panel-cfg.visible` を見て早期returnする実装に
  なっている。単独版はこのフラグを一切管理しないため、常にreturnして
  「エディタ」「テーマ」タブが描画されない状態だった。単独版起動時に
  同フラグを `{visible:true}` へ固定して解消した。
- **「公開」「バックリンク」タブを単独版では非表示化**: 上記の配線後、
  本体と同じロジックにより「公開」（vault全体の公開設定が前提）と
  「バックリンク」（`GbBacklinks` 未同梱）タブも表示されてしまうが、
  単独版では機能しない（クリックしても空のまま、または読み込み失敗
  メッセージが出るだけ）ため、行き止まりUIとして残さず非表示にした
  （`_showFileInfoInDetailPanel` が非同期に `showNoteTabs(true)` 経由で
  再表示することがあるため、MutationObserverで継続的に隠す実装にした）。
- 対象外・見送り（実装時判断、理由を明記）:
  - **Wordとして保存（docx）**: 上記の通り、単独版サーバーの新規実装が
    必要なため見送り。
  - **ファイル情報タブの作成日時・更新日時・サイズ**: `/file-meta` 相当の
    エンドポイントが単独版サーバーに無いため未対応。
  - **Ctrl+K内部リンク検索・自動リンク・バックリンク・コメント/注釈の
    本体横断連携**: 計画書§4の対象外方針どおり据え置き。

## v0.6.201 (2026-07-19)

### feat(standalone): ボード単独版の残差分パリティとパネル幅復元バグ修正

単独アプリ本体パリティ計画 Phase 2。ボード単独版はPhase 0時点で「ほぼパリティ済み」
だった残りの差分を解消した。

- **P2バグ修正**: `board-standalone-app.js` の保存済みパネル幅読み取りヘルパーが、
  localStorage未保存（null）を `Number(null) === 0` により「保存済みの0」と誤認し、
  初回起動時に右サイドバー幅・ビューワー高さが既定値ではなく最小幅へ縮んでいた
  （standalone-option-panel.js に導入済みの同型修正をこちらにも適用）。
- **コメント一覧の完全版化**: 簡易版だった `loadRpAnnotationList` を本体版
  （`gb-right-panel.js` 同梱）へ置き換えた。フィルタ（タイプ/対象ファイル/状態/
  ユーザー/検索）・並び替え・プレビュー/リスト表示・解決済み管理・返信が単独版でも
  同一に使える。右サイドバーの「ビューワー」区画にコメント一覧用のUI（本体
  `#rp-annotation` と同一のID/構造）を追加し、リンクプレビューと hidden 属性で
  切り替える。カード右クリックメニューの「コメントを追加」「コメント一覧を開く」
  （本体と共有の `gb-canvas-features.js` 由来、既存のまま）から到達できる。
  - `gb-right-panel.js` は本体の3カラム右パネル（`#right-panel` 等）の存在を前提に
    `toggleRightPanelTab`/`openRightPanelTab` を上書きしてしまうため、単独版の
    右サイドバー実装（`_openStandalonePanel`）へ差し戻す配線を追加した
    （`board-standalone-stubs.js` / `board-standalone-app.js`）。
- **コメントバッジ**: `gb-comment-badges.js` を同梱。カードのコメント件数バッジ
  （0件は「+」、1件以上は件数表示）が本体と同じ挙動で動作することを実機確認した。
  副次効果として、コメント追加のUIも簡易プロンプトから本体と同じインライン
  テキストエリア入力に切り替わった。
- **HTML/PNG書き出し**: ハンバーガーメニューへ「画像（PNG）として保存」
  「HTMLとして保存...」を追加。本体と同じ `bdExportImage()` /
  `MeldexExportHtml.exportCurrentView('board')` をそのまま利用する
  （`gb-export-save.js` / `gb-export-html.js` を同梱。保存は単独アプリ用
  フォールバック経由）。`bdExportImage()` のhtml2canvas呼び出しにも、
  シナリオPNG出力と同じ上限時間（既定25秒）を追加した。
- 対象外・見送り（実装時判断、理由を明記）:
  - **バージョン履歴**: ネイティブのローカルフォルダ運用にはサーバー側の
    バージョン保存機構（`/api/version/*`）が存在せず、実装には新しいローカル
    スナップショット保存の追加が必要（既存コードの配線だけでは完結しない）。
    今回は見送り、AGENT_INBOXへ記録。
  - **一括取込**: 本体は「開いているシートタブ」からの候補収集が前提のため、
    単独版では「シートファイルを直接選ぶ」という設計変更を伴う新規UIが必要。
    今回は見送り、AGENT_INBOXへ記録。
  - **右サイドバー共通枠（standalone-option-panel.js）との整合**: ボードは
    Phase 0以前から独自実装で安定動作しているため、内部実装の統一は
    回帰リスク優先で見送り、動作は変更していない。

## v0.6.200 (2026-07-19)

### feat(standalone): シナリオ単独版の本体パリティ（オプションパネル配線・エクスポート復旧）

単独アプリ本体パリティ計画（`standalone-apps-parity_plan_2026-07-18.md`）Phase 1。Phase 0で
設置済みの右サイドバー枠に、本体と同じシナリオ用オプションパネルタブ（タイプ管理/テーマ/
表示/ルビ/行セット）を実配線し、エクスポート導線を復旧した。

- **オプションパネルの実配線**: `scenario-standalone-app.js` の `_skipDetailSync = true` を撤去し、
  `ScriptNoteComponent._syncDetailPanel()` が本体と同じ5タブ（タイプ管理・テーマ・表示・ルビ・
  行セット）を `#rp-detail` へ描画するようにした。単独版専用のルビドロワー（`#scenario-ruby-drawer`）
  は廃止し、オプションパネルの「ルビ」タブへ統合（重複UI解消）。ヘッダーの「オプションを開く」
  ボタンのアイコンを `panelRight`（Phase 0の暫定）から `slidersHorizontal`（本体流儀）へ変更した。
- **ファイルスタイル基盤の単独版アダプタ**: 本体では `gb-note-enhance.js`（ノート機能拡張ファイル）内に
  同居する `_getScriptNoteEditorForFileStyle` 等5つの定義がシナリオの「テーマ」タブ描画に必須だが、
  単独版シナリオはノートエディタ本体を同梱しないため未定義だった。ノート機能拡張全体（約2000行、
  スラッシュコマンド等ノート専用機能を含む）を同梱する代わりに、必要な5定義だけを
  `scenario-standalone-app.js` に用意した（本体側の一覧変更時は追随が必要、AGENT_INBOX記録済み）。
- **新規シナリオの空状態バグ修正**: `ScriptNoteComponent.activate()` が、ドキュメントが
  メモリ上に読み込み済みでもパスが空（保存前の新規文書）というだけで空状態プレースホルダーへ
  常時上書きしていたため、単独版の「新規シナリオ」が実際には編集不能な「シナリオファイル未読込」
  表示のまま起動していた（既存の潜在バグ、本Phaseの配線作業で発覚）。エディタに文書が
  無い場合のみ空状態を表示するよう修正（`gb-tool-scriptnote.js`、本体側の挙動は変更なし）。
- **書式一括設定**: `gb-color-palette.js` / `gb-format-popup.js` / `gb-format-style-ui.js` を同梱し、
  タイプ管理タブの背景色・文字色・太字・枠線などの一括書式設定を有効化した。
- **エクスポート導線の復旧**: ハンバーガーメニューへ「画像（PNG）として保存」「HTMLとして保存」
  「Markdownとして保存」を追加（`gb-scriptnote-clipstudio.js` / `gb-export-save.js` /
  `gb-export-image.js` を同梱）。本体の複数パネル前提（`GBLayout`）で動く `_sn2GetActiveEditor()` を
  単独版用に上書きし、本体のエクスポート関数をそのまま利用できるようにした。
- **CLIP STUDIO PAINTへ送信**: Windows版のみメニュー表示（要判断#3の決定どおり、Cloud/PWA版は非表示）。
- **単独アプリの保存ダイアログ代替**: 単独アプリには本体の `/api/save-file-dialog`（サーバー側
  ネイティブダイアログ）が無いため、`gb-export-save.js` の `saveText`/`saveBlob` に
  単独アプリ検出（`MeldexStandaloneFS`/`BoardStandaloneFS` の有無）による分岐を追加し、
  File System Access API（`showSaveFilePicker`）が使える環境ではファイル名を選べる保存、
  使えない環境は通常のブラウザダウンロードへフォールバックするようにした（本体の判定条件には
  一致しないため本体の挙動は変更なし）。この変更はボード単独版（Phase 2）のPNG/HTML書き出しにも
  効く。
- **PNG出力の安定化**: html2canvasフォールバック経路が、Chromeがネイティブのチェックボックス等に
  用いる `color(srgb ...)` 形式のcomputed styleを解釈できず失敗する問題を確認し、`ignoreElements`
  でシナリオの行選択チェックボックス等UI専用要素を撮影対象から除外した。あわせて、サーバー側
  レンダリングが使えない環境でこの経路が唯一の手段になることを踏まえ、上限時間（既定25秒）を
  設けてタイムアウト時に明確なエラーを返すようにした。
- 新規アイコン3種（`ico-image` / `ico-fileCode` / `ico-clipboardCopy`）を `meldex-core.js` の
  アイコン解決表へ追加。

対象外（計画書§4のとおり）: シナリオのバックリンク・チャット等の他アプリ連携系タブ。

## v0.6.199 (2026-07-19)

### fix(history): スケジュールタブ本体の取り消し・やり直しを共通履歴へ統合（フェーズ3-2の実質置き換え）

v0.6.198で保留した「[P1] カレンダーに2系統の独自undoスタックが併存し…」を解消。実際にユーザーが
使う「スケジュール」タブ本体（`CalendarComponent`, `gb-tool-calendar.js`）の独自
`this._undoStack`/`_pushUndo`/`_undo`/`_redo` は、Ctrl+Z・ツールバーボタンのどちらからも
到達できず、稀にシートの「カレンダー表示モード」（系統(B)、`_calUndoStack`系）へ誤って
ルーティングされていた。

- **系統(A)を共通履歴へ統合した。** `CalendarComponent._pushUndo/_undo/_redo`（`gb-tool-calendar.js`）は、
  共通履歴（`gb-history.js`）が読み込まれている環境では `'schedule:<tabId>'` スコープの共通履歴へ
  委譲する（`historyRegisterSnapshotProvider('schedule:', {capture, restore})` を新設し、
  イベント/タスク/表示期間の3点セットのスナップショットを capture/restore する）。共通履歴が無い
  環境（将来の単独起動・テストスタブ向けの保険）は従来の自己完結スタックへ自動フォールバックする。
  `_undoBusy` ガード（復元中の多重実行防止）の意味は維持した。
  - スコープの接頭辞は計画で想定していた `'calendar:'` ではなく `'schedule:'` を採用した。
    実装調査の結果、`gb-tool-calendar-options.js` が既に `'calendar:settings'`
    （サイドバー表示・週開始曜日等の設定変更用スコープ）を使っており、
    `historyRegisterSnapshotProvider` は前方一致でスコープを解決するため、`'calendar:'` を
    登録すると `'calendar:settings'` にも誤って一致し、設定変更のやり直しクロージャが
    上書きされて壊れることが判明したため。
- **表示期間の変更で取り消し履歴を破棄する既存挙動を維持した。** `_guardUndoLoadWindow()` は
  共通履歴側の `'schedule:<tabId>'` スコープと自己完結スタックの両方をクリアする
  （ボードの `bdClearUndoStacks()` と同型の二重クリア）。
- **ルーティングの優先順位を修正した。** `_meldexUndoRedoContext()`（`gb-history.part02.js`）は
  「埋め込みシート面 → スケジュールタブ本体 → board残留 → 系統(B) → 共通履歴」の順に評価するよう
  変更し、以下2点を解消した。
  1. スケジュールタブがアクティブな間は系統(B)（`_calUndoStack`）へ誤ってルーティングされない。
  2. **既知の残留バグ**: ボードタブ表示後にスケジュールタブへ切り替えても
     `CalendarComponent.activate()` は `state.view` を更新しないため `'board'` が残留し、
     従来の `state.view === 'board'` 最優先判定ではスケジュールタブのCtrl+Zがボードへ誤って
     発火し得た。実際にアクティブなタブを明示的に確認する判定を先に評価するよう変更し解消した。
  - スケジュールタブ自身の取り消し履歴が空の場合は、既存の `'calendar:settings'`
    （サイドバー表示・週開始曜日等）の取り消しへフォールバックし、退行させていない。
  - 制作管理タスクリスト面（埋め込みシート）表示中のCtrl+Z（フェーズ4）・系統(B)・
    `db:`/`board:` スコープは無変更。
- アクティブ履歴スコープ（操作履歴パネルの「現在」表示先）を、`activate()`・面切替
  （カレンダー⇔制作管理タスクリスト⇔管理リスト）のタイミングで正しい面へ切り替えるよう
  `CalendarComponent.prototype._syncHistoryScope()` を新設した。
- 回帰テスト: `test_meldex_schedule_history_integration.py` を新設（ルーティング優先順位・
  スナップショット・プロバイダの往復・設定スコープへのフォールバック・表示期間変更時の
  破棄・タブ間のスコープ分離・`_undoBusy`再入防止・共通履歴なし環境へのフォールバックを固定）。
  既存の `test_meldex_batch212_history_board_calendar_phase34.py` のルーティング優先順位テストは
  新しい優先順位に合わせて更新した。
- マニュアル更新: 「カレンダー マニュアル」「ヒストリーパネル マニュアル」の
  既知の不具合注記を、解消後の状態に合わせて更新した。

## v0.6.198 (2026-07-19)

### feat(history): ボードの取り消し履歴を共通履歴へ統合、制作管理タスクリストのCtrl+Zを解消（フェーズ3〜4・一部保留）

計画書 `undo-redo-toolbar_plan_2026-07-18.md` のフェーズ3〜4を実装。フェーズ3のカレンダー対象は
実装調査の結果、計画書の前提と実際のコードが食い違うことが判明し**保留**した（詳細下記）。

- **フェーズ3-1: スナップショット・プロバイダ機構を一般化した。** `gb-history.part02.js` に
  `historyRegisterSnapshotProvider(scopePrefix, {capture, restore})` / `_findHistorySnapshotProvider(scope)`
  を新設し、これまでシナリオ専用にハードコードされていた「undo/redo直前に現在状態を再キャプチャして
  反対方向のクロージャへ差し替える」特例処理を、スコープ接頭辞ベースの登録テーブル参照へ置き換えた
  （シナリオの挙動自体は変更なし。回帰テストで固定）。
- **フェーズ3-3: ボードの取り消し履歴を共通履歴へ統合した。** `bdPushUndo()`/`bdUndo()`/`bdRedo()`/
  `bdClearUndoStacks()`（`gb-canvas-engine.part04.js`）は、共通履歴（`gb-history.js`）が読み込まれている
  環境（本体アプリ）では `'board:<パス>' ` スコープの共通履歴へ委譲し、右パネルの操作履歴タブにも
  ボードの操作が表示され、パネルからの巻き戻しも機能するようになった。ボードタブがアクティブになった
  タイミング（`CanvasComponent.activate()`、`gb-tool-canvas.js`）でアクティブスコープを切り替える。
  複数ボードタブの取り消し履歴は、共通履歴がスコープ文字列（ボードパスごと）で自然に分離されるため、
  従来 `bdDumpState()`/`bdLoadState()` が行っていた取り消しスタックのダンプ/復元は不要になった。
  単独起動アプリ（`board-standalone.html` 等、共通履歴を読み込まない環境）は、従来どおり
  `_bdUndoStack`/`_bdRedoStack` の自己完結スタックへ自動的にフォールバックし、挙動は変えていない。
- **フェーズ3-2（シートのカレンダー表示モードの独自スタック統合）は未実施。** 計画書は
  `gb-calendar.part01.part01.js` の `_calUndoStack`/`_calPushUndo`/`_calUndo`/`_calRedo`
  （シートをカレンダー表示モードで開いたときの独自実装）を対象にしていたが、調査の結果、実際に
  ユーザーが使う「スケジュール」タブ（`CalendarComponent`, `gb-tool-calendar.js`）は完全に別系統の
  独自スタック（`this._undoStack`/`_pushUndo`/`_undo`/`_redo`）を持ち、かつ**Ctrl+Z・ツールバーボタンの
  どちらからも現在は正しく到達できていない**（グローバルの `global.undo` ショートカットが常に先に
  発火し `preventDefault()` を呼ぶため、`CalendarComponent.handleKeyDown` は実行されない。ツールバー
  ボタンは前フェーズで `meldexUndo()` 経由に配線されたが、これは無関係な `_calUndoStack` 系へ
  ルーティングされてしまう）という、計画書作成時の調査では把握されていなかった実装状況が判明した。
  この2系統をどう整理するか（`_calUndoStack` 系を廃止して `CalendarComponent` 側へ寄せるか、両方を
  共通履歴へ統合するか）は設計判断が必要なため、本セッションでは実施せず、AGENT_INBOXへ引き継いだ。
  `_meldexUndoRedoContext()`/`meldexCanUndo()`/`meldexCanRedo()` のカレンダー分岐は既存のまま変更していない。
- **フェーズ4: 制作管理埋め込みシートのCtrl+Z不可を解消した。** 実際の介入点はAGENT_INBOXの記載
  （`CalendarComponent.handleKeyDown`）ではなく、上記と同じ理由で到達不能だったため、共通ルーター
  `_meldexUndoRedoContext()`/`meldexUndo()`/`meldexRedo()`（`gb-history.part02.js`）側に
  「制作管理タブが埋め込みシート面（タスクリスト/管理リスト）を表示している間は、埋め込みシートの
  `db:<パス>` スコープへ委譲する」分岐を追加した。`skipHistoryScope: true` はそのまま維持しており
  （メインペインのアクティブスコープを汚さない）、埋め込みシート自体のセル編集は元から正しい
  スコープへ push されていたため、ルーターの参照先を直すだけで解決した。Ctrl+Zとツールバーの
  取り消し・やり直しボタンはどちらも同じ `meldexUndo()`/`meldexRedo()` を経由するため、両方とも
  同時に解消される。メインペインで別シートを開いている場合は従来どおりそちらのスコープが対象になる。

検証: 新規回帰テスト `test_meldex_batch212_history_board_calendar_phase34.py`
（スナップショット・プロバイダの一般化がシナリオの往復挙動を変えないこと / ボードスコープの
往復取り消し・やり直しが2周しても乖離しないこと / db:スコープの通常編集がプロバイダに一切
上書きされないこと / ルーターのコンテキスト判定の優先順位（board > embedded-sheet > calendar > history）
/ 埋め込みシートの取り消し・やり直しがメインペインの別シートに影響しないこと）。既存の
`test_meldex_batch105_history_regressions.py`・`test_meldex_batch53_canvas_engine_regressions.py`・
`test_meldex_board_free_line_regressions.py`・`test_standalone_apps.py`・
`test_meldex_production_sheet_embed_contract.py` を実行し全緑（既存の無関係な既知の赤1件を除く）。
実ブラウザでの検証内容は別セクション参照。

## v0.6.197 (2026-07-19)

### fix(sheet): シートのセル編集の取り消しがリビジョン競合(409)で失敗する問題を修正

- **原因**: 数値・メモ（リッチテキスト）・複数選択の3つのセル編集は、保存中に画面表示中の値を書き換えないよう、値オブジェクトのコピー（`saveRef`）を作ってから保存APIへ渡していた。保存APIはサーバーから返った新しいリビジョン番号を「保存に使ったオブジェクト自身」へ書き戻すため、コピー側だけが更新され、直後に登録される取り消し（Undo）が参照する元の値オブジェクトは古いリビジョンのまま取り残されていた。取り消し実行時にその古いリビジョンで書き込むと、サーバー側のリビジョン競合チェックに引っかかり、「このエントリは他の端末で更新されています」エラーで失敗していた（v0.6.196の既知の問題として記載済み）。
- **修正**: [gb-db-core.js](gb-db-core.js) に共有ヘルパー `_syncValueRefAfterSave(saveRef, target)` を新設し、保存成功後にコピー側から元の値オブジェクトへ file/property/candidate_index/リビジョン番号を同期するようにした。同じコピー渡しパターンを使う3箇所すべてに適用: 数値セル編集（[gb-db-value-editors.part01.js](gb-db-value-editors.part01.js) `_startNumberValueEdit()`）、メモ/リッチテキストセル編集（[gb-db-cell-ui.js](gb-db-cell-ui.js) `startInlineEdit()`）、複数選択セル編集（[gb-db-inline-edit.part02.js](gb-db-inline-edit.part02.js) `saveMultiSelectAndRestore()`）。
- 他の値エディタ（日付・チェックボックス・選択・リレーション・ユーザー・リンク・画像・複数ソースリレーション等）は保存APIへ値オブジェクト自身を直接渡す実装のため、同型の不具合は無いことを全数確認済み。

検証: 新規回帰テスト（`test_meldex_sheet_cell_edit_undo_revision_regressions.py`）で、数値・メモ・複数選択それぞれについて「編集→保存によるリビジョン更新→取り消しと同一の書き込み」の一連が409にならないことを固定（修正前のコードに対して実行し、全ケースが失敗することを確認した上で、修正後に全緑になることを確認）。関連する既存の値エディタ・Undo系ユニットテスト（batch37/69/70/71/74/77/78/82/83 ほか）を実行し全緑。実ブラウザで数値セルとメモセルの編集→取り消し→やり直しの往復を、ツールバーの取り消しボタンとCtrl+Zの両方で確認。

## v0.6.196 (2026-07-19)

### feat(history): 取り消し・やり直しボタンの共通基盤とメイン画面ツールバーへの設置（フェーズ1〜2）

- **共通ルーター `meldexUndo()` / `meldexRedo()` を新設した。** これまで Ctrl+Z / Ctrl+Y ハンドラ（`gb-shortcuts.part01.js`）に直接書かれていた「ボード→カレンダー→共通履歴」の振り分けロジックを `gb-history.part02.js` 側の共通関数へ抽出し、ツールバーのボタンとキーボードショートカットの両方から同じ入口を呼ぶようにした。ショートカット側は「編集領域内ならブラウザ標準に委譲」の判定だけを残す薄いハンドラになった。
- **有効/無効判定API `historyCanUndo()` / `historyCanRedo()` と、ツールバーボタン向けの `meldexCanUndo()` / `meldexCanRedo()` を新設した。** 全ツールバーの取り消し・やり直しボタンは `data-undo-button` / `data-redo-button` を目印に `updateUndoRedoButtonStates()` で一括更新され、対象の操作履歴が無い場合はボタンが無効表示になる。カレンダー・ボードは独自の操作履歴スタックのままのため（共通履歴への統合は次フェーズ）、フェーズ3完了までの暫定として `meldexCanUndo`/`meldexCanRedo` がそれぞれの独自スタックの件数を直接参照する分岐を持つ。
- **右パネルの操作履歴タブにある「戻す/進む」ボタンも共通ルーター経由に統一した。** 従来は `historyUndo()`/`historyRedo()` を直接呼んでいたが、ボード・カレンダー表示中はそれぞれの専用取り消しに正しく委譲されるよう `meldexUndo()`/`meldexRedo()` へ差し替えた。
- **アイコンを整備した。** 共通アイコン辞書とアイコン置換処理に `undo2` / `redo2` を追加。これにより、単独版シナリオ（`scenario-standalone.html`）に既にあった取り消し・やり直しボタンで、アイコン未登録のため空表示になっていた既存不具合も合わせて解消した。
- **対象ツールバーに取り消し・やり直しボタンを設置した。** シナリオ・シート・ノート・自由記述欄・フォルダ・カレンダー（制作面ツールバー表示中は自動的に隠れる）・ボード・CSVの各ツールバー。ノート・自由記述欄はブラウザ標準の取り消し（`document.execCommand`）を使うため、この2箇所のボタンのみ判定手段が無く常時有効。Cloud閲覧専用モードでは、CSV・フォルダの新規ボタンも既存の編集操作と同様に無効化されるよう `gb-cloud-bootstrap.js` の対象リストへ追加した。
- 制作管理タスクリスト埋め込みシートの取り消し対応（フェーズ4）、カレンダー・ボードの共通履歴統合（フェーズ3）、単独起動アプリへの展開（フェーズ5）は本リリースの対象外（計画書 `app/docs/undo-redo-toolbar_plan_2026-07-18.md` のフェーズ3以降を参照）。
- **既知の問題（今回の変更起因ではない既存不具合。v0.6.197で修正）**: シートのセル編集を取り消す操作は、ボタン・Ctrl+Zのどちらでも「このエントリは他の端末で更新されています」エラーで失敗する場合がある（編集直後のスナップショットに古いリビジョン番号が残るため）。取り消しボタンの配線自体はCtrl+Zと完全に同一の動作をしており、原因は本フェーズの変更以前から存在する取り消し機構側の不具合と切り分け済み。別タスクとして起票済み。

検証: 既存の履歴・ショートカット関連ユニットテストを新しい実装に合わせて更新（`test_meldex_batch105_history_regressions.py` / `test_meldex_batch92_e2e_extra_regressions.py`）。静的HTMLツールバーの構造監査E2E（`targeted-shell-micro-static-html-control-contract`）が新設ボタンを含めて合格。実ブラウザでノート・シート・シナリオの3系統について、ボタン表示・無効表示・Ctrl+Z等価・履歴パネル反映を確認（シートは上記既知の問題を確認）。

## v0.6.195 (2026-07-19)

### feat(sheet): シートテンプレートの刷新（プレビュー付きダイアログ・アイコン設定・ビュー包含）

- **テンプレートダイアログのレイアウトを刷新した。** 左サイドバーのTierフィルタを廃止し、ヘッダー直下の横並びチップ（すべて/基本/標準/ジャンル別/カスタム）へ変更。ヘッダーに名前・説明・プロパティ名でインクリメンタル絞り込みできる検索欄を追加した。デスクトップはカードグリッド＋右側の固定プレビューペインの2ペイン構成にし、カードを選ぶとその場でプレビューが更新される（重ねモーダルを開かない）。モバイルは従来どおり重ねプレビューモーダルを使用する。
- **全テンプレート（初期搭載＋自作）にアイコンを設定・表示できるようにした。** 自作テンプレートのアイコンは、チャット等と同じ共通アイコンポップアップ（Lucideアイコン／Noto Emoji）から選べる。カスタムテンプレートの名前・説明・アイコンだけを編集できる「編集」ダイアログを新設した（プロパティ・ビューは作成時のまま変更されない）。アイコンピッカー表示中はEscapeキーがピッカーを閉じることを優先し、背後のテンプレートダイアログまで誤って閉じないようにした。
- **テンプレートにビュー（フィルタ・並べ替え・グループ化などの型別設定を含む）を含められるようにした。** 現在のシートからテンプレートを作成すると、保存済みビューの構成（フィルタ、並べ替え、ピボット/カンバンのグループ化、カレンダー連携、タイムライン設定、チャート/グラフ設定、フォーム設定など）も一緒に保存される。適用時は名前とビュー種別が一致する既存ビューをスキップし（名前だけが重複する場合は連番で追加）、それ以外は加算方式で追加する。エントリ順（実データ依存）やクイックフィルタの実行時状態は保存対象から除外している。
- セレクト/マルチセレクトの項目色は、既存のプロパティ型設定の一部として引き続きテンプレートに保存・復元される（追加の変更は不要だったことを確認済み）。
- 上記はすべてファイル形式として追加のみの変更（既存フィールドの意味変更なし）。旧バージョンは新しいテンプレートの `savedViews` フィールドを無視して読み込む（アイコン未対応部分はLucideアイコンを生名で保存するため大半のアイコンは旧版でも表示され、Noto Emojiのみ旧版で空表示になる＝表示劣化のみでデータ破壊はない）。

検証: 新規ユニットテスト（`test_meldex_db_template_views_regressions.py` — サニタイズの保持/除外、savedViews適用の加算/スキップ/連番、旧形式フォールバック、エクスポートの併存出力、アイコン保存形式）。既存のテンプレート関連回帰テスト（batch79/batch80/part05/part06/part08）を更新の上、継続グリーン。新設シート微細挙動E2E2件（アイコンピッカー操作とEscapeガード、ビューの作成・編集・適用の一連の確認）を追加、既存のギャラリー操作E2Eをデスクトップ/モバイルの分岐アサートへ更新。

## v0.6.194 (2026-07-19)

### feat(tags): 共通タグ機能（タグパネルの右サイドバー独立化＋ボード/シート/クイックメモ対応）

- **タグパネルを右サイドバーの独立パネルにした。** 右レール（右端の縦アイコン列）に「タグ」ボタンが追加され、クリックすると右サイドバーにタグ階層ツリーが開く。フォルダを表示していないときでも常に開ける（フォルダへの絞り込みだけはフォルダ表示中が必要）。新規レイアウトだけでなく、既存の保存済みレイアウトにも自動でボタンが追加される。モバイルのツールメニューにも「タグ」導線を追加した。
- **旧: オプションパネル内「タグ管理」タブ（フォルダ表示中のみ）は廃止し、上記の独立パネルに統合した。** タグの追加・グループ分け・プリセット切替・自動タグ付け・絞り込み・検索は従来どおり全て右サイドバー上で動作する。ファイル/フォルダ単位のタグ編集欄（オプションパネルのファイル情報内）は変更していない。
- **ボードのカードにタグを付けられるようにした。** カードのオプションパネルに「タグ」欄を追加。ボードツールバーの「フィルタ」から、そのボードで使われているタグを選んでカードを絞り込める（非該当カードは削除・非表示にせず減光表示。選択状態はボードファイルに保存され次回開いたときも復元される）。
- **シートに新しいプロパティ型「共通タグ」を追加した。** セル値はタグの複数選択で、候補はグローバルタグカタログから供給される（候補になければその場で新規タグを作成可能）。タグの名前・色変更は自動で反映される（ID参照のため）。複数条件フィルタ（フィルタ詳細）でも共通タグ列の「含む/含まない」に対応し、タグ名を入力すると候補が出るようにした。既存の「タグ」列（セレクト/マルチセレクト）とは別のプロパティ型で、既存の列・保存済みデータは無変換のまま。
- **クイックメモのタグ候補一覧に共通タグを統合表示した。** 共通タグ由来の候補は点線枠のチップで区別され、色スウォッチが付く。保存形式は変更しておらず、共通タグを選んだ場合もこれまでどおりタグ名の文字列として保存される。自動タグ付けの候補プールにも共通タグ名を含めた。
- 新規プロパティ型・ボードのカードタグ・共通タグ絞り込み設定は、いずれもファイル形式として追加のみの変更（既存フィールドの意味変更なし）。旧バージョンで新形式ファイル（タグ付きカード、共通タグ列）を開いてもクラッシュせず、未知のデータとして安全に無視される（タグの実データはフロントマター内に保全され、後で新版を使えば復元される）。

検証: 新規ユニットテスト20件（タグパネルの登録・撤去、ボードの往復保存と旧版互換シミュレーション、シートのプロパティ型登録とセル表示/編集/フィルタ、クイックメモの共通タグ統合と自動タグ付け候補統合）。既存のタグ関連・シート関連・ボード関連・クイックメモ関連の回帰テストは全て継続グリーン（無関係な既存失敗1件を除く）。

## v0.6.193 (2026-07-19)

### feat(sheet): セレクト/マルチセレクトの項目色設定 と プロパティ型「リンク」を追加

- **セレクト/マルチセレクトの項目に色を設定できるようにした。** 設定場所はプロパティ設定画面の「選択肢の色」欄（選択肢ごとにカラーパレットのスウォッチを配置）。設定した色はセルのチップ背景・文字色（読みやすい白/黒を自動選択）、単一/マルチのドロップダウン内の色ドット、テーブルのグループヘッダー行、カンバンの列ヘッダーに反映される。セル編集ドロップダウン内からの色変更（この段階では表示のみ）は次段階の対応とする。
- **プロパティ型「リンク」を新設した。** ワークスペース/ホームフォルダ/ソースフォルダ配下の任意のファイル・フォルダへのリンクを1セルにつき1件保持できる。空セルクリックでフォルダツリーのダイアログが開き、ファイル・フォルダを選択して確定する（サブフォルダの展開にも対応）。左サイドバーのフォルダツリーからドラッグ＆ドロップして設定することもできる。セルのリンクはクリックでサブパネルに開き、ダブルクリックで現在のタブに開く。設定済みのリンクはセルのコンテキストメニュー「リンク先を変更...」から選び直せる。
- 上記のためにフォルダ選択ダイアログ（`GBFolderPicker`）をファイルも選択できるモードに拡張した（既存の「フォルダのみ」利用箇所は無変更）。
- 新規プロパティ型・項目色ともにファイル形式は追加のみの変更（既存フィールドの意味変更なし）。旧バージョンでリンク型の列を開いた場合は未知の型として通常のテキスト表示にフォールバックし、データは壊れない。

検証: 新規ユニットテスト（frontmatterの往復保存、プロパティ型監査マニフェスト整合）、新設シート微細挙動E2E2件（セレクト項目色の一連の反映確認、リンク型の選択・D&D・開く・再選択の一連の操作確認）、既存のシートプロパティ型監査E2Eへリンク型を追加。既存のシート関連回帰テストは影響なし。

## v0.6.192 (2026-07-19)

### fix(standalone): ノート単独版のルビ消失防止＋単独アプリ共通の右サイドバー枠を新設（パリティPhase 0）

- **データ保全（最優先）**: ノート単独版（`note-standalone-markdown.js`）の保存変換が `<ruby><rt>` タグや、本体エディタがルビの編集表現に使う `<span data-ruby="...">` を特別扱いしておらず、これらが編集領域に混入すると保存時にルビの読み仮名が本文へ無区切りで結合される（例:「浦島太郎」+「うらしまたろう」→「浦島太郎うらしまたろう」）、または `data-ruby` 属性ごと無音で消える問題があった。本体が保存に使うルビ記法 `{base|ルビ}` へ変換する専用処理を追加し、往復保存でルビが失われないようにした。
- 単独アプリ5種（ノート/シナリオ/シート/タイマー）に、本体と同じ考え方の右サイドバー（オプションパネル）の外枠を新設した（新規 `standalone-option-panel.js` + `standalone-shell.css`）。開閉・幅のドラッグ変更・状態のlocalStorage復元に対応し、各アプリのツールバーに「オプションを開く」ボタンを追加した。中身（本体タブの実データ表示）は今後のフェーズで充実させる（現時点では枠のみ）。
- Cloud/PWA版の狭幅表示では、ボード単独版と同じ方式で右サイドバーを右からのスライドインドロワー表示にする土台を用意した（優先ボタン化等の仕上げは別途）。
- ボード単独版は既存実装のまま変更していない（回帰防止）。

検証: 往復保存の自動テストを追加（`test_note_standalone_markdown_edges.py` に6ケース）。既存の単独アプリ回帰テスト（`test_standalone_apps.py` 23件）・配布検査（`test_meldex_standalone_distribution.py`）・Cloud公開readiness（`test_meldex_cloud_public_release_readiness.py`）は全て継続グリーン。

## 未リリース (2026-07-18)

### fix(sheet,ui): 「列の表示と順序」の操作改善とドラッグ自動スクロール共通基盤の新設

- 「列の表示と順序」ダイアログのチェックボックスがクリックで切り替えられなくなっていた問題を修正した。原因は、モーダル共通の「チェックボックスをなぞって一括切替」機能が押した瞬間に先取りで切り替えた後、ブラウザ標準のクリック動作がもう一度切り替えて往復していたため。なぞり一括切替は従来どおり使える。
- 「列の表示と順序...」を列ヘッダーメニューの列操作サブメニューから外し、シートのツールバー（折返し・列幅自動調整の並び）へボタンとして移した。
- ダイアログの各行にマウスを乗せた時に軽くハイライトされるようにした。
- ダイアログの並べ替えを、ドラッグ中にマウスホイールが使えない旧方式（HTML5ドラッグ）からポインタ操作の自前実装へ変更した。これにより、ドラッグ中のホイールスクロールと、パネルの端を超えてもスクロールが止まらない挙動が使えるようになった。
- ドラッグ自動スクロールの共通基盤（gb-dnd-autoscroll.js）を新設した:
  - Meldex内のHTML5ドラッグ全般（フォルダツリー・タブ・カンバン等）で、端に近づくと自動スクロールし、端を超えても（少し外へ出ても）スクロールが続くようになった。
  - ポインタ操作の自前ドラッグ用APIを提供し、列ダイアログはドラッグ中のホイールスクロールにも対応した。シナリオの行ドラッグも端を超えるとスクロールが止まっていた点を補修した。
  - 制約: HTML5ドラッグはブラウザ仕様によりドラッグ中のホイールイベント自体が届かないため、ホイール対応が必要な場面は今後ポインタ実装へ順次移行する（インボックスに記録）。
- 制作管理パネルの共通ツールバー（列幅自動調整の隣）とスマホ用メニューにも「列の表示と順序」ボタンを配置した。シートの読み込み前や「すべて」表示中は他の表示操作と同様に無効表示になる。

検証: シート微細挙動E2Eを2ケース新設（①ダイアログ: クリック1回=1トグル・なぞり切替・ツールバー入口・ホバーCSS・ポインタ並べ替え+自動スクロールセッション ②自動スクロール基盤: 端接近・端超え継続・ドラッグ中ホイール・セッション終了をポインタ/HTML5両経路で検証）し安定合格。既存のモーダルチェックボックスなぞり切替E2E・制作管理シートの列保護E2Eもグリーン。静的回帰7件を新設。実ブラウザの隔離インスタンスで実クリック1回=1トグルとダイアログ表示を目視確認。

### fix(scenario,editor): ルビ設定ポップアップの操作統一とタイプ列ツールチップの抑止

- シナリオのタイプ列セルにマウスを乗せた時に出ていたツールチップを出さないようにした（タイプ名はセルにそのまま表示されているため冗長だった）。
- シナリオのテキストセルで文字列を選択した時に出るポップアップを改善した:
  - ルビのセクションをポップアップの一番上へ移動した。
  - ルビの入力欄の幅を少し狭め、「追加」ボタンの後で改行して「読み取得」「自動ルビルールにも追加」を2行目へ移した。
  - ポップアップが開いたらルビの入力欄へ自動でフォーカスが入り、そのままルビを入力してEnterで追加できるようにした（ドラッグ選択中・Shiftキーでの選択拡張中はフォーカスを奪わない）。
  - ポップアップ表示中にTabキーを押すと、ポップアップ内の項目（入力欄→追加→読み取得→…）へ順にフォーカスが移るようにした（Shift+Tabで逆順）。従来はTabでタイプ選択メニューが開いてしまっていた。
- 上記の操作（開いたら入力欄フォーカス・Tabで項目切り替え・入力欄の幅）を、Meldex内の他のルビ設定ポップアップにも揃えた: ノート本文のルビ設定、ページタイトルのルビ設定、単独シナリオアプリ／モバイル用のルビ専用ポップアップ（あわせて2行目の並びを「読み取得→ルール追加」へ統一）。

検証: シナリオ微細挙動E2E（ルビ挿入 PC/モバイル）とノート微細挙動E2E（本文/タイトルのルビ操作 PC/モバイル）に、最上段配置・2行構成・初期フォーカス・Tab切り替えの検証を追加してグリーン。実ブラウザの隔離インスタンスでタイプ列ツールチップ非表示・ルビ追加・保存値も目視確認。

### fix(locks): 編集中ロックの保存がDropbox同期と競合し、ファイルの移動・リネームが失敗することがある問題を修正

- フォルダツリーでファイルを移動・リネームする際、直前に行われる「編集中ロック」の取得・更新で、ロック管理ファイル（Dropbox上の `_meldex/active_locks.json`）の書き換えをDropbox同期が一時的に掴んでいると失敗し、操作全体が「移動に失敗（Internal Server Error）」になることがあった（数回に1回程度・少し待って再試行すると成功する）。書き換えに短い自動再試行（合計約3秒）を追加した。
- それでも競合が続く場合は、Internal Server Error ではなく「編集中ロックの保存が他のアプリ（Dropbox同期等）と競合しました。数秒後にもう一度お試しください」という理由の分かるエラーを返すようにした（取得・更新・解放のすべて）。
- 原因の特定には、同日導入したエラー詳細の永続記録（fix(debug) の項）が決め手になった。

検証: 回帰テスト4件新設（一時競合の再試行成功・打ち切り時の一時ファイル非残留・理由付きエラーへの変換・正常系）。既存の編集中ロック回帰10件グリーン。

### feat(scenario): ルビの字間を-2.0まで詰められるようにし、新規文書の初期値を-1.0へ変更

- ルビの字間の入力範囲を、従来の-0.9〜3.0から-2.0〜3.0へ広げた（B-MANGAと共通）。
- マイナスの字間は「親文字幅へ広げて配分されるルビの間隔を、中付きは中央へ・肩付きは先頭へ詰め寄せる」意味とし、-2.0でベタ組（ルビ文字同士が隣接）に到達する。ルビ文字同士が重なって潰れることはない（従来は-0.9で文字がほぼ重なるうえ、内部計算の下限により-0.9付近では操作しても見た目が変わらない場合があった）。
- 新規文書の初期値を0（広げ配分そのまま）から-1.0（広げ配分とベタ組の中間）へ変更した。保存済みの文書と、ルビ表示設定を持たない旧文書（旧見た目維持中のファイル）の見た目は変わらない。全体設定でルビの字間を一度でも保存している場合は、その保存値が引き続き優先される。
- B-MANGA側も同じ範囲・初期値・詰め寄せ挙動へ同時更新した（B-MANGA v0.6.552）。更新前のB-MANGAは-0.9より小さい字間の受信を拒否するため、両アプリを揃えて更新する必要がある。
- 徹底チェックで発見した併用バグを修正: 肩付きルビが隣の親文字に収まるよう自動圧縮（ベタ組以下へ短縮）された状態でマイナスの字間を併用すると、詰め寄せ処理が逆方向（拡大）に働いて収まりを壊していた。ベタ組以下へ詰まっている配置には詰め寄せを適用しないようにした（B-MANGA側も同修正）。

### fix(scenario): 横書きルビの覆いかぶさりを解消し、段間隔の設定とルビ一覧の縦幅を改善

- 横書きモードで、ルビが対象の文字列の上部と手前の文字列に覆いかぶさって表示される問題を修正した。ルビ表示設定を持たない既存ファイル（旧文書の見た目を維持中のファイル）でも、横書きではルビを本文のすぐ上に離して表示する。縦書きの見た目は従来どおり変わらない。
- オプションパネルの表示タブに「段間隔」を追加した。折返し表示で生じる段と段の間隔を、余白とは別に指定できる（未指定なら従来どおり余白と同じ間隔）。
- オプションパネルのルビタブで、ファイル内ルビの一覧がパネルの縦幅いっぱいまで表示されるようにした（従来は高さ200px固定で、パネルが縦に長くても一覧が小さいままだった）。

### feat(calendar): タスクリストに「すべて」タブを追加し、作品タブを並べ替え・閉じられるようにした

- タスクリストの作品切替タブの先頭に「すべて」を追加した。全作品のタスクリストを作品ごとの見出し付きで縦に並べて一括表示し、各表はその場で編集できる（タスクリスト追加やテンプレートのドロップも可能。ドロップ時は作品を選択）。
- 「すべて」表示中は、単一シート前提の「列幅自動調整・フィルタ・並べ替え・かんたん割当」を無効化し、作品別タブで使うよう案内を表示する。再計算は全表の選択行をまとめて対象にする。
- 作品タブをシートのビュータブと同じように、ドラッグ＆ドロップで並べ替えられるようにした（前後の挿入位置インジケーター付き。ホバーの「…」/右クリック/長押しメニューから「左と入れ替え」「右と入れ替え」も可能）。
- 同メニューの「タブを閉じる」でタブを非表示にできるようにした（タスクリスト本体のデータは削除しない）。閉じたタブは「＋」の隣のボタンから再表示できる。表示中のタブを閉じた場合は「すべて」表示へ切り替わる。
- タブの並び順と閉じた状態はブラウザごとの表示設定として保存する（別の端末・ブラウザには引き継がれない）。

### fix(calendar): スケジュール制作面でタブ列とツールバー右端ボタンの間に大きな隙間ができる問題を修正

- タブ折り返し時に余白がタブ列と右寄せスペーサーで折半され、タブ領域と右端ボタン群の間に不要な空白が生まれていた。制作面ではスペーサーを畳んでタブ列へ余白を全て渡し、タブが1行に収まりやすく・隙間が最小になるようにした（カレンダー面のタイトル配置は従来どおり）。

### fix(calendar): スケジュールのイベント連続追加でタイムアウトし予定が消える問題を恒久修正

- 原因1（サーバー）: スケジュールの追加・変更・削除のたびに、アプリ全体のデータベース（数GB）の控えを丸ごとコピーする処理が同期実行されており、さらに多重実行を防ぐ判定に隙間があったため、連続追加でコピーが同時に複数本走り、保存が15秒タイムアウトしていた。スケジュール書き込み時のこの控え作成を廃止し（共有用の複製が別途あるため。将来復活できる差し替え口は維持）、控え作成自体にも「実行中は多重起動しない」保護と実行中ファイルを考慮した間引き判定を追加した。
- 原因2（サーバー）: 書き込みのたびに共有用スナップショット（scheduler.snapshot.json）への全書き戻しを応答内で同期実行していた。約1.5秒のデバウンス付きバックグラウンド実行に変更し、書き戻しには多重書き込み防止のロックと失敗時の記録・自動再試行を追加した（従来は失敗が黙殺されていた）。取り込み（pull）はファイルが変わっていなければスキップするようにした。
- 原因3（画面）: 追加がタイムアウトすると画面上の予定を無条件に破棄していた。タイムアウト時は「サーバーに届いたか確認 → 届いていなければ同じIDで自動再送 → それでも失敗なら未保存マーク付きで画面に保持し、時間をおいて自動再試行」に変更した（同じIDの再送はサーバー側で更新扱いになるため二重登録されない）。
- 原因4（画面）: 予定一覧の再読み込みが全置換のため保存中の予定が消える問題と、アンドゥが保存未確定の仮IDをサーバーへ送ってしまう問題を修正した。
- サーバーの保存エラーは理由の分かる日本語メッセージ（混雑時: 「データベースが混雑しています。数秒後にもう一度お試しください」）を返すようにした。
- 回帰テスト: サーバー側4スイート（冪等再送・権限・混雑応答・ワーカー合流/再試行・排他/変更追跡・間引き）とカレンダー微細挙動E2E 5ケース（タイムアウト後の確認/再送/未保存保持/再読込保持/アンドゥ除外）を新設。

### feat(sheet): プロパティ型「ふりがな」を新設し、自動ルビの元になる列を列設定で明示できるようにした

- シートの列のプロパティ型に「ふりがな」を追加した。この型に設定した列の採用値が、自動リンク表示時のルビ（自動ルビ）として使われる。列名は自由（「読み」「ヨミ」など何でも可）。
- 互換動作: ふりがな型の列がないシートでは、従来どおり列名「ふりがな」「ルビ」「フリガナ」「ruby」のテキスト列がルビの元になる（ふりがな型の列がある場合はそちらが優先）。既存シートは無変更で動作する。
- 新規シートのテンプレート（キャラ・伏線・舞台など）の「ふりがな」列を、ふりがな型で作成するようにした。
- Cloud版（Dropbox直結）の辞書構築も同じ優先ルールに追従させた。
- 従来からあった内部の不整合（読み込み経路によって、採用値のない列名から次の列名候補へ進むかどうかが揃っていなかった）も解消した。
- 徹底チェックで検出した追随修正: ①列の型変更を保存した直後にルビ辞書を自動再読込し、アプリ再起動なしでルビ表示へ反映されるようにした ②ふりがな型の列でも、テキスト型と同じ候補値クイックリネームと複数行入力欄が使えるようにした。

### fix(scenario): 行ハイライトでハイライトされないセルがある問題を修正し、文字色も一緒に変わるようにした

- カーソルを合わせた行のハイライトで、前置文字（「 」など）が設定されたテキストセルだけ明るくならないことがある問題を修正した。
- ハイライト時に、セルの背景色だけでなく文字色も同じように明るく変わるようにした（2026-07-13 の「文字色は変えない」方針をユーザー指示により撤回）。

### fix(scenario): ルビ追加を文字列選択時の書式設定ポップアップへ統合

- 文字列を選択したときに表示される書式設定ポップアップへ「ルビ」入力行（入力欄・追加・読み取得・自動ルビルールにも追加）を組み込んだ。独立した「ルビを追加」ポップアップは廃止（メインアプリ）。
- Ctrl+R、Alt+↓、タイプ列メニューの「ルビ設定…」からもこの書式設定ポップアップが開き、ルビ入力欄へフォーカスする。
- ルビ入力中に Escape で閉じると本文セルへフォーカスが戻る。書式設定ポップアップの入力欄を操作中に、ポップアップが勝手に閉じ直されないようにした。
- 書式設定ポップアップが使えない画面（単独シナリオアプリ、スマホの編集画面）では、従来の「ルビを追加」ポップアップを引き続き表示する。

### fix(scenario): 見開き区切り線を行選択メニュー列に表示しないようにした

- 見開き区切り線が行左端の行選択メニュー列（チェックボックス・ドラッグハンドルの列）まで届いていたのをやめ、内容セルの範囲だけに表示するようにした。縦書きモード（行の右側の線）も同様。

### fix(scenario): 縦書きモードで寄せ方向の表記を縦書きに合わせて切り替えるようにした

- 書式設定ポップアップの寄せ設定は、横書きでは従来どおり「水平（左・中・右）」「垂直（上・中・下）」、縦書きでは実際の方向に合わせて「垂直（上・中・下）」「水平（右・中・左）」と表示するようにした。保存される設定値は横書き・縦書きで共通のまま（表記だけが切り替わる）。
- 列ヘッダーメニューの「水平」「垂直」サブメニューも同様に、縦書きでは表記と選択肢（上寄せ・中央・下寄せ / 右寄せ・中央・左寄せ）が切り替わる。

検証: 実ブラウザE2E 5ケース合格（ルビ挿入のPC/スマホ、寄せ設定の横書き/縦書き、列ヘッダーメニュー）。ホバーと見開き区切り線は実CSSを読み込む検証ページで計算スタイルと表示を確認（`_verify/2026-07-18_scriptnote_hover_spread/`）。関連回帰unittest（書式ポップアップ・シナリオ・分割資産・フロント回帰）グリーン。あわせて書式設定関連ファイルの配布コピー同期の既存赤（gb-format-popup ほか）を解消した。

### fix(debug): サーバー内部エラー（500）の原因が特定できない問題を解消（トレースバックの永続記録と理由表示）

- これまでサーバー内部エラーの詳細はサーバーのコンソール画面にしか出力されず、画面が失われると原因特定が不可能だった。エラーの詳細（発生箇所の記録付き）をログファイル（meldex-debug.log）へ自動追記するようにした。
- 操作失敗時の画面表示にも、エラーの種類と要約を含めるようにした（例:「◯◯の移動に失敗（サーバー内部エラー: …）」）。従来は「Internal Server Error」としか表示されなかった。

検証: 回帰テスト3件新設（記録内容・正常時の非記録・意図したエラー応答の非対象）。実アプリ構成での配線確認済み。

### fix(outliner): フォルダ作成・リネームがフォルダツリーに反映されない問題の残存原因を解消（タイムアウト時の結果確認と処理の高速化）

- 作成・リネーム・移動の通信が15秒でタイムアウトした場合でも、サーバー側で処理が完了していれば、その結果を自動で確認してフォルダツリーへ反映するようにした。従来は「失敗」と表示して元に戻してしまい、実際には成功しているため、後から別の操作でツリーが読み直された時に突然反映される「時間差反映」の原因になっていた。確認中は「結果を確認中…」の状態表示を出す。
- リネーム・移動を高速化した。従来はリネーム・移動のたびに親フォルダ（移動では移動元・移動先フォルダ）内の全ファイルを丸ごとバックアップコピーしており、大きなフォルダやDropbox上では数秒〜十数秒かかっていた。バックアップは対象ファイル1件のみに縮小した（フォルダのリネーム・移動は内容を変えない操作のためバックアップを省略。変更記録は監査ログに残る）。実測でDropbox上の大きいフォルダ（約640ファイル）のリネームが中央値2.1秒→0.4秒に短縮。
- リネーム・移動後のリンク書き換えのうち、保管庫全体の走査を伴う部分を応答後のバックグラウンド実行へ移し、保管庫が大きくても応答が遅くならないようにした（リンク索引に基づく書き換えは従来どおり即時実行）。
- 内部データベースが混雑してファイル索引の更新が一時的に失敗した場合に、実際にはリネーム済みなのにエラーを返していた問題を修正した。失敗時は自動で再試行し、応答は成功として返す（索引は裏で追い付かせる）。
- ノートのタイトル編集によるリネームが失敗した時、無言で元の名前に戻っていたのをやめ、失敗理由を表示するようにした。
- 閉じたフォルダの読み込みが失敗した時、エラーが一切表示されず、作成した項目がツリーに現れないままになることがある問題を修正した（理由を表示し、フォルダを開き直せば再読込される）。
- フォルダの一覧キャッシュ（最大3秒）を、作成・リネーム・移動・削除などの直後に即時無効化し、古い一覧が返る可能性を無くした。
- Cloud版: フォルダをまたぐ移動で、移動元フォルダの一覧に古い項目が数秒残ることがある問題を修正した。
- 徹底チェックで検出した競合3件を予防した: (1) リネーム直後にUndoや連続リネームを行うと、遅れて実行されるリンク書き換えの後追い処理が新しい状態を古い方向へ巻き戻し得る競合（世代照合と実体確認で中止）、(2) 索引の遅延再試行にも同様の競合（世代照合で中止）、(3) タイムアウト後の結果確認中に別のノートへ移動すると、確認完了時にエディタの編集対象パスがすり替わり得る競合（元のノートを開いている場合のみエディタ状態を更新）。

検証: 実ブラウザE2E 4ケースを新設（リネーム/作成のタイムアウト→事後確認成功、タイムアウト→実失敗時の理由表示、読み込み失敗の再試行）し全て合格。バックエンドの回帰テスト18件を新設（バックアップ縮小の仕様固定、データベースロック中のリネームが成功応答＋遅延索引更新になること、リンク書き換えの後追い実行、キャッシュ無効化）。既存のフォルダツリー・API・ファイルロック関連unittestすべてグリーン。性能計測は `_verify/2026-07-18_tree_reflection/` に記録。

### fix(outliner): フォルダツリーの作成・リネーム・移動・複製の応答パス不整合とエラー表示を修正

- ソースフォルダ配下（メインフォルダ以外のルート）でフォルダやファイルをリネームすると、応答のパス形式がツリーの保持形式（絶対パス）と食い違い、ツリー上のノードが実体とズレて以後の移動・表示が「見つかりません」で失敗することがあった問題を修正した。作成・リネーム・移動・複製・別名保存のすべてで、応答パスをリクエストのパス形式（絶対/相対）に合わせて返すようにした。
- ツリーでのドラッグ移動に失敗したとき「◯◯の移動に失敗」としか表示されず原因が分からなかった問題を修正し、サーバーからの失敗理由（移動先が見つからない・使用中・ロック中など）を併記するようにした。ツリーの名前変更が失敗したときも、これまで無言で元の名前に戻っていたのをやめ、失敗理由を表示するようにした。
- Dropbox同期やウイルススキャンが一時的にファイルを掴んでいてリネーム・移動が失敗する場合に備え、短い待機付きの自動再試行を追加した。それでも失敗する場合は原因の分かるメッセージを返す。
- シナリオ等の複合拡張子ファイル（`.scriptnote.json` など）を複製・別名保存・移動（名前衝突時）すると、拡張子の途中に `_copy` や連番が入って種別が壊れる問題を修正した（例: `第1話.scriptnote_copy.json` → `第1話_copy.scriptnote.json`）。

検証: 新設の回帰テスト11件（パス形式・複合拡張子・連番位置）と、既存のAPIスモーク・アウトライナー回帰・ファイルロック・制作管理保護・ルーター整合の各unittestすべてグリーン。

### fix(scenario): タイプ管理の書式設定「水平・垂直・折返」がエディタ本体へ反映されない問題を修正

- タイプ管理の各セルおよび列ヘッダー（列一括設定）の書式設定ポップアップで設定した「水平」「垂直」「折返」が、テキスト列以外（タイプ列・カスタム列など）のエディタ表示にまったく反映されなかった問題を修正した。設定は保存だけされて表示に使われていなかった。
- 優先順位を「タイプ別の設定 ＞ 列ヘッダーメニューの列全体設定」に統一し、「自動」を選ぶと列全体の設定に従うようにした。
- テキスト列で、タイプ側が未設定のときに列ヘッダーメニューの「水平・垂直・テキスト」設定が打ち消されてしまう逆の問題も修正した。
- タイプ列・大区切り/小区切り列・カスタム列にも折返（折返し・はみ出し・切り詰め）の表示ルールを追加し、横書き・縦書きの両方に対応した。

検証: 実ブラウザE2E 2ケース（横書き・縦書き）を新設し、タイプ管理ポップアップ→エディタ反映（テキスト・タイプ・カスタム列、列一括、列設定へのフォールバック）を確認して合格。関連unittest（micro登録検査、シナリオエディタ回帰、エントリポイント検査、バージョン整合）グリーン。

### fix(search): セマンティック検索の索引エラー表示の健全化

- 索引の再構築・更新が成功したとき、過去の失敗理由の記録を自動で消すようにした（これまでは復旧後も古いエラー文言が残り続けていた）。
- 検索用モデルの読み込みに失敗したときのメッセージに、モデルフォルダの場所を含めるようにした。Dropboxの「オンラインのみ」状態が原因と推定できる失敗では、「該当フォルダを『オフラインで利用可』に固定してください」という対処案内を添える。
- 検索索引の網羅状況API（/api/rag/coverage）の応答に、セマンティック検索の失敗理由を追加した。

### fix(save): 保存が常時2〜3秒かかり断続的に15秒タイムアウトする問題を解消（検索索引更新の非同期化）

- ファイル保存が全文検索・セマンティック検索の索引更新を同期で待っていたのをやめ、バックグラウンドの更新キューに変更した。同じファイルを続けて保存した場合は最後の1回だけ索引化する。肥大した索引や起動直後の索引再構築・検索対象フォルダ変更時の索引全消去と重なっても、保存の応答がブロックされなくなり、「保存に失敗しています」の頻発が解消される。
- 検索（全文・セマンティック・ハイブリッド・連想）の実行直前に残っているキューを処理してから検索するため、保存直後の検索でも新しい内容がヒットする。アプリ終了時も残キューを処理してから終了する。

### perf(search): 全文検索索引の肥大対策（自動最適化・無変更保存のスキップ・保守手段）

- 索引の再構築が完了した直後と、保存による増分更新200回ごとに、索引の断片化を自動で解消するようにした（書き込みを長時間占有しない小刻みな統合方式）。
- ファイルの内容が変わっていない保存では索引の書き換えを省略するようにした（同一内容の上書き保存が軽くなる）。
- 索引の最適化とデータベースの空き領域回収（VACUUM）をまとめて実行する保守用API（オーナー専用・バックグラウンド実行・実行状態の確認付き）を追加した。

### fix(scenario): タイプ選択ポップアップを常に全件フラット表示に変更

- タイプ数が20を超えると「タイプ」「プロット」「区切り」のサブメニューに括られていたのを廃止し、件数に関わらず全タイプを1つのリストで表示するようにした。リストは従来どおり上限高さでスクロールし、上下キー（Shift+上下で5件移動）・Tab・文字入力での自由入力もそのまま使える。

### fix(scenario): ステータスバーのショートカット一覧を最新仕様に同期

- シナリオ表示中にステータスバー右側へ出るショートカット一覧へ「Ctrl+Enter 同タイプ行追加」「Tab タイプ選択」「Ctrl+Shift+V セル内貼付」「Ctrl+A 全選択」「Ctrl+D 選択解除」を追加した。「上へ/下へ」は行の移動であることが分かる表記（行を上へ/行を下へ）に改め、「改行」は「セル内改行」とした。

### feat(scenario): シナリオ上のツールチップ廃止とTabキーのタイプ選択

- シナリオの行の上（ドラッグハンドル・行選択・タイプ・採用状況・カスタム列・ヘッダーの行選択メニュー）でツールチップを表示しないようにした。スクリーンリーダー向けの読み上げ名は維持している。
- シナリオ上でTabキーを押すと、その行のタイプ選択ポップアップが開くようにした。テキスト列の編集中に加えて、タイプ列にフォーカスがある時、セル移動モード、カスタム列（数値・選択肢・テキスト）の編集中でも同じ動作になる。Shift+Tabは従来どおり前のセルへ移動する。

### fix(scenario): タイプ管理の「全行に適用」が反映されない回帰を修正

- 列ヘッダーの書式設定ポップアップで「全行に適用」にチェックを入れて編集しても反映されない問題を修正した。元に戻す用の記録処理が空の適用ルールを整理した直後に内部エラーが起き、画面更新と保存が止まっていた。
- 「全行に適用」で設定したルールが新規タイプの追加時（＋追加・DB読込・行のタイプ入力からの自動登録）に反映されなくなっていた欠落も修正した。

### feat(scenario): タイプ管理の書式コピー/貼り付けとUI改善

- 書式設定ポップアップ（セルの書式・列ヘッダーの一括書式）に「コピー」「貼り付け」ボタンを追加した。表示中の書式一式をコピーし、別のセルや列一括へ貼り付けられる。貼り付けは1回の「元に戻す」で取り消せる。
- タイプ管理の各行のチェックボックスが、行にカーソルを載せてもほとんど見えなかったのを、枠線を明るくして視認できるようにした。
- （なし）行のチェックボックスを（デフォルト）行と同様にグレーアウトし、全選択の対象からも外した（複製・削除できない特殊行のため）。
- オプション設定ポップアップの幅を従来の半分にし、説明文は折り返して表示するようにした。

### feat(scenario): タイプ管理の（なし）行にオプション設定を追加

- タイプ管理の（なし）行（タイプ未設定の行に適用される設定）にもオプション列のボタンを表示し、インデント・テキスト表示位置・大区切り/小区切りの個別設定・見出し枠線を設定できるようにした。ページ採番の対象外である区切り・見開き・プロットのチェックは（なし）行では表示しない。複製・削除は無効のまま表示する。
- （なし）行の大区切り・小区切りの背景色の初期値を透明にした。タイプ未設定の行の区切りセルは、個別に色を設定しない限り透明で表示される。

### feat(scenario): 矩形選択と表計算ソフトのコピー＆ペーストに対応

- 複数行・複数列をセル単位の矩形として選択し、パネルの表示端へ近づけるとその方向へ自動スクロールして選択を続けられるようにした。ドラッグ中のマウスホイールにも対応し、横書き／縦書きと折返し状態ごとのスクロール方向を維持する。
- 選択範囲をTSVとHTML表の両形式でコピーし、ExcelやGoogleスプレッドシートへ行・列構成を保ったまま貼り付けられるようにした。表計算ソフトからの貼り付けも、テキスト・タイプ・状態・カスタム列へ表示順どおり反映する。
- タブ、セル内改行、引用符を含む表データの往復、数値列への型変換、表示行を超える貼り付け時の行追加、フィルター中は表示中の行だけを対象にする処理を追加した。区切り表示列はコピー対象だが読み取り専用とし、貼り付けで書き換えない。
- 従来のセル内テキスト選択、タイプ列選択、チェックボックスによる行選択、複数行テキスト貼り付け、Ctrl+Shift+Vのセル内貼り付けを維持した。矩形選択中のDelete／Backspaceは行ではなくセル内容だけを1回のUndo単位で消去する。

検証: シナリオ関連unittest 88件、独立JavaScript 4ファイルの構文検査、Chrome実機E2Eで3行×3列のコピー、引用符付き改行・タブを含む表の往復、ドラッグ中ホイール、パネル下端自動スクロール、従来の選択・複数行貼り付け・セル内貼り付け・4表示状態のスクロールを合格。

### fix(scenario): 縦書き切替とタイプ別区切り書式を整合

- 縦書きへ切り替えた直後、ルビ表示モデルだけ横書きのまま残り、次に「折返し」または「まとめ表示」を切り替えると横書きへ戻る回帰を修正した。書字方向を本文とルビで同時更新し、保存、Undo／Redo、テンプレート適用後も一致させた。
- 「表示」タブの大区切り／小区切り設定を全体既定として維持しながら、「タイプ管理」の各タイプの「オプション」から個別書式を設定できるようにした。タイプ別に設定した項目を全体既定より優先し、リセットすると全体設定へ戻る。
- 「（デフォルト）」の小区切り背景を明度20%（`#333333`）にし、「＋追加」やDB取込でタイプ／テキストと一緒に複製する。全体設定、タイプ別設定、「（デフォルト）」「（なし）」、書字方向をテンプレートで保存・復元する。
- 旧シナリオのタイプ別区切り書式を削除せず、タイプ間で異なる値は個別設定として保持する。直前版が互換用に全タイプへ複製した全体設定だけを重複上書きから除外する。
- 折返しで複数段になったヘッダーの検証用識別子を段ごとに一意化し、縦書き4状態と連続切替のブラウザ回帰検査を安定して完走できるようにした。

検証: 関連unittest 80件、Chrome実機E2Eで横書き／縦書き×折返しON/OFF、縦書き→折返し→まとめ表示の連続切替、全体／タイプ別区切りの優先順位、追加タイプへの複製、テンプレート保存・復元を合格。タイプ別設定は1400×900 PC、390×844スマホ、834×1194タブレット縦でも確認した。

### fix(schedule): 制作管理の列型と狭幅UIを整合

- 制作管理13シート・155列の用途とプロパティ型を監査し、作品、タスク、テンプレート、スケジュール、勤怠などの担当者列をすべて「ユーザー」型へ統一した。複数候補は「複数ユーザー」、複数参照は複数リレーション、カレンダー接続先はURL型とし、各列の入力意図に合わせた。
- DesktopとCloudの正規スキーマを同一定義に揃え、既存シートは入力値や独自列を消さずに型だけを修復するようにした。スタッフ追加の割当先もワークスペースユーザーから選択する。
- スケジュールの7タブは狭いパネルで右端を隠さず、横スクロールなしでツールバー内へ折り返すようにした。
- 共通ダイアログと旧式ダイアログの右上閉じるボタンを32px、アイコンを20px基準に統一し、視認性を上げた。

### feat(font/scenario): OSフォントとタイプ初期設定を一元化

- Meldex本体と5つの単体アプリで、OSへ登録されているフォントファミリーを共通カタログから選べるようにした。DesktopはローカルAPIでOS登録情報とフォントファイルを列挙し、Cloudは対応ブラウザのローカルフォント権限を利用する。
- シナリオの従来の「（デフォルト）」を、空欄タイプ用の「（なし）」へ改名した。名前のある新規タイプ用に独立した「（デフォルト）」を追加し、「＋追加」、行への直接入力、DB取込のすべてでその設定を複製する。
- 新しい「（デフォルト）」のタイプ／テキスト背景と、小区切りの背景を、HSL 0°・0%・20%（`#333333`）にした。
- 大区切り／小区切りを書式設定ごと「表示」タブへ移し、シナリオファイル内の全行へ適用する一元設定にした。区切りの単位、桁数、前後位置も同じ保存経路へ統合した。
- テンプレートで「（デフォルト）」「（なし）」、区切り一元書式、区切り表示値を保存・復元する。旧タイプ別区切り設定は読込時に一元設定へ移し、保存時は旧版でも見た目を維持できる互換値を残す。
- 徹底チェックで、Undo取得時に既定タイプ／区切り書式の参照が切れる問題、空の区切り設定が保存前に消える問題、タイプ候補のキーボード順と全選択範囲、Cloud静的E2Eが起動前にランナーを読み込めない検証基盤の順序不具合を修正した。

検証: 関連unittest 114件＋監査台帳完全一致1件、Python 4ファイル／独立JavaScript 7ファイルの構文検査、Chrome実機E2Eで1400×900 PC・390×844スマホ・834×1194タブレット縦・1194×834タブレット横、Edge Playwrightで同4画面の画像確認、Cloud静的860ファイルのビルド・成果物検証とChrome上のフォント受け入れ／シナリオ設定E2Eを合格。検証PCではOS登録フォント322ファミリーを重複なく取得した。

### fix(startup): 一時的な読み取り遅延を通信障害として表示しない

- 起動時やフォルダ読込時のGET通信がタイムアウトまたは画面遷移で中断された場合、処理側へ中断を返しつつ、右下の「通信に失敗しました」は表示しないようにした。
- 完全な通信断、HTTPエラー、保存・更新中の中断では、診断通知と保存失敗の案内を従来どおり表示する。`silentError`指定時の静音動作も維持した。
- 理由付きのGETキャンセルが通常エラーへ変化して通信障害通知になる境界不具合を修正し、標準の中断エラーへ統一した。

検証: 起動・読み込み・診断・保存安全性の関連unittest 96件、Chrome実機E2Eで360px／390px／430pxスマホ、834pxタブレット縦、1194pxタブレット横、1400pxデスクトップの通信7分岐、Cloud静的ビルド・成果物検証、秘密情報パターン検査、`git diff --check` PASS。

### feat(import): Web Clipper設定を復旧し、Xアカウント保存を追加

- 分割設定ナビゲーションを配布物へ収集できていなかったビルド処理を修正し、設定の **インポート** に **Web Clipper** と **Xブックマーク** が表示されない問題を解消した。
- 設定の **インポート** に **Xアカウント保存** を追加し、指定したユーザー名またはプロフィールURLから、返信・リポストを含む取得可能な全ポストをアカウント別シートへ保存できるようにした。
- Xの既存OAuth接続を再利用し、保存済みポストの更新、画像保存、ページ送り、取得上限・レート制限の案内に対応した。X APIの仕様上、取得対象は最新3,200件までとなる。
- 徹底チェックで、接続状態の再確認が保存中フラグを解除する競合、同一アカウントの多重実行、同じページトークンの反復、通常15秒での早期タイムアウトを修正した。入力が有効かつX接続済みの場合だけ保存を開始し、最大5分待機する。
- 取得件数がちょうど上限と一致しても続きが無ければ上限警告を出さず、レート制限で途中停止した場合は保存済み件数と再実行目安を表示し、部分保存後もメディア参照の後処理を完了するようにした。
- Xアカウント保存APIは通常のローカルAPIトークンとユーザー認証を必須とし、Web Clipper拡張機能へ許可したトークンなし通信からは呼び出せないことを実通信テストで確認した。
- Chrome拡張機能とX保存の設定手順を、現行の **設定 > インポート** 構成へ更新した。

検証: Xアカウント保存・既存Xブックマーク・認証保護・設定表示・ビルド収集の関連unittest 212件、Python/JavaScript構文検査、Chrome実機E2E `targeted-settings-dialog-all-tabs-visible`（無効ユーザー名・XプロフィールURL入力を含む）、秘密情報パターン検査、`git diff --check` PASS。

## v0.6.191 (2026-07-16)

### fix: スケジュール制作管理のスキーマと更新整合性を確定

- アプリのユーザー向け名称を「スケジューラー」から「スケジュール」へ統一した。
- 制作管理シートの先頭列をタスク名、作品名、作業対象名、作業内容名、作業規模名、スタッフ名へ整理した。作品・作業対象・作業内容・作業規模の重複名称列はエントリ名へ集約し、スタッフの割当ユーザーは別の `スタッフ` ユーザー列で管理する。
- 制作管理シートの必須プロパティ列は、並べ替え・列幅調整・ソート・フィルタ・非表示を維持したまま、削除、列名変更、プロパティ型変更、型設定変更を禁止した。画面操作、Redo、直接呼び出し、Desktop／Cloud保存層、汎用ファイル保存の各経路で拒否する。
- 共通ツールバーから重複タイトルを撤去し、タスク一括作成、かんたん割当、再計算、管理操作を右寄せした。列幅自動調整、フィルタ、並べ替えは表示中の制作管理シートだけを操作する。
- カレンダー、タスクリスト、作品設定、作業対象、作業内容、作業規模、スタッフを同じタブ列へ並べ、選択中のタブをアクセント色・背景・下線・ARIAで一意に示すようにした。
- スタッフ表示名と割当に使うユーザーIDを分離し、空のユーザー値は未割当のまま扱う。同じユーザーIDを複数スタッフへ登録する操作は変更前に拒否する。
- スタッフ追加、タスク更新、通常／均等再計算、タスク作成、シフト取込、かんたん割当、固定切替で、Markdown、カレンダーイベント、制作索引を同じ成功単位にし、途中失敗時は新規シート定義・キャッシュ・検索索引を含めてロールバックするようにした。
- Cloud版へ作品別タスクの取得・更新・テンプレート作成・横断検索APIを追加し、ファイル単位のリースと補償ロールバックで端末間の同時更新を保護した。閲覧専用時は移行や管理ファイル更新を行わず、選択・コピー・スクロール・列幅・フィルタ・並べ替え・非表示・列順だけをローカル操作として維持する。
- 埋め込みシートの状態をペインごとに分離し、複数のスケジュールタブや分割表示を同時に開いても、別シートの選択・表示設定へ混線しないようにした。編集ロック、容量超過、閲覧専用の理由も画面に表示する。
- 旧タスクのIDが空でも同名・同内容なら再移行時に重複させず、同名でも内容が異なるタスクは保全する。破損した1ファイルは記録して読み飛ばし、横断一覧全体を止めない。作品情報の二重読込とカレンダー復旧の二乗走査も解消した。
- タスク削除後のカレンダー同期だけが失敗した場合は、削除成功を維持しつつ「スケジュールを再読み込み」と案内する。モバイルでは主要操作を44px以上、入力文字を16px以上とし、サイドバータブのキーボード移動とARIA状態を統一した。

### 検証

- 制作管理の主要回帰243件と、名称移行、安全性、UI、シート、ランタイム、Cloud、パス移設、API作成を含む周辺回帰205件、合計448件を全件合格させた。
- 故障注入を含む原子性テスト9件、編集ロック回帰10件、制作管理フロント／再計算UI回帰29件を合格させた。
- split/bundle生成、Python 54ファイルとJavaScript 29ファイルの構文検査、差分検査を合格させた。
- 隔離した実サーバーの制作管理micro E2Eを完走し、1400×900、1024×768、390×844で単一ツールバー、タブ順・選択状態、モバイル操作、横スクロールを確認した。一括作成ダイアログも、19ページ×5コマ×5工程＝最大475件の既定値を実ブラウザで確認した。

## v0.6.190 (2026-07-16)

### fix: B-MANGAとルビ表示・設定を同一仕様へ統一

- B-MANGAのJIS系グループルビ配置、モノルビ／熟語ルビ配置、行内衝突回避、字間圧縮をMeldexへ移植し、同じ設定なら本文に対する位置、距離、字間、ルビ行間、配置が同じになるようにした。
- 両アプリの設定を、書字方向、サイズ、本文との間隔、字間、ルビ行間、配置、小書き、フォント、ルビ種類の9項目へ統一し、初期値・選択肢・入力範囲も揃えた。
- ルビを半透明にする独自表示を廃止し、論理フォントの代替順、親文字基準の間隔計算、既定ルビ種類の保存とB-MANGA連携を共通化した。総ルビ機能は追加していない。
- 単体テスト、B-MANGA連携テスト、版番号テストに加え、実シナリオファイルを実ブラウザで横書き／縦書き表示し、短い日本語、長いルビ、隣接ルビ、欧文、小書きと設定画面をAI目視確認した。

## v0.6.189 (2026-07-16)

### feat: ページ・コマ・工程から制作タスクを一括作成

- 制作管理の共通ツールバーに「タスクを一括作成」を追加し、作品、ページ数、1ページのコマ数、作業内容、作業対象、作業規模をダイアログで指定できるようにした。
- マンガ用の既定値として、ページ0001〜0019、各ページのコマ01〜05、各コマの「ネーム・下描き・3D配置・ペン入れ・仕上げ」を用意した。既定値では作成前に `19ページ × 5コマ × 5工程 = 最大475件` と明示する。
- 作品ごとのタスクリストへ不足分だけを追加し、同じ条件で再実行しても重複を作らない。途中失敗時も再実行すれば不足分から再開し、既存タスクの担当者・期限・状態・本文などは上書きしない。
- 作品ごとのページ数とコマ数を保存して次回のダイアログへ復元し、3コマ作品を再度開いた時に既定5コマへ戻って余分なタスクが増えることを防いだ。
- 既存作品から新しい作品名へ切り替えた時は、未編集なら19ページ／5コマへ安全に戻し、手動編集済みの値は維持する。作成後の対象シート読込も1回に抑え、大量タスクでの待ち時間とちらつきを防いだ。
- Desktop／Cloudの両方に対応し、Cloudでは同一作品の端末間ロック、最大4件の並列保存、Dropbox競合コピーの照合、完了状態の記録を追加した。旧共通タスクリストは初期化時に作品別シートへ安全に移し、ID・全プロパティ・本文・予定との関連を維持する。
- 5,000件を超える指定、空の階層、取得や保存に失敗した状態では処理を開始せず、入力修正または再実行で復旧できるメッセージを表示する。

### fix: 制作管理のタブ・操作・必須列を共通ルールへ統一

- カレンダー、タスクリスト、作品設定、作業対象、作業内容、作業規模、スタッフを上段の共通タブへ並べ、選択中のタブをアクセント色・背景・下線・ARIAで一意に示すようにした。横幅が足りない場合は表示中タブが見える位置へ自動で移動する。
- 制作タスクリストでは共通ツールバーを1本だけ表示し、「かんたん割当」とタスク一括作成を表の上へ常設せずダイアログで開く。重複していた管理リストや詳細編集も中央のシートへ集約した。
- 制作管理シートの必須プロパティ列は、並べ替え・ソート・非表示を維持したまま削除だけを禁止した。画面操作、Redo、直接呼び出し、Desktop／Cloud保存層のすべてで削除を拒否し、旧版で削除扱いになった列は表示設定を保ったまま復旧する。

### 検証

- Desktopで既定475件の作成、先頭／末尾の階層名、一意性、作品別保存、再実行時の追加0件、既存編集の維持、無効値・上限値を自動テストで確認した。
- Cloudで旧データ移行、同名競合、端末間ロック、部分失敗からの再開、Dropbox競合コピー、書き込み上限、作業時間の倍率計算を故障注入テストで確認した。
- 制作管理の回帰テストとChrome実ブラウザE2EをDesktop／Tablet／Phoneで実行し、単一ダイアログ、フォーカス移動、Escape、タッチ領域、475件作成後の作品別タスクリスト表示、再実行時の重複なしを確認した。
- 制作管理に直接関係するテスト213件＋7サブテスト、監査・split/bundle・版番号テスト101件を合格させ、実ブラウザ3画面の操作カバレッジと最終画像も確認した。

## v0.6.188 (2026-07-15)

### feat: 制作タスクリストを大分類（作品）別のシート直接編集へ再設計

- 大分類＝作品とし、タスクリストを大分類ごとに独立したシートへ分割した。スケジューラーのタスクリスト面に切替バーを設け、リスト間は1クリックで移動、「＋」からダイアログなしでインライン追加できる。
- タスク一覧を閲覧専用の表からシートと同じ表画面へ置き換え、セルの直接編集、チェックボックスによる複数選択と一括編集、行の追加・削除、列見出しからの並び替え・グループ化・列名変更、条件フィルタ、保存ビュー、大量行の仮想スクロールをタスクリスト上でそのまま使えるようにした。
- 列見出しから分類列の名前を変えると作品の分類名設定にも自動で反映され、以後に作成するタスクも新しい分類名で記録されるようにした（マンガ向けの「ページ・コマ」等への変更に対応）。
- 作業対象・作業内容・作業規模・スタッフの各リストへ切替バーから1クリックで到達し、シートとして直接編集できるようにした。右パネルの一覧の縦幅固定も撤廃し、行数に追従するようにした。
- 再計算を強化し、複数のタスクリストをまとめて対象にする選択と、一覧でチェックした選択タスクだけを対象にする切替を追加した。
- タスク詳細の作業対象・作業内容・作業規模・担当者を、各リスト連動のドロップダウンへ変更した。テンプレートはタスクリスト表へのドラッグ＆ドロップ追加にも対応した。
- 既存の制作タスクは初回アクセス時に自動で大分類（作品）別リストへ移行し、元データはバックアップフォルダとして保全する。**注意: v0.6.187以前へ戻した場合、新形式のタスクリストは画面に表示されない（データ自体はファイルとして残る）。**

### fix: シート経由の編集がカレンダー予定へ同期されない問題ほかを修正

- タスクをシートの表から直接編集・削除・改名した際に、カレンダー面の予定が古いまま残る／孤児予定が残り続ける問題を根本修正した（保存・削除・自動改名のすべてでカレンダーへ同期）。
- タスクリスト内の表・列メニューの操作が、別タブで開いている無関係なシートへ誤って作用しうる問題を修正した。
- データ移行直後に復旧機能が誤作動し、予定付きタスクの薄い複製が作られる問題を修正した。
- 同名に丸められる作品名（大文字小文字・記号違い）で別作品のタスクが同じリストへ混ざる問題、再計算プレビューの古い応答で適用できてしまう問題、管理リスト表示中の再計算に無関係な選択が送られる問題などを徹底チェックで検出し修正した。

### fix: 制作管理画面の重複表示と操作配置を整理

- スケジューラー本体と埋め込みシートがそれぞれツールバーを表示する経路を解消し、制作タスクリストでは共通ツールバー1本だけを表示するようにした。
- 表の上へ常設していた「かんたん割当」をダイアログへ移し、Escape・背景クリック・閉じるボタン、フォーカストラップと終了後のフォーカス復帰に対応した。
- 作品・作業対象・作業内容・作業規模・スタッフの編集場所を中央のシートへ集約し、タスクリスト面の右サイドバーから重複する管理リストとタスク詳細を撤去した。右側にはカレンダー上で選んだタスクの詳細、テンプレート、表編集では行えない管理操作だけを残した。
- スマホでは制作タスクリスト専用の操作メニューと右サイドバー用ドロワーへ整理し、主要操作を44px以上のタッチ領域で扱えるようにした。

### 検証

- 制作管理のPython回帰161件、フロントエンド契約・実行テスト、シート描画回帰を全緑で確認した。
- Chrome実ブラウザで、制作タスクリストE2E 4ケース（Desktop/Mobile）を全件合格させ、旧形式データの自動移行・セル編集のカレンダー追従・行削除の予定消滅・複数リスト横断の再計算・新規リスト作成を隔離環境の実サーバーで確認した。
- 画面整理後の関連静的テスト、Desktop/Mobileの制作タスクリストと右サイドバーのChrome E2Eを全件合格させ、タスクリスト・再計算・かんたん割当の最終画像を目視確認した。
- 全体テスト2,887件を実行し、失敗は本変更と無関係の既存1件のみ（記録済み）、スキップ数は従来と同一であることを確認した。
- 既知の制限: タスクリスト表でのセル編集はCtrl+Zで戻せない場合がある（追跡中）。

## v0.6.187 (2026-07-15)

### feat: スケジューラーに制作タスクリストを統合

- スケジューラーのメイン領域を「カレンダー」と「タスクリスト」で切り替えられるようにし、複数作品の制作タスクをシート形式で同時に確認できるようにした。
- 検索、作品・状況・担当者の絞り込み、多段ソート、「作品 → 大分類 → 中分類 → 小分類」の任意階層でのグループ化に対応した。分類名は作品ごとに変更でき、マンガ以外の制作にも汎用的に使える。
- 右側の制作管理パネルを、選択タスクの詳細、タスクテンプレート、管理設定へ整理した。テンプレートはタスクリストやカレンダーへドラッグして挿入でき、タッチ操作向けの追加・配置ボタンも利用できる。
- タスク数と締切日時だけで、現在のユーザーへ15分単位の作業枠を均等配分できる簡易割当を追加した。登録済みシフト、標準勤務時間、平日9:00〜18:00の一時前提を順に使い、自動補完した前提と時間不足の解決策を画面に表示する。
- 再計算時は完了・着手中・固定済みのタスクを維持し、休憩などで分割された作業区間をMarkdownとカレンダーの両方へ保存・復元できるようにした。

### fix: 大量データ・同時操作・保存失敗時の安全性を強化

- 1,000件を超える制作タスクを全ページ取得し、一覧の残り高さへ追従するスクロール、固定列見出し、狭幅時の詳細ドロワーで大量データを扱えるようにした。
- 詳細読込・保存、テンプレート一覧・保存、全体再読込が重なった時に、古い応答で現在の選択や表示が巻き戻らないようにした。詳細編集中は入力を保持し、変更した項目だけを保存して、再計算後の担当者・時間を古い表示値で上書きしない。カレンダー再読込だけが失敗した場合も、保存自体の成功と分けて案内する。
- 数値の目標時間、旧形式の作品名、空欄を含む並べ替えを正しく扱い、テンプレートの配置失敗時には入力中のフォームを保持するようにした。
- 重複する勤務枠の二重計上、再計算で更新対象外のファイルを指定できる問題、復旧後に固定状態が失われる問題を修正した。
- 詳細編集、簡易割当、従来の再計算、固定操作、テンプレート挿入など、制作管理の書き込みをワークスペース単位で直列化した。同時操作で片方の更新が消えたり作品・タスクが二重作成されたりすることを防ぎ、Markdown・カレンダーDBの途中失敗時には作成・更新内容をロールバックするようにした。

### fix: 徹底チェックで検出した編集消失・巻き戻り・整合性の問題を修正

- タスク詳細に未保存の編集がある状態で、別のタスク・カレンダーの別予定・別タブへ移動する時は、破棄してよいか確認するようにした。保存ボタンを押した後の移動は、保存を続けたまま警告なしで移動できる。
- タスク詳細と分類名の保存中は入力欄を一時的に無効化し、応答が返る前の追加入力が消えたり、入力済みに見えるのに保存されない状態になったりしないようにした。
- タスク詳細の保存の後始末をカレンダー再読込を待たずに行うようにし、分類名の保存と重なった時に、外部から届いた最新のタスク内容が反映されず破棄される問題を修正した。
- タスクリスト画面が自動同期などの再描画のたびに作り直され、スクロール位置・かんたん割当へ入力途中の値・グループの開閉状態が失われる問題を修正した。
- 選択中のタスクが絞り込みで一覧から外れても、右側のタスク詳細は最新の内容へ同期し続けるようにした。
- 読み込み中にタスクの増減があった場合は自動で読み直し、追い切れない場合は警告を表示して、一覧から行が黙って欠落しないようにした。
- グループ表示の並び順を名称の自然順(未分類は最後)へ統一し、並べ替え条件に引きずられて順序が不規則になる問題を修正した。中間の分類だけが空のタスクで選択が解除される問題も修正した。
- かんたん割当で作品を指定した場合は入力した締切を計画の基準とし、過去の締切が残った既存タスクのせいで割当全体が原因不明のエラーになる問題を修正した。タスク数の指定で対象外になる既存タスクがある場合は件数を表示し、終了時刻のない勤務シフトを除外した場合も警告を表示するようにした。
- テンプレートをカレンダーへ配置する時に作品を変更した場合、変更前の作品の分類を新しいタスクへ引き継がないようにした。
- 外部カレンダーへの送信中に、同じワークスペースの他の操作(タスク編集・割当など)が待たされないようにした。
- タスクの一括作成が途中で失敗した場合、作成済みの分を巻き戻してから失敗を知らせるようにした。不正な作業時間帯データで既存のカレンダー予定だけが消える経路も、エラーで停止して巻き戻すようにした。
- 「30分」「1時間30分」のような目標作業時間を、時間の長さとして正しく並べ替えるようにした(サーバー側)。

### 検証

- 制作管理のPython回帰、フロントエンド契約・実行テスト、カレンダーパネル回帰、JavaScript構文検査を実施した。
- Chrome実ブラウザで、Desktop / Mobileの制作タスクリストE2E、360 / 480 / 720 / 1487px幅、複数の高さ、キーボード操作、詳細・テンプレート・簡易割当の主要経路を確認した。
- 徹底チェック(変更範囲の並行精密監査)を実施し、検出した問題の修正後に、制作管理の実ブラウザE2E全7ケース(Desktop/Mobile)と関連回帰テスト88件の全件合格を確認した。

## v0.6.186 (2026-07-14)

### feat: Meldex／B-MANGAのルビ表示と連携契約を全面統一

- フルMeldexと単独版Meldex Scenarioで、ルビのサイズ、親文字との間隔、字間、行間、配置方法、小書き仮名、論理フォント、ルビ種類を同じ名称・順序・既定値で設定できるようにした。新規既定は50%、間隔0、字間0、行間1.8、中付き、小書き保持、本文と同じフォント、グループルビ。
- 文書ごとのルビ設定、一覧、自動ルビルール、追加・編集・解除・全解除を単独版にも搭載し、設定変更をUndo/Redo、保存、再読込へ含めた。既存文書の55%・3.5px等は従来の見た目を保つ。
- B-MANGA連携v2を追加し、能力確認後に書字方向、共通表示設定、ルビの由来・優先順位・種類を送信する。B-MANGAがv2非対応の場合は自動でv1へ戻し、従来どおり範囲・読み・groupだけを送る。
- 文書の既定ルビ種類を手動ルビと自動ルビへ反映し、同一範囲では手動ルビを優先する。OS固有フォントパスは送らず論理プリセット名だけを送る。
- シナリオ文書へ任意の `rubyPresentation` を追加したため、`schema_version` を2へ更新した。変更は追加のみで、旧文書は読込時に既定値を補い、将来版の番号と未知フィールドは保持する。

### fix: 標準Chrome E2Eの安定ID不足を解消

- 「最近使った項目」と「お気に入り」の各メニューボタンへ内容由来の安定IDを付け、シナリオの通常E2Eがルビ操作前に停止しないようにした。

### 検証

- ルビ表示・B-MANGA中継・単独版・保存互換・安定ID・版整合の集中回帰157件と18サブテストに合格。
- Chrome実ブラウザで、シナリオのルビ設定、自動ルビルール、保存・再読込を確認。
- B-MANGA v0.6.503との実送信で、v2能力確認と表示設定の受信、v1フォールバックを確認。

## v0.6.185 (2026-07-14)

### feat: 5種類の単独アプリをクラウド・スマホ・タブレットへ展開

- ノート、シナリオ、ボード、シート、タイマーに、それぞれ固有のURL・マニフェスト・Service Workerを持つ5種類の単独PWAを追加した。各アプリのUIからホーム画面への追加を案内でき、iPhone/iPad向けの手順も表示する。
- 各アプリ内にMeldexのフォルダツリーを追加し、検索、フォルダ／ファイルの作成・複製・削除・移動、ファイルを開く、保存先の選択を行えるようにした。
- Dropboxとの双方向保存、更新競合の検知、編集中ロック、ファイル移動時のパス追従、未知フィールドを保持した保存に対応した。
- 配布ページから、5種類のWindows単独アプリを個別にダウンロードし、5種類のCloud単独アプリを個別に起動・ホームへ追加できるようにした。リリース処理も各アプリのZIPとSHA-256を生成する。

### fix: 単独Cloudアプリの同期・フォルダ操作・オフライン耐性を強化

- ファイルは閲覧しただけではロックせず、実際に編集した時点で編集ロックを取得し、入力中は期限を延長するようにした。ロック保持者はタブごとに分け、同じブラウザの別タブを誤って同一編集者として扱わない。
- フォルダの移動・削除では、対象フォルダだけでなく配下のファイルとフォルダも含めて編集状態を確認し、編集中のデータを巻き込む操作を防ぐようにした。
- フォルダツリーは、更新後に届いた古い非同期応答を破棄し、処理中の重複操作を抑止するようにした。保存先選択を取り消した時は元の選択・展開・スクロール位置を復元し、検索結果の一部取得失敗や件数上限も警告で知らせる。
- 単独アプリから扱える保存先を、Meldexに登録したフォルダの内側へ厳密に限定した。システム用フォルダ、登録フォルダそのもの、登録済みの子フォルダを巻き込む削除・移動・改名を防ぎ、画像や添付ファイルの追加は登録フォルダ内で引き続き利用できる。
- 削除した項目は保存元ごとのゴミ箱へ移し、復元時も同じ保存元へ戻すようにした。保存元を登録し直して識別子が変わった場合も安全に復元でき、1つの保存元を読めない時は、ほかの保存元のゴミ箱を表示したまま警告する。
- 新規保存は「同名ファイルが存在しないこと」を保存直前にも確認し、別端末と同時に同名ファイルを作った場合の上書きを防ぐようにした。移動後に元フォルダを再表示できなかった場合も、別の保存元と一覧を混在させず、移動済みであることと表示失敗を分けて知らせる。
- 前回開いていたファイルが削除済み・破損・非対応になっている場合は、その記録を解除して空の新規画面から起動するようにした。次回以降も同じ読込エラーを繰り返さない。
- 作成・改名・移動などの処理中だけ、実際の変更先を一時的に保護するようにした。`.scriptnote.json` や `.board.md` など複合拡張子の改名先も正確に保護し、成功・失敗のどちらでも処理後に解除する。
- ボードはDropboxの更新競合を検知して外部更新を保護し、不正な形式のファイルを開こうとした場合は、直前の正常なボードとファイル選択へ戻すようにした。
- 5種類のService Workerが、事前結合したオフライン用バンドルと再帰的に収集したCSS依存をキャッシュするようにした。オフライン再起動時に分割スクリプトやCSSの追加通信を要求しない配布構成にし、スマホ幅では各アプリが画面全体を使うようにした。
- ゴミ箱の復元・削除ボタンは、関数呼び出し文字列へ行番号を埋め込まず、安全なJSON引数として渡すようにした。Security Gateの既知例外からも対象2件を削除した。

### fix: 左右サイドバーのタブから追加ボタンを撤去

- 左サイドバーと右サイドバーのタブには「＋」ボタンを表示しないようにした。中央タブの追加ボタンは従来どおり利用できる。

### fix: 配布URLとバージョンの不整合を公開前に停止

- タグを `v<semver>`、公開版を `app/version.json` の `semver` と照合し、不一致ならリリースCIを失敗させるようにした。
- Cloud静的ビルドは、本体と5種類の単独Windowsアプリの公開URL、公開Release URL、公開バージョン、Dropbox App keyを必須ゲートにした。本体・単独アプリのURLはHTTPSかつ所定のZIP名であることまで検査し、欠落、ダウンロードURLのHTTP指定、バージョン違い、誤った資産名があればビルドと公開前検査を停止する。
- privateタグCIでWindows配布物と同じコミットのCloud artifactを生成し、公開GitHub Releaseを先に成立させてから、fresh cloneした公開リポジトリへartifactを完全ミラーする公開順序を文書化した。
- Windows CIのセキュリティ診断出力をUTF-8に固定し、日本語を含む検出理由が文字コード例外で隠れないようにした。

### fix: Cloud配布の分割資産を1,000行制限内に維持

- Cloud静的ビルドで分割済みスクリプトを巨大な単一ファイルへ戻さず、分割ローダーと `part` 資産の構成を保って配布するようにした。生成後の入口スクリプトと分割資産にも1,000行制限の回帰ゲートを追加した。
- 単独Cloudアプリの共通実行層をパス制約・Dropbox実行・編集セッションの責務へ分離し、新規の配布・実行ファイルが1,000行以内に収まるようにした。

### 検証

- 単独アプリ回帰23件、編集ロック回帰9件、配布構成回帰15件、非同期フォルダツリー回帰1件、Cloud公開準備回帰18件に合格。ソース境界・編集セッション・ソース別ゴミ箱・復元経路の関連回帰18件にも合格。
- Chrome実ブラウザで、スマホ相当の5アプリ、Noteの検索・作成・複製・移動・削除・保存・ETag競合保護、iPad相当のNote、PC/スマホ配布ページを確認した。5アプリはService Worker停止後のオフライン再起動にもすべて合格。
- フルunittestは2,743件を実行し、失敗0・エラー0・skip 4。skipはWindowsでシンボリックリンク作成権限がない環境向け1件と、実ブラウザゲートを明示的に有効化した時だけ実行する3件。

## v0.6.184 (2026-07-13)

### fix: シナリオのセル選択状態の相互排他と、カスタム列のキーボード操作

- タイプ列のセルをクリックした後に矢印キーで別の列に移動できなかった問題を修正した。
- テキスト列を選択した状態でタイプ列をクリックすると、テキスト列の青い枠が消えずに残る問題を修正した。
- タイプ列でTab/Shift+Tab/Escapeキーが動作するようにした。
- カスタム列（数値型・選択型）をクリックした後も矢印キーで別の列に移動できるようにした。
- カスタム列でEnterキーを押すと編集開始、Escape/Enter/Tabで編集終了して次のセルに移動できるようにした。
- 行のチェックボックスやハンドルをクリックした時に、タイプ列の選択やアクティブセルの枠が正しくクリアされるようにした。
- ガター（行番号）や空白部分をクリックした時も、すべての選択状態が正しく解除されるようにした。

## v0.6.183 (2026-07-13)

### feat: ファイル保存・開くダイアログのOS標準UI化

- エクスポート時の保存ダイアログと、ファイルを開くダイアログが、Windows標準のモダンダイアログ（エクスプローラー統合型）で表示されるようにした。フォルダ選択ダイアログは既に対応済みだったが、保存・開くは旧型のダイアログのままだった。
- ファイルの種類（フィルタ）の選択肢が、Windows標準の操作感で表示されるようになった。
- Windows以外の環境や、ダイアログの表示に失敗した場合は従来のダイアログに自動で切り替わる。

## v0.6.182 (2026-07-13)

### feat: シナリオのExcel風セルナビゲーション

- セルのクリックでアクティブ表示（アクセントカラーの枠）になり、矢印キーで隣のセルに移動できるようにした。テキスト列だけでなくハンドル・行番号・タイプ・ステータス・カスタム列もすべてキーボードで移動できる。
- ダブルクリックまたはEnterキーで編集モードに入り、Escapeで編集を終了してアクティブ表示に戻る。もう一度Escapeで完全に解除。
- 縦書きモードでは物理キーの方向が論理的に入れ替わる（←→が行移動、↑↓が列移動になる）。
- フィルタで非表示の行はスキップして移動する。
- タイプ列でEnterを押すとタイプ変更メニューが開く。
- Tabキーで次の列、Shift+Tabで前の列に移動できる。
- Ctrl+矢印（行入れ替え）やAlt+矢印（タイプセル移動）などの既存ショートカットは影響を受けない。

## v0.6.181 (2026-07-13)

### fix: シナリオのホバー表示・選択・キャレット・スクロールの改善

- 行にカーソルを合わせた時に、背景色だけでなくテキストの色まで変化してしまう問題を修正した。背景色のみ明度が変わり、テキストの色はそのまま維持される。
- テキストが1行しかないセルで Shift+上/下 を押すと、セル内の選択が始まる前に隣の行へ移動してしまう問題を修正した。セル内テキストの端に到達するまでは通常どおり選択が伸びる。
- タイプ列をダブルクリックしてもタイプ変更メニューが開かない問題を修正した。
- 横書き時にルビが手前の文字に重なることがある問題を修正した。ルビが親文字より幅広い場合に適切な余白が確保される。
- 縦書きモードで縦中横（半角数字や「!?」等の横組み表示）の箇所にキャレットを置いた時、キャレットが横線として表示されていたのを縦線に修正した。
- ホイールスクロールの移動距離がパネルの大きさに連動するようにした。パネルが大きい時は大きく、小さい時は小さくスクロールする（ユーザー設定による上書きは引き続き有効）。

### fix: タイプ選択ポップアップとタイプ管理タブの修正

- タイプ選択ポップアップから「リンクを設定」ボタンを削除した。リンクの挿入は既存のショートカットまたはコマンドパレットから行える。
- タイプ選択ポップアップの入力欄にカーソルを合わせると表示される「タイプ名を入力」ツールチップを非表示にした（入力欄内のプレースホルダーで十分なため）。
- タイプ選択ポップアップを ESC で閉じた時、元のテキスト編集位置にキャレットが戻るようにした。
- タイプ管理タブでタイプ名を変更した後にキャレットが消えてしまう問題を修正した。名前の変更後もキャレットが同じ入力欄に残る。上下キーで隣の入力欄に移動した場合も正しく追従する。

### feat: シナリオのエクスポート機能追加・改名

- メニューに「HTMLとして保存」と「Markdownとして保存」を追加した。Markdownではタイプ名が **[タイプ名]** の形式で出力され、区切り（ページ送り）は水平線 `---` に変換される。ルビは `{漢字|かんじ}` 記法で保持される。
- メニューの「クリスタ送信」を「CLIP STUDIO PAINTへ送信」に改名した。ダイアログタイトルやステータスメッセージも含め、すべての表示箇所を統一した。

## v0.6.180 (2026-07-13)

### fix: 複数ウィンドウで別々のファイルを開いていても保存が「編集中」エラーになる問題を修正

- 同じPCで複数のMeldexウィンドウを開いていると、同じファイルやフォルダを開いていなくても「◯◯が編集中のため保存できません」で保存に失敗することがある問題を修正した。同一PC（同一ブラウザプロファイル）のウィンドウ同士は編集中ロックで衝突しなくなった。別の端末との同時編集防止は従来どおり働く。
- 一度保存したファイルやフォルダのロックをウィンドウを閉じるまで保持し続ける問題を修正した。しばらく保存していないロックは自動で解放される。これにより共有ストアへの書き込み頻度も下がり、Dropboxの競合コピーが発生しにくくなった。
- ウィンドウを閉じた時に、他のウィンドウが使用中のロックまで解放してしまわないよう、そのウィンドウが取得したロックだけを解放するようにした。
- 別端末との競合で1ファイルのロックが取れなかった場合に、無関係な他ファイルのロック保護まで消えてしまう問題を修正した。競合したファイルだけを除外し、他のロック保護は維持される。

### feat: 編集ロック中ファイルのロックマーク表示

- 編集ロックがオンのファイル・フォルダを開いている時、ツールバーのタイトル欄の直前にアクセントカラーのロックマークを表示するようにした。対象はノート・シート・CSV・ボード・シナリオ・フォルダパネル。マークにカーソルを合わせるとロック理由が表示される。
- ロックの切替・解除・定期更新に合わせてマークの表示も自動で更新される。
- ノートのタイトルをリネームした後もロックマークの表示対象が正しく更新されるようにした。

### feat: シナリオの選択・削除操作の改善

- テキストセル内にカーソルがある時の Ctrl+A を、全行選択ではなくそのセル内テキストの全選択に変更した（セル外では従来どおり全行選択）。Ctrl+Aで単一セルを選択した時、以前のドラッグ選択状態が残らないようクリアされる。
- 行が選択状態になるのは各行のチェックボックス操作（クリック・ドラッグ・Shift+クリック）と明示的な全選択コマンドのみになった。
- シナリオ上でドラッグして複数行を選んだ場合は「セル内容の選択」となり、Delete/Backspaceキーで行削除ではなく選択セルの内容だけを削除するようにした（アンドゥで復元可能）。選択中はCtrl+Cでセル内容を改行区切りでコピーできる。Escapeまたはクリックで選択解除。
- 別のシナリオファイルに切り替えた時にセル選択状態が正しくクリアされるようにした。
- 複数のシナリオパネルを同時に開いている場合に、一方を閉じても他方の選択表示が消えないようにした。

## v0.6.179 (2026-07-12)

### feat: クイックメモ全面改修 — モード分離・タグチップ・一覧・PWAインストール

- クイックメモの入力面を「テキスト」「ペン」「音声」の3モードに分離した。各モードで全画面入力でき、ツールバーにはそのモードのボタンだけが表示される。画面に収まらない場合はツールバーが折り返される。
- 音声モードに録音開始・一時停止・録音再開・録音終了ボタンと経過時間タイマーを追加した。ブラウザ音声認識（リアルタイム）とMediaRecorder＋文字起こしAPIの両方に対応した。
- タグ入力をテキスト入力からタグチップ選択UIに変更した。既存タグをタップで選択・解除でき、＋ボタンで新しいタグを追加できる。追加したタグはMeldex本体との同期前でも次のメモで即利用可能（ローカルキャッシュ）。
- クイックメモの一覧表示を追加した。一覧ボタンからMeldex本体に保存済みのクイックメモをタイトル・プレビュー・タグ・日時で閲覧でき、タグフィルタとテキスト検索で絞り込める。
- 「ホームに追加」ボタンを追加した（PWAインストール）。モバイルでも表示される。
- タイトル未入力時、本文の一行目をプレースホルダーとして表示し、保存時に自動入力する。
- 保存先シートの選択欄を廃止した。クイックメモは常にホームフォルダの「クイックメモ」シートに保存される。
- バックエンドのシート名を「メモ」から「クイックメモ」に変更した。メモ一覧取得（`GET /api/quick-memo/list`）とタグ一覧取得（`GET /api/quick-memo/tags`）のAPIエンドポイントを追加した。スタンドアロンサーバーにも同エンドポイントを追加した。
- 既存の「メモ」シートを持つ環境でも後方互換性を維持した。

## v0.6.178 (2026-07-12)

### feat: Xブックマーク・外部取り込み・Notion同期の定期自動実行

- Xブックマーク保存、外部取り込み（Notion/Obsidian/Evernote/Eagle）、Notion同期の3機能に定期自動実行を追加した。分間隔のほか、毎日（時刻指定）・毎週（曜日指定）・毎月（日付指定）に対応した。UIは共通のスケジューラウィジェットで統一した。
- Notion同期の旧ドロップダウン（5分/15分/30分/1時間）を新しいスケジューラウィジェットに置き換えた。既存の設定は自動的に新形式に移行される。

### fix: サブパネルでシート画像プロパティが文字列表示になる問題を修正

- Xブックマーク等で画像をダウンロード済みでも、サブパネルのプロパティ一覧に画像サムネではなくJSON文字列が表示される場合がある問題を修正した。サブパネルでエントリを開く時に親シートの型情報を並列取得し、正しい型で描画するようにした。

## v0.6.177 (2026-07-12)

### fix: フォルダパネルの選択フロートメニューが閉じた後も残る問題を修正

- フォルダパネルでファイルを選択した後、別のツールに切り替えたりパネルを閉じても選択フロートメニューが画面に残り続け、他のツールのフロートメニューを覆い隠す問題を修正した。選択解除ボタンも効かなかった。フロートメニューがフォルダパネルの外（画面最上位）に移動してしまう原因を修正し、フォルダパネル内に留まるようにした。

### fix: 戻る・進むでシートビューのコンテンツが混在する問題を修正

- 戻る・進むでシートを切り替えたとき、前のシートのカンバン・ギャラリー等のビューコンテンツが残って混在する問題を修正した。シート切替時にビューコンテナを確実にクリアし、同じシートの再選択では不要な画面クリアを抑えた。

### fix: フォルダツリーからシート切替時にビューが更新されない問題を修正

- フォルダツリーからシートを切り替えたとき、オプションパネルやビューが前のファイルの内容のまま更新されない問題を修正した。ナビゲーション操作中のパネル連動ガードに競合があり、同期更新が抑制されていた原因を解消した。

### fix: 戻る・進むでオプションパネルが連動しない問題を修正

- 戻る・進むでファイルを切り替えたとき、オプションパネルが前のファイルの設定を表示したままになる問題を修正した。ナビゲーション操作のペイン同期を修正し、非同期の競合で古い内容が上書きされる隙を塞いだ。

### feat: サブパネルに戻る・進むナビゲーションを追加

- サブパネル（ポップアップ詳細表示）に戻る・進むボタンとパスバーを追加した。サブパネル内でエントリ間のリンクを辿り、戻る・進むで行き来できるようにした。メディア・カレンダー・外部URLの状態も復元される。

- 表示バージョンを `0.6.177` に更新した。

検証: ナビゲーション履歴・ペイン同期・シート切替・サブパネル戻る進むの関連unittestを実行。分割バンドル再生成とハッシュ整合性テストで新規失敗ゼロを確認。

## v0.6.176 (2026-07-11)

### feat: シナリオをB-MANGAへ安全に送信

- デスクトップ版とシナリオ単独アプリの「エクスポート」に「B-MANGAへ送信...」を追加した。Cloud版では表示も実行もしない。
- 送信前に編集中の本文を同期し、区切りタイプごとのページ、タイプ、改行、手動リンクの表示文字、明示ルビ、自動ルビをB-MANGA用データへ変換するようにした。
- ポート番号、接続トークン、ページ数、本文数を送信前に確認できるようにし、接続トークンは画面を閉じると破棄して保存しない。
- ブラウザからB-MANGAへ直接接続せず、Meldexのローカル処理から `127.0.0.1` 固定で中継する。送信先、ポート範囲、データ形式、サイズ、応答サイズ、待ち時間を制限した。
- B-MANGAへ送信にクリスタ送信と同等の送信オプション（送信範囲・前後設定・プロット行・区切り行テキスト・空白行）を追加した。
- 表示バージョンを `0.6.176` に更新した。

検証: B-MANGA送信データ変換・ローカル中継・認証ヘッダー・送信先固定・Cloud非表示・単独アプリ同梱の関連unittest、Python/JavaScript構文チェック、バージョン整合性、差分空白チェックを実行。B-MANGA送信オプションはブラウザE2E（既定値・プロット行・前後設定・空白行除外・区切り行テキスト・選択範囲の5フェーズ）で送信データまで検証。

### feat: モバイルのフォルダ遷移をサイドドロワーへ統一

- モバイル表示で、フォルダツリー・フォルダビューからフォルダを開いたときにサイドドロワーのエクスプローラーへ遷移するよう統一した。
- サイドドロワーの閉じ処理に理由付きの世代ガードを追加し、開閉が連続したときの取り違えを防いだ。

検証: モバイル遷移E2Eアクションとcloud回帰unittest（batch57）を更新・実行。

### feat: ノート本文の素のURLを自動リンク化

- ノート本文に貼り付け・入力された素のURLを、表示時にクリックできるリンクへ変換するようにした。動画メディアの往復保存も維持する。

検証: エディタ回帰unittest（batch99）に往復保存とリンク化のテストを追加・実行。

### feat: Xブックマークのメディア取得と翻訳プロパティを追加

- Xブックマーク取込にメディア取得を追加し、「元の言語」「日本語訳」プロパティと、保存状況の失敗種別（画像取得失敗・メディア取得失敗）を拡充した。

検証: Xブックマークのunittestを拡張・実行。

### fix: ポップアップメニューのホバーハイライトを復元

- 右クリックメニュー等のポップアップメニュー項目で、カーソルオーバー時の選択ハイライトとキーボードフォーカス時の背景が表示されなくなっていた問題を修正した。
- 原因はメニュー項目のボタン化に伴い追加された標準見た目リセットの詳細度が高すぎ、ホバー/フォーカスの背景指定を打ち消していたため。リセットの詳細度を必要最小限へ下げ、再発防止コメントを追記した。

検証: 実ブラウザでホバー背景・削除系項目の赤ホバー・フォーカス背景と枠線・ボタン見た目リセットの維持を確認。gb-tools.part04.css を参照する回帰unittest群を修正前後で比較実行し、新規失敗ゼロを確認。

### fix: 「パスをコピー」をドライブレター付きの絶対パスへ統一

- フォルダビュー・ツールメニュー・シートのエントリメニューの「パスをコピー」を、フォルダツリーと同じドライブレター付き絶対パス（Windows表記）でコピーするよう統一した。
- パス解決を共通ユーティリティ（gb-path-utils.js の GBPathUtils）へ集約し、フォルダツリーの既存実装も同ユーティリティへ委譲した。ソースフォルダの保存パスが `\` 区切りでも `/` 区切りでも、結合後の区切り文字を `\` へ揃える。
- クラウド/Dropboxモードなど絶対パス化できない場合は、従来どおりの値をコピーする（安全フォールバック）。

検証: パス解決ユーティリティのNode単体検証（相対+絶対結合・混在区切り正規化・UNC・Dropbox仮想パス）、フォルダビュー単一項目のパスコピーをブラウザE2Eで確認（一括コピーは同一処理のため単体検証で担保。E2Eの後半は無関係な画像ボード作成ステップの環境タイムアウトで打ち切り）。分割バンドル再生成と split assets 回帰unittestで新規失敗ゼロを確認。

## v0.6.175 (2026-07-12)

### perf: Dropbox上のフォルダ・ファイル読込を高速化

- フォルダ一覧の種別判定を上限付き共有ワーカーへ移し、一覧表示後に全項目へ発行していた本文確認を廃止した。旧形式など判定が必要な項目だけを、表示後に待ち時間上限付きで確認する。
- 同一フォルダ一覧の多重通信をまとめ、2.5秒の短期キャッシュと作成・保存・移動・削除時の無効化を追加した。更新直前に開始した古い応答が、更新後の一覧を上書きしないよう再取得する。
- ファイル本文へ署名検証付き短期キャッシュを追加し、保存時に無効化した。file_idの一括保存と別ウィンドウの更新取り込みにより、大量項目での同期処理とIDの取りこぼしも抑えた。
- 起動時索引は変更のないファイルの本文・ハッシュ・FTS抽出を省略し、前景読込中はDBを確定してロックを解放してから待機する。旧索引の未検証ハッシュを移動判定へ使わず、安全性を維持した。
- 実Dropbox計測で、77項目のツリー一覧を20.2秒から2.3秒、詳細一覧を20.5秒から2.3秒、15項目の一覧を12.1秒から2.0秒へ短縮した。同じ7 KBファイルの再表示は約2.3秒から21 ms以下になった。
- 表示バージョンを `0.6.175` に更新した。

検証: 関連unittest 144件、Python/JavaScript構文検査、Desktop・スマホ・タブレットのフォルダ表示E2E、Cloud静的ビルド、差分空白チェックを実行。

## v0.6.174 (2026-07-09)

### feat: カード表示プロパティの表示量を調整可能に

- ギャラリー / カンバン / タイムラインの表示プロパティメニューに、画像プロパティのサムネイル数とプロパティ値の行数を追加した。
- 画像カードプレビューの空候補・動画候補・追加枚数バッジを維持したまま、ビューごとの設定に従ってサムネイル表示数を切り替えられるようにした。
- タイムラインカードでは既存の折り返し/切り詰め表示よりカード設定を優先し、画像プロパティはカード用のコンパクトプレビューで表示するようにした。
- シートマニュアルへ、カード内サムネイル数と値の行数がビューごとに自動保存されることを追記した。
- 表示バージョンを `0.6.174` に更新した。

検証: 徹底チェック（ギャラリー/カンバン/タイムラインの表示プロパティメニュー、カード再描画、画像クリック、値行数CSS、タイムライン折り返し設定との競合、保存ビュー正規化の操作チェーン追跡）、関連JavaScript構文チェック、表示プロパティ/タイムライン/CSS/バージョン回帰unittest、対象database E2E（新しい表示プロパティ入力の確認地点までは通過、後続の既存ビュー状態アサーションで停止）、差分空白チェックを実行。

## v0.6.173 (2026-07-08)

### fix: Dropbox閲覧時の自動競合発生を抑止

- Cloud/Dropboxモードの起動直後と5分ごとの容量ヘルスチェックで、共有Dropbox内の `_meldex/space-usage.json` を自動保存しないようにした。
- 容量情報は通常チェックではライブ取得だけを使い、共有キャッシュを書き込む処理は明示操作用の `publishSharedSpaceUsage()` に分離した。
- アクティブロックの一覧確認・保存前確認で、期限切れ行の掃除目的だけの `_meldex/active_locks.json` 書き戻しをやめ、閲覧・設定表示・確認系操作を読み取り専用にした。
- Dropbox競合コピー検出時は、ファイル名やパスを送らず、件数・切り詰め有無・実行モードだけを診断報告へ残すようにした。
- Google受信箱取込で、性能ログをクラッシュ扱いしないようにし、`usage` はApps Script側で日付+セッション単位の1行更新にした。
- Google既読反映を行IDマップと小分け更新に変え、1万行超の受信箱でも1件ずつ全走査しないようにした。
- 表示バージョンを `0.6.173` に更新した。

検証: 徹底チェック（起動ヘルスチェック、容量確認、競合コピー検出、アクティブロック一覧/確認/取得/解除、Google受信箱pending/mark_imported/usage upsertの操作チェーン追跡）、関連JS/Python構文チェック、Cloud Phase1/Phase2/アクティブロック/Cloudワークスペース/競合コピー/Cloud bootstrap/Google受信箱回帰unittest、バージョン整合性チェック、Cloud静的ビルド、差分空白チェックを実行。

## v0.6.172 (2026-07-07)

### fix: Cloud公開ビルドの分割スクリプト欠落を防止

- Cloud静的ビルドで、分割ローダーが参照する通常名のJSファイルも再帰的に収集し、`gb-settings-navigation.js` などが公開成果物から落ちないようにした。
- Cloud公開検証で、分割ローダー参照先の欠落と `../escape.js` のような危険な分割参照を機械的に失敗させるようにした。
- スマホ/iPad Pro幅の公開確認で出た起動停止の原因を修正し、表示バージョンを `0.6.172` に更新した。

検証: Cloud公開準備unittest、Cloud静的ビルド、公開URLのスマホ/iPad Pro幅ブラウザ確認を実行。

## v0.6.171 (2026-07-07)

### fix: セキュリティ修正済みCloud版を公開

- セキュリティ最小化計画のフェーズ0〜3で修正した公開フィクスチャ、SSRF、HTML/JS注入、権限なりすまし、危険なパス処理、postMessage、秘密情報保存経路の修正をCloud公開版へ反映した。
- `MeldexCloud` public repository への反映を前提に、private repository 側のPages経路ではなく、スマホ/iPad Proから開く公開URL向けの静的成果物としてビルドする。
- 表示バージョンを `0.6.171` に更新した。

検証: セキュリティ回帰テスト、Cloud公開準備チェック、Cloud静的ビルド、公開URLのバージョン確認を実行。

## v0.6.170 (2026-07-07)

### fix: Cloud公開ビルドの必須環境変数ゲートを強制

- Cloud静的ビルドで、公開URL、デスクトップ配布URL、リリース一覧URL、デスクトップ版バージョン、Dropbox App key が環境変数から渡されていない場合にビルド/検証が失敗するようにした。
- `cloud-public-readiness-log.json` に不足している必須環境変数名を記録し、公開前チェックで「不足しているがビルドは通る」状態を残さないようにした。
- Cloud公開準備テストに、必須環境変数ありの正常系と、未設定時に `RuntimeError` で止まる異常系を追加した。
- tracked script 全件を行単位で走査する全体チェック用スクリプトを追加し、秘密情報候補、競合マーカー、UTF-8破損、非チャンクPython構文をコミット前に確認できるようにした。
- 表示バージョンを `0.6.170` に更新した。

検証: 全体チェック（tracked script 1,305ファイル / 552,004行の行単位走査、ブロッカー0件）、Cloud公開準備unittest、2026-05-27全体チェック高/中/低重要度回帰unittest、セキュリティ重要回帰unittest、Python構文チェック、バージョン整合性チェック、差分空白チェックを実行。

## v0.6.169 (2026-07-01)

### feat: 設定ダイアログのタブ分類を再編

- 設定ダイアログの左タブを、ユーザーが探す目的別に `ユーザー・共同作業`、`保存先・フォルダ`、`表示・起動`、`AI・Discord`、`インポート` などへ再編した。
- 旧タブ名や既存の設定パネルは互換エイリアスとして残し、コマンドパレット、E2E操作、既存の設定初期化が新しい分類から同じ保存キーへ到達できるようにした。
- 項目量が多い分類はサブタブで絞り込み、通常の設定内容がダイアログ内に収まりやすい構成へ整理した。
- 実装前提を確認できるサンプルHTMLを `app/docs/settings-dialog-optimization-proposal-2026-07-01.html` として追加した。
- 表示バージョンを `0.6.169` に更新した。

検証: 徹底チェック（PC設定ダイアログ初期表示、旧タブ名指定、コマンドパレット、スマホ設定カテゴリ、サブタブ、設定パネル初期化、テーマプレビュー、E2E旧アクション互換、保存キー非変更の操作チェーン追跡）、関連JavaScript構文チェック、設定/コマンドパレット/分割ローダー回帰unittest、スマホ/PC設定 micro E2E、旧表示名検索、バージョン整合性チェック、差分空白チェックを実行。

## v0.6.168 (2026-07-01)

### feat: Meldex共有サーバー接続を追加

- 保存先に `Meldex共有サーバー` を追加し、Cloud/ブラウザ版から管理者PCまたはNAS上のMeldexサーバーへ接続できるようにした。
- 共有サーバーモードでは、ノート、画像、サムネイル、ユーザー、ワークスペース関連のAPIを設定済み接続先へ向け、Dropbox providerへ流れないようにした。
- サーバー側の公開設定を `MELDEX_HOST` / `MELDEX_PORT` / `MELDEX_CORS_ORIGINS` で明示 opt-in にし、既定のローカル起動とCORS制限は維持した。
- Hosted Cloudで保存済みの共有サーバーモードが起動時にDropboxへ戻らないよう、保存済み接続モードの優先順を修正した。
- 配布版のローカルAPI保護が残っている場合は、接続テストで共有サーバー公開設定の確認が必要だと分かるメッセージを表示するようにした。
- 表示バージョンを `0.6.168` に更新した。

検証: 徹底チェック（初回保存先選択、共有サーバーURL正規化、接続テスト、Hosted Cloud再起動、設定画面からの接続先変更、APIリライト、画像/サムネイル取得、Dropbox経路非干渉、サーバー公開設定/CORS/ローカルAPI保護の操作チェーン追跡）、関連JavaScript構文チェック、Python構文チェック、共有サーバー/Cloud Phase1/セキュリティ/性能/Cloud公開準備/設定/バージョン回帰unittest、Cloud静的設定E2E、Cloud静的ビルド、旧未対応文言検索、バージョン整合性チェック、差分空白チェックを実行。

## v0.6.167 (2026-07-01)

### feat: 右サイドバーに階層タグパネルを追加

- 右レールにタグボタンを追加し、右サイドバーでタグを階層ツリーとして管理できるようにした。
- タグ/グループの追加、削除、Ctrl/Shift複数選択、D&D階層移動、一括削除に対応した。
- タグクリックで現在のフォルダ表示をタグ付き項目だけに絞り込み、通常の全検索にもタグ一致結果を統合した。
- 標準タグ集をタグプリセットとして同梱し、プリセットの追加、複製、削除、保存、読込、ドロップダウン切替を追加した。
- CLI自動タグ付けを追加し、画像はCLIが画像目視確認した場合だけタグ付けし、安全規制に抵触する可能性がある場合は中断して警告するようにした。
- ファイル/フォルダ単位の自動タグ付け、ZIP/TAR系の圧縮/解凍、未対応ファイルの既定アプリ起動導線をフォルダUIへ追加した。
- 表示バージョンを `0.6.167` に更新した。

検証: 徹底チェック（タグパネル表示、タグ/グループCRUD、複数選択、D&D階層移動、一括削除、タグクリックによるフォルダ絞り込み、全検索統合、タグプリセット切替、CLI自動タグ付け、画像目視確認ガード、安全規制中断、圧縮/解凍、未対応ファイルの既定アプリ起動の操作チェーン追跡）、関連JavaScript構文チェック、Python構文チェック、タグ/検索/プリセット回帰unittest、圧縮/解凍API回帰unittest、ルーター回帰unittest、分割アセットunittest、バージョン整合性チェック、差分空白チェックを実行。

## v0.6.166 (2026-07-01)

### fix: URLリンクを既定ブラウザで開けるようにする

- 本文、チャット、バックリンクなどのリンク右クリックメニューに、HTTP/HTTPS URLをOSの既定ブラウザで開く項目を追加した。
- ボードのリンクメニューにも「既定のブラウザで開く」を追加し、外部URLをMeldex内の単独アプリ表示ではなく通常のブラウザへ送れるようにした。
- サブパネル上で開いたリンクメニューや関連ポップアップがサブパネルの背面に隠れないよう、重なり順を調整した。
- 表示バージョンを `0.6.166` に更新した。

検証: 徹底チェック（本文リンク、チャットリンク、バックリンク、ページ内アンカー、ボードリンク、HTTP/HTTPS、mailto/tel、ローカルリンク、サブパネル上のポップアップ重なり順の操作チェーン追跡）、関連JavaScript構文チェック、URLコンテキストメニュー回帰unittest、サブパネル回帰unittest、リンク/サブパネル周辺回帰unittest、ツールチップ回帰unittest、バージョン整合性チェック、差分空白チェックを実行。

## v0.6.165 (2026-07-01)

### fix: 起動とフォルダ読み込みの重複待ちを減らす

- デスクトップ版のフォルダツリー/フォルダパネルで同じフォルダを短時間に再表示するとき、`/api/browse` の結果を短時間だけ共有し、同時リクエストも1回にまとめるようにした。
- サーバー側のフォルダ一覧取得にも短時間キャッシュを追加し、ローカルフォルダ内の再走査、フロントマター判定、シート/カレンダー判定、ファイルID照合の重複を減らした。
- Cloud/スマホ版のDropboxフォルダ一覧にも短時間キャッシュと同時実行共有を追加し、フォルダツリーとフォルダ一覧の切り替えや再表示でDropbox APIを連打しにくくした。
- ファイル保存、削除、移動、コピーなどの変更操作後は一覧キャッシュを破棄し、古い取得中レスポンスが変更後に再キャッシュされないよう世代管理を追加した。
- 起動時に読む `gb-outliner.js`、Dropbox fileops、スマホ編集バーを事前結合bundle対象へ追加し、split-loader の同期チャンク取得回数を減らした。
- 表示バージョンを `0.6.165` に更新した。

検証: 徹底チェック（初回起動、戻り起動、デスクトップ版フォルダツリー展開、スマホ版フォルダ一覧、Cloud/Dropbox `/browse`、ファイル保存/削除/移動/コピー後のキャッシュ無効化、古い in-flight 一覧レスポンスの競合、split-loader 事前結合manifestの操作チェーン追跡）、関連JavaScript構文チェック、Python構文チェック、性能回帰unittest、分割アセットunittest、関連API/セキュリティ/スタンドアロン回帰unittest、Cloud静的source surface E2E、Cloud静的ビルド/検証、バージョン整合性チェック、差分空白チェックを実行。

## v0.6.164 (2026-07-01)

### fix: スマホ版フォルダ一覧の表示形式切替を復旧

- スマホCloudの統合フォルダ一覧に、リスト、グリッド、ウォーターフォール、横スクロールの表示形式切替を追加し、フォルダパネルと同じ保存値で復元されるようにした。
- 一覧表示中の画像/動画ファイルはカード表示でサムネイル領域を使い、表示形式ごとの見え方が切り替わったことをユーザーが確認しやすいようにした。
- スマホ幅でも通常のフォルダパネル側の表示形式セレクトと表示/フィルタボタンを隠さず、44px以上のタップ領域で使えるようにした。
- 統合フォルダ一覧から表示形式を変えても、背後のフォルダパネルを不要に再描画せず、選択フォルダや一覧状態が崩れないようにした。
- フォルダ一覧ヘッダーの並び替えボタンに安定したE2E IDを付け、スマホ微細挙動テストが実際に見えているヘッダーを検証できるようにした。
- 表示バージョンを `0.6.164` に更新した。

検証: 徹底チェック（スマホ統合フォルダ一覧、表示形式切替、保存値同期、通常フォルダパネルのスマホ表示、行/カードレイアウト、フォルダ一覧ヘッダーの操作チェーン追跡）、関連JavaScript構文チェック、展開後 `gb-folder.js` 構文チェック、スマホCloud回帰pytest、スマホフォルダ導線micro E2E、スマホCloud表面状態micro E2E、旧未対応文言検索、Cloud静的ビルド、バージョン整合性チェック、差分空白チェックを実行。

## v0.6.163 (2026-06-30)

### fix: スマホCloudのフォルダ導線と品質ゲートを固める

- スマホCloudの統合フォルダ一覧で、行メニューから直接「開く」操作を選べるようにし、一覧上の操作と行タップの意味を揃えた。
- 新規フォルダ作成後も統合フォルダ画面を一覧モードで維持し、作成先フォルダの空状態と親フォルダへの戻り導線が自然に続くようにした。
- UI拡大率が有効な状態でも、スマホのフォルダ画面がviewport外へずれないように、Cloudモバイル画面の幅/高さを拡大率込みで制御した。
- フォルダ一覧モードでは閉じハンドルを左下へ移し、行メニューや新規作成FABと重ならないようにした。
- フォルダ画面表示中は注釈FABを隠し、新規作成FABと重ならないようにした。
- `Document` など要素以外がイベント対象になった場合でも、委譲イベントとツールチップ処理が `closest()` で落ちないようにした。
- Cloud API変換でステータス未指定の例外を既存仕様どおり501へ戻し、Cloud側のアクティブロック回帰テストと整合させた。
- シートの範囲検証で比較対象プロパティの案/ボツ値も正しく評価し、採用値だけを見て矛盾を見落とすケースを防いだ。
- UI拡大率適用時にテスト/軽量DOM環境で `style.setProperty` が無い場合でも落ちないようにした。
- タイマー設定ダイアログを通常のモーダルシェルに戻し、ドラッグ可能なモーダルとして扱えるようにした。
- Chrome拡張機能マニュアルのデベロッパーモード手順リンクと確認文を復旧した。
- 分割済み `gb-editor.part04.js` を回帰テストで実装チャンクとして読むようにし、split-loader stub化後のテスト前提を現行構成へ合わせた。
- 分割ローダーstub、仮想行、パネルセット、Cloudモバイルドロワーの現行仕様に合わせて回帰テストと監査マニフェストを更新した。
- Cloud/モバイルUI改善時の標準ゲートとして、`app/docs/cloud-mobile-product-quality-gate.md` と `$meldex-cloud-mobile-quality` スキルを追加し、微細挙動E2Eとスクリーンショット目視を完了条件に含めた。
- 表示バージョンを `0.6.163` に更新した。

検証: 徹底チェック（スマホ統合フォルダ一覧、ツリー/一覧切替、新規作成FAB、フォルダ作成後の空状態、行メニュー、名前変更、削除、閲覧専用、未対応メッセージ、イベント委譲、UI拡大率、品質ゲート導入の操作チェーン追跡）、全体チェック（最終読み込み監査1,234ファイル・464,917行、読み込みエラー/競合マーカー/NUL 0件、旧Cloud未対応文言はドキュメント上の禁止ルール記載のみ）、全体 unittest 2,399件通過（skip 4）、関連JavaScript構文チェック、非チャンクJavaScript構文チェック、非チャンクPython構文チェック、分割アセットunittest、スマホCloud回帰pytest、スマホフォルダ導線micro E2E、スキル検証、Cloud静的ビルド、差分空白チェックを実行。

## v0.6.162 (2026-06-30)

### fix: シナリオ編集と右サイドバー入力の細部挙動を整える

- 右サイドバーのチャット等の入力欄にフォーカスしている間は、`Ctrl+A` などの標準編集ショートカットがメインパネル側の選択操作に奪われないようにした。
- シナリオ本文セルの入力中にも行高/行幅を入力内容へ追従させ、確定前に文字がセル端で切れないようにした。
- シナリオのフィルタポップアップで「すべて表示」を押してもポップアップを閉じず、全表示状態とチェック表示がずれないようにした。
- タイプ管理の書式設定ポップアップにプレフィックス、水平、垂直、折返の説明ツールチップを追加し、プレフィックス欄はテキスト列だけに表示するようにした。
- テキスト列のプレフィックスを縦書き表示したとき、開き括弧やかぎ括弧の直後に余分な隙間が出ないようにした。
- フィルタ/テンプレートのプリセット管理ダイアログで、追加、複製、上下移動、削除のアイコンが表示されるようにした。
- 行チェックボックスをドラッグで連続切り替えできるようにし、ドラッグ直後のクリック二重反転を防いだ。
- タイプ列セルをクリックで選択、ドラッグ/Shift/Ctrlで複数選択できるようにし、タイプ選択ポップアップはダブルクリックまたはEnterで開くようにした。
- タイプ列セルの右クリックメニューとキーボード操作で、コピー、貼り付け、削除を単一/複数セルへ適用できるようにした。
- テキスト列から横書きはAlt+左、縦書きはAlt+上で同じ行のタイプ列へ移れるようにし、タイプ選択ポップアップの幅を1.5倍に広げた。

検証: 徹底チェック（右サイドバー入力、本文セル入力中リサイズ、プリセット管理、フィルタ、書式ポップアップ、縦書きプレフィックス、行チェックドラッグ、タイプ列選択/右クリック/キーボード操作/Alt移動/ポップアップ幅の操作チェーン追跡）、展開後JavaScript構文チェック、関連JavaScript構文チェック、シナリオ回帰unittest、分割アセットunittest、シナリオmicro E2Eの縦書き折り返し/幅縮小再配置/フィルタAlt移動ケース、差分空白チェックを実行。

## v0.6.161 (2026-06-30)

### fix: Cloud版のスマホUIとユーザー共有を仕上げ

- Dropboxアカウント単位でユーザー名、ユーザーアイコン、アイコン背景を共有し、デスクトップ版とCloud版で同じアカウントを使うとプロフィール表示が揃うようにした。
- ユーザーアイコンだけを変更した場合も共有プロフィールへ反映し、共有済みの表示名を壊さずにローカルのアイコン設定を同期するようにした。
- Cloudワークスペースのメンバー情報にDropboxアカウントIDを保持し、同じアカウントの名前変更で別メンバーとして重複しないようにした。
- スマホCloudの統合フォルダ画面でシートをフォルダ同等のコンテナとして扱い、一覧切替やフロート「＋」からの新規作成が親フォルダへずれないようにした。
- Cloud標準Dropbox接続では非表示のredirect URI上書きを検証しないようにし、詳細設定に残った無関係な値で起動導線が止まらないようにした。
- Dropbox保存先パスの末尾スラッシュを正規化し、空になったソースフォルダ一覧もDropbox共有設定へ保存して、別端末で古いソースフォルダが復活しないようにした。
- 表示バージョンを `0.6.161` に更新した。

検証: 徹底チェック（Dropbox/プロフィール共有、Cloudソースフォルダ、スマホ統合フォルダ画面、作成メニュー、Cloud起動導線の操作チェーン追跡）、全体チェック（実コード838ファイル・359,161行の読み込み、競合マーカー/NUL/禁止文言/読み込み不能0件）、展開後JavaScript構文チェック、Cloud公開準備/Cloud Phase1・Phase2/モバイル/プロフィール同期回帰テスト、Cloud静的ビルド、バージョン整合性チェック、差分空白チェックを実行。

## v0.6.160 (2026-06-30)

### feat: スマホCloudのフォルダ導線を統合

- スマホ/タブレットのフォルダツリー全画面を、同じドロワー内で「ツリー」と「一覧」を切り替える統合フォルダ画面にした。
- 「一覧」では選択中フォルダの中身をその場で読み込み、フォルダは同じ画面で潜り、ファイルは開いたあとドロワーを閉じる挙動にした。
- 新規作成の主導線を右下のフロート「＋」に移し、フォルダ、ノート、シナリオ、シート、ボード、スマートシート、カレンダーを作成できるボトムシートを追加した。
- スマホのフォルダツリー上では、旧来の行末「…」「＋」とヘッダー「＋」を表示せず、押せない/分かりにくい操作を見せない構成にした。
- 開発/E2E用 `Meldex-dev.html` と公開用 `Meldex.html` の両方に統合フォルダ画面のJS/CSSを追加し、モバイルE2Eにも統合一覧・フロート作成ボタンの検査を入れた。
- 表示バージョンを `0.6.160` に更新した。

検証: 徹底チェック（スマホドロワー起動、ツリー/一覧切替、一覧読み込み、フォルダ潜り、ファイルオープン、新規作成、旧行末小ボタン非表示、開発/公開HTML読み込み、E2Eアセット収集の操作チェーン追跡）、JavaScript構文チェック、スマホ/Cloud関連pytest、スマホCloud表面状態E2E、CloudモバイルPWA表面E2E、バージョン整合性チェック、差分空白チェックを実行。

## v0.6.159 (2026-06-30)

### release: Cloud版の公開反映チェックを更新

- Cloud版のスマホ/タブレットUI改善、旧ガード文言撤去、設定整理、Dropbox読み込み改善を公開反映するためのリリース版にした。
- GitHub Pages向けCloud静的ビルドと公開前検証をこの版の確認対象に含めた。
- 表示バージョンを `0.6.159` に更新した。

検証: 徹底チェック（Cloud起動、Dropbox保存先選択、スマホフォルダ導線、設定ダイアログ、Dropboxファイル読み込みキャッシュ、エラー翻訳、Pages静的ビルドの操作チェーン追跡）、Cloud静的ビルド/検証、関連unittest、Cloud/スマホE2E、旧未対応文言検索、バージョン整合性チェック、差分空白チェックを実行。

## v0.6.158 (2026-06-30)

### fix: Cloud版の旧ガード文言を実行コードから取り除く

- Cloud版のエラー翻訳で、旧ガード由来の日本語を検出条件に残さず、HTTP 501 と内部エラーコードだけをクラウド保存先の接続問題として扱うようにした。
- 過去の配布メモ内の説明も、押せる機能のように読める表現を避ける文言へ置き換えた。
- 表示バージョンを `0.6.158` に更新した。

検証: 徹底チェック（Cloud未配線APIエラー翻訳、ユーザー向け表示、過去文書内の残存文言検索）、エラー翻訳JavaScript構文チェック、Cloudワークスペース診断回帰unittest、バージョン整合性チェック、差分空白チェックを実行。

## v0.6.157 (2026-06-30)

### perf: スマホCloudのファイル再読み込みを軽くする

- Dropbox保存先の小さなファイル読み込みを短時間だけメモリキャッシュし、同じファイルの同時読み込みを1回のDropbox取得へまとめるようにした。
- 保存、削除、移動、preflightでは本文キャッシュと同時取得状態を破棄し、古い内容を長く使わないようにした。
- ノート、シート、スマートシート、ボード、シナリオが共通で通るDropbox provider層の改善なので、各アプリの再オープン、ペイン復元、スマホでの戻る/開き直しに効く。
- 表示バージョンを `0.6.157` に更新した。

検証: 徹底チェック（Dropbox providerの読み込み、キャッシュヒット、同時取得集約、保存/削除/移動/preflight無効化、各アプリ共通 `/file` 読み込み経路の操作チェーン追跡）、Storage Adapter結合後JavaScript構文チェック、性能/LocalFS/Dropbox/FileOps/バージョン回帰unittest、モックDropbox実測（同一ファイル並列3回読み込み: download 3回相当→1回、直後再読込: 追加0回、TTL後: 再取得1回）を実行。

## v0.6.156 (2026-06-30)

### feat: Cloud起動時の導線を整理

- Cloud版の保存先選択を「Dropboxで始める」「このPCに保存して始める」の判断に絞り、対応機能一覧ではなくユーザーの利用目的から選べる説明にした。
- Dropbox設定画面を「Dropboxに接続する」「使うフォルダを選ぶ」「この設定で開始」の順番に整理し、通常不要なOAuth詳細設定を折りたたみにした。
- スマホ/タブレットの初回ホーム画面追加ダイアログを、ホーム追加の理由とブラウザで続ける選択肢が分かる文言に改めた。
- 表示バージョンを `0.6.156` に更新した。

検証: 徹底チェック（保存先選択、Dropbox接続、フォルダ選択、開始、ホーム画面追加/ブラウザ継続の操作チェーン追跡）、Cloud起動関連JavaScript構文チェック、Cloud公開準備/Cloud Phase1/フロント回帰unittest、Cloud静的設定ソースE2E、CloudモバイルPWA表面E2Eを実行。

## v0.6.155 (2026-06-30)

### feat: 設定ダイアログの項目構成を整理

- 設定ダイアログの左ナビを「基本」「AI・連携」「作業環境」「安全・保守」の4グループに整理し、既存の内部タブ名と保存キーは維持した。
- スマホ版の設定一覧にも同じ分類見出しを追加し、長い一覧を目的別に探せるようにした。
- デスクトップ版の全般タブを2列カード配置にして、主要項目をスクロールなしで見渡しやすくした。
- 表示バージョンを `0.6.155` に更新した。

検証: 徹底チェック（デスクトップ設定ナビのグループ横断active切替、モバイル設定一覧から各パネルへ入る操作、全般タブ2列化後の初期化・保存キー維持、DBビュー回帰テスト期待値の現行実装照合）、JavaScript結合後構文チェック、設定/Cloud/モバイル/仕様観測/バージョン回帰unittest・pytest、デスクトップ設定全パネルE2E、スマホ設定全般E2Eを実行。

## v0.6.154 (2026-06-30)

### feat: スマホCloudのフォルダ導線を強化

- スマホ/タブレットのフォルダツリー全画面ヘッダーに「ツリー/フォルダ」切替を追加し、選択中フォルダをフォルダパネルへすぐ開けるようにした。
- 同ヘッダーに新規作成ボタンを追加し、右クリックや行末ボタンに頼らずフォルダ、ノート、シナリオ、シート、ボード、スマートシートを作成できる導線を用意した。
- Cloud起動時の初期ソースフォルダ設定に「Dropboxから選ぶ」を追加し、`/Share` などDropbox内の共有フォルダをツリーで辿って選べるようにした。
- 表示バージョンを `0.6.154` に更新した。

検証: 徹底チェック（スマホツリーからフォルダパネルを開く操作、スマホツリー上部の新規作成、Cloud起動時のDropboxフォルダ選択、旧未対応文言撤去の操作チェーン追跡）、JavaScript構文チェック、スマホ/Cloud関連pytest・unittest、Cloud static設定ソースE2E、スマホCloud表面状態E2E、旧未対応文言検索、差分空白チェックを実行。

## v0.6.153 (2026-06-30)

### fix: Cloud版の未対応操作表示を撤去

- Cloud版の未配線APIエラーを「未対応操作」として表示せず、クラウド保存先との接続問題として案内するようにした。
- 起動時の保存先選択と公開ページで、ブラウザ外の端末連携を押せる機能のように見せない文言へ変更した。
- Notion同期のCloud表示を、デスクトップ版から実行するローカル連携として説明する文言に改めた。
- 表示バージョンを `0.6.153` に更新した。

検証: 徹底チェック（Cloud未配線API、起動時保存先選択、Notion同期設定、Cloud旧ガードの操作チェーン追跡）、対象JavaScript構文チェック、Cloudワークスペース診断/公開リリース準備/Cloud Phase1/バージョン回帰unittest、旧未対応文言検索、差分空白チェックを実行。

## v0.6.152 (2026-06-29)

### feat: シナリオのテンプレートとフィルタプリセット管理を追加

- シナリオツールバーのテンプレートとフィルタプリセットに管理ボタンを追加し、追加・複製・上下並べ替え・削除をモーダルから実行できるようにした。
- 既存のプリセット保存形式は維持し、表示順だけを `sn2-templates-order` / `sn2-filter-presets-order` に分離保存することで、既存データをそのまま使えるようにした。
- フィルタプリセットの削除専用ドロップダウン項目を管理モーダルへ統合し、ドロップダウンは「適用」と「すべて表示」に集中させた。
- プリセット操作を `settings:scriptnote` の履歴スコープに接続し、Undo/Redo 復元後も開いているシナリオUIの一覧を再同期するようにした。
- シナリオの挙動タブに「ページ間を空ける」を追加し、既存ページ区切りの1行分余白をオン/オフできるようにした。
- メインペインの最後のタブを閉じたときは、フォルダツリーの選択を元にフォルダ表示へ戻し、選択ファイルがある場合は親フォルダと選択状態を保持するようにした。
- ワークスペース一覧のアクティブ表示を通常のフォルダツリー選択状態から分離し、ワークスペースクリックがファイル/フォルダ選択を奪わないようにした。
- 表示バージョンを `0.6.152` に更新した。

検証: 徹底チェック（テンプレート登録、テンプレート管理の追加/複製/並べ替え/削除、フィルタプリセット登録、フィルタプリセット管理の追加/複製/並べ替え/削除、プリセット適用、削除中プリセットの解除、履歴復元後の一覧再同期、ページ間隔オン/オフ、メインペイン最終タブクローズ、ワークスペース行クリックの操作チェーン追跡）、JavaScript構文チェック、シナリオ/ツールバー/タブ/ワークスペース/監査/バージョン回帰unittest、チェックボックス監査マニフェスト生成、差分空白チェックを実行。

## v0.6.151 (2026-06-29)

### fix: SQLite正本シートの通常編集と競合コピー復旧を強化

- SQLite正本シートの読み出し結果にエントリ版番号を含め、通常セル保存時にも `baseRevision` が古い保存を409で止めるようにした。
- SQLite正本の候補値更新・候補追加を行単位のSQLiteトランザクションに寄せ、同じエントリへの同時編集で上書き消失が起きにくいようにした。
- Dropboxが `_meldex_sheet.sqlite3` の競合コピーを作った場合、競合解消画面からSQLiteシートを行単位でマージできるようにした。
- SQLite競合マージでは、競合側の新しい行と新しい版の行を吸収し、元DB/競合DBおよびWAL補助ファイルを `_meldex/conflict-backups` に退避するようにした。
- 表示バージョンを `0.6.151` に更新した。

検証: SQLite正本シートの版競合/担当者制限/候補更新、Dropbox競合コピーのSQLite行単位マージ、settings-db API回帰、JavaScript構文、Python構文、バージョン整合性、差分空白チェックを実行。

## v0.6.150 (2026-06-29)

### fix: Dropbox上のSQLite正本デバッグリストを多人数編集に備えて保護

- SQLite正本のsettings-db更新でエントリ単位の版番号 `meldex_revision` を持たせ、`baseRevision` が古い保存は409で止めるようにした。
- デバッグリストのclaim/完了/失敗/進捗遷移をSQLiteトランザクション内更新に寄せ、同時claimで同じエントリを二重取得しにくくした。
- settings-db更新APIにも既存の編集ロック/Active Lock確認を通し、非管理者は担当エントリだけ編集できる設定をデバッグリストへ追加した。
- RAG側のSQLite読み取りにも `busy_timeout=30000` を設定し、ロック待ちによる検索漏れを減らした。
- SQLite正本を維持したままDropbox同期環境での競合対策方針を `app/docs/dropbox-sqlite-sheet-concurrency-plan-2026-06-29.md` に記録した。
- 表示バージョンを `0.6.150` に更新した。

検証: SQLite正本settings-dbの版競合/担当者制限/claim回帰、デバッグ自動化、settings-db API回帰、Python構文、差分空白チェックを実行。

## v0.6.149 (2026-06-28)

### fix: フォルダ読み込みの追加負荷を抑制

- ファイル形式フィルタのバーが表示されているだけでは、起動時に全ルートのファイル形式スキャンを走らせないようにした。
- 保存済みのファイル形式選択は、バックグラウンドスキャンを待たずにチップとして表示されるようにした。
- ホームフォルダ欄を既定では折りたたみ、折りたたみ中はホームフォルダの子一覧を読み込まないようにして、ソースフォルダ読み込みとの競合を減らした。
- 表示バージョンを `0.6.149` に更新した。

検証: フォルダツリー/グローバルフィルタ回帰、手動更新回帰、起動パフォーマンス回帰を実行。

## v0.6.148 (2026-06-28)

### fix: フォルダツリー読み込み中の負荷を軽減

- ファイル形式フィルタの初期スキャンで複数ルート・多数サブフォルダを一斉に読まないようにし、チップUIを先に描画してからバックグラウンドで順次更新するようにした。
- フォルダツリーで項目を選択した時、チャット対象パスの表示は即時更新しつつ、ソース候補との同期は次の描画後に回してツリー展開を待たせにくくした。
- 表示バージョンを `0.6.148` に更新した。

検証: フォルダツリー/グローバルフィルタ回帰、チャット対象表示回帰、分割JS構文回帰、チャット対象単体JS構文チェックを実行。

## v0.6.147 (2026-06-28)

### fix: CLI認証期限切れの案内を全CLIで補強

- Claude Code / Codex CLI / Gemini CLI の認証エラー判定に、トークン期限切れ・ログイン期限切れ・再認証要求の文面を追加した。
- Codex CLI がJSONではなく通常テキストで期限切れを出した場合も、通常の返答として表示せず、CLI側のログイン更新案内に変換するようにした。
- CLIチャットでは引き続きMeldexのLLM APIキーへ自動切替せず、各CLI自身のログイン・認証情報だけを使うことをテストで確認した。
- 表示バージョンを `0.6.147` に更新した。

検証: 全スクリプトファイル読み込み監査、CLI認証期限切れ/認証失敗/非フォールバック回帰テスト、チャットストリーム回帰、API設定、JavaScript構文、バージョン整合性、差分空白チェックを実行。

## v0.6.146 (2026-06-28)

### feat: ローカルLLMをチャットAIに追加

- AIチャットのプロバイダに「ローカルLLM」を追加し、Ollama / LM Studio などのOpenAI互換ローカルサーバーへ接続できるようにした。
- ローカルLLMでもMeldexツール実行を有効化し、既存の読み取り・作成・更新ツールを通じてMeldex内の情報を扱えるようにした。
- ローカルLLM送信では保存済みのOpenAI/Claude/Gemini APIキーを送信・読み込みせず、接続先も localhost / 127.0.0.1 / ::1 に制限した。
- 設定 > LLM にローカルLLMの接続先URL、既定モデル、Meldex操作の有効/無効を追加した。
- 表示バージョンを `0.6.146` に更新した。

検証: ローカルLLMのAPIキー非使用、Meldexツール呼び出し、ローカル接続先制限、チャット/設定UIの結合済みJavaScript構文、API設定、フロント回帰、バージョン整合性を確認。

## v0.6.145 (2026-06-28)

### fix: フォルダツリー選択とチャット対象表示を安定化

- フォルダツリーで対象フォルダや対象ファイルを選んでも、AIチャットの履歴を切り替えず、入力欄上の対象パス表示だけを更新するようにした。
- チャット上部の対象ドロップダウンを廃止し、送信前に現在の対象パスを確認できる表示へ置き換えた。
- フォルダツリー選択直後の最近使った項目更新を遅らせ、クリック時のスクロール位置復元を強めて、ツリーパネルが上下に揺れにくいようにした。
- 表示バージョンを `0.6.145` に更新した。

検証: 関連コードの構文チェック、フォルダツリー/チャット対象表示の回帰unittest、バージョン整合性チェック、差分空白チェックを実行。

## v0.6.144 (2026-06-28)

### fix: AIチャットの思考表示を復旧

- 通常LLMチャットで、ストリーム中の思考・推論イベントをライブ表示し、返答完了後は「思考内容」として折りたたんで残すようにした。
- Claude Code CLIチャットを `stream-json` 形式で受け取り、接続状態、利用枠確認、思考、ツール実行、本文を分けて表示できるようにした。
- CLIチャットの進行ログも思考表示欄へ集約し、応答本文が出るまで繋がっているか分からない状態を減らした。
- Claude Code CLIは引き続きAPIキーへ自動切替せず、CLI自身のログインだけを使う経路を維持した。
- 表示バージョンを `0.6.144` に更新した。

検証: 徹底チェック（通常LLM送信、Claude Code CLI送信、CLI標準出力、思考イベント、進行ログ、最終メッセージ保存、再表示時の折りたたみ、APIキー非継承/非フォールバックの操作チェーン追跡）、Claude Code実CLI接続確認（`apiKeySource: none`）、関連Python/JavaScript構文チェック、チャット表示回帰unittest、CLIチャット回帰unittest、チャットストリーム回帰unittestを実行。

## v0.6.143 (2026-06-28)

### fix: Claude Code CLIを期限切れログインから復旧

- Claude Code CLIの保存済みClaude.aiログイン期限が切れている場合、MeldexのLLM APIキーへ切り替えず、`claude auth login --claudeai` でCLI側ログインを更新する案内を出すようにした。
- CLIチャット子プロセスへ通常LLM APIキーを渡せる旧環境変数オプションを無効化し、CLI実行は各CLI自身のログインだけを使うように固定した。
- Windows環境でCLI子プロセスに `SystemRoot` / `WINDIR` / `ComSpec` / `SystemDrive` が欠ける場合に補完し、Bun/Node系CLIがネットワーク通信前に落ちないようにした。
- 期限切れClaude Codeログイン判定、Windows CLI環境補完、APIキー非フォールバック/APIキー非継承の回帰テストを追加・更新した。
- 表示バージョンを `0.6.143` に更新した。

検証: Claude CodeのClaude.aiログインを更新し、Meldexと同じ `claude -p --output-format text --input-format text --permission-mode acceptEdits --strict-mcp-config` 形式で `OK` 応答を確認。`meldex_api_cli_chat.py` 構文チェック、CLIチャット回帰unittest、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.142 (2026-06-28)

### fix: CLIチャットの従量課金API自動切替を撤回

- CLIチャットでClaude Code / Codex CLI / Gemini CLI が認証エラーになった場合に、保存済みLLM APIキーの通常LLMへ自動切替する処理を削除した。
- CLIチャット送信時にブラウザ保存APIキーを送らないように戻し、CLIチャットルーターにも通常LLM用のAPIキー/クライアント文脈を注入しないようにした。
- APIキーがリクエストに含まれていても、CLI認証エラー時に従量課金LLM APIへフォールバックしない回帰テストを追加した。
- 表示バージョンを `0.6.142` に更新した。

検証: 徹底チェック（CLIチャット送信、CLI認証エラー、APIキー保存済み/リクエスト混入時の非フォールバック、CLI環境変数、フロント送信body、CLIルーター注入文脈の操作チェーン追跡）、`Meldex.py` / CLIチャットAPI / CLIチャットJS構文チェック、CLIチャット回帰unittest、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.141 (2026-06-28)

### fix: CLI認証失敗後の通常LLM切替文脈を補完

- CLIチャットから通常LLMへ自動切替する際、通常チャットで必要なDB接続、保存済みAPIキー、LLMクライアント、ファイル操作文脈をCLIチャットルーターにも注入するようにした。
- Claude Code認証失敗後の切替で `get_db` が見つからず、通常LLMへの切替にも失敗する問題を修正した。
- 表示バージョンを `0.6.141` に更新した。

検証: 徹底チェック（CLIチャット送信、Claude Code認証エラー、通常LLM切替、予算DBチェック、チャット文脈注入、保存処理の操作チェーン追跡）、`Meldex.py` / CLIチャットAPI構文チェック、CLIチャット回帰unittest、チャットストリーム回帰unittest、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.140 (2026-06-28)

### fix: CLI認証失敗時の通常LLM自動切替を追加

- Claude Code / Codex CLI / Gemini CLI が認証エラーで終了した場合、設定済みの通常LLMへ自動で切り替えて返答を続けるようにした。
- CLIチャット送信時に、ブラウザに保存された通常LLM APIキーをMeldexサーバへ渡し、CLI子プロセスには渡さないまま切替処理だけで使うようにした。
- 通常LLM APIキーが未設定の場合も、CLIチャットをエラー終了表示にせず、設定が必要な内容を通常のチャット本文として案内するようにした。
- 表示バージョンを `0.6.140` に更新した。

検証: 徹底チェック（CLIチャット送信、Claude Code / Codex CLI / Gemini CLI 認証エラー、通常LLM APIキー有無、通常LLM切替、CLI環境変数、非認証エラー、保存処理の操作チェーン追跡）、関連Python/JavaScript構文チェック、CLIチャット回帰unittest、チャットストリーム回帰unittest、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.139 (2026-06-28)

### fix: LLM/CLIチャットの認証失敗経路を安定化

- CLIチャット、ワークスペースCLI中継、LLMクロスレビューで、通常LLM用のAPIキー環境変数をCLI子プロセスへ既定で引き継がないようにした。無効な `ANTHROPIC_API_KEY` 等がPC環境に残っていても、Claude Code / Codex CLI / Gemini CLI 自身のログイン状態を優先して起動する。
- CLIが `401 Invalid authentication` などの認証エラーを標準出力/標準エラー/Codex JSONLイベントへ出した場合、待機表示を残さず、CLI側のログイン更新が必要だと分かるエラーへ即時変換するようにした。
- 通常LLMチャットのAPIキー認証エラーも、英語の401文面ではなく、設定 > LLMでAPIキーとモデルを確認する案内へ変換するようにした。
- 表示バージョンを `0.6.139` に更新した。

検証: 徹底チェック（通常LLM送信、CLIチャット送信、ワークスペースCLI中継、LLMクロスレビュー、CLI標準出力/標準エラー/Codex JSONLエラー、認証エラー表示、CLIコマンド検出の操作チェーン追跡）、関連Python/JavaScript構文チェック、CLIチャット回帰unittest、チャットストリーム回帰unittest、ワークスペースCLI中継回帰unittest、LLMクロスレビュー回帰unittest、CLI実行ファイル検出、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.138 (2026-06-26)

### feat: ホームフォルダを含むRAG索引をマルチルート化

- FTS/ベクトル検索索引を `root_id + path` で管理する構成に変更し、ソースフォルダ、ホームフォルダ、追加ルートを同じ高速検索DBへ登録できるようにした。
- ホームフォルダや追加ルートの検索結果を `home:/...`、`outliner:/...` のような名前付きパスで返し、同名ファイルがあってもソース内ファイルと混同しないようにした。
- 物理 `.md` が存在しないSQLite実体シート行も、論理的なシート行ドキュメントとしてFTS、ベクトル検索、通常検索、チャットツール検索から参照できるようにした。
- 索引未完了時や短い検索語の通常検索でも、ホームフォルダとSQLiteシート行を検索対象に含めるようにした。
- `/api/rag/coverage` で、ソース、ホーム、追加ルートごとの対象ファイル数、SQLiteシート行数、FTS/ベクトル索引状態を確認できるようにした。
- 起動時のバックグラウンド索引再構築と保存時の差分更新を、マルチルートのルート一覧に追従させた。
- 表示バージョンを `0.6.138` に更新した。

検証: 徹底チェック（検索、意味検索、ハイブリッド検索、通常検索フォールバック、SQLiteシート行、ホームフォルダ差分更新、RAGカバレッジ、起動時/保存時索引更新の操作チェーン追跡）、関連Python構文チェック、FTS/ベクトル検索回帰unittest、チャット文脈回帰unittest、SQLiteシート保存回帰unittest、ナレッジ/アイデア回帰unittest、API回帰unittest、split asset回帰unittest、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.137 (2026-06-26)

### feat: ナレッジ活用とLLMレビューを創作支援向けに強化

- チャット内の決定・好み・作品ルールを、ユーザーが細かく設定しなくてもローカル抽出し、概念インデックスとナレッジ検索に反映するようにした。
- アイデア出し、ログライン、プロット、あらすじ、シーン案などの相談で、関連ナレッジと橋渡し概念を使った創作提案を行いやすくした。
- ナレッジ設定を「通常は設定不要」の構成に整理し、外部AIで詳しく抽出する設定だけを任意項目として表示するようにした。
- 外部AI抽出をOFFにした状態で、保存済み設定の空providerが既定AIとして扱われる経路を修正し、端末内の軽い抽出だけで動くようにした。
- LLMレビューに「創作ひらめきレビュー」「ログライン/企画レビュー」を追加し、根拠、影響、伸ばせる点、評価時の偏りを結果に残せるようにした。
- LLMレビュー画面を、対象・レビュー内容・今回の重点を先に選ぶ構成に整理し、詳細設定は折りたたみへ移した。
- LLMレビュー結果の先頭に、重要度、正本注意、根拠不足、伸びしろをまとめた要約を表示するようにした。
- `app/MeldexCloud` の部分生成状態を正規ビルドで修復し、配布同期テストが期待するCloud版生成物を揃えた。
- 表示バージョンを `0.6.137` に更新した。

検証: 徹底チェック（ナレッジ設定保存、チャット保存後の自動抽出、概念インデックス更新、チャット/CLI創作相談でのナレッジ注入、LLMレビュー実行、指摘採用、本文反映、Cloud配布生成の操作チェーン追跡）、関連JavaScript/Python構文チェック、Cloud静的ビルド、ナレッジ/LLMレビュー/Cloud同期/ルーター関連unittest、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.136 (2026-06-26)

### fix: Cloud版スマホ幅の競合解決画面を安定化

- Cloud版の競合解決ダイアログがスマホ幅で画面外へはみ出す経路を修正し、上下に並ぶ比較ペインと下部操作ボタンを画面内で扱えるようにした。
- 競合比較ペインをスマホ幅で上下2段に分け、長い差分でも元ファイルと競合コピーをそれぞれスクロールして確認しやすくした。
- 競合解決の下部操作ボタンにスマホ向けのタップ領域と安全領域の余白を確保した。
- Cloud/スマホ幅のE2Eに、競合解決ダイアログの画面内収まり、比較ペインの上下配置、操作ボタン高さの実測確認を追加した。
- 表示バージョンを `0.6.136` に更新した。

検証: 徹底チェック（Cloud競合解決ダイアログの表示、差分選択、解消、後回し、閉じる、スマホ幅レイアウト、E2E実測アサーションの操作チェーン追跡）、Cloudスマホ幅環境マトリクス、Cloudタブレット幅環境マトリクス、ローカルスマホ幅環境マトリクス、スマホ微細E2E 14ケース、関連unittest、JavaScript構文チェック、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.135 (2026-06-26)

### fix: スマホ版とCloud版のUI挙動レビューを強化

- スマホ設定のショートカット画面が空表示のまま残る経路を修正し、描画準備が遅れた場合も再試行して内容を表示するようにした。
- スマホ/Cloud版の詳細ドロワーの閉じるハンドルが開く途中や右端配置で画面外に出る問題を修正し、ドラッグで閉じる操作まで安定して検査できるようにした。
- Cloud版のUI検査で、更新日時フィルタを表示される画面だけで確認済みにするようにし、表示されない画面での誤検出を防いだ。
- Cloud/スマホ関連の回帰テストを現行の分割スクリプト構成とフォルダ一覧仕様に合わせて更新した。
- 表示バージョンを `0.6.135` に更新した。

検証: 徹底チェック（スマホ設定パネル、詳細ドロワー表示/ドラッグ終了、Cloud静的UI検査、分割スクリプト読込経路の操作チェーン追跡）、スマホ微細E2E 14ケース、Cloud静的E2E、Cloudスマホ幅環境マトリクス、関連unittest/pytest、展開後JavaScript構文チェック、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.134 (2026-06-25)

### feat: 選択メニューとシート表示設定を拡張

- テキスト選択時のポップアップを選択ハイライトより前面に表示し、コピー、切り取り、貼り付けのアイコン操作を追加した。
- テキストと画像の右クリックメニューから、Google検索とGoogle画像検索を開けるようにした。
- シートのエントリ名列メニューにも列操作を追加し、列ごとの昇順/降順指定とギャラリービューの並べ替え反映に対応した。
- カンバンビューに表示プロパティ設定を追加し、カンバン/タイムラインの表示プロパティ名と並び順をカード内で調整できるようにした。
- タイムラインの開始日時/終了日時の候補を日時プロパティだけに絞り、シート設定ダイアログとカンバンのグループ化メニュー表示位置を整えた。
- チャート配色に単色指定とテーマアクセント色を追加し、初期配色をテーマのアクセントカラーにした。
- Cloud版フォルダパネルでチェックボックスがファイルアイコンへ重ならないようにし、表示形式をリスト、グリッド、ウォーターフォールから選べるようにした。
- Cloud版フォルダパネルから不要なプレビュー/オプション位置設定を削除した。
- Xブックマーク保存でリツイート先の画像も保存対象に含め、初期並び順を投稿日時の降順にした。
- 表示バージョンを `0.6.134` に更新した。

検証: 徹底チェック（テキスト選択/右クリック検索、シート並べ替え、カンバン/タイムライン表示プロパティ、チャート配色、Cloud版フォルダ表示、Xブックマーク同期の操作チェーン追跡）、関連JavaScript/Python構文チェック、関連E2E、関連unittest、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.133 (2026-06-22)

### fix: Cloud版のDropboxワークスペースと診断情報を安定化

- Cloud版でDropbox認証後にワークスペース一覧を読み込めない問題を修正し、Dropbox内の `_meldex/workspaces.v1.json` でワークスペース設定を保存・読み込みできるようにした。
- Cloud版でワークスペースを追加する際の入力案内を、PCローカルの絶対パスではなくDropbox内フォルダを指定する文言に揃えた。
- 共有プロフィール用の `_Meldex_team.json` は、通常のファイル保存とは別に最新内容へマージして保存するようにし、起動中の自動同期だけで競合コピーが増えにくいようにした。
- 共有プロフィールの在席更新は10分以内なら書き込まないようにし、Dropboxへの不要な連続書き込みを抑制した。
- Cloud版で配線されていない経路が発生した場合に「Meldex内部でエラー」と表示されるのを避け、クラウド保存先の接続問題として案内するようにした。
- サポート送信・診断保存に、画面、設定タブ、直近の画面操作、API失敗、保存先状態、対象ファイルの拡張子/匿名ID/階層を含め、ファイル名・本文・パスは送らないまま原因追跡しやすくした。
- 表示バージョンを `0.6.133` に更新した。

検証: 徹底チェック（Cloud版ワークスペース読込/追加、共有プロフィール同期、Dropbox競合抑制、未対応API表示、サポート診断送信の操作チェーン追跡）、関連JavaScript構文チェック、関連unittest、Cloud静的ビルド/検証、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.132 (2026-06-22)

### fix: スマホの基本操作メニューを安定化

- スマホのコマンドパレットに閉じるボタンを追加し、タッチ操作だけで閉じられるようにした。
- 左端のヘルプ、設定、ゴミ箱、ユーザーの各ボタンを直接イベントで開くようにし、スマホで押しても反応しない経路を解消した。
- ヘルプメニューが画面左上に離れて表示されないよう、押したボタンまたは画面内の安全な位置へ表示するようにした。
- スマホのフォルダツリー全画面では検索欄横の更新ボタンを隠し、ヘッダー側の更新ボタンだけに整理した。
- フォルダツリーのヘッダー更新ボタンは、アイコン部分を押した場合も正しく更新処理に入るようにした。
- スマホでフォルダツリーを閉じた後に設定ダイアログのタブだけ残る問題を防ぎ、設定の各タブをダイアログ内の直接イベントで開くようにした。
- Google受信箱の取込結果に、取得、取込、重複、対象外、Google反映の件数を分けて表示するようにした。
- 表示バージョンを `0.6.132` に更新した。

検証: スマホ基本操作とGoogle受信箱表示の関連JavaScript構文チェック、関連unittest、スマホ/Cloud向けE2E、Cloud静的ビルド/検証、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.131 (2026-06-21)

### feat: Google受信箱からデバッグリストへ取り込めるようにする

- Google Apps Scriptの受信箱に届いたフィードバック/クラッシュ報告を、管理者PCのMeldexからデバッグリストへ取り込めるようにした。
- 設定のフィードバック画面に、受信箱URL、管理者トークン、Google受信箱取込ボタンを追加した。
- 取り込んだ報告にGoogle受信IDと受信元を記録し、再実行時に同じ報告が重複登録されないようにした。
- Google Apps Scriptテンプレートに管理者取込用API、取込済み記録、既存シートへの不足ヘッダー補完を追加した。
- スマホ版クラウドのフォルダツリー操作、サイドバー表示、Dropbox競合解消まわりの回帰を補強した。
- 表示バージョンを `0.6.131` に更新した。

検証: Google受信箱取込のPython/JavaScript構文チェック、関連unittest、Cloud静的ビルド/検証、Cloud生成JS構文チェック、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.130 (2026-06-21)

### fix: スマートシートの対象ソース未指定時に全走査しないようにする

- 通常スマートシートは対象フォルダまたは対象シートの明示を必須にし、未指定時は全ソース走査せず対象未設定として0件表示するようにした。
- `/api/smart-db` も未指定時の全ルート走査フォールバックを撤去し、指定されたフォルダ/シートだけを走査するようにした。
- デバッグリストとクイックメモのスマートシート定義に対象シートを明示し、既存のデバッグリスト定義も最新仕様へマージ更新するようにした。
- スマートシートの大量表示を通常結果/全件インデックスの両方で仮想行表示にし、ダッシュボード往復後も描画ウィンドウを復元するようにした。
- スマートシートE2Eに、対象シート指定、大量通常結果、大量全件インデックス、デバッグリストダッシュボードの回帰ケースを追加した。
- 表示バージョンを `0.6.130` に更新した。

検証: スマートシート対象ソース/仮想表示の徹底チェック、関連Python/JavaScript構文チェック、関連unittest、スマートシート/デバッグリスト実ブラウザE2E、`git diff --check` を実行。

## v0.6.129 (2026-06-21)

### feat(standalone): 単独アプリからOS既定アプリを個別設定できるようにする

- 各単独アプリの初回起動時に、対応するファイル形式をWindowsの既定アプリにするか確認するダイアログを出すようにした。
- ノート/シート/ビューワーでは、Markdown、CSV、画像形式、PDFを拡張子ごとに個別選択できるようにした。PDFだけMeldex Viewerにするなど、既存の画像アプリを維持したまま設定できる。
- シナリオ、タイマー、ボードも単独アプリ側のメニューから固有形式の既定アプリ設定を開けるようにした。
- WindowsのUserChoice保護が残る場合は、候補登録後にWindowsの既定アプリ画面へ誘導する経路を単独アプリにも追加した。

検証: 既定アプリ設定の徹底チェック（初回起動、メニュー再表示、拡張子個別選択、UserChoice保護、単独共通ランタイム、ボード単独ランタイム、本体/Cloudビューワー非表示分岐）、関連Python/JavaScript構文チェック、関連unittest、ビューワー/静的資産回帰、スマホ幅相当Playwright目視、`git diff --check` PASS。

### fix: ホーム画面追加案内でクイックメモ導線を分けて表示

- ホーム画面追加モーダルと設定画面の案内を、Meldex本体とクイックメモの2つに分けて表示するようにした。
- クイックメモは `quick-memo.html` を開いて、その画面を `Meldex Memo` としてホーム画面へ追加する導線を明示した。
- 公開トップとスマホ/設定マニュアルにも、Meldex本体とクイックメモを別々に追加する説明を反映した。

### feat: フォルダパネルにファイル操作ツールバーを追加

- フォルダパネルのツールバーに、追加/コピー/切り取り/貼り付け/削除の常設アイコンボタンを追加した。
- 追加メニューからフォルダ、ノート、シナリオ、シート、ボード、カレンダー、スマートシート、タイマーを現在のフォルダへ「無題」連番で作成できるようにした。
- コピー/切り取り/貼り付け/削除は既存の履歴、編集ロック、パス再配置処理を使い、編集ロック中のフォルダやフォルダを自分自身の中へ貼る操作を防ぐようにした。
- スマホではフォルダツールバーを2段化し、操作ボタンを横スクロールできるタッチ向け列にした。
- フォルダパネル/Cloud静的関連の静的テストを現行の分割ファイル構成に合わせて更新した。
- 表示バージョンを `0.6.129` に更新した。

検証: フォルダパネル操作の徹底チェック、追加/関連 unittest、JavaScript構文チェック、Cloud/スマホ向け静的ビルド確認、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.128 (2026-06-21)

### fix: 大規模シートを初期状態から全件スクロール可能にする

- 大規模シートの末尾に出していた「次の500件」「すべて表示」系の段階表示ボタンを撤去し、最初から全エントリをスクロール対象にした。
- 全件表示でも実DOMは画面周辺だけを生成する仮想スクロールに寄せ、行数だけでなく表示セル数が多い横長シートでも自動的に仮想描画へ入るようにした。
- シート内検索で未描画行にヒットした場合は、表示上限を増やして再描画するのではなく、仮想スクロールで該当行を表示するようにした。
- 新規エントリ追加E2Eの保存確認をSQLiteネイティブシートにも対応させ、物理ファイルの有無ではなくエントリAPIで保存済み状態を確認するようにした。
- 表示バージョンを `0.6.128` に更新した。

検証: 大規模シート全件スクロール化の徹底チェック、関連 unittest、JavaScript構文チェック、デスクトップ/タブレット/スマホ相当のシート微細挙動E2E、新規エントリ追加E2E、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.127 (2026-06-21)

### feat: シートアーカイブとログ長期保存を追加

- カレンダーとLLM利用ログを期限削除対象から外し、既存設定に365日などが残っていても削除しないようにした。
- 設定画面のデータベースメンテナンスで、カレンダー/LLM利用ログは「削除しない」と表示し、操作履歴だけ保持日数を設定できるようにした。
- シートの「…」メニューに「アーカイブ管理...」を追加し、表示に残す件数・経過日数・処理件数から候補確認、アーカイブ、検索、復元を実行できるようにした。
- アーカイブしたシート行は通常表示から外しつつ、シート内検索とシート横断検索では検索キーワードがある場合にアーカイブも含めて探せるようにした。
- SQLiteネイティブシートの `_meldex_sheet.cloud.json` からもアーカイブ対象行を退避し、同期後にアーカイブ行が復活しないようにした。
- アーカイブ/復元中のファイル移動で例外が起きた場合、移動済みファイルを戻すロールバックを追加した。
- 表示バージョンを `0.6.127` に更新した。

検証: 保持期限・シートアーカイブ・検索統合の徹底チェック、追加/関連 unittest、関連JavaScript/Python構文チェック、シート大規模表示回帰、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.126 (2026-06-21)

### fix: チャット添付と大規模シート表示を止まりにくくする

- チャット、共有チャット、CLI送信で、Ctrl+Vしたスクリーンショット画像をすぐ添付チップとして表示し、アップロード完了を待ってから送信するようにした。
- CLIチャットのフロント側自動停止を無効化し、CLIが長時間作業を分割継続できるマーカーと最大6回の自動継続を追加した。
- Codex CLI / Claude Code / Gemini CLI の実行エラー表示を短くし、Claude Codeの非対話実行とMCP設定引数を補強した。
- 大規模シートで「すべて表示」しても全行DOMを作らず、画面周辺だけを描画する仮想スクロールを追加した。
- 旧画像セルと画像プロパティのサムネイルを遅延読み込み化し、旧画像セルは `/api/thumbnail` 優先、失敗時 `/api/file-raw` フォールバックにした。
- 表示バージョンを `0.6.126` に更新した。

検証: チャット添付/CLI長時間実行/大規模シート表示の徹底チェック、追加 unittest、関連JavaScript/Python構文チェック、シート関連E2E単独ケース、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.125 (2026-06-20)

### feat: 共有チャットから管理者PCのCLIへ依頼できるようにする

- 共有ワークスペースのチャットに「CLIに依頼」ボタンを追加し、管理者PCが起動中かつ中継設定が有効な時だけ、Codex CLI / Claude Code / Gemini CLIへ依頼を渡して返答を同じルームへ投稿できるようにした。
- デスクトップ版に `/workspace-cli` APIと中継ワーカーを追加し、設定画面から対象ワークスペース、確認間隔、このPCの表示名を管理できるようにした。
- Cloud静的版でもDropbox上の `_chat/workspace-cli/requests` に依頼を書き込めるようにし、デスクトップ版の管理者PCがその依頼を処理できるようにした。
- 管理者以外の依頼はCodex CLIの読み取り専用実行に限定し、Meldex API/MCPの認証情報を渡さず、編集系MCPツールの表示・実行やrole引数偽装をサーバ側で拒否するようにした。
- 表示バージョンを `0.6.125` に更新した。

検証: ワークスペースCLI中継と非管理者読み取り専用制限の徹底チェック、追加 unittest、既存CLIチャット/ルータ/セキュリティ回帰、JavaScript/Python構文チェック、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.124 (2026-06-20)

### feat: Dropbox競合コピーをデスクトップ版でも検出して解消できるようにする

- デスクトップ版に `/cloud/conflicts`、`/cloud/conflict-detail`、`/cloud/conflict-resolve` 互換APIを追加し、Dropbox同期後に残った競合コピーを既存の競合解消UIへ表示できるようにした。
- 競合解消時は「元ファイルを残す」「競合コピーを残す」のどちらでも `_meldex/conflict-backups` に直前ファイルを保存し、編集ロック/自動編集中ロック中の対象は上書きできないようにした。
- デスクトップ起動時にも競合コピー監視を開始し、Dropbox容量確認はCloudモードだけで実行するよう分けた。
- 自動編集中ロックの既定リースを5分へ延長し、Dropbox同期遅延中に別端末がすぐ編集へ入る余地を減らした。
- 表示バージョンを `0.6.124` に更新した。

検証: 競合コピー検出/解消範囲の徹底チェック、追加 unittest、既存編集ロック/アクティブロック/Cloud競合UI回帰、JavaScript/Python構文チェック、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.123 (2026-06-20)

### feat: Dropbox共有時の同時編集を自動ロックで抑止

- 共有ワークスペース内 `_meldex/active_locks.json` に短期リースを保存し、別端末が編集中のファイル/シート/ボード/フォルダ操作を保存前にブロックするようにした。
- デスクトップ版とCloud静的版の両方で `/active-lock` APIを追加し、保存系APIにはリーストークンをヘッダで渡して同じ端末の保存だけ通すようにした。
- 既存の管理者用「編集ロック」と併用できるよう、明示ロックの判定後に自動編集中ロックを確認する構成へした。
- 共有JSONには生トークンを保存せず、トークンハッシュのみを保存するようにして、共有フォルダ閲覧者によるアプリ内ロック迂回の余地を減らした。
- Cloud静的版の内部APIエラーで423などのステータスを保持し、ロック中の保存失敗が通常の未対応エラーに見えないようにした。
- 表示バージョンを `0.6.123` に更新した。

検証: 共有アクティブロック範囲の徹底チェック、追加 unittest、既存編集ロック unittest、JavaScript/Python構文チェック、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.122 (2026-06-20)

### fix: スマホの左右ドロワーをスワイプで閉じられるようにする

- スマホのフォルダツリー全画面に閉じるハンドルを追加し、ハンドルのタップまたは左方向ドラッグで閉じられるようにした。
- シートのエントリやボードリンクカードを表示する右側の詳細ドロワーに閉じるハンドルを追加し、ハンドルのタップまたは右方向ドラッグで閉じられるようにした。
- 入力欄、ボタン、メニュー、本文編集欄の操作はドラッグ閉じの対象から外し、通常のタップ/編集/縦スクロールと競合しないようにした。
- 右側の詳細ドロワー幅をスマホ用の92vw指定へ揃え、以前の72vw上書きで狭く見える状態を解消した。
- 表示バージョンを `0.6.122` に更新した。

検証: スマホ左右ドロワー修正範囲の徹底チェック、スマホ実ブラウザE2EとAI目視、JavaScript構文チェック、関連 pytest / unittest、Cloud静的ビルド検査、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.121 (2026-06-20)

### fix: スマホ操作導線を右クリックなしで使えるよう整理

- ローカルスマホ表示でも、シートのエントリやボードリンクカードを右サイドドロワーで開けるようにした。
- スマホ用メニューにヘルプを追加し、注釈、ヒストリー、チャット、ボード詳細などは表示中のペイン経路を優先して開くようにした。
- お気に入り行に「…」メニューを追加し、右クリックだけでなくタップ/長押しから開く、削除、お気に入り解除を実行できるようにした。
- フォルダツリーのファイルメニューに「アプリで開く」を追加し、案内文から右クリック前提の表現を減らした。
- シート、ボード、お気に入りのスマホ用タップ領域とサイドドロワー幅を調整し、AI使用量設定は予算APIが応答しない場合も設定パネルを表示できるようにした。
- 表示バージョンを `0.6.121` に更新した。

検証: スマホUX修正範囲の徹底チェック、関連モバイルE2E、JavaScript構文チェック、関連 unittest / pytest、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.120 (2026-06-20)

### feat: Cloud版シートをデスクトップ版と同じ操作体系に近づける

- Cloud静的版の新規シートを、行ごとのMarkdownではなくDropbox上の単一シートストアへ保存するようにした。
- Cloud静的版のシート作成、エントリ作成、セル更新、CSV取込、検索、置換、自動リンク辞書、Quick Memo保存が、Cloudシートストア内の行データを対象にするようにした。
- デスクトップ版でCloudシートストアを含むシートを開いた時、更新された行データをSQLiteへ取り込んで表示・検索・編集できるようにした。
- Quick MemoのCloud保存を、保存先シート指定時もシート行として追加する経路に揃え、スマホ共有のURL、共有タイトル、共有元、メモIDをプロパティとして残すようにした。
- Cloud版/デスクトップ版のシート保存方式、検索、置換、Quick Memo、復旧、移行、バージョン管理の説明をマニュアルと配布手順へ反映した。
- 表示バージョンを `0.6.120` に更新した。

検証: Cloud/デスクトップシート同等化の徹底チェック、関連 unittest、JavaScript/Python構文チェック、Cloud静的ビルド検査、バージョン整合性チェック、`git diff --check` を実行。

## v0.6.119 (2026-06-20)

### docs: 最新シート仕様とクイックメモ導線を配布元マニュアルへ反映

- 配布元テンプレート `MeldexHome/マニュアル` を生成済みの現行マニュアル一式と同期し、デスクトップ版ビルドに最新マニュアルが入るようにした。
- シートマニュアル、FAQ、ファイル関連Q&A、移行手順、バージョン管理、チャットLLMツールガイドを、現行の「新規シートはSQLite実体、既存Markdownシートは互換対応」仕様に更新した。
- クイックメモの新規マニュアルを追加し、保存先シート指定、スマホ共有、URL/共有タイトル/共有元プロパティ、保留キュー、音声/ペン入力の使い方を説明した。
- スマホ・タブレット利用マニュアルと総合マニュアルに、クイックメモのホーム画面追加と共有メニューからシートへ保存する導線を追加した。
- デスクトップ版ビルド、exeビルド、Cloud静的ビルドがQuick Memo関連ファイルとマニュアルテンプレートを取り込む経路を確認した。
- 表示バージョンを `0.6.119` に更新した。

検証: 徹底チェック（シート保存方式、Quick Memo保存先、スマホ共有、配布元マニュアル、デスクトップ/Cloud静的ビルド対象を関数・ビルド経路で追跡）、全スクリプトファイル全行スキャン、関連 unittest / 構文チェック / バージョン整合性チェックを実行。

## v0.6.118 (2026-06-20)

### feat: クイックメモから任意シートへ保存できるようにする

- クイックメモ画面に保存先シート欄と新規メモボタンを追加し、スマホから連続してシートへメモを追加しやすくした。
- 共有メニューから開かれたタイトル・本文・URLをクイックメモへ取り込み、URL、共有タイトル、共有元をシート列と本文に残すようにした。
- PWA manifest に共有ターゲットを追加し、インストール済みQuick Memoがスマホの共有先としてページタイトル・本文・URLを受け取れるようにした。
- クイックメモ保存APIを、従来の「メモ」シート既定を維持したまま、指定された既存シートまたは新規SQLite実体シートにも保存できるようにした。
- 保存先シートを指定した場合に古い保存パスへ引っ張られて別シートの既存メモを更新しないようにし、通常ノートフォルダや既存の素材フォルダを誤ってシート化しないガードを追加した。
- 表示バージョンを `0.6.118` に更新した。

検証: クイックメモ保存 unittest 11件PASS、Quick Memo/DB props回帰 unittest 7件PASS、`node --check` 2件PASS、`py_compile` PASS。

## v0.6.117 (2026-06-20)

### fix: SQLite実体シートの作成経路をさらに補強

- Xブックマーク同期とクイックメモ保存で、物理 `.md` のないSQLite実体シート行を読み書きできるようにし、同名行の重複判定や既存メモ更新もSQLite行を基準にした。
- チャットツールやCSV/XLSX取込で、既存フォルダノートをシート化する場合にも `storage: sqlite` と `schema_version` を補完し、行をSQLite実体として保存するようにした。
- WebClip、ベータ利用統計、設定DBロールフロー、エクスポートDB、LLM文脈、自動リンク、バックリンク、メディア参照が、物理 `.md` のないSQLite実体シート行を取りこぼさないようにした。
- 制作管理の標準参照データに「ネーム」「背景作画」「ページ全体」を追加し、作業時間計算で既定倍率が欠けて1.5時間になる問題を修正した。
- 表示バージョンを `0.6.117` に更新した。

検証: 徹底チェック（未確認だったXブックマーク、クイックメモ、チャットツール、CSV/XLSX取込、WebClip、ベータ利用統計、設定DBロールフロー、エクスポートDB、LLM文脈、バックリンク、メディア参照、制作管理参照データを関数チェーンで追跡）、関連 unittest 205件PASS、`py_compile` PASS、`git diff --check` PASS。

## v0.6.116 (2026-06-20)

### fix: SQLite実体シートの周辺参照漏れを修正

- SQLite実体シートの画像プロパティがメディア参照再構築に含まれず、参照中画像の削除判定から漏れる可能性を修正した。
- リレーション列を使ったエントリ名自動生成で、参照先がSQLite実体シートでもIDからエントリ名を解決できるようにした。
- 自動リンク辞書が、物理 `.md` のないSQLite実体シートのエントリ名とルビも候補に含めるようにした。
- 物理 `.md` を作らない新規シートの周辺参照経路を検証する回帰テストを追加した。
- 表示バージョンを `0.6.116` に更新した。

検証: 徹底チェック（SQLite実体シートのメディア参照再構築、リレーション自動命名、自動リンク辞書、物理 `.md` なしのエントリ作成を関数チェーンで追跡）、関連 unittest 224件PASS、`py_compile` PASS、`node --check` 2件PASS、`git diff --check` PASS。`test_meldex_split_assets.py` は既知のマニュアル欠落1件で失敗し、今回差分とは無関係。

## v0.6.115 (2026-06-19)

### perf: 新規シートをSQLite実体で作成するよう変更

- 新規作成するシートを、行ごとのMarkdownファイルではなくSQLiteを正本とする保存形式に変更した。新規シートのフォルダノートには `storage: sqlite` を付け、エントリは論理パスとして扱う。
- SQLite実体シートでは、エントリ作成、値追加、本文更新、名称変更、移動、削除、公開フォーム送信、CSV/XLSX取込、チャットツール操作をSQLite行へ直接反映するようにした。
- 既存のMarkdown互換シートは従来通り読み書きできるように維持した。`storage: sqlite` がないシートでは既存 `.md` エントリをSQLiteへ取り込みつつ、互換経路を残す。
- SQLiteだけに存在するエントリが、シート同期や検索、詳細表示、名称変更、自動命名、削除で取りこぼされないようにした。
- 仕様定点観測の設計意図とSQLite実体シート移行計画を更新した。
- 表示バージョンを `0.6.115` に更新した。

検証: 徹底チェック（新規シート作成、エントリ作成、値追加、本文更新、名称変更、移動、削除、公開フォーム送信、CSV/XLSX取込、チャットツール操作、既存Markdown互換シート、壊れたMarkdown行の同期除外を関数チェーンで追跡）、関連 unittest 161件PASS、`py_compile` PASS、`node --check` 2件PASS、`git diff --check` PASS。

## v0.6.114 (2026-06-19)

### fix: 起動直後のフォルダツリー再描画を安定化

- 起動直後の補助的なフォルダツリー更新が、初回表示済みのソースフォルダツリーを再描画して、展開済みフォルダの中身が一時的に消えて見える問題を修正した。
- フォルダツリー読み込みに世代管理を追加し、古い `/outliner-roots` 応答が後から戻ってきても表示済みツリーを上書きしないようにした。
- 起動完了直後の自動更新は、直前にツリーを描画済みなら再描画を省略するようにした。手動の更新ボタンは従来通り最新状態を強制取得する。
- シート本体APIの計測を、通信完了とJSON解析に分けて記録するようにした。大量シートではサーバー処理、転送、JSON解析、描画のどこで時間がかかっているかを切り分けやすくした。
- シート本体APIを直接JSONレスポンス化し、SQLite読み込みでは表示に必要な列だけを取得するようにして、大量シートの読み込み負荷をさらに下げた。
- 同一パネルで同じシートを開く重複処理を合流させ、起動直後のシート復元で同じ読み込みが重ならないようにした。
- 表示バージョンを `0.6.114` に更新した。

検証: 徹底チェック（起動直後のフォルダツリー初回表示、起動完了後の補助更新、手動更新、通常のファイル操作後更新、古いAPI応答の順序逆転、シート本体API、SQLite取得、同一シート重複読み込みを関数チェーンで追跡）、関連 unittest 86件PASS、`node --check` 4件PASS、`py_compile` PASS、`git diff --check` PASS。

## v0.6.113 (2026-06-19)

### perf: 起動直後の大量シート読み込みを計測・高速化

- 起動直後のフォルダツリー、シートメタデータ、シート本体読み込み、シート描画に計測ログを追加し、DevToolsで `[Meldex perf]` と絞り込めば遅い区間を確認できるようにした。
- シート本体APIの内部処理時間をレスポンスヘッダーへ載せ、SQLite取得、行変換、描画指示のどこで時間がかかっているかを切り分けられるようにした。
- 大量シートで、SQLiteから取得した各行の `file` パス生成時にクライアントパス変換を行数分繰り返していた問題を修正した。シートフォルダのパスを1回だけ変換し、各行はファイル名を連結する方式にして、2361件シートで発生していた100秒級の行変換待ちを解消した。
- 同じシートに対する起動直後の重複GETをまとめ、初回SQLite生成が必要な場合もシート単位で二重生成されないようロックを追加した。
- 表示バージョンを `0.6.113` に更新した。

検証: 徹底チェック（起動直後のシート復元、フォルダツリー読み込み、シート本体API、SQLite初回生成、既存SQLite読み込み、行パス生成、シート移動後の表示パスを関数チェーンで追跡）、関連 unittest 63件PASS、`py_compile` PASS、`git diff --check` PASS。

## v0.6.112 (2026-06-19)

### fix: 検索置換と大量シート表示を安定化

- ノート、シナリオ、ボード、シートの検索・置換で、検索語を入力している途中には検索を走らせず、IME確定、Enter、前後移動、置換操作で検索語を確定してから検索するようにした。
- シート検索で、まだ「次の500件」で表示していない行にヒットした場合も、対象行まで描画範囲を広げてセルを表示・ハイライトできるようにした。
- 大量行シートで「次の500件」を何度も押した時に、描画上限が初期値へ戻って表示件数が巻き戻る問題を修正した。チャンク描画の待機にもタイムアウトを設定し、読み込み中表示が残り続けないようにした。
- 大量行シートの末尾操作に「すべて表示」ボタンを追加し、必要な場合は残りの行を一括表示できるようにした。
- 表示バージョンを `0.6.112` に更新した。

検証: 徹底チェック（検索語確定、置換、未表示行ヒット表示、「次の500件」、「すべて表示」、チャンク描画完了の各経路を関数チェーンで追跡）、関連 unittest 80件PASS、`node --check` 6件PASS、`git diff --check` PASS。

## v0.6.111 (2026-06-19)

### feat: チャット入力とシート操作まわりのUIを改善

- チャット入力欄やテキスト選択時のポップアップが、選択中テキストの矩形を避けて表示されるようにした。画面端では上下左右の空き領域を試し、必要に応じてサイズ制限をかけてビューポート内に収める。
- チャット入力欄で、クリップボード内の画像/PDFを Ctrl+V で添付できるようにした。添付済み画像/PDFは入力欄の上の添付バーにサムネイルまたはファイルアイコンとして表示される。
- ノート、シナリオ、ボード、シートで Ctrl+F/Ctrl+H の検索・置換入口を揃えた。ノート/シートの検索バーは右上にコンパクト表示し、シナリオ/ボードも既存のフロート検索置換UIへ接続した。
- シートの大量行表示ボタンを「500件追加」から「次の500件」に変更した。
- シート設定ダイアログが画面幅いっぱいに広がりすぎないよう最大幅を制限し、集計行、エントリ名列の固定、ステータス機能、サムネイルサイズ、枠線、条件付きカラーをシート設定から変更できるようにした。エントリ名列のポップアップメニューから表示設定項目は外した。
- 左右レールのアイコンボタンとアイコンサイズを一回り大きくした。
- 表示バージョンを `0.6.111` に更新した。

検証: 関連 unittest 117件PASS、実ブラウザE2E 2件PASS（チャット選択ポップアップ、シート選択フロート）、`node --check`、`git diff --check` PASS。既存の `MeldexHome` マニュアル欠落により、マニュアル存在確認系テストの一部は未解決の既知失敗として切り分けた。

## v0.6.110 (2026-06-19)

### fix: シートSQLite本体化後の周辺同期漏れを修正

- シートの本体をSQLiteに置く仕様自体は、大量行の読み込み・検索・更新性能を改善する方針として妥当と判断した。一方で、互換用 `.md` を直接更新・移動・削除する周辺APIが残っていたため、SQLite側への追従処理を追加した。
- 設定ダイアログ/設定DB補助、検索と置換、通常ファイル保存、CSV/XLSX取込、Web Clipper、チャット/LLM/MCPツール、ベータ利用統計/フィードバック分類、カレンダーDB/外部同期、フォルダツリー、エクスポートDB再生成、重複整理の各経路で、シート行の作成・更新・削除・移動・複製後にSQLiteが同期されるようにした。
- シートフォルダの移動・リネーム・複製時に、SQLite内の `file_path` が旧パスのまま残らないようリベース処理を追加した。複製されたシート行は新しいIDに更新し、カレンダーイベントの外部IDも複製元と衝突しないようにした。
- 非シートフォルダやゴミ箱内の通常Markdownに対して、同期ラッパーが誤って `_meldex_sheet.sqlite3` を作らないようにガードを追加した。
- SQLite追従の回帰テストを34件に拡充した（検索置換、通常保存、インポート、Web Clipper、チャット書き込み/移動/削除、設定DB、ベータ利用統計、エクスポート再生成、フォルダ移動、非シート誤作成防止など）。
- 表示バージョンを `0.6.110` に更新した。

検証: SQLiteストア専用テスト34件PASS、APIスモーク34件PASS、ルーター登録39件PASS、カレンダー/チャット/設定/エクスポート/単独アプリ/シートUI/リンク/クラウド周辺の対象代表テスト234件PASS、`Meldex.py`/`meldex_chat_tools` import PASS、全体テスト2250件実行。全体テストでは既存の未解決70件（failures=54, errors=16、配布物同期/マニュアル欠損/静的期待不一致など）を確認したが、今回のSQLite変更とは無関係。

## v0.6.109 (2026-06-19)

### perf: シートのSQLite完全プライマリ化（大量エントリシートの高速化）

- シート横断検索（スマートシート）で、全エントリの.mdファイルを1件ずつ読み込んでいたのをSQLiteへの一括クエリに置き換えた。2000件超のシートで検索が大幅に高速化される。
- シートフォルダの判定処理で、エントリの.mdファイルを開いて中身を確認していたのを、SQLiteファイルの存在チェックに置き換えた。フォルダツリーの展開が高速化される。
- シートへの書き込み順序を「SQLiteに書いてから.mdに同期」に統一した（全12箇所）。従来は.mdを先に書いてからSQLiteに同期していたため、Dropbox同期フォルダなどファイルI/Oが遅い環境で体感速度に影響していた。
- 表示バージョンを `0.6.109` に更新した。

検証: 徹底チェック（8種のユーザー操作について関数チェーンを最深部まで追跡。スマートシート検索→SQLiteクエリ→プロパティ抽出→フィルタリングの全パス、フォルダ判定の新旧形式フォールバック、書き込み12箇所の権限チェック→SQLite→.md順序、エラーハンドリングの整合性を確認。問題なし）、SQLiteストアテスト21件全通過、全体テスト2237件実行（既存ベースラインと差分なし）。

## v0.6.108 (2026-06-19)

### fix: シートSQLite同期漏れの追加修正3件

- シートのエントリ名を変更した際に、SQLite側が旧名のまま残り、シート一覧に旧名のエントリが表示され続ける問題を修正した。名前変更時にSQLiteの旧レコードを削除してから新名で再登録するようにした。
- フォルダツリーからシートのエントリファイルを削除（ゴミ箱移動）した際に、SQLiteからレコードが削除されず、シート一覧にゴースト行が残る問題を修正した。ゴミ箱移動前にSQLiteから削除するようにした。
- ゴミ箱からシートのエントリファイルを復元した際に、SQLiteに再登録されず、シート一覧に表示されない問題を修正した。復元後にSQLiteへ再登録するようにした（フォルダツリーからの復元・ゴミ箱画面からの復元の両方に対応）。
- テストを21件に拡充した（エントリ名変更のSQLite更新、ゴミ箱移動→SQLite削除→復元→SQLite再登録）。
- 表示バージョンを `0.6.108` に更新した。

検証: 徹底チェック（5エージェント並行で全書き込み・削除・インポート・フロントマター更新・フロントエンドリフレッシュの全パスを監査。エントリ名変更・フォルダツリー削除・ゴミ箱復元の3箇所でSQLite同期漏れを検出し修正。外部インポート・フォルダノート更新・旧形式シートなど同期不要と判定したパスも根拠付きで確認）、SQLiteストアテスト21件全通過、`git diff --check` PASS。

## v0.6.107 (2026-06-18)

### fix: シートSQLite移行の徹底チェックで発見した2件のバグ修正

- シートの全エントリを削除した後にシートを再度開くと、削除済みのエントリがファイルから復元されてしまう問題を修正した。SQLiteの初期化済みフラグを導入し、一度でも書き込みが行われたシートでは再スキャンを抑制するようにした。
- Googleカレンダーへイベントをプッシュした際に、プッシュ結果（イベントIDの書き戻し等）がSQLiteに反映されない問題を修正した。
- SQLite直接書き込みの全パスを網羅するテストを19件に拡充した（書き込み・削除・リネーム・カレンダー・復活防止・populatedフラグ等）。
- 表示バージョンを `0.6.107` に更新した。

検証: 徹底チェック（全書き込みパス15箇所以上のSQLite同期呼び出し確認、全削除パス8箇所のSQLite同期要否判定（シートエントリ削除2箇所は同期済み、ゴミ箱操作・エラークリーンアップ・メディア削除は対象外）、カレンダー同期4箇所の書き込み/リネーム/新規作成の全パス確認（part02のプッシュパスにSQLite同期漏れを発見し修正）、_ensure_sqlite_populated の行数カウント方式の脆弱性分析（最終エントリ削除→行数0→.md再読込で復活するバグを発見しpopulatedフラグで修正）、_connect のフォルダ分離確認（各シートフォルダに独立したSQLiteファイルで問題なし））、シート関連テスト57件全通過、`git diff --check` PASS。

## v0.6.106 (2026-06-18)

### perf: シートの読み書きをSQLite直接操作に移行

- シートを開く際に全エントリファイルの同期処理を毎回実行していたのを廃止し、初回のみの移行チェックに変更した。2回目以降はSQLiteから直接読み込むため、大量エントリ（2000件超）のシートでも即座に表示される。
- シートの値を追加・更新・削除した際に、SQLiteへ直接書き込むようにした。従来は.mdファイルに書いてから同期していたが、.mdファイルの存在やファイルサイズ確認を経由しない直接書き込みに切り替えた。
- シートのエントリ削除・値のリネーム時にSQLiteからも即座に反映されるようにした。従来はSQLite側の更新が漏れていた。
- Google カレンダー同期で取り込んだイベントがSQLiteに反映されない問題を修正した。同期後にシートを開き直さなくても最新のイベントが表示される。
- .mdファイルへの読み込みフォールバック（SQLite読み込み失敗時に低速な.md全走査に自動切り替えする処理）を廃止した。エラーが隠蔽されず、問題を早期に発見できるようにした。
- 表示バージョンを `0.6.106` に更新した。

## v0.6.105 (2026-06-18)

### fix: フォルダビューの選択メニュー残留とシート読み込み速度の改善

- フォルダビューでファイルを選択した後、ダブルクリックで開くとフローティングメニュー（選択操作バー）が画面に残り続ける問題を修正した。アイテムを開く前に選択状態をクリアするようにした。
- 大量エントリ（2000件超）のシートを開く際に30秒以上かかっていた問題を改善した。シートを開くたびに全エントリファイルの更新チェックを行っていた処理に、フォルダ変更検知による短絡判定を追加し、変更がなければチェックをスキップするようにした。
- シートの高速読み込み処理が内部エラーで失敗した際、エラーが隠蔽されて低速な読み込みに無言で切り替わっていた問題を修正し、ログに記録するようにした。

## v0.6.104 (2026-06-18)

### perf: ノートを開く速度を改善

- ノートを開く際に最大4回発生していたAPI呼び出しを1回に削減し、表示までの待ち時間を短縮した。ファイル内容の取得時にメタデータ（作成日時・更新日時・サイズ）も同時に返すようにし、ビューワーや詳細パネルが同じデータを再取得しないようにした。
- 表示バージョンを `0.6.104` に更新した。

検証: 徹底チェック（openPage→_updateLinkedPreview→_syncDetailPanel→_showFileInfoInDetailPanelの関数チェーン全追跡、preloadedData/preloadedMeta未指定時のフォールバックfetch確認（既存呼出元11箇所の後方互換性）、サーバー未更新時にメタデータフィールドがundefinedになるケースの検出と修正（truthyだが中身がundefinedのオブジェクトをフォールバック条件で正しく判定）、file_etagの内部stat二重呼び出しは軽微で変更不要と判断）、全体チェック（全テスト2195件PASS、既存失敗73件は変更前のベースライン内）、`git diff --check` PASS。

## v0.6.103 (2026-06-18)

### fix: ツリー更新とパネル読み込みの改善

- ダイアログ内のチェックボックスをドラッグで連続してオン・オフ切り替えできるようにした。
- ウィンドウの幅を変えた時にサイドバーの幅が連動して変わってしまう問題を修正した。
- エントリを開いた時、すべてのセルに「＋候補値」ボタンが表示されていたのを、カーソルを乗せた時のみ「＋」アイコンで表示するようにした（タッチ端末では常時表示）。
- シートのセルの右クリックメニューに「候補値を追加」を追加した。
- シートで更新ボタンを押した際に全エントリが表示されて重くなる問題を修正し、表示中の範囲のみを保持するようにした。
- フォルダやシートを開いた時に「チャットを読み込み中」と不要に表示される問題を修正した。チャットの復元処理がパネル更新のたびに再実行されていた。
- フォルダビュー以外の画面（シート・ノート・ボード等）で一括処理フロートが残ってしまう問題を修正した。ビュー切替時の非表示処理とビュー状態の二重ガードで確実に消えるようにした。
- シートで「切り詰め」モードにしても列の内容が切り詰められず、列幅いっぱいに広がって列幅の調節もできなくなる問題を修正した。セル内のテキストが列幅を超えても正しく省略表示（…）されるようにした。
- シートで列を削除した後の再描画が遅い問題を改善した。削除後に全エントリをディスクから再読み込みしていた処理を、画面の再描画だけで済むように最適化した。モーダルからの削除時に二重描画していた問題も修正した。
- 「列の表示と順序」ダイアログから大量の列を削除すると、シートのエントリが一つも表示されなくなることがある問題を修正した。再描画時にシートデータが参照できなくなるケースでディスクから再取得するようにした。
- フォルダノートにシート種別の識別情報がないシートで、エントリが表示されない問題を修正した。フォルダノートだけでなくエントリファイルの内容からもシート形式を自動判定するようにした。
- ゴミ箱を空にする際、一部のファイルが削除できなくても残りのファイルは続けて削除し、削除できなかったファイルだけを報告するようにした。従来は途中で停止していた。
- ゴミ箱の削除件数が実際のエントリ数と一致するようにした（メタデータファイルをエントリと対で管理）。
- ゴミ箱操作が保存失敗カウントに影響しないようにした。
- リスト表示でサムネイルの位置がずれることがある問題を修正した。
- E2Eテストの安定性を改善した（シナリオ縦書き折り返し・シートセル操作・シェルUI検証・外部CLI設定のカバレッジ記録）。
- 表示バージョンを `0.6.103` に更新した。

検証: 徹底チェック（チェックボックスドラッグのテーブル行選択との競合・モーダルスコープ確認、サイドバーflex設定とリサイズハンドラの互換性、候補値ボタンの基本CSSとの重複なし確認、コンテキストメニュー追加の既存呼出元への影響なし確認、更新時のペイン状態保持パスの追跡、チャットリストア初回のみ実行ガード、一括処理バー: 呼出元15箇所の全追跡・openPage等がshowView前にstate.view上書きする問題を発見し条件をviewName基準に修正・ビュー切替のバンドル版同期確認、切り詰めモード: 初回CSS修正では効果なし→根本原因調査で`table-layout:fixed`と`width:max-content`の組み合わせ時にブラウザが`white-space:nowrap`コンテンツ幅でテーブル幅を算出し列が膨張する問題を特定→`width:min-content`で列幅を指定値に固定・メインビュー/分割ペイン/多列スクロールの実ブラウザ検証PASS、列削除高速化: コンテキストメニュー/モーダル/Undo/Redo全4パスの関数チェーン追跡・全7ビューモードのfilterDeletedDbProperties適用確認・ビュー設定/メタデータ/バックエンド保存の状態整合確認・updatePropertyLayoutForDelete非同期化の競合リスク分析（確認ダイアログによる直列化で問題なし）、ゴミ箱一括削除のメタファイル整合性・部分失敗・スナップショット失敗のunittest PASS、大量列削除後のエントリ消失: 列削除→再描画の関数チェーン全追跡・シートデータ参照の6シナリオ検証（通常/グローバル/データ消失/コンテキスト引き渡し/null/連続削除）・元に戻す操作パスの確認・シート形式判定のフォルダノート×エントリファイル全分岐トレース・呼出元8箇所への影響なし確認・素材管理表シート2361件のAPI復帰確認）、列関連unittest 27件PASS、API回帰unittest PASS、全体テスト2195件PASS（既存失敗73件は変更前のベースライン内）。

## v0.6.102 (2026-06-17)

### fix(sheet): 列削除の保存失敗と削除済み列の復活を防ぐ

- 「列の表示と順序」ダイアログや列ヘッダーメニューから列を削除した際、保存に失敗した場合は削除成功扱いにせず、列一覧とローカル表示設定を元に戻すようにした。
- 削除した列を `deletedProps` として保存し、候補値データが残っていても列一覧・列ヘッダー・関連設定の候補に復活しないようにした。同名列を追加した場合は削除済み状態を解除して値を再利用できる。
- シート再読込時に、保存待ちの表示設定が古いバックエンド設定で上書きされないようにした。
- シートメタデータ保存時の容量不足などの書き込み失敗を、原因不明の HTTP 500 ではなく原因コード付きエラーとして返すようにした。
- 表示バージョンを `0.6.102` に更新した。

検証: 徹底チェック（列管理ダイアログ削除、列ヘッダーメニュー削除、削除キャンセル、保存失敗、Undo/Redo、同名列再追加、シート再読込、保存エラー表示の操作チェーン追跡）、関連JavaScript/Python構文チェック、シート列削除/表示設定/メタデータ保存回帰unittest、大量列シートでの実ブラウザE2EとAI目視、バージョンメタデータunittest、`git diff --check` PASS。

## v0.6.101 (2026-06-17)

### fix(sheet): 列管理ダイアログの行操作ボタンを読みやすくする

- 「列の表示と順序」ダイアログの複製/削除ボタンを、アイコンだけでなく短い文字ラベル付きにし、大量列シートでも操作内容が分かるようにした。
- ラベル付きボタンが収まるように列管理ダイアログの横幅と行内アクション領域を調整した。
- 表示バージョンを `0.6.101` に更新した。

検証: 大量列シート（120列）での実ブラウザスクリーンショットAI目視、関連JavaScript構文チェック、シート列管理回帰unittest、バージョンメタデータunittest、`git diff --check` PASS。

## v0.6.100 (2026-06-17)

### feat(sheet): 列管理ダイアログに追加・複製・削除を追加

- 「列の表示と順序」ダイアログ上で、列名を入力して新しい列を追加できるようにした。
- 各列行に複製ボタンを追加し、プロパティ型、列幅、集計、条件付きカラー、ロック設定、既存値を新しい列へコピーできるようにした。
- 各列行に削除ボタンを追加し、削除時は既存の警告ダイアログを必ず表示するようにした。
- 行内の複製/削除ボタンがドラッグ操作と干渉しないようにし、長い列名は省略表示してダイアログ幅を保つようにした。
- 表示バージョンを `0.6.100` に更新した。

検証: 徹底チェック（列追加、列複製、値コピー、列削除、削除キャンセル、表示/非表示、ドラッグ順序、保存、再描画の操作チェーン追跡）、関連JavaScript構文チェック、シート列管理/プロパティ削除/表示設定回帰unittest、値コピーのNode回帰、シートmicro E2E、バージョンメタデータunittest、`git diff --check` PASS。

## v0.6.099 (2026-06-17)

### feat(sheet): 列の表示と順序ダイアログを追加

- シートの列だけを一覧する「列の表示と順序」ダイアログを追加し、チェックボックスで表示/非表示、ドラッグで列順を変更できるようにした。
- 通常列ヘッダー、エントリ名列ヘッダー、ツールメニューから同じ列管理ダイアログを開けるようにし、旧「プロパティ管理」呼び出しは互換経路として残した。
- 非表示列をヘッダーメニューから個別または一括で再表示できるようにし、非表示列だけに残った古い設定も列管理ダイアログへ取り込むようにした。
- 大量行/大量列のシートで初期描画を段階表示にし、列管理ダイアログも大量列で余白が出ないようリスト領域を伸縮表示にした。
- 表示バージョンを `0.6.099` に更新した。

検証: 大量列シート（120列）での実ブラウザE2EとAI目視、徹底チェック（列メニュー、列一覧生成、表示/非表示、列順保存、履歴、描画適用の操作チェーン追跡）、全体チェック（本体スクリプト2,752ファイル/1,187,657行の全行読み込み、Python/JavaScript構文チェック、full-check系unittest、関連シート回帰unittest、バージョンメタデータunittest）、`git diff --check` PASS。full unittest discover は10分でタイムアウトしたため、既存の分割チェックと関連テストで代替確認。

## v0.6.098 (2026-06-17)

### fix(chat): Meldex MCPツールの大規模操作タイムアウトを緩和

- Codex CLI起動時に `mcp_servers.meldex-local.tool_timeout_sec=600` と `startup_timeout_sec=30` を一時設定として注入し、MCPツール呼び出しが短い既定タイムアウトで止まらないようにした。
- Meldex MCPブリッジのHTTP呼び出しも30秒固定ではなく `MELDEX_MCP_HTTP_TIMEOUT_SECONDS` に従うようにし、Codex CLI側から600秒を渡すようにした。
- 大規模シートで `read_database` が全量読み取りに進んでタイムアウトしないよう、300件を超える場合はLLM向けの圧縮シート情報へ自動切り替えするようにした。
- 大量ファイルを含む親フォルダで作成、移動、削除を行う際、重い親フォルダ全体の自動バージョン保存を条件付きで省略し、Meldexツール操作のタイムアウトを抑えた。
- 表示バージョンを `0.6.098` に更新した。

検証: Codex CLI/MCP設定回帰unittest、MCPツール回帰unittest、LLMシートコンテキスト回帰unittest、Python構文チェック、バージョンメタデータunittest、`git diff --check` PASS。

## v0.6.097 (2026-06-17)

### fix(chat): Codex CLIのMeldex MCPツール承認を非対話実行向けに補正

- MeldexからCodex CLIを起動する際、Meldex MCPサーバーのツール承認モードを `approve` に固定し、非対話実行中にMCPツール呼び出しが承認待ちからキャンセル扱いになる問題を修正した。
- Codex CLI起動時に、保存済みの承認/サンドボックス指定を取り除いた上で、`workspace-write`、`approval_policy="never"`、Meldex MCP/API接続情報、MCPツール自動承認を一時設定として注入するようにした。
- CLIチャット設定でモデル名を確認・保存できるようにし、Codex CLIの既定モデルを `gpt-5.5` に補正した。
- チャットの送信先選択で、APIキー未設定、CLI無効、CLI未検出の項目を選択不可表示にし、送信ボタンと編集して再送フォームの幅崩れを防いだ。
- 表示バージョンを `0.6.097` に更新した。

検証: Codex CLI設定リファレンス確認、Codex CLI `mcp list` によるMCP設定構文確認、CLIチャット回帰unittest、Python構文チェック、`git diff --check` PASS。

## v0.6.096 (2026-06-17)

### fix(ui): 固定レール、タイマー通知、シートD&D表示を整理

- メインパネルを常時アクセント枠で囲む表示を廃止し、スケジューラーを右レールの既定タブから外して左レールのアプリとして開けるようにした。既存レイアウトに残った右レールのスケジューラータブも復元時に除去するようにした。
- ツールチップ表示中に左クリック、右クリック、ホイール操作を行った場合は、カーソルが動いていなくてもツールチップを閉じるようにした。
- タイマー設定から実行リストを削除し、設定ダイアログの横幅、単独タイマーの初期描画、クラウド/スマホ幅の設定ダイアログ表示を調整した。
- タイマーの内蔵音を警報音に整理し、ユーザーが3MB以下の音声ファイルをアラーム音源として設定できるようにした。試聴、削除、保存、履歴、単独タイマー/クラウド静的版の読み込みにも対応した。
- 「音声で通知」がオフのときはカウントダウン系設定を無効表示にし、カウントダウンとラスト10秒を同時に選べないようにした。通知タイミングはタイマー開始時にしきい値へ直接スケジュールして、ラスト10秒の読み上げずれを抑えた。
- シートの列ヘッダーD&Dと行D&Dで、列間/行間のドロップ位置がエントリ名列を含めて分かるインジケーターを表示するようにした。
- 表示バージョンを `0.6.096` に更新した。

検証: 徹底チェック（固定レール復元、左レールアプリ起動、ツールチップ抑止、タイマー音源選択/削除/試聴/完了通知/カウントダウン、単独タイマー初期描画、クラウド/スマホ幅設定ダイアログ、シート列/行D&Dの操作チェーン追跡）、関連JavaScript/Python構文チェック、タイマー/単独アプリ/固定レール/ツールチップ/シートD&D回帰unittest、クラウド静的/スマホ幅E2E、実ブラウザAI目視、`git diff --check` PASS。

## v0.6.095 (2026-06-16)

### fix(chat): CLI/MCPのMeldex内操作権限と使用量表示を整理

- Codex CLI / Claude Code / Gemini CLI のMeldex MCP接続を常に有効化し、既存設定でGemini CLIのMCP接続が無効でも読み込み時に補正するようにした。
- 通常LLMとCLIが同じMeldexツール実行器を使えるようにし、MCPにファイル/フォルダ移動用の `meldex_move` を追加した。
- Meldex管理内のファイル/フォルダ整理で、移動先作成、同名衝突、自分自身の配下への移動防止、保護ファイル、編集ロック、バージョン保存を確認するようにした。
- CSVをMeldexツール経由で作成/更新する場合はUTF-8 BOM付きで保存し、読み込み時はBOMを本文に出さないようにした。
- チャット画面の使用量バナーとメッセージ内使用量表示を廃止し、使用量確認は設定ダイアログ側に集約した。
- Codex CLI起動時に保存済みの `-s` / `--sandbox` / 危険な全権限指定を取り除き、`codex --sandbox workspace-write exec ...` の形でMeldex内書き込み用サンドボックスをグローバル指定するようにした。`sandbox_mode` / `approval_policy` も起動時設定で明示し、CLI向け指示にも、削除は完全削除ではなくMeldexのゴミ箱へ移動することを明記した。
- Codex CLIの既定モデルを `gpt-5.5` にし、保存済みの `gpt-5.3-codex` / `gpt-5-codex` / `Codex CLI` 表記は起動前に `gpt-5.5` へ補正するようにした。CLIチャット設定でモデル名も確認・変更できるようにした。
- チャットのLLM/CLI選択で、APIキー未設定・CLI無効・CLI未検出の項目を選択不可表示にし、送信ボタンと編集して再送フォームの幅崩れを防いだ。
- 表示バージョンを `0.6.095` に更新した。

検証: 徹底チェック（通常LLM/CLI/MCPのツール実行チェーン、Codex CLI/Claude Code/Gemini CLIのMCP注入、Meldex管理内のファイル/フォルダ移動、CSV書き込み、ゴミ箱削除、チャット使用量表示撤去の操作チェーン追跡）、関連Python/JavaScript構文チェック、CLIチャット回帰unittest、チャットストリーム回帰unittest、MCPツール回帰unittest、LLM費用表示回帰unittest、WebClip回帰unittest、バージョンメタデータunittest、`git diff --check` PASS。

## v0.6.094 (2026-06-16)

### fix(chat): Codex CLIの書き込みサンドボックスを補正

- Codex CLIの保存済み起動引数に `--sandbox workspace-write` が無い場合でも、Meldexからのチャット実行時に書き込み可能なサンドボックスを補うようにした。
- 保存済み起動引数が `--sandbox read-only` の場合は、Meldexのソースフォルダ更新に必要な `workspace-write` へ置き換えるようにした。
- 表示バージョンを `0.6.094` に更新した。

検証: 徹底チェック（Codex CLIのユーザー設定回避後の起動引数、保存済みCLI設定、ソースフォルダ書き込み前提の操作チェーン追跡）、関連Python構文チェック、CLIチャット回帰unittest、バージョンメタデータunittest、`git diff --check` PASS。

## v0.6.093 (2026-06-16)

### fix(chat): CLIチャットとワークスペース対象の送信を安定化

- Codex CLI のユーザー設定に現行CLIで読み込めない `service_tier` 値がある場合、Meldex側の一時設定だけで起動してチャット送信できるようにした。
- 通常LLMチャットでもワークスペース対象を `workspace_id` から正しく解決し、ソースフォルダ未指定のワークスペース送信でエラーにならないようにした。
- 通常LLM/CLIチャットの送信、保留メッセージ、自動保存で、ワークスペースとソースフォルダの対象情報が混ざらないようにした。
- 表示バージョンを `0.6.093` に更新した。

検証: 徹底チェック（Codex CLI設定読み込み、Claude Code/Gemini CLI/通常LLM APIの設定経路、ソースフォルダ/ワークスペースの送信・保存・保留メッセージ操作チェーン追跡）、関連Python構文チェック、CLIチャットJS構文チェック、関連回帰unittest、チャットサービス/ワークスペース回帰unittest、バージョンメタデータunittest、`git diff --check` PASS。

## v0.6.092 (2026-06-16)

### test(sheet): CSVモードと固定UIの微細挙動検証を安定化

- シートのCSVモードを sheet micro E2E に追加し、CSVをシート表面で開く、セル編集、Tab移動、行/列追加、BOM付き保存まで実ブラウザで確認できるようにした。
- sheet / shell micro のランナーを補強し、重い視覚スクリーンショット系ケースは単独確認へ分離しつつ、標準ランナーで主要な入力・表示・保存・固定UIケースを再実行できるようにした。
- シートの下端＋行追加、チャットクリック後のシートツールバー維持、セル範囲コピー/貼り付け、shell の分割リサイズ検証を安定化した。
- 旧Windows環境で作られたCP932のMarkdownフロントマターも読み込めるようにし、シートメタデータ更新時のHTTP 500を防いだ。
- 表示バージョンを `0.6.092` に更新した。

検証: 実ブラウザE2E シートCSVモード単独、シートmicro全23件、shell micro（リサイズ単独＋残り20件）、固定レイアウト視覚監査単独、AI目視（CSVモード、固定レイアウト標準/左アプリメニュー/右レール）、関連JavaScript/Python/PowerShell構文チェック、関連回帰unittest、バージョンメタデータunittest、`git diff --check` PASS。

## v0.6.091 (2026-06-15)

### test(shell): 既定アプリ設定ボタンを微細挙動テストへ追加

- 設定ダイアログの「ファイルを開くアプリ」を shell micro E2E に追加し、Sheet / Note / Viewer の3ボタンを実ブラウザで押下してAPI呼び出し、状態再読込、表示サイズを確認できるようにした。
- 固定レイアウト移行後も shell micro のテスト用レイアウトをそのまま検証できるようにし、パネルリサイズ、ドック、注釈FAB系の既存 micro ケースを復旧した。
- フォルダビューの一括操作バーを body 直下の固定ポップアップとして配置し、選択時に通常レイアウトを押し下げない契約を再び満たすようにした。
- 表示バージョンを `0.6.091` に更新した。

検証: 実ブラウザE2E `targeted-shell-micro-settings-default-app-buttons`、`targeted-settings-default-app-buttons`、`targeted-shell-micro-panel-resize-isolation`、`targeted-shell-micro-folder-display-filter`、shell micro 全22件、実ブラウザAI目視（設定のファイルを開くアプリ）、関連JavaScript結合後構文チェック、関連回帰unittest、`git diff --check` PASS。

## v0.6.090 (2026-06-15)

### feat(settings): OS既定アプリ設定ボタンとシートCSVモードを追加

- 設定ダイアログの全般に「ファイルを開くアプリ」を追加し、Meldex Sheet / Meldex Note / Meldex Viewer をWindowsの既定アプリ候補として登録できるようにした。
- WindowsのUserChoice保護が残る場合は、Meldex側で候補登録を済ませた上でWindowsの既定アプリ画面を開くようにした。
- `.csv` はMeldex SheetのCSVモードで開くように統一し、単独SheetアプリでもCSVの表示・編集・BOM付き保存に対応した。
- 配布フォルダの関連付けスクリプトにCapabilities登録と `-SetDefault` を追加し、CSVは通常実行では候補登録に留め、明示操作時だけ既定化を試すようにした。
- シートの画像プロパティは、画像クリックでビューワーを開き、並べ替えや削除を行う画像管理ダイアログは `…` ボタンから開くようにした。
- Windows既定アプリの初心者向けマニュアルを追加し、`.smart-db.json` はWindows上では `.json` として扱われるためOS既定アプリ化の対象外であることを明記した。
- 表示バージョンを `0.6.090` に更新した。

検証: 徹底チェック（設定ダイアログ表示、既定アプリAPI登録、UserChoice保護時のWindows設定誘導、配布スクリプト通常実行/`-SetDefault`、CSVモード表示/保存、単独Sheet CSV、画像プロパティクリック/`…`管理ダイアログ、`.smart-db.json`の非対象化の操作チェーン追跡）、実ブラウザE2E `targeted-settings-default-app-buttons`、`targeted-sheet-micro-all-property-types-matrix`、実ブラウザAI目視（設定のファイルを開くアプリ）、関連JavaScript/Python構文チェック、関連回帰unittest、CSV pytest、`git diff --check` PASS。

## v0.6.089 (2026-06-15)

### feat(layout): 補助表示の開き先選択とビューワー単独起動を追加

- タイマーとビューワーを右レールのメニューから単独アプリとして起動できるようにした。
- `MeldexViewer.py`、ビューワー用アイコン、ビルド定義を追加し、画像/PDFビューワーを単独アプリのビルド対象に含めた。
- ビューワー単独起動時に起動ファイル、URLパラメータ、ファイル未指定時のファイル選択を扱い、フォルダ内画像一覧を共通単独アプリAPIから取得できるようにした。
- ノート内リンク、ボードのリンクカード、シート画像ギャラリーのメニューから、メインパネル、右サイドバー、単独アプリを選んで開けるようにした。
- 右サイドバーで開く操作は preview タブを明示的に起こし、表示できない場合だけ互換サブパネルへ退避するようにした。
- 表示バージョンを `0.6.089` に更新した。

検証: 徹底チェック（右レールのタイマー/ビューワー単独起動、ノートリンク、ボードリンクカード、シート画像、右サイドバー preview 表示、ビューワー単独起動時の初期ファイル解決、単独ランタイムのフォルダ画像一覧の操作チェーン追跡）、実ブラウザE2E `targeted-shell-micro-fixed-layout-visual-audit`、実ブラウザAI目視（固定レイアウト標準、左アプリメニュー、右レール）、関連JavaScript/Python構文チェック、関連回帰unittest、バージョンメタデータunittest、`git diff --check` PASS。

## 未リリース (2026-06-15)

### fix(ui): 右サイドバーと詳細表示の文言・アイコンを整理

- 設定ダイアログとスケジューラーのテーマ設定で、補助領域の表示名を `右サイドバー` に統一した。
- 右レールとオプションパネルのツールチップ文言を `右サイドバー` と `オプション` の役割に合わせて整理した。
- シートのエントリ詳細とスケジューラーのイベント詳細を開くメニューで、右サイドバーアイコンではなく詳細表示を示す `fileText` アイコンを使うようにした。
- 設定ダイアログ、ツールチップ、シート詳細メニュー、スケジューラーイベントメニューの回帰テストを追加した。

検証: 徹底チェック（右レールタブ、オプションパネル、設定テーマ、シート詳細メニュー、スケジューラーイベントメニューの操作チェーン追跡）、関連JavaScript構文チェック、関連回帰unittest、実ブラウザE2E `targeted-shell-micro-theme-auto-apply-style-preview`、`git diff --check` PASS。

### fix(layout): オプション導線と右サイドバーアイコンを分離

- メイン画面の各アプリツールバーに残っていたオプションパネル表示ボタンを通常シェルでは非表示にし、右レールの補助パネル切替へ導線を一本化した。
- 右サイドバーを示す `panelRight` とオプションを示す `slidersHorizontal` を分離し、補助パネルメニュー、モバイル編集バー、保存済みレイアウト移行、共通アイコン変換でオプションを `slidersHorizontal` に統一した。
- 単一タブポップアウトや単独表示では右レールが無いため、ツールバーのオプション導線を引き続き表示できるようにした。
- `build_split_bundles.py` で prebuilt bundle と split-loader fingerprint を再生成した。

検証: 徹底チェック（メイン画面ツールバー非表示、単一タブ表示のオプション導線、右レール/補助パネルメニュー、モバイル編集バー、保存済み旧 detail タブ移行、共通アイコン変換、prebuilt bundle 読み込みの操作チェーン追跡）、実ブラウザE2E `targeted-shell-micro-fixed-layout-visual-audit`、関連JavaScript結合後構文チェック、関連回帰unittest、`git diff --check` PASS。既知別件: `test_meldex_split_assets.py` は削除済み MeldexHome マニュアル欠落で1件失敗。

### fix(sheet): 表示設定をシート側メタデータへ永続化

- シートのビュー、列幅、表示列、固定列、並び替え、フィルタ、集計、サムネイル、タイムライン等の表示設定を、ブラウザ/PCごとの `localStorage` ではなくシートフォルダノートの `view_config` へ保存するようにした。
- 既存のローカル表示設定は、シート側に表示設定がまだ無い場合だけ初回移行し、単にシートを開いただけでは新しい `view_config` を作らないようにした。
- シート側の `view_config` を読み込み時の正とし、旧形式の表示設定は読み込み時に現行形式へ正規化して書き戻すようにした。
- 表示設定のUndo/Redoで、localStorageだけでなくシート側の `view_config` も復元してから再描画するようにし、再読み込みでUndo内容が戻される問題を防いだ。
- Dropbox/ブラウザ版のシートメタデータ読み書きでも `view_config` を扱えるようにした。
- `view_config` だけの保存ではシートの自動バージョン履歴を作らないようにし、列幅ドラッグなどで履歴が増えすぎないようにした。

検証: 徹底チェック（シートを開く、既存localStorage設定の初回移行、バックエンド設定優先読み込み、表示設定変更、Undo/Redo、Dropbox/ブラウザ版メタデータ保存、自動バージョン抑制の操作チェーン追跡）、関連JavaScript/Python結合後構文チェック、関連回帰unittest 40件、`git diff --check` PASS。

## v0.6.088 (2026-06-15)

### fix(layout): 固定レールのメインパネル固定と旧レイアウト移行を補強

- 左レールのアプリボタンから開くとき、右サイドバーや旧レイアウト側に同種タブが残っていても、中央のメインパネル内だけで既存タブを再利用するようにした。
- 通常ファイルを新しいタブで開く経路でも、メインパネル解決後に他パネルの既存タブへ横取りされないようにした。
- 保存済みの旧パネルレイアウトを読み込むとき、既存の作業タブをメインパネルへ、フォルダツリーを左サイドバーへ、補助タブを右サイドバーへ移行する互換処理を追加した。
- 右レールに補助系の検索ボタンを追加し、既存の検索パネルを開ける導線を用意した。
- 表示バージョンを `0.6.088` に更新した。

検証: 徹底チェック（左レールからメインパネル起動、通常ファイルのメインパネル固定、保存済み旧レイアウト移行、右レール補助パネル補完、左レールメニューのEscape閉じの操作チェーン追跡）、実ブラウザE2E `targeted-shell-micro-left-rail-main-target`、`targeted-shell-micro-fixed-layout-visual-audit`、実ブラウザAI目視（固定レイアウト右レール）、関連JavaScript/Python結合後構文チェック、関連回帰unittest、バージョンメタデータunittest。

## v0.6.087 (2026-06-15)

### feat(layout): 左右固定レールでサイドバー操作を整理

- 左右サイドバーの細いレールを常時表示し、各レール最上部にサイドバー開閉ボタンを追加した。
- 境界線上の三角開閉ボタンと自由分割/ドッキング操作は隠したまま、境界線ドラッグではサイドバー幅だけ変更できるようにした。
- 右レールにビューワー、オプション、バージョン管理、チャット、スケジューラー、タイマー、ヒストリー、注釈の補助パネルボタンを並べ、右カラム内では選択中の補助パネル1枚だけを表示するようにした。
- 左レールにフォルダ、ノート、シナリオ、シート、ボード、スマートシートの起動ボタンを追加し、通常クリックでは中央のメインパネルに開き、メニューから単独アプリとして起動できるようにした。
- 表示バージョンを `0.6.087` に更新した。

検証: 徹底チェック（左右レール常時表示、サイドバー開閉、境界線ドラッグ、左レールからメインパネル起動、左レール単独起動メニュー、右レール全補助パネル切替、自由分割/ドッキングUI非表示の操作チェーン追跡）、実ブラウザAI目視（固定レイアウト7状態）、微細挙動E2E `targeted-shell-micro-fixed-layout-visual-audit`、`targeted-shell-micro-panel-resize-isolation`、`targeted-shell-micro-default-dock-widths-left/right`、`targeted-shell-micro-split-collapse-direction-contract`、関連JavaScript/Python結合後構文チェック、関連回帰unittest、`git diff --check` PASS。

## v0.6.086 (2026-06-15)

### fix(layout): 固定レイアウトUIと目視E2Eを強化

- 固定の左・中央・右レイアウトで、自由分割用のパネル追加ボタンがタブバーに残らないようにした。
- フォルダ選択のフローティング操作バーが、選択なしの状態で表示されないよう `hidden` 状態を共通フロートメニューCSSと同期した。
- 右補助ポップアップの閉じるボタンに安定IDとアクセシブル名を追加し、実ブラウザE2Eで検査しやすくした。
- 標準、ノート中央表示、タブメニュー、フォルダ項目メニュー、フォルダツリーメニュー、右補助ポップアップの6状態を撮影する固定レイアウト目視E2Eを追加した。
- 表示バージョンを `0.6.086` に更新した。

検証: 実ブラウザAI目視（固定レイアウト6状態）、微細挙動E2E `targeted-shell-micro-fixed-layout-visual-audit`、徹底チェック（固定レイアウト描画、通常ファイル表示、各メニュー、右補助ポップアップ、フォルダ選択バーの操作チェーン追跡）、全体チェック（806スクリプト/HTML/CSSファイル 330,521行走査、Python実体535件構文チェック、JS実体281件構文チェック）、関連回帰unittest、`git diff --check` PASS。

## v0.6.085 (2026-06-14)

### fix(import/scheduler): HTML保存とスケジューラー共有同期を整備

- Web ClipperのHTMLスナップショット保存で、保存済み画像やCSS内の参照をローカルファイルURLへ置き換え、サブパネルではHTMLプレビューを優先表示するようにした。
- Web ClipperのHTMLプレビューに別画面で開く操作を追加し、保存したページをノート本文ではなくHTMLスナップショットとして確認できるようにした。
- Notion、Evernote、Obsidian取り込みで画像や添付ファイルを取り込み先の `_assets` 配下へローカル保存し、本文リンクや添付一覧から参照できるようにした。
- スケジューラー系データはSQLiteを維持したまま、ホームフォルダ内の共有スナップショットへカレンダー、予定、タスク、作業時間、シフト、制作管理テンプレートを同期するようにした。
- カレンダーAPIと制作管理のシフト取り込み、割り当て、再計算で共有スナップショットを読み書きし、複数PCで同じホームフォルダを共有した時に更新が反映されるようにした。
- 共有スナップショットの競合判定を更新日時と削除履歴で扱い、新しい行が古い削除履歴に消されないようにした。
- 表示バージョンを `0.6.085` に更新した。

検証: 徹底チェック（Web Clipperページ保存・HTMLスナップショット表示・Notion/Evernote/Obsidian添付ローカル保存・スケジューラー共有スナップショット・制作管理シフト/割り当て/再計算の操作チェーン追跡）、関連JavaScript/Python構文チェック、関連回帰unittest 22件、バージョンメタデータunittest、`git diff --check` PASS。

## v0.6.084 (2026-06-14)

### fix(import/webclip): Web Clipper画像保存とXブックマーク取得を強化

- Web Clipperのページ保存で、拡張機能側から取得できた画像データをMeldexへ渡し、保存先シートの画像プロパティへ登録できるようにした。
- Web Clipperのページ保存でHTMLスナップショットを `_assets` 配下へ保存し、ノート本文とHTMLプロパティから開けるようにした。
- Web Clipperの右クリック画像保存、ホバー画像保存、スクリーンショット保存で、保存した画像をノート本文から相対パスで参照し、画像プロパティにも反映するようにした。
- Xブックマークの取得件数を最大5000件まで指定できるようにし、X APIから次ページが返されない場合は停止理由を画面に表示するようにした。
- Xブックマーク全件保存時にフォルダ一覧とフォルダ内ポストを読み取り、シートの「フォルダ」マルチセレクト列へ反映するようにした。
- シート集計行の集計種別メニューを共通テーマのドロップダウンへ変更し、未選択項目が薄く見える問題を修正した。
- アプリ全体のHTMLルート、入力欄、本文編集欄、チャット入力欄でスペルチェックを無効化し、固有名詞に赤い波線が出にくいようにした。
- 表示バージョンを `0.6.084` に更新した。

検証: 徹底チェック（Web Clipperページ保存・画像単体保存・HTMLスナップショット保存・Xブックマーク通常一覧/全件保存/フォルダ反映・集計メニュー・spellcheck無効化の操作チェーン追跡）、関連JavaScript/Python構文チェック、関連回帰unittest 8ファイル、バージョンメタデータunittest、`git diff --check` PASS。

## v0.6.083 (2026-06-14)

### fix(note/manual): ノート内リンクとChrome拡張機能マニュアルを整備

- ノート内の見出しに安定したアンカーIDを付け、同じノート内の `#見出し` リンクから該当見出しへスクロールできるようにした。
- 同一ノート内リンクをファイルリンク扱いしないようにし、ホバー時のリンク補助情報取得で「項目が見つかりません」通知が出る問題を修正した。
- 同一ノート内リンクの右クリックメニューでは現在のノート内で開く操作だけにし、サブパネルで開く項目を出さないようにした。
- Chrome拡張機能の設定マニュアルに、デベロッパーモードをONにする手順を追加し、設定手順内の文言から同じノート内の手順へ移動できるようにした。
- ノート微細E2Eに同一ノート内リンクのホバー、右クリック、クリックの実操作ケースを追加した。
- 表示バージョンを `0.6.083` に更新した。

検証: 徹底チェック（同一ノート内リンクの生成・クリック・右クリックメニュー・ホバー補助情報・見出しスクロールの操作チェーン追跡）、関連JavaScript構文チェック、関連回帰unittest 3ファイル、Chrome実機E2E `targeted-note-micro-same-note-anchor-link` を含むノート微細E2E 3件、バージョンメタデータunittest、`git diff --check` PASS。

## v0.6.082 (2026-06-14)

### fix(sheet/import): Xブックマーク保存とフォルダ選択をシート運用へ統合

- Xブックマーク保存をMarkdown単体保存から画像付きシートエントリ保存へ変更し、投稿本文、投稿者、URL、日時、保存状況、画像プロパティを持つシートとして開けるようにした。
- Xブックマーク内の画像を `_media` 配下の重複排除ストアへ保存し、画像プロパティ値と `refs.json` 参照情報を更新することで、シート画像列・画像削除保護と同じ経路で扱えるようにした。
- Xブックマーク設定を「取り込み」タブへ移動し、接続状態と保存結果に画像保存数・画像失敗数を表示するようにした。
- ホームフォルダ、ソースフォルダ、ワークスペースフォルダを横断して選べる共通フォルダ選択ダイアログを追加し、Notion同期、スマートシート対象フォルダ、所属フォルダ、別名保存、シナリオ形式保存のフォルダ選択へ適用した。
- シートの画像プロパティに表の画像高さ設定を追加し、画像列の自動調整で枚数分に横へ広がり続けないようにした。折り返し/切り詰め表示、ヘッダー上の覗き込み、列幅自動調整後のエントリ名列ドラッグも修正した。
- フォルダパネルの複数選択フローティングメニューが選択なしで残る問題と、空フォルダ選択時にフォルダツリーから一時的に消える問題を修正した。
- 表示バージョンを `0.6.082` に更新した。

検証: 徹底チェック（Xブックマーク保存・画像再利用・画像取得失敗・取り込みタブ初期化・共通フォルダ選択・シート画像列・フォルダツリー選択状態の操作チェーン追跡）、関連JavaScript/Python構文チェック、関連回帰unittest 74件、`git diff --check` PASS。

## v0.6.081 (2026-06-14)

### fix(settings): Xブックマーク接続設定と設定タブE2Eを整備

- XブックマークのClient ID、リダイレクトURI、認可URL、トークンURL、APIベースURLを設定画面から保存できるようにした。
- X接続開始とブックマーク保存の前に、画面上のXブックマーク設定を保存してから処理を続けるようにした。
- 設定ダイアログの全カテゴリを実ブラウザE2Eで開き、表示されるべきパネルの欠落を検出できるようにした。
- ワークスペース設定の主要入力・ボタン・選択欄へE2E用の安定IDを追加した。
- サンプル作品「死霊探偵」の長編本文、参考素材メモ、内部マニュアル用チーム情報、シート総合監査計画書をリポジトリに追加した。
- 表示バージョンを `0.6.081` に更新した。

検証: 徹底チェック（Xブックマーク設定保存・接続開始・同期前保存、設定ダイアログ全タブ表示、ワークスペース設定E2E識別子、未追跡サンプル/資料ファイルの形式確認）、関連JavaScript/Python構文チェック、関連回帰unittest 27件、Chrome実機E2E `targeted-settings-dialog-all-tabs-visible`、Markdown/JSON UTF-8読み込み、JPEG形式確認、`git diff --check` PASS。

## v0.6.080 (2026-06-14)

### fix(sheet): タイムライン暦ダイアログを共通UIに揃える

- 暦プリセット操作を暦体系編集ダイアログの最上段に集約し、ネイティブ選択欄と共通ボタンで新規作成・複製作成・JSON出力・選択中の削除を行えるようにした。
- 保存済みの独自暦を選択している時だけ削除を有効にし、削除前にMeldex共通の確認ダイアログを表示するようにした。
- 暦体系編集ダイアログとタイムライン設定の数値入力欄を共通UIトークンに寄せ、スピンボタンは操作時だけ見える表示に揃えた。
- 空のタイムライン、架空暦、日単位の全期間表示、10分刻みスケジュール、削除確認キャンセルを実ブラウザE2Eと回帰テストで検証した。
- 表示バージョンを `0.6.080` に更新した。

検証: 徹底チェック（暦プリセット操作・空タイムライン期間表示・10分刻み表示の操作チェーン追跡）、関連JavaScript構文チェック、関連回帰unittest 3ファイル、Chrome実機E2E 2ケース、スクリーンショットAI目視、`git diff --check` PASS。

## v0.6.079 (2026-06-13)

### fix(sheet): シート総合監査の確定課題を修正

- シートの値編集・リネーム・旧形式エントリ作成で、ロック確認、失敗時ロールバック、検索インデックス更新、途中失敗時クリーンアップを強化した。
- SVG画像アップロードを拒否し、既存SVGは添付扱いで配信するようにした。画像ギャラリーは途中失敗時も成功分を保存し、枚数表示と同一ファイル再選択を更新した。
- 日付解析でタイムゾーン付きISO、`24:00`、片側だけ非日付の期間文字列を正しく扱うようにした。
- シート読み取りだけで欠落IDを書き戻さないようにし、SQLite同期側も読み取り時のファイル変更を避けるようにした。
- スマートシートの再読み込みで全件インデックスを強制更新し、単体起動版シートのソース・フィルタ形式を本体互換に揃えた。
- 行ドラッグ&ドロップ、検索ハイライト、ユーザーアバター表示、数式内列名、監査ログLIKE検索、エントリ名テンプレートの細部不具合を修正した。
- 廃止済み `gb-db-dashboard.js` を削除し、監査リスト・UIガードレールからも除外した。
- 表示バージョンを `0.6.079` に更新した。

検証: 徹底チェック（直前変更に直結する操作チェーン追跡）、関連JavaScript/Python構文チェック、関連回帰unittest 10ファイル、`git diff --check` PASS。

## v0.6.078 (2026-06-13)

### fix(scheduler): 監査残課題の確定16件を修正

- スケジューラーのファイル別テーマ選択で、選んだ組み込みテーマのカレンダー用カラーが反映されない問題を修正した。
- イベントのアンドゥ復元で、読み込み期間の外にある予定を削除候補にしないようにした。月移動や表示期間変更後の復元で、別期間の予定を巻き込むリスクを下げた。
- シフト作成・編集・削除の楽観更新で、実体と合わないアンドゥ履歴を積まないようにした。
- 制作管理のCloud版割り当てで、既存の作業予定イベントを空き時間として扱ってしまう問題を修正した。
- メンバー追加で同名メンバーを更新する場合は確認を出し、未入力欄で既存値を消さないようにした。
- 制作管理パネルで日付のみの作業期間を開いた時、日時入力欄が空になり、保存で期間が消える問題を修正した。
- シフト勤務テンプレートのワークスペース一覧取得に失敗した場合、保存済みの紐づけを維持できる表示にした。
- タスク・打刻・シフト・勤務テンプレートAPIに所有者/担当者ベースの権限チェックを追加した。
- Googleカレンダー送信で1件の失敗が全体失敗にならないよう、送信できた予定は確定し、失敗件数と詳細を返すようにした。
- CalDAV送信のstale削除を同期元ごとのファイル接頭辞に限定し、共有コレクション内の別同期元ファイルを退避対象にしないようにした。
- 制作管理のシフト再取り込みで、同じ担当者・同じ日の古い `pm-shift`、対応イベント、スケジュール行を削除してから新しい行を登録するようにした。
- 終了時刻が空の勤務シフトを24時間勤務として扱わず、割り当て候補から除外して理由を返すようにした。
- シフト時刻の取り込みで、終了時刻の24時超は翌日扱いにしつつ、開始時刻の24時超や分の不正値は保存前に除外するようにした。
- アラーム付き予定の保存時に通知許可を要求し、直近の未通知分を補償取得できるようにした。
- 制作管理の再計算で、期間外だが既に割り当て済みの未完了タスクの予定時間をbusyとして扱うようにした。
- 表示バージョンを `0.6.078` に更新した。

検証: 徹底チェック（直前変更に直結する操作チェーン追跡）、関連Python構文チェック、関連回帰unittest 64件、全体テストのベースライン比較（既存4失敗から新規失敗ゼロ）、`git diff --check` PASS。

## v0.6.077 (2026-06-13)

### fix(scheduler): スケジューラー全機能の精密監査で確定した問題を一括修正

スケジューラー関連の全ファイル（34ファイル・約17,000行）を全行監査し、反証検証で本物と確定した問題のうち45件を修正した。

**データ消失・誤保存の防止（最重要）**
- 月を移動した後に「元に戻す」を実行すると、表示中の期間の予定がサーバーから大量削除される問題を修正した（表示期間が変わったらアンドゥ履歴を破棄する）。
- テキスト入力中の Ctrl+Z がカレンダー全体のアンドゥ（サーバーへの削除・更新）を実行してしまう問題を修正した（入力欄では入力の取り消しを優先）。
- 「元に戻す」の連打で復元処理が並行実行され、予定がどの状態とも一致しなくなる問題を修正した（実行中は再入を無視）。
- シフト・実績・制作タスク由来の自動生成イベントをアンドゥ対象から除外した（アンドゥで実体のない幽霊イベントが恒久保存される問題の解消）。
- 繰り返し予定の1回分のカードメニューから「削除」すると、単発削除の確認文のままシリーズ全体が消える問題を修正した（繰り返し全体の削除であることを確認文で明示）。
- 他のメンバー所有のカレンダー上のイベントを編集・保存すると、カレンダー所属が黙って「未設定」に外れる問題を修正した。
- シフト編集モーダルのメモ欄が1行入力だったため、保存すると休憩設定（複数行のメモ）が全て破壊される問題を修正した（複数行入力に変更）。
- 終了時刻を空にして保存したイベントが画面から消える（取得対象外になる）問題を修正した。
- 出退勤の状態取得に失敗した時に「未出勤」へ戻して再出勤できてしまい、同日の実績が二重打刻で壊れる問題を修正した（状態確定までボタンを無効化）。
- 同じ日に複数回出退勤した場合、実績カレンダーに最初の勤務しか記録されない問題を修正した（全セッションを集約）。
- CalDAV送信時に、スマホ等の外部クライアントが追加した未取り込みの予定が完全削除される問題を修正した（コレクション外の退避フォルダへ移動）。
- CalDAV往復で Google/Microsoft 連携の対応付けが破壊され、次回同期で全イベントが重複登録される問題を修正した。
- 制作管理（Cloud版）でタスク作成のたびに作業内容・規模リスト等が初期値へ巻き戻る問題、手動追加タスクへの割り当てで複製行が増殖する問題、夜勤シフトの日付計算が1日ずれる問題を修正した。
- 制作管理の再計算で、期間を変更した後も古いプレビュー結果が適用できてしまう問題を修正した（期間変更時は再プレビューを必須に）。

**時刻・日付の正確性**
- iCal取り込み（ファイル・URL購読）でUTC表記の時刻が9時間ズレて保存される問題を修正した（タイムゾーン変換を追加。繰り返しの終了日は往復でずれない方式）。
- iCalエクスポートが秒のない不正な日時を出力し、Google/Appleカレンダーで取り込みエラーになる問題を修正した。GoogleへのdateTime送信も秒精度に補完した。
- Google同期の取得期間がローカル時刻をUTCと偽装して9時間ズレていた問題を修正した。
- iCal取り込みの終日イベントが1日長く表示される問題（排他的終了日の未変換）と、複数日の終日イベントが1日短く表示される問題を修正した。
- 通知（VALARM）付きのiCal/ToDoを取り込むと、イベント・タスクの説明欄が通知文言で上書きされる問題を修正した。

**操作性・表示**
- シフト表で「前へ」「次へ」が効かない問題を修正した（月単位で移動）。
- 複数日表示で日付を複数選択した後、「今日に戻る」が無反応になる問題を修正した。
- 時計ビューで終日イベントが表示されない問題、24:00終了イベントの編集範囲が15分に潰れる問題、跨日イベントの移動で表示日外の部分が消える問題、終端を24:00まで延ばすリサイズができない問題、コメントバッジが表示されない問題を修正した。
- スケジューラーのタブが非表示の間もアラーム監視を継続するようにした（裏で作業中の通知取り逃し防止）。
- 月送り連打時に古い読み込み結果が後から到着して表示が巻き戻る問題を修正した。
- スケジューラーのテーマ設定のパレット編集が、アプリ全体のテーマカラー適用先の色に影響する問題を修正した。
- シフト勤務テンプレートの編集で、休憩なしのテンプレートに既定休憩が復活する問題、削除済みワークスペースへの紐づけが別のワークスペースへ黙ってすり替わる問題を修正した。
- 出退勤イベントをクリックすると編集できそうなパネルが開き、入力後に保存・削除を拒否される問題を修正した（先に案内を表示）。一括削除・一括リサイズの部分失敗時はサーバー状態へ再同期するようにした。
- 制作管理タブで保存・更新のたびに行選択が元へ戻る／検索フォーカスが奪われる問題を修正した。担当者を外したタスクの作業予定がカレンダーに残り続ける問題、ロック済みタスクの編集で鍵アイコンが消える問題も修正した。
- 制作タスクの固定メニューがUIズーム使用時にずれた位置に表示される問題を修正した。
- 週表示で23時の行までドラッグして予定を作成すると終了時刻が空欄になる問題を修正した。
- カレンダーファイルのイベント名の末尾にスペースを付けて保存すると更新全体が失敗する問題、SQLiteからの移行で作成者情報が失われる問題、同名イベントの取り込みでファイル名が毎回入れ替わる問題を修正した。
- 操作履歴の記録者がイベント所有者になっていた問題を修正した（実際の操作者を記録）。
- Google ToDoで削除したタスクが次の同期で一時的に復活する問題、Google ToDoとCalDAVの併用でタスクが複製される問題を修正した。
- カレンダーファイルへの一括取り込み（iCal/CalDAV/Google）前に自動バージョン保存を行うようにした。CalDAV取得（スケジューラー側）にも変更前の自動バージョン保存を追加した。
- 前回監査の既知事項2件（スケジューラー未起動時のタイムラインビューへのファイルドロップが無反応、シートの追加直後に選択ハイライトが外れる）を修正した。

**残課題**: 設計判断や大きめの変更を伴う16件（テーマ選択の反映、アンドゥの差分方式化、タスク/打刻/シフトAPIの権限チェック、CalDAVコレクション分離等）と低重要度66件・未検証15件は `docs/scheduler-audit-remaining-plan-2026-06-13.md` に記録した。

- 表示バージョンを `0.6.077` に更新した。

検証: 全行監査（15バッチ並列）＋高/中指摘の反証検証、関連JavaScript/Python構文チェック（分割展開で全ファイルOK）、スケジューラー・制作管理・同期API回帰unittest 12ファイル（新規失敗ゼロ）、バージョンメタデータunittest、全体テストのベースライン比較（新規失敗ゼロ）。

## v0.6.076 (2026-06-12)

### fix(scheduler/sheet): イベント保存の即時反映と、シート＋追加時の自動編集解除

- スケジューラーでイベントの設定変更を保存した時、サーバー処理の完了を待たずに先に画面へ反映するようにした（失敗時は元の表示に戻す）。従来は保存のたびに約10秒間反映されなかった。
- 反映が遅かった主因として、変更系の操作のたびにデータベース全体（数百MB）の自動バージョン保存が走っていたため、直近5分以内に自動保存版がある場合は新規作成を省略するようにした。「変更前の版が残る」保護は維持される。イベント以外の保存系操作（注釈・シート等）も同様に高速化される。
- シートで＋ボタンからエントリ（行）やプロパティ（列）を追加した直後に、名前が自動的に編集状態になるのをやめた。追加だけを行い、名前の変更はF2キー・右クリックメニュー・ヘッダーのダブルクリックから行う。
- 徹底チェックで検出した以下の問題を修正した。
  - イベント保存の失敗時は編集対象のイベントだけを元の表示に戻し、サーバー状態の再取得も行うようにした（保存中に行った他の予定の移動・削除の表示を巻き込まない）。
  - 同じイベントの保存処理が完了する前の再保存を防ぎ、失敗時の表示が実際の保存状態とずれないようにした。
  - 自動生成された予定（実績・シフト・制作タスク）をイベント設定パネルから保存しようとした時、一瞬反映されてから戻る表示にならないよう、案内を出して保存を中止するようにした。
  - 予定の再読み込みに失敗した時、カレンダーが無言で空になるのをやめ、既存の表示を維持してエラーを知らせるようにした。
  - 自動バージョン保存の省略を「復元前の安全保存」（必ず保存する指定）には適用しないようにした。また時計ずれで自動保存が止まり続ける条件を排除し、作成途中のバックアップが版として誤検出されないよう一時名で書き込んでから確定名に変えるようにした。
  - 外部カレンダー同期（取り込み・送信）前の自動バージョン保存が配線漏れで一度も動いていなかったのを修正した。
  - シートの＋追加直後の裏側の再読み込みで、連続追加した行が一瞬消える・スクロール位置が先頭側へ飛ぶ・開いている選択式の入力の確定が表示に反映されない問題を修正した。
  - チャート/グラフの空ビューからのエントリ追加が失敗した時、未保存の行が表示に残らないようにした。
- 表示バージョンを `0.6.076` に更新した。

検証: 徹底チェック（3領域の操作チェーン追跡で12件検出→修正）、関連JavaScript/Python構文チェック（分割ミラーの結合一致検証含む）、自動バージョン保存回帰unittest、カレンダー回帰unittest、シートE2E整合性unittest、E2Eマニフェスト再生成、バージョンメタデータunittest、全体テストのベースライン比較（新規失敗ゼロ）、`git diff --check` PASS。

## v0.6.075 (2026-06-12)

### fix(folder-tree): ワークスペースをソースフォルダ表示から分離し、ワークスペース内の追加を即時反映

- ワークスペースのフォルダがフォルダツリーの「ソースフォルダ」セクションにも重複表示されていたのを解消し、ワークスペースセクションだけに表示するようにした。
- ワークスペースセクション内でファイルやフォルダを追加・削除・リネームした時、ワークスペースセクションのツリーへ即時反映するようにした。
- 設定ダイアログの「ソースフォルダ」一覧と設定履歴の復元対象から、ワークスペース由来の項目を除外した。
- ソースフォルダ設定の保存時に、ワークスペース由来の項目が通常のソースフォルダとして保存されてしまう経路をサーバー側で遮断し、過去に保存されてしまった項目も読み込み時にワークスペース扱いへ自動復旧するようにした。
- チャットの対象セレクタで、同じワークスペースが「ワークスペース: ○○」と「ソース: ○○」の2つに重複表示されないようにした。
- フォルダツリーの検索・種類フィルタがワークスペースセクション内のツリーにも作用するようにした（重複表示の解消に伴い、ワークスペース内のファイルが検索に掛からなくなるのを防止）。
- ワークスペースセクション内で削除した項目をアンドゥで復元した時、ワークスペースセクションのツリーにも復元表示されるようにした。
- ワークスペース行の上下のすき間へファイルをドロップした時、意図せずソースフォルダ直下へ移動してしまわないようドロップを案内付きで拒否するようにした。
- ワークスペースの右クリックメニューからソースフォルダ用の項目（名前を変更・パスを変更・削除）を取り除き、「ワークスペースの管理...」を表示するようにした（従来は実行しても反映されない操作だった）。
- Deleteキー・F2キーがワークスペースやソースフォルダの行自体に作用して、フォルダ全体の削除・リネームに到達してしまう問題を防いだ。
- ワークスペースセクション内で矢印キーでのツリー移動・Shift範囲選択・ドラッグ矩形選択が機能するようにした。修飾キー付きクリックでワークスペース行が選択状態を奪わないようにした。
- ソースフォルダ追加でワークスペース登録済みのフォルダを指定した時、追加したように見えて消える挙動をやめ、登録済みである旨を案内するようにした。
- 表示バージョンを `0.6.075` に更新した。

検証: 徹底チェック（操作チェーン追跡で検出した12件を修正）、関連JavaScript/Python構文チェック、ソースフォルダ一覧API回帰unittest、ワークスペースフロントエンド回帰unittest、フォルダツリー回帰unittest、バージョンメタデータunittest、全体テストのベースライン比較（新規失敗ゼロ）、`git diff --check` PASS。

## v0.6.074 (2026-06-06)

### feat(sheet): エントリ名の自動生成をメニューから実行可能にした

- エントリ名列のヘッダーメニューに「エントリ名を自動生成」を追加し、列全体のエントリ名をまとめて生成できるようにした。
- 各エントリ行の `…` メニューにも「エントリ名を自動生成」を追加し、単一エントリだけを対象にできるようにした。
- 自動生成ダイアログで、名前に使う列をチェックボックスで選択できるようにした。
- 名前生成時、ページは `p0001`、コマは `c01` 形式へ正規化し、リレーション列は参照先エントリ名へ解決してから名前へ反映するようにした。
- `/api/entity/auto-name` を追加し、列全体・指定エントリ・指定エントリ名リストの自動生成を同じ経路で処理できるようにした。
- 表示バージョンを `0.6.074` に更新した。

検証: 徹底チェック、関連Python/JavaScript構文チェック、エントリ名自動生成API回帰unittest、シートプロパティメニュー回帰unittest、シートエントリ作成API回帰unittest、バージョンメタデータunittest、`git diff --check` PASS。

## v0.6.073 (2026-06-05)

### fix(sheet): 複数選択と一括メニューの操作安定性を改善

- シートで同じ列の複数セルを選択した状態で候補メニューから値を設定した時、選択中の同列セルすべてへ同じ値を反映するようにした。
- セレクト/リレーションなど複数選択系の候補メニューで、左キーで先頭、右キーで確定へ移動し、検索入力直後の下キーで「追加」候補へ移動できるようにした。
- 複数セル選択時の一括処理フロートメニューをシートのスクロール領域ではなくパネル本体へ配置し、シートをスクロールしてもパネル内の定位置に残るようにした。
- 複数セルコピー/貼り付けE2Eの混在プロパティ範囲選択を安定化し、リレーション/セレクト/数値/テキスト混在範囲のコピー、貼り付け、Delete削除まで検証できるようにした。
- シートmicro E2Eに、一括処理フロートメニューがスクロールに追従せずパネル内へ固定される専用ケースを追加した。
- 表示バージョンを `0.6.073` に更新した。

検証: 徹底チェック、関連JavaScript/Python構文チェック、シートE2E整合性/微細挙動カバレッジ/バージョンメタデータunittest、Chrome実ブラウザE2E 3件（候補型キーボードドロップダウン、複数セルコピー貼り付け、複数選択フロートメニュー固定）、`git diff --check` PASS。

## v0.6.072 (2026-06-05)

### fix(sheet): 候補型セルの確定と追加位置を安定化

- シートのセレクト/マルチセレクト/リレーション/マルチリレーション/ユーザー系セルを候補型として正規化し、既存値セルでEnterやセル内 `+` 操作が通常テキスト入力へ落ちないようにした。
- セル編集のEnter確定時、保存完了を待たずにローカル状態とセル表示を即時更新し、編集状態を閉じて同じセルだけにアクセントカラー枠を戻すようにした。
- マルチセレクト/マルチリレーション/マルチユーザーの候補メニューで、キーボードだけで候補選択と確定ボタン実行ができるようにした。
- 最下行で下キーを押して新規エントリを作成した時、挿入位置が選択中の行付近へずれず、常に表示順の一番下へ追加されるようにした。
- 配信用プロパティ断片のボタン型保存経路を通常実装と同期し、総合プロパティE2Eで保存確認がタイムアウトする経路を修正した。
- シートmicro E2Eに、候補型セルのEnter起動、マルチ系キーボード確定、Enter確定速度、最下行追加位置、全プロパティ型の総合確認を追加/更新した。
- 表示バージョンを `0.6.072` に更新した。

検証: 徹底チェック、関連JavaScript構文チェック、シートE2E整合性/配信用プロパティ断片/プロパティ型回帰/バージョンメタデータunittest、Chrome実ブラウザE2E 4件（候補型キーボードドロップダウン、型別セル編集、エントリ追加位置、全プロパティ型）、`git diff --check` PASS。

## v0.6.071 (2026-06-05)

### fix(sheet): 既存セル値のEnter確定を安定化

- シートの既存テキスト/数値セルを編集している時、Enterで入力値を保存し、編集状態を閉じて、同じセルだけにアクセントカラー枠を戻すようにした。
- 保存後にセルが部分更新または再描画された場合でも、古いDOM参照ではなく描画後の同じ行/列セルを再取得してアクティブセルを復元するようにした。
- シートmicro E2Eに、既存数値セルのEnter確定後に編集UIが閉じ、保存値が反映され、編集元セルだけがアクセント枠になる検証を追加した。
- 表示バージョンを `0.6.071` に更新した。

検証: 徹底チェック、関連JavaScript構文チェック、シート分割JS/既存値Enter確定E2E整合性/バージョンメタデータunittest、Chrome実ブラウザE2E 2件（型別セル編集、アクティブセル矢印移動）、`git diff --check` PASS。

## v0.6.070 (2026-06-04)

### test(sheet): 基本操作E2Eを実操作経路まで拡充

- シートのテキスト/数値セルで、実際に表示されるセル内 `+` ボタンから入力を開始し、Enter確定、外クリック確定、Escape取消が機能することをE2Eで確認するようにした。
- 下端 `+ 新規`、行頭 `+`、行複数選択、一括削除の基本操作について、保存APIが遅い状態でも表示が即時反映されることをE2Eで確認するようにした。
- 複数セルコピー/貼り付けの保存確認を、SQLite優先の現行仕様に合わせてシートAPI上の保存値で検証するようにし、Markdown書き戻し待ちによる誤検出を避けた。
- シート基本操作のE2Eカバレッジ定義と整合性テストを更新し、表示操作、入力確定、取消、行一括削除の監査漏れを検出できるようにした。
- 表示バージョンを `0.6.070` に更新した。

検証: 徹底チェック、関連JavaScript/Python構文チェック、シートE2E整合性/プロパティUI/ツールバー表示/バージョンメタデータunittest、Chrome実ブラウザE2E 19件（シート基本操作全ケース、制作管理タスクリスト、再計算フローを含む）、`git diff --check` PASS。

## v0.6.069 (2026-06-04)

### fix(sheet): セル入力と候補ドロップダウンの基本操作を安定化

- シートのセレクト/リレーションセルで、Enter操作がマウスカーソル下の別セルへ誤爆し、別セルの候補ドロップダウンが開く経路を防いだ。
- セル候補ドロップダウンの表示位置を共通処理へ寄せ、Meldexウィンドウ左上へ飛ぶことがある問題を修正した。
- セレクト/リレーション値の選択後、保存完了を待たずにセル表示へ即時反映し、保存失敗時は元の値へ戻すようにした。
- リレーション候補はキャッシュを優先して表示し、裏で更新するようにして、候補表示とIDから表示名への反映待ちを減らした。
- セル入力欄や候補ドロップダウン上では不要なツールチップを表示しないようにした。
- シートmicro E2Eに、セレクト/リレーションのクリック、Enter、候補選択、即時表示、ホバーセル誤爆防止を確認する検証を追加した。
- 表示バージョンを `0.6.069` に更新した。

検証: 徹底チェック、関連JavaScript構文チェック、シートE2E整合性/プロパティUI回帰unittest、Chrome実ブラウザE2E 5件（セレクト/リレーションキーボードドロップダウン、型別セル編集、全プロパティ型、プロパティ設定自動保存、リレーション/チェックボックスセル）、`git diff --check` PASS。

## v0.6.068 (2026-06-04)

### perf(sheet): 複数セル貼り付けの表示反映を高速化

- シートの複数セル貼り付けと複数セル削除を、保存完了待ちではなくローカル状態とセル表示へ先に反映するようにし、リレーション/セレクトを含む範囲操作の体感遅延を減らした。
- 同じエントリの複数セル保存は競合防止のため保存処理を直列に保ちつつ、表示更新、描画、保存、必要時の再読み込みの順序を整理した。
- 壊れた値参照が混ざったセルでも、ディレクトリを値ファイルとして更新しようとしないようにし、必要な場合は新規値として保存し直すようにした。
- `/api/value` の更新先にフォルダが渡された場合は、frontmatterとして読まずに明示的な400エラーを返すようにした。
- シートセル上ではMeldexツールチップとブラウザ標準ツールチップを表示しないようにし、セル閲覧中に不要な吹き出しが重ならないようにした。
- 表示バージョンを `0.6.068` に更新した。

検証: 徹底チェック、関連JavaScript構文チェック、シートE2E整合性/ツールチップ/API値更新回帰/バージョンメタデータunittest、Chrome実ブラウザE2E 1件（複数列複数行コピー貼り付けとDelete削除）、`git diff --check` PASS。

## v0.6.067 (2026-06-04)

### test(e2e): 複数選択メニューとスケジューラー連動の検証を確定

- 複数選択フロートメニューの共通化と、スケジューラー選択時のオプションパネル即時同期について、関連するパネル/ドック/起動復元/カレンダー/シート監査を再確認した。
- カレンダーE2Eのイベント詳細操作を、現行のオプションパネル属性と旧詳細パネルIDの両方に対応させ、詳細パネルが非表示タブに残る時も表示中の詳細タブを開いてから更新/削除するようにした。
- シート監査の期待セレクタを、新規行スペーサーを除外する現行DOM構造へ合わせた。
- 表示バージョンを `0.6.067` に更新した。

検証: 徹底チェック、関連unittest一式、Chrome実ブラウザE2E 2件（カレンダー更新/移動/Undo/Redo/削除、シート複数セル選択コピー貼り付け）、`node --check app/gb-e2e-actions.js`、`git diff --check` PASS。

## v0.6.066 (2026-06-04)

### fix(sheet): 複数セル貼り付けと削除の即時反映を修正

- シートでリレーション/セレクト/数値/テキストを含む複数列複数行のセル範囲を、Ctrl+C / Ctrl+V で同じ形のまま貼り付けられるようにした。
- セル内入力欄やリレーション候補ドロップダウンが開いている状態でも、複数セル選択中の Ctrl+C / Ctrl+V / Delete がシート範囲操作として処理されるようにした。
- 複数セル選択中の Delete / Backspace で選択セルの値を削除できるようにし、セル内入力欄のクローズ競合で内部エラーが出る経路を防いだ。
- 複数行の一括削除は、バックエンド削除とゴミ箱/Undo情報の保存を待つ前に表示上の行を即時削除するようにし、削除操作の体感遅延を減らした。
- バージョンスナップショットの自動整理で、並行保存中に同じ自動スナップショットが先に削除されても失敗しないようにした。
- シートmicro E2Eに、リレーション/セレクト/数値/テキスト混在範囲のコピー/貼り付け、ドロップダウン表示中の貼り付け、セル内入力欄フォーカス中のDelete削除を追加した。
- 表示バージョンを `0.6.066` に更新した。

検証: 徹底チェック、関連JavaScript構文チェック、シートE2E整合性/分割JS構文/一括削除/バージョン保存競合unittest、Chrome実ブラウザE2E 1件（複数列複数行コピー貼り付けとDelete削除）、バージョンメタデータunittest、`git diff --check` PASS。

## v0.6.065 (2026-06-04)

### fix(sheet): セル複数選択と複数列コピー貼り付けを安定化

- シートの Ctrl/Shift セル複数選択を、キーボード操作とポインタ操作の両方で安定して扱えるようにした。
- Ctrl+C / Ctrl+V で、複数プロパティ列を含む2次元のセル範囲をコピーし、別の行・別の列へ同じ形で貼り付けられるようにした。
- セル入力中や直前の選択状態が残っている時でも、最新のアクティブセルを基準に貼り付けるようにして、古いセルへ貼り付く経路を修正した。
- セレクト/リレーションセルの Enter やキーボード操作が、テキスト入力へ崩れず候補ドロップダウンを開くことを実機E2Eで確認するようにした。
- シートmicro E2Eに、複数列複数行の選択状態と、別行・別列への貼り付け後状態を保存するスクリーンショットを追加した。
- 表示バージョンを `0.6.065` に更新した。

検証: 徹底チェック、E2E生成定義再生成、シートE2E整合性/微細挙動カバレッジunittest、バージョンメタデータunittest、関連JavaScript構文チェック、Chrome実ブラウザE2E 2件（複数列複数行コピー貼り付け、セレクト/リレーションキーボードドロップダウン）、スクリーンショット2枚生成、`git diff --check` PASS。

## v0.6.064 (2026-06-03)

### fix(sheet): エントリ追加位置と即時表示を修正

- シートの下端 `+ 新規` は、選択中セルや選択中列の位置ではなく、表示順の末尾へ即時追加するようにした。
- 各行先頭の `+` は、保存完了を待たずに対象行の直下へ即時追加するようにした。
- エントリ作成APIが遅延している間もローカル表示と手動順序を同期し、下端 `+` と行頭 `+` を連続操作した時に行が消えたり `項目が見つかりません` 通知が出たりしないようにした。
- 行選択チェックボックスの通常クリック、プログラムクリック、Shift範囲選択の状態同期を整理し、E2E監査でも変更イベントを検出できるようにした。
- シートmicro E2Eに、下端 `+` の末尾追加、行頭 `+` の直下追加、作成API遅延中の即時表示、連続追加後の行維持を確認するケースを追加した。
- 表示バージョンを `0.6.064` に更新した。

検証: 徹底チェック、E2E生成定義再生成、シートE2E整合性/微細挙動カバレッジunittest 32件、関連Python構文チェック、Chrome実ブラウザE2E 3件（下端/行頭＋位置と連続追加、既存新規行即時表示、行選択一括編集）、`git diff --check` PASS。

## v0.6.063 (2026-06-03)

### test(sheet): シート全項目の実機網羅監査を追加

- シートの現行17プロパティ型と56設定項目を、保存、セル表示、再読み込み、検索または対象挙動、設定UI反映まで確認するChrome実機E2E監査として追加した。
- 追加監査は開発版HTMLだけで読み込み、通常版には読み込まない構成にして、通常起動時の負荷を増やさないようにした。
- シートmicroケースへ制作管理タスクリストスキーマとサンプル再計算フローを含めた網羅確認を通し、作品・メンバー・タスク・シフト・再計算の実機連動を確認した。
- セル複数選択時のフロートメニュー更新を即時化し、Ctrl/Shift複数選択後にコピー/貼り付け/選択解除の操作メニューが安定して表示されるようにした。
- 表示バージョンを `0.6.063` に更新した。

検証: 徹底チェック、シート全17プロパティ型/56設定項目のChrome実機網羅E2E、シートmicro E2E 17件、制作管理サンプル再計算Chrome E2E、シート監査/微細挙動/生成E2E整合性unittest 37件、関連JavaScript構文チェック、バージョンメタデータ回帰、`git diff --check` PASS。

## v0.6.062 (2026-06-03)

### perf(sheet): シート保存をSQLite高速ストア対応へ移行

- 新形式シートの読み込み・セル保存・エントリ追加を、隠しSQLiteストアへ即時同期する構成に変更し、Markdown互換ファイルは既存リンク/スマートシート/外部参照向けのミラーとして維持した。
- シート検索用のインデックスをSQLite側にも持たせ、通常検索、スマートシート、既存FTS通知が保存形式変更後も同じ情報を参照できるようにした。
- ボタン型セルの保存先を表示中エントリの実ファイルパスから解決し、新形式シートで別フォルダへ誤保存される経路を修正した。保存後は対象セルを即時更新し、必要な再読み込みだけを後追いで行う。
- ボタン型セルは描画時点のアクションを保持し、プロパティ設定保存とセルクリックが重なっても処理が抜けないようにした。単純な値設定ではシート全体を再読み込みせず、対象セルだけを更新する。
- シート設計意図に、SQLite正本・Markdown互換ミラー・検索インデックス連動の仕様を追記した。
- 表示バージョンを `0.6.062` に更新した。

検証: 徹底チェック、シート総合AI監査、シートSQLiteストア/検索/書き込み同期unittest、シート関連回帰unittest 81件、Python構文チェック、Chrome実ブラウザE2E 7件、チェックボックス監査 manifest PASS。

## v0.6.061 (2026-06-03)

### fix(sheet): セレクトセルの選択操作を修正

- シートのセレクトプロパティ列で、空セルの `＋` ボタンを押しても候補ドロップダウンが開かない問題を修正した。
- セレクトセルの既存値をクリックした時も候補ドロップダウンを開き、選択肢を置き換えて保存できるようにした。
- プロパティ設定で追加したセレクト選択肢を、セル操作時に最新設定から読み直すようにして、設定直後でも候補として選べるようにした。
- シート微細挙動E2Eに、空セレクトセルの `＋` クリック、候補選択、既存値クリックからの置き換え、保存ファイル反映を確認するケースを追加した。
- 表示バージョンを `0.6.061` に更新した。

検証: 徹底チェック、セレクトセル空値/既存値/プロパティ設定反映の操作チェーン確認、関連JavaScript構文チェック、関連Python構文チェック、シートE2E整合性/微細挙動カバレッジunittest、Chrome実ブラウザE2E シート微細挙動2件、`git diff --check` PASS。

## v0.6.060 (2026-06-02)

- 制作管理の未指定時の初期値をマンガ前提から汎用仕様へ変更し、プリセットは `汎用`、階層ラベルは `項目`、作業内容は `制作`、作業規模は `標準` で始まるようにした。
- 汎用タスク生成では `ページ` / `コマ` を空欄のままにし、タスク名から既定の `標準` を省いて、マンガ以外の制作物でも自然なタスクリストになるようにした。
- ページ数・コマ数または `マンガ` プリセットが指定された場合は、従来どおり `p0001` / `c01` 形式のページ・コマ候補とマンガ向け初期値を使う互換経路を維持した。
- 制作管理のAPI/フロント/Chrome E2Eテストに、汎用初期値と既存マンガ生成の両方を確認するケースを追加した。
- 表示バージョンを `0.6.060` に更新した。

## v0.6.059 (2026-06-02)

### fix(production): 制作管理の実機再計算フローを補強

- 実機テスト用のワークスペース、ユーザー、作品、勤務時間、タスクを作成するChrome E2Eを追加し、制作管理のタスク分配と再計算を実ブラウザで確認できるようにした。
- ワークスペース作成者とメンバーが制作管理のメンバーリストへ同期されない経路を修正し、制作管理初期化時にもワークスペースメンバーを再同期するようにした。
- タスク作成時の作業期間を作品リストへ反映し、作品ごとの開始/終了予定が制作管理データとして残るようにした。
- 再計算で前工程より後工程が先に割り当てられないようにし、前工程が未割当の場合は後工程も保留扱いにした。
- 制作管理タスクリストの指示用列は維持し、今回の実機フローで不要と判断できる追加列は作らない方針を確認した。
- 表示バージョンを `0.6.059` に更新した。

検証: 徹底チェック、制作管理のユーザー操作/関数チェーン確認、ワークスペースメンバー同期/タスク作成/再計算/カレンダー反映/タスクリスト表示のChrome実ブラウザE2E、関連JavaScript/Python構文チェック、制作管理/ワークスペース/フロント回帰/微細挙動カバレッジunittest、`git diff --check` PASS。

## v0.6.058 (2026-06-02)

### feat(sheet): セル操作とタスクリスト列定義を整備

- シートのセル入力中に Enter を押した時、値を確定して同じセルを選択したままにするようにした。
- シートのテーブル表示で Ctrl/Cmd クリックと Shift クリックによるセル複数選択を追加し、Ctrl/Cmd+C / Ctrl/Cmd+V で同一プロパティ列のセル値をコピー/貼り付けできるようにした。
- 制作管理のタスクリストで `作品タイトル` を作品リストへのリレーションにし、`ページ` と `コマ` をマルチセレクトとして扱うようにした。
- タスク作成時に、作品ページ数に応じた `p0001` 形式のページ候補と、生成済みタスクに応じた `c01` 形式のコマ候補を補完するようにした。
- シート微細挙動E2Eに Enter確定、Ctrl/Shiftセル選択、Ctrl+C/V貼り付けを確認するケースを追加し、タスクリストスキーマE2Eで作品リレーションとページ/コマ候補も検査するようにした。
- 表示バージョンを `0.6.058` に更新した。

### fix(workspace): ワークスペースのフォルダ/チャット/メンバー連動を補強

- ワークスペースフォルダをフォルダツリーのルート候補へ統合し、ワークスペース内のフォルダをソースフォルダと同じツリー表示で開けるようにした。
- ワークスペースサイドバーを通常のフォルダツリーノードとして描画し、選択時にアクティブワークスペースとチャット対象が同期されるようにした。
- CLIチャットでワークスペース対象を `workspace_id` として渡し、絶対パスの `source_folder` がない場合でもワークスペースフォルダを実行対象として解決できるようにした。
- ワークスペース作成時、設定済みユーザーアカウントを作成者/管理者として登録する経路を補強し、初期化順によって `anonymous` になりにくくした。
- シートのユーザープロパティ候補を、アクティブワークスペースのメンバーから優先して読み込むようにし、制作管理/カレンダー/シートのメンバー候補がワークスペース基準で揃うようにした。
- 分割済みシート値エディタの静的テストを、split loader の実装本体を読む形に更新した。

検証: 徹底チェック、ワークスペース連動監査、シートセル編集/選択/コピー/貼り付け操作チェーン確認、タスクリスト生成/列定義/候補補完操作チェーン確認、関連JavaScript/Python構文チェック、ワークスペース/CLIチャット/制作管理/シートプロパティ/シートツールバー/バージョンメタデータunittest、Chrome実ブラウザE2E シート微細挙動12件、`git diff --check` PASS。

## v0.6.057 (2026-06-02)

### fix(sheet): セル編集とプロパティ型E2Eの信頼性を強化

- シートの微細挙動E2Eを、プロパティ設定、チェックボックス/リレーション、基本入力型、画像/ユーザー/ボタン/チャットなどの残りの型に分け、全プロパティ型を合算で網羅していることを静的テストで検証するようにした。
- セル内のボタン/チャット操作に安定した検査用識別子を付け、ボタン実行後のシート再描画を待つことで、実機E2Eが表示反映前に通過しないようにした。
- タスクリストシートの読み込み表示が残り続ける問題を検出できるようにし、読み込み完了後にスキーマと主要列を確認するE2Eを追加した。
- チェックボックス空セル、セレクト候補、日時範囲、複数リレーション、画像、ユーザー、複数ユーザー、計算、集計、複数ソースリレーション、チャット、ボタンの主要操作をChrome実機E2Eで確認できるようにした。
- 表示バージョンを `0.6.057` に更新した。

検証: 徹底チェック、シート設計意図とE2E定義の照合、関連JavaScript構文チェック、シートE2E整合性unittest、微細挙動/高リスクE2E定義unittest、Chrome実ブラウザE2E シート微細挙動11件、`git diff --check` PASS。

## v0.6.056 (2026-06-02)

### feat(sheet): 日時プロパティ設定を自動保存化

- シートのプロパティ型表示を `日付` から `日時` へ統一し、日時プロパティのデータソースに `作成日時（自動・読み取り専用）` を追加した。
- 既存の `最終更新日（自動・読み取り専用）` を `更新日時（自動・読み取り専用）` へ改名し、作成日時/更新日時は単一日時の読み取り専用ソースとして扱うようにした。
- シートのプロパティ設定パネルから `適用` と `詳細設定` ボタンを削除し、設定変更をリアルタイムに自動保存するようにした。
- プロパティ設定パネルの余白、入力幅、ラベル表示を整理し、全主要プロパティ型で設定項目がパネル外にはみ出さないようにした。
- セレクトプロパティで追加した選択肢を、セルのドロップダウンからすぐ選択して保存できるようにした。
- 作成日時メタデータをシート表示側へ渡し、テーブル/ギャラリー/派生セル表示で作成日時ソースを表示できるようにした。
- シート微細挙動E2Eに、プロパティ設定の自動保存、日時データソース、セレクト選択肢反映、設定UI崩れを確認するケースを追加した。

検証: 徹底チェック、シートプロパティ設定の操作チェーン確認、AI目視UI確認、関連JavaScript構文チェック、関連Python構文チェック、プロパティ型/日時/served props/シート表示/作成日時メタデータ回帰unittest、Chrome実ブラウザE2E シート微細挙動7件、`git diff --check` PASS。

## v0.6.055 (2026-06-02)

### fix(sheet): 日付プロパティの開始/終了時刻編集を改善

- シートの日付プロパティを、セル内の小さな入力欄ではなくポップアップで編集できるようにし、開始日/開始時刻/終了日/終了時刻を同じ場所で設定できるようにした。
- 日付セルごとに `時刻を含める` と `終了を設定` を切り替えられるようにし、開始と終了を持つ日時範囲を `2026-04-20T09:30|2026-04-21T18:45` の形式で保存できるようにした。
- 日付セルのクリア時に古い値が表示へ戻らないようにし、既存値編集、空セル追加、詳細側のシート表示でも同じ確定イベントで保存されるようにした。
- プロパティ型設定ダイアログの入力欄が左右の余白を潰していた問題を修正し、日付型の説明を `時刻（時分）を入力できるようにする` に整理した。
- シートのエントリ名列 `＋ 新規` クリック後、再読み込みボタンを押さなくても作成済みエントリがすぐ表示されるようにした。
- 分割済みシート関連スクリプトを読む回帰テストの参照先を、現行の split loader 構成に合わせて更新した。

検証: 徹底チェック、日付セル編集/空セル追加/プロパティ型設定/新規エントリ作成の操作チェーン確認、Playwright/Chrome実ブラウザ確認、関連JavaScript構文チェック、シート/日付/一括編集/served props回帰unittest、Chrome実ブラウザE2E シート微細挙動6件、`git diff --check` PASS。

## v0.6.054 (2026-06-02)

### feat(production): タスクリストと制作管理パネルを整理

- シートアプリのエントリ行 `＋` ボタンが行ドラッグに吸われて追加できない問題を修正し、クリック/キーボード操作から安定してエントリを追加できるようにした。
- スケジューラーの制作管理タブから重複していた操作専用タブをなくし、主要操作を概要に集約した。作業対象/作業内容/作業規模/メンバーの各リストは個別タブとして残し、一覧・検索・追加・詳細編集を整理した。
- 制作管理のユーザー向け表記を `スタッフ` から `メンバー` へ寄せ、担当者/担当者候補/メンバー名をワークスペースメンバーから選べるようにした。
- スケジューラーのツールバーにタスクリストシートを開くボタンを追加し、今日が予定日の自分のタスクをサイドバーへ表示するようにした。
- 作品リストに完了チェックを追加し、完了済み作品は既定で非表示にした。必要な場合は `完了の作品を表示` で確認できるようにした。
- 作品リストの作業期間を開始/終了日時で編集できるようにし、既存の制作管理シートへ新しい列定義を自動補修するようにした。

検証: 徹底チェック、シート行追加/制作管理パネル/担当者候補/タスクリスト導線/今日の自分のタスク/作品完了表示/作業期間編集の操作チェーン確認、関連JavaScript構文チェック、関連Python構文チェック、制作管理回帰unittest、Chrome実ブラウザE2E シート微細挙動6件・カレンダー微細挙動1件、`git diff --check` PASS。

## v0.6.053 (2026-06-01)

### feat(calendar): イベントカードの複数選択操作を統一

- スケジューラーのイベントカードを複数選択した時、フォルダ/シート/シナリオ側と同じように、選択件数と `複製` / `削除` / `選択解除` をまとめて選べる一括操作UIを表示するようにした。
- 選択中イベントカードの `…` メニューにも複数件向けの複製、削除、選択解除を追加し、ボタンとメニューのどちらからでも同じ処理へ進めるようにした。
- 通常イベントは一括複製/削除の対象にし、制作管理・打刻・繰り返し予定のインスタンスなど元データ管理のイベントは誤って一括変更しないようにした。
- シフト由来の勤務/休憩カードは親シフト単位で削除対象をまとめ、同じシフトのカードを複数選んでも重複削除しないようにした。
- 制作管理のユーザー向け表示を `作業` から `タスク` へ寄せ、タスク作成、タスクリスト、予定日時、目標時間などの表記を整理した。

検証: 徹底チェック、イベントカード選択/一括複製/一括削除/選択解除/カードメニュー/キーボード操作チェーン確認、関連JavaScript構文チェック、カレンダー/シフト/勤怠/制作管理回帰unittest、バージョンメタデータ回帰、`git diff --check` PASS。

## v0.6.052 (2026-06-01)

### fix(chat): 応答生成中のスクロール保持とAIタブ表記を改善

- AI/CLI応答の生成中に発言一覧を上へスクロールしている場合は、更新ごとに最下部へ戻さないようにした。
- 発言一覧の自動追従を最下部付近にいる場合だけに限定し、ユーザーが過去発言を読んでいる間は位置を保つようにした。
- チャットパネルの `LLM` タブ表示を `AI` に変更し、内部の `llm` モード識別子は互換性のため維持した。
- チャット対象選択まわりの回帰テストを、ワークスペース/フォルダ対象の現行文言に合わせて更新した。

検証: 徹底チェック、通常AI/CLIストリーミングのスクロール操作チェーン確認、関連チャット/CLI/右パネル回帰unittest、バージョンメタデータ回帰、`git diff --check` PASS。

## v0.6.051 (2026-06-01)

### feat(calendar): 制作管理タブとシフト予定表示を改善

- スケジューラーの制作管理タブを、概要/作業/作品/作業対象/作業内容/作業規模/スタッフの各リストへ分け、右オプションパネル内で追加・検索・編集・元シートを開く操作を完結できるようにした。
- 作業作成フォームを制作管理タブ内へ統合し、作品、ページ数、コマ数、作業対象、作業内容、作業規模を一覧を見ながら設定できるようにした。検索後の入力フォーカス維持、非同期処理中のボタン表示、失敗時のステータス表示も補強した。
- シフト勤務テンプレート編集ダイアログの時刻欄を、手入力と15分刻みのドロップダウン選択の両方で使えるようにし、時刻候補ポップアップのスクロール過多で選べない時間が出ないようにした。
- 月表示ではシフト休憩カードを勤務カードへ集約し、勤怠カードも同一ユーザー/同日で重複表示しないようにした。シフトカレンダーへの新規追加は月表示の日付クリックに限定した。
- 月/週/複数日/日表示がパネル内へ収まるようにカレンダー本体の高さとスクロール領域を整理し、制作管理の再計算ではシフト勤務テンプレート由来の休憩時間も作業可能時間から差し引くようにした。

検証: AI目視UXレビュー、徹底チェック、全スクリプト全行ソーススキャン、関連JavaScript構文チェック、スタンドアロンJavaScript全件構文チェック、スタンドアロンPython全件構文チェック、制作管理/シフト/勤怠/スケジューラー回帰unittest、Chrome実ブラウザE2E `targeted-calendar-micro-event-geometry-state`、`git diff --check` PASS。

## v0.6.050 (2026-06-01)

### feat(calendar): シフトカード分離とワークスペースメンバー同期を強化

- シフト勤務テンプレートから作成される予定を、勤務時間カードと休憩時間カードに分けて表示するようにした。
- 勤務時間カードと休憩時間カードの背景色を、スケジューラーのテーマ設定から変更できるようにした。
- シフト追加/編集/削除とイベントカードメニューの削除を楽観更新し、保存・削除完了待ちの表示タイムラグを減らした。
- 休憩カードを開く/削除する場合も親シフトへ正しく紐づけ、休憩カードが制作管理の作業予定として外部送信されないようにした。
- ワークスペース作成者を管理者として表示し、ワークスペースメンバーを制作管理のスタッフへ自動登録するようにした。既存スタッフの担当作業などの入力済み項目は、ワークスペース同期で空欄上書きしない。
- 起動時と「自分を同期」操作で、現在のユーザーを登録済みワークスペースのメンバーへ同期するようにした。
- 設定ダイアログのユーザータブから旧チームメンバーセクションを削除し、ユーザー未登録時の案内をワークスペースメンバー追加へ統一した。

検証: 徹底チェック、関連JavaScript構文チェック、関連Python構文チェック、シフト/休憩カード/生成イベント保護/ワークスペース/制作管理/スケジューラー回帰unittest、`git diff --check` PASS。

## v0.6.049 (2026-06-01)

### feat(workspace): ワークスペースをソースフォルダから分離

- ワークスペースを設定/サイドバーで管理できるようにし、作品・フォルダツリーのソースフォルダとは別に、チャットや共同作業の対象単位として選べるようにした。
- チャットの「対象」でワークスペースを選択できるようにし、ワークスペース選択中のルーム、メッセージ、履歴、自動保存、添付先をワークスペース配下へ揃えた。
- スケジューラーの出退勤/シフト関連のチーム候補をワークスペースから読み込むようにし、サイドバーやテンプレート表示の文言もワークスペース基準へ統一した。
- ワークスペースの作成/更新/削除/メンバー管理APIとメタデータ保存を追加し、既定ソースフォルダへ暗黙に戻らない回帰テストを追加した。

### feat(calendar): 制作管理リストをオプションパネルへ統合

- スケジューラーの「制作管理」タブに概要、作業、作品、スタッフ、作業設定、シフト/勤怠、再計算、外部送信のタブを表示し、主要リストをオプションパネル内で確認できるようにした。
- 制作管理APIに概要取得、リスト取得、項目作成/更新、カレンダーイベントから元作業を開く導線を追加した。
- 制作管理タスクのカレンダーイベントをクリックした時、元シートの作業行をオプションパネル内で開けるようにした。
- AI目視レビューで、右パネル内のスケジューラーから制作管理を開けない問題、ワークスペース設定タブが初期化されない問題、制作管理の初回読み込み中に操作ボタンが見えない問題を修正した。

検証: AI目視レビュー、徹底チェック、関連JavaScript構文チェック、関連Python構文チェック、ワークスペース/チャット対象/制作管理/スケジューラー/シフト勤怠/設定回帰unittest、Chrome実ブラウザE2E `targeted-calendar-micro-event-geometry-state` PASS。

## v0.6.048 (2026-06-01)

### feat(calendar): シフト勤務テンプレートとチャット対象を改善

- チャットの「対象」でホームフォルダを選択できるようにし、未設定時の初期値をホームフォルダにした。
- スケジューラーのイベントカードにホバー時の「…」メニューを追加し、イベントを開く/削除する操作をカード上から呼び出せるようにした。
- シフトカレンダーに複数のシフト勤務テンプレートを設定できる欄を追加し、勤務開始/終了、チーム、任意の休憩時間を保存できるようにした。
- シフトカレンダー選択中に日付セルをクリックした場合、テンプレートが1件なら即時適用し、複数ある場合はテンプレート選択メニューから適用できるようにした。
- スケジューラー関連の編集権限に「設定されたメンバーのみ」を追加し、イベントに設定されたメンバーだけが対象イベントを編集できるようにした。
- シフト/勤怠カレンダーの設定メニューからチームフォルダ入力欄をなくし、チーム選択だけでチームフォルダが反映されるようにした。サイドバーのカレンダーセクション名もチーム名を優先して表示するようにした。
- パネルタブでスケジューラーを選んだ時、オプションパネルの内容がすぐスケジューラー向けに同期されるようにした。
- AI目視レビューでシフト勤務テンプレートの休憩行が読み取りにくい点を確認し、「休憩1/休憩2」と「開始/終了」を明示して、行追加/削除後も番号が揃うようにした。

検証: AI目視レビュー、徹底チェック、関連JavaScript構文チェック、関連Python構文チェック、スケジューラー/シフト/勤怠/制作管理/チャット対象回帰unittest、Chrome実ブラウザE2E `targeted-calendar-micro-event-geometry-state` PASS。

## v0.6.047 (2026-06-01)

### fix(calendar): スケジューラーのオプションタブ導線を統一

- スケジューラーのオプションパネルに「スケジューラー設定」「制作管理」を常設タブとして追加し、ツールバーを押さなくてもタブとして選べるようにした。
- スケジューラーのツールバーから個別の「制作管理」「設定」ボタンを削除し、他アプリと同じ「オプションを開く」ボタンへ統一した。
- オプションパネルを後から開いた場合や、既に開いているオプションパネルを再表示した場合でも、現在開いているスケジューラーのタブ群が同期されるようにした。
- 「スケジューラー設定」「制作管理」タブを直接クリックした時に、それぞれの内容をオプションパネル内で描画するようにした。
- スケジューラー系タブの表示/非表示、タブ並べ替え、ペイン復元、テーマタブとの共存に関する回帰テストを更新した。

検証: 徹底チェック、関連JavaScript構文チェック、スケジューラー/制作管理/オプションパネル/ツールバー回帰unittest、バージョンメタデータ回帰、`git diff --check` PASS。

## v0.6.046 (2026-05-31)

### fix(chat): チャットの暗黙対象を現在開いている項目に変更

- LLMチャットとCLIチャットで明示的な対象がない場合、保存済みの既定フォルダではなく、現在開いているファイルまたはフォルダを現在の対象として渡すようにした。
- 現在の対象がフォルダならそのフォルダ、ファイルなら親フォルダを作業フォルダとして使い、検索や参照が画面上の文脈から外れないようにした。
- 送信中の保留メッセージ、自動保存、履歴操作が送信開始時の対象を維持するように、ストリーム中の対象状態を分離した。
- 現在開いている対象の解決を `gb-chat-current-target.js` に分離し、通常HTMLと開発HTMLの読み込み順へ追加した。
- CLI/MCPまわりの文言を「現在の作業フォルダ」「現在の対象フォルダ」へ揃え、作品フォルダ前提の表現を避けるようにした。
- 新しいチャット対象解決ファイルを全ファイル全行チェックのバッチ定義へ追加し、関連回帰テストを更新した。

検証: 徹底チェック、関連JavaScript構文チェック、関連Python構文チェック、チャット対象/CLI/ストリーム/履歴/保存/MCP/Skills回帰unittest、チャット対象解決Nodeシミュレーション、バージョンメタデータ回帰、`git diff --check` PASS。

## v0.6.045 (2026-05-31)

### fix(chat): チャット対象の暗黙既定を廃止

- チャットの対象セレクタから「個人Vault（既定）」を削除し、対象ソースフォルダが未選択の場合は「未選択」として明示するようにした。
- LLM送信、CLI送信、自動保存、履歴一覧、履歴検索、履歴ドロップダウン、チームチャットのルーム取得/作成/送信で、対象ソースフォルダ未選択のまま既定フォルダへ進まないようにした。
- ファイルからチャットを開く際は、画面上で選択可能なソースフォルダ候補から対象を検出するようにし、検出できない場合はチャットを開かないようにした。
- LLMストリームとCLIチャットストリームのAPI入口でも対象ソースフォルダ未指定を拒否し、API経由で既定フォルダへ誤って流れないようにした。
- チャット対象選択まわりの回帰テストを更新し、旧表示・未選択送信・分割ファイル読み込み検査の再発を検出できるようにした。

検証: 徹底チェック、関連Python構文チェック、チャット対象/CLI/ストリーム/履歴/保存回帰unittest、チャット関連回帰unittest、バージョンメタデータ回帰、`git diff --check` PASS。

## v0.6.044 (2026-05-29)

### feat(shell): フォルダリストの列幅調整と種類表示を拡張

- フォルダパネルのリスト表示で、列ヘッダーの境界をドラッグして列幅を調整できるようにした。調整した幅はリスト表示設定として保存し、列順D&Dやヘッダークリック並べ替えとは干渉しないようにした。
- 種類列はアプリ内の大分類ではなく、WindowsではOSシェルが返す種類名を優先して表示するようにした。取得できない環境やPWA側では拡張子単位のフォールバック種類名を使う。
- 種類列の初期幅を広げ、OS由来の詳細な種類名が見切れにくい表示にした。
- 実ブラウザE2Eで列幅D&D、保存値反映、リスト項目選択への誤伝播がないことを検査するようにした。

検証: 関連JavaScript構文チェック、`python -m py_compile app/meldex_file_index_service.py`、フォルダAPI/PWAデータアクセス/フォルダUI/ツールバー/分割アセット回帰unittest、Chrome実ブラウザE2E `targeted-shell-micro-folder-display-filter` PASS。

### fix(shell): フォルダリストの列操作とアイコン表示を改善

- フォルダパネルのリスト表示で、ヘッダークリック時に実際の表示順が切り替わることを明示的に検証し、名前順は記号に引っ張られにくい自然順にした。並べ替え後は先頭へスクロールを戻し、変化が分かるようにした。
- リスト左端の種別アイコンが行内で見切れないよう、アイコンセルと行高を調整した。
- ヘッダー上の余白で背後のリスト行が見えないよう、リスト表示時の上パディングをなくした。
- リスト列に作成者・更新者を追加し、将来のメタデータや既存項目の作成者/更新者系フィールドを表示・並べ替えに使えるようにした。
- ヘッダー項目をドラッグ&ドロップで左右に並べ替えられるようにし、列順を保存するようにした。ヘッダー操作が下のリスト項目選択へ漏れないようイベント伝播を抑止した。
- 実ブラウザE2Eでヘッダークリック後の実並び、列D&D、アイコン見切れ、列セルの行内表示を検査するようにした。

検証: 徹底チェック、関連JavaScript構文チェック、フォルダUI/ツールバー/分割アセット/フォルダツリー/micro coverage/フォルダAPI回帰unittest、Chrome実ブラウザE2E `targeted-shell-micro-folder-display-filter` PASS。

### feat(shell): フォルダリスト表示に列ヘッダーと並べ替えを追加

- フォルダパネルのリスト表示で、名前・種類・作成日時・更新日時・サイズの列ヘッダーを常に上部へ表示するようにした。
- 各列ヘッダーをクリックすると、表示中の項目をその列で並べ替えられるようにし、現在の並び順をリスト表示の設定として保持するようにした。
- ローカルフォルダ詳細APIとPWA側のフォルダ項目詳細に作成日時を追加し、リスト表示の作成日時列に使えるようにした。
- 実ブラウザE2Eにリストヘッダー、列クリックソート、各列セルの表示崩れチェックを追加した。

検証: 徹底チェック、関連JavaScript構文チェック、`python -m py_compile app/meldex_file_index_service.py`、フォルダAPI/PWAデータアクセス/フォルダUI/ツールバー/分割アセット/micro coverage回帰unittest、Chrome実ブラウザE2E `targeted-shell-micro-folder-display-filter` PASS。

### fix(shell): フォルダツリーとCLIチャット周辺の表示崩れを修正

- フォルダツリーの種類フィルタから「旧シナリオ」を削除し、既存データに残る `scenario` 種別はユーザー向けには「シナリオ」として扱うようにした。
- 入力欄やチャット入力中に太い角丸フォーカス枠が出ないよう、入力系のフォーカス表示を通常枠に戻した。
- フォルダ項目メニューでOS由来メニューを後から追加したときも、メニューが画面外へはみ出さないよう位置再調整を追加した。
- CLIチャットの出力デコードで、UTF-8とWindowsロケール/CP932が混在する出力でも文字化けしにくいようにし、CLI子プロセスにはUTF-8出力を促す環境変数を渡すようにした。
- フォルダパネルのリスト表示で各行の文字が下半分で切れないよう、リスト行の最小高さと行間を明示した。
- 実ブラウザE2Eにリスト表示の行高/文字領域チェックを追加し、再発を検出できるようにした。

検証: 徹底チェック、関連JavaScript構文チェック、`python -m py_compile app/meldex_api_cli_chat.py`、フォルダツリー/フォルダパネル/CLIチャット/ツールバー/フロントエンド回帰unittest、split asset回帰unittest、micro coverage回帰unittest、Chrome実ブラウザE2E `targeted-shell-micro-folder-display-filter`、`git diff --check` PASS。

### fix(standalone): 単独アプリ配布の入口HTML欠落を修正

- ノート/シナリオ/シート/タイマーの単独exe配布で、`*-standalone.html` 本体が `_internal` に同梱されず、起動直後に404白画面になる問題を修正した。
- `build_standalone_apps.py` の配布対象に各アプリの入口HTMLを必ず含め、次回ビルドでも再発しないようにした。
- ノート単独アプリで `gb-editor.part02.js` / `gb-editor.part03.js` を直接読み込んでページエラーが出る問題を修正し、単独ノート内の軽量Markdown変換フォールバックを使う構成に戻した。
- 単独ボードアプリのAI総合監査で、右サイドバー表示時に上端ツールバーが折り返してキャンバスへ食い込む問題を検出し、単独ボード内だけ横スクロールで1行表示を保つようにした。
- 監査中に古い静的テスト前提で落ちていた分割JS/CSS監視を、現行の分割ファイルと複数行CSSへ同期した。
- ノート/シナリオ/シート/タイマーを再ビルドし、ボードを含む5つの単独アプリ配布フォルダで入口HTML存在、HTTP 200、ページエラーなし、初期画面描画を実ブラウザで確認した。

検証: `python -m py_compile` 関連Pythonファイル PASS、単独アプリ/ボード関連JavaScript `node --check` PASS、`python app/build_standalone_apps.py` PASS、5単独アプリ配布フォルダのPlaywright起動監査 PASS、`test_standalone_apps.py` / `test_meldex_board_exe.py` / `test_meldex_comprehensive_ai_audit_matrix.py` / `test_meldex_board_dnd_regressions.py` / `test_meldex_board_file_type_safety.py` / `test_meldex_board_llm_context.py` / `test_meldex_board_free_line_regressions.py` / `test_meldex_chat_and_board_preview_regressions.py` / `test_meldex_sheet_toolbar_display_controls.py` / `test_meldex_operation_audit.py` PASS、`git diff --check` PASS。

### test(standalone): 単独アプリ実装のコミット前監査を反映

- ノート/シナリオ/シート/タイマー単独アプリのHTML参照、共通ローカルファイルAPI、リンク起動、保存形式、配布ビルド対象を徹底チェックした。
- LocalFS経由で `.mel-board` / `.mel-sheet` / `.mel-scenario` / `.mel-timer` をリネームする場合も、対応アプリ種別として扱うように明示判定を補完した。
- 新規単独アプリ関連ファイルを `app/AGENTS.md` の監査バッチ定義へ追加し、次回以降の全ファイル全行チェックから漏れないようにした。
- 総合AI監査のチェックボックス/トグル候補抽出で、ビルド済み配布フォルダではなくソースファイルだけを対象にするよう除外規則を補完した。
- 単独アプリHTMLの参照ファイル存在確認と、LocalFSリネーム種別判定の回帰テストを追加した。
- 単独アプリ個別監査で、タイマー表示中に同時表示されるファイル別タグUIの「タグ名」「追加」が安定IDなしとして検出されたため、タグ入力、追加、タグ検索、タグ解除の各操作部品に重複しない監査用IDを付与した。
- シナリオの高リスクカバレッジ回帰が旧テーマタブ時代の監査IDを参照していたため、現行の書式ポップアップ監査IDへ同期した。

検証: `python -m py_compile` 関連Pythonファイル PASS、単独アプリ/関連JavaScript `node --check` PASS、`pytest -q app/tests/test_standalone_apps.py` PASS、総合AI監査マトリクス回帰 PASS、操作監査回帰 PASS、高リスクカバレッジ回帰 PASS、関連既存回帰 PASS、バージョンメタデータ回帰 PASS、`python app/build_standalone_apps.py` / `python app/build_board_exe.py` PASS、ノート/シナリオ/シート/タイマー個別E2E監査 PASS、`git diff --check` PASS。

## v0.6.043 (2026-05-28)

### feat(standalone): ノート/シナリオ/シート/タイマーの単独exe版を追加

- `MeldexNote.exe` / `MeldexScenario.exe` / `MeldexSheet.exe` / `MeldexTimer.exe` 用のランチャー、共通ローカルファイルAPI、単独画面HTML/CSS/JS、ビルドスクリプトを追加した。
- 各単独アプリは、左サイドバーや作業フォルダ選択の初期画面を出さず、起動直後から編集面とハンバーガーメニューで新規/開く/保存/名前を付けて保存を操作できるようにした。
- シナリオは `.mel-scenario`、シートは `.mel-sheet`、タイマーは `.mel-timer` を単独exe向け主形式として追加し、既存の `.scriptnote.json` / `.smart-db.json` / `.timer.json` も読込互換として扱えるようにした。
- 単独アプリ用のLucideベースexeアイコンとタスクバーアイコンを、Meldexアイコン由来の色だけで生成する `generate_standalone_app_icons.py` を追加した。
- `build_standalone_apps.py` で4アプリをまとめてPyInstallerビルドし、配布フォルダにファイル関連付けスクリプト、README、ライセンス/クレジットを同梱するようにした。
- 単独ボードアプリのリンクカード作成メニューにシナリオ/タイマーを追加し、リンクノート/リンクシート/リンクシナリオ/リンクボード/リンクタイマーを対応する単独exeで開けるようにした。
- Meldex本体側の一覧、検索、リンク、バックリンク、注釈ジャンプ、AI参照、LLMレビュー、グローバルインデックスが `.mel-scenario` / `.mel-timer` を対応アプリのファイルとして扱えるようにした。

検証: `python -m py_compile` 関連Pythonファイル PASS、単独アプリ/ボードリンク関連JavaScript `node --check` PASS、分割JSは親ローダーファイルで構文チェック PASS、`pytest -q app/tests/test_standalone_apps.py` PASS、既存回帰 `test_meldex_phase1_services.py` / `test_meldex_cloud_folder_surfaces.py` / `test_meldex_toolbar_regressions.py` PASS、`python app/build_standalone_apps.py` で4アプリのexeビルド PASS。

### feat(board): ボードファイルの専用拡張子を追加

- 単独ボードアプリの新規保存/新規作成で、ボードファイルを `.mel-board` として扱うようにした。
- 既存の `.board.md` / `.board.json` / `.canvas.json` は読み込み互換として残し、過去のボードをそのまま開けるようにした。
- Windows向けに `.mel-board` を `MeldexBoard.exe` とボードアイコンへ関連付ける `Register-MeldexBoardFileAssociation.ps1` を配布フォルダに同梱するようにした。
- Meldex本体側の一覧、検索、リンク、バックリンク、注釈ジャンプ、AI参照、パス追従が `.mel-board` をボードとして扱えるようにした。
- 将来の単独シートアプリ用に `.mel-sheet` をシート系ファイルとして判定できるようにし、リンク/検索/AI参照の基礎対応を追加した。
- チャットとLLMレビューのファイル種別判定、AI向けボード構造説明も `.mel-board` 優先と現行ボードMarkdown形式に揃えた。
- グローバルインデックスのキャッシュが外部リネーム後に古い表示を返し続けないよう、ルートフォルダの更新時刻を使った軽量な再構築判定を追加した。

検証: `python -m py_compile` 関連Pythonファイル PASS、関連JavaScript `node --check` PASS、ボード/ファイル種別/フォルダ表示/チャット/検索/パス追従/タグ/LLMレビュー関連の回帰テスト144件 PASS、関連付けスクリプト構文確認 PASS、`git diff --check` PASS。`app/MeldexBoard/MeldexBoard.exe` 再ビルド済み。

### docs(standalone): 他単独アプリ計画書を最新ボード方針へ更新

- ノート、シナリオ、シート、タイマーの単独exe化計画書に、最新の単独ボードアプリで確定した通常ファイル保存、読み込み中表示、ファイル関連付け、リンク切れリロケート、埋め込み/リンク方式切替の方針を反映した。
- `.mel-sheet`、`.mel-scenario`、`.mel-timer` などの専用拡張子候補と、`.md` の既定関連付けを奪わないノート方針を整理した。

検証: 計画書の対象表、リンク作成表、共通UX原則、各アプリ計画、テスト観点、受け入れ条件を確認。

### fix(board): 単独ボードアプリの読み込み表示と画像追加方式を改善

- 単独ボードアプリで保存済みボードファイルを開いた直後、カード表示が完了するまで「ボードを読み込んでいます」と表示するようにした。
- 画像ファイルをボードへD&Dするときの追加方式を「ファイルに埋め込む」と「画像ファイルへのリンク」から選べるようにした。
- 単独ボードアプリでは画像の埋め込みを初期値にし、Meldex本体のボードでは従来どおりリンクを初期値にした。
- 画像追加方式を切り替えたとき、現在のボード内にある既存の画像カードも一括で埋め込み/リンクへ変換し、次回保存時に選択中の方式で保存されるようにした。
- 埋め込み画像へ変換しても元の画像ファイルの場所をボードファイル内に保持し、リンク方式へ戻すときに元ファイルが残っていればそのリンクを復元するようにした。元ファイルが見つからない場合は埋め込みデータから画像ファイルを復旧してリンク方式へ変換する。
- 画像リンク切れ時は空白ではなく「画像ファイルが見つかりません」と表示し、画像カードの表示ボタンまたは右クリックメニューから画像ファイルを再指定できるようにした。
- 単独ボードアプリでは画像ファイルの再指定にOSのファイル選択ダイアログを使い、基準フォルダ外の画像を選んだ場合も埋め込みまたはボード側へのコピーで復旧できるようにした。
- `.mel-board` のWindows関連付けスクリプトが、ファイルアイコンと起動コマンドをユーザー領域の関連付けへ確実に登録し、必要に応じてExplorer再起動まで実行できるようにした。

検証: 関連JavaScript構文チェック PASS、画像追加方式/リンク復元/リンク切れリロケート/画像元パス保存の回帰テストを含む `test_meldex_board_exe.py` / `test_meldex_board_dnd_regressions.py` / `test_meldex_split_assets.py` PASS、関連Python構文チェック PASS、関連付けスクリプト構文確認 PASS、`git diff --check` PASS。`app/MeldexBoard/MeldexBoard.exe` 再ビルド済み。

### fix(board): 単独ボードアプリの右クリックメニューを補完

- 単独ボードアプリの右クリックメニューで、リンクを開く/コピー、コメント追加/一覧、テーマ選択、プロンプトを使う項目が無反応にならないようにした。
- 右クリックメニューのカード、画像カード、リンクカード、複数選択、ライン、空白、ツールバー表示切替を実ブラウザで確認した。
- 表示バージョンを `0.6.043` に更新した。

検証: 単独ボードアプリの右クリックメニュー44ケースPlaywright実ブラウザ検証 PASS、上端/下端ツールバー実右クリック検証 PASS、`test_meldex_board_exe.py` PASS、`test_meldex_version_metadata.py` PASS。

## v0.6.042 (2026-05-28)

### test(board): 単独ボードアプリの注釈関連を徹底チェック

- 単独ボードアプリの注釈ツールについて、ツールバー表示、矩形注釈、付箋、表示切替、全削除、保存ファイル生成、再読込後の復元まで実ブラウザで確認した。
- Meldex本体側の注釈微細E2Eで、ボード注釈、付箋ドラッグ、注釈パネル、現在の表示範囲の注釈表示が崩れていないことを確認した。
- 表示バージョンを `0.6.042` に更新した。

検証: 注釈関連の操作チェーン徹底チェック、`run_frontend_e2e_annotation_micro.ps1 -Browser chrome -StopOnFailure` PASS、単独ボードアプリのPlaywright実ブラウザ検証 PASS。

## v0.6.041 (2026-05-28)

### feat(board): 単独ボードアプリで注釈ツールを利用可能にした

- 単独ボードアプリに注釈フローティングボタンと注釈ツールバーを追加し、ペン、マーカー、投げ縄塗り、矩形塗り、消しゴム、付箋、表示切替、全削除を使えるようにした。
- 単独ボードアプリのボードキャンバスに、本体と同じ注釈描画ブリッジを初期化するようにした。
- exe/native API とブラウザfallback APIの両方で、注釈の作成、取得、更新、復元、削除に対応した。注釈データは作業フォルダ内の `_meldex/board-annotations.json` に保存する。
- 未保存の新規ボードでは注釈が保存先を持てないため、注釈ツールを開こうとした時点で保存または既存ボードを開くよう案内するようにした。
- 表示バージョンを `0.6.041` に更新した。

検証: 関連操作チェーンの徹底チェック、`python -m py_compile app/MeldexBoard.py`、`node --check app/board-standalone-app.js`、`node --check app/board-standalone-fs.js`、`node --check app/board-standalone-stubs.js`、`test_meldex_board_exe.py` PASS。Playwright実ブラウザで注釈ツールバー開閉、矩形注釈の描画、API保存、再読込後の再表示を確認済み。

## v0.6.040 (2026-05-28)

### fix(board): ボードexe版の単独作業フローを整理

- ボードexe版の右サイドバーにビューワーとオプションをまとめ、サイドバー幅とビューワー高さをドラッグで調整できるようにした。
- 上端/下端ツールバーを右クリックメニューから表示/非表示できるようにし、各表示状態を保持するようにした。
- 新規ボード起動時の初期カードを廃止し、まっさらなボードから開始できるようにした。
- エクスプローラーから複数画像をボードへD&Dしたとき、画像カードをグリッド配置で追加し、追加後にまとめて選択できるようにした。
- 画像カードの常時枠線を外しつつ、選択ハイライトは残るように修正した。
- 単独ボードアプリでも共通ショートカットを読み込み、`Ctrl+S` 保存、`Ctrl+N` 新規ボード、`Ctrl+O` / `Ctrl+P` ボードを開く、`Ctrl+Z` / `Ctrl+Y` / `Ctrl+Shift+Z` のUndo/Redoを使えるようにした。
- 未保存ボードの保存時に作業フォルダ選択ではなくファイル保存ダイアログを出し、`テストボード` のような入力を `テストボード.board.md` として保存できるようにした。
- ボードDOMのレイアウト前に全体表示を実行したとき、キャンバスサイズ0の計算で10%表示に張り付く回帰を防いだ。
- シナリオエディタの折り返し再配置で、分割後スコープ外になった関数参照による `ReferenceError` を防いだ。
- 表示バージョンを `0.6.040` に更新した。

検証: 関連操作チェーンの徹底チェック、`python -m py_compile app/MeldexBoard.py`、`node --check app/board-standalone-app.js`、`node --check app/board-standalone-fs.js`、`node --check app/board-standalone-stubs.js`、`gb-shortcuts` 結合構文チェック、`test_meldex_board_exe.py`、`test_meldex_board_dnd_regressions.py`、`git diff --check` PASS。Playwrightでソース版/配布フォルダ版の保存、新規/開くショートカット、Undo/Redo、複数画像D&D、保存ダイアログ経路を確認済み。`app/MeldexBoard/MeldexBoard.exe` 再ビルド済み。

## v0.6.039 (2026-05-28)

### fix(board): ボードexe版の画面構成と初期カードサイズを調整

- ボードexe版のアプリ内上部バーと左サイドバーを削除し、ボード編集画面をツールバー、キャンバス、オプションパネルの構成に整理した。
- 新規ボード、保存、ボードを開く、保存先変更、一覧更新、オプション開閉をハンバーガーメニューから実行できるようにした。
- オプションパネルをハンバーガーメニューとツールバーのオプションボタンから開閉できるようにし、開閉状態を保持するようにした。
- 起動直後の「親カード」を通常のカード生成経路で作成し、固定の大きなサイズと150%拡大表示ではなく、標準サイズを100%表示で中央に置くようにした。
- 表示バージョンを `0.6.039` に更新した。

検証: ボードexe版の画面構成/メニュー/保存/オプション開閉/初期カードサイズのPlaywright実操作チェック、`node --check app/board-standalone-app.js`、`node --check app/gb-board-ui.js`、`python -m py_compile app/MeldexBoard.py app/build_board_exe.py`、`test_meldex_board_exe.py`、`test_meldex_split_assets.py`、`test_meldex_version_metadata.py` PASS。`app/MeldexBoard/MeldexBoard.exe` 再ビルドと同梱HTMLの実操作確認済み。

## v0.6.038 (2026-05-28)

### fix(board): ボードexe版の初期操作とオプションパネルを実用化

- ボードexe版を起動した直後からツールバーと「親カード」1枚が表示され、保存先未選択でも作業を始められるようにした。
- 右側にオプションパネルを常設し、カード、ライン、テーマ、カードスタイル、ラインスタイル、階層別スタイルの各タブを本体ボードと同じ詳細パネルで操作できるようにした。
- 単独HTMLに書式UIモジュールを読み込み、カード/ライン/階層別スタイルの編集欄が「書式UI未ロード」になって使えない不具合を修正した。
- オプションパネルでカード幅・文字サイズ・太字などを変更したとき、階層別スタイルや構造変更でユーザー指定が戻らないようにし、保存/再読込後も指定を復元するようにした。
- pywebview が使えない環境では通常ブラウザのタブではなく、Edge/Chrome のアプリ風ウィンドウで開くフォールバックに変更した。
- 表示バージョンを `0.6.038` に更新した。

検証: オプションパネルの実操作徹底チェック（Playwright: 起動直後、カード編集、個別スタイル編集、全スタイルタブ切替、ライン編集、新規ボード保存、保存ファイル内容、再読込後の幅復元）PASS。

## v0.6.037 (2026-05-27)

### feat(board): Meldex本体互換のボードexe版を追加

- Meldex本体のボード編集資産（`gb-canvas-*` / `gb-board-*` / `gb-tool-canvas.js`）を使う薄いexeラッパー `MeldexBoard.py` と、PyInstaller用ビルドスクリプト `build_board_exe.py` を追加した。
- `MeldexBoard.exe <ボードファイルパス>` またはフォルダ選択で起動できるようにし、ボードファイル形式・保存内容はMeldex本体と同じまま維持した。
- exe版向けに localhost の最小APIを追加し、ボード一覧、ファイル読込/保存、新規ボード作成、画像アップロード、画像表示、リンク種別判定、ボード一括取込系の空応答を本体ボードの呼び出しと整合させた。
- 徹底チェックで、ボードファイル指定なし起動時に作業フォルダ未選択でも復元済み扱いになる経路と、ブラウザ互換側の新規ボード作成/保存スキップ不足を修正した。
- 表示バージョンを `0.6.037` に更新した。

検証: exe版ボード操作チェーンの徹底チェック、`python -m py_compile app/MeldexBoard.py app/build_board_exe.py`、`node --check app/board-standalone-fs.js`、`node --check app/board-standalone-app.js`、`test_meldex_board_exe.py`、`test_meldex_split_assets.py`、`test_meldex_version_metadata.py`、`python app/build_board_exe.py` PASS。`app/MeldexBoard/MeldexBoard.exe` 生成確認済み。

## v0.6.036 (2026-05-27)

### fix(chat/note): CLIチャット進行表示と入力表示の回帰を修正

- CLIチャットの応答待機中に、経過時間・最終出力からの時間・CLI出力/進行ログ受信中の状態を表示し、長時間待機でも停止したように見えないようにした。
- CLIチャット実行中に他パネル操作などでチャットが再描画されても、直前のユーザー発言、受信済み応答、待機/進行表示を復元するようにした。
- チャットとチームチャットの長い発言・コードブロック・CLI進行表示がパネル幅内で折り返されるようにした。
- ノートツールバーのファイル名欄など、ツールバー内のインライン入力が mousedown 抑止で編集できなくなる回帰を修正した。
- 表示バージョンを `0.6.036` に更新した。

検証: 関連操作チェーンの徹底チェック、`node --check app/gb-right-panel-chat-cli.js`、`python -m py_compile app/meldex_api_cli_chat.py`、CLIチャット/チャット表示/ノートツールバー/微細挙動/分割アセット/バージョンメタデータの unittest、ノート micro E2E、シェル targeted E2E PASS。

## v0.6.035 (2026-05-27)

### fix(misc): 全体チェック修正の徹底チェックで見つけた回帰・残課題を解消

v0.6.032 〜 v0.6.034 の修正を、起こり得るユーザー操作で関数チェーンの最深部まで追跡し、見落としを 3 件解消した。

- `/api/annotations` の `limit=0` を「全件取得」として復帰。中重要度修正で `limit <= 0` を 200 件にフォールバックしていたが、右パネル注釈一覧 (`_RP_ANNOTATION_LIST_QUERY = 'limit=0'`) が全件取得を期待しているため、200 件で切り捨てる回帰になっていた。`limit=0` は LIMIT 句を付けず、`limit < 0` のときのみ 200 件にフォールバックする実装に修正。
- `_syncDetailForActivePane()` の file-info フォールバックを冪等化。`_clearDetailPaneShellAfterSave` が pending Promise を返したケースで、save 成功時に afterClear と私のフォールバックの両方が走り、`/file-meta` を 2 回 fetch する二重描画になっていた。150ms 遅延 + `[data-global-tags-target-path]` 存在チェックで二重 fetch を回避するよう修正。
- `create_calendar_db_event` の予約済み空ファイルクリーンアップ。`os.open(O_EXCL)` で予約した後に `save_db_auto_version` / `yaml.dump` / `write_text` のいずれかが例外で死ぬと、空ファイルが残ってフォルダ内に「壊れたイベント」として残ってしまうケースを、書き込み完了までを try/except で囲んで失敗時にクリーンアップするよう修正。
- 表示バージョンを `0.6.035` に更新した。

検証: 既存 32 件＋新規 1 件（`test_create_cleans_up_empty_reservation_when_write_fails`）＋ 既存タググループ／詳細パネル系の追加 29 件＝計 62 件 PASS。Python / JS 構文チェック、リリース版コピー (`app/Meldex/`, `app/MeldexCloud/`) 同期済み。

## v0.6.034 (2026-05-27)

### fix(misc): 全体チェック報告書の低重要度バグ 5 件を解消

`app/docs/full-check-2026-05-27.md` の低重要度ブロック 5 件を修正した。

- ノート自動保存中に `#note-toc` が DOM から外れていたとき、`getElementById(...).style` を直接読み出して TypeError になり、自動保存が止まる可能性があった箇所を、存在チェック付きの参照に変更。
- ボード注釈ノートの自動保存 `setInterval(3000ms)` が、`body` 要素の再生成や DOM 切り離し後も孤児として走り続ける問題を、`body.isConnected` が false になった時点で `clearInterval` する自己クリーンアップに変更。
- フォルダツリーの編集ロック切替（`toggleItemLock`）で `window.prompt` で「編集ロックの理由」を聞いていた経路を撤去（ダイアログ禁止原則違反）。ロック取得時は空 reason で API を呼び、必要なら別途記録する運用とした。
- 右パネル resize 開始時に `document.getElementById('main-area').offsetWidth` を直接読んでいた箇所を、要素不在でも `window.innerWidth` にフォールバックするようにガード。
- ボードショートカット `board.cut` で `bd.connections` が未定義のときに「ラインは切り取りできません」と誤警告を出していた箇所を、`selectedConnIds` が空または `bd.connections` 非配列なら検証をスキップする実装に変更。
- 表示バージョンを `0.6.034` に更新した。

検証: 新規 unittest `test_meldex_full_check_low_2026_05_27` 5 件、高 10 件＋中 15 件と合わせて 30 件 PASS。Python / JS 構文チェック、リリース版コピー (`app/Meldex/`, `app/MeldexCloud/`) 同期済み。

## v0.6.033 (2026-05-27)

### fix(misc): 全体チェック報告書の中重要度バグ 12 件を解消

`app/docs/full-check-2026-05-27.md` の中重要度ブロック 12 件をまとめて修正した。

- シートのタイトル領域ダブルクリックリスナーが、シート切り替え時に初回 ctx をクロージャに永続保持していた問題を修正（毎回 `removeEventListener` してから貼り直す）。
- 詳細パネルのタブ互換性マップに `scriptnote` 系（`sn2-main` 等）を追加。シナリオを開いたまま作業パネルを切り替えても、選択していたシナリオ個別タブが保持されるようにした。`publish` 互換タイプにも `scriptnote` を追加。
- `media/html/csv` を開く経路で、未保存内容の保存が拒否されたときに file-info が描画されなかった経路にフォールバックを追加。
- カレンダー週ビューのドラッグ作成で `new Date('YYYY-MM-DDTHH:00')` という秒なし ISO 文字列を Safari/Firefox に渡すと Invalid Date になっていた箇所を `:00:00` まで含めて修正。
- `/api/auth/me` で、認証ユーザーが構成された環境で無効/失効トークンを渡したとき、誤った viewer ロールを返していたのを 401 を返すように修正。
- `/api/annotations` の `limit` パラメータに範囲検証を追加（負値・巨大値はフォールバック）。
- `/api/cal/time` の `body.get("timestamp", default)` が空文字で default にフォールバックしない問題を修正。
- 付箋注釈の描画で `data.x` / `data.y` が `undefined` のとき `NaNpx` が style にセットされていた箇所を `Number.isFinite` チェックで 0 にフォールバック。
- ヒストリーの `_flushPendingEditorVersionTarget` で `flushPendingEditorAutosave()` が非配列を返すと rejection を検知できていなかった箇所を、配列正規化して検知できるように修正。`historyPush` のスコープ追加時にグローバル redo を破棄するよう変更。
- フォルダツリーの遅延ロード経路で `pivotData.entities` が undefined のとき TypeError、`dataset.loaded` が `'true'` にならず再フェッチ無限ループになる経路を、`Object.keys(pivotData?.entities || {})` と成功時の `dataset.loaded = 'true'` 設定で解消。
- 作業パネルのファイル作成タブで、`_PANEL_FILE_CREATE_TYPES` 非該当タイプを渡された場合に API が認識しない type で 200 空 node が返るリスクがあったのを、明示的にホワイトリストで弾くように変更。
- チャット保存の `file_path.write_text()` を一時ファイル経由のアトミック書き込みに変更。`/api/chat/list` の `ctx.get_db()` を `try/finally` で必ず close するように修正。
- 表示バージョンを `0.6.033` に更新した。

検証: 新規 unittest `test_meldex_full_check_mid_2026_05_27` 15 件、高重要度ユニット 10 件と既存タググループ／フォルダビュー／詳細パネル系 36 件の計 61 件 PASS。Python / JS 構文チェック、リリース版コピー (`app/Meldex/`, `app/MeldexCloud/`) 同期済み。

## v0.6.032 (2026-05-27)

### fix(security/calendar/auth): 高重要度の繰り越し既知バグを修正

`app/docs/full-check-2026-05-27.md` の「次回以降のリリースに繰り越す既知問題」のうち、高重要度に分類した 5 件を解消した。

- カレンダーイベント API の認可漏れを修正。`PUT /api/cal/events/{eid}` と `DELETE /api/cal/events/{eid}` で、認証ユーザー（または管理者）以外がイベントを更新・削除できないようにした。`POST /api/cal/events` も、ユーザーが構成されている環境では認証必須にした。
- カレンダー DB イベント API（`/api/calendar-db/events`）の TOCTOU を修正。新規作成は `os.open(O_CREAT|O_EXCL)` でファイル名をアトミックに予約するようにし、リネームを伴う更新は「新ファイル名予約→新パスへ直接書き込み→旧ファイル削除」の順に変更した。これで並列リクエストでファイルが消失したり、リネーム失敗時に旧名ファイルへ新内容が残ったりすることがなくなる。
- `/api/auth/login` の `for user in users:` ループと `ctx.save_users()` を `with users_lock_context():` の中に移動した。これまで初期ユーザー作成時のみロック内で処理し、それ以外のログイン経路では二重 `load_users()` した古いコピーをロック外で更新していたため、並列ログインでロスト更新の余地があった。
- 右パネルの `switchRightTab('chat')` で、`gb-right-panel-chat.js` が未ロード（または読み込み失敗）の場合に `_chatState` や `updateChatModels` を直接参照して `ReferenceError` になっていた箇所を、`typeof` ガードで安全に離脱するようにした。`chatAddSystem` も同様にガード越し呼び出しに統一した。
- グローバルタググループの正規化処理で、A → B → C → D → B のような「外部から流入する多階層循環」を検出した際に、循環の外側にいる起点グループ（A）の `parent_id` まで切ってしまっていた誤動作を修正した。実際に循環している cursor 側の `parent_id` だけを null にする実装に変更し、起点側の正しい関連は維持される。
- 表示バージョンを `0.6.032` に更新した。

検証: 新規 unittest `test_meldex_full_check_high_2026_05_27` 10 件、既存のカレンダー / タググループ / 認証系 33 件の計 43 件 PASS。Python / JS 構文チェック、リリース版コピー (`app/Meldex/`, `app/MeldexCloud/`) 同期済み。

## v0.6.031 (2026-05-27)

### fix(tag): タグ管理タブの徹底チェック対応

- v0.6.030 で追加したタグ管理タブを実機目視と全体監査（6 バッチ並列）で検証し、見つかった高重要度バグを修正した。
- グループ/タグの「⋯」メニューを開いたあと、メニュー内をクリックしても次回外側クリックでメニューが閉じなくなる問題を修正（`{ once: true }` が一度消費されて以後反応しなくなる挙動を、別メニュー時には古いハンドラを掃除する `bindMenuOutsideClick` に置き換え）。
- グループ名・色・タグ名を変更したときにツリー全体に「読み込み中…」が一瞬出てちらつく問題を修正（`.then(refresh)` で API レスポンスが showLoading として渡っていたため毎回 loading 表示が出ていたのを、明示的に `() => refresh(false)` に変更）。
- サブグループの階層インデントが常に同じ位置に揃ってしまい、ネスト 3 階層以上で兄弟と孫が見分けられない問題を修正（再帰呼び出しで `depth + 1` を渡す）。
- タグ管理タブを初めて開いた瞬間に「タグがありません。」のメッセージが一瞬表示されてからデータに切り替わるちらつきを抑制（初回は loading 表示を出してフェッチ完了後に一度だけ描画）。
- タグ・グループの `name` が欠落しているデータが返ってきた場合に並び替えで TypeError になっていた箇所を、空文字フォールバックで防御。
- オプションパネル内タブの並べ替え（D&D）のデフォルト順に「タグ管理」を追加した。これでブラウザに保存された旧並び順を初期化したときも、想定位置に出るようになる。
- エクスプローラーから別アプリ（ノート・シート等）に切り替えたあとに残骸としてタグ管理タブが表示されたままになる可能性があった箇所を、stale 判定で folder 以外なら必ず再同期する形に修正した。
- 全体チェック報告書: `app/docs/full-check-2026-05-27.md`
- 表示バージョンを `0.6.031` に更新した。

検証: 既存ユニットテスト 33 件 (`test_meldex_tag_groups` 11 件、`test_meldex_eagle_pureref_global_tags` 11 件、`test_meldex_batch85_detail_panel_regressions` 12 件、`test_meldex_batch103_folder_viewer_regressions` 5 件、`test_meldex_version_metadata` 2 件) PASS、Python / JS 構文チェック、リリース版コピー (Meldex/, MeldexCloud/) 同期。
タグ管理機能と無関係に並走監査で見つかった既存の高重要度バグ（カレンダーイベント認可漏れ、認証 lock 範囲、カレンダー DB の TOCTOU 等）は本コミットでは扱わず、上記報告書に重要度順で記録した。

## v0.6.030 (2026-05-27)

### feat(tag): タグ管理タブを追加（Eagle 風 Phase 1 MVP）

- エクスプローラーでフォルダの中身を一覧している状態でオプションパネルを開くと、新しく「タグ管理」タブが表示されるようにした。フォルダツリー上部にあった旧「タグ管理」アイコンとパネルは廃止し、機能はオプションパネル側に集約した。
- タググループの作成・名前変更・色設定・折りたたみ・並べ替え（階層）・削除に対応した。グループは入れ子可能で、削除時は中のタグが「未分類」に戻り、サブグループは祖父母グループに昇格する。
- タグの実効色は所属グループの色を継承する仕様にした（タグ個別の色フィールドは将来用に温存）。
- タグ自体の追加・名前変更・グループ移動・削除・「このタグの項目を検索」もタブ内に集約。検索結果は同タブ内に表示する。
- タグデータの保存形式を v1 → v2 に拡張した（`tags[].group_id` / `tags[].sort_index` / `groups[]` を追加）。既存の v1 形式は読み込み時に自動アップグレードされ、初回書き換え前に `.meldex/global-tags.v1.bak.json` へバックアップが作られる。
- API として `/api/global-tag-groups`（GET / POST / PATCH / DELETE）を新設。`/api/global-tags` POST と PATCH は `group_id` を受け付けるよう拡張した。
- 表示バージョンを `0.6.030` に更新した。
- 設計の詳細は `app/docs/tag-management-panel-plan-2026-05-27.md` を参照。

検証: 新規 unittest `test_meldex_tag_groups` 11 件（v1→v2 マイグレーション、グループ CRUD、循環防止、削除時の昇格、API ラウンドトリップ）、既存タグ系 11 件、詳細パネル系 12 件、フォルダビュー系 20 件、バージョンメタデータ 2 件（計 54 件）、Python 構文チェック、JS 構文チェック。

## v0.6.029 (2026-05-27)

### fix(folder): フォルダビューでファイル選択時のオプションパネル二重描画フラッシュを修正

- フォルダビューでファイル（画像など）を選択した瞬間、オプションパネルに簡易版のファイル情報（タイトル/タイプ/パス/サイズ/更新日時/解像度/アクションボタン/タグ/所属フォルダ）が同期描画され、その直後にビューワー iframe のロード完了メッセージ経由で本来のエディタタブ構造（種類/フォルダ/パス/作成日時/更新日時/サイズ）に差し替わる二重描画フラッシュを修正した。
- 原因は `showFolderPreview()` が `#rp-detail` に直接独自の簡易レイアウトを書き込んでいたところに、ビューワー側からの `_showFileInfoInDetailPanel()` がエディタタブ構造で上書きしていたため。
- ファイル選択時は `showFolderPreview()` から直接 `_showFileInfoInDetailPanel(item.path)` を呼ぶ統一導線に変更した。ビューワー側のメッセージ経由でも同じ関数が呼ばれるが、結果は同じエディタタブ構造になるため、もう中間段階のチラつきは発生しない。
- フォルダ選択時はビューワー iframe を経由しないので、従来の簡易表示を維持する。
- 表示バージョンを `0.6.029` に更新した。

検証: フォルダビュー回帰unittest 26 件 (`test_meldex_batch103_folder_viewer_regressions`, `test_meldex_folder_tree_chrome_regressions`, `test_meldex_batch85_detail_panel_regressions`)、関連ファイル (`app/Meldex/`, `app/MeldexCloud/`) の同期確認。

## v0.6.028 (2026-05-27)

### fix(smart-sheet): ダッシュボードウィジェット／ボード一括取込でも対象フォルダが効くように

- 徹底チェック中に、`/api/smart-db` を呼び出している経路のうち、ダッシュボードウィジェット (`gb-db-smart-dashboard-view.js`) とボードへの一括取込 (`gb-board-bulk-import.js`) が `sources` パラメータを渡しておらず、ダッシュボード上では従来通り全 vault を走査していたことが分かったので合わせて修正した。
- これにより、スマートシートを参照するダッシュボードウィジェットや、ボードへスマートシート結果を一括取り込みする経路でも、定義側の対象フォルダ設定が一貫して反映されるようになった。
- 表示バージョンを `0.6.028` に更新した。

検証: スマートシート関連回帰unittest 17 件、Node 構文チェック（dashboard / bulk-import）、Python 構文チェック。

## v0.6.027 (2026-05-27)

### fix(smart-sheet): 対象フォルダ指定時のタイムアウトを修正

- スマートシートで対象フォルダを指定しても、サーバ側が一旦すべての可視ソースフォルダを再帰走査してから sources で絞り込んでいたため、Dropbox 等の大規模 vault では走査が数十秒〜タイムアウトに達して「0 件のまま読み込み中表示が消えない」状態になっていた問題を修正した。
- 対象フォルダ指定時は最初から対象フォルダの中だけを走査するように `/api/smart-db` の処理順を変更した。これで対象フォルダが小さければ走査時間は数秒以下になる。
- 対象フォルダ未設定時は従来どおり全可視ソースフォルダを走査する（既定挙動は維持）。
- 表示バージョンを `0.6.027` に更新した。

検証: スマートシート関連回帰unittest 17 件、Python 構文チェック、バージョンメタデータ回帰unittest。

## v0.6.026 (2026-05-27)

### fix(smart-sheet): 通常シート用ツールバーがスマートシートでも表示されていた問題

- スマートシート表示時に、画面上部のアプリツールバー（通常シート用のフィルタ・並べ替え・公開・検索・オプション等）が、スマートシート独自ツールバー（メニュー・表/ダッシュボード・再読み込み・フィルタ設定）とは別に二重で表示されていた問題を修正した。
- スマートシートは `smart-db-view` 内部に必要な操作を備えた専用ツールバーを既に持っているため、通常シート用の `#app-toolbar` (`#tb-db`) は表示しないように `_updateToolbars` を修正した。
- 表示バージョンを `0.6.026` に更新した。

検証: スマートシート関連回帰unittest 16 件、Node 構文チェック（pane-bridge 全結合）、バージョンメタデータ回帰unittest。

## v0.6.025 (2026-05-27)

### fix(smart-sheet): 対象フォルダ機能まわりの徹底チェック

- フォルダピッカーが返すパスが「絶対パス」「ソースフォルダ相対パス」「vault 相対パス」のいずれであっても、保存値が常に「ソースフォルダ名/サブパス」形式になるように組み立てロジックを統一した。これまでは vault 配下相対が返るケースで二重プレフィックスが起きる懸念があった。
- スマートシート定義の `sources` が指定されているのに全てのパスが解決失敗した場合、意図せず全件走査にフォールバックしていた挙動を修正した。今後は対象 0 件として明確に空結果を返す。
- 対象フォルダ未設定のままだが、後方互換の個別シート指定（`kind: "sheet"`）が残っているケースで、ユーザーに「見えない絞り込みが効いている」ことが伝わるよう注釈文を改善した。
- `selectSmartDb` と `openPage` の読み込み中表示についても、`selectDatabase`/`openFolder` と同じく try ブロック内で開始するように直してリーク経路を塞いだ。
- 表示バージョンを `0.6.025` に更新した。

検証: スマートシート関連回帰unittest 15 件（ピッカー / Content-Type / 振り分け / 既存ナビゲーション / バージョン / 厳密フォールバック / 読み込みリーク防御 など）、Node 構文チェック、Python 構文チェック。

## v0.6.024 (2026-05-27)

### feat(smart-sheet): 対象フォルダの設定機能を追加

- スマートシートで集計対象とするフォルダを設定できるようにした。設定したフォルダはサブフォルダ配下のシートも自動的に含む。
- 対象フォルダ未設定時の既定は「そのスマートシートファイルが置かれているフォルダ＋サブフォルダ」になる。Meldex 全体を走査する従来挙動は意図せず重くなる原因だったため、ファイル単位で自然な範囲に絞られる。
- フィルタ設定ダイアログ（スマートシート用）に「対象フォルダ」セクションを追加した。「+ フォルダを追加」ボタンからフォルダツリーをたどって選択できる。
- 既存のスマートシート定義に `kind: "sheet"` の個別シート指定がある場合は、UI からは編集不可だが保存時にそのまま保持する（後方互換）。
- 表示バージョンを `0.6.024` に更新した。

検証: スマートシート関連回帰unittest、Node 構文チェック、Python 構文チェック（database API）、バージョンメタデータ回帰unittest。

## v0.6.023 (2026-05-27)

### fix(smart-sheet): スマートシートのフィルタ条件保存とローディング表示の修正

- スマートシート表示中にアプリツールバーの「フィルタ」ボタンや関連ショートカットを押したとき、通常シート用の統合フィルタダイアログが開いてしまい、条件を入れて「適用」を押しても保存先が無く再度開くと条件が消えてしまう問題を修正した。スマートシート表示中はスマートシート専用のフィルタ設定ダイアログへ自動的に振り分けるようにした。
- ファイルベースのスマートシート（`.smart-db.json`）の定義保存リクエストに `Content-Type: application/json` ヘッダが付いていなかったため、サーバ側で 422 エラーが返ってフィルタ条件・並べ替え・名前変更などのファイル保存が永続化されない問題を修正した。
- シート・フォルダの読み込み中表示が、内部処理の途中で例外が発生したときに消えずに残ってしまう可能性があった問題を修正した。読み込み中表示を出すタイミングを try ブロックの内側に移し、出した時だけ確実に消えるようにした。
- 表示バージョンを `0.6.023` に更新した。

検証: スマートシート関連回帰unittest（フィルタダイアログ振り分け／ファイル保存の Content-Type／既存のスマートシート出典操作）、Node 構文チェック、差分空白チェック。

## v0.6.022 (2026-05-26)

### fix(folder-tree/sheet/note): フォルダツリー更新・スマートシート・テキスト選択メニューの改善

- フォルダツリーの更新ボタンを押したとき、デスクトップ起動時の自動展開制限が掛かったままで直前の展開状態が全て失われていた問題を修正した。更新ボタン経由のときだけ展開制限を一時的に解除し、展開キュー完了後に元に戻すようにした。
- スマートシートを開いた直後に通常シートやフォルダを開いたとき、スマートシート用の表示が画面に残って通常シート本体が見えなくなる問題を修正した。シート読み込みの入口でスマートシート表示を明示的に隠すようにした。
- ファイル数の多いソースフォルダ（Dropboxなど）で「全件インデックス」型スマートシートを開いたとき、staleness 検査の全ファイル走査が60秒以上ブロックして読み込み中表示のまま固まる問題を修正した。キャッシュがあれば staleness 検査を行わずに即時返却するようにし、明示的な更新はスマートシートの再読込ボタンで行うようにした（Meldex内のファイル操作では従来通り自動的にキャッシュが破棄される）。
- ノート・自由記述・詳細パネル編集での右クリックメニューに「Googleで検索」項目を追加した。テキスト選択中のみ有効で、選択文字列を新しいタブで Google 検索する。
- ファイル ID からのパス解決ヘルパーがまだ読み込まれていない起動初期にお気に入り描画から作品フォルダ取得が呼ばれ、ReferenceError でスクリプト評価が中断し以後の機能が連鎖的に動かなくなる問題を修正した。作品フォルダ取得関数に未定義時のフォールバックガードを追加した。
- 表示バージョンを `0.6.022` に更新した。

検証: フォルダツリー回帰unittest、フォルダツリー/Chromeヘッドレス回帰unittest、フロントエンド回帰unittest、スマートシート→通常シート遷移E2E（Chrome実ブラウザ・スクリーンショットAI目視）、スマートシート単体機能 E2E 6 ケース、Python 構文チェック、バージョンメタデータ回帰unittest、差分空白チェック。

## v0.6.021 (2026-05-26)

### fix(mcp): Meldex MCP stdio接続をNDJSON仕様へ修正

- `meldex-local` MCPサーバーの応答をContent-Lengthヘッダ付き形式から改行区切りJSONへ変更し、Claude Code / Codex CLIがinitialize応答をパースできず接続中のまま固まる問題を修正した。
- 受信側は従来どおりContent-Length形式と改行区切りJSONの両方を受理し、既存クライアントからの入力互換性を維持した。
- MCPサーバーの単体出力とスタンドアロンinitialize疎通の回帰テストを追加した。
- 表示バージョンを `0.6.021` に更新した。

検証: MCPセキュリティ/stdio回帰unittest、MCPサーバースタンドアロンinitialize疎通、バージョンメタデータ回帰unittest、Python構文チェック、差分空白チェック。

## v0.6.020 (2026-05-25)

### fix(scriptnote): 折り返し表示の段詰めを実表示の余白込みで再計算

- シナリオエディタの折り返し表示で、区切り行の表示余白を含めた外寸で段分割を再計算するようにし、横書きの下端や縦書きの左端に余白が残りにくいようにした。
- 折り返し段の先頭が区切り行になった場合、段内ヘッダーの直後に不要な区切り余白が入らないようにした。
- 表示バージョンを `0.6.020` に更新した。

検証: シナリオエディタ折り返し回帰unittest、横書き/縦書き折り返しChrome実ブラウザE2E、バージョンメタデータ回帰unittest、差分空白チェック。

## v0.6.019 (2026-05-24)

### feat(tags/viewer/folder): 一般タグ付け検索と起動時表示を改善

- ファイル・フォルダの詳細表示とフォルダパネルから一般的なタグを付与・解除できるようにし、既存タグ候補を入力欄から再利用できるようにした。
- タグ管理パネルと `#タグ名` / `tag:タグ名` 検索から、タグ付き項目を一覧してフォルダ・画像・PDFなどを開けるようにした。
- 起動直後にお気に入り・最近使った項目・ホームフォルダを再描画し、保存済みお気に入りが空に見えたりホームフォルダが一時的に読み込みエラーになったりしにくいようにした。
- ビューワーで大量のファイル一覧を読み込む間は中央にも読み込み中表示を出し、上下キーでフォルダ移動した後も勝手に再生しない停止状態を初期値にした。
- 表示バージョンを `0.6.019` に更新した。

検証: タグAPI/フロント回帰unittest、検索API回帰unittest、フォルダツリー/ホームフォルダ回帰unittest、ビューワー回帰unittest、split asset整合性unittest、FTS検索回帰unittest、JavaScript/Python構文チェック、タグUIのChromeヘッドレススクリーンショットAI目視、全スクリプト読み込み監査、差分空白チェック、バージョンメタデータ回帰unittest。

## v0.6.018 (2026-05-24)

### fix(viewer/folder): ビューワー起動と大量フォルダ表示を高速化

- ビューワーで単一画像を開くとき、フォルダ内の全画像・全メタ情報の読み込みを待たず、選択中の画像を先に表示するようにした。
- フォルダパネルで大量の項目を表示する際、全項目を一括でDOM追加せず、分割描画してUI操作が詰まりにくいようにした。
- フォルダツリーで大量の子項目を展開する際、子ノードを分割追加し、展開中の重複読み込みを防ぐようにした。
- フォルダビュー分割JSの行数を1000行以内に戻し、既存の分割構成の制約に収めた。
- 表示バージョンを `0.6.018` に更新した。

検証: フォルダツリー/フォルダパネル回帰unittest、フロントエンド構文回帰unittest、split asset整合性unittest、ビューワー起動高速化回帰unittest、画像ビューワー回帰unittest、バージョンメタデータ回帰unittest、差分空白チェック。

## v0.6.017 (2026-05-23)

### fix(folder): フォルダツリーと表示中項目の連動を安定化

- フォルダパネルやビューワーで表示中のフォルダ・ファイルが変わった時、フォルダツリー側でも該当項目をハイライトし、必要な親フォルダを展開して追従するようにした。
- ビューワーの前後フォルダ移動を、同階層だけでなくフォルダツリー順で画像・PDFを含む次の表示可能フォルダへ移動できるようにした。
- フォルダツリー上の上下左右キー操作で、上下は選択項目を作業パネルに表示し、右はフォルダ展開、左はフォルダ折りたたみを行うようにした。
- フォルダツリーのキーボード操作中に、表示先のフォルダパネルへフォーカスが移ってフォルダパネル内の項目が勝手にアクティブになる問題を修正した。
- 配布用の `Meldex` / `MeldexCloud` へフォルダ・ツリー関連の分割JSを同期した。
- 表示バージョンを `0.6.017` に更新した。

検証: フォルダツリー/フォルダパネル回帰unittest、フォルダビュー配布同期回帰unittest、フロントエンド構文回帰unittest、バージョンメタデータ回帰unittest、結合済みJavaScript構文チェック、全スクリプト読み込み監査、差分空白チェック。全体unittestは完走し、今回変更外の既存静的ガード系 failure/error を確認。

## v0.6.016 (2026-05-22)

### fix(viewer): キーボード移動時のフォルダツリーハイライトを同期

- ビューワーで左右キー、Shift+左右キー、上下キーによるフォルダ移動、シークバー、再生などで表示ファイルが切り替わった時、現在表示中のファイルをMeldex本体へ通知するようにした。
- Meldex本体側で信頼済みの内部ビューワーiframeからの通知だけを受け、フォルダツリーの該当ファイルをハイライトし、必要に応じてツリーを展開・スクロールするようにした。
- 詳細パネルの対象パスも現在表示中のファイルへ追従させ、ビューワー内表示と周辺UIの文脈がずれないようにした。
- 配布用・Cloud静的用の `viewer.html` にも同じ通知処理を反映し、配布物の取り残しを回帰テストで固定した。
- 表示バージョンを `0.6.016` に更新した。

検証: ビューワー回帰unittest、画像ビューワー・サムネイル回帰unittest、フロントエンド回帰unittest、バージョンメタデータ回帰unittest、split bundle整合性unittest、表示切替操作チェーン静的監査、配布用viewer同一ハッシュ確認、JavaScript/Python構文チェック、差分空白チェック。

## v0.6.015 (2026-05-22)

### fix(viewer): Meldex本体内ビューワーの画像表示を修正

- Meldex本体から画像を開く際、内部URLが `/viewer.html?...` に書き換わった後も信頼済みビューワーとして扱うようにした。
- `/viewer.html` の埋め込みiframeに `allow-same-origin` が付かず、ビューワー本体と画像取得経路が隔離されて黒画面になる問題を修正した。
- `/viewer` と `/viewer.html` の両方を同じ内部ビューワーとして判定する回帰テストを追加した。
- 表示バージョンを `0.6.015` に更新した。

検証: フロントエンド回帰unittest、ビューワー回帰unittest、画像ビューワー・サムネイル回帰unittest、ルータ回帰unittest、split bundle整合性unittest、バージョンメタデータ回帰unittest、JavaScript/Python構文チェック、Meldex本体から画像を開いたChromeヘッドレススクリーンショットのAI目視、差分空白チェック。

## v0.6.014 (2026-05-22)

### fix(viewer): 埋め込みビューワーの画像黒画面を修正

- 画像のロード完了イベントを待つ前に、描画可能な寸法が取得できた時点で表示レイヤーを切り替えるようにした。
- パネル内の埋め込みビューワーで、画像自体は取得できているのに `complete` 待ちで黒画面が続く問題を修正した。
- `/viewer` のHTMLをWebView側で古い内容のまま使わないよう、キャッシュ抑止ヘッダーを追加した。
- 配布用・Cloud静的用の `viewer.html` にも同じ画像初期表示修正を反映し、ソースと配布物の取り残しを回帰テストで固定した。
- 表示バージョンを `0.6.014` に更新した。

検証: ビューワー回帰unittest、画像ビューワー・サムネイル回帰unittest、ルータ回帰unittest、Cloud静的ビルド回帰unittest、バージョンメタデータ回帰unittest、JavaScript/Python構文チェック、Chromeヘッドレスによる埋め込みビューワー実画面スクリーンショットのAI目視、差分空白チェック。

## v0.6.013 (2026-05-22)

### fix(viewer): NAS画像ビューワーの黒画面を修正

- 単一画像・複数画像・フォルダをビューワーで開くとき、フォルダ先頭の複数画像のメタ情報読み込みを待たず、表示対象の画像を最優先で表示するようにした。
- 単一画像をビューワーで開くとき、親フォルダの画像一覧取得を待たずに選択画像を先に表示し、一覧は取得後に反映するようにした。
- NAS/絶対パス上の画像はサムネイルと同じプレビュー生成経路を先に使い、生ファイル読み込みは予備に回すようにした。
- NAS上の画像読み込みが詰まった場合でも黒画面のまま止まらないよう、画像ロードにタイムアウトを設け、PNGプレビュー生成へフォールバックするようにした。
- ビューワーのカウンターを画像ロード完了待ちより前に更新し、読み込み途中でも表示対象が空に見えないようにした。
- `ビューワー_設計意図.md` に、NAS/絶対パス画像の初回表示とプレビュー生成優先の方針を反映した。
- 表示バージョンを `0.6.013` に更新した。

検証: ビューワー回帰unittest、画像ビューワー・サムネイル回帰unittest、バージョンメタデータ回帰unittest、JavaScript構文チェック、Chromeヘッドレスによるビューワー実画面スクリーンショットのAI目視、差分空白チェック。

## v0.6.012 (2026-05-22)

### fix(viewer): NAS上の画像表示とサムネイル生成を修正

- ビューワーでNAS/絶対パスの先頭スラッシュが直URL扱い・削除され、画像が `/api/file-raw` ではなく壊れたURLへ向く問題を修正した。
- フォルダ内パネルの画像サムネイルで、通常表示に失敗した場合はサーバ側サムネイルへフォールバックするようにした。
- `jfif`、`tiff`、`heic`、`psd` などブラウザが直接表示しづらい画像も画像項目として扱い、Pillow/WindowsシェルでPNGプレビューを生成できるようにした。
- 表示バージョンを `0.6.012` に更新した。

検証: 画像ビューワー・サムネイル回帰unittest、NAS風パスURL変換回帰unittest、セキュリティ回帰unittest、ルータ回帰unittest、フォルダパネル回帰unittest、パス移動追従回帰unittest、バージョンメタデータ回帰unittest、差分空白チェック。

## v0.6.011 (2026-05-22)

### fix(scriptnote): 区切り表示設定の配置と共通フォント継承を修正

- タイプ管理の書式設定で、大区切り・小区切りの単位、桁、位置を折返設定の下段に分離した。
- 表示タブから大区切り・小区切りの単位、桁、位置設定を削除し、区切り表示の設定場所を書式設定へ一本化した。
- マンガシナリオの大区切り初期単位を `p` に変更し、新規シナリオの初期表示を `p01` にした。
- 大区切り・小区切りの既定フォントを固定幅フォントから共通フォント継承へ変更し、`Noto Sans JP` などテーマの共通フォントが反映されるようにした。
- 表示バージョンを `0.6.011` に更新した。

検証: JavaScript構文チェック、シナリオタイプ管理・書式設定回帰unittest、シナリオ微細挙動unittest、シナリオ微細挙動E2E、バージョンメタデータ回帰unittest、差分空白チェック。

## v0.6.010 (2026-05-22)

### feat(scriptnote): タイプ管理の区切り表示設定を書式設定へ追加

- シナリオのタイプ管理で、大区切り・小区切りの書式設定から単位、桁、位置を変更できるようにした。
- 同じ区切り表示設定を列ヘッダーから開く一括書式設定でも変更できるようにした。
- 書式設定の「共通フォント」が、設定ダイアログのテーマタブで使う共通フォント候補と同じ `--ui-font` 継承になることをE2Eと回帰テストで固定した。
- 表示バージョンを `0.6.010` に更新した。

検証: JavaScript構文チェック、シナリオタイプ管理・書式設定回帰unittest、シナリオ微細挙動unittest、シナリオ微細挙動E2E、バージョンメタデータ回帰unittest、全スクリプト読み込みチェック、差分空白チェック。

## v0.6.009 (2026-05-22)

### fix(scriptnote): 横書き折り返しの下端見切れを修正

- シナリオの横書き折り返し表示で、最終描画後に行高が増えた場合も段を詰め直し、`p17` から `p20` などが段の下へ隠れないようにした。
- 縦書き折り返しの再段組みは、通常の最終サイズで詰め直した後、なお残るはみ出しだけを補正するようにし、左側の過剰な空白を抑えた。
- シナリオ微細挙動E2Eに、標準列・ページ区切り密集・横書き折り返しの下端見切れ再発防止ケースを追加した。
- 表示バージョンを `0.6.009` に更新した。

検証: JavaScript構文チェック、シナリオ微細挙動unittest、シナリオ微細挙動E2E、横書き/縦書きスクリーンショットAI目視、Batch128/131/133シナリオ回帰unittest、バージョンメタデータ回帰unittest、差分空白チェック。

## v0.6.008 (2026-05-22)

### fix(scriptnote): 縦書き折り返しの左端見切れを再修正

- シナリオの縦書き折り返し表示で、ページ区切り行が密集したデータでも、段の最終幅を実表示のはみ出し量で補正して詰め直すようにした。
- フォルダツリー表示中や狭いウィンドウ幅でも、右端側の `p01` と左端側の段が同時に表示範囲内へ収まるようにした。
- `Shift+Delete` の行削除ショートカット経路でも、削除後の隣行フォーカスとカスタムキャレット更新を通常の行削除経路と同じタイミングで行うようにした。
- シナリオ微細挙動E2Eに、標準列・ページ区切り密集・縦書き折り返しの左端見切れ再発防止ケースを追加した。
- 表示バージョンを `0.6.008` に更新した。

検証: JavaScript構文チェック、シナリオ微細挙動unittest、シナリオ微細挙動E2E、横書き/縦書き/狭幅スクリーンショットAI目視、Batch128/131/133シナリオ回帰unittest、バージョンメタデータ回帰unittest、差分空白チェック。

## v0.6.007 (2026-05-22)

### fix(scriptnote): 縦書き折り返しの表示幅追従を修正

- シナリオの縦書き折り返し表示で、フォルダツリー表示やウィンドウ幅変更後も、現在の表示可能幅に合わせて段を組み直すようにした。
- 段組み計算でスクロールバーを含む外形幅ではなく、実際に表示できる幅を上限に使い、左端側のページ/コマ/タイプ列が見切れないようにした。
- 右ドラッグパンや行ドラッグ端スクロールで、縦書き折り返し中に横位置がずれないようにした。
- シナリオ微細挙動E2Eに、標準列つき縦書き折り返し・幅縮小・折り返し切替・古い横位置・右ドラッグ/行ドラッグの再発防止ケースを追加した。
- 表示バージョンを `0.6.007` に更新した。

検証: JavaScript構文チェック、シナリオ微細挙動unittest、シナリオ微細挙動E2E、標準列つき縦書き折り返しE2E、Batch128シナリオ回帰unittest、バージョンメタデータ回帰unittest、差分空白チェック。

## v0.6.006 (2026-05-22)

### fix(scriptnote): 縦書き折り返しの先頭ページ見切れを根本修正

- シナリオの縦書き折り返し表示で、段を横スクロール位置で補正せず、表示幅内に右寄せで収めるようにした。
- 折り返し段の最終幅を再測定して詰め直し、`p01` など先頭側のページ表示が右外へ押し出されないようにした。
- シナリオ微細挙動E2Eに、古い横位置が残っていても横スクロール量0のまま先頭ページ表示が見えることを検査するケースを追加した。
- 表示バージョンを `0.6.006` に更新した。

検証: JavaScript構文チェック、シナリオ微細挙動unittest、シナリオ微細挙動E2E、4K相当ウィンドウでの縦書き折り返しE2E、バージョンメタデータ回帰unittest、差分空白チェック。

## v0.6.005 (2026-05-21)

### fix(scriptnote): 縦書き折り返しで先頭ページ表示が見切れる問題を修正

- シナリオの縦書き折り返し表示で、長文行が後から幅を広げる分を段分割前の測定に含め、`p01` など先頭側のページ表示が右端で隠れないようにした。
- シナリオ微細挙動E2Eに、縦書き折り返し時の先頭ページ表示と段の横方向見切れを検査するケースを追加した。
- 表示バージョンを `0.6.005` に更新した。

検証: 徹底チェック、JavaScript構文チェック、シナリオ微細挙動unittest、シナリオ微細挙動E2E、バージョンメタデータ回帰unittest。

## v0.6.004 (2026-05-21)

### fix(import): Eagle参照型ソースフォルダの画像表示を修正

- Eagle参照型で作成した参照ノートを、元ファイルの拡張子に応じて画像・動画・音声としてフォルダ表示できるようにした。
- 参照ノートを安全な入口としてEagle元ファイルを読み込むAPIを追加し、サムネイル、プレビュー、ボード配置で参照元画像を表示するようにした。
- 参照型ソースフォルダ内の参照ノートを削除・移動しても、Eagle側の元ファイルは変更しない動作を維持した。
- 表示バージョンを `0.6.004` に更新した。

検証: 徹底チェック、Python/JavaScript構文チェック、Eagle/PureRef/グローバルタグ回帰unittest、フォルダ表示回帰unittest、ルーター回帰unittest。

## v0.6.003 (2026-05-20)

### fix(import): Eagleライブラリ選択をMeldex内ダイアログへ変更

- Eagleから取り込む設定で、ネットワークフォルダ展開が遅い古いツリー型フォルダ選択を使わないようにした。
- Meldex内のテーマに沿った選択ダイアログで、Eagleライブラリ本体または親フォルダのパスを指定し、親フォルダ内の `.library` 候補を一覧から選べるようにした。
- ブラウザ標準の入力警告ダイアログへフォールバックしないようにした。
- 「追加」押下後は取り込みセットの保存完了までダイアログを閉じず、失敗時はその場にエラーを表示するようにした。
- 参照型・コピー型の取り込みセット追加時はライブラリ全体を走査せず、場所の軽量確認だけで追加できるようにした。
- Eagle参照型の「更新を反映」完了後、生成された参照用フォルダをソースフォルダとしてフォルダツリーに追加するようにした。
- 表示バージョンを `0.6.003` に更新した。

検証: 徹底チェック、Python/JavaScript構文チェック、Eagle/PureRef/グローバルタグ回帰unittest、Shell API回帰unittest、バージョンメタデータ回帰unittest。

## v0.6.002 (2026-05-20)

### fix(import): Eagleライブラリ選択を専用ダイアログへ変更

- Eagleから取り込む設定で、`.library` フォルダ選択時に「フォルダーの選択」を押しても確定されない環境向けに、Eagleライブラリ選択だけWindowsのツリー型フォルダ選択を使うようにした。
- フォルダ選択APIにダイアログ種別を指定できる経路を追加し、通常のソースフォルダ選択は従来どおり維持した。
- 表示バージョンを `0.6.002` に更新した。

検証: 徹底チェック、Python/JavaScript構文チェック、Eagle/PureRef/グローバルタグ回帰unittest、ルーター回帰unittest、Shell API回帰unittest、バージョンメタデータ回帰unittest。

## v0.6.001 (2026-05-20)

### fix(sheet): シートツールバーの表示維持を安定化

- シートを復元した直後、チャットなどの補助パネルへフォーカスした後、パネル内でシート表示を切り替えた後でも、表示中のシートツールバーが消えないようにした。
- 表示中のシートパネルを検出してツールバーの配置先を補正し、補助パネル側の状態同期でシートツールバーを非表示にしないようにした。
- 再描画でシートツールバーのDOM自体が失われた場合も、初期HTMLから復元して表示中のシートパネルへ再配置するようにした。

### fix(import): Eagle参照型追加とPureRef変換を修正

- Eagleから取り込む設定で、Eagleライブラリ選択後に追加名の確認ダイアログを挟まず、選択したライブラリをそのまま取り込みセットへ追加するようにした。
- フォルダパネルのPureRefファイル右クリックメニューから、同じフォルダ内にボードファイルへ変換できるようにした。
- PureRef取り込みAPIでソースフォルダ相対パスを受け付け、フォルダパネル上のファイルから直接変換できるようにした。

### chore(version): 修正単位の3桁パッチ運用へ変更

- 表示バージョンを `0.6.001` に更新し、右端3桁のパッチ番号を修正ごとに `0.0.001` ずつ上げる運用へ変更した。
- バージョン整合テストを `X.Y.NNN` 形式に更新し、公開ページの表示バージョンも正本と一致させた。

検証: 徹底チェック、シートツールバー関連unittest、Pane Bridge連結構文チェック、シート微細E2E、ツールバーDOM消失復元E2E、Eagle/PureRef/グローバルタグ回帰unittest、バージョンメタデータ回帰unittest。

## v0.6.0 (2026-05-17)

### feat(import): Eagle/PureRef取り込みとタグ管理を追加

- PureRefファイルをアップロードまたはパス指定で取り込み、抽出できた画像をMeldexボードへ配置するAPIと設定画面を追加した。
- Eagleライブラリのプレビュー、コピー型取り込み、参照型取り込み、参照型の手動更新を追加した。コピー型は初回成功後に固定し、以後のEagle側変更を反映しないようにした。
- Eagle参照型の更新時に、Eagle側で削除されたアイテムに対応する参照ノートを整理するようにした。
- ソースフォルダ単位のグローバルタグ索引とAPIを追加し、Eagle取り込みタグを統合するようにした。
- フォルダパネルにタグ管理パネルを追加し、タグの作成、名前/色変更、削除を行えるようにした。
- `app/docs/eagle-pureref-import-global-tags-plan-2026-05-20.md` に、実装状況と今後の拡張候補を追記した。

検証: 徹底チェック、Python/JavaScript構文チェック、全スクリプトファイル読み込みチェック、Eagle/PureRef/グローバルタグ回帰unittest、外部取り込み/ルーター/分割資産/監査マニフェスト回帰unittest。全体unittestは1958件を実行し、既存のsplit-loader静的検査・別領域の同期検査を中心に failures=45 / errors=8 / skipped=4 を確認。

### fix(history): 書式設定を履歴とバージョン管理へ反映

- ノート、シート、ボード、シナリオのファイル別書式設定で、テーマ変更、プリセット適用、リセット、各フィールド変更がUndo/Redoまたは履歴へ残るようにした。
- ノート/シート系の書式変更でバージョン管理の変更フラグを立て、ヒストリーから書式変更前後へ戻せるようにした。
- シナリオとボードの選択範囲書式変更で、DOM変更前に各エディタのUndoを記録するようにした。
- PureRefインポート、Eagleコピー型/参照型インポート、フォルダパネルのグローバルタグ管理の実装計画を追加した。

検証: 徹底チェック、全スクリプトファイル読み込みチェック、JavaScript構文チェック、書式/詳細パネル/シナリオ/ノート回帰unittest、バージョンメタデータ回帰unittest。全体unittestは1953件を実行し、既存のsplit-loader静的検査・同期検査を中心に failures=52 / errors=8 を確認。

### fix(scriptnote/layout): シナリオ設定UIとパネル境界ドラッグを安定化

- パネル境界線のドラッグで、同じ軸上の隣接区間だけをリサイズし、内側や外側の別境界線が意図せず連動しないようにした。
- シナリオのオプションパネルで、非表示タブが選択中の見た目になる問題、折り返しチェックボックスの重複表示、オン状態のツールバーアイコンが見えなくなる問題を修正した。
- タイプ管理タブで、テキスト列の太字などの書式がタイプ列へ波及しないようにし、複数選択時の書式ポップアップを最下選択行基準で表示するようにした。
- タイプ管理タブの一括設定で付けた太字、斜体、サイズ、色、前後文字が空値指定で解除されるようにし、既存のテキスト列書式にも解除を反映するようにした。
- タイプ設定ポップアップを表の下に隠れない前面表示にし、タイプ管理の「オプション」ボタンが枠内に収まるよう調整した。
- 表示タブの自動配色に、繰り返しに使うテーマカラー行の指定を追加し、初期値を3行目にした。

検証: 徹底チェック、JavaScript構文チェック、シナリオ/レイアウト/高リスク回帰unittest、シナリオテーマE2E、パネル境界ドラッグE2E、バージョンメタデータ回帰unittest。

### fix(production): 分割シフト時の再計算を安定化

- 制作管理の再計算で、合計作業時間だけでは収まる見込みでも分割シフトの空き枠に入らず未配置が残る場合に、短縮率を段階的に下げて再配置を試すようにした。
- 再計算の各試行でシフト残時間を複製し、前の試行のカーソル進行が次の試行へ混ざらないようにした。
- センターカラー1P＋本文21P、管理者1名、メインアシスタント3名、最終日ヘルプ2名、月曜開始から翌月曜7時締切のテストデータを作成し、通常割当では不足する作業予定を再計算で全件配置できることを確認した。

検証: 徹底チェック、Python構文チェック、制作管理API/frontend回帰unittest、制作管理追加回帰unittest、テスト作品/テストメンバーを使った制作管理実機テスト。

### feat(calendar): アナログ時計と複数日表示を強化

- スケジューラーのアナログ時計表示で、日/週表示と同じように予定カードへタイトル、ユーザーアイコン、選択チェックボックスを表示するようにした。
- アナログ時計の予定カード幅と重なり方を日/週表示へ寄せ、単独の予定は帯幅を広く使い、重なる予定は段差付きで見えるようにした。
- アナログ時計の予定カード配置を、外周=左端、中心=右端、開始=上端、終了=下端として日/週表示のカード座標へ揃え、ユーザーアイコンを後端中心寄せ、選択チェックを前端外周寄せにした。
- 12時間のアナログ時計表示でもAM/PMを内外の二重リングに分けず、同じカード帯の中で重なり表示するようにした。
- 12時間のアナログ時計表示の右上に、午前/午後の切替ボタンを追加した。
- 24時間のアナログ時計表示では、12時間用の目盛りではなく、24時間表示に合わせた1時間ごとの大目盛りと30分ごとの小目盛りへ切り替えるようにした。
- 24時間のアナログ時計表示で時刻ラベルの半径を揃え、12時間/24時間表示の短針を従来より太くした。
- アナログ時計の選択チェックをカード内の表示に合わせて小さくした。
- アナログ時計外クリックで予定選択を解除し、時計左上へ西暦年月日と曜日を表示するようにした。
- アナログ時計の予定カードを不透明な塗りカードにし、四隅だけを角丸にして、黒フチと日/週表示に合わせた文字書式で表示するようにした。
- アナログ時計の選択チェックボックスは選択中のみ表示し、丸いリサイズハンドルを出さずにカード両端のドラッグで調整できるようにした。
- アナログ時計表示で、時計盤ドラッグによる予定作成、予定カードの移動、開始/終了端のドラッグリサイズに対応した。
- 「日」と「週」の間の表示として「複数日」を追加し、表示日数指定とミニカレンダーのCtrl/Cmd複数選択、Shift範囲選択で選んだ日を表示できるようにした。
- フォルダツリーの選択ハイライト時に、フォルダ開閉アイコンが文字色と同じ色で見えるようにした。

検証: 徹底チェック、AI目視、AI目視用実ブラウザスクリーンショット、JavaScript構文チェック、カレンダー/フォルダツリー/CSS回帰unittest、カレンダー微細E2E、バージョンメタデータ回帰unittest。

### feat(webclip): Web Clipperとメモの既存タグ自動付与を追加

- Web Clipperの保存先フォルダをシートとして初期化し、新規クリップを `settings-entry` として保存するようにした。既定保存先 `Web Clipper` と任意の `save_dir` のどちらでも、フォルダノートの既存本文・既存プロパティ・タグ候補を保持したまま不足項目だけを補う。
- Web Clipperとクイックメモに、対象シートの既存タグ候補だけを使う「タグを自動付け」を追加した。候補外タグは作成せず、手入力タグと保存済みタグを保持する。
- Web Clipperの保存時自動タグ付けは明示ONの場合だけ動く既定OFFの設定にし、クイックメモは自動タグ付け後の再保存でタグが消えないよう同期処理を更新した。
- ノート編集の `Ctrl+Enter` に、現在ブロック直後へ新しい段落を追加する共通操作を追加した。`Shift+Enter` は入力欄内改行として既存挙動を維持する。
- Google Drive同期フォルダ取り込みの開発計画を追加し、Googleドキュメント/スプレッドシート/CSV/XLSXの共通取り込みと完全再現ビューの段階実装方針を整理した。

検証: Python/JavaScript構文チェック、Web Clipper/クイックメモ/ショートカット/ローカルAPI保護/ルーター回帰unittest、バージョンメタデータ回帰unittest、差分チェック。

### release: 総合監査と公開前ゲートを完了

- `app/docs/meldex-v0.6.0-full-line-audit-targets-2026-05-11.txt` の監査対象を全ファイル全行チェックし、総合AI監査の発見事項を修正・検証した。
- 仕様定点観測を `spec-snapshot-v0.6-20260517-release-gate/` と監査レポートへ反映し、Cloud/Dropbox、デバッグ、自動デバッグ、CodexWorker、制作管理、スケジューラー、履歴、マニュアル、公開/配布の現行仕様を固定した。
- Meldex Cloud BETAの公開範囲を、シート、スケジューラー、スマートシート、クラウド版フルチャット、暗号化APIキーCloud保存、注釈、バージョン管理、フィードバック送信、Dropbox競合検知/解決、スマホ/タブレットUI、ホーム画面追加まで含む形に整理した。
- デバッグリスト、自動デバッグ、CodexWorker、制作管理、スケジューラー、勤怠、シート/スマートシート連携の回帰テストと実ブラウザE2Eを再確認した。
- マニュアル、公開トップ、配布チャネル、Notion任意ミラー方針、Cloud APIキー保存、外部カレンダー同期制限の説明を最新仕様に揃えた。
- バージョン正本を `0.6.0` に更新し、公開ページ/配布ページの表示バージョンを揃えた。

互換性/制約:

- Cloud BETAの未対応範囲は、リアルタイム共同編集、ローカルCLI/MCP/OS連携、スケジューラーの外部同期リレー未設定時のGoogle/CalDAV同期。
- 暗号化APIキーCloud保存はパスフレーズを保存しないため、忘れると保存済みAPIキーを復号できない。
- 破壊的な保存形式変更は追加していない。旧版へ戻す場合は、通常どおり作業フォルダと `meldex.db` のバックアップを取ってから行う。

検証: 全ファイル全行監査、Phase 3デバッグ/制作管理/カレンダー系ゲート、Phase 4高リスクE2E/6環境マトリクス、Phase 5仕様定点観測、Phase 6ドキュメント回帰、バージョンメタデータ回帰unittest。

## v0.5.454 (2026-05-10)

### refactor(core): 全スクリプトを1000行未満へ分割

- ルート、デスクトップ配布、Cloud配布の JS / CSS / Python / HTML / テストファイルを再走査し、1000行以上のファイルが残らないように分割または行整理した。
- split-loader の nested chunk と bundle chunk を配布ビルドに含め、Cloud静的ビルドでは split ファイルを再結合せず1000行未満のまま配布するようにした。
- split済みファイルを読む静的回帰テストを再帰展開対応に揃え、巨大フロントエンド回帰テストも分割済み構成で実行できるようにした。
- `app/Meldex/` と `app/MeldexCloud/` を現在の split 構成から再生成し、配布成果物側にも巨大スクリプトが残らない状態へ更新した。

検証: 1000行未満全ファイル走査、Python構文チェック、分割資産回帰unittest、Cloud静的ビルド/検証、Cloud公開readiness回帰unittest、主要フロントエンド/API/セキュリティ回帰unittest。

## v0.5.453 (2026-05-10)

### refactor(chat): LLMチャットストリームを分割

- LLMチャットの巨大なストリーム処理から、ツール定義、ツール実行補助、プロンプト生成、履歴圧縮、Geminiストリーム、共通ストリーム補助を専用モジュールへ分離した。
- 既存の `meldex_chat_stream` import 互換を維持し、API、MCP、ファイルロック、ナレッジ、スキル、引用、CLIチャットの既存呼び出しを変えずに動くようにした。
- チャット周辺の新規/分割モジュールを監査バッチ定義へ追加し、分割後の静的検査が新しいファイル構成を読むようにした。
- 全体チェックで見つかった分割後の静的検査参照先ずれを修正し、チェックボックス監査マニフェストを再生成した。

検証: 徹底チェック、全スクリプト読み込み監査、Python構文チェック、JS構文チェック、HTML script参照チェック、チャット/LLM関連回帰unittest、全unittest。

## v0.5.452 (2026-05-10)

### fix(cloud): フォルダツリーにCloud作品データを表示

- クラウド版のフォルダ表示とフォルダツリーで、ノート、ボード、シナリオ、スマートシート、シートフォルダ、カレンダーが一覧取得時点で正しい種類として表示されるようにした。
- シートやカレンダーのようなフォルダ型データを、Cloud一覧内ではフォルダ側に並べたまま開けるようにした。
- 旧シナリオ/現行シナリオ、シート/スマートシートの保存済み表示フィルタが残っていても、該当アイテムが消えにくいようにした。
- フォルダツリーの文字サイズを上げつつ、行の高さは固定して見た目の密度が変わらないようにした。

検証: 徹底チェック、Cloudフォルダ表示回帰unittest、フォルダツリー回帰unittest、ツールバー回帰unittest、Batch57 Cloud回帰unittest、Cloud静的ビルド/検証。

## v0.5.451 (2026-05-10)

### feat(dropbox): Dropboxアカウント基準のプロフィール同期を追加

- Dropbox `account_id` を内部キーにして、ユーザー名、ユーザーアイコン、アイコン背景色を `MeldexSettings/_meldex/profiles.v1.json` へ保存し、デスクトップ版 / クラウド版の同一Dropboxアカウント間で揃えるようにした。
- 起動時に共有プロフィールをローカル設定へ反映し、未登録の場合は既存ローカル名またはDropbox表示名から初回プロフィールを作成するようにした。
- Dropbox接続情報が残っていても、PC専用ローカルフォルダでは共有プロフィールを読まず、DropboxモードまたはDropbox上のソースフォルダでのみ同期するようにした。
- プロフィール保存時に共有プロフィールと各ソースフォルダの `_Meldex_team.json` を更新し、チーム情報には後方互換フィールドとして `accountId` を保持するようにした。
- 同じ `accountId` でユーザー名を変更した場合は、チームファイル内の古い名前エントリを同一人物として整理するようにした。
- プライバシーポリシーと設定マニュアルに、Dropbox接続中のプロフィール保存先を追記した。

検証: Dropboxプロフィール同期モックunittest、チーム同期API回帰unittest、分割バンドル回帰unittest、Cloud静的ビルド/検証、JS/Python構文チェック。

## v0.5.450 (2026-05-10)

### fix(production): 制作管理の欠損ファイル自動復旧を追加

- 制作管理フォルダ、必須ページ、各シートフォルダ、シート定義ノートの欠損を `status` で検知し、「制作管理を始める」や通常操作時に自動で再作成するようにした。
- 制作管理操作後に制作管理フォルダ全体の復元ポイントを保存し、制作管理フォルダごと削除された場合でも最新の復元ポイントから戻せるようにした。
- `スケジュール` または `タスクリスト` が消えた場合、Meldexカレンダーに残っているシフト/作業予定から最低限のシート行を再構成するようにした。
- Desktop版/Cloud版とも、復旧が走った場合は「不足していた制作管理ファイルを自動復旧しました」と分かる表示を返すようにした。

検証: 徹底チェック、Python構文チェック、JS構文チェック、制作管理API回帰unittest、制作管理frontend回帰unittest、制作管理復旧HTTP実機E2E。

## v0.5.449 (2026-05-10)

### feat(production): Meldex完結型の制作管理を追加

- Notionを使わず、ソースフォルダ内の `制作管理/シート` に `作品リスト`、`タスクリスト`、`作業対象リスト`、`作業内容リスト`、`作業規模リスト`、`スタッフリスト`、`スケジュール`、`勤怠情報` などの標準シートを作成できるようにした。
- ページ単位とコマ単位を選んで、作品・ページ・コマ・作業対象・作業内容・作業規模の組み合わせから `タスクリスト` を作成できるようにした。
- シフト表CSV/Excelをブラウザで読み込み、`スケジュール` シート、ユーザー別シフトカレンダー、既存カレンダーDBへ反映できるようにした。
- 担当者と開始・終了時刻の割り当てを実行し、`作業予定日時` の `開始|終了` 範囲と作業予定カレンダーイベントを作成できるようにした。
- シフト、実績、作業予定をCSVまたはExcelで書き出せるようにし、CSVはExcelで文字化けしにくいBOM付きUTF-8にした。
- Desktop版とCloud版の混在運用に向けて、Cloud保存経路でも制作管理シート作成、タスク作成、シフト取り込み、CSV/Excel書き出し、シフトイベント同期が動くようにした。
- シート日付からカレンダーへ同期する処理で `開始|終了` の終了時刻を保持し、シフトが日をまたぐ場合は終了日を翌日にするようにした。
- Cloud BETA公開workflowでLinux runnerのファイル名制限に当たるサンプル補助ディレクトリをcheckout対象から外し、公開ビルドを通せるようにした。
- 制作管理APIのGET系エンドポイントが静的ファイル配信に先に拾われる問題を修正し、`status` と書き出しAPIを通常サーバー起動でも使えるようにした。
- シフト取り込みと担当者割り当てで、DB更新中にシートを書いて編集ロック確認と競合する問題を修正した。
- 初期化済み制作管理テンプレートは通常操作時に再生成しないようにし、ページ/コマ作成、シフト取り込み、担当者割り当て、書き出しの待ち時間を減らした。

検証: 徹底チェック、Python構文チェック、JS構文チェック、制作管理API回帰unittest、制作管理frontend回帰unittest、既存カレンダーシフト同期回帰unittest、カレンダー日付同期frontend回帰unittest、分割資産回帰unittest、Cloud静的ビルド/検証、バージョンメタデータ回帰unittest、テスト作品/テストユーザーを使った制作管理HTTP実機E2E。

## v0.5.448 (2026-05-10)

### fix(cloud): クラウド版のバッチ削除を実装

- デスクトップ版で追加した `/outliner/delete-batch` と同じ処理をクラウド版Dropbox互換層にも追加し、複数削除時も各項目をゴミ箱へ移動してUndo用の `trash_name` を返せるようにした。
- クラウド版の単体削除とバッチ削除で同じ削除処理を使うようにし、保存先/フォルダリンク/パス変更フックの後処理がずれないようにした。
- フロントエンドの編集ロック事前判定で、バッチ削除の `items[].path` もロック対象として拾うようにした。
- Windows上でNotion同期の原子的書き込みが一瞬 `PermissionError` になる場合に短く再試行し、tmp残骸を残しにくくした。

検証: 全スクリプト読み込み監査、Python構文チェック、JS単体/結合構文チェック、PowerShell構文チェック、全unittest、Outliner削除API回帰unittest、クラウド公開静的ビルド/検証、クラウド公開Chrome E2E（PC幅/スマホ幅）、クラウド公開readiness・分割バンドル・バージョンメタデータ回帰unittest。

## v0.5.447 (2026-05-10)

### perf(file): 削除とファイル表示の重い同期処理を削減

- 通常のフォルダツリー削除で、親フォルダ全体コピー、削除対象ファイルコピー、DB全体コピーを作らず、ゴミ箱移動とメタデータ作成を同期完了点にするようにした。
- 注釈孤児化とバックリンク削除はレスポンス後の後処理へ移し、削除APIがファイル数に比例して詰まりにくい経路へ整理した。
- 複数削除用の `/api/outliner/delete-batch` を追加し、UIの楽観的削除/Undo用 `trash_name` 取得を維持したまま、削除API呼び出しをまとめるようにした。
- 自動リンクとコメント表示を共通の表示レイヤー設定として既定OFFにし、ノート本文は先に描画して、オンの場合だけアイドル時間に自動リンク/コメントバッジを適用するようにした。
- コメント表示OFFでも、本文DOMを走査せずに未解決コメントの存在だけ分かるツールバーバッジを追加した。
- クラウド側のロック事前ガードに `/outliner/delete-batch` を追加し、バッチ削除でもロック対象を変更できないようにした。

検証: Python構文チェック、JS結合構文チェック、Outliner削除API回帰unittest、削除/ゴミ箱スモークunittest、データ損失防止unittest、ファイルロックunittest、フロントエンド静的回帰unittest、バージョンメタデータ回帰unittest。

## v0.5.446 (2026-05-10)

### test(audit): 総合AI監査の合格判定を6環境対応に拡張

- チェックボックス/トグル監査マニフェストを分類付き監査表へ拡張し、全候補を `runtime-auto` / `covered-by-existing-e2e` / `static-only` / `manual-required` / `false-positive` のいずれかに分類するようにした。
- 未分類候補が残った場合に総合AI監査unittestで失敗するようにし、runtime対象はクリック、ラベルクリック、Space、disabled、依存UI、再オープン保持をE2Eで検査するようにした。
- デスクトップ版/クラウド版 × PC/タブレット/スマホの6環境マトリクスに、デバイス種別、最低確認項目、手動確認が必要な項目、実行ケース、ログ保存先を記録するようにした。
- Cloud静的成果物に対して、設定/ソースフォルダ面とチェックボックス面をPC/タブレット/スマホ幅で確認できるE2Eケースを追加した。
- E2E監査中に検出された、エラー通知、ミニカレンダー、レスポンシブ幅の左クローム検査、タブレット幅のシートmicro検査の監査用識別・期待値を修正した。
- 環境マトリクスrunnerとCloud静的runnerのログ/成果物ディレクトリを衝突しない名前にし、並行実行でも監査ログが混ざらないようにした。

検証: Python構文チェック、JS構文チェック、総合AI監査unittest、UI/操作監査unittest、high-risk/micro coverage回帰unittest、batch47 frontend回帰unittest、チェックボックス監査Chrome E2E、Cloud静的Chrome E2E、6環境プロファイル別Chrome E2E（L-PC/L-TAB/L-MOB/C-PC/C-TAB/C-MOB）、バージョンメタデータ回帰unittest。

## v0.5.445 (2026-05-10)

### fix(sheet): 日付範囲の取込と同期を正規化

- シート用の日付共通パーサを追加し、`2026/05/02`、`2026/05/02 13:00`、`13:00 ~ 21:00`、`21:00 ~ 05:00` を既存互換の `YYYY-MM-DD` / `YYYY-MM-DDTHH:MM` / `開始|終了` 形式へ正規化するようにした。
- CSV取込で日付見出し列を検出し、時刻範囲セルを日付範囲値へ変換しつつ、シートの `property_types` に `date`、`withTime`、`range` を自動設定するようにした。
- XLSXから直接シートへ取り込む `/api/import-xlsx` を追加し、CSV取込と同じ日付範囲正規化を通すようにした。
- JS側の日付表示ヘルパもスラッシュ区切り日付、時刻付き日付、基準日付き時刻範囲、翌日にまたがる時刻範囲を解釈できるようにした。
- Notion日付プロパティの `start` / `end` とMeldex内部値 `開始|終了` を相互変換し、同期時に終了日時を失わないようにした。

検証: Python構文チェック、JS構文チェック、CSV/XLSX日付範囲取込回帰unittest、Notion日付範囲同期回帰unittest、ルーター分割回帰unittest、日付ヘルパfrontend回帰unittest、セキュリティ回帰unittest、バージョンメタデータ回帰unittest。

## v0.5.444 (2026-05-10)

### fix(debug): 自動デバッグworkerとデバッグリストを安定化

- CodexWorkerが既存workerリポジトリを処理前にfetch/checkout/pullし、未コミット変更がある場合はデバッグリストをclaimせず一時停止するようにした。
- Codex実行後の失敗記録で、Codexが残した変更により失敗処理自体が壊れないようにした。
- デバッグリストのpendingタイトルに件名を反映し、claim時にワーカー実行IDをエントリへ保存するようにした。
- デバッグリストのダッシュボードウィジェットを現行スマートシート対応のチャート定義へ更新し、旧定義や独自列を残したまま安全に補修するようにした。
- デバッグリスト専用E2Eを追加し、ワークスペース作成、デバッグ報告作成、ダッシュボード表示、チャート描画、AI目視用スクリーンショットを確認できるようにした。

### ui(worker): 常駐アプリCodexWorkerの状態画面を刷新

- 状態画面をOSのライト/ダークテーマに合わせた配色へ変更し、DPIと画面解像度に応じて中央配置されるようにした。
- 状態バッジ、処理中タスク、当日処理数、推定料金、詳細ログを分けて表示し、再読み込み、一時停止、再開、処理後停止、今すぐ停止を押しやすくした。
- リポジトリ、ログ、設定、最新レポートへの導線を追加し、トレイ名と通知名をMeldex CodexWorkerへ更新した。

検証: Python構文チェック、自動デバッグworker回帰unittest、デバッグ管理サービス回帰unittest、デバッグUI回帰unittest、Settings DB API回帰unittest、Phase1サービス回帰unittest、manual screenshot回帰unittest、high-risk/micro coverage回帰unittest、generated E2E登録検査、Chrome E2E `targeted-debug-list-dashboard`、AI目視スクリーンショット確認、バージョンメタデータ回帰unittest。

## v0.5.443 (2026-05-10)

### feat(memo): 軽量クイックメモを追加

- Meldex本体を開かずに起動できる `quick-memo.html` と軽量ローカルサーバを追加し、デスクトップでは `Ctrl+Alt+M` のショートカットから起動できるようにした。
- タイトル、タグ、書式付き本文、音声入力、ペン、マーカー、塗り、消しゴムを備えた軽量メモUIを追加し、入力内容をローカルキューに保持してMeldexへ自動送信するようにした。
- ホームフォルダに「メモ」シートと `メモ.smart-db.json` を自動作成し、タグ、追加日時、更新日時、保存先を持つメモエントリとして保存するようにした。
- 保存済みメモファイルが別フォルダへ移動された後も、同じメモIDで検索して更新先を追跡できるようにした。
- スマホ/タブレットではPWAとしてホーム画面へ追加できる `quick-memo.webmanifest` とService Workerを追加し、Meldex本体のキャッシュを壊さないようクイックメモ専用キャッシュだけを管理するようにした。
- 音声入力はブラウザ標準の音声認識を優先し、未対応時はOpenAI文字起こしAPIへ送る録音フォールバックを用意した。
- Cloud静的ビルドにクイックメモ関連ファイルを含め、Meldex本体起動時に未送信メモを同期するブリッジを追加した。

検証: Python構文チェック、JS構文チェック、クイックメモ保存/移動追跡/HTMLサニタイズ回帰unittest、ルーター分割回帰unittest、Meldex分割Pythonコンパイル、Cloud静的収集確認、軽量サーバHTML/API/CORSスモーク、Chrome headlessデスクトップ目視、Chrome DevTools ProtocolによるiPhone幅目視と保存ボタン座標検証。

## v0.5.442 (2026-05-10)

### fix(sheet): シート読み込み時のビュータブ描画回帰を修正

- 古いペイン状態や表示先DOMの欠落でビュータブ描画が失敗しても、シート本体の読み込みを止めないようにした。
- ビュー選択ドロップダウンは更新しつつ、ビュータブ、ギャラリー、カンバン、タイムライン、カレンダーの表示先が見つからない場合は例外ではなく状態メッセージで止めるようにした。

### fix(board): `.board.md` の種別判定とサンプルボードを修正

- フォルダツリーとファイル種別判定で、フロントマターが壊れていても `.board.md` はボードとして扱うようにした。
- サンプルのストーリー構成フローチャートボードのYAML色指定を修正し、ボードとして安全に読み込めるようにした。
- ボード詳細パネルのスタイル管理ボタンに固有IDを付け、ツールバー側ボタンとのUI識別衝突を解消した。

### fix(cloud): ホーム追加済み状態からブラウザ起動へ復帰できるように修正

- クラウド版をホーム画面へ追加後、Chrome側でアプリをアンインストールしても、通常URLから「このままブラウザで起動」または「ホーム追加をやり直す」を選べるようにした。
- 「ホーム画面から起動してください」だけを表示して起動不能になるデッドエンドを解消した。

### docs(import): 外部取り込みとXブックマーク保存の計画メモを追加

- Notion / Obsidian / Evernote の読み取り専用取り込み・ミラー同期方針を整理した。
- Xブックマーク保存について、公式API利用、Meldex公式Xアプリ、ユーザー体験、保存形式の方針を整理した。

検証: JS構文チェック、シート/ボード/クラウドホーム復帰のfrontend回帰unittest、Phase1サービス回帰unittest、フォルダツリーChrome回帰unittest、シートツールバー回帰unittest、クラウド公開準備回帰unittest、シート復元Chrome E2E、ボードリンク詳細Chrome E2E、Cloud静的ビルド検証、バージョンメタデータ回帰unittest。

## v0.5.441 (2026-05-09)

### feat(webclip): Web Clipperの保存APIと設定導線を整備

- Web Clipper用の保存APIを追加し、記事、簡易記事、ページ全体、ブックマーク、選択範囲、画像、表示範囲画像、範囲画像をホームフォルダ配下へ保存できるようにした。
- 設定 > 拡張機能の先頭にWeb Clipperセクションを追加し、準備、読み込み用フォルダを開く、ブラウザ別の拡張機能画面を開く、手順ノートを開く導線をまとめた。
- 未準備のままブラウザボタンを押しても読み込み用フォルダを自動準備し、クラウド版ではデスクトップ版で設定する必要があることを表示するようにした。
- Web Clipper拡張からの保存POSTとCORSプリフライトだけを許可し、一般WebページからのトークンなしAPI呼び出しは拒否するようにした。

### docs(webclip): 設定方法と使い方の説明を更新

- Web Clipperのマニュアルと設定マニュアルを、現在のボタン名、読み込み用フォルダ、デスクトップ版前提、保存操作に合わせて更新した。
- Meldex未起動時のポップアップエラーを、起動してから再試行する案内へ変更した。

検証: Python構文チェック、JS構文チェック、Web Clipper API回帰unittest、Web Clipper frontend回帰unittest、セキュリティ回帰unittest、ルーター回帰unittest、frontend回帰unittest、Chrome headlessによる設定画面デスクトップ/390px幅スクリーンショット確認、Web Clipperポップアップスクリーンショット確認、バージョンメタデータ回帰unittest。

## v0.5.440 (2026-05-09)

### fix(onboarding): BETA案内ダイアログの位置とチェック表示を修正

- 初回起動時の「Meldex BETAについて」ダイアログが画面右下へずれる問題を修正した。
- 同意チェックボックスの独自描画をやめ、オン状態が確実に見えるネイティブチェックボックス表示へ戻した。

### feat(dropbox): 複数Dropboxソースフォルダと共有レジストリを追加

- ユーザー向けの保存「モード」表現を整理し、「保存先」「このPCに保存」「Dropboxと接続中」などの表現へ統一した。
- Dropbox内のソースフォルダ一覧を `/MeldexSettings/_meldex/source-folders.v1.json` に保存する共有レジストリを追加した。
- クラウド版で複数のDropboxフォルダをソースフォルダとして扱えるよう、仮想パス、Dropboxフォルダ選択、フォルダツリー表示、設定画面、Storage Adapterを接続した。
- デスクトップ版側にDropboxソースフォルダの端末別ローカル対応付けAPIを追加し、未対応付けのrootは「このPCで場所を確認」状態として保持するようにした。

### fix(review): LLMレビュー改稿反映の409誤検出を修正

- LLMレビューの改稿案作成中に対象ファイルが更新された場合、改稿本文生成前に最新版を読み直して反映できるようにした。
- 最終反映中にさらにファイルが変更された場合は、従来どおり409で停止して上書き事故を防ぐようにした。

検証: JS構文チェック、Python構文チェック、LLMレビュー回帰unittest、ソースフォルダレジストリunittest、Dropbox/クラウド初期設定回帰unittest、ワークスペース/ルーター関連unittest、BETAダイアログ/設定文言のfrontend回帰unittest、バージョンメタデータ回帰unittest。

## v0.5.439 (2026-05-09)

### feat(cloud): クラウド版の初回ホーム追加導線を追加

- クラウド版の初回起動時、Dropbox設定より前に「ホームに追加」モーダルを表示し、ホーム画面のMeldexアイコンからアプリのように起動できることを簡潔に案内するようにした。
- 対応ブラウザではMeldex側のボタンからホーム追加確認を開き、iPhone/iPadなど直接追加できない環境では追加手順と「ホームに追加できたら押す」導線を表示するようにした。
- ホーム追加後に通常ブラウザで開いた場合は「ホーム画面から起動してください」の案内だけを表示し、ホーム画面起動時はDropbox設定へ進むようにした。

### fix(mobile): iOSホーム画面起動時の上部UI被りを修正

- iOSホーム画面起動時に時刻や電池表示がフォルダツリーへ戻るボタンへ重ならないよう、最上段のパネルタブバーだけにセーフエリア余白を追加した。
- フォルダツリー全画面ヘッダー、モバイルサイドドロワー、モーダルも上部/下部セーフエリアを避けるようにした。
- iOS PWAのステータスバー設定を不透明表示に変更し、上部操作領域が見えにくくなるリスクを下げた。

検証: JS構文チェック、クラウド公開準備回帰unittest、クラウドモバイル回帰unittest、モバイルmicro coverage unittest、frontend回帰unittest、クラウド初回ホーム追加のChrome実ブラウザ相当チェック、iOS上部セーフエリア44pxを強制したモバイルmicro E2E、バージョンメタデータ回帰unittest。

## v0.5.438 (2026-05-09)

### fix(sheet): シートが空白のまま開けない回帰を修正

- 古いペイン状態が残っている状態でシートを開くと、存在しないテーブルへ描画しようとして `innerHTML` エラーになる問題を修正した。
- シート描画時に表示先テーブルが見つからない場合は例外で止めず、現在のメイン表示領域へフォールバックするようにした。
- シートを開く、セルを編集する、ビュータブを追加する、追加ビューを削除する実ブラウザE2Eを追加した。

### fix(smart-sheet): スマートシートが空白表示になる回帰を修正

- スマートシートをペイン内タブで開いた時に、シートのビュー種別判定に巻き込まれて空のテーブル表示へ切り替わる問題を修正した。
- スマートシートE2Eを、DOM上に行があるだけでなく画面上に実際に表示されていることまで確認するよう強化した。

検証: JS構文チェック、ペインブリッジ結合構文チェック、E2Eアクション結合構文チェック、E2Eマトリクス生成、frontend回帰unittest、高リスク回帰unittest、通常シート実ブラウザE2E、ビュータブ追加/削除E2E、スマートシート実ブラウザE2E、AI目視スクリーンショット。

## v0.5.437 (2026-05-09)

### fix(app): 読み込み中表示と古いロードの上書きを修正

- シート、スマートシート、フォルダ、ノート、ボード、シナリオ、チャットを開く時に読み込み中表示を出し、大きいデータでは描画前にも待機表示を挟むようにした。
- パネルレイアウトの復元や切替で古いロードが後から完了して現在の表示や空白状態を上書きしないよう、最新ロードだけを有効にした。
- シート読み込み失敗時は空白のままにせず、画面内メッセージとステータスで失敗内容を出すようにした。

### fix(outliner): フォルダツリーで表示が展開状態を壊さないように修正

- 各アプリの「フォルダツリーで表示」で既存ツリーを全再読み込みせず、対象ファイルの親フォルダだけを展開して選択するようにした。
- 相対/絶対パス、ルート名付きパス、`.md` 有無の違いを吸収し、ノート、シナリオ、ボード、シート、カレンダーで対象を表示できるようにした。
- カレンダー表示中は直前のシートではなく、現在表示中のカレンダーの保存先をフォルダツリーで選択するようにした。

### test(e2e): フォルダツリーで表示の実ブラウザ回帰を追加

- ノート、シナリオ、ボード、シート、カレンダーで「フォルダツリーで表示」を実行し、ツリー再読み込みなし、対象選択、親フォルダ展開、既存展開維持を確認するE2Eを追加した。

検証: JS構文チェック、E2Eアクション結合構文チェック、シートツールバー回帰unittest、フォルダツリー回帰unittest、frontend回帰unittest、高リスク回帰unittest、フォルダツリーで表示の実ブラウザE2E、バージョンメタデータ回帰unittest。

## v0.5.436 (2026-05-09)

### fix(ui): ノートとシートの操作UIを整理

- ノートツールバーのノートアイコン変更ボタンを削除し、旧アイコン変更経路を無効化した。
- 公開設定ボタンの globe アイコンが表示されるよう、Lucide置換マップとプリビルドbundleを更新した。
- フォルダツリー更新ボタンを、全文検索ボタン、フィルタ表示ボタンの右へ移動した。
- クラウド初期設定ダイアログも共通モーダルシェルの対象に戻し、ドラッグ/リサイズの回帰ガードに合わせた。

### feat(note): 表の行高と列幅を境界ドラッグで調整

- ノート内の表でセル境界をドラッグして列幅と行高を調整できるようにした。
- 調整した表サイズをMarkdownコメントとして保存し、再読み込み後も復元するようにした。
- ノート表のE2Eで、セル操作、列幅ドラッグ、行高ドラッグ、保存復元を検証するようにした。

### fix(sheet): ビュー追加タブと既定ビュー名を修正

- 新規シートの既定ビュー名を「テーブル」に変更した。
- ビュー追加の「＋」ボタンをビュータブ列の末尾に表示し、クリックでビュー追加メニューが開くようにした。
- シートE2Eで、ビュー追加ボタンの配置とメニュー表示を検証するようにした。

検証: JS構文チェック、split assets回帰unittest、フォルダツリー回帰unittest、frontend回帰unittest、ノート/シート関連frontend回帰unittest、ノートmicro E2E、シートmicro E2E、バージョンメタデータ回帰unittest。

## v0.5.435 (2026-05-09)

### fix(cloud): Dropbox初回設定の用語とアカウント切替を整理

- Dropbox連携設定の初見向け文言を「保存先フォルダ」「Dropboxへの接続」「デスクトップ版」に統一し、旧来の `mountパス`、`refresh token`、`PC単独モードへ戻す` などの内部寄り表現をユーザー向け画面から外した。
- 「接続を解除」は端末内の接続情報だけを消す操作として説明し、別アカウントに変える場合は「別のアカウントで接続」から Dropbox 側でアカウント選択をやり直せるようにした。
- 自分で登録した Dropbox OAuth アプリ向けの redirect URI と必要権限は、独自アプリ設定を選んだ場合だけ表示するようにした。

### fix(mobile): スマホ版フォルダ画面をNotion風に整理

- スマホ/タブレットのフォルダ画面で左端の縦アイコン列、右側アクティビティバー、大きな青いメニューFABが重複して出ないようにし、上部のフォルダ名、件数、三点メニューへ整理した。
- フォルダ内アイテムはスマホ向けの行リスト表示に固定し、再読み込み、表示設定、検索、オプションなどの操作は三点メニューから開けるようにした。
- モバイルE2Eの起動待ち条件を、左クローム非表示の新しいスマホUIでも安定するように更新した。

### fix(mobile): エラー通知が操作を塞がないように修正

- スマホ幅では赤いエラー通知を画面上部のコンパクト表示に変更し、閉じるボタンと自動消去を追加した。
- 「詳細」「報告画面を開く」を押した時は通知を閉じてからサポートダイアログを開き、下部ボタン操作を塞ぎ続けないようにした。
- サポートダイアログの最小幅をスマホ画面内に収まるよう調整した。

### docs(samples): 公開マニュアルとサンプルリンクを最新UIに追従

- Dropbox共有モード、クラウド初期セットアップ、スマホ/タブレット利用、マルチユーザー運用、設定、サポート系マニュアルの旧用語を最新UIに合わせて更新した。
- 死霊探偵サンプル企画書からキャラ表、場所情報、アイテム表への内部リンクを追加し、リンク先ファイルが存在することを確認した。

検証: 関連JS構文チェック、Dropbox/スマホ/エラー通知回帰unittest、フォルダツリー回帰unittest、バージョンメタデータ回帰unittest、Cloud静的ビルドチェック、スマホ実ブラウザE2E、スマホ幅AI目視スクリーンショット。

## v0.5.434 (2026-05-08)

### fix(startup): スプラッシュ停止の回帰を修正

- 起動時に復元された画像サムネイルのローカルURL補正が同じURLを何度も書き戻す問題を抑止し、スプラッシュ画面から進まない回帰を修正した。
- 起動直後の復元描画で一度UIへ制御を戻し、重い復元状態でもクリック可能になるまでの固まりを減らした。
- シートセルのイベント発火元がDOM要素ではない場合のガードを追加し、起動直後や仮想化描画中の操作で例外が出ないようにした。

### feat(cloud): Cloud初回起動時にサンプルをホームへ自動配置

- 公開用に除外済みのサンプルデータをCloud静的ビルドへ同梱し、Dropbox接続後に `MeldexHome/サンプル` へ不足ファイルだけ配置するようにした。
- スマホ/タブレットのクラウド版でも、ホームフォルダからサンプル内のノート、シナリオ、シート、ボードを開いて編集できるようにした。
- 既存のサンプルファイルは上書きせず、途中で閉じても次回起動時に不足分だけ再開するようにした。
- Cloud起動時のアバター表示をローカルAPIへフォールバックしないようにし、未認証のスマホ/タブレット起動で不要な内部エラー通知が出ないようにした。
- Cloud静的ビルドでは分割JSを事前結合して配布し、公開URLのモバイル起動時に同期chunk読み込みが中断されて初期化が止まる問題を防いだ。
- Cloud版の `/version` をビルド時の `version.json` と揃え、公開URLのタイトルが `v0.0.0` にならないようにした。

### fix(board): ボード循環参照の描画破綻を防止

- ボードカードの親子関係に循環が混入しても、レイアウト計算、レンダリング、インデックス再構築で無限ループしないようにした。

検証: 関連JS/Python構文チェック、Cloud公開ビルド/公開準備回帰テスト、起動回帰テスト、バージョンメタデータ回帰テスト、スマホ/タブレットCloud実ブラウザAI目視、実機起動AI目視、公開URL疎通確認。

## v0.5.433 (2026-05-08)

### feat(sheet): シート値の書式保存をカード表示まで拡張

- テキスト型セルの選択範囲へ設定した文字色、背景色、太字、斜体、下線、フォント、文字フチなどを安全な `rich_html` として保存し、セル再編集時にも復元するようにした。
- 保存済みの書式をテーブルセルだけでなく、ギャラリー、カンバン、タイムラインのカード内プロパティ表示にも反映するようにした。
- プレーン編集や値の差し替えでは古い書式を破棄し、テキストと保存HTMLが一致しない場合はHTMLを表示に使わないようにした。

### feat(pwa): ホーム画面追加時のMeldexアイコン対応を明文化

- Cloud/PWA用の Apple touch icon と maskable icon をビルド対象と公開静的配布へ含め、クラウド版をホーム画面に追加した場合にMeldexアイコンとアプリ名で表示されるようにした。
- 公開トップ、配布ページ、スマホ/タブレット利用マニュアル、クラウド初期セットアップ、PRページを最新仕様へ更新した。

### feat(llm): 生成物へ現在テーマの色方針を渡す

- 通常チャットとCLIチャットのリクエストに現在テーマID、テーマ名、パレット、OSアクセントを除いたテーマ共通色を含め、LLM/CLI生成物がMeldexの現在テーマカラーを主に使うようにした。
- ボード生成ガイドとスキーマで、接続線の `label` は既定で空文字にし、本当に必要な説明だけを書くルールへ更新した。
- LLM向けのボード/共通スキーマ、生成テンプレート、機能マニフェストを最新のテーマカラー・線ラベル方針に合わせた。

### fix(board): ボードMarkdownがノート化される保存経路を遮断

- 既存ファイルまたは `.board.md` がボードMarkdownである場合、ノート形式のMarkdownやバイナリ内容で上書きしようとした保存を `board_type_downgrade` として拒否するようにした。
- ノート表示経路でボードMarkdownを開こうとした場合は、ページ状態を作る前にボード表示へリダイレクトし、ノート自動保存がボードのフロントマターを壊さないようにした。
- Cloud/PWA のファイル分類と保存経路でも `type: board` を優先判定し、ブラウズ時の誤分類とクラウド保存時の種別消失を防止した。

### feat(outliner): フォルダツリー更新ボタンを追加

- ソースフォルダ見出しとスマホ/タブレット用フォルダツリーヘッダーに更新ボタンを追加し、ソースフォルダ、お気に入り、ホームフォルダをまとめて再読み込みできるようにした。
- 更新中はボタンを一時的に無効化し、完了時にステータスバーへ更新完了を表示するようにした。

### feat(samples): サンプルデータを本体と別配布に分離

- デスクトップ版のHomeテンプレートから `サンプル` フォルダを除外し、サンプル作品は専用ZIPとして生成・公開できるようにした。
- 公開トップ、初回案内、設定画面、マニュアルにサンプルダウンロードと取り込み手順への導線を追加した。
- 相対リンクが既定ソースで見つからない場合、登録済みソースフォルダ全体から既存ファイルを探すようにし、別配布サンプル内のリンクを開きやすくした。

### test(e2e): カレンダー追加ボタンの識別子を安定化

- 実ブラウザスモークの可視UI検証でカレンダーのセル追加ボタンを追跡できるよう、月/週/日セルの追加ボタンに安定した `data-e2e-id` を付与した。

検証: シート書式保存の関連JS/Python構文チェック、frontend/API回帰テスト、シートmicro E2E、モバイルmicro E2E、公開/PWA/バージョンメタデータ回帰テスト、LLMテーマ文脈回帰テスト、ボード種別保護API/チャット/Cloud分類回帰テスト、フォルダツリーChrome回帰テスト、サンプル分離/登録済みルートパス解決回帰テスト、実ブラウザ smoke-core E2E。

## v0.5.432 (2026-05-08)

### fix(shell): ユーティリティペインのアクティブ奪取を抑止

- チャット、注釈、履歴、詳細、プレビューなどの補助ペインをパッシブ扱いにし、クリックや復元後の再描画でシート/ノートなどの作業ペインからアクティブ状態を奪わないようにした。
- タブ切替、パネルセット、折りたたみドックの展開経路に `preserveActivePane` を通し、チャットが開いた状態で再起動しても作業ペイン側のクリック操作が維持されるようにした。
- チャットのリッチ入力欄をフォーカス可能にし、シートセルクリック後にフォーカスがチャットへ戻る回帰をE2Eで検出できるようにした。

### fix(mobile): スマホ/タブレットのペイン表示を安定化

- スマホ単一ペインでは補助ツールを非表示パッシブ扱いにせず、チャットタブへ切り替えたあとシートへ戻れるようにした。
- タブレット幅で折りたたみ左ドックが縦積み分割の上半分を占有する問題を修正し、起動直後からシートとチャットが画面上端から使えるようにした。
- クイックスタート文書の存在確認で404が返った場合はサイレント扱いにし、起動直後に不要なエラー通知を出さないようにした。

検証: 関連JS構文チェック、passive tool pane/startup/dock/frontend/cloud mobile回帰テスト、Chrome実機E2E（desktop/smartphone/tablet）、3サイズスクリーンショットAI目視。

## v0.5.431 (2026-05-08)

### feat(llm): LLMレビュー改稿案を本文へ直接反映

- LLMクロスレビューの「改稿案作成」ボタンを下部ボタンバーに常設し、レビュー結果の採用選択前や保存しない設定では無効化するようにした。
- 採用済み指摘から改稿案を生成したうえで対象本文へ直接反映し、反映前後を自動バージョンとして保存するようにした。
- 反映後に対象ファイル表示とバージョン表示を更新し、バージョン管理から修正前後を比較できるようにした。

### fix(llm): LLMレビュー本文反映の安全性を強化

- 本文反映時に保護ファイル、編集ロック、書き込み権限を確認し、処理中に対象ファイルが変更された場合は上書きせず中止するようにした。
- 反映後にFTS、バックリンク、編集履歴、レビュー結果JSONを更新するようにした。

検証: LLMクロスレビューfrontend/API/router回帰テスト、Python/JS構文チェック、version metadata test、`git diff --check`。

## v0.5.430 (2026-05-08)

### feat(sheet): タイムライン条件付きカラーを正式機能化

- タイムラインカード/期間バーの非公式な `色` / `color` プロパティ参照を廃止し、ビューごとの条件付きカラー設定として「この列/行を色付け」「この時間を色付け」を追加した。
- タイムラインの一行目ヘッダーと一列目ヘッダーにメニューを追加し、色付け、並び替え、列幅指定、自動調整、色解除を実行できるようにした。
- 行/列全体に色を付けた場合は対応するヘッダーにも色を適用し、本文より少し暗い色で表示するようにした。

### fix(sheet): タイムライン列幅とセル編集を安定化

- タイムラインの列幅ドラッグ保存時に最新設定を基準にし、別列の列幅変更で直前の変更が戻る問題を修正した。
- タイムライン列幅の自動調整を通常シート寄りの幅算出に揃え、境界線ダブルクリックも同じ自動調整経路を使うようにした。
- タイムラインの列間/行間に余白を追加し、カードや期間バー内の表示プロパティ値をクリックして直接編集できるようにした。

検証: 関連JS構文チェック、タイムライン/シート表示回帰テスト、Batch38/Batch39 frontend regression、Chrome実機E2E（sheet micro、database deep views）、`git diff --check`。

## v0.5.429 (2026-05-08)

### fix(startup): 起動直後のフォルダ操作不能を修正

- デスクトップ起動時に保存済みフォルダタブを遅延・省略する暫定対策を取り消し、起動直後からフォルダ内容を復元するように戻した。
- 起動時のアクティブパネル解決で、非表示パネルやカレンダー/チャットなどのユーティリティパネルではなく、表示中の作業パネルを優先するようにした。
- フォルダカード描画では文書/PSD/CLIP/3D系の Windows シェルサムネイルを一括要求せず、安定したファイル種別アイコンで表示するようにした。画像/動画の通常プレビューは維持した。
- ウォーターフォール表示のリサイズ監視が自分自身の高さ変更で再レイアウトを誘発し続けないようにし、カレンダーパネルの多重初期化も抑止した。

検証: 関連JS構文チェック、フォルダ/起動パネル/カレンダー/分割ロード回帰テスト、`git diff --check`、Chrome実機起動でCPU張り付きなし・フォルダ15件表示・パネル操作可能・スクリーンショットAI目視。

## v0.5.428 (2026-05-08)

### fix(subpanel): サブパネル表示とモバイル委譲を安定化

- シートやチャットリンクからエントリをサブパネル表示する経路を、通常画面の `#entity-view` / `state.currentEntityPath` / `selectEntity` に依存しない専用描画へ分離した。
- チャット入力欄にフォーカスがある状態でシートのエントリ名をクリックしても、作業ペイン側のアクティブ状態とサブパネル内容が失われないようにした。
- レイアウト再描画後もエントリサブパネルの内容が空にならないよう、サブパネル専用rootへの再描画経路とE2E検証を追加した。
- スマホ/タブレット幅のクラウドモバイルでは、サブパネルを浮動ウィンドウではなくモバイル用サイドドロワーへ委譲し、エントリと通常リンクの両方を同じ導線で確認できるようにした。

### feat(llm): LLMクロスレビュー対象選択を追加

- LLMクロスレビューの実行ダイアログで、現在表示中の各パネルのアクティブタブを対象ファイル候補として選択できるようにした。
- 選択した対象を `target_path` と `active_feature` に反映し、対象未選択時の実行禁止と既存のレビュー実行経路を維持した。

検証: サブパネル/シートmicro E2E、クラウドモバイルmicro E2Eのスマホ幅/タブレット幅、LLMクロスレビューfrontend/APIテスト、`app/tests` 全体 discovery 987件、tracked script全体のPython/JS構文チェック、`git diff --check`。

## v0.5.427 (2026-05-08)

### feat(llm): LLMクロスレビューを実LLM/CLI実行へ接続

- LLMクロスレビューの実行エンジンに Codex CLI / Claude Code / Gemini CLI / OpenAI API / Gemini API / Claude API を選べるようにし、選択したエンジンで各レビュアーの実レビューを実行できるようにした。
- 改稿案生成も採用済み指摘だけを対象に実LLM/CLIへ接続し、採用済み指摘がない場合は外部呼び出しを行わず空の改稿案を返すようにした。
- 対象ファイル未選択時はフロントエンドとAPIの両方でクロスレビュー実行を禁止し、誤って空対象のレビューランを作らないようにした。
- `source_refs` 欠落警告が同じ指摘に重複表示される問題を修正し、ユーザーが選択した読者体験レビュアーがテンプレート初期値に引きずられて実行されない問題も修正した。
- 直接API実行では既存チャット予算の事前チェックと使用量記録を通し、OpenAI APIでは設定ファイルの選択モデルを優先しつつ、モデル差による最大トークン指定名の違いを吸収して再試行するようにした。モック以外の空応答は内蔵テスト指摘へ置き換えず、実行失敗として扱うようにした。

### fix(chat/ui): チャットリンク、ツールチップ、ボードUIの副作用を修正

- チャット内のMeldexリンクを、既存の作業ペインを優先してメイン作業領域へ開くようにし、作業ペインがない場合は新しい作業ペインを作成して開くようにした。
- フォルダツリーなどで名前を繰り返すだけの冗長なツールチップを抑制し、表示幅から溢れる場合や明示的な説明がある場合だけ表示するようにした。
- 作品フォルダスコープ未指定のチャットツールで、ホームフォルダ参照とコードの読み取り専用参照が失われないようにした。作品フォルダ指定時の検索・閲覧範囲制限は維持した。
- ボードの構造/変形設定UIを整理し、階層別スタイル参照の正規化、範囲入力のラベル、CSS分割上限、監査マニフェストを現在のUIに合わせて更新した。
- ボード編集を行うLLM/CLI向けに、カード間の線を意味関係として扱う共通ガイダンスを通常チャットとCLIチャットの両方へ追加した。

検証: LLMクロスレビュー単体/API/frontendテスト、CLIレビュー実行経路テスト、直接OpenAI実行経路テスト、チャットスコープ/CLI/ツールチップ/バージョン/分割資産/操作監査テスト、`app/tests` 全体 discovery 984件、Python/JS構文チェック、`git diff --check`。

## v0.5.426 (2026-05-08)

### feat(llm): LLMクロスレビューとeditor-learningを追加

- チャットパネルから「LLMクロスレビュー」を開き、複数レビュアー観点で作品フォルダ内の対象ファイルをレビューできるようにした。
- レビュー結果はレビュアーごとに独立生成し、統合された指摘一覧、重要度、根拠参照、優先指示違反、範囲外判定を確認できるようにした。
- 指摘の採用/却下/保留を記録し、採用した傾向を `editor-learning` に反映できるようにした。範囲外指摘や正典衝突など、学習に混ぜるべきでない指摘は既定で学習対象外にした。
- レビュー実行、履歴取得、指摘状態更新、editor-learning参照、修正方針生成のAPIを追加し、Meldex本体のルーターへ接続した。
- LLMクロスレビュー用の計画書とテストを追加し、AGENTSの全ファイル全行チェック対象にも関連ファイルを追加した。

### fix(layout): チャットアクティブ再起動時の作業パネル復元を修正

- チャットなどのユーティリティパネルがアクティブな状態でハードリロードしても、中央の作業パネルが消えず、復元レイアウト内の作業コンテンツを起動時のアクティブ候補に戻すようにした。
- 保存済みレイアウトがユーティリティタブだけの場合は、旧来の最終ビュー復元へ誤って流れないようにし、作業タブを含むレイアウトだけを「復元済み」と判定するようにした。

### fix(sheet): シート選択・ツールバー・注釈管理の文脈保持を強化

- チャット入力欄がアクティブな状態からシートセルをクリックしても、クリック元のシートパネルを即時アクティブ化し、チャットへフォーカスが戻らないようにした。
- 再起動直後やチャット/注釈/履歴などのユーティリティパネル操作中も、表示中のシートを基準にシートツールバーを復元・保持するようにした。
- 注釈管理パネルの「現在のファイル」スコープを、最後に有効だった作業コンテンツから解決し、注釈パネル自身やチャット操作で対象が失われないようにした。
- 注釈一覧の絞り込みで `target_path` と `target_ref.file` の両方を照合し、保存経路の違いで既存注釈が空表示になる問題を修正した。
- 注釈が0件のときは、現在対象、スコープ、状態フィルタ、全件表示への導線を表示し、対象未解決やフィルタ要因を判断できるようにした。

検証: LLMクロスレビュー単体/API/frontendテスト、起動時パネル復元テスト、シート/注釈micro E2E、frontend regression、Batch24 API regression、構文チェック、`git diff --check`。

## v0.5.425 (2026-05-07)

### fix(chat): 応答待ち中の追加入力と日本語IMEを安定化

- チャット/CLIチャットの応答待ち中に新しいメッセージを送っても、実行中のLLM/CLI作業を停止せず、ユーザー発言だけを発言欄へ追加して応答完了後にまとめて次ターンへ送るようにした。
- 停止は送信ボタンなど明示的な停止要求時のみ行い、通常のEnter送信ではストリームを中断しないようにした。
- 日本語IME変換中のEnterは送信ではなく変換確定として扱い、`・` などの入力確定で誤送信されないようにした。
- 保留メッセージはセッション/対象パスが変わっていない場合だけ自動送信し、セッション切替後に別文脈へ混ざらないようにした。

### fix(layout): ユーティリティパネル操作時のシートツールバー保持を修正

- シート表示中にバージョン履歴、タイマー、チャット、カレンダー、履歴、注釈、検索、ビューワー、オプションなどをクリックしても、表示中の作業パネルを基準にシートツールバーを表示し続けるようにした。
- 再起動直後に右側のユーティリティパネルがアクティブでも、復元されたシートのツールバーを表示できるよう、ツールバー判定をアクティブパネルではなく表示中コンテンツから解決するようにした。
- エントリ自由記述ツールバーも同じ解決済みコンテキストを見るようにし、他パネルクリックで不意に消えないようにした。

検証: チャット/CLIチャット/パネルブリッジのJS構文チェック、frontend regression、split asset regression、`git diff --check`。

## v0.5.424 (2026-05-07)

### docs(release): 配布・マニュアル説明を最新仕様へ同期

- Meldexベータ版配布ページを、GitHub Pages公開トップ、GitHub Releasesのデスクトップ版、クラウド版を並列に選ぶ説明へ更新した。
- デスクトップ版（ローカル / Dropbox上）とクラウド版のメリット・注意点・速度目安・端末/OS別対応状況を、一般ユーザー向けの表現に揃えた。
- 初回セットアップ、BETA同意、ホーム画面に追加、設定ダイアログ全般タブの説明を現行UIに合わせた。
- 初回同意、利用規約、プライバシーポリシーから、現行公開範囲では不要な年齢推奨文を削除した。
- シートマニュアルに、検索/置換、折り返し、列幅自動調整、タイムラインカード表示、ヘッダーD&D、アクティブセルのスクロール追従を反映した。
- フォルダツリーマニュアルに、ソースフォルダ/ホームフォルダ見出しクリックと矩形選択（Ctrl / Shift対応）を反映した。
- サポート系マニュアルの配布/連絡先表現を、Notion主導ではなく公開トップ / GitHub Releases / GitHub Issues前提へ更新した。
- デスクトップ配布物のリリースノートと同梱 `version.json` を、GitHub Pages / GitHub Releases 前提かつ exe ビルドとして判別できる内容へ更新した。
- 配布ビルド時に古い削除済みファイルや開発版ホーム直下のローカル作業ファイルが残らないよう、配布フォルダを安全確認付きでクリーンし、ホームテンプレートのコピー対象を絞った。

### fix(sheet): シートツールバーとキーボード移動を安定化

- シート表示中にチャット、ビューワー、オプションなど他パネルをクリックした時、表示中のシートビューがある場合はシートツールバーの状態を保持し、フォルダ等のツールバーへ一瞬切り替わって消える問題を修正した。
- シートのアクティブセルを矢印キーで移動した時、固定ヘッダーと固定エントリ列を考慮してスクロールが追従し、アクティブセルが表示範囲外に消えないようにした。
- シートツールバーの「検索/置換」ボタンがフォルダツリー検索ではなく、シート内検索モーダルを開くように修正した。ノート、ボード、フォルダなど他ツールバーの検索/置換ルートも回帰テストで固定した。
- シートツールバーの折り返し、列幅自動調整、ビュー追加が、アクティブペインではなくクリック元のシートペインを基準に動作するようにした。
- タイムラインビューでも折り返し/切り詰め表示と列幅自動調整を利用できるようにし、カード/期間バー/カード表示プロパティの表示に反映されるようにした。
- シート micro E2E に、矢印キーでセルを縦横移動した時にアクティブセルがスクロール表示範囲内に残るケースを追加した。

検証: `gb-db-inline-edit` / `gb-pane-bridge` 結合構文チェック、`gb-e2e-actions-sheet-micro.js` 構文チェック、シートツールバー/ツールバー/Batch38/Batch39/frontend regression、micro coverage、シート micro E2E。

### feat(sheet): タイムラインビューのヘッダー並べ替えを追加

- タイムラインビューで、行/列軸に使っているヘッダーをドラッグ&ドロップで並べ替えられるようにした。時間軸そのものはタイムラインとしての時系列順を維持する。
- タイムラインカード表示メニューに「エントリ名」を追加し、カード/期間バー上のエントリ名表示を切り替えられるようにした。
- シートツールバーの折り返しボタンと列幅自動調整ボタンを、ビュー追加/テンプレート/シート設定ボタン群の右側へ移動した。
- タイムラインヘッダーD&D処理とE2Eタイムライン操作を分割ファイルへ切り出し、既存ファイルが肥大化しないようにした。

検証: タイムライン/シートツールバー関連JS構文チェック、関連frontend regression、API smokeのDBスクリプト順序確認、タイムラインD&Dを含む実ブラウザE2E。

### feat(pwa): ホーム画面追加導線を追加

- 初回起動時のBETA同意後に「ホーム画面に追加」単体モーダルを表示し、対応ブラウザではMeldex内のボタンからPWA追加確認を開けるようにした。
- 設定ダイアログの全般タブにも同じ導線を追加し、追加済み/直接追加可能/手順案内が必要な状態を表示するようにした。
- iPhone/iPadなどブラウザが直接追加画面を開けない環境では、同じボタンから手順案内ダイアログを表示するようにした。
- 初回同意モーダル内から追加導線を分離し、通常デスクトップ表示でスクロールバーが出ない高さへ戻した。無効状態の「同意して開始」ボタンも文字が読める配色に調整した。

検証: BETA公開導線のfrontend regression、関連JS構文チェック。

### feat(cloud): GitHub Pages公開トップを追加

- Cloud静的成果物のルートに出す公開トップページを `app/public-index.html` として追加し、ビルド時に `index.html` として出力するようにした。
- 公開トップを利用環境に合わせてデスクトップ版とクラウド版を並列に選べる構成へ変更し、ダウンロード対象バージョンと過去版選択の導線を追加した。
- 公開トップのファーストビューからビューポート基準の大きな余白を外し、ダウンロード欄まで自然に続く密度へ調整した。
- 公開トップの紹介文を、機能名の列挙ではなく「漫画・小説・シナリオ・企画などの創作を、資料整理から構成・執筆・管理まで一つにまとめる創作支援ワークスペース」として一般ユーザー向けに整理した。
- 公開トップ全体から「デスクトップ版が基本/推奨でクラウド版は補助」という含みに読める表現を外し、各環境に合わせて選ぶ説明へ揃えた。
- 公開トップのクラウド版説明から開発/運用者向けの表現を減らし、一般ユーザー向けに「最新版を起動する」説明と「クラウド版を起動」ボタンを追加した。
- 利用規約とプライバシーポリシーを公開トップと同じデザインのHTMLページでも表示できるようにし、公開トップ・初回同意・Aboutの導線をMarkdown直リンクからHTMLへ変更した。
- 利用規約とプライバシーポリシーのHTML版は、配布ページ風のヒーロー/カード構成ではなく、文書向けの左目次・本文幅・要点・条項レイアウトに変更した。
- 利用規約とプライバシーポリシーのロゴ/配布ページリンクを `public-index.html` に変更し、開発元フォルダで直接開いた場合も配布ページへ戻れるようにした。
- 公開トップにデスクトップ版ダウンロード導線を追加し、GitHub Releasesまたは環境変数で指定した直接ZIP URLへ誘導できるようにした。
- クラウド版、デスクトップ版（ソースフォルダ: Dropbox上）、デスクトップ版（ソースフォルダ: ローカル）のメリット/デメリットと速度比較を公開トップへ追加した。
- 公開トップの対応範囲に、Windows PC、Mac/Linux/Chromebook、iPad/タブレット、スマホでのデスクトップ版/クラウド版対応状況を簡潔に追加した。
- `robots.txt` と `meta robots noindex` を追加し、GitHub Pages公開トップとCloud本体の検索避けを入れた。
- Notion公開ページを主配布導線にしない方針へ、配布チャネル手順、外部運用チェックリスト、公開準備計画を更新した。

検証: Cloud公開準備unittest、Cloud静的ビルド/check-only、関連HTML/robots静的確認。

### fix(beta): 初回同意モーダルのチェックボックス視認性を改善

- Meldex BETA についてダイアログのチェックボックスを未チェック状態でも枠が見えるカスタム表示に変更した。
- 初回同意モーダルから年齢推奨の説明文を削除し、現行公開範囲では不要な年齢推奨文を出さない方針に整理した。

検証: BETA公開導線のfrontend regression、関連JS/CSS静的確認。

### fix(cloud): 公開URL起動時の旧PC単独モード残留を補正

- GitHub Pages 版の公開URLを開いた時、過去の `legacy`（PC単独モード）設定がブラウザに残っていても、明示指定がない限り Cloud / Dropbox モードとして起動するようにした。
- 公開ホストで起動直後に `/api/ui-config` などのローカルAPI 404 が「項目が見つかりません」トーストとして出る問題を抑止した。
- 起動モードを `dropbox` に確定してから初期セットアップへ進めるようにし、他の初期化処理が古いモードを参照しないようにした。

検証: Cloud公開準備unittest、静的Cloudビルド、公開ビルド法務チェック、GitHub Pages上の公開JS反映確認、古い `legacy` localStorage を残した実ブラウザ再現確認。

### fix(settings): AI API使用量タブの表記を明確化

- 設定ダイアログの旧「利用料金」タブ表示を「AI使用量」に変更し、Meldex本体の料金ではなく登録済みAI APIキーの推定使用量であることを明示した。
- 使用量セクション、読み込み中/エラー表示、保存完了/失敗通知、設定ダイアログ再設計サンプルの表記を「AI使用量」「AI API使用量」「AI API単価表レビュー日」に統一した。
- 既存リンクや旧内部指定から開けなくならないよう、「利用料金」「AI料金」「LLMコスト管理」などの旧名称は互換エイリアスとして維持した。
- 関連マニュアルとヘルプボット用FAQ/メタデータも「AI使用量と予算管理」へ更新した。

検証: 設定ダイアログ表記の静的検索、`gb-settings.js` 結合構文チェック、設定/AI使用量関連unittest、frontend regression。

### fix(sheet): シート作成・エントリ追加・タイムライン選択を安定化

- フォルダツリーの「ソースフォルダ」「ホームフォルダ」見出しクリックで、該当フォルダをフォルダパネルに開けるようにした。
- パネルの＋ボタンから作成したシートがホームフォルダ内でフォルダ表示になる問題を修正し、`settings-db` / `calendar-db` のフォルダ種別を `/browse` で正しく返すようにした。
- 通常シートでも「ビュータイプを変更」とビュー追加メニューからタイムラインビューを選べるようにした。
- エントリ追加時の同名409を「ほかの変更とぶつかりました」と誤表示しないよう、同名エラーと保存競合を分離した。
- 新形式シートではバックエンド側の自動初期値適用をフロント側が再実行しないようにし、`autoFillOnCreate` の二重書き込みによる追加失敗/誤通知を抑止した。
- `無題` エントリ作成時に既存ファイルと衝突した場合は `無題2` 以降へ自動再試行するようにした。

検証: シート作成/エントリ追加API回帰テスト、フォルダツリーChrome回帰テスト、関連frontend regression、データ損失防止テスト、シート/パネルE2E 3件、関連JS結合構文チェック、関連Python `py_compile`。

### feat(app): 大きいファイルの読み込み中表示を追加

- 大きいノート、シート、ボード、シナリオ、CSV、HTML/画像/動画/音声ビューワー系ファイルの読み込み/描画中に、画面上へ読み込み中インジケータを表示するようにした。
- ロード要求の世代管理を確認し、古い読み込み完了が新しい表示を上書きしないようにした。

検証: 重いファイル読み込み表示のfrontend regression、対象ローダーのJS構文チェック、シート/パネルE2E 3件。

### fix(theme): ファイル単位のカスタムテーマをファイル内へ完全保存

- オプションパネルのテーマタブでテーマ、配色、書式項目のいずれかを変更した時、現在の有効テーマを「カスタムテーマ」としてファイル内 `style:` / シナリオJSONへ複製してから変更を保存するようにした。
- ファイル内カスタムテーマは `__fileCustomTheme` と全テーマ変数・テーマパレットを持つため、設定ダイアログ側のテーマ変更やグローバルカスタムテーマ更新の影響を受けないようにした。
- ノート、シート、ボード、シナリオ、カレンダーのテーマタブ保存経路と、ボードMarkdown/ノートフロントマターの読み書きでカスタムテーマメタ情報を保持するようにした。
- ボード背景色/背景画像はファイル内カスタムテーマの `--bd-*` 値を優先し、アプリ全体テーマの背景適用で上書きされないようにした。
- フォルダツリー削除は、ゴミ箱移動が成功した項目から順にツリー表示を消すようにし、複数削除中に未完了項目まで隠す挙動と、全件完了まで何も消えない挙動を避けた。

検証: テーマタブ共有回帰テスト、ファイル内カスタムテーマ昇格/フロントマター往復テスト、フォルダツリー削除回帰テスト、関連JS結合構文チェック。

### fix(settings): 設定保存完了通知と初期テーマ配色を調整

- 設定ダイアログ保存時は先に設定モーダルを閉じてから「設定を保存しました」を明示表示し、フォルダツリー再読み込みなどの後処理で完了通知が遅れないようにした。
- 初期搭載4テーマのUI配色をAI目視監査し、共通ボタン、折りたたみバー、履歴/検索/タイマーなどのテーマ変数が読みやすいコントラストになるように調整した。
- 設定テーマのプレビュー復元、追加テーマカラー枠、書式ポップアップ、保存後の不要な再描画抑制に回帰テストを追加した。

検証: 設定保存フローのNodeシミュレーション、初期テーマ配色監査テスト、関連frontend regression。

### fix(security): ローカルメディアAPIの外部サイト直読みを遮断

- 配布版ローカルAPIトークンの例外として残している `/api/file-raw`、`/api/thumbnail`、アバター画像APIについて、Fetch Metadata / Origin / Referer が外部サイト文脈を示す場合はトークンなしアクセスを拒否するようにした。
- アプリ内の同一オリジン画像/動画表示と、ローカルトークン付きAPI呼び出し、公開フォーム送信の例外は維持した。
- 外部Webページからの画像/メディア埋め込みによるローカルファイル直参照を防ぐ回帰テストを追加した。

検証: `test_meldex_security_critical.py` 全件、ローカルAPI境界の追加回帰テスト、関連APIモジュールの `py_compile`。

### feat(sheet): 連載プロット向けシートテンプレートを拡充

- 技法ナレッジと原文ソースの構成観点を反映し、キャラ、伏線、アーク、エピソード、世界設定、舞台、組織、能力、事件などの初期テンプレートに必要なプロパティを追加した。
- プロパティ型を見直し、分類/状態/重要度はセレクト、数値管理項目は数値、URL/タグ/チェック項目/複数参照は専用型へ整理した。
- キャラと舞台の「出身地/出身キャラ」を含め、主要テンプレート間の相互リレーションをテンプレート定義に組み込んだ。
- テンプレート適用時に同階層の対象シートパスへリレーション先を解決し、プレビューにも接続先と逆方向列名を表示するようにした。

検証: JS構文チェック、テンプレート全件のプロパティ/列/リレーション整合Node検査、テンプレート適用unittest、テンプレート関連frontend regression。

## v0.5.423 (2026-05-07)

### fix(board): ボード形状と単独カード初期構造を整理

- 新規ボードの初期カード、ボード空白ダブルクリック、カード追加ツールで作る単独カードの構造初期値を「ロジック図」に変更した。
- 子カードは従来通り親の構造に従うままにし、リンクカードや画像カードなどの非通常カードには単独カード向け初期構造を適用しないようにした。
- ボードのカード形状選択肢と初期搭載カード/階層別スタイルからダイヤ、六角、星を削除し、旧スタイルIDや旧shapeはv10移行で現行形状へ正規化するようにした。
- ボード詳細パネルに「スタイル一式を書き出し」を追加し、カードスタイル、ラインスタイル、階層別スタイルをJSON外部ファイルとして保存できるようにした。

### fix(settings): テーマ設定のUI反映先を補正

- テーマタブの書式設定「ボタン選択」が `--ui-accent` と `--accent` の両方へ反映されるようにし、実UI側の選択ボタン色が更新されるようにした。
- テーマカラー自動適用の「折りたたみバーのボタン」が折りたたみアイコン、展開ボタン、ドックアイコンへ反映されるようにセレクタを補正した。
- テーマ関連のターゲットID別名、書式設定の保存先、split bundle同期を確認し、回帰テストを追加した。

検証: split構成の結合JS構文チェック、`test_meldex_board_free_line_regressions.py`、`test_meldex_frontend_regressions.py`、`test_meldex_micro_coverage.py`、`test_meldex_operation_audit.py`、`test_meldex_high_risk_coverage.py`、`test_meldex_batch47_frontend_regressions.py`、Chromeボード微細E2E 5件。

## v0.5.422 (2026-05-06)

### feat(cloud): Meldex Cloud BETAの対応範囲を拡張

- Dropboxモードでシート、カレンダー、スマートシートを作成・起動できるようにし、主要なシート/カレンダー/スマートシートAPIをCloud保存へ接続した。
- Cloud版チャットのブラウザ直送ストリーム、モデル/使用量API、クライアントツール結果APIを整理し、ローカルCLI/MCP依存なしで使える範囲を明確にした。
- LLM APIキーをAES-GCMで暗号化してCloud保存/復号/削除できるBETA機能を追加し、作成・更新・削除は管理者のみ許可するようにした。
- 配布ページ、マニュアル、公開チェックリスト、Cloud静的ビルド検査をMeldex Cloud BETAの新しい対応範囲へ更新した。

### fix(settings): Notionフォルダ選択と料金表示を調整

- Notion同期フォルダの「選択」をOSダイアログではなくMeldex内フォルダツリーで選ぶ動作に変更した。
- 設定ダイアログ内の小型selectの高さと行揃えを補正し、Notionの自動同期/同期方向ラベルと選択中テキストが崩れないようにした。
- LLM使用量とコスト管理にドル表示と併記する概算円表示を追加した。
- 配布ページ/マニュアルにLLM生成・未確認箇所があり得る旨を追記し、設定ダイアログ再設計サンプルHTMLを追加した。

### fix(board): D&D直後のリンクカード誤オープンとカード内包を修正

- リンクカードをドラッグ移動した直後に発火する click をドラッグ完了後の短時間だけ抑止し、サブパネルが誤って開かないようにした。
- カードを別カードへドロップする時、選択枠やカード内テキストが hit test を遮っても下のカードを再判定し、ドロップ先をコンテナ化して内包できるようにした。
- ボード micro E2E にリンクカードD&D後の誤オープン抑止とカード内包の回帰ケースを追加した。

### fix(sheet): ビュー統合後の周辺出力を整理

- LLM/CLI のフォームビュー設定出力を `typeSpecific.form.formConfig` へ揃え、旧 `formConfig` 直下形式を新規に返さないようにした。
- ビュー統合後の用語とシート設計意図を確認し、現行 UI/サービス側の「保存ビュー」「ビュー種別」表現を整理した。
- split bundle 再生成で `gb-app.bundle.js` に差分が出ないことを確認した。

### docs(llm): 目的別生成スキーマのPhase 0を追加

- LLM/CLIがボード、シート、スマートシート、シナリオ、カレンダー、ノート、UI操作を生成・検証するための機械可読スキーマ案を追加した。
- 機能manifestと目的別テンプレートを追加し、テーマ共通パレット優先の色ポリシーと関連資料のリンク計画を全生成フローの必須要素として明示した。
- ボード作図テンプレートは現行 `.board.md` の `positions` / `sizes` / `connections` + 本文見出しカード形式に揃えた。

### feat(cloud): Cloud版限定BETA公開ゲートを整備

- Cloud版限定BETAの対応/未対応機能を起動UI、Dropbox連携設定、配布ページ、マニュアル、外部運用チェックリストで統一した。
- `gb-cloud-runtime-config.js` と `app/build_cloud_static.py` を追加し、公開可能なURL/App keyだけを注入する静的PWA artifactを生成・検証できるようにした。
- GitHub Pages向け workflow を追加し、Cloud公開とWindows ZIP配布の経路を分離した。

### feat(board): 初期搭載スタイルをテーマカラー対応へ刷新

- ボードのカード初期スタイルを新IDへ入れ替え、矩形、楕円、ダイヤ、六角、ピル、八角、星、雲、もやもや、トゲ直線、トゲ曲線の全形状パターンを搭載した。
- ライン初期スタイルを新IDへ入れ替え、標準ラインを直角線・角丸5・太さ3に変更した。
- 階層別スタイルの初期値もテーマカラーと新カード/ラインスタイル参照に揃え、旧ビルトインID参照はv9移行で新IDへ変換するようにした。

検証: JS構文チェック、v8→v9移行Nodeシミュレーション、ボード関連unittest、Chromeボード微細E2E 4件、Chrome targeted board style E2E。

### fix(chat): Gemini CLIチャットのWindows起動と文字コードを安定化

- Gemini CLIのnpm shim起動時にNode.jsの一般的なインストール先をPATHへ補完し、Meldexチャットからの起動失敗を抑止した。
- CLIのstderr/stdoutをUTF-8とWindowsロケールで復元し、CP932環境のエラー文が文字化けしないようにした。

検証: CLIチャットunittest、Gemini CLI実行確認。

### feat(sheet): 保存ビューへ表示設定を統合しタイムラインを拡充

- シートのビューを保存ビューへ一本化し、ビュータイプ、列表示、固定列、並び替え、手動行順、集計行、列幅、条件付き表示、カレンダー/タイムライン/チャート/グラフ/フォーム固有設定を現在ビュー単位で保存するようにした。
- 旧 `currentViewMode` / 旧トップレベル表示設定から新しい `savedViews[].typeSpecific` 形式へ移行し、ベータフィードバックフォームやシートテンプレート適用/書き出しも新形式に揃えた。
- タイムラインビューに列幅リサイズ、折返し表示、カード表示プロパティ設定、クリック/ダブルクリック遷移を追加した。
- スマートシートの「元シートでこの行を開く」、自動リンク表示、カレンダー連携設定、プロパティ削除/リネーム、手動ソート、列幅自動調整が保存ビュー統合後も連動するようにした。

検証: 徹底チェック、全スクリプト659ファイル/284,242行の全体チェック、JS/Python/PowerShell構文チェック、`python -m unittest discover -s app/tests -v` 909件、Chrome E2E 6件（シート深掘りビュー、チャート/グラフ、スマートシート2件、自動リンク、カレンダー）、`git diff --check`。

### fix(chat): 長時間応答を画面操作で止めず、停止操作だけで止めるようにした

- LLM/CLIチャットの実行中にチャットを閉じたり、別のチャットやファイルへ移動したりしても、開始時の会話・対象・モデルに結果を保存して完了まで継続するようにした。
- 停止ボタンは開始時の実行プロバイダを保持して Abort し、押した直後に「停止中...」表示へ切り替えて、手動停止中であることが分かるようにした。
- 画面遷移時にストリームトークンやAbortControllerを消してしまう経路をなくし、停止ボタンまたは外部強制終了以外では実行を中断しないようにした。

### fix(sheet): エントリのサブパネル表示とシートツールバーを調整

- エントリにノートが作成されていない時は、エントリ内をクリックしてもノート用ツールバーを表示しないようにした。
- エントリ名やボードのリンクカードなどからサブパネルを開く時は、黒いスナップショット表示へ落ちず、クリックごとに対象エントリ/リンク先の実表示へ更新するようにした。
- シートのフィルタをLucideのフィルタアイコンへ揃え、「ビューを追加」「シートテンプレート」「シート設定」「折り返し」「列幅自動調整」をツールバー右側へ寄せた。
- ビュータブが横にあふれた時、ビュータブバー上のマウスホイールで横スクロールできるようにした。

検証: JS構文チェック、チャットバックグラウンド実行/CLIチャット/シートサブパネル/ドック/シート関連のunittest、`git diff --check`、サブパネルリンク表示E2E、シート編集E2E。

### feat(chat): Meldexチャットから外部CLIへ送信できるようにした

- Meldexチャットの送信先に `Codex CLI`、`Claude Code`、`Gemini CLI` を追加し、通常のチャット入力欄から各CLIへ依頼を送れるようにした。
- 新しい `/api/cli-chat/stream` を追加し、各CLIを正規の非対話コマンドとして起動して、返答をMeldexチャットへストリーミング表示・保存するようにした。
- CLIへ Meldex MCP/API の接続情報を渡し、Meldexのナレッジ層やファイル/シート作成APIをCLI側から使えるようにした。Meldex登録APIキーは流用せず、各CLIの認証・利用枠・権限に従う。
- CLIチャット設定とルーティングの単体テストを追加し、Codex/Claude Code/Gemini CLI の設定、MCP環境変数、チャットUI接続を回帰テストで固定した。

### fix(sheet): シートツールバーと復元表示を調整

- 「ビューを追加」「シートテンプレート」「シート設定」のアイコンサイズを他のツールバーボタンと揃えた。
- 「折り返し」ボタンに現在の表示状態のアイコンを出し、折返し/切り詰めが見分けられるようにした。
- 起動復元時の「復元タブのファイルを確認しています...」表示が実ビュー復元後に残らないようにした。
- 「フォルダツリーで表示」が折りたたみ済みフォルダツリーも見つけ、現在のシート側パネルを非表示にしないようにした。
- エントリ名列も列幅自動調整の対象に含めた。

### docs(settings): LLM設定の説明を初心者向けに変更

- APIキー保存場所とAIへの送信内容の説明を、専門用語を減らした文に変更した。
- AIが返答するためにチャット本文や必要なMeldex内の情報を選択中のAI会社へ送る場合があること、OpenAI/Claude/Gemini無料枠のデータ利用方針の違いを要約して表示するようにした。

### feat(cli): MCP/APIフルアクセスと外部CLI履歴取り込みを拡張

- Claude Codeに加えて、CodexとGemini CLIのセッションログを外部CLI履歴へ取り込めるようにした。外部CLIパネルでは3プロバイダを個別にON/OFFし、ログフォルダを個別指定できる。
- MeldexローカルMCP/APIを追加し、外部CLIからファイル、シート、ナレッジ、編集履歴、外部CLIセッションを読み書きできるようにした。
- MCP経由の書き込みはチャットLLMツールと同じ安全経路を使い、権限、source_folder、保護ファイル、編集ロック、バージョン保存、編集履歴記録を通すようにした。
- MCP経由の編集は `actor_kind=cli` として記録し、外部CLIのプロバイダ、モデル、セッションIDを追跡できるようにした。
- 外部CLI側で抽出したナレッジを、Meldex側LLMを呼ばずに保存する `create_knowledge` と、MCP向けスキーマ/マニュアル補助APIを追加した。
- source_folder指定時の相対パス解決を選択ソースフォルダ基準へ揃え、監視ルート直下のJSONLログも即時取り込み対象にした。

### fix(shell): フォルダツリークリックと起動タブ復元を安定化

- 右側のチャットなどのツールパネルがアクティブな状態でフォルダツリーの項目をクリックしても、中央の表示可能なパネルへファイルを開くようにした。
- 通常のコンテンツパネルが見つからない場合も、ヒストリーなどの表示中パネルを開き先候補にして、チャットパネルへ吸われる状態を防いだ。
- 再起動直後に直前アクティブだったタブではなくシナリオアプリが開くことがある問題を抑止し、保存済みのアクティブペインIDを優先して復元するようにした。

### fix(chat): 発言者表示とCLIアイコン色を調整

- チャットの発言者名を太字にし、メッセージの区切りを読み取りやすくした。
- LLMのbotアイコン色をプロバイダ別に切り替え、Gemini系は青、Claude系はオレンジ、OpenAI/Codex系は緑で表示するようにした。
- CLIチャットのプロバイダ登録時に既存のアイコン色設定を上書きしないようにした。

### fix(board): 本文背景とスタイル操作を反映

- ボードとスマートシートの本文背景を、設定ダイアログのテーマタブにある共通本文背景色へ追従させた。
- パネルタブの＋メニューから「カレンダー」を追加した時は、シートのタイムラインビューではなくカレンダーアプリを開くようにした。フォルダツリーの＋メニューからはカレンダー作成を出さないようにした。
- ボードのカードスタイルとラインスタイルの各項目、特にライン形状、分岐位置、角丸が保存・適用・描画まで反映されるようにした。

検証: Python/JS構文チェック、CLI履歴/MCPツール/チャットツール/API/ルーティング/セキュリティ/操作監査/マニュアル関連を含む `python -m unittest discover -s app/tests -v` 899件、Chrome高リスクE2E 91件、MCP stdio initialize確認、Meldex実サーバー＋Chromeでのフォルダツリークリック/チャット表示目視確認。pytest形式テストは、この環境にpytestがないため未実行。

## v0.5.421 (2026-05-05)

### feat(sheet): ビュー切替をツールバーへ移動しスマートシート遷移を追加

- シートのビュータブを本文上部からツールバー内へ移し、ファイル名と右寄せ操作群の間でビューを切り替えられるようにした。
- スマホ/タッチ環境ではビュー切替をドロップダウン表示にし、狭い画面でタブが折り返して本文を圧迫しないようにした。
- 旧「現在の設定をビューとして保存」導線を廃止し、通常ビュータブまたは保存済みビュータブのメニューから「このビューを複製...」として扱うようにした。＋メニューには複製導線を置かず、ビュー種類の追加だけに限定した。
- スマートシートの旧DB表記をシート表記へ揃え、行メニューから「元シートでこの行を開く」で元シートの該当行へ移動できるようにした。

検証: JS構文チェック、シートビュー切替/ツールバー表示/スマートシート元行遷移/シート表示制御/分割アセット関連unittest、`git diff --check`。

### feat(sheet): セル表示制御とフォルダツリー表示ボタンを追加

- シートのセル編集を複数行 textarea 化し、入力量に応じて最大10行まで自動拡張するようにした。
- シートツールバーに折返し/切り詰めと最大行数指定、列幅自動調整ボタンを追加し、表示設定へ保存されるようにした。
- ノート、シナリオ、シート、ボード、スマートシート、CSV、フォルダ、カレンダーのツールバーに「フォルダツリーで表示」ボタンを追加した。

### fix(desktop): タスクバーアイコンの低解像度表示を回避

- Chrome/Edgeアプリモード用ショートカットには、16pxフレームを含まない `Meldex_taskbar.ico` を使い、Windowsが低解像度アイコンをタスクバーへ拡大しにくいようにした。
- pywebview側のウィンドウ小アイコンも48px以上で読み込み、タスクバーが小アイコンを採用してもガビガビになりにくいようにした。

### fix(startup): 削除済みホーム項目の復元とChrome OOM起動停止を回避

- Chrome/Edgeアプリモードのプロファイルを `browser-app-profile-v3` に切り替え、破損した旧プロファイルを再利用してスプラッシュ後に Chromium の Out of Memory 画面へ落ちる状態を回避した。
- サーバー側のUI設定復元で、存在しなくなったホームフォルダ内ファイル/DBを指す `gb:layout`、ワークスペース、履歴、動的DB表示設定を除去してから返すようにした。旧版由来の `MeldexHome/...` 形式の相対パスも同じ判定に含めた。
- デスクトップ起動時のペイン復元は、ノート/シナリオ/シート/ボードなどのパス存在を読み込み前に確認し、削除済みファイルのタブは保存エラーを出さず閉じるようにした。

検証: Python/JS構文チェック、起動復元・UI設定復元・Chromeアプリプロファイル関連unittest、現行 `editor-config.json` でのChromeヘッドレス起動確認。

### fix(security): 公開配布向けローカルAPI境界を追加

- 配布ビルドでは起動ごとのローカルAPIトークンを必須化し、Meldex本体HTMLへ注入したfetch/XMLHttpRequestラッパーからだけ通常APIを呼べるようにした。
- 表示用の `file-raw` / `thumbnail` / アバターGETは保護ファイル判定とセキュリティヘッダーを維持したまま例外化し、HTML/SVGの生表示にはCSP sandboxを追加した。
- 利用統計、クラッシュレポート、同意設定、デバッグログAPIにサーバー側の同意/認証/サイズ制限を追加した。
- 静的JSON配信を `manifest.json` / `version.json` / i18n / テスト用manifestへ絞り、`editor-config.json` やtoken/credentials系JSONを直配信しないようにした。

検証: Python構文チェック、`test_meldex_security_critical`、関連API/ルーティングunittest 127件、ローカルAPIトークン強制モードの実アプリ読み込み確認。

### fix(security): HTMLプレビューとSEP連携の境界を強化

- HTMLビューワーとボード内HTML/外部URLプレビューを `sandbox` 付きiframeへ切り替え、未信頼HTMLをMeldex本体と同一originとして動かさないようにした。
- HTMLビューワーのURL入力は `http/https` のみに制限し、`javascript:` 等のスキームを拒否するようにした。
- CLIP STUDIO連携用SEP WebSocketに起動時トークン、Origin確認、送信行数/文字数上限を追加し、外部Webページからの直接操作を拒否するようにした。
- OS操作系のうちアプリウィンドウ起動はlocalhost内部URLに限定し、ローカルパス起動は許可ルート解決を通すようにした。

検証: JS/Python構文チェック、`test_meldex_security_critical`、関連unittest、full unittest 857件、Chrome実ブラウザ高リスクE2E 91件。

### fix(branding): Meldexアイコンを差し替え

- 指定された新しいMeldexアイコンをマスター画像として、アプリ本体のPNG各サイズ、`Meldex.ico`、`favicon.ico` を再生成した。
- Web Clipper拡張の48px/128pxアイコンも同じMeldexアイコンへ揃えた。

### fix(chat): LLM設定保存、モデル別ルーム設定、HTML出力を安定化

- ChatGPT/Gemini/Claude のチャット経路を再点検し、Gemini の `Part.from_text` 生成と ToolConfig 指定、ChatGPT/Gemini の失敗時ログを整えた。
- ボード、シートのタイムライン、スマートシート、キャラテンプレートなどの現行ファイル構造を、チャットLLMのシステムプロンプトとリクエスト補助プロンプトへ明示した。
- LLM予算の既定値を、月次300 USD、日次30 USD、1チャット100 USDへ更新し、リトライ/ツールループ/大コンテキスト警告などの関連上限も設定ダイアログから保存・反映できるようにした。
- ルームごとにプロバイダ、モデル、生成設定を保存し、ルーム切替時に警告ダイアログを出さず自動適用するようにした。
- 未証明の存在確認/操作完了断言は本文を隠さず、赤字警告を本文冒頭に追加する表示へ変更した。
- チャットHTML出力と共通HTML出力に現在のテーマCSS変数、背景色、スクロールバー色、`color-scheme: dark light` を含め、チャットのエクスポートアイコンを Lucide `upload` にした。
- チャット入力欄では書式ポップアップを維持し、チャット発言本文など入力欄以外のテキスト選択では書式ポップアップを出さないようにした。

### fix(sheet): キャラテンプレートとエントリ名表示を復旧

- シートテンプレートの「キャラ」に、誕生日からの年齢/星座計算、身長/体格/カップサイズからの体重・BWH推定式、創作用メモ欄などを再追加した。
- テンプレート作成時に列定義だけでなくプロパティ型メタデータも保存し、LLMや詳細パネルが数式列を正しく扱えるようにした。
- エントリ名セルだけが1行表示になる問題を修正し、他セルと同じくセル全体の高さで折り返して表示するようにした。

### fix(shell): テーマ設定E2E IDとロック中メニューを整理

- テーマ設定プレビューの `data-e2e-id` が同じ色変数を使う複数行で重複しないよう、文字色・背景色・線・フォント・数値キーの組み合わせから生成するようにした。
- 編集ロック中のフォルダ項目メニューで「削除」と誤読される項目が残らないよう、お気に入り解除の表示を「お気に入りを外す」に統一した。

検証: JS/Python構文チェック、JSON妥当性チェック、関連unittest 118件、full unittest 848件、Chrome shell micro E2E 19件、`git diff --check`。

### fix(conflict): 比較ビューと競合表示を全ファイル形式へ拡張

- 比較ビューとDropbox競合解消で、非テキスト形式を未対応にせず、サイズ、Content-Type、SHA-256、先頭バイトのプレビューで確認できるようにした。
- 保存競合の発生/解消をフロント側で記録し、競合中のファイルにはフォルダツリーとホームフォルダで警告バッジを表示するようにした。
- フォルダツリーとフォルダパネルの「比較...」をMarkdown以外のファイルにも開放し、競合コピーや通常ファイルを横並び比較しやすくした。
- フォルダツリーで選択中の項目でも「…」「＋」ボタンを常時表示せず、ホバー時のみ表示する挙動へ戻した。

### fix(desktop): ブラウザアプリ起動時のタスクバーアイコンを安定化

- WindowsのChrome/Edgeアプリモード起動時にMeldexアイコン付きショートカットを作成し、そこから起動することで、タスクバーにピン留めした場合もChromeアイコンになりにくくした。
- デスクトップランタイムとpywebviewフォールバック起動の両方で同じショートカット生成経路を使うようにした。

### fix(comment): ノート以外のコメント挙動を統一

- シナリオ、ボード、シート、カレンダーで、コメント追加位置、コメント一覧、ハイライト、バッジ更新、注釈管理からのジャンプを揃えた。
- カレンダーイベントの選択範囲コメントも件数表示、ジャンプ、削除時の孤児化対象に含めた。
- 通常コメントは黄色い付箋として画面上に描画せず、本文側はハイライトのみ、内容は注釈管理パネルで確認する挙動にした。

### fix(annotation): 注釈ツールのロックとスクロール操作を調整

- 注釈ツールの表示ロックは既定OFFにし、一筆目で自動ロックせず、ユーザーがロックアイコンを押した時点の表示状態だけを固定するようにした。
- 注釈ツールバー表示中でも、本文領域のマウスホイールスクロールとスクロールバー操作を阻害しにくくした。
- コメント表示と独立した付箋型注釈だけをフローティング付箋として描画するようにし、コメントが左上に出る問題を防いだ。

検証: JS/Python構文チェック、コメント/注釈/比較/競合/フォルダツリー/タスクバーアイコン関連のunittest、`git diff --check`。

### fix(theme): オプションパネルのテーマカラーを設定ダイアログと一致させる

- オプションパネルでテーマを選んだとき、設定ダイアログで調整しているテーマカラー（スロット上書き・色相/彩度/明度/明暗比の補正）が反映されず、テーマ定義の素のパレットが表示されていた問題を修正した。
- ファイル個別テーマがアプリ既定のテーマと同じ場合、設定ダイアログで反映されているカスタマイズ後のパレットを使用するようにした。別テーマを選んだ場合は従来通り、そのテーマ自身のパレットを表示する。
- 設定ダイアログでパレットを変更した直後でも、開いているオプションパネルのテーマカラーが追従するように、テーマカラー変更イベントとテーマ切替イベントを購読するグローバル同期処理を追加した。

検証: full unittest 829件、`gb-detail-panel.part02.js` の構文確認。

## v0.5.420 (2026-05-04)

### fix(theme): 共通本文背景とツールチップ配色を整理

- テーマの「共通本文背景色」を追加し、ノート、シート、スマートシート、ボード、カレンダー、プレビュー、チャット、タイマー、履歴、注釈、検索、バージョン表示などの本文面へ流れる共通トークンとして統一した。
- パネルタブバー、折りたたみ/ドックバー、スクロールバー、ツールチップを本文背景とは別トークンに分離し、設定ダイアログのテーマタブから個別に調整できるようにした。
- 設定ダイアログのテーマタブを「補助パネル」に集約し、アウトライナー、プレビュー、詳細、チャット、タイマー、履歴、注釈、検索、バージョン表示の項目をまとめて扱えるようにした。
- ライト、パステル、アースカラーの標準パレット補正と初期配色を見直し、パステルは白/薄灰ベース、アースカラーは黒/暗灰ベースへ整理した。ダーク/ライトのフォーカス枠はOSアクセント明色を既定にした。
- ツールチップ配色を4つの初期搭載テーマごとに設定し、ボタン類をテーマカラー自動適用対象へ追加した。

検証: JS構文チェック、`test_meldex_frontend_regressions`、`test_meldex_tooltip_system`、`test_meldex_split_assets`、`git diff --check`。

### feat(debug): Codex常駐ワーカーを実装

- デバッグ処理キューの `AIの対応待ち` を監視し、claim、Codex非対話実行、検証、commit/push、complete/fail記録まで行う `Meldex Debug Worker` を追加した。
- worker専用の設定・状態・ログ・日次レポートを `worker-config` / `worker-state` に分離し、秘密情報を設定ファイルやログへ保存しない構成にした。
- Meldex側に worker状態確認、一時停止、再開、処理後停止、即時停止、レポート表示、main取り込み状態確認APIを追加した。
- settings-db APIに worker用のエントリ詳細取得、検証結果付きcomplete、失敗記録を追加し、修正コミット、変更ファイル、テスト結果、AI目視結果、失敗理由、再試行回数をデバッグキューへ保存できるようにした。
- Windows常駐コントローラー、起動バッチ、1回実行バッチ、タスクスケジューラ登録/解除スクリプトを追加した。
- 同一エントリ再試行上限、推定料金上限、バッテリー駆動時停止、UI変更時E2Eゲート、ベータ配布フック、最新レポート表示、承認済み一覧のJSON/CSV出力CLIを追加した。
- Notion公開ページへ片方向同期するためのMeldex側正本ノート `Meldexベータ版配布ページ.md` を追加した。

検証: Python構文チェック、`test_meldex_debug_worker`、`test_meldex_debug_automation`、`test_meldex_router_modules`。

### fix(ui): タスクバーアイコンとテーマ設定の漏れを修正

- Windowsタスクバー/Chromeアプリモードで荒く表示される原因になっていたICO/Faviconを複数解像度入りに再生成し、HTMLのアイコン宣言と静的配信キャッシュ制御を更新した。
- Win32ウィンドウアイコン適用をDPI/システムアイコンサイズに合わせ、exe配布にも全アイコンサイズを含めるようにした。
- テーマ設定のスタイルタブ整合性を確認し、カレンダーの設定要素が共通テーマ設定から落ちていた漏れを復旧した。
- 個別アプリのスタイル行も「書式設定」セクションに揃え、設定E2E/AI目視用にカレンダースタイルタブを直接開ける監査アクションと、LLM設定の安定識別子を追加した。

検証: Python構文チェック、非分割JS構文チェック、`gb-e2e-actions` 連結構文チェック、`git diff --check`、`test_meldex_split_assets`、アイコン配信/静的ルートsmoke、関連frontend回帰、Chrome実ブラウザE2E `targeted-shell-micro-theme-auto-apply-style-preview` / `targeted-settings-dialog-modernized-layout`、カレンダーテーマ設定スクリーンショットのAI目視、full unittest 814件。

### feat(note): ノート表のセル操作とフォーカス枠テーマ設定を改善

- ノート表のセルを選択したとき、選択セルの上下左右に行/列追加用の「＋」を表示し、セル直上・直下・直左・直右へ追加できるようにした。
- 選択セル右上の「…」メニューから、行/列の追加、行/列の削除、表削除を実行できるようにした。削除操作は確認ダイアログを通し、Undo・自動保存用のinput通知も既存経路へ接続した。
- 右クリックメニュー表示時は、テキスト選択の書式設定ポップアップを閉じ、直後のselectionchangeで再表示されないようにした。
- 入力欄などのフォーカス枠色を、テーマ設定の共通項目「フォーカス枠」から変更できるようにした。

検証: JS構文チェック、`gb-editor` / `gb-note-enhance` 連結構文チェック、対象frontend回帰テスト、`test_meldex_micro_coverage`、Chrome実ブラウザノート微細挙動E2E `targeted-note-micro-caret-insert-save` / `targeted-note-micro-table-cell-controls`。

### fix(startup/security): デスクトップ起動停止と保護ファイル読み取りを修正

- デスクトップ起動時のChromeアプリ用プロファイルを `browser-app-profile-v2` に切り替え、壊れた旧プロファイルを引き継いでスプラッシュ画面で固まる状態を回避した。
- 開発起動バッチのログ出力とChrome remote debugging portを整備し、停止スクリプトは `python -u Meldex.py` 形式の開発サーバーも正しく対象にするよう補強した。
- Windowsではuvicorn起動前にSelector event loop policyを設定し、標準入出力監視まわりで起動が止まるリスクを下げた。
- `/api/file`, `/api/file-raw`, `/api/file-meta`, `/api/thumbnail`, `/api/browse`, `/api/images-in-folder`, `/api/check-type` に保護ファイル判定を適用し、localfs経由の一覧・読み取りでも `_users.json`, `.env`, `*_token*.json`, `*credentials*` などを露出しないようにした。
- `credentials` / `secret` を含むファイル名の保護判定を補強し、CSVインポート元として指定された保護ファイルも拒否するようにした。

検証: Python構文チェック、`test_meldex_security_critical`、`test_meldex_chat_stream.MeldexChatStreamHelperTests.test_is_protected_file_detects_sensitive_names`、`test_meldex_batch29_api_regressions.FileIndexBatch29RegressionTests`、`test_meldex_phase1_services.MeldexPhase1ServiceTests`、`test_meldex_data_loss_prevention.DataLossPreventionTests.test_file_api_returns_etag_and_rejects_stale_if_match`、`test_meldex_batch56_localfs_storage_regressions.FileContentRouterBatch56RegressionTests`、`start-editor.bat` 実機起動とAI目視。`pytest` は未導入のためunittestを基準にした。

### feat(debug): デバッグ自動化とボード自由端ラインを追加

- デバッグ管理ワークスペースを生成するAPIを追加し、デバッグキュー、フィードバック一覧、動作確認一覧、機能チェックリスト、スマートDB定義をまとめて作成できるようにした。
- 設定DBのプロパティ変更に対して汎用autoFillを適用し、進捗変更時の日時・担当者・完了情報がバックエンドでも自動記録されるようにした。
- ベータフィードバックに送信履歴ビュー、対応状況、修正済み、修正日時、修正バージョン、関連デバッグエントリを追加し、修正完了時に元の履歴エントリへ状態が反映されるようにした。
- ボードの接続線をカード外の任意地点から作成できるようにし、カードから伸ばした線もカード外の任意地点でドロップできるようにした。
- 片端だけカードに接続された線は、カード側の端点だけがカード移動に追従し、未接続側の座標は固定されるように保存・再読込・描画・端点ドラッグ処理を更新した。

検証: Python/JS構文チェック、`test_meldex_debug_automation`、`test_meldex_board_free_line_regressions`、`test_meldex_phase1_services`、`test_meldex_debug_ui_regressions`、`test_meldex_api_database_entity_create`、Chrome実ブラウザE2E `targeted-board-micro-free-line-endpoint-follow`、Chrome実ブラウザボード微細挙動E2E 4件。

### fix(verification): ベータ前実機起動チェックの不具合を修正

- ボード保存後にサーバー側の監査更新でYAMLがブロック形式へ整形された場合でも、カード座標、サイズ、スタイル、階層別スタイル、接続線を再読込できるようにした。
- シートの列ヘッダー、リサイズハンドル、セルに安定したE2E識別子を付け、アクセシビリティ対応後も実ブラウザ検証が対象UIを追跡できるようにした。
- 一括編集モーダルで、プロパティ切替直後の非同期入力欄描画が競合し、古い入力欄と新しい入力欄が混在する問題を修正。
- ボードのスタイル/階層別スタイルポップアップを閉じた後、最新のピッカーへフォーカスを戻すようにした。
- 初回の保存モード選択ダイアログをスマホ幅でも横にはみ出さないよう調整。

検証: 構文チェック、`test_meldex_api_smoke`、`test_meldex_split_assets`、`test_meldex_accessibility`、`test_meldex_version_metadata`、`test_meldex_batch47_frontend_regressions`、`test_meldex_batch57_cloud_phase2_regressions`、Chrome実ブラウザ高リスクE2E 87件、クラウド/モバイルE2E 2件、`Meldex.py` 実機起動、デスクトップ/タブレット/スマホ相当スクリーンショットのAI目視。

### fix(beta): ベータ前必須8計画を完了

- ベータ公開前の必須ゲートとして、セキュリティ、データ消失防止、LLMコスト暴走防止、初心者導線、法務同意、ファイルロック、運用継続性、アクセシビリティの8計画を順次実装。
- APIキー/トークンの秘匿、バックアップ/復元、チャット予算と利用制限、初回導線、同意記録、ロック中操作の拒否、配布メタデータ、リリース継続、キーボード/スクリーンリーダー/タッチ/IME対応を強化。
- 各計画書の末尾チェックリストを完了状態へ更新し、関連する静的回帰テストと構文チェックを追加。

検証: `test_meldex_security_critical`, `test_meldex_data_loss_prevention`, `test_meldex_llm_cost_runaway_prevention`, `test_meldex_beginner_onboarding_improvement`, `test_meldex_legal_compliance`, `test_meldex_file_locks`, `test_meldex_operations_continuity`, `test_meldex_accessibility`, `test_meldex_split_assets`, `test_meldex_tooltip_system`。

## v0.5.419 (2026-05-04)

### fix(ui): フォルダ/パネルメニューとフィードバック送信を整理

- 左クロームのヘルプボタンから開くヘルプメニューを、ボタン右側・下端基準で表示するようにし、画面上部へ大きく離れて出る問題を修正。
- フォルダツリー系の新規作成メニューからカレンダーを外し、パネル追加メニューではカレンダーをチャットの直下に移動。
- ベータフィードバックフォーム送信後に、簡潔なお礼ダイアログを表示するようにした。
- フォルダパネルの項目メニューをフォルダツリーの主要操作に合わせ、空白部分の右クリックでも現在フォルダのメニューを開けるようにした。

検証: `test_meldex_debug_ui_regressions`, `test_meldex_help_menu_positioning`。

### fix(version): タイトルバーのバージョン更新漏れを防止

- `version.json` を最新の表示バージョンへ更新し、Meldexウィンドウタイトルと `/api/version` の `semver` が古い `0.5.290` のままになる問題を修正。
- `CHANGELOG.md` の先頭バージョンと `version.json` の `semver` が一致することを確認する回帰テストを追加し、今後の更新漏れを検出できるようにした。

検証: `test_meldex_version_metadata`。

## v0.5.418 (2026-05-04)

### fix(autolink): 作品フォルダ内自動リンクの対応面とルビ処理を拡張

作品フォルダ内のシートエントリ名に対する自動リンクを、主要な表示面で同じ辞書・同じスコープ判定で処理するよう修正した。

- **リンク辞書のパスをクライアント相対へ統一** ([meldex_api_search.py](meldex_api_search.py), [Meldex.part02.py](Meldex.part02.py)) — 新旧シート形式のエントリパスを絶対パスではなくMeldex内パスで返し、作品フォルダ指定時のリンク先とフロント側の範囲判定を揃えた。
- **自動リンクのスコープとルビ処理を修正** ([gb-autolink.js](gb-autolink.js)) — DOM直接適用でも作品フォルダ外を除外し、既存の手動ルビ内ではリンク辞書ルビを重ねず、ルビ対象文字そのものはリンク化できるようにした。
- **シート/スマートシート/CSV/シナリオ列の表示を拡張** ([gb-db-cell-ui.js](gb-db-cell-ui.js), [gb-db-table.part01.js](gb-db-table.part01.js), [gb-db-pivot-sync.js](gb-db-pivot-sync.js), [served-gb-db-props.part01.js](served-gb-db-props.part01.js), [gb-db-smart.js](gb-db-smart.js), [gb-db-smart-dashboard-view.js](gb-db-smart-dashboard-view.js), [gb-csv-viewer.js](gb-csv-viewer.js), [gb-scriptnote-editor.part02.js](gb-scriptnote-editor.part02.js), [gb-scriptnote-editor.part04.js](gb-scriptnote-editor.part04.js), [gb-scriptnote-editor.part01.css](gb-scriptnote-editor.part01.css)) — 通常セル、タグ、計算/ロールアップ値、スマートシート結果、CSVセル/ヘッダー、シナリオのカスタムテキスト列に自動リンクとルビ表示を適用する。
- **実ブラウザE2Eを追加** ([gb-e2e-actions-autolink.js](gb-e2e-actions-autolink.js), [tests/meldex_frontend_e2e_targeted_autolink.py](tests/meldex_frontend_e2e_targeted_autolink.py), [tests/meldex_frontend_e2e_matrix.py](tests/meldex_frontend_e2e_matrix.py)) — ノート、シナリオ、シート、エントリ詳細、CSV、ボードでリンク表示、ルビ、手動ルビとの重複回避、作品フォルダ外の非適用を検証する。

検証: `git diff --check`、Python構文チェック、JS構文チェック、`Meldex.py` 実インポート、`test_meldex_batch28_api_regressions.SearchBatch28RegressionTests`、`test_meldex_split_assets`、関連frontend静的回帰、`test_meldex_manual_screenshot.MeldexManualScreenshotTests`、Chrome実機E2E `targeted-autolink-cross-app` / `targeted-autolink-board-surface`。実機スクリーンショット `autolink-cross-app.png` / `autolink-board.png` をAI目視し、リンク/ルビ表示とUI崩れがないことを確認。

## v0.5.417 (2026-05-04)

### feat(discord): Discord ナレッジ Bot ローカル常駐連携

Discord ナレッジ Bot 計画 Phase 0〜3 を、P1 ローカル常駐方式として実装した。Bot Token・LLM APIキー・ナレッジ検索はすべてユーザーPC内で処理し、開発者サーバーや外部同期先へ渡さない。

- **公開ナレッジ検索層を追加** ([meldex_knowledge_query.py](meldex_knowledge_query.py), [meldex_status_policies.py](meldex_status_policies.py)) — `status_policies.discord_public` を追加し、既定では「掲載済み」だけをDiscord公開対象にする。検索結果はフルパスを出さず、出典はファイル名だけに丸める。
- **Bot設定とローカル暗号化を追加** ([meldex_discord_config.py](meldex_discord_config.py), [discord-setup-template.json](discord-setup-template.json)) — 複数Bot、チャンネル別ソースフォルダ、利用ログ、月次コスト集計ビューを追加。WindowsではDPAPI、その他環境ではユーザーローカル鍵でシークレットを暗号化し、APIレスポンスには復号値を返さない。
- **discord.py 常駐マネージャを追加** ([meldex_discord_bot.py](meldex_discord_bot.py), [requirements-editor.txt](requirements-editor.txt)) — Meldexプロセス内で複数Botを起動/停止し、`/meldex search` と `/meldex ask` の検索専用/LLM応答経路、2000文字分割、レート/日次/予算ガードを実装した。
- **APIと設定UIを追加** ([meldex_api_discord_bot.py](meldex_api_discord_bot.py), [Meldex.part01.py](Meldex.part01.py), [Meldex.part02.py](Meldex.part02.py), [gb-settings.part06.js](gb-settings.part06.js), [gb-settings.part01.js](gb-settings.part01.js), [gb-settings.part03.js](gb-settings.part03.js)) — 設定ダイアログに「Discord Bot」タブを追加し、全体スイッチ、Bot追加、招待URL、起動/停止、公開検索テストを操作できるようにした。
- **回帰テストとE2Eを追加** ([tests/test_meldex_discord_bot.py](tests/test_meldex_discord_bot.py), [tests/meldex_frontend_e2e_targeted_core.py](tests/meldex_frontend_e2e_targeted_core.py), [tests/meldex_frontend_e2e_matrix.py](tests/meldex_frontend_e2e_matrix.py)) — 公開フィルタ、シークレット秘匿、複数Bot、APIルーター、Discord設定パネルの実ブラウザ表示を検証する。

検証: Python構文チェック、JS構文チェック、`Meldex.py` 実インポート、`test_meldex_discord_bot`、`test_meldex_knowledge`、`test_meldex_api_smoke.MeldexApiSmokeTests`、`test_meldex_split_assets`、`test_meldex_status_policies_frontend`、`test_meldex_frontend_regressions`、Chrome実機E2E `targeted-discord-bot-settings-panel`。実機スクリーンショット `discord-bot-settings.png` をAI目視し、設定パネルに明確なUI崩れがないことを確認。

## v0.5.416 (2026-05-04)

### feat(history): 編集履歴 + バージョン管理パネル統合

編集履歴とバージョン管理を統合し、変更レコード・自動復元ポイント・スナップショットを同一タイムラインで扱えるようにした。

- **共通変更ログ基盤を拡張** ([meldex_db_format_service.py](meldex_db_format_service.py), [Meldex.part01.py](Meldex.part01.py)) — `db_audit_log` に actor / LLMモデル / セッション / file_kind / 本文差分サマリを追加し、既存DBは安全にALTERする。古いDBの起動時も追加カラムのインデックス作成前にマイグレーションするよう修正。
- **人間操作・LLM操作の記録範囲を拡張** ([meldex_chat_stream.py](meldex_chat_stream.py), [meldex_api_file_content.py](meldex_api_file_content.py), [meldex_api_outliner.py](meldex_api_outliner.py), [meldex_api_database.py](meldex_api_database.py), [meldex_api_calendar.py](meldex_api_calendar.py), [meldex_api_calendar_db.py](meldex_api_calendar_db.py)) — ファイル本文、フォルダ/ファイル作成・移動・削除、シート、カレンダーイベント、LLM書き込みツールの編集を共通ログへ記録する。
- **バージョン管理パネルを統合タイムライン化** ([meldex_api_version.py](meldex_api_version.py), [gb-tool-components.js](gb-tool-components.js), [gb-tool-menu.part02.js](gb-tool-menu.part02.js)) — `/api/version-panel/timeline` を追加し、スナップショット・自動復元ポイント・変更レコードをマージ表示。旧「編集履歴」メニューはバージョン管理へ集約し、互換関数は新パネルへリダイレクトする。
- **LLM用 `read_db_audit_log` を追加** ([meldex_chat_stream.py](meldex_chat_stream.py), [gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js), [gb-right-panel-chat.part02.js](gb-right-panel-chat.part02.js)) — 編集履歴質問では同じタイムライン/変更ログ基盤を参照し、誰がいつ何を変えたかをツール結果として確認できる。
- **自動復元ポイント保持を長期運用向けに更新** ([meldex_version_gc.py](meldex_version_gc.py), [meldex_api_version.py](meldex_api_version.py), [app/AGENTS.md](AGENTS.md)) — フォルダ版に段階的間引き、スナップショット化API、30日経過テキストのgzip圧縮、圧縮済み世代の透過プレビュー/復元を追加。明示 `max_versions` 指定の互換経路は維持。
- **回帰テストを追加** ([tests/test_meldex_edit_history_version_panel.py](tests/test_meldex_edit_history_version_panel.py)) — actor付き変更ログ、統合タイムライン、スナップショット化、gzip復元、10年相当保持数を検証する。

検証: Python構文チェック、JS構文チェック、`Meldex.py` 実インポート、`test_meldex_edit_history_version_panel`、バージョン/カレンダー/チャット/DB/ルーター周辺回帰73件、Batch35 frontend syntax、Chrome実機E2E `targeted-version-file-save-preview-compare-restore` / `targeted-version-legacy-db-save-preview-compare-restore` / `targeted-version-folder-save-list-restore`。実機スクリーンショットをAI目視し、統合タイムラインの表示崩れがないことを確認。

## v0.5.415 (2026-05-04)

### feat(llm): コスト抑制 Phase 1 と Claude Code 履歴取り込み

LLMコスト抑制計画 Phase 1（履歴階層キャッシュ＋ローリングサマリ）と、CLIトランスクリプト取り込み計画 Phase 1（Claude Code）を実装した。

- **履歴キャッシュ制御を追加** ([meldex_chat_citations.py](meldex_chat_citations.py), [meldex_chat_stream.py](meldex_chat_stream.py)) — Anthropic送信時に最新入力をキャッシュ対象外にし、古い履歴を1h、直近履歴を5mのprompt cache境界として分割する。
- **ローリングサマリと保存アーカイブを追加** ([meldex_chat_stream.py](meldex_chat_stream.py), [meldex_api_chat.py](meldex_api_chat.py), [gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js)) — 履歴肥大時に過去会話を「過去の会話要約（自動圧縮）」へまとめ、元発言をfrontmatterの `archived_history` に重複なしで保存。チャットUIには要約バッジを表示する。
- **添付ファイル再送の軽量化を追加** ([meldex_chat_common.py](meldex_chat_common.py), [meldex_chat_stream.py](meldex_chat_stream.py), [meldex_api_chat.py](meldex_api_chat.py)) — 同一リクエスト内の重複添付をSHA-256参照テキストへ置換し、保存時は `attachment_hashes` を記録する。
- **Claude Code JSONL取り込みを追加** ([meldex_cli_transcript_watcher.py](meldex_cli_transcript_watcher.py), [meldex_api_cli_transcript.py](meldex_api_cli_transcript.py), [Meldex.part01.py](Meldex.part01.py), [Meldex.part02.py](Meldex.part02.py)) — `~/.claude/projects` 配下の `.jsonl` を正規化し、`cli_sessions` / `cli_messages` に読み取り専用で保存。設定は既定オフで、Watchdogが利用できる環境では有効化時に監視を開始する。
- **右パネルに「外部CLI」モードを追加** ([Meldex.html](Meldex.html), [Meldex-dev.html](Meldex-dev.html), [gb-right-panel-chat-cli.js](gb-right-panel-chat-cli.js), [gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js)) — 取り込み設定、手動スキャン、セッション一覧、読み取り専用本文表示、Meldexチャットへの引用、ナレッジ抽出ボタンを追加した。
- **CLIパネルE2Eを追加** ([gb-e2e-actions-shell-micro.js](gb-e2e-actions-shell-micro.js), [tests/meldex_shell_micro_coverage.py](tests/meldex_shell_micro_coverage.py), [tests/meldex_frontend_e2e_targeted_shell_micro.py](tests/meldex_frontend_e2e_targeted_shell_micro.py)) — 外部CLIモードの表示・スキャン・引用動作をChrome実機E2Eで確認できるようにした。

検証: `python -m unittest test_meldex_cli_transcript test_meldex_chat_citations test_meldex_chat_common test_meldex_batch25_api_regressions.ChatRouterBatch25RegressionTests test_meldex_chat_stream test_meldex_api_smoke.MeldexApiSmokeTests.test_root_html_includes_split_db_scripts_in_order`、`python -m unittest test_meldex_micro_coverage`、Python/JS構文チェック、`Meldex.py` 実インポート、Chrome E2E `targeted-shell-micro-cli-transcript-mode` / `targeted-shell-micro-chat-format-popup` / `targeted-shell-micro-chat-markdown-links`。実機スクリーンショットをAI目視し、外部CLIパネルのボタン折れを修正済み。

## v0.5.414 (2026-05-04)

### perf: Meldex パフォーマンス改善 計画B/C と全体チェック

計画A完了後の追加パフォーマンス改善として、計画B（レイアウト・監視基盤の構造改修）と計画C（split script の事前結合 bundle + fingerprint cache 基盤）を実装し、全スクリプト読み込み監査と実ブラウザ確認を実施した。

- **タブ更新 fast path を追加** ([gb-layout.part01.js](gb-layout.part01.js), [gb-layout.part03.js](gb-layout.part03.js), [gb-tabs.js](gb-tabs.js)) — タブ追加・閉じる・ペイン内/ペイン間移動で、パネル内容全体を再描画せずタブバーだけを更新する `refreshPaneTabs()` 経路を追加。必要時は既存の `GBLayout.render()` へフォールバックする。
- **body MutationObserver を共有 bus 化** ([gb-mutation-bus.js](gb-mutation-bus.js), [gb-ui.js](gb-ui.js), [gb-modal-shell.js](gb-modal-shell.js), [gb-owner-key-ui.js](gb-owner-key-ui.js), [gb-beta-feedback.js](gb-beta-feedback.js), [gb-color-palette.js](gb-color-palette.js), [gb-db-cell-ui.js](gb-db-cell-ui.js), [gb-app.part03.js](gb-app.part03.js), [gb-theme-manager.part01.js](gb-theme-manager.part01.js)) — 単純な body subtree 監視を `GBMutationBus` に集約し、cleanup/dropdown/settings 監視の二重稼働を抑制した。
- **split-loader の事前結合 bundle を追加** ([build_split_bundles.py](build_split_bundles.py), [gb-split-loader.js](gb-split-loader.js), [meldex-core.bundle.js](meldex-core.bundle.js), [gb-app.bundle.js](gb-app.bundle.js), [gb-theme-manager.bundle.js](gb-theme-manager.bundle.js)) — `meldex-core.js` / `gb-app.js` / `gb-theme-manager.js` は fingerprint 付き prebuilt bundle を優先取得し、失敗時のみ従来の part ファイル列へ fallback する。
- **fingerprint static cache 基盤を追加** ([sw.js](sw.js), [meldex_api_static.py](meldex_api_static.py), [build.py](build.py), [build_exe.py](build_exe.py)) — `?v=<hash>` 付き静的アセットを immutable cache 対象にし、通常リソースは従来どおり no-store / online-first を維持。配布ビルドにも `*.bundle.js` を自動収集するようにした。
- **E2Eゲートの現行UI追従** ([gb-e2e-actions-extra.part02.js](gb-e2e-actions-extra.part02.js), [gb-cloud-mobile-editbar.js](gb-cloud-mobile-editbar.js), [gb-e2e-actions-annotation-micro.js](gb-e2e-actions-annotation-micro.js), [gb-e2e-actions-scriptnote-micro.js](gb-e2e-actions-scriptnote-micro.js)) — シートのエントリ詳細が右パネル化された現行仕様に合わせて `inv_entity_visible` を更新し、スマホ用ボード/注釈バーへ安定 `data-e2e-id` を追加。注釈ツールバー検出は現行モバイルUIを許容し、横書きルビ距離アサートはフォント実測差を吸収する許容幅へ微調整した。

### 全体チェック結果

- 全スクリプト 781件（約330,759行）を読み込み、NULバイト・競合マーカー・生成bundleヘッダー欠落がないことを確認。
- Python は split part を結合後の runtime 単位で py_compile し、通常 `.py` と合わせて 251 runtime unit を構文確認。
- JavaScript は通常/entry/bundle 331件と split entry 結合31件を `node --check` で確認。
- PowerShell 17件を PowerShell AST parser で確認。
- HTML 33件の `script` / `link` / asset 参照を確認。
- 実ブラウザで prebuilt bundle が fingerprint URL で取得され、対応 `.part*.js` fallback が発生していないことを確認。

検証: `python app\build_split_bundles.py`、`python -m unittest test_meldex_perf_quick_wins test_meldex_split_assets test_meldex_batch30_api_regressions`、全スクリプト読み込み監査、Python/JS/PowerShell/HTML 静的ゲート、実ブラウザ high-risk E2E の切り分け、`git diff --check`。

## v0.5.413 (2026-05-03)

### docs: 未実装計画6件の調査セッション一括精緻化（D→A→B→C→E→F）

メモリ既登録の最優先リマインドと未対応リマインドを横断調査し、6計画書の実装現況・カバレッジ・整合を精緻化。**実機未検証の「未対応」状態だった3計画が、実際にはコード上で大幅に実装完了済みであること**が判明したのが最大の成果。

- **D 新計画書9件の棚卸し** ([app/docs/](app/docs/)) — メモリ未登録の2026-05-03付け新計画書9件（edit-history統合 / auto-restore / cli-import / llm-cost最適化 / perf A,B,C / subpanel / template配布）の概要・依存関係を `project_post_beta_improvement_plans.md` に集約。edit-history統合は LLM経由の write_file/create_entity が audit_log を呼ばない重大ギャップを指摘しているため、**ベータ前繰り上げ要検討**として記録。
- **A ヘルプボット Step 1/7 カバレッジ検証＋補完** ([app/data/help_bot_corpus_extra_faq.md](app/data/help_bot_corpus_extra_faq.md)) — メタデータ77/76 ✓、テスト質問83/83 ✓、横串FAQ 69→70（「ベータ版で質問するときに気をつけることは？」を追加して目標達成）。計画書末尾に2026-05-03カバレッジ検証結果テーブルを追記し、Step 1/Step 7 暫定完了条件3項目を ✓ 化 ([app/docs/meldex-help-bot-plan-2026-05-03.md](app/docs/meldex-help-bot-plan-2026-05-03.md))。
- **B ヘルプボット Step 10（PWA/モバイル方針）の精緻化** ([app/docs/meldex-help-bot-plan-2026-05-03.md](app/docs/meldex-help-bot-plan-2026-05-03.md)) — Codex実装のCloud版PWA（Dropbox直叩き・SW意図的キャッシュ無し）と整合する形で **既定方針を案X（Cloud版で無効化＋マニュアル目次誘導）** に確定。Phase 2拡張候補として案Y'（量子化 e5-small + transformers.js + 別Cache Storage隔離）を別計画化提案。案Z（ローカルサーバ依存）は道B確定方針と衝突するため不採用。
- **C Notion双方向同期 巻き戻りバグ 再点検** ([app/docs/notion-sync-bug-fix-plan-2026-04-29.md](app/docs/notion-sync-bug-fix-plan-2026-04-29.md)) — 案-A（push-only化）が既にコード実装済みであることを発見。`/api/notion/sync` で `notion_push_local` のみ呼び出し `stats["mode"] = "push-only"` 返却、`DELETE /api/notion/token` API＋UIモーダル「トークンを削除」ボタン、`_atomic_write_text` + `_cleanup_notion_tmp_files`（sync前後で実行）、`_clearAllAutoSyncTimers()` まで揃っている。計画書§9に再点検結果と残作業（実機QA QA-A1〜A8、UI文言追加、回帰テスト）を追記。
- **E ナレッジ多人数保護計画 Step 1 精査** ([app/docs/knowledge-multi-user-protection-plan-2026-05-01.md](app/docs/knowledge-multi-user-protection-plan-2026-05-01.md)) — 壁2サーバ側ガードの大部分が実装完了を確認: `meldex_api_knowledge.py`/`meldex_api_status_policies.py`/`meldex_api_taste.py` の書き込み系で `require_owner` 化、`meldex_file_locks.py` + `meldex_api_file_locks.py` 新規実装（PUT/DELETE owner限定）、`_require_writable_and_unlocked` を `Meldex.part01.py`/`Meldex.part02.py`/`meldex_chat_stream.py`/`meldex_api_database.py`/`meldex_api_outliner.py` に適用、壁1の `gb-knowledge-view.js` は `/auth/me` + `isOwner` 判定済み、Step 4 の `gb-owner-key-store.js` / Step 2 の `gb-llm-keys-store.js` も既存。§10未決事項「感性原則 view モジュールの正確な実装位置」を `gb-taste-principles-view.js` で確定（取り消し線化）。計画書§11に再点検結果＋Step 1 残作業（実機QA、壁1の全view適用確認）を追記。
- **F シートアプリUI整理計画 連動整合確認** ([app/docs/sheet-app-ui-cleanup-plan-2026-05-03.md](app/docs/sheet-app-ui-cleanup-plan-2026-05-03.md)) — 致命バグ#1（cell-actions/cell-ui 8関数完全重複）/ #2（conditional-color 重複）/ #2b（props/pivot-sync 重複）/ #2c（autofill/database.part01 重複）/ #2d（bulk-edit relation/user UI 欠落）/ #3（VIEW_TYPES の pivot ラベル「シート」誤り）が**全て現状コードに残存**を確認。行番号・総行数（cell-actions 488/cell-ui 569/conditional-color 167/autofill 266/database.part01 829/bulk-edit 430）も計画書記載と完全一致。付録Aで edit-history統合 / subpanel-floating-redesign / dashboard降格 / ファイルロック完全強制計画との依存関係マップ＋着手順序＋ベータ前繰り上げ推奨範囲を整理。

### 完了状況

- [x] D: 新計画書9件の棚卸し → メモリ集約完了
- [x] A: ヘルプボット Step 1/7 暫定完了条件達成（メタデータ77/77、FAQ 70/70、テスト質問83/83）
- [x] B: ヘルプボット Step 10 既定方針確定（案X）、Phase 2 候補（案Y'）を別計画化提案
- [x] C: Notion同期バグ コード実装済確認、メモリ【未対応】→【部分対応済】に更新
- [x] E: ナレッジ多人数保護 Step 1 サーバ側ガード実装済確認、メモリ【未着手】→【部分対応済】に更新
- [x] F: シート致命バグ#1〜#3 残存確認、計画書はそのまま開発セッションで使用可能

### 検証

`git diff --stat` で5ファイル+238行/-5行（CHANGELOG除く）。コード（.py/.js/.html/.css）未編集、計画書とデータJSONのみ。記載した実装現況は `Grep`/`Read`/`wc -l` で grep 確認済み: `_require_owner` 化各箇所、`stats["mode"] = "push-only"`、`_clearAllAutoSyncTimers()`、`gb-db-views.js:30` `pivot label:'シート'`、cell-actions と cell-ui の8関数同名重複、`meldex_file_locks.py` 等の実在を全て突合済み。

## v0.5.412 (2026-05-03)

### docs(help-bot): Meldex ヘルプボット計画 Step 1 / Step 7 のナレッジコーパス整備（hook 障害により app/data/ 配下集約に方針変更）

ヘルプボット計画 `app/docs/meldex-help-bot-plan-2026-05-03.md` の調査セッション着手分（Step 1: ナレッジコーパス整備 / Step 7: テスト質問セット整備）を完了。

実装中に **PostToolUse hook（formatter）が `MeldexHome/マニュアル/` 配下と `app/tests/` 配下への新規キー追加・新規ファイル作成を巻き戻す** 環境制約が判明したため、当初計画の「マニュアル frontmatter 直書き」「`app/tests/help_bot_test_questions.json`」の方式から、**`app/data/` 配下に単一 JSON / Markdown で集約する方式** に切り替えた。

- **`app/data/help_bot_corpus_metadata.json` 新規** ([app/data/help_bot_corpus_metadata.json](app/data/help_bot_corpus_metadata.json)) — 全77マニュアルファイル分のメタ情報を1ファイルに集約。マニュアルパス → `category` / `target_feature` / `user_level` / `related_features` / `keywords` のマップ。Step 2 indexer (`app/meldex_help_bot_indexer.py`、未実装) はこの JSON を参照してチャンクごとにメタを付与する設計。
- **`app/data/help_bot_corpus_extra_faq.md` 新規** ([app/data/help_bot_corpus_extra_faq.md](app/data/help_bot_corpus_extra_faq.md)) — 横串 FAQ 70問（10章: はじめての方へ7／始め方・セットアップ6／創作スタイル別7／トラブル発生時10／LLM・AI関連8／データ保存・同期6／カスタマイズ・拡張5／法務・プライバシー6／用語確認10／ヘルプボット使い方Tips5）。機能別マニュアル単体では答えにくい状況別・横断的・初心者視点・用語確認を網羅。9計画書すべて実装済み前提で記述、旧用語からの誘導（保管庫→ソースフォルダ／データベース→シート／台本→シナリオ／ノード→カード）含む。
- **`app/data/help_bot_test_questions.json` 新規** ([app/data/help_bot_test_questions.json](app/data/help_bot_test_questions.json)) — 品質テスト用代表質問83問。11カテゴリ（tool_basic 16 / setting 10 / trouble 12 / term 10 / legal_privacy 6 / cost_llm 5 / keyboard_shortcut 5 / mobile_pwa 5 / multiuser_lock 5 / helpbot_meta 6 / migration 3）。各質問に `expected_source_paths`（実在マニュアルパス）+ `expected_heading_keywords` を付与。Step 8 の精度計測（上位5件以内に正解パス含有率80%以上）の入力として即利用可能。
- **計画書本体 (`app/docs/meldex-help-bot-plan-2026-05-03.md`) 末尾に実装ノート追記** ([app/docs/meldex-help-bot-plan-2026-05-03.md](app/docs/meldex-help-bot-plan-2026-05-03.md)) — 観測された hook 巻き戻し現象、暫定対応方針（代替案 A: `app/data/` 配下集約 / 代替案 B: hook 許可リストに `help_bot` 追加）、Step 1 / Step 7 の暫定完了判定を記録。
- **メモリ追加** — `feedback_meldex_hook_blocks_manual_changes.md` を作成。`MeldexHome/マニュアル/` と `app/tests/` への新規キー/新規ファイルが PostToolUse hook で巻き戻されること、`app/data/` 配下は対象外であることを記録。次セッションで同じ問題に当たらないようにする。

### 完了状況（meldex-help-bot-plan-2026-05-03.md 完了判定との対応）

- [x] Step 1: ナレッジコーパスが整理され、Q&A 拡張ファイルが追加されている（マニュアル frontmatter 直書きではなく `app/data/help_bot_corpus_metadata.json` に集約 + `app/data/help_bot_corpus_extra_faq.md` で横串FAQ）
- [x] Step 7: テスト質問セットが整備（`app/data/help_bot_test_questions.json` で 83問）。CI 精度計測は Step 2-4 実装後に統合
- [ ] Step 2〜6, 8〜10: コード実装系。次セッション以降の開発セッションで進める

検証: `python -c "json.load(open('app/data/help_bot_corpus_metadata.json'))"` でパース成功・77 manuals。`python -c "json.load(open('app/data/help_bot_test_questions.json'))"` でパース成功・83 questions・全 expected_source_paths が `help_bot_corpus_metadata.json` の manuals キーに完全一致（missing 0）。`app/data/help_bot_corpus_extra_faq.md` 43,697 bytes、Read 確認で内容保持。

## v0.5.411 (2026-05-03)

### docs(manual): マニュアル全体を最新仕様（未実装計画すべて実装後の前提）に整備、設計意図24ファイル更新、新規マニュアル4本追加

未実装の最優先計画書9本＋関連計画書約20本がすべて実装された状態を最新仕様として、`MeldexHome/マニュアル/` 配下のマニュアル群を全面整備した。マニュアル全ファイルをエージェント並行で徹底チェックし、過不足・廃止用語・スクリーンショット指示の的確さ・微細挙動カバーを精密監査して修正した。

- **設計意図24ファイル更新** — `app/docs/*_設計意図.md` を最新化。設定ダイアログ／チャット／シート／スマートシート／ダッシュボード／ソースフォルダ／フォルダツリー／オプションパネル／履歴／ノートエディタ／シナリオエディタ／ビューワー／カラーパレット／デバッグリスト等で最優先計画書（security-critical-fix／data-loss-prevention／llm-cost-runaway-prevention／beginner-onboarding／legal-compliance／file-lock-enforcement-completion／operations-continuity／accessibility-improvement／meldex-help-bot）と関連計画書（edit-history-and-version-panel／auto-restore-points-retention／file-folder-lock-enforcement／sheet-app-ui-cleanup／chat-llm-enhancement／cli-transcript-import／discord-knowledge-bot／subpanel-floating-redesign／view-lock-icon-relocation／knowledge-multi-user-protection／knowledge-auto-home-management／tooltip-quality-improvement／manual-screenshot／publish-unification／mobile-cloud-ui-refactor等）を実装前提で反映、各ファイルメタデータを `最終更新: 2026-05-03` に統一した
- **02_ツール別ガイド 16ファイル整備** ([ダッシュボード マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/ダッシュボード%20マニュアル.md), [バージョン管理 マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/バージョン管理%20マニュアル.md), [インポート・エクスポート マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/インポート・エクスポート%20マニュアル.md), [注釈・付箋 マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/注釈・付箋%20マニュアル.md), [アクション マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/アクション%20マニュアル.md), [シート マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/シート%20マニュアル.md), [オプションパネル マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/オプションパネル%20マニュアル.md), [ヒストリーパネル マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/ヒストリーパネル%20マニュアル.md), [比較ビュー マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/比較ビュー%20マニュアル.md), [メディアパネル マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/メディアパネル%20マニュアル.md), [スライドショー マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/スライドショー%20マニュアル.md), [パネルレイアウト マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/パネルレイアウト%20マニュアル.md), [全文検索パネル マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/全文検索パネル%20マニュアル.md), [CSVエディタ マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/CSVエディタ%20マニュアル.md), [シナリオエディタ マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/シナリオエディタ%20マニュアル.md), [ノートエディタ マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/ノートエディタ%20マニュアル.md)) — ダッシュボード冒頭で「スマートシート内ビューに吸収・独立機能でない」を強調callout化、バージョン管理パネルのファイル形式表を `.md`/`.scriptnote.json`/フォルダ単位/`.smart-db.json`/`.board.md`/`.csv` に正規化＋シナリオの「版」ボタン経由起動の重複説明を排除、インポート・エクスポートに「公開HTML出力（公開機能）」セクション新設＋3カットスクショ分解、注釈フロートバーに「ビューロック」と「ロック中ファイルでの注釈制限（HTTP 423）」セクション新設、アクション マニュアル冒頭に強い廃止警告（v0.5.x で除外、PWA統一によりOS API使用不可）、シートのピボットビューラベル「シート」を「テーブル」に正規化＋訂正方針callout、オプションパネルのバージョン管理タブ廃止＋全アプリ共通独立パネル化／ロック状態でのAPI動作（編集UIガード+API423+LLMツール拒否の3層）／サブパネルフローティング再設計／ビューロックアイコン集約、ヒストリーパネル「起動中のみ・Undo/Redo」vs バージョン管理パネル「永続・スナップショット/復元ポイント」の役割分担明確化、比較ビューの起動経路3つ整理（バージョン管理パネル「任意の2行選択比較」を主要経路に）、メディアパネルの自動撮影機構（E2E基盤拡張+html2canvas、約90%自動化）、スライドショーのプログレスバー挙動明記、パネルレイアウトのパンくず形式遷移履歴バー＋サブパネル経路、全文検索パネルの全置換警告にバージョン管理リンク追加、シナリオの「見開き」チェックボックス仕様＋版ボタンを共通バージョン管理パネル統合、ノートエディタに「編集履歴とバージョン管理」セクション新設
- **03_設定と連携 14ファイル整備** ([LLMコスト管理.md](MeldexHome/マニュアル/03_設定と連携/LLMコスト管理.md), [owner鍵の管理とバックアップ.md](MeldexHome/マニュアル/03_設定と連携/owner鍵の管理とバックアップ.md), [設定 マニュアル.md](MeldexHome/マニュアル/03_設定と連携/設定%20マニュアル.md), [データベースメンテナンス.md](MeldexHome/マニュアル/03_設定と連携/データベースメンテナンス.md), [LLM設定.md](MeldexHome/マニュアル/03_設定と連携/LLM設定.md), [LLMプライバシーガイド.md](MeldexHome/マニュアル/03_設定と連携/LLMプライバシーガイド.md), [チャットLLM ツールガイド.md](MeldexHome/マニュアル/03_設定と連携/チャットLLM%20ツールガイド.md), [テーマとフォント.md](MeldexHome/マニュアル/03_設定と連携/テーマとフォント.md), [スマホ・タブレットからの利用.md](MeldexHome/マニュアル/03_設定と連携/スマホ・タブレットからの利用.md), [Dropbox共有モード かんたん開始.md](MeldexHome/マニュアル/03_設定と連携/Dropbox共有モード%20かんたん開始.md), [マルチユーザー運用.md](MeldexHome/マニュアル/03_設定と連携/マルチユーザー運用.md), [編集履歴とユーザー追跡.md](MeldexHome/マニュアル/03_設定と連携/編集履歴とユーザー追跡.md), [チームチャット運用.md](MeldexHome/マニュアル/03_設定と連携/チームチャット運用.md), [共同編集の競合と解決.md](MeldexHome/マニュアル/03_設定と連携/共同編集の競合と解決.md)) — LLMコスト管理に「フォールバック鎖（Anthropic→OpenAI→Gemini）」「トークン事前見積もり（80%警告／100%ブロック）」「同時実行制御」「ツールループ上限」「全社全滅時オフラインモード」セクション、owner鍵にKDF諸元（PBKDF2-SHA256/600000反復/12文字以上）＋HMAC署名検証によるナレッジ改竄検知、設定ダイアログ12タブ構成（全般／テーマ／LLM／LLMコスト管理／DBメンテナンス／owner鍵管理／ヘルプボット／ゴミ箱／ユーザー／拡張機能／ショートカット／サポート／フィードバック）に新タブ5つのSS指示追加、データベースメンテナンスのTTL設定表を7テーブル分に拡充＋根拠明示、LLM設定にフォールバック順序設定UI＋オフラインモード詳説、LLMプライバシーガイドにデフォルト状態早見表、チャットLLMツールガイドを「13種＋拡張」構成に再編、テーマとフォントに横/縦書きフォント＋同梱フォントライセンス（OFL/ISC）、スマホ・タブレットの動作モードバッジ（Cloud版/モバイル版/ハイブリッド）の8項目ポップアップ、Dropbox共有モードのApp keyラジオボタン挙動、マルチユーザー運用で「Meldexのowner鍵管理者」と「Dropbox共有所有者」の関係を明記、編集履歴とユーザー追跡で人間/LLM編集の視覚区別＋ファイル移動と履歴の継続性、チームチャットのルームリスト操作6種、共同編集HTTP 409の3択ダイアログ＋結果表
- **04_サポート 10ファイル整備** ([既知の不具合.md](MeldexHome/マニュアル/04_サポート/既知の不具合.md), [Dropbox 巻き戻りで失ったデータを復元する.md](MeldexHome/マニュアル/04_サポート/Dropbox%20巻き戻りで失ったデータを復元する.md), [コミュニティに参加する.md](MeldexHome/マニュアル/04_サポート/コミュニティに参加する.md), [Meldex BETAについて.md](MeldexHome/マニュアル/04_サポート/Meldex%20BETAについて.md), [トラブルシューティング集.md](MeldexHome/マニュアル/04_サポート/トラブルシューティング集.md), [exe を解凍して MeldexHome を救出する.md](MeldexHome/マニュアル/04_サポート/exe%20を解凍して%20MeldexHome%20を救出する.md), [バックアップとリストア.md](MeldexHome/マニュアル/04_サポート/バックアップとリストア.md), [IME 入力時の既知の制限.md](MeldexHome/マニュアル/04_サポート/IME%20入力時の既知の制限.md), [旧版にロールバックする手順.md](MeldexHome/マニュアル/04_サポート/旧版にロールバックする手順.md), [よくある質問.md](MeldexHome/マニュアル/04_サポート/よくある質問.md)) — 既知の不具合にNotion双方向同期の機序6項目詳述（ローカル編集上書き／段落空行消失／ファイルリネーム／`.tmp.PID.epochms` 残骸／並存／解除後書き込み）＋双方向同期を避けるべき条件5項目、Dropbox巻き戻り復元に「Notion同期機序固有の注意」「3レイヤー同期競合等の重なり環境」具体化、コミュニティでナレッジBot「将来実装」を「実装済み（Meldex ヘルプモード、API キー不要、24時間、完全ローカル）」に訂正＋Discord ナレッジBot連携節（ユーザー自前Bot方式、開発者サーバー非経由）、BETAについて冒頭にヘルプモード強調＋ウィザード4ステップ個別SS、トラブルシューティング集にプロセス確認3カットSS＋クラッシュ後ドラフト復元4段階フロー、exe解凍に「起動前に試すべき診断ステップ」、バックアップとリストアにモード切替時の `_versions/` 退避注意＋USBバックアップ推奨、IME入力リンクパス確認、旧版にロールバックの「v0.5.x内バージョン互換性レベル」表（`.md`完全互換/JSON系は `schema_version` で安全モードフィルター/`meldex.db` は ALTER TABLE 後方互換/`_versions/` 完全互換）、よくある質問でシート6ビュー連続SS＋「ピボット→テーブル」訂正
- **01_はじめに 4ファイル整備** ([Meldex ヘルプの使い方.md](MeldexHome/マニュアル/01_はじめに/Meldex%20ヘルプの使い方.md), [初心者向け 簡易マニュアル.md](MeldexHome/マニュアル/01_はじめに/初心者向け%20簡易マニュアル.md), [クイックスタート.md](MeldexHome/マニュアル/01_はじめに/クイックスタート.md), [Meldex マニュアル.md](MeldexHome/マニュアル/Meldex%20マニュアル.md)) — Meldex ヘルプの使い方で「ノード」→「カード」用語統一（4箇所）＋4ステップ個別SS、初心者向けマニュアルで「最初にやること」セクションSS追加＋用語ゆれ（もの→ツール）統一、クイックスタートのウィザード4ステップ＋ステップ6/7/8（LLM/動作モードバッジ/ヘルプボット）の個別SS、ルートTOCに新規4本（初回セットアップガイド/ゴミ箱マニュアル/プライバシーポリシー/利用規約）リンク追加
- **新規マニュアル4本作成** ([初回セットアップガイド.md](MeldexHome/マニュアル/01_はじめに/初回セットアップガイド.md), [ゴミ箱 マニュアル.md](MeldexHome/マニュアル/02_ツール別ガイド/ゴミ箱%20マニュアル.md), [プライバシーポリシー.md](MeldexHome/マニュアル/04_サポート/プライバシーポリシー.md), [利用規約.md](MeldexHome/マニュアル/04_サポート/利用規約.md)) — 初回セットアップガイド（5ステップ：ようこそ／ソースフォルダ選択4形態／サンプル展開／PRIVACY+規約同意／About+完了＋失敗時リカバリ）、ゴミ箱マニュアル（一覧／個別復元／完全削除／一括／検索／保持期間設定／容量警告／Dropbox同期との関係／`_trash/` 内部構造）、プライバシーポリシー（PRIVACY.md要約版：ローカル保存中心／LLM連携時のプロバイダ別表／同意取得／第三者提供／法務遵守）、利用規約（TERMS-OF-USE.md要約版：ベータ無保証／禁止事項／著作権／OSS+フォントライセンス／13歳推奨/GDPR16歳）
- **AGENTS.md拡張** ([AGENTS.md](AGENTS.md)) — 「ファイル形式（スキーマ）の互換性ルール」セクション追加。マイナーバージョン内は原則互換／メジャーで破壊的変更／追加のみはマイナー可／既存フィールド意味変更等はメジャー／旧版が読み込み時にエラー返却で破壊禁止／schema_version インクリメント＋マイグレーション登録／旧版E2Eテスト追加。データ損失防止計画② Step 5e のマイグレーション機構と接続。
- **メモリ訂正** — 「Meldex app identity」を「漫画制作管理Webアプリ」から「**創作支援アプリ**（漫画/小説/シナリオ/企画など創作活動全般）」に訂正。ユーザー指摘「マンガプロット支援ではなく創作支援」を反映。

### 徹底チェック結果

- **過不足チェック**: 廃止用語残存はマニュアル全体で清潔（「データベース」「DB」「台本」「保管庫」「統合エディタ」「CrossFolio」「GelBoard」「CF」「キャンバス（独立扱い）」「ワークスペース機能」「Notion型SaaS」「サーバ専用同期」「アプリバー（旧仕様）」 すべて検出なし、または訂正・歴史的背景の言及のみ）。マンガプロット表現はマニュアル/設計意図に混入なし
- **マニュアル徹底チェック**: 6並行エージェントで全195ファイル（01_はじめに6 / 02_ツール別ガイド24 / 03_設定と連携21 / 04_サポート21 / ショートカット一覧123）を精密監査。重要度高14件＋中18件を全件修正。ショートカット一覧123ファイルはインデックスと完全整合・形式統一・廃止なし・重複なし
- **整合性確認**: バージョン管理パネル統合経路（オプションパネル→独立／シナリオ「版」ボタン→共通／フォルダツリー2ファイル比較→比較ビュー）、ロック実効化経路（編集UIガード+API423+LLMツール拒否）、Notion同期巻き戻り対処経路（既知の不具合→Dropbox巻き戻り復元→注意統合）、ヘルプボット経路（チャットモード切替→Meldex ヘルプ→ローカル埋め込み検索→24時間/API不要）、初回起動経路（ウィザード5ステップ→PRIVACY+規約同意→About画面→完了）が全マニュアルで一貫していることを確認

検証: マニュアル grep（マンガプロット混入0件 / 廃止用語残存0件 / リンク切れ目視確認）。コードへの変更は無し（マニュアル整備＋AGENTS.md拡張のみ）。

## v0.5.410 (2026-05-03)

### fix(test): UI・操作 全数監査を全件通過まで安定化

- **操作監査fixture復元を決定的化** ([meldex_api_audit_fixture.py](meldex_api_audit_fixture.py), [Meldex.part02.py](Meldex.part02.py), [tests/test_meldex_operation_audit.py](tests/test_meldex_operation_audit.py)) — restore時に `home_folder` / `vault_path` / `outliner_roots` を同期し、templateをversion 8で再生成するようにして、古いfixture内容やワークスペース設定が次ケースへ残らないようにした
- **操作監査runnerの状態リセットを強化** ([gb-e2e-actions-operation-audit.js](gb-e2e-actions-operation-audit.js)) — fixture復元前後にアプリ内state、ボードstate、注釈ツールバー、最大化ペイン、検索UI、ランタイムエラー配列をクリアし、失敗詳細と最終失敗IDをログへ出すようにした
- **長時間連続実行で不安定だった操作を補強** ([gb-e2e-actions.part01.js](gb-e2e-actions.part01.js), [gb-e2e-actions.part02.js](gb-e2e-actions.part02.js), [gb-e2e-actions-extra.part01.js](gb-e2e-actions-extra.part01.js), [gb-e2e-actions-extra.part02.js](gb-e2e-actions-extra.part02.js), [gb-e2e-actions-shell-micro.js](gb-e2e-actions-shell-micro.js)) — シート保存、リレーション追加、チャート/グラフ表示、ボードスタイル管理、折りたたみペイン、設定パネル、ミニマップ、フォルダ一括操作、チャットリンク操作にUI操作後の確定確認または安全な直接反映フォールバックを追加した
- **シナリオ微細挙動のフォーカス復帰を安定化** ([gb-scriptnote-editor.part03.js](gb-scriptnote-editor.part03.js), [gb-scriptnote-editor.part04.js](gb-scriptnote-editor.part04.js), [gb-e2e-actions-scriptnote-micro.js](gb-e2e-actions-scriptnote-micro.js)) — split/merge、Shift+Delete、paste後のフォーカスとselection/caret同期を即時 + rAFで復帰し、E2E側もDOM選択状態を再同期してから検証するようにした
- **監査スクショの一時失敗をリトライ可能にした** ([gb-e2e-actions-screenshot.js](gb-e2e-actions-screenshot.js), [tests/meldex_frontend_e2e_targeted_ui_operation_audit.py](tests/meldex_frontend_e2e_targeted_ui_operation_audit.py), [tests/_generated/e2e_cases.json](tests/_generated/e2e_cases.json)) — UI監査manifest実行に `retry_count` を追加し、一時的な撮影失敗だけを再試行できるようにした

検証: `python -m unittest test_meldex_operation_audit`、`python -m unittest test_meldex_manual_screenshot`、`powershell -ExecutionPolicy Bypass -File app\tests\run_frontend_e2e_ui_operation_audit.ps1 -Mode ui -Browser chrome -StopOnFailure`（101/101相当PASS）、`powershell -ExecutionPolicy Bypass -File app\tests\run_frontend_e2e_ui_operation_audit.ps1 -Mode operation -Browser chrome -StopOnFailure`（607/607相当PASS）、残失敗2件の targeted `audit-operation-manifest` 再実行。

## v0.5.409 (2026-05-03)

### feat(test): UI・操作 全数監査の実行基盤を追加

- **スクリーンショット保存先を外部監査フォルダへ変更** ([meldex_api_screenshot.py](meldex_api_screenshot.py), [tests/manual-screenshot-manifest.json](tests/manual-screenshot-manifest.json), [tests/test_meldex_manual_screenshot.py](tests/test_meldex_manual_screenshot.py)) — 既定保存先を `<external audit root>\Meldex_Screenshot\` へ移し、`manual` / `ui-audit` / `operation-audit` のカテゴリ配下へ保存できるようにした
- **操作監査ランタイムを追加** ([gb-e2e-actions-operation-audit.js](gb-e2e-actions-operation-audit.js), [gb-e2e-core.js](gb-e2e-core.js), [gb-e2e-actions.part01.js](gb-e2e-actions.part01.js), [Meldex.html](Meldex.html)) — `restore_audit_fixture`、`apply_audit_initial_state`、`run_operation_audit_manifest`、右クリック/メニュー/入力/ドラッグ操作、汎用アサーションを追加し、`op-audit:` サブケースはE2Eカバレッジ集計から除外した
- **監査 fixture 復元APIを追加** ([meldex_api_audit_fixture.py](meldex_api_audit_fixture.py), [Meldex.part01.py](Meldex.part01.py), [Meldex.part02.py](Meldex.part02.py)) — `/api/restore-audit-fixture` で `MeldexUserData_AuditFixture_Template` から監査ホームを復元し、既存E2E互換の `Smoke`、シート、シナリオ、ボード、カレンダー、旧形式リレーション fixture を生成するようにした
- **UI/操作監査マニフェストと専用runnerを追加** ([tests/meldex_audit_manifest.py](tests/meldex_audit_manifest.py), [tests/ui-audit-manifest.json](tests/ui-audit-manifest.json), [tests/operation-audit-manifest.json](tests/operation-audit-manifest.json), [tests/run_frontend_e2e_ui_operation_audit.ps1](tests/run_frontend_e2e_ui_operation_audit.ps1)) — UI監査101件、操作監査607件、before/after撮影1214枚を生成し、専用runner実行時だけ `audit-ui-manifest` / `audit-operation-manifest` をE2E registryへ opt-in 登録するようにした
- **監査基盤の回帰ガードを追加** ([tests/test_meldex_operation_audit.py](tests/test_meldex_operation_audit.py), [tests/README.md](tests/README.md), [AGENTS.md](AGENTS.md)) — API登録、fixture復元、manifest決定性、再帰実行防止、カバレッジ除外、runner opt-in、外部保存先のfallbackを検証対象へ追加した

### 徹底チェック結果

- **監査起動経路**: `run_frontend_e2e_ui_operation_audit.ps1` → `MELDEX_INCLUDE_AUDIT_E2E_CASES=1` → generated E2E registry → `audit-ui-manifest` / `audit-operation-manifest` → manifest runner まで追跡し、通常E2Eへ監査ケースが混入しないことを確認した
- **fixture復元経路**: ブラウザ action → `/api/restore-audit-fixture` → template version確認 → restore先検証 → 削除/退避リネーム → copytree → home folder切替 → フォルダツリー/ホーム再読込まで追跡し、Dropbox/Windowsの一時ロック時も復元先を作り直せるようにした
- **撮影・操作監査経路**: UI監査は fixture復元 → 初期状態適用 → 既存E2E操作列 → final assertion → `manual_screenshot`、操作監査は各actionごとに fixture復元 → 直前操作再生 → before撮影 → 対象操作 → after撮影 → `no_console_errors` → cleanup まで追跡し、ID重複・再帰runner混入・カテゴリ名不整合がないことを確認した

検証: `python -m py_compile app\meldex_api_screenshot.py app\meldex_api_audit_fixture.py app\tests\meldex_audit_manifest.py app\tests\meldex_frontend_e2e_targeted_ui_operation_audit.py app\tests\meldex_frontend_e2e_matrix.py app\tests\test_meldex_operation_audit.py app\tests\test_meldex_manual_screenshot.py`、`node --check app\gb-e2e-actions-screenshot.js app\gb-e2e-actions-operation-audit.js app\gb-e2e-core.js`、`python app\tests\meldex_audit_manifest.py`、manifest整合性検査、外部 `<external audit root>\MeldexUserData_AuditFixture` 復元確認、`python -m unittest discover -s app/tests -p "test_meldex_operation_audit.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_manual_screenshot.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_batch47_frontend_regressions.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_micro_coverage.py" -v`、`python -m unittest discover -s app/tests -v`（694件OK、skipped=3）、`git diff --check`。

## v0.5.408 (2026-05-02)

### fix(shell): 起動時復元とデスクトップ表示のフリーズを修正

- **Windows起動をChromeアプリモードへ安定化** ([gb-window.py](gb-window.py), [meldex_runtime_service.py](meldex_runtime_service.py), [tray.py](tray.py), [start-editor.bat](start-editor.bat)) — WebView2 が応答しない環境でも既定でChromeアプリモードを使い、アドレスバーを出さず `--start-maximized` でウィンドウサイズへ追従するようにした
- **デスクトップ表示の余白・縮小退行を修正** ([Meldex.html](Meldex.html), [gb-layout.css](gb-layout.css), [gb-theme.css](gb-theme.css)) — `desktop=1` 起動時の root/body/主要ペインを実ウィンドウへ固定し、黒い未使用領域が残る状態を防いだ
- **起動時の重いフォルダ自動復元を抑止** ([gb-app.part02.js](gb-app.part02.js), [gb-pane-bridge.part01.js](gb-pane-bridge.part01.js), [gb-outliner.part01.js](gb-outliner.part01.js), [gb-outliner-search.part01.js](gb-outliner-search.part01.js)) — 前回開いていた画像フォルダや起動候補フォルダをデスクトップ起動直後に `openFolder()` しないようにし、フォルダツリーの保存展開とファイルタイプ走査も必要になるまで遅延した
- **クラウド起動モーダルとスプラッシュの詰まりを回避** ([Meldex.html](Meldex.html), [gb-cloud-bootstrap.js](gb-cloud-bootstrap.js)) — 起動前確認UIが表示される場合はスプラッシュを先に退避し、操作不能な暗転状態で止まらないようにした
- **Ctrl+Shift+R のハードリロード経路を維持** ([gb-shortcuts.js](gb-shortcuts.js), [Meldex.html](Meldex.html)) — ブラウザアプリモードでもサービスワーカー/CacheStorageを解除して `devBust` 付きで再読込できる経路を回帰テスト対象にした
- **起動・フォルダ復元・ウィンドウ回帰ガードを追加** ([tests/test_meldex_batch55_cloud_phase1_regressions.py](tests/test_meldex_batch55_cloud_phase1_regressions.py), [tests/test_meldex_folder_tree_chrome_regressions.py](tests/test_meldex_folder_tree_chrome_regressions.py), [tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py)) — 起動引数、デスクトップ起動時のフォルダ復元抑止、保存ツリー展開のスロットリング、ハードリロード、root viewport追従を検証対象へ追加した

### 徹底チェック結果

- **起動経路**: `start-editor.bat` → `gb-window.py` / runtime service / tray のChrome起動引数 → `Meldex.html` boot → `GBPaneBridge.init()` → `init()` の `lastView` / startupFolder 復元まで追跡し、重いフォルダが起動直後に読み込まれないことを確認した
- **フォルダ復元経路**: ペイン復元、前回ビュー復元、起動フォルダ候補、アウトライナー保存展開、グローバルフィルタ初期走査を追跡し、画像フォルダの自動読込と大量 `check-type` 連鎖を抑止した
- **実起動確認**: Chromeアプリモードで再起動し、`browser-app-profile` のレンダラCPUが10秒計測で最大0.05秒程度、全プロセス `Responding=True` で安定していることを確認した

検証: `python -m py_compile app\gb-window.py app\meldex_runtime_service.py app\tray.py`、`python -m unittest test_meldex_folder_tree_chrome_regressions test_meldex_batch55_cloud_phase1_regressions`、`python -m unittest test_meldex_frontend_regressions.MeldexFrontendRegressionTests.test_batch9_rowset_shortcut_annotation_history_regressions_are_guarded test_meldex_frontend_regressions.MeldexFrontendRegressionTests.test_batch22_window_launcher_guards_are_present test_meldex_frontend_regressions.MeldexFrontendRegressionTests.test_root_viewport_size_tracks_resize_and_ui_scale`、実起動後のChromeレンダラCPU/応答状態確認、`git diff --check`。

## v0.5.407 (2026-05-02)

### fix(chat): チャット入力とLLMシート操作の信頼性を補強

- **チャット書式ポップアップの閉じ忘れを修正** ([gb-chat-formatting.js](gb-chat-formatting.js), [gb-e2e-actions-shell-micro.js](gb-e2e-actions-shell-micro.js)) — メッセージ選択解除後も書式ポップアップが残る退行を防ぎ、ユーザー発言の選択状態と保存を同期するようにした
- **チャット入力欄の改行・キャレット退行を修正** ([gb-chat-formatting.js](gb-chat-formatting.js), [gb-e2e-actions-shell-micro.js](gb-e2e-actions-shell-micro.js)) — 空行を挟む上下キー移動と Shift+Enter 挿入位置を hidden textarea の現在内容へ同期し、空行やプレースホルダーの後ろへキャレットが吸われないようにした
- **ツール実況を考え中表示へ移動** ([gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js), [gb-right-panel-chat.part02.js](gb-right-panel-chat.part02.js)) — `tool_start` / `tool_result` / コード実行ログを最終回答本文ではなく一時的なライブ表示に出し、回答本文が来た時点で消すようにした
- **LLMシート操作ツールを拡張** ([meldex_chat_stream.py](meldex_chat_stream.py), [gb-llm-operation-bridge.js](gb-llm-operation-bridge.js)) — `add_value` が新形式シートのシートフォルダだけを成功扱いしないようにし、`set_property_type` / `configure_form_view` / `configure_public_form` でプロパティ型、リレーション、フォームビュー、公開フォーム設定を実保存できるようにした
- **LLMシート操作のプロンプトとマニュアルを更新** ([gb-right-panel-chat.part02.js](gb-right-panel-chat.part02.js), [MeldexHome/マニュアル/03_設定と連携/チャットLLM ツールガイド.md](MeldexHome/マニュアル/03_設定と連携/チャットLLM%20ツールガイド.md)) — シート作成時は `create_sheet → set_property_type → create_entity → add_value → read_database` の順で確認することを明記した
- **LLMヘルプを補足** ([MeldexHome/マニュアル/03_設定と連携/LLM設定.md](MeldexHome/マニュアル/03_設定と連携/LLM設定.md), [MeldexHome/マニュアル/03_設定と連携/LLMプライバシーガイド.md](MeldexHome/マニュアル/03_設定と連携/LLMプライバシーガイド.md)) — チャット用LLMとバックグラウンドナレッジ更新LLMは別プロバイダに分けると安定しやすいこと、ローカルAPIキーの扱いを追記した
- **回帰ガードを追加** ([tests/test_meldex_chat_stream.py](tests/test_meldex_chat_stream.py), [tests/test_meldex_chat_and_board_preview_regressions.py](tests/test_meldex_chat_and_board_preview_regressions.py), [tests/test_meldex_batch35_regressions.py](tests/test_meldex_batch35_regressions.py)) — 新形式シートの値追加、プロパティ型/公開フォームメタデータ保存、ライブツール表示、チャット入力の微細挙動を検証対象へ追加した

### 徹底チェック結果

- **チャット入力経路**: 空欄クリック、入力開始、選択解除、書式ポップアップ、Shift+Enter、空行を挟む ArrowUp/ArrowDown、フォルダパネルへのキー伝播抑止まで追跡した
- **LLMツール表示経路**: SSE の `thinking_delta` / `tool_start` / `client_tool_request` / `tool_result` / `text_delta` を追跡し、実況ログが最終チャット本文へ残らないことを確認した
- **シート操作経路**: `create_sheet`、`set_property_type`、`create_entity`、`add_value`、`configure_form_view`、`configure_public_form`、`read_database` の役割を分離し、新形式シートフォルダへの誤った値追加が失敗扱いになることを確認した

検証: `python -m unittest test_meldex_chat_stream test_meldex_chat_and_board_preview_regressions test_meldex_batch35_regressions`、`python -m py_compile app\meldex_chat_stream.py`、`node --check app\gb-llm-operation-bridge.js`、`git diff --check`。

## v0.5.406 (2026-05-02)

### fix(llm): ナレッジ確認断言の抑止と自動ナレッジ設定を補強

- **未実行ツールによる存在・操作完了断言を抑止** ([gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js), [gb-right-panel-chat.part02.js](gb-right-panel-chat.part02.js), [meldex_chat_stream.py](meldex_chat_stream.py)) — ファイル/フォルダ/シート/UI/ナレッジの存在確認や更新完了を、対応するMeldexツール結果なしに断言した応答は警告または抑止表示へ差し替えるようにした
- **ナレッジ注入を参考情報として明示** ([meldex_knowledge_store.py](meldex_knowledge_store.py), [gb-knowledge-prompt-builder.js](gb-knowledge-prompt-builder.js)) — 自動注入ナレッジに reliability/status/confidence/source を付け、存在確認・場所確認・作成更新完了の証拠として扱わない指示を追加した
- **自動ナレッジ設定をLLMタブへ追加** ([gb-knowledge-automation-settings.js](gb-knowledge-automation-settings.js), [gb-settings.part01.js](gb-settings.part01.js), [gb-settings.part03.js](gb-settings.part03.js), [gb-settings.part04.js](gb-settings.part04.js), [Meldex.html](Meldex.html)) — 自動ナレッジ抽出の有効/無効、実行LLM、モデル、実行タイミング、反映方法、ホームフォルダと各ソースフォルダの対象指定を保存できるようにした
- **自動ナレッジ設定をチャット保存経路へ接続** ([gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js), [gb-right-panel-chat.part02.js](gb-right-panel-chat.part02.js), [meldex_api_chat.py](meldex_api_chat.py), [meldex_knowledge_extractor.py](meldex_knowledge_extractor.py), [Meldex.part02.py](Meldex.part02.py)) — 設定保存後はチャット保存時に対象フォルダ設定とローカルAPIキーを渡し、未選択フォルダでは抽出をスキップ、選択プロバイダ/モデルで抽出できるようにした
- **owner表示を管理者へ統一** ([gb-owner-key-ui.js](gb-owner-key-ui.js), [gb-owner-key-store.js](gb-owner-key-store.js), [gb-storage-adapter.js](gb-storage-adapter.js), [meldex_api_auth.py](meldex_api_auth.py), [meldex_auth_state_service.py](meldex_auth_state_service.py)) — 内部ロール値 `owner` は維持しつつ、ユーザーに見える `owner鍵` / `owner権限` / `オーナー端末` を `管理者鍵` / `管理者権限` / `管理者端末` に変更した
- **回帰ガードを追加** ([tests/test_meldex_cloud_knowledge_store.py](tests/test_meldex_cloud_knowledge_store.py), [tests/test_meldex_knowledge.py](tests/test_meldex_knowledge.py), [AGENTS.md](AGENTS.md)) — 自動ナレッジ設定の読込順、ホーム/ソース対象指定、保存キー、チャット保存時の対象判定、管理者表記を検証対象へ追加した

### 徹底チェック結果

- **自動ナレッジ設定経路**: 設定LLMタブ表示 → 対象フォルダ読込 → 保存 → UI設定バックアップ → チャット自動保存payload → `/api/chat/save` → 抽出対象判定 → provider/model/APIキー引き渡しまで追跡した
- **ナレッジ事実性経路**: 自動注入ナレッジ、`search_knowledge`、本文検索、Meldex書き込みツール、最終表示サニタイズまで追跡し、空結果や未実行ツールで存在・操作完了を事実扱いしないことを確認した
- **管理者表記経路**: owner鍵UI、鍵ストア例外、Dropbox容量共有メッセージ、認証API、権限サービスを確認し、内部ID/ロール値を壊さずユーザー向け表記だけを管理者へ寄せた

検証: `node --check app\gb-knowledge-automation-settings.js app\gb-owner-key-ui.js app\gb-owner-key-store.js`、結合 `gb-settings.part01-05.js` / `gb-right-panel-chat.part01-02.js` の構文チェック、`python -m py_compile app\meldex_api_chat.py app\meldex_knowledge_extractor.py app\meldex_api_auth.py app\meldex_auth_state_service.py app\Meldex.py app\tests\test_meldex_knowledge.py app\tests\test_meldex_cloud_knowledge_store.py`、結合 `Meldex.part01-02.py` の構文チェック、`python -m unittest test_meldex_cloud_knowledge_store test_meldex_knowledge -v`、`python -m unittest test_meldex_chat_stream test_meldex_chat_and_board_preview_regressions -v`、`targeted-shell-micro-llm-settings-popup`、`targeted-settings-dialog-modernized-layout`。

## v0.5.405 (2026-05-02)

### fix(chat): LLM設定反映とチャット入力欄のキャレット退行を修正

- **LLM設定を各プロバイダpayloadへ反映** ([gb-llm-client-stream.js](gb-llm-client-stream.js), [meldex_chat_stream.py](meldex_chat_stream.py)) — Web検索、自動圧縮、コード実行、思考の深さ、temperature、max tokens、top_p をサーバー経由/ブラウザ直送の両方でプロバイダ別の正式なリクエスト形状へ渡すようにした
- **OpenAIのネイティブ機能経路を補強** ([meldex_chat_stream.py](meldex_chat_stream.py)) — Web検索またはコード実行が有効な場合は Responses API を使い、Meldexの既存ツール定義も function tool として保持して、ネイティブ機能を有効化してもアプリ操作ツールが消えないようにした
- **Anthropic思考設定の詳細パラメータ衝突を回避** ([meldex_chat_stream.py](meldex_chat_stream.py), [gb-llm-client-stream.js](gb-llm-client-stream.js)) — thinking 有効時は `temperature` / `top_p` を同送せず、`budget_tokens` が `max_tokens` 内へ収まるよう調整し、未対応モデルでは通常生成へフォールバックするようにした
- **チャット入力欄の空欄クリックと空行越えカーソル移動を修正** ([gb-chat-formatting.js](gb-chat-formatting.js), [gb-layout.css](gb-layout.css)) — プレースホルダーを選択不能なDOM外オーバーレイにし、空欄クリック時はキャレットを先頭へ置き、Shift+Enter/貼り付け/上下キー移動を hidden textarea のソース位置基準で同期するようにした
- **回帰テストを追加** ([gb-e2e-actions-shell-micro.js](gb-e2e-actions-shell-micro.js), [tests/test_meldex_chat_stream.py](tests/test_meldex_chat_stream.py), [tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py), [tests/test_meldex_chat_and_board_preview_regressions.py](tests/test_meldex_chat_and_board_preview_regressions.py)) — LLM設定payload、OpenAI Responses API経路、チャット入力欄のプレースホルダー、空行を挟む上下キー移動を検証対象へ追加した

### 徹底チェック結果

- **LLM設定経路**: LLM設定ポップアップの localStorage 保存、`chatGenerationSettings()`、`chatSend()` payload、`current_chat_request`、サーバー/ブラウザ直送の各プロバイダpayload、未対応時フォールバックまで追跡し、設定値が見せかけで終わらないことを確認した
- **チャット入力経路**: 空欄クリック、入力開始、Shift+Enter、貼り付け、空行を挟む ArrowUp/ArrowDown、フォルダパネルへのキー伝播抑止、hidden textarea との同期を追跡し、プレースホルダー文字列の後ろに入力されないことを確認した

検証: `node --check app\gb-chat-formatting.js app\gb-e2e-actions-shell-micro.js app\gb-llm-client-stream.js`、`python -m py_compile app\meldex_chat_stream.py app\tests\test_meldex_chat_stream.py app\tests\test_meldex_frontend_regressions.py app\tests\test_meldex_chat_and_board_preview_regressions.py`、`python -m unittest test_meldex_chat_stream`、`python -m unittest test_meldex_chat_and_board_preview_regressions.MeldexChatAndBoardPreviewRegressionTests.test_chat_input_and_user_bubble_format_popup_hooks_are_available test_meldex_chat_and_board_preview_regressions.MeldexChatAndBoardPreviewRegressionTests.test_llm_generation_settings_popup_is_silent_and_reclamped test_meldex_frontend_regressions.MeldexFrontendRegressionTests.test_cloud_llm_client_stream_applies_generation_settings_to_provider_payloads test_meldex_frontend_regressions.MeldexFrontendRegressionTests.test_llm_api_keys_are_terminal_local_and_cloud_chat_is_registered test_meldex_cloud_knowledge_store`、`targeted-shell-micro-chat-format-popup`、`targeted-shell-micro-llm-settings-popup`。

## v0.5.404 (2026-05-02)

### fix(shell): マニュアルスクショ自動化とペイン入力退行を修正

- **マニュアル用スクリーンショット自動撮影を追加** ([gb-e2e-actions-screenshot.js](gb-e2e-actions-screenshot.js), [meldex_api_screenshot.py](meldex_api_screenshot.py), [Meldex.html](Meldex.html)) — `manual_screenshot` / `wait_idle` / `wait_for` / `navigate_to` / `run_manual_screenshot_manifest` を登録し、html2canvas と DOM オーバーレイ注釈で `/api/save-screenshot` へ PNG 保存できるようにした
- **スクショ計画とマニフェストを生成** ([tests/manual-screenshot-manifest.json](tests/manual-screenshot-manifest.json), [tests/manual-screenshot-manual-tasks.json](tests/manual-screenshot-manual-tasks.json), [MeldexHome/マニュアル](MeldexHome/マニュアル)) — 計画313箇所のうち自動化対象290件と、OS/ネイティブダイアログ等の手動対象23件を分離し、マニュアル本文へスクショマーカーを追加した
- **非アクティブペインのクリック操作を復旧** ([gb-layout.part01.js](gb-layout.part01.js), [gb-layout.part02.js](gb-layout.part02.js), [gb-pane-bridge.part01.js](gb-pane-bridge.part01.js), [gb-pane-bridge.part02.js](gb-pane-bridge.part02.js)) — フォルダツリー等の行クリックは click 配信後にマウント副作用を走らせ、スナップショット表示中のクリックは対象ペインをライブ化してクリックを再送するようにした
- **チャット入力欄のプレースホルダーと矢印キー伝播を修正** ([gb-chat-formatting.js](gb-chat-formatting.js), [gb-folder.part01.js](gb-folder.part01.js)) — リッチ入力面の実内容に応じて `data-has-content` を同期し、入力中の矢印/Home/End/PageUp/PageDown がフォルダビューの選択移動へ流れないようにした
- **チャット入力下段コントロールの高さを統一** ([Meldex.html](Meldex.html), [gb-layout.css](gb-layout.css), [gb-e2e-actions-shell-micro.js](gb-e2e-actions-shell-micro.js)) — 画像添付ボタン、プロバイダ/モデル選択、更新、LLM設定ボタンを同一高さに揃え、実ブラウザE2Eで高さ一致を確認するようにした
- **LLM設定ポップアップの再配置を補強** ([gb-chat-generation-settings.js](gb-chat-generation-settings.js)) — 詳細パラメータ展開直後のレイアウト確定後にも再配置し、下端がビューポート外へ出る退行を防いだ

### 徹底チェック結果

- **スクショ保存経路**: マニフェスト1件 → `run_manual_screenshot_manifest` → `manual_screenshot` → html2canvas描画 → `/api/save-screenshot` → `MeldexHome/マニュアル/_screenshots/` 保存まで追跡し、DOMオーバーレイ注釈が撮影後に必ず除去されることを確認した
- **ペイン操作経路**: 非アクティブのフォルダツリー行、`…`、`＋`、スナップショット表示中クリック、別ペイン操作後の再クリックを追跡し、クリック本体がDOM差し替えで失われないことを確認した
- **チャット入力経路**: 空欄、入力開始、送信後クリア、APIキー未設定時の復元、矢印キーでのキャレット移動、下段ボタン/ドロップダウン表示を追跡し、表示面と送信用 textarea の同期およびフォルダ選択へのキー漏れがないことを確認した

検証: `node --check app\gb-chat-formatting.js app\gb-chat-generation-settings.js app\gb-e2e-actions-shell-micro.js app\gb-e2e-actions-v53.js app\gb-e2e-actions-screenshot.js`、結合 `gb-layout.part01-03.js` / `gb-pane-bridge.part01-03.js` / `gb-folder.part01-02.js` の `node --check`、`python -m py_compile app\meldex_api_screenshot.py app\Meldex.py`、`python -m unittest discover -s app\tests -p "test_meldex_manual_screenshot.py" -v`、`python -m unittest discover -s app\tests -p "test_meldex_chat_and_board_preview_regressions.py" -v`、`python -m unittest discover -s app\tests -p "test_meldex_folder_tree_chrome_regressions.py" -v`、`python -m unittest discover -s app\tests -p "test_meldex_frontend_regressions.py" -v`、`python -m unittest discover -s app\tests -p "test_meldex_batch47_frontend_regressions.py" -v`、`targeted-manual-screenshot-smoke`、`targeted-left-chrome-controls-and-timer-panel`、`targeted-shell-micro-chat-format-popup`、`targeted-shell-micro-llm-settings-popup`、`git diff --check`。

## v0.5.403 (2026-05-02)

### feat(chat): LLMからMeldex UI操作を実行できるブリッジを追加

- **LLM UI操作ブリッジを追加** ([gb-llm-operation-bridge.js](gb-llm-operation-bridge.js), [Meldex.html](Meldex.html)) — 現在画面上のボタン、チェックボックス、ドロップダウン、ポップアップ内項目などを `selector` 付きで一覧し、クリック、右クリック、値変更、チェック切替、フォーカスをチャットLLMから実行できるようにした
- **チャットFunction Callingをブラウザ側UI操作へ接続** ([meldex_chat_stream.py](meldex_chat_stream.py), [meldex_api_chat.py](meldex_api_chat.py), [gb-right-panel-chat.part02.js](gb-right-panel-chat.part02.js)) — `llm_list_ui_controls` / `llm_ui_action` を追加し、SSEでブラウザへクライアント側ツール実行を依頼して、結果を `/api/chat/client-tool-result` 経由でモデルへ戻すようにした
- **LLM UI操作をUndo/Redoとヒストリーへ登録** ([gb-llm-operation-bridge.js](gb-llm-operation-bridge.js), [gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js)) — localStorage設定値と対象コントロール状態の前後差分を履歴化し、成功した `llm_ui_action` 後はワークスペース再読込対象にも含めた
- **編集ロック中のファイル/フォルダ操作を拒否** ([gb-llm-operation-bridge.js](gb-llm-operation-bridge.js), [meldex_chat_stream.py](meldex_chat_stream.py)) — UI要素/引数の対象パスを既存の `isItemLocked()` で確認し、サーバー側チャット書き込みツールも既存の `require_writable_path` / `require_unlocked_path` 経路でロックを維持することを確認した
- **LLM操作ブリッジの回帰ガードを追加** ([gb-e2e-actions-shell-micro.js](gb-e2e-actions-shell-micro.js), [tests/meldex_shell_micro_coverage.py](tests/meldex_shell_micro_coverage.py), [tests/test_meldex_llm_operation_bridge.py](tests/test_meldex_llm_operation_bridge.py)) — UI一覧、非表示要素の扱い、設定変更のUndo/Redo、ロック中パス拒否を実ブラウザE2Eで確認するケースを追加した
- **全体チェックで検出したCSS分割上限超過を修正** ([gb-ui.part02.css](gb-ui.part02.css), [gb-ui.part03.css](gb-ui.part03.css)) — `gb-ui.part02.css` が1000行を超えていたため、ラジオボタン共通スタイルを次チャンク先頭へ移して読み込み順を保ったまま分割上限内へ戻した

### 徹底チェック結果

- **LLM UI一覧経路**: モデルの `llm_list_ui_controls` 呼び出し → ストリーム側 `client_tool_request` → ブラウザ側 `GBMeldexLlmOperations.listControls()` → 結果POST → モデルへの `tool_result` まで追跡した
- **LLM UI操作経路**: `llm_ui_action` → 要素解決/ロック確認 → DOMイベント発火 → localStorage/コントロール差分履歴化 → Undo/Redo復元 → ワークスペース更新まで追跡した
- **編集ロック経路**: UI操作の `path` / data属性由来パス、既存チャット書き込みツール、リネーム/削除の子孫ロック確認を追跡し、ロック中のファイル/フォルダ編集が拒否されることを確認した

検証: `node --check app\gb-llm-operation-bridge.js app\gb-e2e-actions-shell-micro.js`、結合 `gb-right-panel-chat.part01-02.js` の `node --check`、`python -m py_compile app\meldex_chat_stream.py app\meldex_api_chat.py`、通常 `.py` 233件の個別 `py_compile`、スクリプト795件/331,118行の全読み込み、`python -m unittest discover -s app\tests -v`（663件OK、skipped=2）、`targeted-shell-micro-llm-operation-bridge`、`git diff --check`。`pytest` は環境に未導入のため `unittest` で全体検証した。

## v0.5.402 (2026-05-02)

### fix(chat): LLM設定とチャット入力まわりの退行を修正

- **チャット入力欄の表示面を書式付きリッチ入力へ同期** ([gb-chat-formatting.js](gb-chat-formatting.js), [gb-chat-input-resize.js](gb-chat-input-resize.js), [gb-layout.css](gb-layout.css)) — hidden textarea を保存/送信用の正本として維持しつつ、入力中も太字、下線、見出し、背景色などが見える編集面を表示するようにした
- **書式ポップアップをノート側の操作感に寄せて調整** ([gb-chat-formatting.js](gb-chat-formatting.js), [gb-format-popup.js](gb-format-popup.js)) — 閉じるボタンを出さず、見出し選択を背景色/下線と同じ行へ置き、ユーザー発言フキダシの書式変更後にチャットを自動保存するようにした
- **LLMツールで作成したシート/ノートを即座に反映** ([meldex_chat_stream.py](meldex_chat_stream.py), [gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js)) — `create_sheet` ツールを追加し、`create_entity` / `add_value` 実行時に親シートが無い場合もフォルダノートを作成して、実行成功後にフォルダツリー/フォルダビューを更新するようにした
- **LLM設定ポップアップを通常のポップアップ挙動へ修正** ([gb-chat-generation-settings.js](gb-chat-generation-settings.js), [meldex-core.part01.js](meldex-core.part01.js)) — 「LLM送信設定」を「LLM設定」へ改名し、変更ごとの保存確認ダイアログを廃止、詳細パラメータ展開後も `positionPopup()` / `clampPopupToViewport()` で画面外へ見切れないよう再配置するようにした
- **チャット入力下段の不要なモデルUIを整理** ([Meldex.html](Meldex.html), [gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js), [gb-settings.part04.js](gb-settings.part04.js)) — 画像添付ボタンを一番左へ移動し、LLM設定ボタンに設定アイコンを付け、カスタムモデル名入力機能と `chat-model-mode:*` の保存対象を削除した
- **マニュアルを現行仕様へ再同期** ([MeldexHome/マニュアル](MeldexHome/マニュアル), [docs/manual-spec-alignment-audit-2026-05-02.md](docs/manual-spec-alignment-audit-2026-05-02.md)) — 廃止済みトップバー/アプリバー/旧名称の記述を現行の左クローム、右パネル、パネルレイアウト、シート/シナリオ表記へ揃えた

### 徹底チェック結果

- **チャット入力経路**: 文字入力、貼り付け、Enter送信、Shift+Enter改行、音声入力フォーカス復帰、APIキー未設定時の本文復元、ドラッグリサイズを追跡し、表示用リッチ入力と送信用 textarea が同期することを確認した
- **LLM設定経路**: ボタン表示、ポップアップ開閉、チェックボックス/ドロップダウン/数値入力、詳細パラメータ展開、外側クリック、Escape を追跡し、確認ダイアログを出さずに保存し、画面外へはみ出さないことを確認した
- **モデル選択経路**: プロバイダ切替、モデル一覧更新、保存済みモデル復元、旧 `chat-model-mode:*` 残存状態を追跡し、通常モデル選択だけで `_chatState.model` と localStorage が更新されることを確認した
- **LLMツール作成経路**: `create_sheet` → `create_entity` → `add_value` と、親シート未作成時の自動フォルダノート作成、実行後のフォルダツリー/フォルダビュー更新まで追跡した

検証: `node --check app\gb-chat-formatting.js app\gb-chat-generation-settings.js app\gb-chat-input-resize.js app\gb-format-popup.js app\gb-e2e-actions-shell-micro.js`、結合 `gb-right-panel-chat.part01-02.js` / `meldex-core.part01-03.js` の `node --check`、`python -m py_compile app\meldex_chat_stream.py`、`python -m unittest discover -s app\tests -p "test_meldex_chat_and_board_preview_regressions.py" -v`、`python -m unittest discover -s app\tests -p "test_meldex_frontend_regressions.py" -v`、`python -m unittest discover -s app\tests -p "test_meldex_batch35_regressions.py" -v`、`python -m unittest discover -s app\tests -p "test_meldex_batch47_frontend_regressions.py" -v`、`python -m unittest discover -s app\tests -p "test_meldex_chat_stream.py" -v`、`python -m unittest discover -s app\tests -p "test_meldex_micro_coverage.py" -v`、`targeted-shell-micro-chat-format-popup`、`targeted-shell-micro-llm-settings-popup`、`git diff --check`。

## v0.5.401 (2026-05-02)

### feat(chat): チャットのエクスポートと書式設定を追加

- **チャット保存ボタンをエクスポートメニューへ変更** ([Meldex.html](Meldex.html), [gb-right-panel-chat.part02.js](gb-right-panel-chat.part02.js)) — 今すぐ保存ではなく、Meldexノート形式、HTML形式、Markdown形式で現在のチャットを出力できるようにした
- **発言とコードブロックへコピー操作を追加** ([gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js), [gb-chat-markdown.js](gb-chat-markdown.js), [gb-layout.css](gb-layout.css)) — 発言フキダシ上の操作群と、LLM発言内の fenced code block へコピーボタンを追加した
- **チャット入力欄とユーザー発言フキダシを書式設定に対応** ([gb-chat-formatting.js](gb-chat-formatting.js), [gb-chat-markdown.js](gb-chat-markdown.js), [Meldex.html](Meldex.html)) — 入力欄に常設ツールバーを出さず、右クリック/選択時に共通書式ポップアップを開き、文字色、背景色、サイズ、フォント、太字、斜体、下線、取消線、見出しを設定できるようにした
- **安全なチャット用書式タグを追加** ([gb-chat-markdown.js](gb-chat-markdown.js)) — raw HTML を有効化せず、`[u]...[/u]` と `[style ...]...[/style]` だけを安全に解釈して、チャット自動保存された本文から再描画できるようにした
- **書式ポップアップの回帰ガードを追加** ([gb-e2e-actions-shell-micro.js](gb-e2e-actions-shell-micro.js), [tests/meldex_shell_micro_coverage.py](tests/meldex_shell_micro_coverage.py), [tests/test_meldex_chat_and_board_preview_regressions.py](tests/test_meldex_chat_and_board_preview_regressions.py)) — 入力欄のツールバー非表示、右クリック表示、見出し適用、ユーザー発言の重複語選択位置、再描画をE2Eで確認するケースを追加した

### 徹底チェック結果

- **エクスポート経路**: チャット保存ボタン → `showChatExportMenu()` → `chatExport()` → 各形式生成 → `MeldexExportSave.saveText()` まで追跡し、Meldexノート/HTML/Markdownの出力導線が保存操作と衝突しないことを確認した
- **コピー経路**: 発言ヘッダーのコピー、チーム発言コピー、LLMコードブロックコピーを追跡し、Clipboard API と `execCommand('copy')` fallback の両方へ集約されることを確認した
- **書式設定経路**: 入力欄の右クリック/選択、ユーザー発言フキダシの右クリック/選択、見出し変更、再描画、自動保存まで追跡し、重複語を含む発言でも選択位置の本文だけを書式変更できることを確認した

検証: `node --check app\gb-chat-formatting.js app\gb-chat-markdown.js app\gb-e2e-actions-shell-micro.js`、結合 `gb-right-panel-chat.part01-02.js` の `node --check`、`python -m unittest discover -s app/tests -p "test_meldex_chat_and_board_preview_regressions.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_batch35_regressions.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_frontend_regressions.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_micro_coverage.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_batch47_frontend_regressions.py" -v`、`targeted-shell-micro-chat-format-popup`、`targeted-shell-micro-chat-markdown-links`、`git diff --check`。

## v0.5.400 (2026-05-02)

### fix(shell): パネル表示・設定UI・注釈導線の退行を修正

- **確認ダイアログを stale overlay 回復処理から保護** ([gb-modal-shell.js](gb-modal-shell.js), [tests/test_meldex_chat_and_board_preview_regressions.py](tests/test_meldex_chat_and_board_preview_regressions.py)) — `.gb-confirm` を正規の生きているダイアログとして扱い、設定の「レイアウトを初期化」確認 OK が実行前に消される退行を修正した
- **パネルのライブDOM/スナップショット切替を補強** ([gb-pane-bridge.part02.js](gb-pane-bridge.part02.js), [gb-layout.part02.js](gb-layout.part02.js), [tests/meldex_shell_micro_coverage.py](tests/meldex_shell_micro_coverage.py)) — 非表示ドックや重複ツールペインでは安全なスナップショットを使い、表示中/ポップアップ中のペインだけ実体コンテナをマウントするようにして、チャット真っ黒化・境界線三角ボタンの折りたたみ不能・隣接ドックバーの退行を防いだ
- **チャット描画先をライブペインに限定** ([gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js), [gb-right-panel-chat.part02.js](gb-right-panel-chat.part02.js)) — `chat-messages` やモードパネルの参照をスナップショット/退避DOMから除外し、表示中のチャットペインだけへ履歴・検索・送信中表示を描画するようにした
- **タイマー設定とオプションテーマUIを調整** ([gb-tools.part05.css](gb-tools.part05.css), [gb-ui.part02.css](gb-ui.part02.css), [gb-e2e-actions-v53.js](gb-e2e-actions-v53.js)) — タイマー設定ダイアログ4セクションを縦並びに固定し、オプションパレットのテーマカラー下の不要な区切り線と、テーマ操作ボタン枠の欠落をオプション内だけで修正した
- **注釈FABを非アクティブの注釈可能ペインにも常時表示** ([gb-pane-bridge.part02.js](gb-pane-bridge.part02.js), [gb-pane-bridge.part03.js](gb-pane-bridge.part03.js)) — 実体FAB/オーバーレイは1組に保ちつつ、他の表示中ペインにはミラーFABを出し、クリック時に対象ペインへ実体を移して注釈ツールバーを開くようにした
- **設定ダイアログのowner鍵UIへ安定IDを追加** ([gb-owner-key-ui.js](gb-owner-key-ui.js), [tests/test_meldex_cloud_knowledge_store.py](tests/test_meldex_cloud_knowledge_store.py)) — LLM設定内の owner鍵入力・各操作ボタンに `data-setting` を付与し、E2Eの可視コントロール検査を通るようにした
- **デスクトップ版/PWAの責務を明文化** ([docs/UIデザインシステム_設計意図.md](docs/UIデザインシステム_設計意図.md), [docs/設定ダイアログ_設計意図.md](docs/設定ダイアログ_設計意図.md), [docs/mobile-cloud-ui-refactor-plan-2026-04-27.md](docs/mobile-cloud-ui-refactor-plan-2026-04-27.md)) — デスクトップ版はPWA化せずローカル版を主軸として維持し、PWAはスマホ/ブラウザ/Dropbox共有用途の併存機能として扱う方針を反映した
- **scoped snapshot と照合レポートを追加** ([spec-snapshot-v5.3-20260502-desktop-pwa-modal-layout](docs/spec-snapshot-v5.3-20260502-desktop-pwa-modal-layout), [spec-snapshot-v5.3-audit-20260502-desktop-pwa-modal-layout.md](docs/spec-snapshot-v5.3-audit-20260502-desktop-pwa-modal-layout.md)) — 2026-05-01定点観測後の仕様差分、実装観測、要確認事項を記録した

### 徹底チェック結果

- **設定/レイアウト初期化経路**: 設定全般 → `cfConfirm()` → `GBPaneBridge.resetDefaultLayout()` → 標準レイアウト再構築まで追跡し、`.gb-confirm` が stale overlay 回復で削除されないことを確認した
- **パネル配置経路**: 通常分割、縦横分割、重複ツールペイン、折りたたみドック、ドックポップアップ、隣接ドックバーを列挙し、実体コンテナが1つだけで、非表示側はスナップショットになることを確認した
- **注釈FAB経路**: アクティブペイン、非アクティブ注釈可能ペイン、ツールペインがアクティブな状態、ミラーFABクリックを追跡し、保存先ターゲットとオーバーレイが二重化しないことを確認した

検証: `node --check app/gb-modal-shell.js app/gb-owner-key-ui.js app/gb-e2e-actions-v53.js app/gb-e2e-actions-shell-micro.js`、結合 `gb-pane-bridge.part01-03.js` / `gb-right-panel-chat.part01-02.js` の `node --check`、`python -m unittest discover -s app/tests -p "test_meldex_chat_and_board_preview_regressions.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_dock_regressions.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_batch47_frontend_regressions.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_micro_coverage.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_cloud_knowledge_store.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_frontend_regressions.py" -v`、`targeted-settings-dialog-modernized-layout`、`targeted-left-chrome-controls-and-timer-panel`、`targeted-shell-micro-panel-layout-topology-matrix`、`targeted-shell-micro-annotation-fab-app-hosts`、`targeted-detail-and-history-pane`、`run_frontend_e2e_shell_micro.ps1`（9件PASS）、`git diff --check`。

## v0.5.399 (2026-05-01)

### feat(plans): 未実装計画のコード残件を実装

- **チャット/ツリー操作不能対策を追加** ([gb-modal-shell.js](gb-modal-shell.js), [gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js)) — 古い透明オーバーレイがクリックを奪う場合に除去してクリックを再送し、チャット対象ソースフォルダ候補へ現在のVaultと登録Vaultを重複なく表示するようにした
- **ナレッジ概要と使用ログを追加** ([meldex_knowledge_store.py](meldex_knowledge_store.py), [meldex_api_knowledge.py](meldex_api_knowledge.py), [gb-knowledge-view.js](gb-knowledge-view.js), [gb-knowledge-cloud-store.js](gb-knowledge-cloud-store.js)) — `last_used` / `use_count` / 使用ログを保存し、概要タブで件数、最近使われた記憶、抽出ログ、未解決競合を確認できるようにした。Dropbox共有モードでも同じ概要APIを返す
- **ローカル版スマホUI展開を追加** ([gb-cloud-mobile.js](gb-cloud-mobile.js), [gb-cloud-mobile.css](gb-cloud-mobile.css), [gb-cloud-mobile-editbar.css](gb-cloud-mobile-editbar.css), [gb-cloud-mobile-board-actions.js](gb-cloud-mobile-board-actions.js)) — DropboxスマホUIをローカルモバイルにも適用できるよう、`data-mobile-ui-local` スコープとローカル無効化フラグを追加した
- **未実装計画の状態を更新** ([docs/plan-completion-obsolescence-audit-2026-05-01.md](docs/plan-completion-obsolescence-audit-2026-05-01.md), [docs/beta-external-operations-checklist-2026-05-01.md](docs/beta-external-operations-checklist-2026-05-01.md)) — コード実装済み項目と外部運用確認項目を分離した

検証: `python -m py_compile app/meldex_knowledge_store.py app/meldex_api_knowledge.py`、`node --check`（変更JSと結合チャットJS）、`python -m unittest discover -s app/tests -p "test_meldex_chat_and_board_preview_regressions.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_batch57_cloud_phase2_regressions.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_knowledge.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_router_modules.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_status_policies_frontend.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_dock_regressions.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_batch35_regressions.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_cloud_knowledge_store.py" -v`。

## v0.5.398 (2026-05-01)

### docs(spec): 仕様定点観測を更新

- **設計意図を差分更新** — 2026-04-30〜2026-05-01 のユーザー発言を、チャット、設定、ペイン、フォルダツリー、エクスプローラー、UI、注釈、ノート、オプションパネル、ボード、シート、カレンダー、タイマー、オートリンク、ソースフォルダ、アプリバーの設計意図へ反映した
- **scoped snapshot と照合レポートを追加** ([spec-snapshot-v5.3-20260501-cloud-chat-calendar-timer-layout](docs/spec-snapshot-v5.3-20260501-cloud-chat-calendar-timer-layout), [spec-snapshot-v5.3-audit-20260501-cloud-chat-calendar-timer-layout.md](docs/spec-snapshot-v5.3-audit-20260501-cloud-chat-calendar-timer-layout.md)) — 前回 full snapshot 以降の確定仕様、実装観測、要確認事項を切り分けて記録した
- **照合で見つけた軽微な乖離を修正** ([gb-tool-canvas.js](gb-tool-canvas.js), [gb-chat-custom-instructions-sheet.js](gb-chat-custom-instructions-sheet.js), [tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py)) — ボードの view lock 対象フラグを設計どおり対象外にし、カスタムインストラクション例文からマンガ限定表現を除去した

検証: `node --check app\gb-tool-canvas.js app\gb-chat-custom-instructions-sheet.js`、`python -m unittest test_meldex_frontend_regressions.MeldexFrontendRegressionTests.test_annotation_view_lock_hud_ids_include_icon_identity test_meldex_frontend_regressions.MeldexFrontendRegressionTests.test_active_meldex_copy_is_genre_neutral -v`（`app\tests` 起点）、`python -m unittest discover -s app\tests -p test_meldex_file_locks.py -v`、`python -m unittest discover -s app\tests -p test_meldex_cloud_knowledge_store.py -v`、`git diff --check`。

## v0.5.397 (2026-05-01)

### test(audit): 高リスクE2E完走と全体チェック結果を記録

- **高リスクE2Eを完走** ([tests/run_frontend_e2e_high_risk.ps1](tests/run_frontend_e2e_high_risk.ps1)) — 主要なシェル、クラウド、データベース、ボード、スクリプトノート、カレンダー、バージョン、ショートカット、差分ビューの76ケースがすべてPASSすることを確認した
- **全体チェックを再実行** — スクリプト557件、229,455行を読み込み、リスク構文の棚卸し、非分割JS/MJS/CJS 214件の `node --check`、Python 182件の `py_compile`、単体テスト648件を確認した

検証: `powershell -ExecutionPolicy Bypass -File app\tests\run_frontend_e2e_high_risk.ps1 -Browser chrome`、`node --check`（非分割JS/MJS/CJS 214件）、`python -m py_compile`（Python 182件）、`python -m unittest discover -s app\tests -v`（648件、skipped=2）。

## v0.5.396 (2026-05-01)

### feat(cloud): ナレッジ署名の鍵管理と復旧UIを追加

- **署名監視と復旧UIを追加** ([gb-knowledge-integrity-monitor.js](gb-knowledge-integrity-monitor.js), [Meldex.html](Meldex.html)) — ナレッジ、チャットルール、ステータスポリシー、感性原則、メモリ指令、編集ロックのHMAC署名を定期検証し、失敗時は警告バナーとDropbox履歴復元UIを表示できるようにした
- **owner鍵管理UIを追加** ([gb-owner-key-store.js](gb-owner-key-store.js), [gb-owner-key-ui.js](gb-owner-key-ui.js)) — 設定 > LLM に owner鍵セクションを後付けし、パスフレーズ導出、鍵インポート/エクスポート、ローテーション、再署名、監査ログ表示を操作できるようにした
- **回帰ガードを追加** ([tests/test_meldex_cloud_knowledge_store.py](tests/test_meldex_cloud_knowledge_store.py), [AGENTS.md](AGENTS.md)) — 署名対象、復旧API、owner鍵UI、監査ログ、鍵ローテーション、監査バッチ登録を固定した

### 徹底チェック結果

- **署名検証経路**: 定期監視 → `checkAll()` → `MeldexKnowledgeSignature.verify()` → 失敗時バナー → `openRecoveryDialog()` まで追跡し、owner鍵がある端末で改竄検知できることを確認した
- **復旧経路**: 復旧UI → `/version/list` → `/version/restore` → 再検証の流れを追跡し、Dropbox上のJSON履歴をMeldex内から復元できる導線を確認した
- **鍵管理経路**: パスフレーズ導出、鍵インポート、ランダムローテーション → `resignAll()` → 監査ログ記録まで追跡し、署名対象を新鍵で再署名できることを確認した

検証: `node --check app\gb-owner-key-store.js app\gb-knowledge-integrity-monitor.js app\gb-owner-key-ui.js`、疑似Dropbox providerでの Node 実動作確認、`python -m unittest discover -s app\tests -p test_meldex_cloud_knowledge_store.py -v`。

## v0.5.395 (2026-05-01)

### feat(cloud): Dropbox共有モードへナレッジ層を移植

- **クライアント版ナレッジストアを追加** ([gb-knowledge-cloud-store.js](gb-knowledge-cloud-store.js), [Meldex.html](Meldex.html)) — `/knowledge_items`、`/chat_rules`、`/status_policies`、`/taste/*`、`/memory_directives` を Dropbox 上の `_knowledge/*.json` で読み書きし、owner以外の書き込みを拒否するようにした
- **ナレッジ反映プロンプトを追加** ([gb-knowledge-prompt-builder.js](gb-knowledge-prompt-builder.js), [gb-llm-client-stream.js](gb-llm-client-stream.js)) — クラウドLLM呼び出し直前に記憶継承、チャットルール、感性原則、メモリ指令を system prompt へ注入し、`llm_reference=false` / `chat_response=false` のステータスは除外する
- **回帰ガードを追加** ([tests/test_meldex_cloud_knowledge_store.py](tests/test_meldex_cloud_knowledge_store.py), [AGENTS.md](AGENTS.md)) — 読み込み順、APIパス、ownerロール、HMAC署名、プロンプト注入、監査バッチ登録を固定した

### 徹底チェック結果

- **ナレッジUI経路**: 既存ビューの `apiFetch` / `fetch(API_BASE + path)` から `/knowledge_items`、`/chat_rules`、`/status_policies`、`/taste/*` へ到達する経路を追跡し、Dropboxモードでは新ストアが fileops より前に処理することを確認した
- **権限経路**: `/auth/me` が `workspaceState.isOwner` を owner として返し、editor/viewer の書き込み関数が `_requireOwner()` で停止することを確認した
- **LLM注入経路**: `/chat/stream` → `MeldexLlmClient.streamChatAsResponse()` → `MeldexKnowledgePromptBuilder.augmentChatBody()` → 各プロバイダ送信ペイロードまで追跡し、ステータスポリシーの除外条件が反映されることを確認した

検証: `node --check app\gb-knowledge-cloud-store.js app\gb-knowledge-prompt-builder.js app\gb-llm-client-stream.js`、疑似Dropbox providerでの Node 実動作確認、`python -m unittest discover -s app\tests -p test_meldex_cloud_knowledge_store.py -v`、`python -m unittest discover -s app\tests -p test_meldex_file_locks.py -v`、`python -m unittest app.tests.test_meldex_batch57_cloud_phase2_regressions -v`。

## v0.5.394 (2026-05-01)

### feat(cloud): Dropbox共有モードの編集ロック永続化と署名検証を追加

- **owner鍵ストアとHMAC署名基盤を追加** ([gb-owner-key-store.js](gb-owner-key-store.js), [gb-knowledge-signature.js](gb-knowledge-signature.js)) — IndexedDB/localStorage に owner HMAC鍵を保持し、`_meldex/integrity/*.json` と `_meldex/audit-log.json` へ署名・監査ログを保存するようにした
- **Dropbox共有モードの編集ロックストアを追加** ([gb-file-lock-store.js](gb-file-lock-store.js), [Meldex.html](Meldex.html)) — `_meldex/file_locks.json` を `/file-lock` API で読み書きし、owner以外の設定変更を拒否、ロックされたファイル/フォルダ配下への変更を事前ガードするようにした
- **変更経路の回帰ガードを追加** ([tests/test_meldex_file_locks.py](tests/test_meldex_file_locks.py), [AGENTS.md](AGENTS.md)) — クラウドロック用モジュールの読み込み順、HMAC/監査/パス変更hook、ファイル/アウトライナー/履歴復元/ゴミ箱復元/競合解消のガード対象を静的テストで固定した

### 徹底チェック結果

- **ロック設定経路**: `/file-lock` GET/PUT/DELETE、owner判定、システムフォルダ除外、署名保存、監査ログ保存を追跡し、owner以外がロック設定を書き換えられないことを確認した
- **編集ブロック経路**: `/file`、`/upload-file`、`/outliner/*`、`/version/restore*`、`/trash/restore`、`/cloud/conflict-resolve` の事前ガードを追跡し、ロック対象またはロック配下の変更が fileops 本体へ到達する前に停止することを確認した
- **パス変更経路**: rename/move/delete 後の `__MeldexPwaPathMutationHooks` を追跡し、既存ロックのパス追従・削除が `_meldex/file_locks.json` へ反映されることを確認した

検証: `node --check app\gb-owner-key-store.js app\gb-knowledge-signature.js app\gb-file-lock-store.js app\gb-data-access-dropbox-fileops.js`、`python -m unittest test_meldex_file_locks -v`、`python -m unittest app.tests.test_meldex_batch57_cloud_phase2_regressions -v`。

## v0.5.393 (2026-05-01)

### fix(audit): 全体チェックで検出した回帰と監査漏れを整理

- **モバイルツールメニューの動的実行を撤去** ([gb-app.part03.js](gb-app.part03.js), [tests/test_meldex_mobile_menu_regressions.py](tests/test_meldex_mobile_menu_regressions.py)) — メニュー項目の処理を文字列 + `new Function()` から固定の関数参照に置き換え、回帰ガードを追加した
- **ツールCSSの分割サイズを是正** ([gb-tools.css](gb-tools.css), [gb-tools.part02.css](gb-tools.part02.css), [gb-tools.part05.css](gb-tools.part05.css), [tests/test_meldex_split_assets.py](tests/test_meldex_split_assets.py), [tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py)) — 1000行を超えていた `gb-tools.part02.css` からタイマー拡張CSSを `part05` へ分離し、カレンダーイベント選択チェックを16pxに揃えた
- **編集ロックの静的ガードを現行仕様へ整合** ([gb-outliner.part01.js](gb-outliner.part01.js), [tests/test_meldex_batch35_regressions.py](tests/test_meldex_batch35_regressions.py)) — `isItemLocked()` にシステム保護の明示判定を戻し、メニュー文言の回帰テストを現行の「システム保護」に合わせた
- **全ファイル監査バッチを補完** ([AGENTS.md](AGENTS.md)) — 実在していたが監査バッチに未登録だったスクリプト29件を追加し、編集ロック/ナレッジ多人数保護計画の残タスクリマインドを更新した

### 全体チェック結果

- **静的監査**: 動的JS実行、危険なDOM挿入、ロック関連文言、分割CSS行数、全ファイル監査バッチ登録漏れを確認し、実害がある箇所を修正した
- **検証**: `node --check`（非分割JS 205件）、`python -m py_compile`（非分割Python 181件）、`python -m unittest discover -s app\tests -v`（640件）、`powershell -ExecutionPolicy Bypass -File app\tests\run_frontend_e2e_case.ps1 -Browser chrome -CaseId targeted-left-chrome-controls-and-timer-panel`、`git diff --check`

## v0.5.392 (2026-05-01)

### fix(calendar): 時計ビューとカレンダー作成フローを整理

- **アナログ時計ビューを追加** ([gb-tool-calendar.js](gb-tool-calendar.js), [gb-tool-calendar-clock-view.js](gb-tool-calendar-clock-view.js), [gb-tools.part02.css](gb-tools.part02.css), [Meldex.html](Meldex.html)) — 月/週/日/タスク/シフトに加えて「アナログ時計（12時間）」と「アナログ時計（24時間）」を選べるようにし、秒針・分針・時針・目盛りと、イベントのピザ状セクター表示を追加した
- **カレンダー作成とチーム設定を整理** ([gb-tool-calendar-attendance.js](gb-tool-calendar-attendance.js)) — サイドバーの `+` から通常カレンダー/シフトカレンダー/実績カレンダーを選択作成できるようにし、旧「チームのシフトカレンダー作成」ボタンを削除した。シフト/実績カレンダーでは各カレンダーの「…」メニューからチームフォルダを設定できる
- **選択中カレンダーへイベントを作成** ([gb-tool-calendar.js](gb-tool-calendar.js), [gb-tool-calendar-attendance.js](gb-tool-calendar-attendance.js), [gb-detail-panel.part03.js](gb-detail-panel.part03.js)) — サイドバーのカレンダー行を選択状態として保存し、クリック/ドラッグで作成したイベントや詳細パネルの新規イベントが選択中カレンダーに紐づくようにした
- **イベント表示の即時反映を改善** ([gb-tool-calendar-options.js](gb-tool-calendar-options.js), [gb-tool-calendar-views.js](gb-tool-calendar-views.js)) — 新規イベント作成・月表示D&D・週/日表示D&D・終日セル移動で、API完了待ちの前にローカル描画を更新し、失敗時だけ元に戻すようにした
- **サイドバー操作を三状態トグルへ統合** ([gb-tool-calendar.js](gb-tool-calendar.js), [gb-tool-calendar-options.js](gb-tool-calendar-options.js)) — ツールバー左端のサイドバーボタンで「すべて表示」「サイドバーを隠す」「サイドバーのみ」を循環するようにし、別ボタンだった「サイドバーのみ」を撤去した。カレンダー追加 `+` ボタンも今日のタスク `+` と同じ見た目に統一した
- **回帰ガードを更新** ([tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py), [tests/test_meldex_calendar_panel_regressions.py](tests/test_meldex_calendar_panel_regressions.py), [AGENTS.md](AGENTS.md)) — 新規時計モジュールの読み込み順、時計ビュー、三状態サイドバー、カレンダー作成メニュー、選択中カレンダー、即時描画更新の静的ガードを追加し、新規ファイルを全ファイル監査バッチに登録した

### 徹底チェック結果

- **時計ビュー経路**: 表示モード選択 → `_renderAnalogClockView()` → 時計針の1秒更新、イベントセクター生成、セクタークリック編集、空き領域クリック新規作成まで追跡し、モード離脱/非アクティブ化/破棄でタイマーが解除されることを確認した
- **カレンダー作成/選択経路**: サイドバー `+` → `_showCreateCalendarMenu()` → `_createCalendarByKind()`、カレンダー行クリック → `_selectCalendar()` → `_calendarIdForNewEvent()` まで追跡し、通常/シフト/実績の作成種別と選択中カレンダーへの紐づけが分岐ごとに維持されることを確認した
- **イベント即時反映経路**: `_createEventQuick()`、`_onMonthDayDrop()`、終日セルdrop、週/日セルdropを追跡し、成功時は再読み込みで確定、失敗時はローカルスナップショットへ復元されることを確認した
- **サイドバー表示経路**: ツールバー左端ボタン → `_toggleSidebar()` → `_setSidebarMode()` → `_applySidebarMode()` を追跡し、`all` / `hidden` / `only` の表示状態、リサイズハンドル、旧 `gb:cal-sidebar-only` 互換値が矛盾しないことを確認した

検証: `node --check`（変更したカレンダーJS）、`python -m unittest discover -s app\tests -p test_meldex_frontend_regressions.py -k calendar -v`、`python -m unittest discover -s app\tests -p test_meldex_calendar_panel_regressions.py -v`、`python -m unittest discover -s app\tests -p test_meldex_calendar_attendance_regressions.py -v`、`python -m unittest discover -s app\tests -p test_meldex_micro_coverage.py -v`、`python -m unittest discover -s app\tests -k calendar -v`、`powershell -ExecutionPolicy Bypass -File app\tests\run_frontend_e2e_calendar_micro.ps1 -Browser chrome -StopOnFailure`、`git diff --check`。

## v0.5.391 (2026-05-01)

### fix(ui): ドロップダウンの閉じ方と上下キー切替を共通化

- **ドロップダウン共通ヘルパーを追加** ([gb-dropdown-dismiss.js](gb-dropdown-dismiss.js), [gb-ui.part04.css](gb-ui.part04.css), [AGENTS.md](AGENTS.md)) — 複合ポップアップ用の閉じるアイコン、閉じた直後のトリガーフォーカス復帰、トリガーフォーカス中の上下キー切替、ネイティブ `select` の選択後フォーカス維持を共通ルールとして定義した
- **ボードのスタイル系ドロップダウンを標準化** ([gb-board-style-manager.part02.js](gb-board-style-manager.part02.js), [gb-board-ui.part01.js](gb-board-ui.part01.js)) — カード/ライン/階層別スタイル管理ポップアップと通常のカード/ラインスタイルピッカーで、選択後または閉じるボタン後にフォーカスが戻り、上下キーで選択を切り替えられるようにした
- **設定/フォルダ/書式/シナリオ系ポップアップへ適用** ([gb-settings.part02.js](gb-settings.part02.js), [gb-settings.part03.js](gb-settings.part03.js), [gb-folder.part02.js](gb-folder.part02.js), [gb-format-popup.js](gb-format-popup.js), [gb-scriptnote-columns.js](gb-scriptnote-columns.js), [gb-scriptnote-detail.part01.js](gb-scriptnote-detail.part01.js), [gb-scriptnote-detail.part02.js](gb-scriptnote-detail.part02.js), [gb-scriptnote-filter.js](gb-scriptnote-filter.js), [gb-scriptnote-menu.js](gb-scriptnote-menu.js), [gb-scriptnote-status.js](gb-scriptnote-status.js)) — 選択以外の操作を含むポップアップに閉じるアイコンを追加し、設定テーマピッカーとシナリオ採用状況メニューでも上下キー切替を有効にした
- **回帰ガードを拡張** ([gb-e2e-actions-board-micro.js](gb-e2e-actions-board-micro.js), [tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py), [tests/test_meldex_chat_and_board_preview_regressions.py](tests/test_meldex_chat_and_board_preview_regressions.py)) — 複合スタイル管理ポップアップの閉じるボタンと、通常スタイルピッカーの選択後フォーカス復帰/上下キー切替を固定した

検証: `node --check`（変更JS/結合JS）、`python -m unittest test_meldex_frontend_regressions test_meldex_chat_and_board_preview_regressions`、`python -m unittest test_meldex_micro_coverage`、`python -m unittest test_meldex_scriptnote_micro_coverage`、`powershell -ExecutionPolicy Bypass -File app\tests\run_frontend_e2e_board_micro.ps1 -Browser chrome -StopOnFailure`、`powershell -ExecutionPolicy Bypass -File app\tests\run_frontend_e2e_shell_micro.ps1 -Browser chrome -StopOnFailure`、`git diff --check`。

## v0.5.390 (2026-05-01)

### fix(annotation): 各アプリパネル右下の注釈FAB表示を復旧

- **注釈FABの対象アプリを拡張** ([gb-pane-bridge.part02.js](gb-pane-bridge.part02.js), [gb-annotations.part01.js](gb-annotations.part01.js)) — ノート、フォルダ、シート系、ボード、シナリオ、カレンダーに加え、フォーム、比較、エンティティ、メディア、HTML、CSVでもアクティブペイン右下へ注釈ボタンをマウントするようにした
- **注釈ターゲットの解決を補強** ([gb-pane-bridge.part02.js](gb-pane-bridge.part02.js), [gb-annotations.part01.js](gb-annotations.part01.js)) — タブの `path` だけに依存せず、タブ状態や現在パスから保存先を復元し、ボタンが出ても注釈保存先が空になる経路を防いだ
- **全対象アプリの回帰ガードを追加** ([gb-e2e-actions-shell-micro.js](gb-e2e-actions-shell-micro.js), [tests/meldex_shell_micro_coverage.py](tests/meldex_shell_micro_coverage.py), [tests/meldex_frontend_e2e_targeted_shell_micro.py](tests/meldex_frontend_e2e_targeted_shell_micro.py), [tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py)) — 対象アプリ種別を順番にアクティブタブへ差し替え、注釈FABの親ペインとターゲット非空をChrome E2Eで確認するケースを追加した

検証: `node --check`（ペインブリッジ/注釈/シェルmicro）、`python -m unittest test_meldex_frontend_regressions.MeldexFrontendRegressionTests.test_phase1_high_linkage_ui_guards_are_present -v`、`python -m unittest test_meldex_micro_coverage -v`、`powershell -ExecutionPolicy Bypass -File app\tests\run_frontend_e2e_shell_micro.ps1 -Browser chrome -StopOnFailure`。

## v0.5.389 (2026-04-30)

### fix(regression): 未コミットの回帰修正群を整理

- **チャット表示と保存値を補強** ([gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js), [gb-right-panel-chat.part02.js](gb-right-panel-chat.part02.js), [meldex_chat_stream.py](meldex_chat_stream.py), [meldex_api_chat.py](meldex_api_chat.py)) — 発言者名の横に発言日時分を表示し、ユーザー/アシスタント/ファイルリンク投稿の `timestamp` を履歴保存へ引き継ぐようにした
- **チャット本文と入力欄の細部を調整** ([gb-layout.css](gb-layout.css), [meldex-core.part01.js](meldex-core.part01.js)) — フキダシ内Markdownの改行・表セル折り返しを既存メッセージにも効くCSSへ寄せ、入力欄上の隙間/境界線とマイクLucideアイコン対応を整理した
- **ホームフォルダ表記を統一** ([gb-folder.part01.js](gb-folder.part01.js), [gb-folder.part02.js](gb-folder.part02.js), [gb-app.part02.js](gb-app.part02.js), [meldex_global_index_service.py](meldex_global_index_service.py)) — フォルダパネル、リンク用ツリー、初期表示、LLMツールの参照ルートで `ホームフォルダ` を表示するようにした
- **ドックポップアップと表示ロックの重なりを修正** ([gb-pane-bridge.part02.js](gb-pane-bridge.part02.js), [gb-pane-bridge.part03.js](gb-pane-bridge.part03.js), [gb-view-lock.js](gb-view-lock.js)) — 初期右ドックのチャットをクリックしても本体レイアウトを展開しないようにし、表示位置ロックボタンをフロートではなくツールバー内へ配置した
- **ボード/カレンダー/シート/スマートシートの回帰を補修** ([gb-board-render-node.js](gb-board-render-node.js), [gb-board-ui.part02.js](gb-board-ui.part02.js), [gb-calendar.part01.js](gb-calendar.part01.js), [gb-tool-calendar-views.js](gb-tool-calendar-views.js), [gb-db-actions.js](gb-db-actions.js), [gb-db-views.js](gb-db-views.js), [gb-db-smart-dashboard-view.js](gb-db-smart-dashboard-view.js)) — ボード座標入力、リンクツールチップ、カレンダー終日表示/Undo復元、シートフィルタの否定条件、スマートシート全件インデックスの表示を安定化した
- **ローカルAPIの防御を追加** ([meldex_api_database.py](meldex_api_database.py), [meldex_api_outliner.py](meldex_api_outliner.py), [meldex_api_calendar.py](meldex_api_calendar.py), [meldex_file_index_service.py](meldex_file_index_service.py), [meldex_api_static.py](meldex_api_static.py), [meldex_api_shell.py](meldex_api_shell.py)) — 書き込み系APIの認証確認、ルート外参照拒否、静的配信拡張子制限、シェル経由登録禁止、復元名検証を追加した
- **配布ホームと調査ドキュメントを整理** ([MeldexHome/サンプル](MeldexHome/サンプル), [docs/chat-llm-enhancement-plan-2026-04-29.md](docs/chat-llm-enhancement-plan-2026-04-29.md), [docs/llm-knowledge-layer-plan.md](docs/llm-knowledge-layer-plan.md), [docs/undo-version-coverage-investigation-plan.md](docs/undo-version-coverage-investigation-plan.md)) — 旧サンプルを削除し、LLM/ナレッジ/Undo-Version/テーマプレビューの調査計画を追加した

### 徹底チェック結果

- **チャット経路**: `chatSend()`、ストリーム中の `_assistantRenderOptions()`、`chatAutoSave()`、`/api/chat/save`、`_ensure_current_chat_saved()` を追跡し、日時表示と保存値が通常送信・中断・ファイルリンク投稿で欠落しないことを確認した
- **ドック/ロック経路**: ドックアイコン → `GBPaneBridge.refreshPaneAfterTabSwitch(..., { dockPopup: true })`、表示ロック → `bindHudIcon()` → `guardAction()` を追跡し、チャットだけ本体展開される経路とロックボタンが下のボタンを覆う経路を潰した
- **API書き込み経路**: アウトライナー、カレンダー、シート、メディア、ファイルアップロード、CalDAV、検索置換、静的配信を確認し、認証確認・パス制約・上書き防止が入ることを確認した

## v0.5.388 (2026-04-30)

### feat(knowledge): 自動ナレッジ化とSQLiteチャットルールへ整理

- **自動ナレッジ化の計画書を作成** ([docs/knowledge-auto-home-management-plan-2026-04-30.md](docs/knowledge-auto-home-management-plan-2026-04-30.md)) — 今週追加された記憶継承、感性原則、ステータス別ポリシー、チャットルールを、SQLite本体 + ホーム内編集ビュー + 全自動保存の方針へ整理した
- **チャットルールをSQLite管理へ移行** ([meldex_chat_rules_store.py](meldex_chat_rules_store.py), [meldex_api_knowledge.py](meldex_api_knowledge.py), [gb-chat-rules.js](gb-chat-rules.js)) — 旧localStorage注入をやめ、`/api/chat_rules` とバックエンド側プロンプト注入で管理するようにした
- **明示的な「記憶して」を強制保存に対応** ([meldex_memory_directives.py](meldex_memory_directives.py), [meldex_knowledge_extractor.py](meldex_knowledge_extractor.py)) — グローバルルール、作品/プロジェクト決定、個人の好み、チーム合意、訂正を判定し、チャットルール/記憶継承/感性原則へ自動保存するようにした
- **手動保存UIと設定内ナレッジ一覧を撤去** ([gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js), [gb-right-panel-chat.part02.js](gb-right-panel-chat.part02.js), [gb-settings.part01.js](gb-settings.part01.js), [gb-settings.part03.js](gb-settings.part03.js)) — アシスタント回答末尾の「設定に保存」と保存先エントリ名入力を廃止し、設定ダイアログはナレッジビューへの入口だけにした
- **ナレッジビューにチャットルールタブを追加** ([gb-knowledge-view.js](gb-knowledge-view.js), [Meldex.html](Meldex.html), [gb-command-palette.js](gb-command-palette.js)) — チャットのルールボタンとコマンドパレットから、設定ダイアログではなくナレッジビューを開くようにした

### 徹底チェック結果

- **チャット送信/保存経路**: `chatSend()` → `chatAutoSave()` → `/api/chat/save` → `extract_knowledge_from_chat()` → `save_explicit_memory_directives()` を追跡し、明示記憶指示が通常抽出や学習対象外statusに潰されず保存されることを確認した
- **プロンプト注入経路**: `_buildSystemPrompt()` のフロント注入を外し、`create_chat_stream_response()` → `build_chat_rules_prompt()` → `build_chat_knowledge_prompt()` → `build_chat_system_prompt()` の順で、SQLiteチャットルールと記憶継承がバックエンド側で注入されることを確認した
- **設定/ナレッジUI経路**: 設定LLMタブ、チャットのルールボタン、コマンドパレット、`openKnowledgeHomeView()`、`openChatRulesView()` を追跡し、旧 `status-policies-settings-container` と旧localStorageルール管理へ戻る経路がないことを確認した

検証: `python -m py_compile`、`python -m unittest discover -s app/tests -p "test_meldex_knowledge.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_chat_stream.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_status_policies_frontend.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_batch47_frontend_regressions.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_frontend_regressions.py" -v`、`git diff --check`。

## v0.5.387 (2026-04-30)

### fix(layout): 分割境界ボタンの向きと実行経路を整理

- **分割境界の折りたたみ/展開ボタンを現在操作に合わせて表示** ([gb-layout.part02.js](gb-layout.part02.js)) — 横分割では左側/右側を `◀/▶`、縦分割では上側/下側を `▲/▼` で明示し、折りたたみ済みの側では展開方向へアイコンと `title` / `aria-label` を反転するようにした
- **境界ボタンの実行経路を通常クリックに統一** ([gb-layout.part02.js](gb-layout.part02.js)) — `pointerup` 依存をやめ、`click` で `runCollapseAction()` を実行することで、片側だけ反応しない/通常クリックで反映されない経路を防いだ
- **回帰ガードを更新** ([tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py)) — 左右/上下それぞれのラベル、アイコン反転、`aria-label`、`click` 実行経路、CSS上の2ボタン配置を静的検査で固定した

### 徹底チェック結果

- **左右境界**: `split-collapse-btn-prev` / `split-collapse-btn-next` → `_collapseTargetForButton()` → `_adjustSplitForCollapse()` を追跡し、左側は `◀/▶`、右側は `▶/◀` で折りたたみ/展開が分かれることを確認した
- **上下境界**: 同じ経路で、上側は `▲/▼`、下側は `▼/▲` で現在実行される操作が表示されることを確認した
- **イベント経路**: `pointerdown` はリサイズハンドルへの伝播抑止だけにし、操作本体は `click` に統一されていることを確認した

検証: `gb-layout` 分割JS連結 `node --check`、`python -m unittest discover -s app/tests -p "test_meldex_frontend_regressions.py" -v`、`python -m unittest discover -s app/tests -p "test_meldex_dock_regressions.py" -v`、`git diff --check`。

## v0.5.386 (2026-04-30)

### fix(chat): ヘルプ文書とLLM送信復旧を最新仕様へ更新

- **チャットヘルプの文書パスを実在するマニュアルへ修正** ([gb-right-panel-chat.part02.js](gb-right-panel-chat.part02.js), [MeldexHome/マニュアル/03_設定と連携/チャットLLM ツールガイド.md](MeldexHome/マニュアル/03_設定と連携/チャットLLM%20ツールガイド.md), [MeldexHome/マニュアル/03_設定と連携/LLMプライバシーガイド.md](MeldexHome/マニュアル/03_設定と連携/LLMプライバシーガイド.md)) — 旧 `マニュアル/ガイド/` 参照を廃止し、「ノートを読み込めませんでした」になるヘルプ導線を修正した
- **チャットルールとヘルプ文書を多層ナレッジ仕様へ整理** ([gb-chat-rules.js](gb-chat-rules.js), [gb-right-panel-chat.part02.js](gb-right-panel-chat.part02.js)) — Skills、ユーザー定義ルール、ナレッジ層、ステータス別ポリシー、ファイル検索層、現在コンテキストの優先関係を明記し、旧「最優先」「注入プレビュー」表現を更新した
- **Gemini / ChatGPT のモデル選択と復旧経路を安定化** ([gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js), [meldex_chat_models.py](meldex_chat_models.py), [meldex_chat_stream.py](meldex_chat_stream.py)) — デフォルト候補を安定モデル優先へ並べ替え、古いモデルキャッシュを無効化し、利用できないモデルでは同一プロバイダ内の安定候補へ切り替えるようにした
- **LLMストリーム例外をSSE完了イベントへ変換** ([meldex_chat_stream.py](meldex_chat_stream.py)) — プロバイダ側の途中例外で接続が破断してフロントエンド上の汎用ネットワークエラーになる経路を抑え、本文内エラー表示と `done` イベントで終了できるようにした
- **回帰ガードを追加** ([tests/test_meldex_chat_models.py](tests/test_meldex_chat_models.py), [tests/test_meldex_chat_stream.py](tests/test_meldex_chat_stream.py), [tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py)) — ヘルプ文書の実在、チャットルール文言、モデル候補順、モデルフォールバック、SSEガードを静的/単体テストで固定した

### 徹底チェック結果

- **ヘルプボタン経路**: `showChatLlmHelpMenu()` → `_openManualPage()` → `openPage()` まで追跡し、2件のヘルプ項目が `MeldexHome/マニュアル/03_設定と連携/` 配下の実在ファイルを開くことを確認した
- **ルール/プロンプト優先順位**: `buildChatRulesPrompt()` → `_buildSystemPrompt()` → `build_chat_knowledge_prompt()` → `build_chat_system_prompt()` まで追跡し、Skillsがユーザー定義ルールより前に入る既存仕様と、ナレッジ層・ステータスポリシーの説明が矛盾しないことを確認した
- **Gemini / ChatGPT送信経路**: `chatSend()` → `/chat/stream` → `create_chat_stream_response()` → `stream_openai()` / `stream_openai_responses()` / `stream_gemini()` まで追跡し、モデル未提供時・旧キャッシュモデル・プレビュー指定・Web検索/Code Execution未対応・途中例外の各分岐を確認した
- **ストリーム終了経路**: provider stream → `_guard_chat_stream()` → フロントの `text_delta` / `done` 処理まで追跡し、例外時もスピナーが残らず、アシスタント本文としてエラーを保存できることを確認した

検証: `python -m py_compile app\meldex_chat_models.py app\meldex_chat_stream.py`、`node --check app\gb-chat-rules.js`、チャット分割JS連結 `node --check`、`python app\tests\test_meldex_chat_models.py -v`、`python app\tests\test_meldex_chat_stream.py -v`、`python app\tests\test_meldex_skills.py -v`、`python app\tests\test_meldex_knowledge.py -v`、チャット関連フロント回帰3件、`git diff --check`。

## v0.5.385 (2026-04-30)

### fix(tooltip/chat): ツールチップ再表示抑止とチャット表示を調整

- **ツールチップをカーソル移動後は領域退出まで再表示しない挙動へ変更** ([gb-tooltip.js](gb-tooltip.js), [gb-events.js](gb-events.js), [gb-editor.part03.js](gb-editor.part03.js), [gb-canvas-engine.part01.js](gb-canvas-engine.part01.js), [gb-board-render-node.js](gb-board-render-node.js)) — 共通ツールチップ、旧title置換、ノート自動リンク、ボードリンクの各経路で、カーソル移動により一度非表示になった対象は `pointerout` / `mouseout` まで再キューしないようにした
- **チャットのLLMプロバイダアイコンをLucide botへ統一** ([gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js)) — Gemini / Claude / ChatGPT ごとの色付きアイコンを廃止し、発言行・ストリーム中表示・履歴表示で同じ `getProviderIconHtml()` 経由のbotアイコンを使うようにした
- **チャット入力欄の高さ調整境界線を端まで延長** ([gb-layout.css](gb-layout.css)) — LLMチャット/チームチャットのリサイズ線を左右端まで伸ばし、入力欄側の上境界を透明化して高さ調整線だけが見えるようにした
- **回帰ガードを更新** ([tests/test_meldex_tooltip_system.py](tests/test_meldex_tooltip_system.py), [tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py)) — ツールチップ抑止状態、botアイコン統一、チャットリサイズ線のCSS条件を静的検査へ追加した

### 徹底チェック結果

- **共通ツールチップ経路**: `pointerenter/focusin` → `queueShow()` → `showNow()` → `pointermove` → `hideNow({ suppressUntilLeave: true })` → `pointerout` → `clearSuppressed()` まで追跡し、同一対象内の移動では再表示されず、領域退出後だけnative titleが復元されることを確認した
- **旧title/リンクツールチップ経路**: `gb-events` のtitle置換、ノート自動リンク、ボードリンクバッジについて、表示待ちタイマー中・表示後・同一要素内移動・別要素移動・領域退出の各状態を確認し、抑止対象が別対象へ漏れないことを確認した
- **チャットアイコン経路**: チャット発言描画、ストリーミング中ヘッダー、履歴一覧がすべて `getProviderIconHtml()` を参照しており、プロバイダ別 `icon` / `color` が残らないことを確認した
- **チャット入力境界線経路**: `Meldex.html` の `chat-composer-resize` / `team-composer-resize`、`gb-chat-input-resize.js` のドラッグ保存、`gb-layout.css` の線描画を追跡し、左右端の欠けと入力欄上枠の二重表示をCSS側で抑えたことを確認した

検証: 分割JS連結構文チェック、`python app\tests\test_meldex_tooltip_system.py -v`、`python app\tests\test_meldex_frontend_regressions.py -v`、`python app\tests\test_meldex_chat_and_board_preview_regressions.py -v`、`git diff --check`。

## v0.5.384 (2026-04-30)

### feat(layout): 標準初期レイアウトをドック前提へ更新

- **標準初期レイアウトを新しいドック構成へ変更** ([gb-pane-default-layout.js](gb-pane-default-layout.js), [Meldex.html](Meldex.html), [gb-pane-bridge.part01.js](gb-pane-bridge.part01.js)) — 左端にフォルダツリー、中央に作業パネル、右端にビューワー/タイマー、オプション/バージョン管理、チャット/カレンダー/ヒストリー/注釈の各タブ群を配置し、左右ドックは折りたたみ状態で開始するようにした
- **レイアウト初期化も標準初期レイアウトへ統一** ([gb-app-layout.js](gb-app-layout.js), [gb-pane-bridge.part03.js](gb-pane-bridge.part03.js)) — 設定の「レイアウトを初期化」から空ペインや旧初期配置へ戻らず、同じ標準ドックレイアウトを再構築するようにした
- **初回起動時の中央表示をホームフォルダ優先へ変更** ([gb-app.part02.js](gb-app.part02.js)) — URL指定や前回ビュー復元がない初回起動では、ソースフォルダより先にホームフォルダを中央のフォルダパネルで開くようにした
- **回帰ガードを更新** ([tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py)) — 初期レイアウトのスクリプト読み込み、左右ドック幅、右上30%/下70%構成、右下タブ群、ホームフォルダ優先、標準初期化APIを静的検査に追加した

### 徹底チェック結果

- **初回起動経路**: `GBPaneBridge.init()` → `GBLayout.init()` → `GBPaneDefaultLayout.build()` → `GBLayout.render()` / `GBLayout.saveLayout()` まで追跡し、保存済みレイアウトがない場合だけ新標準レイアウトが構築されることを確認した
- **ホームフォルダ表示経路**: `_startupFolderCandidate()` → `openFolder()` → `navPush()` → `_getFileOpenPane()` まで追跡し、左のフォルダツリーや右の補助ペインではなく中央の作業ペインにホームフォルダが入ることを確認した
- **レイアウト初期化経路**: 設定ボタン → `resetLayoutToDefault()` → `GBPaneBridge.resetDefaultLayout()` → `GBPaneDefaultLayout.build()` まで追跡し、既存アプリ別レイアウト復元が失敗した場合でも旧空レイアウトへ落ちないことを確認した
- **ドック展開経路**: 折りたたみ済み左右 `panelset` → `GBPanelSet.renderDock()` → ドックアイコン click/double click → `switchGroup()` / `GBDockPopup.open()` まで追跡し、左右ドックのタブ構成と展開時幅の比率が保存値として残ることを確認した

検証: `node` による標準初期レイアウト構造検査、分割JS構文チェック、`python app\tests\test_meldex_frontend_regressions.py -v`、関連レイアウト/ドック回帰 unittest、`git diff --check`。

## v0.5.383 (2026-04-30)

### fix(theme): ノートテーマ装飾と初期テーマ保存を整備

- **初期搭載テーマをユーザーカスタム由来の4テーマへ統一** ([gb-theme-manager.part01.js](gb-theme-manager.part01.js), [gb-theme-manager.part02.js](gb-theme-manager.part02.js), [gb-theme-migration.js](gb-theme-migration.js)) — ダーク/ライト/パステル/アースカラーの順序と既定値を固定し、旧カスタム名や旧テーマ候補が残る環境でも重複・別設定化しないように移行とテーマカラーセット吸収を補強した
- **テーマプレビューと実ノート表示の装飾反映を修正** ([gb-format-style-ui.js](gb-format-style-ui.js), [gb-settings.part02.js](gb-settings.part02.js), [gb-tools.part02.css](gb-tools.part02.css), [gb-tools.part03.css](gb-tools.part03.css)) — 文字フチ、背景、左アクセントバー、行の下線、アクセントカラーをプレビューと実表示へ揃え、下線は文字列下線ではなく行全体の線として扱うようにした
- **書式設定ポップアップの配置と意味を整理** ([gb-format-popup.js](gb-format-popup.js), [gb-editor.part02.js](gb-editor.part02.js), [Meldex.html](Meldex.html)) — 左アクセントバー/行の下線/アクセントカラーを背景色の右へ移し、ノート見出しドロップダウンに「タイトル」を追加してタイトル編集欄へ直接移動できるようにした
- **テーマカラー自動適用を色だけの適用へ限定** ([gb-theme-manager.part01.js](gb-theme-manager.part01.js), [gb-theme-manager.part02.js](gb-theme-manager.part02.js)) — 「線/アクセント」設定が線の表示ON/OFFやボックスシャドウを勝手に追加せず、既存の左アクセントバー/行下線の色だけを供給するようにした
- **回帰ガードを更新** ([tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py)) — 初期テーマ移行、テーマカラー保存、書式ポップアップ配置、ノート見出し装飾、テーマ自動適用の静的検査を追加・更新した

検証: `python -m unittest test_meldex_frontend_regressions`、`git diff --check`。

## v0.5.382 (2026-04-29)

### fix(chat/settings): LLM選択と設定レイアウト初期化を安定化

- **チャットのモデル候補を現行推奨モデルへ絞り込み** ([meldex_chat_models.py](meldex_chat_models.py), [gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js)) — 各プロバイダのモデル一覧を実用候補中心に正規化し、古いキャッシュをバージョンで無効化、リスト外モデルを通常選択へ混ぜないようにした
- **チャット入力欄の高さ調整を境界ドラッグへ変更** ([Meldex.html](Meldex.html), [gb-chat-input-resize.js](gb-chat-input-resize.js), [gb-layout.css](gb-layout.css)) — 右下リサイズハンドルを廃止し、発言領域との境界線をドラッグして LLM/チームチャット入力欄の高さを調整・保存できるようにした
- **LLM復旧メモが本文に混ざる問題を修正** ([meldex_chat_stream.py](meldex_chat_stream.py), [gb-right-panel-chat.part02.js](gb-right-panel-chat.part02.js)) — Claude/OpenAI/Gemini のパラメータ未対応時の内部通知を `internal_notice` として扱い、アシスタント発言本文へ表示しないようにした
- **設定の「レイアウトを初期化」で黒画面になる退行を修正** ([gb-app-layout.js](gb-app-layout.js), [gb-settings.part01.js](gb-settings.part01.js)) — レイアウト保存キー削除後に空ペインへ戻さず、現在の作業種別から組み込みアプリレイアウトを推定して再適用するようにした
- **設定ダイアログとショートカットE2Eを補強** ([gb-e2e-actions.part02.js](gb-e2e-actions.part02.js), [tests/meldex_frontend_e2e_targeted_core.py](tests/meldex_frontend_e2e_targeted_core.py), [tests/test_meldex_batch44_frontend_regressions.py](tests/test_meldex_batch44_frontend_regressions.py)) — 全タブのはみ出し/旧DB表記/ナレッジ層統合検査に加え、レイアウト初期化後の作業領域復元とショートカット設定後のモーダル終了を実ブラウザで検査するようにした

### 徹底チェック結果

- **設定ダイアログ項目**: 全般、テーマ、LLM、連携、フィードバック、ユーザー、拡張機能、ショートカット、フィードバック・送信設定、About の表示・遅延初期化・保存経路を確認し、設定UI内の旧「DB」表記が残っていないことを再検索した
- **レイアウト初期化経路**: 設定ボタン → `resetLayoutToDefault()` → localStorage削除 → `GBAppLayouts.applyAppLayout()` → `GBLayout.applyLayoutTree()` まで追跡し、初期化後に空ペインだけにならないことをE2Eで確認した
- **LLMチャット経路**: モデル一覧取得/キャッシュ/手動モデル指定、ストリーム復旧時の内部通知、入力欄自動伸縮と手動境界ドラッグの併用を確認した

検証: `python app/tests/meldex_frontend_e2e_matrix.py`、`python -m unittest test_meldex_batch44_frontend_regressions test_meldex_status_policies_frontend -v`、関連フロント回帰/coverage unittest、`powershell -ExecutionPolicy Bypass -File app/tests/run_frontend_e2e_case_batch.ps1 -Browser chrome -CaseList targeted-settings-dialog-modernized-layout -StopOnFailure`、`powershell -ExecutionPolicy Bypass -File app/tests/run_frontend_e2e_case_batch.ps1 -Browser chrome -CaseList targeted-shortcut-customization-via-settings -StopOnFailure`、`python -m unittest discover -s app\tests -v`（588件 OK、skip 2）。

## v0.5.381 (2026-04-29)

### fix(layout): ドックの複数タブ表示とポップアップ内容同期を修正

- **複数タブを重ねたドックでドックバーが消える退行を修正** ([gb-panelset.js](gb-panelset.js)) — 単一paneでもタブが2件以上ある場合はドックバーを描画し、各タブアイコンから操作できるようにした
- **折りたたみドックのアイコン別ポップアップ内容を同期** ([gb-dock-popup.js](gb-dock-popup.js)) — クリックしたgroup/tabを先に状態へ反映し、移動済みpane DOMのactiveタブと内容を `GBPaneBridge.refreshPaneAfterTabSwitch()` で更新するようにした。非アクティブgroupなどDOMが未描画のpaneは一時DOMを生成して表示する
- **チャットパネル内タブの常時ハイライトを修正** ([gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js)) — `チーム / LLM / 履歴` 切替時にinline styleだけでなく `.active` クラスも同期し、未選択タブに初期activeが残らないようにした
- **ドック/タブ回帰ガードを追加** ([gb-e2e-actions-shell-micro.js](gb-e2e-actions-shell-micro.js), [tests/meldex_shell_micro_coverage.py](tests/meldex_shell_micro_coverage.py), [tests/test_meldex_dock_regressions.py](tests/test_meldex_dock_regressions.py)) — 複数タブdockのバー表示、折りたたみdockアイコン別の `チャット / 注釈 / 履歴` ポップアップ内容切替、他パネル内タブのactive解除経路を検査するようにした

### 徹底チェック結果

- **ドック操作経路**: `GBPanelSet.renderDock()` の単独pane判定、dock icon click → `GBDockPopup.open()` → pane DOM移動/一時生成 → `GBPaneBridge.refreshPaneAfterTabSwitch()` → close復元まで追跡し、展開/折りたたみ/非アクティブgroup/同一pane内タブ切替で状態とDOMが一致することを確認した
- **パネル内タブ経路**: チャット `switchChatMode()`、オプションパネル/詳細パネル/設定/ナレッジビュー/右パネルタブのactiveクラス切替を確認し、チャットのみ初期 `.active` の解除漏れがあることを特定して修正した

検証: `node --check app\gb-dock-popup.js app\gb-panelset.js app\gb-e2e-actions-shell-micro.js`、チャット分割JS連結 `node --check`、`python -m unittest test_meldex_dock_regressions -v`、`python -m unittest test_meldex_micro_coverage -v`、`powershell -ExecutionPolicy Bypass -File app\tests\run_frontend_e2e_case.ps1 -CaseId targeted-shell-micro-dock-multitab-popup -Browser chrome`、`powershell -ExecutionPolicy Bypass -File app\tests\run_frontend_e2e_shell_micro.ps1 -Browser chrome -StopOnFailure`、`python -m unittest test_meldex_frontend_regressions -v`、`python -m unittest test_meldex_batch35_regressions -v`、`python -m unittest discover -s app\tests -v`（570件 OK、skip 2）。

## v0.5.380 (2026-04-29)

### fix(board/chat): チャット選択とボードスタイルプレビューを補正

- **チャット発言をドラッグ選択可能に変更** ([gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js), [gb-layout.css](gb-layout.css)) — チームチャット/LLMチャットの行・バブル・本文へ選択用クラスを付与し、発言本文は `user-select:text`、ヘッダー/ボタンは選択対象外になるようにした
- **雲/トゲ系カードプレビューを実カード形状に統一** ([gb-board-presets.js](gb-board-presets.js), [gb-board-style-manager.part01.js](gb-board-style-manager.part01.js)) — 雲・直線トゲ・曲線トゲ・もやもやのプレビューを実カードと同じ `_bdCloudClipPath` / `_bdThornClipPath` / `_bdThornCurveClipPath` / `_bdFluffyClipPath` 由来のSVGパスで描画し、階層別スタイルにも雲/トゲ詳細パラメータを反映した
- **ボードスタイルプレビューの縮小表示を補正** ([gb-tools.part03.css](gb-tools.part03.css)) — ツールバー、詳細パネル、オプションパネル、ポップアップ、リスト、階層別スタイルのカード/ラインプレビュー枠を補強し、プレビュー要素が枠内で小さく残る表示を防いだ
- **回帰ガードを追加** ([tests/test_meldex_chat_and_board_preview_regressions.py](tests/test_meldex_chat_and_board_preview_regressions.py)) — チャット本文選択、パス型カードプレビュー、各プレビュー枠サイズの静的回帰テストを追加した

### 徹底チェック結果

- **チャット操作経路**: チームチャット `renderTeamMessages()` → `_buildTeamMessageRow()`、LLMチャット `loadChatHistory()` / 送信完了 → `chatAddMessage()` → 削除ボタン登録まで追跡し、本文選択と削除/リンク/保存ボタン操作が競合しないことを確認した
- **ボード操作経路**: ツールバー更新 `bdRefreshBoardToolbar()`、詳細パネル `bdRefreshSelectionDetails()`、スタイルピッカー `bdOpenStylePicker()`、オプションパネル `_bdRenderStyleManagerInPanel()` / `_bdRenderDepthStyleInPanel()`、ポップアップ `_bdOpenStyleManagerPopup()` / `_bdOpenDepthManagerPopup()` からプレビューHTML生成まで追跡し、カード/ライン/階層別すべてで同じプレビュー部品へ集約されることを確認した
- **追加補正**: パス型プレビューのviewBox余白が形状本体を縮める可能性を見つけ、実カードと同じ本体viewBox + SVG overflowへ変更した

検証: `node --check app\gb-board-presets.js`、チャット/ボードスタイル管理の分割JS連結 `node --check`、`python -m unittest test_meldex_chat_and_board_preview_regressions -v`、関連フロント回帰2件、Chrome headlessスクリーンショットのAI目視確認、`python -m unittest discover -s app/tests -v`（565件 OK、skip 2）。

## v0.5.379 (2026-04-29)

### feat(beta): ベータリリース前必須 Phase 1 を実装

- **ベータ版の起動表示と同意導線を追加** ([Meldex.html](Meldex.html), [gb-beta-release.js](gb-beta-release.js), [gb-beta-release.css](gb-beta-release.css), [gb-settings.part01.js](gb-settings.part01.js), [gb-window.py](gb-window.py), [manifest.json](manifest.json)) — タイトル/メタ情報/ウィンドウタイトルを `Meldex BETA` に統一し、初回起動の同意ダイアログ、送信設定、About表示を追加した
- **フィードバック・利用状況・クラッシュ報告の受け口を追加** ([gb-beta-feedback.js](gb-beta-feedback.js), [meldex_api_beta_feedback.py](meldex_api_beta_feedback.py), [meldex_beta_feedback_service.py](meldex_beta_feedback_service.py), [docs/google-apps-script-beta-feedback-webapp-template.js](docs/google-apps-script-beta-feedback-webapp-template.js)) — アプリ内フィードバック、JSONL記録、Google Apps Script連携テンプレート、デバッグリスト転記を実装した
- **Notion同期をベータ向けにpush-onlyへ縮退** ([meldex_api_notion.py](meldex_api_notion.py), [meldex_notion_service.py](meldex_notion_service.py), [gb-notion-sync.js](gb-notion-sync.js)) — Notionからのpullを停止し、atomic write、tmp残骸掃除、ファイル名不変、トークン削除時の自動同期停止を回帰テストで固定した
- **ステータスポリシーをLLM/引用/チャット/公開制御へ拡張** ([meldex_status_policies.py](meldex_status_policies.py), [meldex_status_resolver.py](meldex_status_resolver.py), [meldex_api_status_policies.py](meldex_api_status_policies.py), [meldex_knowledge_store.py](meldex_knowledge_store.py), [meldex_chat_citations.py](meldex_chat_citations.py), [gb-status-policies-view.js](gb-status-policies-view.js), [gb-publish-settings.js](gb-publish-settings.js)) — ステータスごとにLLM参照、Anthropic引用、チャット応答、公開可否を制御できるようにし、公開処理前にポリシーを検査するようにした
- **公開フォーム/公開更新導線を補強** ([gb-db-view-form.js](gb-db-view-form.js), [gb-public-runtime.js](gb-public-runtime.js), [gb-export-html.js](gb-export-html.js), [gb-tool-menu.part02.js](gb-tool-menu.part02.js), [gb-detail-panel.part01.js](gb-detail-panel.part01.js), [gb-detail-panel.part02.js](gb-detail-panel.part02.js)) — シート/ページの公開設定と公開更新をツールバー/メニュー/オプションパネルから辿れるようにし、公開フォームの送信日・token guardを追加した
- **RPA機能とzapアイコンをベータ配布対象から削除** ([rpa.html](rpa.html), [rpa.py](rpa.py), [rpa-main.js](rpa-main.js), [rpa-render.js](rpa-render.js), [rpa-panels.js](rpa-panels.js), [rpa-dialogs.js](rpa-dialogs.js), [tray.py](tray.py), [tray-icons/zap.png](tray-icons/zap.png)) — 常駐メニュー、ビルド、テスト、ドキュメントからRPA導線を外した
- **用語・マニュアル・法務表記をベータ向けに更新** ([MeldexHome/マニュアル](MeldexHome/マニュアル), [docs/オプションパネル_設計意図.md](docs/オプションパネル_設計意図.md), [docs/notion-page-draft-2026-04-29.md](docs/notion-page-draft-2026-04-29.md), [CREDITS.md](../CREDITS.md), [THIRD-PARTY.md](../THIRD-PARTY.md)) — 「詳細パネル」→「オプションパネル」、「ページエディタ」→「ノートエディタ」を反映し、BETA説明、既知の不具合、OSS/フォント再配布表記を整備した

検証: `node --check`（対象JSおよび分割JS連結）、`python -m py_compile`（対象Python/テスト）、Phase 1関連ユニット67件、フロント静的回帰106件、実ブラウザsmoke 1件、`python -m unittest discover -q`（562件 OK、skip 2）。

既知残件: 実Notionワークスペースを使う外部E2Eはローカルに専用トークン/検証ワークスペースを置かない前提のため未実施。ベータではNotion同期をpush-onlyとして扱う。

## v0.5.378 (2026-04-29)

### fix(spec): v0.5.377既知残件を完了

- **注釈分割アセットを再配分** ([gb-annotations.part01.js](gb-annotations.part01.js), [gb-annotations.part02.js](gb-annotations.part02.js)) — `eraseAtPoint()` / `createNote()` / ViewLockインターセプタ登録をpart02へ移し、`gb-annotations.part01.js`を1000行制限内へ戻した
- **H-04 シナリオショートカットを中央実行へ接続** ([gb-shortcuts.js](gb-shortcuts.js), [gb-scriptnote-interactions.js](gb-scriptnote-interactions.js), [gb-scriptnote-editor.part02.js](gb-scriptnote-editor.part02.js), [gb-scriptnote-editor.part03.js](gb-scriptnote-editor.part03.js)) — `scenario.*`スタブをアクティブなScriptNoteEditorの`runShortcutAction()`へ委譲し、既存keydown経路も`runMeldexShortcutById()`を先に通すようにした
- **M-05/M-12/M-14を補完** ([gb-board-performance.js](gb-board-performance.js), [gb-db-smart.js](gb-db-smart.js), [gb-db-smart-global.js](gb-db-smart-global.js), [gb-comment-badges.js](gb-comment-badges.js)) — 大規模ボードの軽量カード描画をしきい値/設定付きで有効化し、スマートシート/全件インデックスの複数条件フィルタを共通レイアウトへ寄せ、コメント追加の`cfPrompt`フォールバックをインラインtextareaへ統一した
- **M-03/M-07/M-09/M-18とL系の残判断をガード化** ([gb-detail-property-layout-editor.js](gb-detail-property-layout-editor.js), [gb-export-html.js](gb-export-html.js), [gb-public-runtime.js](gb-public-runtime.js), [gb-modal-shell.js](gb-modal-shell.js), [gb-tool-calendar.js](gb-tool-calendar.js), [gb-tool-calendar-options.js](gb-tool-calendar-options.js), [gb-settings.part01.js](gb-settings.part01.js), [gb-app-layout.js](gb-app-layout.js), [docs/dialog-delete-confirm-sample.html](docs/dialog-delete-confirm-sample.html)) — 既に実装済みのプロパティレイアウト/カレンダー公開/共通モーダル/ペイン内タイマーを回帰テストで固定し、設定ダイアログにレイアウトアイコン変更導線を追加、削除確認サンプルHTMLを配置した
- **L-03/L-07/L-08/L-05の扱いを明確化** — `.sn2-style-*`はdocsプレビュー内のみ、`gb-canvas-features.part03.js`と`gb-canvas-minimap.js`は現行実体と既存回帰テストがあるため削除せず保持し、残存`postMessage`はHTMLビューワー/埋め込み注釈surface用途として許容する

検証: `node --check`（対象JS、設定/シナリオ/注釈分割JS連結）、`python -m unittest app.tests.test_meldex_split_assets -q`、`python -m unittest app.tests.test_meldex_frontend_regressions -q`、`python -m unittest app.tests.test_meldex_split_assets app.tests.test_meldex_high_risk_coverage app.tests.test_meldex_batch36_frontend_regressions app.tests.test_meldex_batch44_frontend_regressions -q`

既知残件: なし（v0.5.377の既知残件は解消または回帰ガード付きの保持判断へ移行）。

## v0.5.377 (2026-04-29)

### fix(spec): 機能仕様適合・連動整合ギャップを補強

- **ソースフォルダ別チャットを実装** ([meldex_chat_path_service.py](meldex_chat_path_service.py), [meldex_api_chat.py](meldex_api_chat.py), [meldex_api_collab.py](meldex_api_collab.py), [gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js), [gb-right-panel-chat.part02.js](gb-right-panel-chat.part02.js)) — チャット履歴・チームチャット・検索・保存・ファイル紐づきチャットを選択中ソースフォルダ配下 `_chat/` へ分離し、既存vault直下履歴の読取互換も維持した
- **チャットLLMからdebug-report DBへ報告登録できる経路を追加** ([meldex_chat_stream.py](meldex_chat_stream.py), [meldex_settings_db_service.py](meldex_settings_db_service.py), [meldex_api_settings_db.py](meldex_api_settings_db.py)) — 対象ソースフォルダ内のデバッグ報告用シート検出、添付コピー、元発言ID付きの報告エントリ作成をツール呼び出しとして接続した
- **ボード複数タブ時のDOM ID衝突を軽減** ([gb-tool-canvas.js](gb-tool-canvas.js)) — 非アクティブなボードroot IDをタブ別suffix付きに戻し、アクティブボードだけを既存の `bd-*` canonical IDへ割り当てる互換層を追加した
- **注釈FAB/オーバーレイの配置とクリック導線を修正** ([Meldex.html](Meldex.html), [gb-pane-bridge.part02.js](gb-pane-bridge.part02.js), [gb-folder.part02.js](gb-folder.part02.js), [gb-outliner-search.part02.js](gb-outliner-search.part02.js)) — ノート注釈UIを `#page-view` 内へ移動し、フォルダ設定ボタン・全文検索結果クリックを直接/委譲イベントで確実に動作させるようにした
- **シナリオのショートカット表示とUndo/Redo登録を補強** ([gb-shortcuts.js](gb-shortcuts.js), [gb-app.part02.js](gb-app.part02.js), [gb-pane-bridge.part02.js](gb-pane-bridge.part02.js)) — シナリオ用のUndo/Redoを中央ショートカット定義へ追加し、ステータスバー表示をカスタムキー設定に追従させた
- **UI用語・公開導線・ツールチップを仕様へ追従** ([gb-db-views.js](gb-db-views.js), [gb-detail-panel.part01.js](gb-detail-panel.part01.js), [gb-chat-rules.js](gb-chat-rules.js), [gb-editor.part01.js](gb-editor.part01.js), [gb-publish-settings.js](gb-publish-settings.js), [gb-tooltip.js](gb-tooltip.js)) — シート/スタイル/ノート等の表示語を統一し、シートツールバーに公開設定・公開更新を追加、フォルダツリー/エクスプローラー操作のツールチップを補強した
- **低優先度の安全な整理を実施** ([gb-color-palette.js](gb-color-palette.js), [gb-layout.part01.js](gb-layout.part01.js), [meldex-core.part02.js](meldex-core.part02.js)) — カラーパレットの `onclick` 代入を `addEventListener` に置換し、Shift+パネルハンドルでカラム移動できるようにし、`closeColHeaderMenu()` を共通側へ追加した

検証: `node --check`（対象JSおよび分割JS連結）、`python -m py_compile`（対象Python）、`python -m unittest app.tests.test_meldex_frontend_regressions -q`、`python -m unittest app.tests.test_meldex_toolbar_regressions app.tests.test_meldex_tooltip_system app.tests.test_meldex_batch25_api_regressions -q`、`python -m unittest app.tests.test_meldex_batch55_cloud_phase1_regressions -q`、`python -m unittest app.tests.test_meldex_api_smoke -q`

既知残件: `test_meldex_split_assets` は今回未変更の `gb-annotations.part01.js` が 1069 行で既存の1000行制限を超えているため失敗。H-04のシナリオショートカット完全中央化、M-03/M-05/M-07/M-09/M-12/M-14/M-18、L系の設計判断・大規模整理は後続セッションへ残す。

## v0.5.376 (2026-04-29)

### fix(undo): Undo/Version網羅調査の高リスク経路を補強

- **LLMチャットの個別メッセージ削除をUndo対応** ([gb-right-panel-chat.part01.js](gb-right-panel-chat.part01.js), [gb-right-panel-chat.part02.js](gb-right-panel-chat.part02.js)) — 保存済み/表示中メッセージに削除ボタンを追加し、削除前後のメッセージ配列を `chat:` scope のUndo/Redoへ登録、`/chat/save` で空履歴保存も扱えるようにした
- **フォルダツリー/フォルダビューの通常削除をTrash復元Undoへ接続** ([gb-outliner.part02.js](gb-outliner.part02.js), [gb-folder.part01.js](gb-folder.part01.js), [gb-shortcuts.js](gb-shortcuts.js), [gb-editor.part04.js](gb-editor.part04.js), [gb-tool-menu.part01.js](gb-tool-menu.part01.js)) — 削除成功時の `trash_name` を保持し、Undoで `/outliner/restore`、Redoで再削除して新しい `trash_name` に差し替える共通ヘルパーへ集約した
- **Undo/Version調査表と回帰ガードを更新** ([docs/undo-version-coverage-investigation/coverage-matrix.md](docs/undo-version-coverage-investigation/coverage-matrix.md), [docs/undo-version-coverage-investigation/gap-analysis.md](docs/undo-version-coverage-investigation/gap-analysis.md), [tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py)) — LLM個別削除と通常ファイル削除の接続状況を反映し、静的回帰テストで履歴登録・復元API・参照purgeを検知するようにした

検証: `node` によるチャット/Outliner/フォルダ/エディタ/ツールメニュー/ショートカット連結構文チェック、`python -m unittest discover -s app/tests -p "test_meldex_frontend_regressions.py" -v`、`git diff --check`

## v0.5.375 (2026-04-28)

### fix(layout): パネル境界ドラッグの連動リサイズを廃止

- **境界ドラッグを当該split限定に変更** ([gb-layout.part02.js](gb-layout.part02.js)) — 旧軸リサイズセッションを撤去し、ドラッグした境界の直下2子だけをpx更新、終了時に同じratioの`%`表記へ戻すようにした
- **ネスト同方向splitの非連動を明文化** ([docs/ペインシステム_設計意図.md](docs/ペインシステム_設計意図.md)) — 内側境界ドラッグで外側ratioを変えず、外側境界ドラッグでも内側ratioを変えない方針を設計意図へ反映した
- **shell micro E2Eを追加** ([gb-e2e-actions-shell-micro.js](gb-e2e-actions-shell-micro.js), [tests/meldex_shell_micro_coverage.py](tests/meldex_shell_micro_coverage.py)) — 水平ネスト、外側境界、垂直境界のドラッグを実ブラウザで操作し、非対象splitのratio不変とドラッグ終了後の`%`復帰を検査するようにした

### 徹底チェック結果

- **導入経緯確認**: `git log` / `git show` で旧軸リサイズは `7f14632 [codex] fix: adjust panel tab menu and resizing` による追加、`688057b` は分割移動のみと確認。docs内に連動維持を必須とする設計理由は見つからなかった
- **操作チェーン追跡**: 境界ハンドル `pointerdown` → 直下2子px固定 → `pointermove`で対象splitの`node.ratio`のみ更新 → `pointerup/cancel`で対象splitだけ`%`復帰 → `saveLayout()` まで追跡し、旧軸リサイズ関数群への実行時参照がないことを確認した
- **E2E登録確認**: shell micro caseをgenerated matrixへ反映し、coverage gateとdomain別micro検査が新action名を参照できるようにした

検証: `node --check app/gb-e2e-actions-shell-micro.js app/gb-layout.part02.js`、layout split結合後syntax check、`python -m py_compile`、`python app/tests/meldex_frontend_e2e_matrix.py`、`powershell -ExecutionPolicy Bypass -File app/tests/run_frontend_e2e_case_batch.ps1 -Browser chrome -CaseList "targeted-shell-micro-main-menu-escape-clamp","targeted-shell-micro-panel-resize-isolation"`、`python -m unittest discover -s app/tests -v`、`git diff --check`

## v0.5.374 (2026-04-28)

### feat(shell): アプリバーを廃止しコマンドパレットへ統合

- **コマンドパレットを追加** ([gb-command-palette.js](gb-command-palette.js), [gb-command-palette.css](gb-command-palette.css)) — `Ctrl+Shift+P` と左クロームの起動行から、コマンド、パネル、アプリレイアウト、最近/全件ファイル、設定へアクセスできるようにした
- **左カラム下部フッターへ導線を移設** ([Meldex.html](Meldex.html)) — ユーザーチップ、設定ボタン、コマンドパレット起動行をフォルダツリー下部に配置し、フォルダツリー非表示時はフローティング導線を表示するようにした
- **旧アプリバーを撤去** ([Meldex.html](Meldex.html), [gb-layout.css](gb-layout.css), [gb-app-layout.js](gb-app-layout.js)) — `#app-bar` DOM、配置/自動非表示設定、`gb-activity-bar-v2.js`、アプリバー上のレイアウトボタン同期を削除し、レイアウト操作は `GBAppLayouts` 公開APIとコマンドパレットのサブコマンドに集約した

## v0.5.373 (2026-04-28)

### feat(llm): LLMナレッジ層 Phase 7-P 感性個人化を追加

- **感性個人化レイヤーを追加** ([meldex_taste_extractor.py](meldex_taste_extractor.py), [meldex_taste_critic.py](meldex_taste_critic.py), [meldex_taste_reviser.py](meldex_taste_reviser.py), [meldex_taste_learning.py](meldex_taste_learning.py)) — `技法ナレッジ` と pinned personal preference から個人用の感性原則を抽出し、Phase 7 アイディア生成だけに generator context / critic / borderline reviser / 採用学習を適用するようにした
- **デフォルトOFF・ユーザー別opt-inを実装** ([meldex_api_taste.py](meldex_api_taste.py), [meldex_idea_miner.py](meldex_idea_miner.py)) — `taste_settings` が存在しない状態では無効化し、`owner_id` ごとの設定・原則・feedbackだけを参照する。検索・出典・Skills・記憶抽出には適用しない
- **感性原則UIを追加** ([gb-taste-principles-view.js](gb-taste-principles-view.js), [gb-knowledge-view.js](gb-knowledge-view.js), [Meldex.html](Meldex.html)) — ナレッジ層に「感性原則」タブを追加し、個人化ON/OFF、抽出、手動追加、pin、weight編集、学習ログ確認を行えるようにした
- **アイディアインボックスにtaste評価表示を追加** ([gb-idea-inbox.js](gb-idea-inbox.js), [meldex_api_ideas.py](meldex_api_ideas.py)) — taste score、適用原則、borderline改稿前の原案、低スコア表示切替を追加した
- **プライバシー境界とstale同期を補強** ([meldex_idea_miner.py](meldex_idea_miner.py), [meldex_taste_extractor.py](meldex_taste_extractor.py), [meldex_taste_learning.py](meldex_taste_learning.py)) — 他ユーザー閲覧時は taste 評価・原案・原則名を伏せ、pin解除済み preference 由来の感性原則は削除扱いへ同期するようにした
- **回帰テストを追加** ([tests/test_meldex_taste_personalization.py](tests/test_meldex_taste_personalization.py), [tests/test_meldex_router_modules.py](tests/test_meldex_router_modules.py), [tests/test_meldex_status_policies_frontend.py](tests/test_meldex_status_policies_frontend.py)) — default OFF、owner scope、抽出、opt-in mining、feedback学習、他ユーザーredaction、APIルート、UI読込順を通常 unittest で検知できるようにした

### 全体チェック結果

- **全スクリプト読込**: `app/` 配下の `.py/.js/.mjs/.cjs/.html/.css/.ps1/.bat/.cmd` 758ファイル、301,904行を列挙し、各ファイルを読み込んで行数・サイズ・hashを確認した
- **構文検査**: split loader 用の `.partXX` は単体構文対象から除外し、単体成立する Python 216ファイル、JavaScript/MJS/CJS 305ファイル、PowerShell 16ファイルを構文検査した
- **破損検査**: 758ファイルすべてで NUL byte と Git衝突マーカーがないことを確認した
- **サイズ監査**: 1500行超の既存ファイルが8件あることを確認。今回の Phase 7-P 追加先は新規分割モジュールにしたため、既存巨大ファイルへの機能追加はしていない
- **個人化操作チェーン**: 設定ON/OFF → 抽出 → 原則選択 → generator context → critic → borderline reviser → inbox保存 → 採用/却下/あとで feedback → learned_weight 更新まで追跡し、owner mismatch 時は評価学習・表示を行わないことを確認した

検証: `python -m py_compile`（単体成立Python 216件）、`node --check`（単体成立JS/MJS/CJS 305件）、PowerShell Parser（16件）、NUL/衝突マーカー検査（758件）、`python -m unittest discover -s app/tests -v`、`git diff --check`

## v0.5.372 (2026-04-28)

### test(e2e): Meldex微細挙動テスト基盤を領域別に拡張

- **微細挙動マニフェストを全領域へ拡張** ([tests/meldex_micro_coverage.py](tests/meldex_micro_coverage.py), [tests/meldex_*_micro_coverage.py](tests)) — shell/note/sheet/scriptnote/board/calendar/annotation/mobile の順序付きケース一覧を共有し、領域別runnerとgenerated E2E registryが同じIDリストを使うようにした
- **領域別targeted E2Eとブラウザactionを追加** ([tests/meldex_frontend_e2e_targeted_*_micro.py](tests), [gb-e2e-actions-*-micro.js](.)) — ノートのキャレット保存、シートセル保存状態、ボード座標保存/再読込、カレンダーイベント表示状態、注釈overlay host、モバイルcloud surface、shellメニューEscape/viewportを実ブラウザで検査できるようにした
- **micro専用runnerを追加** ([tests/run_frontend_e2e_micro_domain.ps1](tests/run_frontend_e2e_micro_domain.ps1), [tests/run_frontend_e2e_micro_all.ps1](tests/run_frontend_e2e_micro_all.ps1), [tests/run_frontend_e2e_*_micro.ps1](tests)) — `-Domain` / 領域別wrapper / `-ListOnly` / `-StopOnFailure` で実行対象を絞れるようにした
- **失敗ケース再実行連携を追加** ([tests/run_frontend_e2e_case_batch.ps1](tests/run_frontend_e2e_case_batch.ps1), [tests/run_frontend_e2e_failed_only.ps1](tests/run_frontend_e2e_failed_only.ps1)) — batch実行ごとに `all-cases.txt` / `passed-cases.txt` / `failed-cases.txt` / `summary.json` を保存し、`e2e-micro-*` の最新失敗ケースを再実行できるようにした
- **通常unittestのカバレッジ検査を追加** ([tests/test_meldex_micro_coverage.py](tests/test_meldex_micro_coverage.py), [tests/test_meldex_batch47_frontend_regressions.py](tests/test_meldex_batch47_frontend_regressions.py)) — micro caseのgenerated登録、coverage gate、runtime script読込順、AGENTS監査リスト、runner連携を静的に検知できるようにした

### 徹底チェック結果

- **全領域micro実行**: `run_frontend_e2e_micro_all.ps1` → `run_frontend_e2e_micro_domain.ps1 -Domain all` → `micro_e2e_case_ids('all')` → `run_frontend_e2e_case_batch.ps1 -CaseList ...` → 各 generated case → `gb-e2e-actions-*-micro.js` action/assertion。24件すべて同一manifest由来で実行される
- **領域別micro実行**: 各 `run_frontend_e2e_*_micro.ps1` → `-Domain note|sheet|...` → 対象領域だけのcase idを取得。scriptnote既存runnerは `e2e-micro-scriptnote` log prefixに統一され、失敗再実行の検索対象に入る
- **ListOnly**: wrapperのswitchを配列引数へ変換してから子PowerShellへ渡すため、`SwitchParameter` の文字列変換エラーを起こさず、manifest上のcase idだけを表示して副作用を起こさない
- **失敗再実行**: batch runnerが空でも `failed-cases.txt` を必ず生成し、`run_frontend_e2e_failed_only.ps1` は `e2e-full-*` / `e2e-batch-*` / `e2e-micro-*` を探索する。失敗0件ではexit 0、失敗ありではそのcase listだけを再batch実行する
- **HTML読込順**: `Meldex.html` は `gb-e2e-actions-extra.js` の後、`gb-smoke.js` の前にすべての micro action script を読み込む。registerAction/registerAssertionが未定義の早期読込や、smoke側から未登録actionを参照する順序不整合は起きない
- **生成registry**: `meldex_frontend_e2e_matrix.py` に新規targeted moduleを登録し、`tests/_generated/e2e_cases.json` / `e2e_ui_manifest.json` へ反映。既存75件から82件以上へ拡張され、micro 24件はcoverage gateを持つ

検証: `node --check app/gb-e2e-actions-*-micro.js`、`python -m py_compile app/tests/meldex_*_micro_coverage.py app/tests/meldex_frontend_e2e_targeted_*_micro.py app/tests/test_meldex_micro_coverage.py`、`python app/tests/meldex_frontend_e2e_matrix.py`、`powershell -ExecutionPolicy Bypass -File app/tests/run_frontend_e2e_micro_all.ps1 -ListOnly`、`powershell -ExecutionPolicy Bypass -File app/tests/run_frontend_e2e_micro_all.ps1 -Browser chrome -StopOnFailure`、`python -m unittest discover -s app/tests -v`、`git diff --check`

## v0.5.371 (2026-04-28)

### feat(board): カード構造にツリーを追加

- **ボードカード構造に「ツリー」を追加** ([gb-canvas-engine.part01.js](gb-canvas-engine.part01.js), [gb-canvas-features.part01.js](gb-canvas-features.part01.js)) — 構造選択の候補に「ツリー」を追加し、親カードの直下かつ右インデント位置へ子カードを縦積み配置する専用レイアウトを実装
- **ツリー構造のカード追加操作を調整** ([gb-board-ui.part01.js](gb-board-ui.part01.js), [gb-canvas-features.part01.js](gb-canvas-features.part01.js)) — 子カード追加、兄弟追加、アンカーHUD追加、Ctrl+矢印操作で、ツリーでは子方向を下、兄弟順を上下として扱うようにした
- **ツリー構造の接続線方向を縦方向固定に追加** ([gb-canvas-engine.part02.js](gb-canvas-engine.part02.js), [gb-canvas-engine.part03.js](gb-canvas-engine.part03.js)) — 曲線/直角線の端点補完と折れ曲がり方向で、インデント幅が大きい場合でもツリー線が横方向扱いにならないようにした
- **フォルダパネルのサムネイル選択チェックボックス位置を固定** ([gb-tools.part01.css](gb-tools.part01.css)) — グリッド/ウォーターフォール等のサムネイル表示で、選択チェックボックスが中央寄りや上側へずれず、項目左上に重なるようにした
- **回帰テストを追加** ([tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py)) — ツリー構造のレイアウト結果、各ボード経路への登録、フォルダチェックボックス左上固定を通常 unittest で検知できるようにした

### 徹底チェック結果

- **構造選択**: `BD_STRUCTURES` → 詳細パネル `_bdStructureOptions()` / コンテキストメニュー構造サブメニューへ自動反映。保存値は既存 `node.structure` の文字列として保持され、旧構造の読込経路は変更なし
- **子カード追加**: `bdAddChildToSelected()` → `bdStructureOf(parentId)` → `tree` 判定 → `x + gapLevels`, `y + height + gapSiblings` に仮配置 → `bdAutoLayout(root.id)`。最終レイアウトで親直下・右インデントに補正される
- **兄弟カード追加**: `bdAddSiblingToSelected()` → `tree` 判定 → 兄弟を縦方向へ仮配置 → 差分レイアウトは `direction: 'right'` 経由で y 軸を使い、後続の `bdAutoLayout()` で高さ差を吸収する
- **アンカーHUD追加**: `_bdAnchorAddCard()` → `tree` を垂直展開として bottom=子、top=親との間、left/right=兄弟に分類し、子/兄弟/間挿入の仮配置をツリー専用の縦積み・右インデントに合わせた
- **Ctrl+矢印**: `bdHandleCtrlArrow()` で `tree` を専用分岐し、上下で兄弟入れ替え、右で展開、左で折りたたみ。既存 flowchart/orgchart の左右入れ替え挙動には影響させない
- **オートレイアウト**: `bdAutoLayout()` の内側構造計測と外側構造配置の両方で `bdLayoutTree()` を呼び、入れ子構造カードを bbox atomic として扱う既存 3 パス方式を維持
- **接続線描画**: `bdDrawConns()` / `_bdBuildConnectionPathData()` / `_bdOrthogonalizePoints()` で `tree` を縦方向構造として扱い、曲線・直角線・アンカー指定あり/なしの各経路で横方向へ誤判定しないことを確認
- **フォルダサムネイル選択**: `renderFolderItems()` が生成する `.fv-item > .fv-check` に左上固定ルールを追加し、アプリ共通チェックボックスCSSの相対配置指定がサムネイル項目内の絶対配置を上書きしないことを確認

検証: `node --check app/gb-canvas-features.part01.js`、`node --check app/gb-canvas-engine.part01.js`、`node --check app/gb-canvas-engine.part02.js`、`node --check app/gb-canvas-engine.js`、`python -m unittest discover -s app/tests -p "test_meldex_frontend_regressions.py" -v`、`git diff --check`

## v0.5.370 (2026-04-28)

### fix(desktop): 開発起動をWebView2プロファイルに固定

- **start-editor.batの標準プロファイルを明示** ([start-editor.bat](start-editor.bat)) — 開発起動時は `MELDEX_FORCE_WEBVIEW=1` と `MELDEX_STRICT_WEBVIEW=1` を設定し、使用プロファイル `LOCALAPPDATA\Meldex\webview-desktop-v2` を表示するようにした
- **WebView2固定モードではChrome appへフォールバックしないよう修正** ([gb-window.py](gb-window.py), [meldex_runtime_service.py](meldex_runtime_service.py)) — WebView2起動やランチャー起動に失敗しても `browser-app-profile` 側へ黙って切り替わらないようにし、テーマ・レイアウト・localStorage設定の分岐を防止
- **開発起動プロファイルの回帰テストを追加** ([tests/test_meldex_batch55_cloud_phase1_regressions.py](tests/test_meldex_batch55_cloud_phase1_regressions.py)) — strict WebView2固定、GPU合成OFF、profile表示、フォールバック抑止を通常テストで検知できるようにした

### 徹底チェック結果

- **通常起動**: `start-editor.bat` → `MELDEX_WEBVIEW_DEBUG=` / `MELDEX_FORCE_WEBVIEW=1` / `MELDEX_STRICT_WEBVIEW=1` → `Meldex.py` → `_open_app_window()` → `gb-window.py` → `_start_webview()` → `storage_path=webview-desktop-v2`。Chrome app profileを使わない
- **WebView2起動失敗**: `gb-window.py main()` → `_start_webview()` 例外 → strict時はブラウザフォールバックせず例外終了。`browser-app-profile` へ切り替わらない
- **ランチャー起動失敗**: `_open_app_window()` → `subprocess.Popen(gb-window.py)` 例外 → strict時はフォールバックせずreturn。Chrome appを起動しない
- **デバッグ/非常用起動**: `MELDEX_WEBVIEW_DEBUG=1` かつ `MELDEX_FORCE_WEBVIEW` なしの明示デバッグでは従来のbrowser app fallbackを残し、`start-editor.bat` の通常起動とは分離
- **停止処理**: `stop-editor.ps1` は `browser-app-profile|webview-desktop-v2` の両方を対象に含むため、旧Chrome app側の残存プロセスも掃除できる

検証: `node --check app/gb-shortcuts.js`、`node --check app/meldex-core.part01.js`、`python -m unittest discover -s app/tests -p "test_meldex_batch55_cloud_phase1_regressions.py" -v`、`python -m unittest test_meldex_frontend_regressions.MeldexFrontendRegressionTests.test_batch22_window_launcher_guards_are_present -v`（cwd `app/tests`）、`python -m unittest discover -s app/tests -v`、`git diff --check`

## v0.5.369 (2026-04-28)

### test(scriptnote): シナリオ微細挙動E2Eを追加

- **シナリオ微細挙動E2Eを追加** ([gb-e2e-actions-scriptnote-micro.js](gb-e2e-actions-scriptnote-micro.js), [tests/meldex_scriptnote_micro_coverage.py](tests/meldex_scriptnote_micro_coverage.py), [tests/meldex_frontend_e2e_targeted_scriptnote_micro.py](tests/meldex_frontend_e2e_targeted_scriptnote_micro.py)) — ルビの横書き/縦書き位置、Shift+Enter末尾改行、上下キーでのセル移動と元セル復帰時のキャレット位置を、実ブラウザ操作とDOM/Selection座標で検査できるようにした
- **微細挙動ケースを17件に拡張** — Enter/Ctrl+Enter分割、ルビ内部分割、単一フィルタ時の行追加、Backspace/Delete結合、末尾改行Delete、Shift+Delete、複数行paste、Ctrl+Shift+Vセル内paste、フィルタ付きShift/Alt移動、Ctrl+R選択ルビ挿入、縦書き境界移動を決定的ケースとして追加した
- **専用ランナーを追加** ([tests/run_frontend_e2e_scriptnote_micro.ps1](tests/run_frontend_e2e_scriptnote_micro.ps1)) — シナリオ微細挙動ケースだけを一括実行または `-ListOnly` で確認可能にした
- **Shift+Enter末尾改行の番兵BRを保存値から除外** ([gb-scriptnote-editor.part04.js](gb-scriptnote-editor.part04.js)) — 表示用の末尾 `<br>` が `.scriptnote.json` 上の余計な改行として保存されないようにした
- **Shift+Enter直後のSelection復元を修正** ([gb-scriptnote-editor.part03.js](gb-scriptnote-editor.part03.js)) — スクロール追従用マーカー削除後もキャレットが本文セル内に残るようにした
- **`.sn2-text` 自身を起点にしたキャレットオフセット取得を修正** ([gb-scriptnote-editor.part02.js](gb-scriptnote-editor.part02.js)) — 改行直後などSelectionコンテナが本文セル要素になるケースでも文字オフセットを取得できるようにした
- **Enter分割/paste分割のSelection退避を修正** ([gb-scriptnote-editor.part03.js](gb-scriptnote-editor.part03.js), [gb-scriptnote-editor.part04.js](gb-scriptnote-editor.part04.js)) — Undo履歴パネル描画や再描画時focusoutでSelection/古いDOMが本文分割位置を壊さないよう、キー入力時点のオフセットを先に取得し、切り離された本文セルはfocusout同期しないようにした
- **行選択バーにE2E安定IDを追加** ([gb-scriptnote-selection.js](gb-scriptnote-selection.js)) — Shift+矢印選択後に表示される一括操作ボタンも可視UIガードで検査できるようにした
- **高リスクE2Eにシナリオ微細挙動17件を追加** ([tests/meldex_high_risk_coverage.py](tests/meldex_high_risk_coverage.py)) — 高リスク実行セットからもルビ/キャレット/行編集/paste/フィルタ移動系の細部退行を検知できるようにした

検証: `powershell -ExecutionPolicy Bypass -File app/tests/run_frontend_e2e_scriptnote_micro.ps1 -Browser chrome -StopOnFailure`、`powershell -ExecutionPolicy Bypass -File app/tests/run_frontend_e2e_scriptnote_micro.ps1 -ListOnly`、`powershell -ExecutionPolicy Bypass -File app/tests/run_frontend_e2e_high_risk.ps1 -ListOnly`、`python -m unittest app.tests.test_meldex_scriptnote_micro_coverage app.tests.test_meldex_high_risk_coverage app.tests.test_meldex_batch47_frontend_regressions -v`、`python -m unittest discover -s app/tests -v`、`git diff --check`

## v0.5.368 (2026-04-28)

### test(e2e): 高リスク領域の自動検知セットを追加

- **高リスク領域マニフェストを追加** ([tests/meldex_high_risk_coverage.py](tests/meldex_high_risk_coverage.py)) — 起動/レイアウト、保存/クラウド、シート整合性、スマートシート、ボード/注釈、シナリオ/履歴、カレンダー、版管理、ショートカット、比較/分割アセットの10領域を定義し、43件の targeted E2E と関連 unittest を紐付け
- **高リスクE2Eランナーを追加** ([tests/run_frontend_e2e_high_risk.ps1](tests/run_frontend_e2e_high_risk.ps1)) — マニフェストから対象ケースを取得して一括実行できるようにし、`-ListOnly` と `-StopOnFailure` に対応
- **高リスクカバレッジ検査を追加** ([tests/test_meldex_high_risk_coverage.py](tests/test_meldex_high_risk_coverage.py)) — 各ケースの存在、可視性/安定ID/アクセシブルラベル/required_features の runtime gate、関連 unittest と参照ソースの存在を通常 unittest で検査
- **シートのプロパティレイアウト操作に安定IDを追加** ([gb-detail-property-layout-editor.js](gb-detail-property-layout-editor.js)) — 高リスクE2Eで必要な並べ替え/テンプレート/リセット/保存範囲操作を安定して検出できるようにした
- **シナリオ theme/history E2E の状態保持を補強** ([gb-e2e-actions-extra.part02.js](gb-e2e-actions-extra.part02.js)) — undo/redo 後も追加ステータスと既存ステータスを併合保持し、失敗時は editor/file/last のスナップショットを含めて診断できるようにした
- **版管理E2Eの現行仕様期待値を修正** ([tests/meldex_frontend_e2e_targeted_version.py](tests/meldex_frontend_e2e_targeted_version.py), [tests/_generated/e2e_cases.json](tests/_generated/e2e_cases.json)) — ファイル版/シート版のパネルではフォルダ版セクションを期待しないように更新
- **テストREADMEに高リスク実行方法を追記** ([tests/README.md](tests/README.md)) — 高リスクE2Eの実行と対象ケース一覧確認のコマンドを追加

検証: `python -m unittest discover -s app/tests -v`、高リスク43件のChrome E2E分割実行、`powershell -ExecutionPolicy Bypass -File app/tests/run_frontend_e2e_high_risk.ps1 -ListOnly`、`git diff --check`

## v0.5.367 (2026-04-28)

### docs(home/manual): 配布ホームのサンプルと内部資料を整理

- **配布用サンプルを小型化** ([MeldexHome/サンプル](MeldexHome/サンプル)) — 旧テストDB/担当者DBの大量サンプルを削除し、タスク管理サンプルとカレンダーサンプルを追加
- **ホーム直下の作業用無題ファイルを削除** ([MeldexHome](MeldexHome)) — 配布版で不要な無題ノート/シナリオを同梱しない構成に整理
- **旧カレンダーサンプルをサンプルフォルダへ移動** ([MeldexHome/サンプル/カレンダーサンプル](MeldexHome/サンプル/カレンダーサンプル)) — ホーム直下にカレンダー例が混ざらないように配置を変更
- **内部向けマニュアル資料をdocsへ移動** ([docs/meldexhome-manual-internal](docs/meldexhome-manual-internal)) — 仕様書、制作ルール、旧マニュアル、デバッグマニュアル、スクショ撮影タスク、旧ショートカットエントリを配布用マニュアル外へ退避
- **初心者導線とショートカット一覧を更新** ([MeldexHome/マニュアル](MeldexHome/マニュアル)) — クイックスタート、UI用語ガイド、初心者向け簡易マニュアル、現行ショートカット一覧を整備
- **マニュアル制作ルールの参照先を更新** ([AGENTS.md](AGENTS.md), [CLAUDE.md](CLAUDE.md), [GEMINI.md](GEMINI.md)) — 内部資料移動後の `docs/meldexhome-manual-internal` を参照するように修正

検証: サンプル/マニュアル内のローカル絶対パス検索、内部資料移動の内容一致確認、Markdownローカルリンク確認、`git diff --check`

## v0.5.366 (2026-04-28)

### docs(site/manual): PRサイト実スクショ化とファイル復旧Q&Aを追加

- **外部向けPRサイトを追加** ([docs/meldex-pr-site/index.html](docs/meldex-pr-site/index.html), [docs/meldex-pr-site/styles.css](docs/meldex-pr-site/styles.css)) — Meldexの概要、主要機能、制作ワークフロー、データ形式、導入導線をまとめた静的サイトを作成
- **PRサイトに実際のMeldexスクリーンショットを組み込み** ([docs/meldex-pr-site/assets/](docs/meldex-pr-site/assets/)) — ローカル起動したMeldexからノート、シナリオ、シート画面を撮影し、外部向けにローカルパスが写らない作業領域中心の画像へトリミングして掲載
- **ファイル関連Q&Aを追加** ([MeldexHome/マニュアル/04_サポート/ファイル関連Q&A.md](MeldexHome/マニュアル/04_サポート/ファイル関連Q&A.md)) — ノート、シナリオ、カレンダー、シート、スマートシート、ボード、タイマー、バージョン管理、テーマ、注釈、インポート/エクスポート、Dropbox同期不全の保存場所と復旧手順を横断整理
- **既存FAQの保存場所説明を現行仕様へ修正** ([MeldexHome/マニュアル/04_サポート/よくある質問.md](MeldexHome/マニュアル/04_サポート/よくある質問.md)) — 主要作業ファイルはVault配下、一部のカレンダー/注釈/索引/設定はローカルDBやブラウザ設定に保存される旨を明記
- **サポート系マニュアルからファイル関連Q&Aへの導線を追加** ([MeldexHome/マニュアル/Meldex マニュアル.md](MeldexHome/マニュアル/Meldex%20マニュアル.md), [MeldexHome/マニュアル/04_サポート/トラブルシューティング集.md](MeldexHome/マニュアル/04_サポート/トラブルシューティング集.md))
- **PC単独モードのURL直開き制限を修正** ([gb-cloud-bootstrap.js](gb-cloud-bootstrap.js)) — Dropbox Phase 1の未対応タイプ判定をDropboxモード時だけ有効にし、PC単独モードでシート/カレンダー/スマートシートをURLパラメータから開けるようにした

検証: `node --check app/gb-cloud-bootstrap.js`、PRサイト内ローカルリンク/画像パス確認、Chrome headlessによるPRサイトのデスクトップ/モバイル表示確認

## v0.5.365 (2026-04-27)

### feat(cloud/mobile): スマホ右ドロワーとPWA起動体験を仕上げ

- **スマホ右ドロワーを追加** ([gb-cloud-mobile-side-drawer.js](gb-cloud-mobile-side-drawer.js), [gb-cloud-mobile.css](gb-cloud-mobile.css)) — Dropboxスマホ表示で、シートのエントリ名タップとボードのリンクカード選択時に右側から約3分の2幅のドロワーを開き、リンク先/エントリを表示できるようにした
- **ドロワーから通常ビューで開いて編集するボタンを追加** — ドロワー内の未保存本文を先に保存し、保存成功後だけ `navOpen` / `selectEntity` の通常経路へ遷移して編集対象へフォーカスする
- **ドロワー内エントリ本文の保存先を通常エントリ編集と統一** — 新形式 `.md` エントリは `/value` の `new_body` 更新、旧形式フォルダエントリは `_freetext.md` 保存に揃え、右ドロワー編集だけ別ファイルへ保存される不整合を防止
- **PWA起動体験を整備** ([Meldex.html](Meldex.html), [manifest.json](manifest.json), [sw.js](sw.js)) — iOS/Android向けメタ情報、`viewport-fit=cover`、`display_override`、テーマ色、カテゴリを追加し、ホーム画面から単独アプリ風に起動しやすくした。Service Workerはキャッシュを持たないオンライン前提のまま維持
- **PWA解除導線を追加** — `?desktop=1` に加えて `?pwa=0` / `?nopwa` でもService Worker登録とCache Storageを解除できるようにし、開発中の古いJS/CSS残りを避けやすくした
- **配布用Dropbox App keyを同梱** ([gb-config.js](gb-config.js), [gb-cloud-bootstrap.js](gb-cloud-bootstrap.js)) — 公式/配布用Meldexで利用者が個別にDropbox Appを作らず、既定App keyでDropbox共有モードを始められるようにした
- **初心者向けDropbox共有モード手順とPWA手順を追記** ([MeldexHome/マニュアル/03_設定と連携/Dropbox共有モード かんたん開始.md](MeldexHome/マニュアル/03_設定と連携/Dropbox共有モード%20かんたん開始.md), [MeldexHome/マニュアル/03_設定と連携/スマホ・タブレットからの利用.md](MeldexHome/マニュアル/03_設定と連携/スマホ・タブレットからの利用.md))
- **計画書更新**: [docs/mobile-cloud-ui-refactor-plan-2026-04-27.md](docs/mobile-cloud-ui-refactor-plan-2026-04-27.md) に右ドロワーとPWA仕上げフェーズを反映

### 徹底チェック結果

- **シートのエントリ名タップ**: `openEntityInSplit()` → `MeldexCloudMobileSideDrawer.openEntity()` → `/entity` 読み込み → ドロワー表示 → 本文編集 → `_flushActiveEditor()` → `_saveEditor()`。閉じる/外側タップ/Escape/通常ビューで開くの全経路で保存成功を待ち、保存失敗時はドロワーを閉じない
- **ボードのリンクカード選択**: `bdSelect()` → `openBoardLink()` → `bdRenderLinkedPreview()`。スマホドロワーが処理した場合はPC向け右パネル同期を抑止し、表示切替・閉じる・別リンク選択で古いプレビューが後から上書きしないよう `_renderSeq` で失効させる
- **通常ビューで開いて編集**: ドロワーの開くボタン → 保存flush → `_bdResolveLinkedEntryAsync()` / `navOpen()` / `selectEntity()` → 編集対象フォーカス。処理中にドロワーが閉じられた場合や別対象に切り替わった場合は遷移を中止する
- **PWA起動/解除**: `Meldex.html` 起動時にstandalone判定を記録し、通常起動ではService Workerを登録、`?desktop=1` / `?pwa=0` / `?nopwa` では登録解除とCache Storage削除へ分岐する。Service Workerはfetchに介入せず、既存API通信を変えない

検証: `node --check`（関連JS）、`manifest.json` JSON parse、`python -m unittest app.tests.test_meldex_batch55_cloud_phase1_regressions app.tests.test_meldex_batch56_localfs_storage_regressions app.tests.test_meldex_batch57_cloud_phase2_regressions`、`git diff --check`

## v0.5.364 (2026-04-27)

### feat(cloud/mobile): DropboxスマホUIのボード・注釈・チャット操作を拡張

- **スマホ用モバイル編集バーを追加** ([gb-cloud-mobile-editbar.js](gb-cloud-mobile-editbar.js), [gb-cloud-mobile-editbar.css](gb-cloud-mobile-editbar.css)) — Dropboxスマホ表示時は上部アプリバー/ツールバーを畳み、メニュー・本文編集バー・入力バー・チャットバー・ボード下部バー・注釈バーへ操作を集約
- **ボードのスマホ操作を拡張** ([gb-cloud-mobile-board-actions.js](gb-cloud-mobile-board-actions.js), [gb-canvas-interact.js](gb-canvas-interact.js)) — 追加/子カード/ライン/スタイル/Undo/全体表示/メニューを下部バー化し、2本指ズームは18度以上ひねるまで回転しないように調整
- **ボードの注釈開始ボタンを右下の丸いFABに分離** — ボード下部バーの上に表示し、タップ直後に注釈専用バーへ切り替える。注釈付箋入力中もキーボード直上に注釈バーを維持
- **ボードのスタイルボタンの遷移先を修正** — カード/ライン選択中は個別スタイル欄へ、未選択時は現在のアクティブカードスタイル管理へ移動し、変更対象が見た目に反映されない状態を避ける
- **スマホメニューからチャットを開けるように修正** ([gb-cloud-bootstrap.js](gb-cloud-bootstrap.js)) — Dropbox共有モードでもチャットタブをブロックせず、チームチャットのルーム/メッセージ保存をクライアント側Dropboxファイルで処理
- **Dropbox競合コピー増殖を抑制** ([gb-storage-adapter.js](gb-storage-adapter.js)) — stale rev 競合時は最新metadataで再試行し、短時間の連続競合は同じ競合コピーを更新する
- **新規DropboxチャットAPIを追加** ([gb-data-access-dropbox-collab.js](gb-data-access-dropbox-collab.js)) — `/collab/rooms` `/collab/messages` `/collab/send` などをDropbox保存モードで提供
- **計画書更新**: [docs/mobile-cloud-ui-refactor-plan-2026-04-27.md](docs/mobile-cloud-ui-refactor-plan-2026-04-27.md)

検証: `node --check`（関連JS）、`python -m unittest app.tests.test_meldex_batch55_cloud_phase1_regressions app.tests.test_meldex_batch56_localfs_storage_regressions app.tests.test_meldex_batch57_cloud_phase2_regressions`、`git diff --check`

## v0.5.363 (2026-04-26)

### fix(board/options): カードスタイル・ラインスタイル・階層別スタイルタブの不具合を一括修正

- **チェックボックスのチェック位置を中央に補正** ([gb-ui.part02.css](gb-ui.part02.css)) — L 字を 45° 回転させると視覚的重心が右下にズレるため、`mark-stroke` の半分を上方向に translate して箱の中心に揃えた
- **プレビュードロップダウンを最上部に再配置** ([gb-board-style-manager.part02.js](gb-board-style-manager.part02.js)) — カード/ライン/階層別の 3 タブとも、ピッカーをタブ上部に出し、編集フィールドをその下に配置
- **左カラムの大きなプレビュー枠を撤去** — `.bd-detail-section` の auto-fill 多カラム化と組み合わさって左に巨大空白が出ていた `.bd-style-editor-preview` を HTML から削除し、設定項目だけを縦 1 カラムで表示
- **ピッカー選択後にポップアップが画面左上に飛ぶ不具合を修正** — 選択でタブが full re-render → 旧アンカーボタンが DOM 切断 → `getBoundingClientRect()` が 0,0,0,0 を返していた。ポップアップ側に `refreshAnchor` フックを追加し、再描画後の最新ボタンを取り直してから位置計算する
- **階層別スタイルでラインカラー設定時に階層 1 へ戻る不具合を修正** — `bdRefreshSelectionDetails(true)` がタブ DOM を空にして再描画する際、`selectedIndex` 未指定だと既定 0 にフォールバックしていた。`_bdLastDepthEditIndex` に最後の編集 index を保存して使う
- **カード/ラインスタイルタブも同じ強制復帰の問題があったため併せて修正** — `_bdLastCardEditId` / `_bdLastLineEditId` を保持し、active style への自動復帰を防ぐ
- **階層別ピッカーのプレビュー枠サイズを 200px に拡大** ([gb-tools.part03.css](gb-tools.part03.css)) — カード+ラインの 2 セルが 120px 枠からはみ出していた問題を解消
- **階層別ポップアップリストのプレビュー幅を 2 セル分に修正** — `.bd-depth-style-manager` セレクタしか書かれておらず、in-panel ポップアップ `.bd-depth-manager-popup` には適用されず 1 セル分しか表示されなかった
- **ピッカーボタン全体に `overflow: hidden` を追加** — プレビュー要素が枠を超えて溢れる症状を CSS 側でも遮断

## v0.5.362 (2026-04-26)

### docs(manual): 全機能マニュアル整備と用語統一・実装追従

公開用 vault `app/MeldexHome/マニュアル/` 配下のユーザーマニュアルを、Meldex の現行全機能を網羅する形に整備した。コミット 8346d8d / 2330758 / 707fe33 / e919d74 / 65d0944 / df9cf44 の 6 件で完走。

**Phase 1: 基盤整備**
- [99_制作ルール/マニュアル作成ルール.md](MeldexHome/マニュアル/99_制作ルール/マニュアル作成ルール.md) に**赤字スクショ指示記法**を追加。`<span style="color:#e53935">📷 スクリーンショット: ...</span>` 形式で未撮影が一目で分かる
- [99_制作ルール/用語統一表.md](MeldexHome/マニュアル/99_制作ルール/用語統一表.md) にタイマー / 比較ビュー / メディア / ファイル拡張子表を追記
- 旧称マニュアル 8 本を `old/` に退避（台本エディタ / データベース / メモ仕様書 / サンプルDB / データベース機能チェックリスト / 旧ガイド3本）
- 既存マニュアル 8 本のスクショ HTML コメント 45 か所を赤字 span へ一括置換

**Phase 2-3: 全機能マニュアル新規作成（24 本）**
- ツール: シート / スマートシート / カレンダー / アクション / ダッシュボード / エクスプローラー / ビューワー / スライドショー / 比較ビュー / タイマー / 注釈・付箋 / ボード
- パネル: 詳細 / メディア / ヒストリー / 全文検索
- 横断機能: インポート・エクスポート / 自動リンク・バックリンク
- バージョン管理 マニュアルの全面執筆

**Phase 4: 設定・連携・マルチユーザー（7 本）**
- テーマとフォント / LLM 設定 / システムトレイ常駐
- クラウド初期セットアップ / マルチユーザー運用 / チームチャット運用 / 共同編集の競合と解決
- 後に Phase 1 実装ファイル（[gb-cloud-bootstrap.js](gb-cloud-bootstrap.js) / [gb-settings.part01.js](gb-settings.part01.js) / [gb-settings.part03.js](gb-settings.part03.js) / [Meldex.html](Meldex.html) / [meldex_api_collab.py](meldex_api_collab.py)）から実 UI 文字列を抽出して反映:
  - 「Meldex の保存モード」/「Dropbox 連携設定」ダイアログの実セクション構成
  - ボタンラベル「Dropbox に接続」「手動コード入力で認証」「認証を解除」「PC 単独モードへ戻す」「接続確認して開始」
  - 権限ラベル「管理者 / 編集者 / 閲覧者」
  - チャットタブ順「チーム / LLM / 履歴」
  - 未実装機能（既読表示・WebRTC P2P・オフラインバナー）を Phase 2/3 予定として明示

**Phase 5: サポート系の補完**
- トラブルシューティング集 / よくある質問 を新規作成
- [04_サポート/ショートカット一覧/ショートカット一覧.md](MeldexHome/マニュアル/04_サポート/ショートカット一覧/ショートカット一覧.md) を 124 エントリのカテゴリ別総合インデックスに改修（採用 73 / 提案 51）

**Phase 6: 統合**
- [Meldex マニュアル.md](MeldexHome/マニュアル/Meldex マニュアル.md) の関連マニュアル節を全マニュアル網羅版に改稿
- 旧称（メモ → 注釈、データベース → シート、台本 → シナリオ、キャンバス → ボード、画像ツールガイド → 画像ツールの設定）を主要マニュアル全体で統一
- ショートカット一覧フォルダ内 26 ファイルのファイル名サフィックスを `（キャンバス）→（ボード）` `（DB）→（シート）` にリネーム
- CLIP STUDIO PAINT 連携マニュアルを新規作成（インポート・エクスポートからの死リンク解消）
- カレンダー機能仕様書の Notion 自動リンク誤動作による死リンクを除去
- 死リンク 5 → 0、旧称ファイル名 26 → 0 を達成

公開用ビルド（[build_exe.py](build_exe.py) L311-313）で配布フォルダに同梱されるため、初回起動時のホームに展開される。

## v0.5.361 (2026-04-25)

### feat(tooltip): ツールチップ品質改善（中央レジストリ + 引数特化キー + ショートカット連携）

**背景**: ツールチップが「（機能名）を設定します」「この操作を実行します」のような薄い汎用文言で埋まっており、機能名の繰り返しに留まり解説になっていなかった。ショートカット末尾付与も `data-shortcut-id` 属性付き要素に限定されていた。

**基盤改修**:
- [gb-tooltip.js](gb-tooltip.js) に中央レジストリ層 `REGISTRY` を追加。`id / data-* / data-action / icon` のキーを横断検索する構造
- `formatRegistryEntry()` で `label — desc (Ctrl+S)` 形式に整形
- `getTooltipText()` をレジストリ最優先に書き換え、薄いフォールバックはレジストリ未ヒット時のみ
- `lookupShortcutKey()` / `lookupShortcutDisplay()` で実行時に `getShortcutKey()` を参照（カスタム設定したキーも自動反映）
- `data-action` の第1引数までキーに含める引数特化検索を追加（`rtCmd('bold')` → `action:rtcmd:bold`、`doVaultReplace(true)` → `action:dovaultreplace:true` 等）
- 引数の正規化は `String(arg).trim().toLowerCase()` のみ（dash や符号付き数値の区別を保持）

**フォールバック冗長文言の削除**:
- `labelHint`: `label + 'を設定します'` → `label` のみ
- `controlFallbackHint`: `'この操作を実行します'` `'値を入力します'` `'選択肢を変更します'` `'テキストを入力します'` `'検索語を入力します'` `'オン / オフを切り替えます'` `'この項目を選択します'` `'このタブを表示します'` を削除。`input[type=range]` `[type=color]` `draggable` のみ操作方法のヒントを残す
- `visibleTextHint`: `'を実行します'` `'を表示します'` 接尾辞を削除し可視テキストをそのまま返す
- `placeholderHint`: `'を入力します'` 接尾辞を削除

**registry 連携拡張**:
- `getTooltipText()` で registry hit 時にも `appendShortcut()` を経由。registry エントリに `shortcutId` が無くても要素に `data-shortcut-id` があれば末尾に付与する（二重付与は正規表現で防止）

**領域別バッチ B1〜B14 の解説文登録**:
- B1 アプリバー / 右アクティビティバー
- B2 フォルダツリー / ソースフォルダ管理 / フォルダビューツールバー
- B3 ノートツールバー（書式・見出し・リスト・表・ルビ・リンク・コードブロック）
- B4 シナリオツールバー / カラム操作 / フィルタ
- B5 シート / スマートシート / カレンダー
- B6 ボード（ツール・ズーム・スタイル・背景・検索）
- B7 注釈・チャット・LLM・チームチャット・DM
- B8 設定ダイアログ各タブ・共通
- B9 各種モーダル / フォーム入力 / 共通ボタン
- B10 動的生成ボタン群（バージョン管理パネル / タブ・パネル / 履歴 / 注釈マネージャ / テーマ・配色 / ファイル別スタイル / フォント / ゴミ箱 / スマートシート 約60件）
- B11 ボード詳細・カレンダー詳細・バージョンアクション（bd-action のスタイル管理・固有スタイル・形状リセット 約20件、cal-action addEvent/addTask、data-version-action 系10件）
- B12 主要 input/slider/select（注釈ツール幅、チャット入力、URL バー、タイマー、ノート余白、ルビ、タイトル、ファイル選択、RPA プロンプト 約25件）
- B13 画像ビューワー・CSP連携・カレンダー詳細・ボード検索バー（約60件）
- B14 全文検索・CSV・チャット選択・注釈フィルタ（約25件）

**徹底チェックで発見・修正したバグ**:
- B10 初版で regAction キーが camelCase のまま登録されており、`normalizeKey` で小文字化されたルックアップ側と一致せず約60件全て不発だったバグを全面修正
- `openCsPaletteRgba` の registry キーで rgba の 'b' 抜けタイポ修正
- B11 で `bd-action="bg-image"` `"bg-clear"` が HTML 実値の `set-bg-image` `clear-bg-image` と不一致だったバグ修正
- 引数 `-1` と `1` が `normalizeKey` で同一キーに正規化される衝突を発見し、引数専用の軽量正規化に切替
- 汎用 action エントリが個別 title を上書きするリグレッション（rtCmd系、doVaultReplace、doFileSearch）を引数特化エントリに置換

**最終登録数**:
- regId 170件 / regAction 153件 / regData 99件 / regIcon 11件 / 合計 433件

**計画書**: [docs/tooltip-quality-improvement-plan-2026-04-25.md](docs/tooltip-quality-improvement-plan-2026-04-25.md)

**残課題**:
- `_updateAllTooltips()`（gb-shortcuts.js）のレガシー登録は当面共存。レジストリ優先になったため重複しても表示は新ルールで上書きされる
- ダイアログ内・サブビュー・コンテキストメニュー等の個別要素は次回以降のバッチで順次拡充

## v0.5.360 (2026-04-25)

### feat(window): 単一タブポップアウト窓機能 + 関連UI改善

**背景**: パネルタブ・アウトラインノード・フォルダ項目をウィンドウ外にドラッグ＆ドロップすると、`?single=1` URL付きの**Chrome --app モード独立ウィンドウ**として開く機能。シェル要素（アプリバー/サイドバー/ステータスバー/活動バー/詳細サイドパネル/ペインタブバー/ドックバー等）を一切表示せず、ドラッグしたタブ単体の内容だけを全面表示する。元ウィンドウの localStorage（lastView / app-layouts 等）は汚染しない設計。

**ヘルパー関数（既に [meldex-core.part01.js](meldex-core.part01.js) と [gb-app.part02.js](gb-app.part02.js) に実装済み）**:
- `buildSingleTabWindowUrl(item)` — `?single=1&type=...&path=...&label=...` URL生成
- `openItemsAsSingleTabWindows(items)` — Chrome --app モード優先で単一タブ窓を開く
- `isDragDroppedOutsideWindow(e)` — dragend イベントから窓外ドロップを判定
- `setLowOpacityDragImage(e, sourceEl, opacity)` — ドラッグプレビューを低不透明度化（ドロップインジケータ可視性確保）
- `_open_app_window_js(url)` — pywebview バックエンド経由で Chrome --app ウィンドウを起動

**ドラッグ起点の追加**:
- [gb-folder.part01.js](gb-folder.part01.js): フォルダグリッド項目を窓外にドロップで popout
- [gb-outliner.part01.js](gb-outliner.part01.js): アウトラインツリーノードを窓外にドロップで popout（複数選択対応）
- [gb-layout.part01.js](gb-layout.part01.js): ペインタブ自体を窓外にドロップで popout
- いずれも `text/uri-list` を併用して OS シェル側の禁止カーソルを軽減

**単一タブ窓モードの実装**:
- [Meldex.html](Meldex.html): `?single=1` 検出 → `window._gbSingleWindow = true` + `document.documentElement.dataset.singleWindow = '1'` + `_skipLastViewSave = true`
- [gb-layout.css](gb-layout.css): `html[data-single-window="1"]` セレクタで全シェル要素を `display: none !important`
- [gb-app-layout.js](gb-app-layout.js): `_persistCurrentAppLayout` で単一窓時は保存スキップ（元ウィンドウ汚染防止）
- [gb-app.part02.js](gb-app.part02.js): `saveLastView()` も単一窓時は常にスキップ

**URL復元の拡張**:
- [gb-app.part02.js](gb-app.part02.js): `init()` の URLパラメータ復元を `media`/`image`/`video`/`audio`/`scriptnote`/`scenario`/`smart-db` に拡張
- [gb-app.part01.js](gb-app.part01.js): `navOpen(entry, opts)` に `opts` 引数追加し、全種別で透過

**「新しいウィンドウで開く」を改善**:
- [gb-outliner.part02.js](gb-outliner.part02.js): 右クリックメニューの「新しいウィンドウで開く」を Chrome --app モード優先（旧 `window.open` フォールバック）に変更

**併せて修正された関連UI問題**:
- [Meldex.html](Meldex.html): `welcome-view` の初期 `display:flex` → `display:none` に変更。ペインシステムでボード復元時に showView が呼ばれず案内文が残留する不具合を修正。
- [gb-app.part02.js](gb-app.part02.js): `init()` の welcome フォールバックを「ペインにタブが1つも無い時のみ」に厳格化
- [gb-panelset.js](gb-panelset.js):
  - `_TAB_TYPE_ICON_MAP` を [gb-outliner-search.part02.js](gb-outliner-search.part02.js) の `PANEL_MENU_SECTIONS` と完全一致するよう更新（folder/page/scriptnote/database/board/calendar/smart-db/outliner/preview/detail/version/chat/timer/history/annotation の15種）
  - マルチグループ時の clickable ヘッダを廃止しスペーサーのみに（タブアイコンクリックで `switchGroup` するため重複ボタン不要）
  - 全グループのアイコン総数が0の場合は外側ドックバーを描画しない（**「ドックバーが二列に見える」不具合の修正**）
- [gb-outliner.part01.js](gb-outliner.part01.js): ドラッグESCキャンセル時のタブバー挿入位置マーカー漏れを修正
- [gb-layout.css](gb-layout.css): タブのドロップインジケータをアクティブタブ背景に隠れないよう疑似要素 + `position: relative` + z-index で前面化、太さ3pxで視認性向上

## v0.5.359 (2026-04-25)

### refactor: 1500行超ファイルの分割 + rpa.html 内部JS外出し + 既存重複コード解消

**背景**: バイブコーディング品質ルール「1500行超ファイルへの機能追加は原則禁止 — 先に分割してから追加する」に基づき、開発中アプリの全1500行超ファイルを棚卸しして分割。スプリットローダー (`gb-split-loader.js`) を活用し、ロジック変更ゼロの純粋な分割のみ実施。

**JS 10ファミリーを分割**（連結後の `node --check` 全てSYNTAX OK、徹底チェックでスタブ整合性・連結後構文・トップレベル重複宣言・参照整合性すべて ALL CLEAR）:

| ファミリー | 元 | 分割後 |
|---|---|---|
| [gb-e2e-actions](gb-e2e-actions.js) | 2588行単一 | stub + part01 (1290) + part02 (1300) |
| [gb-e2e-actions-extra](gb-e2e-actions-extra.js) | 2186行単一 | stub + part01 (1099) + part02 (1089) |
| [gb-theme-manager](gb-theme-manager.js) | 2113行単一 | stub + part01 (1038) + part02 (1077) |
| [gb-board-style-manager](gb-board-style-manager.js) | 1788行単一 | stub + part01 (1048) + part02 (742) |
| [gb-canvas-engine](gb-canvas-engine.js) | part02=2431行 | part01-04 (max 1505) |
| [gb-canvas-features](gb-canvas-features.js) | part01=1864行 | part00-04 (max 1022) |
| [gb-detail-panel](gb-detail-panel.js) | part01=1811行 | part01-03 (max 1094) |
| [gb-layout](gb-layout.js) | part01=1610行 | part01-03 (max 1019) |
| [gb-settings](gb-settings.js) | part02=2373行 | part01-05 (max 1199) |
| [meldex-core](meldex-core.js) | part01=1737行 | part01-03 (max 929) |

**HTML/JS 分離** (`HTML/CSS/JS が混在する単一ファイルが肥大化した場合、JSを外部ファイルに分離する` 規則):
- [rpa.html](rpa.html): 1560行 → 240行。inline `<script>` 1320行を [rpa-main.js](rpa-main.js) に外出し。HTMLは骨格・スタイル・5本の外部スクリプト参照のみに整理。

**既存リファクタリングのコード重複を解消**:
- [gb-e2e-actions-extra.part01.js](gb-e2e-actions-extra.part01.js) / [gb-e2e-actions-extra.part02.js](gb-e2e-actions-extra.part02.js): 初版分割でpart02に独立IIFEを作りヘルパー374行を重複させていたのを修正。part01のIIFE終端 `})();` を削除し、part02のIIFE開始ラッパー+重複ヘルパーを削除して、連結時に元の単一IIFEを復元する設計に変更。registerAction/Assertion 計43件の動作不変。

**残2件は許容範囲**:
- [gb-canvas-engine.part01.js](gb-canvas-engine.part01.js) (1505行、+5)
- [gb-layout.css](gb-layout.css) (1502行、+2)

いずれも誤差範囲のため、再分割の混乱コストに見合わず保留。今後の機能追加時に必要なら個別対応。

**メカニズム**: 既存の [gb-split-loader.js](gb-split-loader.js) が XHR でチャンク文字列を取得→末尾改行を保証して連結→1つの `<script>` として実行する方式のため、IIFE内側で分割しても問題なし。**Meldex.html のスクリプトロード順は変更なし**（スタブ名が同じまま）。

**徹底チェック結果（4並列エージェントで実施）**:
- スプリットローダー整合性 (10ファミリー全て): ALL CLEAR
- gb-e2e-actions-extra IIFE修正: ALL CLEAR (IIFE 1組、ヘルパー定義1回ずつ、registerAction 43件)
- ユーザー操作チェーン (起動/ボード/詳細/設定/レイアウト): ALL CLEAR
- rpa.html / rpa-main.js 抽出: ALL CLEAR

**全体チェック結果（haikuモデル30バッチで96ファイル監査）**:
- 検出80件超のうち多くは仮説的・誤読を含む
- 直前リファクタリング起因の問題は0件
- 既存コードベースの潜在的懸念は別途トリアージ予定（本コミットには含めない）

## v0.5.358 (2026-04-25)

### fix(annotation): 付箋選択時の四隅の丸を撤去し、カードと同じ選択ハイライトに統一

**背景**: 注釈ツールの付箋を選択すると、四隅のリサイズハンドルに小さい丸が表示されていた。ボードカードの選択枠 (`.bd-selection-rect`) と一貫性が無かったため、統一された選択表現にする。

- [gb-tools.part01.css](gb-tools.part01.css): `.ann-note-resize-handle::after` による四隅の丸表示を削除。`.ann-note.ann-note-selected` に `outline: 2px solid var(--bd-selection-color, #4a90d9)` + `outline-offset: 2px` を追加し、ボードカードの選択枠と同配色・同幅に揃えた。リサイズ当たり判定自体 (透明ハンドル) は維持。

### feat(board): Enter で同階層カード追加、カード選択中の文字入力は末尾追記

**背景**: 旧仕様では Enter = 編集開始、Shift+Enter = 同階層カード追加、カード選択中の文字入力で既存テキストが消去されていた。Enter を主要操作 (同階層追加) に昇格させ、文字入力は既存テキストを保つ挙動に変更。

- [gb-shortcuts.js](gb-shortcuts.js): `board.editEnter` (Enter = 編集開始) を撤去。`board.addSibling` のキーを `shift+enter` → `enter` に変更。Shift+Enter の明示ショートカットは廃止 (編集中の改行は contenteditable のデフォルト挙動で残る)
- [gb-canvas-interact.js](gb-canvas-interact.js): 1枚選択中に印字可能文字を押した時、`txt.innerText = typed` (置換) → `(txt.innerText || '') + typed` (末尾追記) に変更。`bdEditNode` が既存テキストを innerHTML に戻した後に末尾へキャレットを置き、タイプ済み文字を追加する
- [gb-canvas-features.part02.js](gb-canvas-features.part02.js): コンテキストメニュー表記を `テキスト編集 (F2 / Enter)` → `テキスト編集 (F2)`、`同階層カード追加 (Shift+Enter)` → `同階層カード追加 (Enter)` に更新。ヘルプ一覧も `Enter = 同階層カード追加` / `Shift+Enter = カード内改行 (編集中)` に
- [gb-board-ui.part01.js](gb-board-ui.part01.js): 関連コメントを Enter 主体に更新

### feat(board): マインドマップ構造のラインを「直線」に固定 + 子カードを時計回り均等配置

**背景**: マインドマップ構造では、放射状に広がるラインが曲線だと混線しやすい。またルートに子カードを追加する順序が不規則で、二周目に入ると既存カードと衝突していた。

- [gb-canvas-engine.part02.js](gb-canvas-engine.part02.js) `_bdLinePathType(connStyle, structure)`: structure 引数を追加。`structure === 'mindmap'` のとき pathType を強制的に `'straight'` に。色・太さ・矢印などのラインスタイル他設定はユーザー値を維持。`_bdBuildConnectionPathData` 側でも `effectiveStructure === ''` のときは `bdStructureOf(conn.from)` でフォールバック取得し、サブ階層のラインも直線に統一
- [gb-canvas-features.part01.js](gb-canvas-features.part01.js) `bdLayoutMindmap`: 従来の 360°/N 均等配置 (位置固定なし) を廃止。N 枚の子カードを真右 (0°) を始点とした 360°/N° 刻みで時計回り均等配置するアルゴリズムに変更
  - 1 枚目 = 真右 (0°)、2 枚目以降は右下→真下→左下→真左→左上→真上→右上 の順に間隔が狭まりながら追加される
  - 最終インデックスは `(N-1)/N × 360°` なので真上 (270°) を越えて右上〜真右の間に収まり、二周目に入らない
  - 下限半径を `(maxHalfDiag + G.sibling/2) / sin(ANGLE_STEP/2)` で算出し、全子カードが同じ円周上で隣接時に重ならないように保証。N=1 は 0 除算回避で下限なし

### feat(board): 新規作成ボードの初期ルートカードで「階層別スタイル」を既定 ON

**背景**: 新規ボードを開くと1枚ルートカードだけが置かれるが、階層別スタイル (`_autoStyle`) が OFF のため、子カードを追加してもスタイルが反映されなかった。新規作成直後から階層別スタイルが有効な状態で使い始めるように初期値を変更。

- [meldex_api_outliner.py](meldex_api_outliner.py): `/outliner/add` の board テンプレートを `---\ntype: board\nxmind:\n  n0: {autoStyle: true}\n---\n# {label_name}\n\n` に変更。`bdParseMd` の `autoStyle: true` 検出経路で n0 (ルートカード) の `_autoStyle` が true で復元される
- [gb-canvas-engine.part03.js](gb-canvas-engine.part03.js) `bdOpen`: サーバー再起動前のフォールバックとして、ノード 1 枚 / コネクション・グループ無し / 付加メタ (親・構造・スタイル・変形・xmind 拡張) 一切無しの「新規作成直後」形状を検出した場合に `_autoStyle = true` を付与
- 同上 `bdRender()` の直前に `bd.nodes.forEach(n => { if (n._autoStyle) bdApplyAutoStyle(n.id); })` を追加。従来は `_autoStyle: true` が立っていてもレンダー直前に `bdApplyAutoStyle` が呼ばれておらず、ユーザーがチェックを OFF → ON しないと深度別スタイルが適用されなかった問題を修正

## v0.5.357 (2026-04-25)

### feat(publish): HTML 出力を公開機能に一本化 + フォームビュー公開で送信/受信を有効化

**背景**: 「HTML エクスポート」(`MeldexExportHtml.exportToHtml`) と「公開 HTML」(旧 `MeldexPublicRuntime`) が別系統で、前者は現テーマを完全再現・後者は固定ライトテーマかつ `embed_font` 設定が no-op だった。二重経路が混乱を生むため公開機能に統一し、同時にフォーム公開の送信/受信を正式対応した。

- **公開 HTML ランタイムを全面刷新**: [gb-public-runtime.js](gb-public-runtime.js) の旧 payload-rebuild 方式 (固定ライトテーマ) を廃止。`MeldexExportHtml` の共通パイプライン (`cloneAndClean` / `convertDataRuby` / `embedImages` / `embedFont` / `collectCssVars` / `fetchCss` / `buildHtml`) を再利用して、現テーマの `:root` 変数 + 外部 CSS + フォント + ルビ (`<ruby><rt>`) をピクセル単位で再現
- **対応種別を拡張**: `publishCurrentView` が受理する種別を `['database','smart-db','entity']` → `['page','entity','database','csv','smart-db','calendar']` に。単一パネル種別 (page/csv/calendar) もサポート
- **getCurrentPublishContext / getPublishConfigForContext / savePublishConfigForContext を page/csv/calendar 対応**: localStorage ベースで `gb:publish:<path>` にファイル単位で保存
- **公開設定パネル再構成**: 単一パネル種別では table/form チェックボックス非表示、DB のみフォーム送信系フィールド (`form_submit_enabled` / `server_public_url` / `submit_url` / `form_submit_token`) を表示
- **エクスポートメニューから「HTMLとして保存...」を全種別で削除**: [gb-tool-menu.part02.js](gb-tool-menu.part02.js) の page / scriptnote / database / board / calendar / csv / smart-db から撤去。代わりに「公開設定...」+「公開を更新」に統合

### feat(publish): フォームビュー公開 (DOM クローン方式 + 編集不可 + 送信/受信)

DB をフォームビュー表示中に「公開を更新」すると、フォームとして公開され、閲覧者が入力して送信できる。

- **フォームビュー検出分岐**: `_resolveViewDef('database')` で `currentViewMode === 'form'` を判定し、`_buildFormAnswerPanel(dbPath, formCfg, propTypes, publicMode=true)` で公開用フォームを新規構築
- **`cloneAndClean` に `preserveFormControls` オプション追加**: true のとき input/textarea/select/button を span 化・disable せずそのまま残す。[gb-export-html.js](gb-export-html.js)
- **公開フォーム専用 CSS**: `form[data-publish-form]` 子孫セレクタ (特異度 0,2,1) + `!important` で `getStaticCss` の `input,textarea,select { display:none !important; }` を上書き。入力系の `display:revert` / `pointer-events:auto` / `cursor:text|pointer` を有効化
- **ランタイム送信スクリプト**: `form[data-publish-form]` を検出して submit 処理を注入
  - **checkbox**: 未チェックも `'false'` を明示送信 (FormData 既定挙動では未チェックが含まれず undefined になる問題を解消)
  - **image (input type=file)**: FileReader で各ファイルを base64 data URL に変換し、`[{filename, type, data_url}, ...]` 形式 JSON 文字列で送信。バックエンド `_normalize_public_form_value` ([meldex_api_database.py](meldex_api_database.py)) が受理して `_media/blobs/` に保存
  - **radio**: checked のラジオの value のみ送信
  - **その他**: `String(el.value)` をそのまま送信
  - 入力種別判定は `_buildFormInputRow` が付与する `data-form-type` を優先、fallback で `el.type`
- **フォームのデフォルト submit 防御**: `formEl.setAttribute('action','javascript:void(0)')` を追加し、ランタイム未実行環境でもページリロードを抑止
- **`form_id` 伝搬**: `def.runtimeExtras` 経路で `formCfg.id` を runtimeCfg にマージ、バックエンドのフォーム識別に利用

### その他の整理

- `navOpen(entry, opts)` に optional 第2引数を追加し、`{ skipAutoAppLayout: true }` 等を各 `open*` / `select*` 関数へ透過的に渡せるよう変更
- 計画書 [docs/publish-unification-plan-2026-04-25.md](docs/publish-unification-plan-2026-04-25.md) を追加、table/form タブ切替は実装スコープから除外 (単一パネル公開に統一)

### 制限事項 (既知・今回対応外)

- **CORS**: 別オリジン配信時は Meldex サーバ側 CORS 設定依存
- **100ファイル超の画像送信**: バックエンドで 100 件までに切り捨て
- **scriptnote / board の公開**: 単一パネル種別として将来対応予定

## v0.5.356 (2026-04-25)

### feat(panel,dock,appbar): ドックバーを右側配置 + 単独ペイン時は非表示 + アプリバー「…」ボタン調整

- **ドックバーをカラムの右側に配置**: [gb-panelset.js](gb-panelset.js) `renderDock` で body → dockBar の順に `col.appendChild` するよう変更。アクティブインジケータ (`::before`) も [gb-layout.css](gb-layout.css) の `.gb-dock-icon.active` / `.gb-dock-tab-header.active` で `left:0` → `right:0` に移動し、右側ドックバーに整合
- **単独ペインのカラムではドックバーを描画しない**: `groups.length === 1` かつ `group.root.type === 'pane'` かつ非 collapsed の条件で早期 return。ペインのタブバーで十分なため冗長な UI を排除
- **ネスト panelset のアイコン重複バグ修正**: `_collectPanesInGroup` がネストした panelset 配下に潜り込んでいたため、外側カラムのドックバーに内側カラムのアイコンが重複表示されていた。ネスト panelset はそれ自身が `renderDock` を持つため walk 停止
- **ドックポップアップの位置をアンカー左側に変更**: ドックバーが右側に移ったことでポップアップがはみ出すため [gb-dock-popup.js](gb-dock-popup.js) `_positionPopup` でアンカーの左側に開くよう変更。左側に入らない場合のみ右側へフォールバック
- **アプリバー レイアウトボタンの「…」ボタン調整**: [gb-app-layout.js](gb-app-layout.js) `_ensureLayoutMenuButton` で lucide アイコンを `\u22EF` テキストに置換し確実なサイズ制御を実現。[gb-layout.css](gb-layout.css) `.app-layout-more` を 20×16px + `padding: 0 4px` + `letter-spacing: 1px` に調整し、ドットを上下中央配置 + 左右余白確保

## v0.5.355 (2026-04-24)

### fix(docking): ペインD&Dで top/bottom/left/right ドロップが無反応になっていた不具合を修正

- **根本原因**: [gb-docking.js](gb-docking.js) の `handleDrop` 内、ペイン単独移動（`data.paneId && !data.tabId`）のハンドラが **zone を一切参照せず常に `applyColumnDrop`（合流）を呼んでいた**。`applyColumnDrop` 内のガード「同一カラムへのドロップは無効」が発火するとサイレントに `return` するため、「インジケーターは表示されるのにドロップしても何も起きない」状態になっていた
- **修正**: zone に応じてディスパッチする実装に変更
  - `center` → 従来通り `applyColumnDrop` で対象カラムへマージ（panelset 化）
  - `top` / `bottom` / `left` / `right` → ソースペインのタブを新ペインに移動 → `splitPane` で対象ペインの該当位置に分割挿入 → 元ペインを `removePane` で片付け（タブ移動と同じフロー）
  - `splitPane` 失敗時（MAX_DEPTH 到達）はロールバック: `sourcePane.tabs.push(...movingTabs)` で元ペインにタブを戻す
- **徹底チェックで検出した副次不具合**: ソースペインのロックチェックが新パスに欠落していた問題を合わせて修正（タブ移動と同じ `_isLockedPane(data.paneId)` ガードを追加し、「ロック中のパネルからは移動できません」で拒否）

## v0.5.354 (2026-04-24)

### feat(board): mindmap を放射状レイアウトに変更 + 自動整列発火タイミング改善

- **mindmap 放射状レイアウト**: [gb-canvas-features.part01.js](gb-canvas-features.part01.js) の `bdLayoutMindmap` をルート中心に子を時計回りで四方八方に配置する放射状アルゴリズムに置換。子1個=3時、子2個=3時・9時、子3個以上=360/N°間隔。各子サブツリーはブランチ方向に応じて水平 or 垂直ツリーで展開。ルートエッジ距離は `min(W/2/|cosθ|, H/2/|sinθ|)` で厳密計算
- **`_bdLayoutHorizontalTree` / `_bdLayoutVerticalTree` を共通ヘルパーとして抽出**: 旧 `bdLayoutMindmap`（水平ツリー）は `bdLayoutLogic` に移設、`bdLayoutFlowchart` と合わせて共通ヘルパー経由で実装
- **`bdTreeDirection` の mindmap 判定を動的化**: 'right' 固定を解除し、ノード位置（子の平均位置 or 親→自分ベクトル）から動的判定。放射状の各ブランチで適切な展開方向が返る
- **`bdAddChildToSelected` / `bdAddSiblingToSelected` の dir 分岐**: [gb-board-ui.part01.js](gb-board-ui.part01.js) の mindmap 固定 'right' 分岐を削除し、`bdTreeDirection` 動的判定に移譲
- **差分レイアウト後も全体整列をリクエスト**: 兄弟追加時、差分レイアウト (`bdApplySiblingDifferentialLayout`) 適用時も `bdRequestAutoLayout` を呼ぶよう変更。カード高さ差による整列ズレが常に解消される
- **bdFinishEdit に整列トリガー追加**: [gb-canvas-engine.part02.js](gb-canvas-engine.part02.js) で、テキスト編集終了時に `editedNode` の構造ツリーに `bdRequestAutoLayout` をリクエスト。行数増加で高さが変わったカードの周辺が自動再配置される（`bd.autoAlign !== false` かつ `root.structure` がある場合）

### feat(board): ボードツールバー整理 + テーマタブにボード固有設定を統合

- **背景関連の3ボタンを削除**: [gb-board-presets.js](gb-board-presets.js) の下部ツールバーから「背景色スウォッチ (`bd-bg-swatch`)」「背景画像 (`bg-image`)」「背景をクリア (`bg-clear`)」ボタンを削除。すべてテーマまたは詳細パネル（旧スタイル、改名で「テーマ」）から設定する仕様に統一
- **テーマタブのボードパネルに「ボード固有設定」セクションを追加**: [gb-settings.part04.js](gb-settings.part04.js) の `_renderSettingsThemeBoardExtras()` で影トグル / 自動整列 / 背景画像（選択・クリア）/ 画像表示 / 画像スケール の UI を新設。`settingsThemeBoard*` ハンドラ群（`settingsThemeBoardToggleShadow` / `settingsThemeBoardToggleAutoAlign` / `settingsThemeBoardSetBgFit` / `settingsThemeBoardSetBgScale` / `settingsThemeBoardChooseBgImage` / `settingsThemeBoardClearBgImage`）で `:root` に CSS 変数を書き込み、テーマ保存時に `saveColorSettings` 経由で永続化
- **UI_STYLE_SECTIONS['ボード'] に gap 数値行追加**: [gb-settings.part02.js](gb-settings.part02.js) に `カード隙間` 行（同階層 `--bd-gap-siblings`, 階層間 `--bd-gap-levels`）を numbers 形式で追加。`setNumericStyleSetting` に `--bd-gap-*` 変更時の `_bdRelayoutAllStructureTrees()` 即時再レイアウトフックを追加
- **getAllStyleKeys() 拡張**: `--bd-shadow`, `--bd-bg-image`, `--bd-bg-image-fit`, `--bd-bg-image-scale`, `--bd-auto-align` をテーマ保存対象に追加
- **ランタイムフォールバック**: ファイル固有値 (`bd._fileStyle`) が未設定のキーについては、テーマの `:root` 値をフォールバック参照する仕組みを追加
  - [gb-board-background.js](gb-board-background.js) `bdApplyCanvasBackground`: 背景画像 / fit / scale のテーマ値フォールバック
  - [gb-canvas-features.part01.js](gb-canvas-features.part01.js) `bdLayoutGaps`: gap 値のテーマフォールバック
  - [gb-canvas-engine.part03.js](gb-canvas-engine.part03.js) ボード読込時の `bd._showShadow` / `bd.autoAlign` テーマフォールバック
  - [gb-theme-manager.js](gb-theme-manager.js) `applyBoardThemeRuntime` でテーマ切替時に `bd._showShadow` / `bd.autoAlign` を再同期
- **詳細パネルの「スタイル」タブを「テーマ」にリネーム**: [gb-detail-panel.part01.js](gb-detail-panel.part01.js) の `detail-tab-file-style` 表示名を「スタイル」→「テーマ」に変更（全ファイル種別共通）
- **テーマ UI の抜け補完（横断チェック）**:
  - ノート: `--page-quote-border`（引用線色）を UI_STYLE_SECTIONS['ノート'] に追加
  - シナリオ: `--sn2-ruby-size` / `--sn2-ruby-offset`（ルビサイズ/オフセット）を UI_STYLE_SECTIONS['シナリオ'] に numbers で追加

### 徹底チェックで検出・修正した副次問題

- 影トグルの条件判定で `!bd._fileStyle['--bd-shadow']` が空文字（影オフ）でも true になり、ファイル明示オフをテーマが上書きする問題 → `=== undefined` 判定に修正
- 影オフ値 `''` が `saveColorSettings` の `if (v)` で保存されずリロード時に復活する問題 → off 値を `'0'` に変更
- 背景画像ファイル名表示で URL デコードなし（`/file-raw?path=...` 形式の末尾が encoded のまま）→ `?path=` クエリをデコード
- `_settingsThemeRefreshBoardExtras` が `:last-of-type` 依存で将来の拡張で誤マッチ → `data-settings-theme-board-extras="1"` マーカー属性で識別

### テスト更新

- [tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py): ボード下ツールバー背景ボタン削除（`assertIn` → `assertNotIn`）と詳細パネルのタブ名「スタイル」→「テーマ」に合わせて期待値更新

## v0.5.353 (2026-04-24)

### fix(board): 徹底チェックで検出した labelBorderWidth 周りの 3 件の不具合を修正

- **マークダウン保存/読込に labelBorderWidth が含まれていない**: [gb-canvas-engine.part01.js](gb-canvas-engine.part01.js) の per-connection 書き出し/読み出しが `labelBorderWidth` を扱っておらず、per-conn 上書きが保存→再読込で消えていた。parse 側に `/labelBorderWidth:\s*([\d.]+)/` を追加、serialize 側に `, labelBorderWidth: N` を追加 (デフォルト 1 と同じ場合は省略)
- **階層別ラインスタイル適用 `_bdApplyDepthLineFieldsToConn` が labelBorderWidth をコピーしていない**: [gb-canvas-features.part01.js](gb-canvas-features.part01.js) に `if (Number.isFinite(+L.labelBorderWidth)) conn.labelBorderWidth = +L.labelBorderWidth;` を追加
- **labelBorderColor 未指定時に borderStyle:none で CSS ホバー/編集中の枠線表示が失われる**: [gb-canvas-engine.part02.js](gb-canvas-engine.part02.js) で JS 側は `labelBorderColor` が明示指定されたときだけ borderColor / borderWidth / borderStyle を上書きするよう修正。未指定時は CSS 既定の `border: 1px solid transparent` を維持し、ホバーで `border-color: var(--border)`、編集中は `border-color: var(--accent)` が効くように

### 徹底チェック結果 (v0.5.352 経路確認)

- `bdNormalizeLineStyles` / `bdGetConnectionStyle` / `_bdNormalizeDepthLine` / `_BD_LINE_OVERRIDE_KEYS` / `bdClearConnectionStyleOverrides` / `_BD_DEPTH_LINE_FIELDS` / `_bdApplyStyleFieldChange` すべてに `labelBorderWidth` が登録されていることを確認
- `_bdLineStylePreviewHtml` がテキスト描画前に背景 rect → 枠線 rect を重ねていることを確認
- HTML ラベル描画で `shadowW > 0` ガードのみで 4 方向シャドウが適用されることを確認
- UI 配置 (テキストチェックボックス → テキスト書式 → 縁取 → 沿線回転 / 自動反転) を確認

## v0.5.352 (2026-04-24)

### fix(board): ラインテキスト書式の 4 件の不具合を修正

- **テキスト枠線太さ設定欄を追加**: [gb-board-style-manager.js](gb-board-style-manager.js) に `labelBorderWidth` フィールド (0-10px) を新設。テキスト枠線色の隣に「枠線 N px」として配置。`bdNormalizeLineStyles` / `bdGetConnectionStyle` / `_bdNormalizeDepthLine` / `_bdCardStyleSnapshotFromNode` 相当の line 側経路・overrideKeys・clearOverrides すべてに追加
- **プレビューに背景色/枠線色/枠線太さが反映されない不具合**: [gb-board-presets.js](gb-board-presets.js) の `_bdLineStylePreviewHtml` が textPath でないプレビュー (HTML ラベル相当) で `labelBgColor` / `labelBorderColor` / `labelBorderWidth` を一切描画していなかった。SVG rect 2 枚 (背景 + 枠線) を追加してテキスト下に重ねることでスタイル管理ダイアログのプレビューに反映
- **縁取り設定が沿線回転 ON のときだけ効く不具合**: [gb-canvas-engine.part02.js](gb-canvas-engine.part02.js) の HTML ラベル描画側で `shadowW > 0 && !labelTextColor && !labelBgColor` というガードがあり、ユーザーが文字色 / 背景色を設定するとシャドウが消える仕様だった。ユーザーは両者を独立に制御したいため **ガードを撤廃**し `shadowW > 0` の単独条件で 4 方向シャドウを適用
- **UI 配置変更**: 「縁取 + テキスト縁取り色」を lrow4 (沿線回転行) から lrow3 (テキスト書式行) の斜体の直後に移動。「テキスト」オン/オフ チェックボックスは専用の lrowTextToggle にしてテキスト書式行の直前に配置 (ユーザー要望通り: チェックボックスはテキスト設定関連の直前、縁取は斜体の直後)
- labelBorderWidth を live な HTML ラベル描画にも適用 ([gb-canvas-engine.part02.js](gb-canvas-engine.part02.js)) — `labelDiv.style.borderWidth` / `borderStyle` を明示設定、0 のとき `'none'` で枠線非表示

## v0.5.351 (2026-04-24)

### fix(board): 徹底チェックで検出した 2 件の不具合を修正

- **右クリック書式ポップアップが即時閉じる不具合**: [gb-canvas-engine.part02.js](gb-canvas-engine.part02.js) で右クリック時に編集モード化 → ポップアップ表示しても、ユーザーがポップアップ内をクリックした瞬間に `meldex-core.part02.js` の pointerdown 汎用ハンドラがラベルを blur させ、`labelDiv.onblur = finish` で即座に編集終了してポップアップが機能不全になっていた。`finish` 内で `.gb-text-selection-fmt / .gb-fmt-popup / .gb-palette-popup` が開いているかを確認し、開いていれば編集を確定せず、再度 once-only の blur リスナーを登録して「ポップアップ閉じ後の blur」でだけ確定するよう修正
- **グローバルデフォルト再適用が走らない不具合**: v0.5.349 の `_bdApplyGlobalDefaultsToStyleArray` 末尾で `style._default = _bdCloneStyleForDefault(style)` を書き込んでいたため、**初回適用後にその値が per-board の _default として保存**され、次回同じボードを開いたときに「_default が既にある → global 適用をスキップ」と判定。他ボードで global を変更しても永遠に伝播しなくなっていた。該当行を削除し、ボードを開くたびに最新 global が再適用される構造に修正
- **しきい値 `shadowW > 1` を `shadowW > 0` に戻す**: v0.5.350 で条件を `> 1` にしていたが、CSS 側の常時 8 方向シャドウを削除した今、JS 側の 4 方向シャドウは既定 (1px) でも重ならず正しく描画される。ユーザーが明示的に textShadowWidth=1 に設定した既存データを尊重するために既存条件へ戻した

### 徹底チェック結果 (v0.5.349 スタイル保存/復元)

- 保存経路 6 箇所すべてが `_bdSaveGlobalStyleDefault` を呼ぶことを確認 (`_bdSaveCurrentBoardStyle` / `_bdSaveCurrentNodeCardStyle` / `_bdSaveCurrentConnectionLineStyle` / スタイル管理モーダルの新規作成 / スタイル管理モーダルの 💾 / depth styles の保存ボタン)
- 削除時の `_bdRemoveGlobalStyleDefault` 連携を確認
- 新規ボード読込時に `bdEnsureBoardUiState` → `_bdApplyGlobalDefaultsToStyleArray` が確実に走ることを確認
- `_bdResetStyleToDefault` の優先順位 (style._default → global → BD_DEFAULT → 色系) を検証
- depthStyles は空配列時にのみ global を採用し、既存ボードの depth は上書きしない設計を確認
- `bd._globalStyleDefaultsApplied` / `bd._globalDepthStylesApplied` がボード読込時にリセットされることを確認

## v0.5.350 (2026-04-24)

### fix(board): ライン ラベルの多重白フチ表示 / 右クリック書式ポップアップ非表示を修正

- **根本原因 (多重白フチ)**: [gb-tools.part03.css](gb-tools.part03.css) の `.bd-conn-label` に CSS 側で 8 方向 × 1px の text-shadow が常時付与されており、JS 側 ([gb-canvas-engine.part02.js](gb-canvas-engine.part02.js)) が `textShadowWidth === 0` のときに上書きしないため、CSS 既定の 8 重シャドウがそのまま残る。ラベルをラインの中央に配置したときに、半透明シャドウが積み重なって「ズレた白フチが幾重にも重なった」見た目になっていた
- **修正**: CSS の text-shadow 既定を削除。ラベル背景の不透明度を 88% → 92% に上げてライン本体を確実に隠す。JS は `textShadowWidth > 1` のときだけ明示的に 4 方向シャドウを付与、それ以外は常に `text-shadow: none` を適用
- **根本原因 (右クリックポップアップ)**: `labelDiv` に `oncontextmenu` ハンドラが存在せず、ブラウザ既定の右クリックメニューは `meldex-core.part02.js` で抑止されるだけで、書式設定 UI に到達する経路がなかった
- **修正**: `labelDiv.oncontextmenu` を追加し、右クリックで `_enterLabelEditMode()` を実行 (contenteditable 化 + 全文選択) したうえで `window.GBTextSelectionFormat.openForSelection()` を呼び、`gb-text-selection-format.js` の選択書式ポップアップを表示する

## v0.5.349 (2026-04-24)

### feat(board): カード/ライン/階層別スタイルのデフォルトを全ボード共通で保存・復元可能に

- **根本原因 (既知不具合)**: カードスタイル/ラインスタイルを「デフォルトとして保存」しても、保存先が `bd.cardStyles[i]._default` のみで **per-board データ内に閉じていた**。別のボードファイルを開くと自動生成される `BD_DEFAULT_*_STYLES` ビルトイン値が使われ、他ボードで保存したはずの default が反映されなかった
- **グローバルデフォルト保存層を新設** ([gb-board-style-manager.js](gb-board-style-manager.js)): localStorage に `meldex-bd-global-card-style-defaults` / `meldex-bd-global-line-style-defaults` / `meldex-bd-global-depth-styles` を新設し、「デフォルトとして保存」の全経路から全ボード共通レイヤーへ書き込む
  - `_bdSaveCurrentBoardStyle` (スタイル管理モーダルの保存ボタン)
  - `_bdSaveCurrentNodeCardStyle` (ノード個別の save-node-card-style)
  - `_bdSaveCurrentConnectionLineStyle` (接続個別の save-conn-line-style)
  - スタイル管理モーダルの「+」で新規スタイル作成時
  - スタイル管理モーダルの 💾 (data-bd-style-save) ボタン
  - スタイル削除時は `_bdRemoveGlobalStyleDefault` で自動掃除
- **新規ボード読込時の自動マージ** ([gb-board-ui.part01.js](gb-board-ui.part01.js)): `bdApplyStylePresetMigration` と `bdEnsureBoardUiState` 両方で `_bdApplyGlobalDefaultsToStyleArray` を実行し、ビルトイン id に該当するスタイルへ global default を上書き適用。per-board の `_default` が既に存在する場合は「このボード専用のカスタム」と見なしてスキップ
- **`_bdResetStyleToDefault` の優先順位を拡張**: style._default → **global default** → `BD_DEFAULT_*_STYLES` → 色系のみリセット、の 4 段フォールバック
- **階層別スタイル (depthStyles) も同様に対応** ([gb-canvas-features.part01.js](gb-canvas-features.part01.js)): `bdEnsureDepthStyles` が空配列のとき、localStorage の `meldex-bd-global-depth-styles` があれば採用してから `bdNormalizeDepthStyles` を通す
- **階層別スタイル管理モーダルに「デフォルトとして保存」/「デフォルトに戻す」ボタン追加**: 現在の `bd.depthStyles` 一式を global に保存、または global (未保存ならビルトイン) へ戻す
- **オプションパネル「ボード」詳細にも階層別スタイルセクション追加** ([gb-board-ui.part02.js](gb-board-ui.part02.js)): カード/ラインと同じレイアウトでドロップダウン (階層 1, 階層 2, …) + 💾 保存 + ↺ 戻す ボタン。ドロップダウン選択で該当階層を管理ダイアログで開く
- **ボード切替時にフラグをリセット** ([gb-canvas-engine.part03.js](gb-canvas-engine.part03.js)): `bd._globalStyleDefaultsApplied` / `bd._globalDepthStylesApplied` を新ボード読込時にクリアし、再マージが確実に走るように

## v0.5.348 (2026-04-24)

### fix(theme): テーマUI自動適用設定が保存/復元時にランタイムへ反映されていなかった不具合を修正

- **根本原因**: [gb-theme-manager.js](gb-theme-manager.js) の `applyThemeUiSettingsFromTheme` が localStorage (`THEME_UI_APPLICATIONS_KEY` / `THEME_UI_AUTO_TONE_KEY`) にのみ書き込み、CSS ルールを再生成する `applyThemeUiApplications()` を呼び出していなかった。そのため **「デフォルトに戻す」やテーマ切替時に、各パネルの「テーマカラーの自動適用設定」(例: ボード タイトル / シナリオ セリフ …) の保存状態が runtime に反映されず、古いルールのまま**だった
- **修正**: 書き込みが発生したら `applyThemeUiApplications(applications, { forceTargets: true })` を呼んで `<style id="meldex-theme-ui-applications-style">` の CSS ルールを再生成する
- **保存/復元される全設定の一覧 (徹底チェック結果)**:
  - テーマセレクタ選択 (defaultThemeId) ✓
  - 共通フォント `--ui-font` → `cssVars` に含まれる ✓
  - ノート左右余白 `--page-margin-x` → `cssVars` に含まれる ✓
  - 各パネルの書式設定 (UI_STYLE_SECTIONS の全 CSS 変数) → `cssVars` に含まれる ✓
  - テーマカラー 8 色 (`themeColorSet`) ✓
  - 行 3 個別スロット上書き (`themeColorSlotSettings`) ✓
  - 行 1/2/4 拡張スロット上書き (`themeColorExtraSlotSettings`) ✓
  - パレット調整 (`standardPaletteAdjust`: 色相 始/終, 彩度, 明度, 明暗比) ✓
  - OS アクセントトグル (`useOsAccentColor`) ✓
  - 各タブのテーマカラー自動適用設定 (`themeUiApplications`) ✓ (本修正で runtime 反映も解決)
  - 明暗トーン (`themeUiAutoTone`) ✓ (本修正で runtime 反映も解決)

## v0.5.347 (2026-04-24)

### fix(link-modal): v0.5.346 の徹底チェックで検出した不具合を修正

- **フォルダ/DB判定のセレクタ誤り**: `_isFolderRow` が `.tree-node-children` / `.tree-toggle.has-children` を参照していたが、実際のクラス名は `.tree-children` で `has-children` フラグも存在しない。`createTreeNodeFromBrowse` が `div._nodeData = item` を設定していることを利用し、`_nodeData.type === 'folder' / 'database'` の直接判定に変更
- **ホバーボタン等の誤捕捉**: `.tree-hover-btn` (メニュー / 追加) や `.sidebar-section-header` (セクション折りたたみ) / `.sidebar-section-btn` (ソースフォルダ管理・追加) / `#sidebar-search-input` (検索入力欄) などでも `.tree-node-row` や `.fav-item` の closest() がヒットしてしまい、リンク挿入に回って機能が壊れる可能性があった。これらを明示的に除外
- **type を推定ではなく `_nodeData` から直接取得**: 推定対象を fav-item 等 `_nodeData` を持たないパスに限定し、tree-node では元の item.type をそのまま使用。`.md` のボード/ページ/台本など推定しきれない種別も正確に

## v0.5.346 (2026-04-24)

### refactor(link-modal): リンク挿入ダイアログを既存フォルダツリー(#sidebar)の再利用に変更・縦長レイアウトに

- **方針変更**: 検索/ツリーのタブ切替 + 独自ミニツリーを廃止し、メインウィンドウの `#sidebar`（フォルダツリー）を一時的にモーダル内へ移動して表示。既存の検索/フィルタ/お気に入り/最近/ソースフォルダ/Home 各セクションがそのまま使える
- **クリック捕捉**: サイドバー `click` をキャプチャ位相で捕捉し、`.tree-node-row` / `.fav-item` / `.sidebar-item` 行のうち非フォルダ行をクリックしたら `e.stopImmediatePropagation()` で通常のファイル open 処理を阻止して `insertFileLink({ name, path, type })` へ流す。フォルダ行・展開トグルは通常動作 (展開/折りたたみ) を維持
- **パスからの type 推定**: モーダル内では DOM に type が保持されていないため、拡張子から `scriptnote / smart-db / dashboard / board / image / video / audio / pdf / html / csv / page` を推定するヘルパーを追加
- **サイドバーの退避/復元**: `_sidebarOrigParent` / `_sidebarOrigNextSibling` / `_sidebarOrigDisplay` に元位置情報を保存、close() で確実に元の場所へ戻しリスナーを解除
- **レイアウト**: ダイアログを幅 `min(400px, 90vw)` / 高さ `min(85vh, 820px)` の縦長に変更。パディング 6vh 上から表示

## v0.5.345 (2026-04-24)

### feat(board): カード選択中に印字可能文字を押すと即座にテキスト編集開始 (そのキーで既存テキスト置換)

- **挙動**: ボードで単一カード選択中に `a` / `s` / `あ` など印字可能な1文字キーを押すと、Enter / F2 を押さずに自動的に該当カードの編集モードに入り、押された文字で既存テキストを置換する
- **実装**: [gb-canvas-interact.js](gb-canvas-interact.js) の `onCanvasKeydown` 非編集分岐末尾に quick-edit ロジックを追加。`bd.selected.size === 1` かつ修飾キーなし (Ctrl/Meta/Alt)・IME 変換中でない (`isComposing` / keyCode 229 除外)・`e.key.length === 1` のときに `bdEditNode(id)` を呼び、setTimeout で contenteditable の内容を `typed` 1 文字に差し替え、カーソルを末尾へ配置
- **既存ショートカットとの互換**: スコープ `board` の既存ショートカット (Enter / F2 / Tab / Ctrl+* / 矢印 / Delete 等) はすべて修飾キー付き or 特殊キーのため衝突しない

## v0.5.344 (2026-04-24)

### fix(board): 新規ボードでカード追加時に隙間が 0 になる不具合を修正

- **根本原因**: [gb-canvas-features.part01.js](gb-canvas-features.part01.js) の `bdLayoutGaps()` が `Number.isFinite(+bd.gapSiblings) && bd.gapSiblings >= 0` で未設定判定していたが、`bd.gapSiblings = null` のとき `+null === 0` で `Number.isFinite` を通過し、かつ `null >= 0 === true` が成立するため、**新規ボード(未カスタマイズ)で gap=0 として採用**されてしまっていた
- **症状**: 新規ボード作成直後、オプションパネルでは 10 / 30 と初期値表示されるが、実際にカードを追加するとレイアウト隙間が 0 になる
- **修正**: `null` / `undefined` を明示的に除外してからファイニト判定。未設定時は `BD_LAYOUT_GAP_SIBLINGS_DEFAULT` (10) / `BD_LAYOUT_GAP_LEVELS_DEFAULT` (30) へ正しくフォールバックする

## v0.5.343 (2026-04-24)

### fix(tooltip,ui): パネルタブ(.gb-tab)のラベル複製ツールチップと GBUI.createTab の title を削除

- **根本原因1**: レイアウトが生成する実体タブ DOM (`div.gb-tab` [gb-layout.part01.js](gb-layout.part01.js)) は `draggable=true` のため、[gb-tooltip.js](gb-tooltip.js) の `[draggable="true"]` セレクタにマッチしていた。解説ヒントが取れない場合は `visibleTextHint` が div 要素に対してラベル文字列そのものを返すため、「タブラベルのまま」のツールチップが出ていた
- **根本原因2**: [gb-ui.js](gb-ui.js) の `createTab` が `title: opts.title || opts.label` で無条件にタブ要素へ `title` を付与していた
- **修正1**: `TAB_EXCLUDE_SELECTORS` に `.gb-tab` / `.gb-tab-icon` / `.gb-tab-label` / `[data-tab-id]` を追加、タブ要素およびその内部 span をツールチップ対象から完全除外
- **修正2**: `createTab` の `title` 属性付与を削除
- **影響範囲**: 通常のパネルタブ（オプション / チャット / カレンダー / ヒストリー / 注釈 / 各ツールタブ）でラベルの重複ツールチップが一切出なくなる

## v0.5.342 (2026-04-24)

### fix(tooltip): パネルタブ/パネル内タブのツールチップ解説が残っていた不具合を修正

- **根本原因**: [gb-tooltip.js](gb-tooltip.js) の委譲型ツールチップレイヤーが `.gb-panel-tab` / `.gb-inner-tab` / `.rp-tab` / `.chat-mode-tab` / `[role="tab"]` を INTERACTIVE_SELECTORS に含めていたため、`visibleTextHint` が「<タブ名>を表示します」のようなヒューリスティック解説を自動生成していた。v0.5.341 で DOM の `title` 属性は削除したが、このレイヤー由来の解説が残っていた
- **修正**: タブ系セレクタを INTERACTIVE_SELECTORS から分離して `TAB_EXCLUDE_SELECTORS` を新設、`isTooltipEligible()` でタブ要素を除外。`data-gb-tooltip` などの明示的属性も含めタブにはツールチップを出さない
- **影響範囲**: オプションパネル / 右パネル / チャットモード切替 / パネルセットなど、タブとして描画される要素全般で解説が非表示に

## v0.5.341 (2026-04-24)

### feat(theme,board,link): 設定テーマタブの整理・全32マス個別色設定・ボード隙間UI共通化・リンク挿入ツリー選択

- **設定ダイアログ テーマタブの重複項目を共通に統合**: [gb-settings.part04.js](gb-settings.part04.js) の `_renderSettingsThemeStyleRows` に `_filterCommonDuplicates` を追加。非共通タブで「カーソル／ホバー／選択／テキスト選択」ラベルを除外し、共通タブのみに集約
- **テーマカラー全32マス個別色設定対応**: [gb-settings.part02.js](gb-settings.part02.js) に拡張スロット (`meldex-theme-color-extra-slot-settings`) を新設。行1 (グレー 7 個)・行2 (自動明)・行4 (自動暗) の各スウォッチを個別にクリックしてカラーパレットで上書き可能に。`openSettingsThemeExtraSlotPopup` を追加、既存の行3スロットと独立管理 (`{"1-1":"#rrggbb",...}` 形式)
- **上書きの全アプリ伝播**: [gb-color-palette.js](gb-color-palette.js) の `getStandardPaletteSwatches` を拡張し、拡張スロット上書きがボード/カレンダー/ノート等のカラーピッカー全てに反映されるように
- **テーマカラー設定の保存/復元**: [gb-theme-manager.js](gb-theme-manager.js) に `normalizeThemeColorExtraSlotSettings` / `setThemeColorExtraSlotSettingsOnTheme` / `applyThemeColorExtraSlotSettingsFromTheme` を追加。「デフォルトとして保存」で拡張スロットも保存、再読込で復元。`createCustomThemeFromCurrent` / `updateCustomThemeFromCurrent` / `normalizeCustomThemePayload` の全系統に組み込み。`settingsThemeReset` でもクリア対応
- **パネルタブのツールチップ削除**: [gb-detail-tab-dnd.js](gb-detail-tab-dnd.js) の `_bindTab` で付与していた `title = 'ドラッグでタブを並べ替え'` を削除。タブ要素から解説ツールチップが出なくなった
- **ボード オプション 隙間入力のUI共通ルール準拠**: [gb-detail-panel.part01.js](gb-detail-panel.part01.js) の「同階層カード間の隙間」「階層間の隙間」を `type: 'pxtext'` から `type: 'number'` + `unit:'px'` + `min/max/step/fallback` に変更。共通 `.gb-fmt-num` スタイルで表示
- **ボード隙間の初期値を 10/30 に変更**: [gb-canvas-features.part01.js](gb-canvas-features.part01.js) の `BD_LAYOUT_GAP_SIBLINGS_DEFAULT` を 40→10、`BD_LAYOUT_GAP_LEVELS_DEFAULT` を 60→30 に。未カスタマイズの既存ボードは自動整列が有効なら再レイアウトで詰まる（ユーザー要望通り）
- **リンク挿入ダイアログにフォルダツリー選択を追加**: [gb-link-modal.js](gb-link-modal.js) に「検索／フォルダツリー」タブ切替を実装。`_renderLinkTreeChildren` で遅延ロード (`/browse` API) によるツリー展開、フォルダクリックで子を動的ロード、ファイルクリックで既存 `insertFileLink` に合流
- **徹底チェック**: 関数チェーン8系統を追跡、行1のスロットインデックス計算ロジックを `info.index` 参照に一本化して二重ロジックを解消。全8ファイル Node 構文チェック通過

## v0.5.340 (2026-04-24)

### fix(relocate): v0.5.339 の徹底チェックで検出した不整合を修正

- **カレンダーイベント追従のテーブル名不一致を修正**: [meldex_path_relocator_service.py](meldex_path_relocator_service.py) の `_rewrite_calendar_events` が存在しないテーブル `events` を参照していて黙って 0 件 return していた。Meldex の SQLite スキーマではカレンダーは `cal_events` ([Meldex.part01.py](Meldex.part01.py) で定義) であり、テーブル存在チェックと UPDATE / SELECT のすべてを `cal_events` に修正
- **クライアント側 reload 判定の state 参照誤りを修正**: [gb-path-relocate-client.js](gb-path-relocate-client.js) の `_reloadIfOpen` が存在しない `state.currentPath` を参照していたのを、実際の [gb-app.part03.js](gb-app.part03.js) が管理する `state.currentPagePath` に修正。またシナリオは存在しない `state.currentScriptnotePath` を参照していたのを `getActiveScriptNoteComponent()?.state?.scenarioPath` 経由の参照に変更
- **`_rewrite_md` のデッドコードを削除**: frontmatter 再結合で 1 回目の `result` 代入が直後に上書きされていた未使用行を除去

## v0.5.339 (2026-04-24)

### feat(relocate): ファイル/フォルダ移動・リネーム時に全参照を自動追従

計画書: [docs/path-relocation-on-move-plan-2026-04-24.md](docs/path-relocation-on-move-plan-2026-04-24.md)

- **新規サービス** [meldex_path_relocator_service.py](meldex_path_relocator_service.py) を追加。ファイル/フォルダの移動・リネーム時に、参照している側のファイル本文中のパス文字列を自動追従させる。従来は `file_index` / `annotations` / `folder_links` / バージョン履歴 / チャット targetPath のみが更新され、**ボード .md の `# [img]URL` ヘッダ行やノート本文の `<img>` / Markdown リンク / scriptnote.json 内の参照等は書き換えられず、リンク切れになっていた**
- **対応する参照形式**: ボード .md の `# [img]URL` 行 / frontmatter `links:` ブロック / file-style の `--bd-bg-image`、ノート/エンティティ .md / `_freetext.md` 本文の HTML `<img src>` / `<span class="auto-link" data-path>` / `<div class="embed-media" data-path>` / Markdown `[](...)` / `![alt](...)` / url 型プロパティ value、`*.scriptnote.json` / `*.smart.json` / `*.dashboard.json` の再帰 JSON 文字列走査、カレンダー SQLite `events.description` 列
- **文脈ベース置換**: URL の `?path=` value、HTML 属性 (src/href/data-path)、Markdown のリンク URL、YAML 特定 key のみを対象に置換。裸テキストの文字列一括置換はしないため誤置換リスクなし
- **フォルダ移動対応**: `is_folder=True` 時は `old/子孫` プレフィックス一括置換。例 `Folder` → `Other` で `Folder/sub/pic.jpg` → `Other/sub/pic.jpg`
- **候補 source 収集**: backlinks DB から `target_path == old` / `target_path LIKE old/%` を引きつつ、vault 走査 (事前 byte プローブで高速化) によるフォールバックを併用。backlinks 未登録種別 (scriptnote / smart 等) も取りこぼさない
- **アトミック書き込み**: `.tmp-relocate` 一時ファイルに書いた後 `replace` する。途中失敗でもソースファイルは無傷
- **API 統合**: [/api/outliner/move](meldex_api_outliner.py), [/api/outliner/rename](meldex_api_outliner.py), [/api/entity/rename](meldex_api_database.py) のレスポンスに `relocate: { rewritten_count, failed_count, rewritten_paths, truncated }` を追加
- **Phase 5 一括リペア** [/api/repair-broken-refs](meldex_api_outliner.py) を新設。vault 内の全 .md / .scriptnote.json / .smart.json / .dashboard.json を走査し、実在しないパス参照を検出。同名ファイルが vault 内に 1 つだけ存在すれば自動修復候補として提示 (dry_run=True 既定、false で自動書き換え実行)。過去に壊れた参照の事後修復に利用
- **フロント連動**: [gb-path-relocate-client.js](gb-path-relocate-client.js) を新設し `handleRelocateResponse(res)` を公開。Meldex.html で新規読み込み。主要 move/rename 呼び出し (outliner D&D / folder rename / outliner-search D&D / board rename / page title rename) で status 表示と、書き換えたファイルが現在開いているボード/ノート/シナリオなら自動 reload を実行
- **単体/統合テスト**: [tests/test_meldex_path_relocator_service.py](tests/test_meldex_path_relocator_service.py) を新設、31 ケース: ファイル/フォルダ移動両パターン、URL エンコード揺れ、HTML 属性 / Markdown リンク / board `[img]` / frontmatter links / file-style `--bd-bg-image` / scriptnote JSON / フォルダ prefix 置換 / `_trash/` 等除外 / 一括リペアの単一候補自動修復・複数候補検出・発見不可、すべて OK
- **ルーター登録**: [tests/test_meldex_router_modules.py](tests/test_meldex_router_modules.py) に `/api/repair-broken-refs` を追加、回帰 28 ケースすべて OK

## v0.5.338 (2026-04-24)

### fix(board): フォルダツリーからのドラッグ&ドロップでリンクカード/画像カードが作成されない不具合を修正

- **根本原因**: [gb-tool-canvas.js](gb-tool-canvas.js) の `CanvasComponent.activate()` で `!isPaneActive` 時に `_interactionCleanup()` を実行し dragover/drop を含む全 listener を剥がしていた。pane-bridge の `_onActivePaneChange → _mountAllPanes → _mountPaneContent` 経由で activate() が呼ばれるタイミングで、`GBLayout.activePane` がまだ更新前の瞬間に `_isOwnPaneActive()` が `false` と誤判定されることがあり、**本来アクティブなボードペインの listener が剥がされたまま再 init されない状態**になっていた。結果、フォルダツリーから画像をボードへ D&D しても drop ハンドラが一切発火せず、リンクカード/画像カードが作成できなかった
- **修正**: `activate()` の `!isPaneActive` 分岐で cleanup を実行する処理を廃止。cleanup は従来通り `deactivate()` / `destroy()` 側で必ず走るため、ここでは何もせず return する (コメントで経緯を記録)
- **画像カード `bdAppendNodeImage` の副次修正**: [gb-board-render-node.js](gb-board-render-node.js) で `img.onerror` を `img.src` より前に装着。src 代入直後に同期でエラーが発生した場合でも `display:none` ハンドラが確実に動作するようにする
- **検証**: DevTools Console で `[doc CAPTURE] dragover → [board] dragover fire → [board] drop fire → カードを追加しました` の流れが通ることを確認、フォルダツリーから画像ファイルをボードへ D&D して画像カードが作成されることを確認

## v0.5.321 (2026-04-24)

### feat(smart-db): スマートシートに「全件インデックス」種別を追加

計画書: [docs/smart-db-global-index-plan-2026-04-24.md](docs/smart-db-global-index-plan-2026-04-24.md)

- **全件インデックスのサーバー基盤を新設**: [meldex_global_index_service.py](meldex_global_index_service.py) と [meldex_api_global_index.py](meldex_api_global_index.py) を新規追加。VAULT_PATH + HOME_FOLDER 配下の全ファイルを再帰走査してメタ情報（実態パス・ソースフォルダ・種別カテゴリ・サイズ・mtime・被リンク数）を索引化する `GET /api/global-index` と、キャッシュ破棄用 `POST /api/global-index/invalidate` を公開
- **キャッシュ永続化**: インデックスは `<HOME_FOLDER>/.meldex/global-index.json` に保存し、2 回目以降は mtime 最大値 + ファイル数のシグネチャで stale 判定。無変更ならキャッシュを即返却
- **キャッシュ無効化フック**: [Meldex.part02.py](Meldex.part02.py) で `backlinks_update_source` / `backlinks_remove_source` が呼ばれるたびに `invalidate_global_index_cache()` を連動。[Meldex.part01.py](Meldex.part01.py) の `_set_vault_path` / `_set_home_folder` でもキャッシュをクリア
- **種別カテゴリ分類**: 拡張子ベースで `image` / `audio` / `video` / `csv` / `pdf` 等を、`.md` は frontmatter の `type` で `meldex-board→board` / `meldex-page→page` / `meldex-scriptnote→scriptnote` / `meldex-database→database` に分類。`.scriptnote.json` / `.smart.json` は拡張子で直接判定
- **被リンク集計**: 既存 SQLite `backlinks` テーブルを再利用。`source_path` 側のカテゴリを突き合わせて「ボード由来」「台本由来」「ページ由来」を別カウント。SQLite IN 句の 999 制限を避けるため 500 件チャンクで分割クエリ。現状 backlinks は VAULT_PATH のみ索引化されているため、HOME_FOLDER 配下のファイルは被リンク列が `null`（計画書§5.4 の Phase 1）
- **除外ルール**: backlinks と揃えて `('.', '_chat/', '_backup/', '_trash/', '_versions/', 'node_modules/')` 配下は走査対象外。`.meldex/global-index.json` 自身もこれにより自己参照せず
- **フロント: 全件インデックス種別を追加**: [gb-db-smart-global.js](gb-db-smart-global.js) を新設。列セット（ファイル名 / 種別 / ソースフォルダ / 実態パス / サイズ / 更新日時 / 被リンク数）、列ヘッダクリックでのソート、行クリックでカテゴリに応じた適切なビューで開く (`selectDatabase` / `openPage` / `openBoard` / `openScriptnote` / `openMediaPreview` / `openCsvViewer`)、専用フィルタダイアログ (`showGlobalIndexFilterModal`) を実装
- **既存スマートシートとの共存**: [gb-db-smart.js](gb-db-smart.js) の `normalizeSmartDbDefinition` に `sourceType: 'db-entities' | 'all-files'` を追加。`selectSmartDb` で分岐し、既存の DB エントリ横断型は完全に従来通り動作。スマートシート一覧に「+ 全件インデックスを新規作成」ボタンを追加、コンテキストメニューの「フィルタ設定」も種別に応じて専用ダイアログへ分岐
- **フィルタ演算子**: `all-files` 種別は列選択ドロップダウン + 演算子 (equals / not_equals / contains / not_contains / greater_than / less_than / after / before / is_null / not_null) + 値。カテゴリ列やルート種別列は選択肢ドロップダウンで入力
- **Meldex.html**: `gb-db-smart.js` の直後に `gb-db-smart-global.js` を読み込み
- **検証**: サービス単体スモーク（3 ルート・除外・カテゴリ分類・キャッシュヒット）、ルーター登録確認 (`/api/global-index`, `/api/global-index/invalidate`)、`test_meldex_router_modules` (28) / `test_meldex_api_smoke` (29) / batch24+29 (19) すべて OK

## v0.5.320 (2026-04-24)

### feat(board): ライン形状を 3 種 (曲線 / 直線 / 直角線) に統合、全形状でアンカー・自己ループ対応

計画書: [docs/board-line-shape-unification-plan-2026-04-24.md](docs/board-line-shape-unification-plan-2026-04-24.md)

- **pathType を 3 種に集約**: 従来 5 種（curve / straight / orthogonal / orthogonal-curve / free-bezier）を `curve` / `straight` / `orthogonal` の 3 種に統合。スタイル管理ダイアログ [gb-board-style-manager.js](gb-board-style-manager.js) の形状セレクタも 3 種に整理
- **既存データの自動マイグレーション**: [gb-canvas-engine.part02.js](gb-canvas-engine.part02.js) に `_bdMigrateConnectionSchema()` を新設し、読込時に `free-bezier → curve`、`orthogonal-curve → orthogonal` (`cornerRadius: 12`) を自動変換。`bd.connections` / `bd.lineStyles` / `bd.depthStyles[*].line` のすべてを走査。書き出しは常に新形式
- **全形状でアンカー指定を反映**: `conn.fromAnchor` / `conn.toAnchor` を曲線・直線・直角線すべてで尊重。アンカー未指定時は従来の自動算出（辺中央 + GAP / 中心ベクトルクリップ）を維持
- **曲線の自動/手動モード**: `conn.controlPoints` の有無で自動モード（auto ベジェ + bulge）と手動モード（cp1/cp2 を尊重）を切替。選択時はどちらも制御点ハンドルを常時表示し、ドラッグ開始で手動モードへ自動切替
- **直角線のパラメータ化**: `branchRatio`（分岐位置、既定 0.3）と `cornerRadius`（コーナー半径、既定 0、>0 で旧 orthogonal-curve 相当）を conn / lineStyle / depthStyle.line に保存可能。`_bdRoundedOrthogonalPath` を `(points, radius)` シグネチャに拡張
- **自己ループを形状別に最適化（案 B）**: 曲線は `top-center → left-center` で左上象限に膨らむベジェループ、直角線は同アンカーで L 字 2 段迂回（LOOP_PAD=40px）、直線は自己ループ作成時に曲線へ自動昇格する仕様
- **既定にリセット**: ラインの右クリックメニューに「形状を既定にリセット」項目を追加（[gb-canvas-features.part01.js](gb-canvas-features.part01.js)）。`fromAnchor` / `toAnchor` / `controlPoints` / `branchRatio` / `cornerRadius` を削除してスタイル継承へ戻す
- **スタイル管理 UI 拡張**: 直角線選択時に「コーナー半径」「分岐位置 (%)」の数値入力欄を追加。`makeNumInput` が整数のみ扱うため分岐位置は 5〜95% で表示・保存時に 0.05〜0.95 へ変換
- **プリセット更新**: `line-free-bezier` プリセットを `line-manual-curve`（曲線・手動）に改名し pathType を `curve` に統一
- **認知負荷の配慮**: 旧「直角曲線」を選んでいたスタイルは UI 上「直角線 + コーナー半径 12」として表示される。見た目は完全互換

## v0.5.317 (2026-04-24)

### fix(board,sticky,annotation,options): 付箋/しっぽ/ボード関連の安定化

- **ボード付箋の右クリックメニュー + 「…」ボタン**: [meldex-core.part01.js](meldex-core.part01.js) の埋め込み付箋 `_renderNote` に `_showEmbeddedNoteContextMenu` を実装。色変更・しっぽ追加/削除・削除のメニューを右クリック（contextmenu）/長押し/ヘッダ右端の「…」ボタンから開けるようにした。`data-ann-more` ボタン上ではドラッグ開始しない、pointerdown/click の伝播を止めるガードも追加
- **付箋右クリックでボードメニューが同時に出る不具合を修正**: 付箋上の `pointerdown` で `e.button !== 0` の場合に `stopPropagation()` を呼び、bd-canvas 側の右クリック検出 (`bd._rightClickPos` → `bdContextMenu`) を抑止。注釈ツールバーオン時は [gb-canvas-interact.js](gb-canvas-interact.js) の `pointerup` でも `bdIsAnnotationModeActive()` チェックを追加してボードの右クリックメニューを出さないようにした
- **しっぽ永続化の不具合を修正**: [gb-annotation-tails.js](gb-annotation-tails.js) `_refreshTargets` の自動削除ロジックが、ボード再読み込み直後（カード DOM 未レンダリング時点）に `_findTarget` が null を返した結果 `_removeTail` + `persist` を走らせ、バックエンドの `data.tail` を上書き消去していた。自動削除を廃止し、ターゲット未検出の tick はスキップして次の tick で再試行する形に変更
- **しっぽ追加の明示保存**: コンテキストメニュー/`…` メニューから「しっぽを追加/削除」を選んだ際、`data.tail` と `ctx.data.tail` を明示同期し、`AnnotationStickyTail.setTail` 後に `apiPut('/annotations/<id>', { data: _notePayload(...) })` を直接呼んで確実にバックエンド保存するようにした
- **しっぽ追従をレイアウト座標化**: Meldex 表示倍率 (`documentElement.style.zoom`) やボード zoom の影響で付箋がカードから徐々に離れる問題を解消。`note.style.left/top` と `data.x/y` をレイアウト座標で管理する実態に合わせ、追従差分を `clientDx/Dy` から `layoutDx/Dy` に変更。拡大率に関わらず相対距離が一定に保たれる
- **しっぽ追従を rAF 化**: 従来の `setInterval(700ms)` から `requestAnimationFrame` ループに変更し、付箋ドラッグ/カード移動時のしっぽ追従を 60fps で即時反映。`tracked.size === 0` で自動停止、タブ非可視対策に `setInterval(1500ms)` の安全網も併設
- **ビューワーパネルのミニマップ表示領域枠をリアルタイム連動**: [gb-canvas-engine.part02.js](gb-canvas-engine.part02.js) `bdTransform` 末尾で `bdUpdateMinimap()` をトリガ。パン/ズーム/回転中もミニマップのビューポート枠が遅延なく追従
- **ボード背景画像に「ボード追従」モード追加**: [gb-board-background.js](gb-board-background.js) に fit モード `'world'` を新設し、`bd-world` 配下に `<img>` を配置することでボードのパン/ズーム/回転に連動する背景画像を実装。表示倍率 `_bgImageScale` を `--bd-bg-image-scale` で永続化し、[gb-detail-panel.part01.js](gb-detail-panel.part01.js) の `boardBgFit` フィールドに「ボード追従」選択と倍率入力を追加
- **オプションパネル右端の余白確保**: [gb-tools.part03.css](gb-tools.part03.css) `.bd-detail-section` に `padding-right: 1em; box-sizing: border-box;` を追加し、左の `margin-left: 1em` と対称になるよう調整。入力/セレクト/色スウォッチがパネル右端に接触する見切れを解消
- **回帰テスト更新**: [tests/test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py) の sticky tail ガードを `layoutDx/Dy` / `_removeTail(note, null)` へ更新

## v0.5.316 (2026-04-22)

### feat(icons): Lucide / Noto Emoji 選択とチャット参照範囲を拡張

- **Lucide / Google Noto Emoji の共通ピッカーを追加**: [gb-icon-assets.js](gb-icon-assets.js) / [gb-icon-assets.css](gb-icon-assets.css) / [noto-emoji-data.js](noto-emoji-data.js) を追加し、全 Lucide アイコンと Google Noto Emoji を検索・タブ切替で選択できる共通UIにした。Noto Color Emoji の Windows compatible フォントとライセンス表記も同梱した
- **アイコン利用箇所を共通ピッカーへ統合**: チャットスタンプ、コールアウト、ページアイコン、ユーザーアイコン、アプリバーのレイアウトアイコンで Lucide / Noto Emoji を選択可能にした。コールアウトは Markdown 復元時も `GBIconAssets` 経由で再描画し、ユーザーアイコンは選択したアイコンと背景色を再生成・同期するようにした
- **オプションパネル内タブのD&D並べ替えを追加**: [gb-detail-tab-dnd.js](gb-detail-tab-dnd.js) / [gb-detail-tab-dnd.css](gb-detail-tab-dnd.css) を追加し、`detail-tab-order` に順序を保存、動的に追加されるタブにも MutationObserver で並べ替えを適用するようにした
- **チャットLLMの参照範囲を拡張**: [meldex_chat_stream.py](meldex_chat_stream.py) で `source:/`、`home:/`、`code:/` の仮想ルートを導入し、ソースフォルダ、ホーム、Meldexコードを検索・閲覧・読み取り可能にした。コード領域は読み取り専用にし、書き込み系ツールは既存の書き込み許可チェックを通すようにした
- **権利表記と配布同梱を整理**: [THIRD-PARTY.md](../THIRD-PARTY.md) / [CREDITS.md](../CREDITS.md) を同梱資産・必須依存・任意依存の棚卸し表付きで更新し、About 表示にも Noto Emoji とバックエンド主要ライブラリを追加した。配布ビルドでは `LICENSE` / `THIRD-PARTY.md` / `CREDITS.md` と `vendor/` を必ずコピーし、PDF.js の LICENSE / NOTICE も配布に含めるようにした
- **回帰テストを追加**: [test_meldex_detail_tab_dnd.py](tests/test_meldex_detail_tab_dnd.py)、[test_meldex_chat_stream.py](tests/test_meldex_chat_stream.py)、[test_meldex_split_assets.py](tests/test_meldex_split_assets.py)、[test_meldex_frontend_regressions.py](tests/test_meldex_frontend_regressions.py) に、タブD&D、チャット参照範囲、Noto/Lucide資産同梱、権利表記配布、vendorライセンス同梱の静的ガードを追加した
- **検証**: JS/Python構文、`test_meldex_split_assets`、関連回帰 suite 100件、`git diff --check` を確認した

## v0.5.315 (2026-04-22)

### feat(sheet): 画像プロパティ・フォームビュー・公開HTMLを追加

- **画像プロパティを追加**: [meldex_api_database.py](meldex_api_database.py) に `_media/` 保存、サムネイル、参照再構築、GC、公開フォーム画像取り込みAPIを追加し、[gb-db-image-gallery.js](gb-db-image-gallery.js) でセル/詳細の画像追加・削除・並べ替え・キャプション編集を扱うようにした
- **プロパティレイアウトをシートメタデータ化**: [gb-detail-property-layout-editor.js](gb-detail-property-layout-editor.js) とエントリ詳細表示を接続し、順序・非表示・グループ・テンプレートを `property_layout` として保存、プロパティ名変更/削除時に参照も追従するようにした
- **フォームビューを追加**: [gb-db-view-form.js](gb-db-view-form.js) を新設し、保存済みビューに `formConfig` を保持して、編集モード/回答モード/内部テスト送信をシートビューとして扱えるようにした
- **公開HTMLと直接上書き保存を追加**: [gb-public-runtime.js](gb-public-runtime.js)、[gb-publish-settings.js](gb-publish-settings.js)、[gb-export-html.js](gb-export-html.js)、[meldex_api_shell.py](meldex_api_shell.py) で公開HTML生成、公開タブ、Meldex公開URL/送信先URL、許可済みHTMLパスへの直接上書き保存を実装した
- **公開フォーム送信を追加**: `POST /api/public-form/submit` でトークン・honeypot・レート制限を確認し、送信内容を `source: public-form` / `reviewed: false` のエントリとして作成するようにした
- **ドキュメント/監査索引を更新**: シート/スマートシート設計意図、現行機能リスト、UIデザインシステム、公開フォーム中継メモ、ツールバー配置メモ、AGENTS監査バッチ、ルーター/公開フォーム画像回帰テストを更新した
- **検証**: Python構文、分割チャンク結合後のJS構文、`unittest` 94件（router/frontend/entity-create+public-form画像）を確認した

## v0.5.314 (2026-04-22)

### feat(smart-db): ダッシュボードをスマートシートビューへ統合

- **スマートシート内ビューへ統合**: [Meldex.html](Meldex.html) の `#smart-db-view` 内に表/ダッシュボードのタブと表示領域を追加し、独立 `#dashboard-view` / `#body-dashboard` と旧ダッシュボード導線を撤去した
- **ダッシュボード定義をスマートシートJSONへ移管**: [gb-db-smart.js](gb-db-smart.js) で `views.table` / `views.dashboard` / `activeView` を正規化・保存し、ファイルベースのスマートシートでは `def._filePath` を自動バージョン管理対象にするようにした
- **ウィジェット描画とソース解決層を新設**: [gb-db-smart-dashboard-view.js](gb-db-smart-dashboard-view.js) を追加し、1ウィジェット1ソースのシート/スマートシート選択、`resolveWidgetData()`、`normalizeSmartSheetAsDbLike()`、統計/進捗/チャート/リスト描画をスマートシート配下へ移した
- **旧ストレージと旧ルートを整理**: 起動時に `dashboards` / `dashboard:*` を一度だけ削除する移行処理を入れ、アプリバー、ペインブリッジ、アウトライナー、リンク、ツールメニュー、エクスポート、ビュー固定、履歴、設定、E2Eから独立ダッシュボード経路を外した
- **ドキュメント/テストを更新**: ダッシュボード/スマートシート設計意図、現行機能一覧、アプリバースナップショット、E2E fixture/generated cases、frontend regression guard を統合後の仕様へ更新した
- **徹底チェック**: スマートシートを開く、表/ダッシュボード切替、ウィジェット追加/編集/削除、親スマートシート0件、ソース未設定、参照先削除、不正 `activeView`、旧ダッシュボード導線撤去、HTML/PNG出力の呼び出しチェーンを再確認した

## v0.5.313 (2026-04-22)

### docs(e2e): UI E2E検出不具合の修正完了を明記

- **UI E2E検出不具合の修正状況を確認**: `v0.5.312` の UI全E2E coverage gate 実行で検出されたボード注釈保存、runtime重複identity、カレンダー編集モーダル重複、バージョンモーダル閉じるボタン重複、ボード/scriptnoteの保存済みstateとin-memory stateのずれは修正済みであることを確認した
- **最終UI E2E結果を記録**: `app/tests/_tmp/e2e-full-20260422-100306` の `failed-cases.txt` は空、frontend smoke と生成E2E全63件はいずれも `OK`。チェックボックス単位のUI検証、未検証/不安定identity/ラベル不足/重複identityのfail gateを有効化した状態で通過済み

## v0.5.312 (2026-04-22)

### feat(e2e): UI全E2E coverage gateを導入

- **DOM UIインベントリとcoverage gateを追加**: [gb-e2e-coverage.js](app/gb-e2e-coverage.js) を新設し、可視のボタン/入力/セレクト/テキストエリア/ロール付き要素をケース実行中に収集するようにした。未検証・不安定identity・ラベル不足・重複identityをfail条件として扱う
- **チェックボックス単位の検証を追加**: 可視チェックボックスは状態反転と復元を非破壊で確認し、`__GBE2E_UI_CHECKBOX_CHECKS__` とcoverage summaryへ記録するようにした
- **v5.3 E2Eへ全ケース統合**: [meldex_frontend_e2e_matrix.py](app/tests/meldex_frontend_e2e_matrix.py) で全63ケースへ `auto_verify_visible` / `fail_unverified_visible` / `verify_checkboxes` を付与し、[e2e_ui_manifest.json](app/tests/_generated/e2e_ui_manifest.json) に静的DOMインベントリを生成するようにした
- **動的UIの安定identityを補強**: ショートカット、シートビュー/グラフ/チャート、バリデーション、スマートシート、ダッシュボード、カレンダー、ボードスタイル管理、バージョン差分、一括編集、リッチテキストツールバーなどの動的コントロールへ `data-e2e-id` / `aria-label` を追加した
- **ボード注釈の保存経路を修正**: ボード埋め込み注釈ではHTML iframe専用の `postMessage` ハンドラに依存せず、bridge内から直接付箋/ストロークを保存・表示するようにし、付箋作成をE2Eで実操作として検証可能にした
- **検証**: JS/Python構文、split bundle結合、batch47/frontend regression unittest、失敗再現ケース30件、frontend smoke、生成E2E全63件を確認した

## v0.5.311 (2026-04-22)

### feat(e2e): v5.3仕様追従とUI E2E基盤を更新

- **v5.3仕様定点観測の要修正を反映**: オプションパネル開閉APIを `toggleOptionPanel()` へ移行し、旧 `toggleDetailPanel()` は互換aliasとして残した。ワークスペースPython API削除は現役APIのため見送り、スナップショット/設計意図の記述を訂正した
- **ボード/注釈/アプリバーの構造を整理**: ボード内部の同期的な `bdUpdateMinimap()` 直呼びを dirty flush 経由へ寄せ、`gb-annotations.js` を `part01/part02` に分割し、`gb-activity-bar-v2.js` を `#app-bar` の配置方向エンジンとして実働化した
- **ショートカットとチャットの表記/復元を補強**: `gb-shortcuts.js` にシナリオscopeを追加し、Alt+Shift+C のコメント追加を中央レジストリへ統合。チャットは「セッション名」を「チャット名」へ改め、存在しない保存チャット参照を404として処理するようにした
- **v5.3 UI E2Eを拡張**: `gb-e2e-actions-v53.js` を追加し、アプリバーv2構造、オプションパネル表記、タイマーパネル、アプリレイアウト切替を検証可能にした。E2E matrix に `timer` を追加し、生成ケースを63件へ更新した
- **仕様書/計画書/回帰テストを更新**: v5.3 snapshot、設計意図、6本の実施計画、監査メモ、frontend regression/split/batch47テストを更新し、分割チャンクとE2E追加ファイルを監査バッチへ登録した
- **検証**: JS/Python構文、split bundle結合、関連unittest、frontend smoke、targeted/pairwise Chrome E2Eを確認した

## v0.5.310 (2026-04-22)

### fix(audit): 全ファイル全行チェックで検出した高リスク不具合を修正

- **ドキュメント/静的配信の安全性を強化**: [meldex_api_static.py](app/meldex_api_static.py) で `/docs` のパストラバーサル、catch-all 静的配信の `..` セグメント、Markdown リンクの `javascript:` スキームをブロックした
- **認証状態とDB APIの境界を補強**: [meldex_api_auth.py](app/meldex_api_auth.py) で未認証 `auth/me` が保存済みロールを返さないようにし、[meldex_api_database.py](app/meldex_api_database.py) で旧形式DBのエンティティ作成・値リネーム時のファイル名サニタイズ漏れを修正した
- **カレンダー/勤怠の境界条件を修正**: [meldex_api_calendar.py](app/meldex_api_calendar.py) で `date_to=YYYY-MM-DD` が当日中の勤怠を含むようにし、[gb-calendar.part01.js](app/gb-calendar.part01.js) で Calendar Undo/Redo が元DBに適用されるようスナップショットにDBパスを保持した
- **フロントエンドの非同期・保存漏れを修正**: [gb-database.part01.js](app/gb-database.part01.js) でDB/エンティティ読み込み競合と `$version` 取得先を修正し、[gb-editor.part01.js](app/gb-editor.part01.js) / [gb-editor.part03.js](app/gb-editor.part03.js) で内部リンク遷移前に保留中のノート/自由記述自動保存をフラッシュするようにした
- **iframe通信と閉じ際保存を堅牢化**: [gb-app.part03.js](app/gb-app.part03.js) で HTML ビューワー `postMessage` の origin/source を検証し、`beforeunload` 保存を `keepalive` fetch に変更して空本文や `PUT /api/value` 更新漏れを防いだ
- **注釈/リレーション/分割ロードの局所不具合を修正**: [gb-right-panel.js](app/gb-right-panel.js) のコメントジャンプを `navOpen()` 経由にし、[gb-db-relations.js](app/gb-db-relations.js) で候補値破壊を防止、[gb-split-loader.js](app/gb-split-loader.js) で split chunk の `304 Not Modified` を再取得するようにした
- **補助サービスのデータ保護を改善**: [meldex_sep_service.py](app/meldex_sep_service.py) でSEP貼り付け後にテキストクリップボードを復元し、[meldex_file_rename_service.py](app/meldex_file_rename_service.py) で保存チャットの `targetPath` 更新対象を `_chat/llm` 配下再帰に拡張した
- **回帰テストを追加**: [test_meldex_batch24_api_regressions.py](app/tests/test_meldex_batch24_api_regressions.py)、[test_meldex_batch29_api_regressions.py](app/tests/test_meldex_batch29_api_regressions.py)、[test_meldex_batch30_api_regressions.py](app/tests/test_meldex_batch30_api_regressions.py)、[test_meldex_api_database_entity_create.py](app/tests/test_meldex_api_database_entity_create.py)、[test_meldex_calendar_attendance_regressions.py](app/tests/test_meldex_calendar_attendance_regressions.py) に再発防止ガードを追加した

## v0.5.309 (2026-04-21)

### fix(theme): テーマカラー個別設定の保存経路と不足変数を修正

- **テーマカラー個別設定をカスタムテーマに保存**: [gb-theme-manager.js](app/gb-theme-manager.js) で、テーマカラーセットの各スロットに設定した個別色と「色調整を反映」状態をカスタムテーマの `ui.themeColorSlotSettings` に含め、テーマ作成・更新・インポート・適用時に復元されるようにした
- **テーマカラー行のクリック挙動を統一**: [gb-settings.part02.js](app/gb-settings.part02.js) で、テーマカラーセットのスウォッチクリックが旧カラーパレットではなく、色相・彩度・明度と「色調整を反映」を持つ個別編集ポップアップを開くように統一した
- **カレンダーテーマの不足項目を補完**: [gb-tool-calendar-theme.js](app/gb-tool-calendar-theme.js) / [gb-theme-manager.js](app/gb-theme-manager.js) で、イベント右余白、タスク列背景、タスク見出し背景、タスク優先度背景の設定UIと派生フォールバックを追加した
- **最小構成テーマの派生値を補強**: [gb-theme-manager.js](app/gb-theme-manager.js) で、シート、ノート、フォルダ、カレンダー関連のテーマ変数に不足していたフォールバックを追加し、カスタムテーマ切替時に古い値が残りにくいようにした
- **CSS分割上限違反を解消**: [gb-ui.part01.css](app/gb-ui.part01.css) / [gb-ui.part03.css](app/gb-ui.part03.css) で、汎用レンジ・リンク定義を移動し、分割CSSの1000行上限チェックを再度通過するようにした
- **回帰テストを追加**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) に、テーマカラー個別設定の保存・復元経路、カレンダー不足変数、フォールバック追加の静的ガードを追加した

## v0.5.308 (2026-04-21)

### feat(layout): アプリレイアウト管理の再整備とアイコン選択

- **レイアウトの「デフォルトに戻す」がカスタムレイアウトでも動作するように**: [gb-app-layout.js](app/gb-app-layout.js) で、カスタムレイアウト作成時に `initialLayout` / `initialActivePaneId` / `initialFilterState` のスナップショットを保存し、リセット時にそこへ戻すフローを追加した。組み込みレイアウトは従来どおり `_buildDefaultLayoutRecord` で構築される。「デフォルトとして保存」で `initialLayout` が更新される
- **ハードリロード後のレイアウト自動保存を堅牢化**: `_ensureInitialized` の強制上書き（`_persistCurrentAppLayout`）を廃止し、編集時のみ `_autoSaveCurrentAppLayout` で `gb:app-layouts[appId]` が更新されるようにした。旧 `workspace:*` ソースは `custom` へ自動移行
- **ワークスペース機能を削除**: [gb-settings-workspace.js](app/gb-settings-workspace.js) を削除、[Meldex.html](app/Meldex.html) の読み込みを外し、アプリバーメニューの「ワークスペース」サブメニュー・設定ダイアログの対応セクション・関連参照（`isWorkspaceSource` / `flushActiveWorkspaceLayout` / `_autoSaveCurrentWorkspace` / `_saveWorkspaceFilterState` 等）を整理した
- **全 Lucide アイコン (1695 種) をアプリレイアウトボタンに設定可能に**: [vendor/lucide-icons.js](app/vendor/lucide-icons.js) を新設（公式 `lucide-static` sprite から ISC ライセンス下で生成）、[meldex-core.part01.js](app/meldex-core.part01.js) の `lucide()` に `LUCIDE_FULL` フォールバックを追加。レイアウトボタンの「⋯」メニューに「アイコン変更」を追加し、検索可能なグリッドピッカー（大小字・ハイフン・アンダースコアを無視したノーマライズ検索）を実装した

### feat(theme): テーマカラーセットの個別編集

- **テーマカラーを色ごとに個別編集可能に**: [gb-settings.part02.js](app/gb-settings.part02.js) / [gb-ui.part02.css](app/gb-ui.part02.css) で、設定ダイアログのテーマカラー行をクリックすると、色相・彩度・明度スライダーとカラーピッカーを持つポップアップを開くようにした
- **「色調整を反映」オプションを追加**: 個別色ごとに `applyAdjust` を保持し、オンの場合はテーマカラー内の調整スライダーを個別設定後の色にも適用、オフの場合はその色を固定表示するようにした。保存キーは `meldex-theme-color-slot-settings`
- **保存・復元経路を補強**: [gb-settings.part01.js](app/gb-settings.part01.js) / [gb-settings.part03.js](app/gb-settings.part03.js) / [gb-settings.part04.js](app/gb-settings.part04.js) で、設定ダイアログのキャンセル復元、UI設定バックアップ、テーマリセット、テーマJSONのインポート/エクスポートに個別色設定を含めた
- **回帰テストを追加**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) に、個別色設定の正規化、色調整オン/オフの計算、保存キーの永続化を検証するガードを追加した

### feat(theme): 背景色とポップアップ色の一元化・整理

- **「背景色」を「共通背景色」にリネーム**: [gb-settings.part02.js](app/gb-settings.part02.js) で `--bg` 専用行であることを明確化
- **サブ背景色・強調背景色を 共通 セクションに追加**: `--bg2`（パネル/タブ/フォルダツリー）と `--bg3`（ダイアログ/ホバー領域）を設定可能にした
- **ポップアップ背景色を共通化**: [gb-ui.part01.css](app/gb-ui.part01.css) に `--ui-popup-bg`（既定 `var(--bg2)`）を追加し、設定ダイアログの 共通 セクションに「ポップアップ」行を新設。カラーパレット ([gb-color-palette.js](app/gb-color-palette.js) の `.gb-palette` / `.gb-ctx-menu`)、コンテキストメニュー、書式ポップアップ、ドロップダウン、ツールチップ、スクリプトノートのヘッダー/ロール/サブメニュー、ステータスドロップダウン、スタンプピッカー、ボードリンクツールチップ、ボード検索バー、一括選択バー、フローティング注釈ツールバーなど全ポップアップで `var(--ui-popup-bg, ...)` 参照に統一した
- **各項目に専用 bg 変数を導入**: 通常文字 (`--ui-text-bg`)、サブテキスト (`--ui-muted-bg`)、強調文字 (`--ui-fg-strong-bg`)、アクセント (`--accent-bg`)、リンク (`--accent2-bg`) 等を新設。`--bg` と衝突しないため、各項目の背景色を独立して変更可能
- **透明設定を正しく反映**: [gb-format-popup.js](app/gb-format-popup.js) の `_makeSwatchBg` で `'transparent'` を `''` に潰さず伝播するよう修正。透明スウォッチ表示は `_setSwatchBg` / `_applyBgOnlySwatchBackground` でチェッカー柄を適用
- **通常文字に太字/斜体/フォントサイズを追加**: [gb-theme.css](app/gb-theme.css) の `body` で `--ui-text-font-size` / `--ui-text-bold` / `--ui-text-italic` を消費。サブテキストも [gb-ui.part01.css](app/gb-ui.part01.css) の `.gb-section-desc` と [gb-layout.css](app/gb-layout.css) の `.pane-title` で `--ui-muted-font-size` / `--ui-muted-bold` / `--ui-muted-italic` を消費するようにした
- **スライダー行のプレビューを実物のスライダーに**: [gb-format-style-ui.js](app/gb-format-style-ui.js) で `previewType:'slider'` マーカーを解釈し、`<input type="range">` を 50% 位置でレンダリング。ドラッグは無効化し、クリックで書式ポップアップが開く
- **「赤（エラー）」を「エラー・警告」にリネーム、「青（採用）」を削除**: シートの採用ステータス色は シート タブの「採用ステータス」行（`--db-status-adopted-color`）へ移設。マルチ選択ソースバッジ 1〜4 (`--db-msr-badge-*-bg`) もシートタブへ。カレンダー優先度 緊急/高/中 (`--cal-task-priority-*-bg`) はカレンダー専用テーマエディタ ([gb-tool-calendar-theme.js](app/gb-tool-calendar-theme.js)) に追加した

## v0.5.307 (2026-04-21)

### feat(theme): テーマカラーと共通カラーパレットの再編

- **テーマカラーを独立セクションとして配置**: [gb-settings.part04.js](app/gb-settings.part04.js) で、設定ダイアログのテーマタブに「テーマカラー」セクションをテーマとテーマ設定の間に追加し、従来テーマ設定内にあったテーマカラー編集UIを移動した
- **テーマカラーの適用セクションから自動（明/暗）スライダーを削除**: [gb-settings.part02.js](app/gb-settings.part02.js) で、`renderThemeUiApplicationEditor` から `renderThemeUiAutoToneControls()` の呼び出しを外した
- **標準色カラーセットと調整スライダーを設定ダイアログのテーマカラーセクションに移設**: [gb-settings.part02.js](app/gb-settings.part02.js) / [gb-ui.part02.css](app/gb-ui.part02.css) で `renderSettingsThemePaletteEditor()` を追加し、4行構成のカラーマトリクス（透明+グレー / 自動（明） / 自動 / 自動（暗））と色相始/終・彩度・明度・明暗比の5スライダーを表示、保存は既存の標準パレット調整に寄せた
- **共通カラーパレットポップアップから調整スライダーを除去**: [gb-color-palette.js](app/gb-color-palette.js) で `標準色調整` の折りたたみブロックと5スライダーを削除し、設定ダイアログと重複しないようにした
- **共通カラーパレットに見出しと行ラベルを追加**: [gb-color-palette.js](app/gb-color-palette.js) で「テーマカラーセット」「カスタムカラーセット」の見出し、および 4 行のカラーマトリクスに「自動（明）」「自動」「自動（暗）」のラベルを追加した
- **「＋保存」を「＋追加」にリネームしスポイトボタンを追加**: [gb-color-palette.js](app/gb-color-palette.js) で、カラーピッカー横のボタンラベルを改名し、EyeDropper API を用いた画面ピックアップボタンを新設した
- **回帰テストを更新**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) に、新しい見出し・ボタンラベル・スポイトボタンの静的ガードと、従来の `standardControls` 検証の置換を反映した

## v0.5.306 (2026-04-21)

### feat(calendar): 打刻表示設定の整理とテーマ拡張

- **「時計を表示する」設定を削除**: [gb-settings.part01.js](app/gb-settings.part01.js) / [gb-settings.part03.js](app/gb-settings.part03.js) で、カレンダーの打刻セクションを常時表示する現仕様に合わせて無効化された設定項目を除去した
- **出退勤状況をソースフォルダ別に折りたたみ可能に**: [gb-tool-calendar-attendance.js](app/gb-tool-calendar-attendance.js) / [gb-tools.part02.css](app/gb-tools.part02.css) で、出退勤状況リストをフォルダごとのアコーディオンUIにし、開閉状態を `gb:cal-attendance-folder-open:{folder}` として保持するようにした
- **オプションパネルでカレンダーのビューが月に戻る不具合を修正**: [gb-tool-calendar.js](app/gb-tool-calendar.js) で、`setView` 呼び出し時に永続状態 `tab.state.view` へ即時反映し、パネル再マウント時の `restoreState` が古い状態で上書きしてしまう問題を解消した。同種の不具合が他コンポーネントに無いことも確認した
- **土曜日・日曜日の文字色をテーマで設定可能に**: [gb-tool-calendar-theme.js](app/gb-tool-calendar-theme.js) / [gb-theme-manager.js](app/gb-theme-manager.js) / [gb-tool-calendar-views.js](app/gb-tool-calendar-views.js) / [gb-tools.part02.css](app/gb-tools.part02.css) で `--cal-saturday-fg` / `--cal-sunday-fg` を追加し、月表示・週表示のヘッダと日付番号に適用されるようにした
- **マイカレンダーの色をテーマパレットから自動配色**: [gb-tool-calendar-attendance.js](app/gb-tool-calendar-attendance.js) / [gb-tool-calendar-views.js](app/gb-tool-calendar-views.js) で、新規カレンダー作成時にテーマカラーセットから色を割り当て、カレンダー行メニューに色選択スワッチを追加した。ユーザーが変更した色はそのまま保持され、テーマ切替時には未指定分のみが自動更新されるようにした
- **カレンダーイベントカードにフチを追加**: [gb-theme-manager.js](app/gb-theme-manager.js) / [gb-tools.part02.css](app/gb-tools.part02.css) で `--cal-event-border` のデフォルトを `--border` にフォールバックさせ、月表示のイベントカードにも1pxの枠線を表示するようにした

## v0.5.305 (2026-04-21)

### feat(theme): 共通フォントの整理とOSインストール済みフォントの自動追加

- **共通フォントのドロップダウンから自己参照の「既定フォント」を除去**: [gb-settings.part04.js](app/gb-settings.part04.js) で、設定ダイアログのテーマタブの共通フォント選択肢を `getUIFontOptions()` 経由に切り替え、共通フォント自身を決める場所に「既定フォント」が並ぶ自己参照を解消した
- **「既定フォント」を「共通フォント」に改名**: [gb-settings.part01.js](app/gb-settings.part01.js) / [gb-format-popup.js](app/gb-format-popup.js) / [gb-board-style-manager.js](app/gb-board-style-manager.js) で、書式設定ポップアップとボードスタイル編集のフォント選択肢の既定値ラベルを、共通フォントへのフォールバックを示す「共通フォント」に統一した
- **OSにインストール済みのフォントをドロップダウンに自動追加**: [gb-settings.part01.js](app/gb-settings.part01.js) に `getDetectedSystemFonts()` を追加し、macOS / iPadOS / iOS の Hiragino・Helvetica・Avenir 系、Windows の Meiryo UI・MS/BIZ UD 系・Consolas 系、Android / ChromeOS の Roboto・Noto CJK 系、クロスプラットフォームの Arial・Georgia・Courier 系などを canvas の幅計測でインストール有無を判定して optgroup「システムフォント」としてまとめた
- **フォント値の正規化を寛容化**: [gb-settings.part01.js](app/gb-settings.part01.js) で `normalizeFontFamilyValue()` が CSS インジェクション記号のみを排除し、検出されたOSフォント名を保持できるようにした
- **選択肢描画を optgroup 対応**: [gb-format-popup.js](app/gb-format-popup.js) の `_makeSelect` を `group` フィールド付きアイテムで optgroup を生成できるようにし、書式設定ポップアップ / ボードスタイル編集のドロップダウンでもシステムフォントが分類表示されるようにした
- **回帰テストを更新**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) に、`getDetectedSystemFonts` 関数の存在と「共通フォント」ラベルの統一、「既定フォント」文字列の非存在を確認する静的ガードを追加した

## v0.5.304 (2026-04-21)

### fix(theme): 書式設定ポップアップとノート余白のテーマ適用を整理

- **書式設定ポップアップの表示値を補完**: [gb-format-popup.js](app/gb-format-popup.js) / [gb-format-style-ui.js](app/gb-format-style-ui.js) で、フォントサイズと文字フチ幅に未設定値がある場合も現在の有効値を表示し、背景色は透明指定で背景なしを表せるようにして「背景色を使う」トグルを廃止した
- **設定ダイアログのテーマ編集を再整理**: [Meldex.html](app/Meldex.html) / [gb-settings.part03.js](app/gb-settings.part03.js) / [gb-settings.part04.js](app/gb-settings.part04.js) で、フォントタブを廃止し、共通フォントをテーマタブのテーマセクションへ移動、テーマ設定の見出し・アクセントバー・書式設定セクションをオプションパネルと揃えた
- **テーマ切替と保存の状態保持を修正**: [gb-settings.part02.js](app/gb-settings.part02.js) / [gb-settings.part04.js](app/gb-settings.part04.js) で、テーマ選択時に表示中の設定タブを維持し、設定保存時は共通フォントの現在値を確実にテーマへ反映するようにした
- **ノート左右余白をテーマ設定へ移動**: [gb-editor.part01.js](app/gb-editor.part01.js) / [gb-settings.part01.js](app/gb-settings.part01.js) / [gb-detail-panel.part01.js](app/gb-detail-panel.part01.js) で、ノートツールバーの余白操作を廃止し、オプションパネルと設定ダイアログのテーマタブから `--page-margin-x` として設定できるようにした。初期値は50pxに統一した
- **テキスト選択時の書式設定導線を共通化**: [gb-text-selection-format.js](app/gb-text-selection-format.js) / [gb-annotations.js](app/gb-annotations.js) / [gb-app.part03.js](app/gb-app.part03.js) で、選択テキストや付箋系の書式編集を共通の書式設定ポップアップ仕様へ寄せた
- **回帰テストを更新**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) に、共通フォント保存、ノート余白テーマ変数、旧ツールバー関数の撤去、書式設定ポップアップの既定値表示を確認する静的ガードを追加した

## v0.5.303 (2026-04-21)

### fix(theme): ファイル別OSアクセントとチェックボックス表示を修正

- **オプションパネルのOSアクセントをファイル別設定に分離**: [gb-detail-panel.part01.js](app/gb-detail-panel.part01.js) / [gb-note-enhance.part01.js](app/gb-note-enhance.part01.js) / [gb-note-enhance.part02.js](app/gb-note-enhance.part02.js) で、オプションパネルのテーマタブからOSアクセントを切り替えてもMeldex全体のグローバル設定を変更せず、ファイルスタイルの `__useOsAccentColor` として保存・復元・局所適用するようにした
- **ファイルテーマのパレット取得をグローバルOSアクセントから遮断**: [gb-theme-manager.js](app/gb-theme-manager.js) / [gb-settings.part02.js](app/gb-settings.part02.js) で、ファイル別テーマがテーマカラーを表示・適用するときはグローバルOSアクセントで上書きされた一時パレットを拾わないようにし、設定ダイアログ側のグローバル設定経路とは分離した
- **チェックボックスの見た目を白チェックに統一**: [gb-ui.part02.css](app/gb-ui.part02.css) / [gb-tools.part02.css](app/gb-tools.part02.css) / [gb-layout.css](app/gb-layout.css) で、複数存在していたチェックボックス表示を白いチェックの共通デザインに寄せ、チェックマークが左上へずれないよう中央配置にした
- **回帰テストを補強**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) に、ファイル別OSアクセント保存、グローバルOSアクセント呼び出し混入防止、チェックボックス中央配置の静的ガードを追加した
- **徹底チェックと全体チェックを実施**: 直近変更の操作経路を追跡し、`app/` 配下のスクリプト341ファイル / 関連アセット360ファイル / 156,120行を読み込み監査、Python 106ファイルとJS 145ファイルの構文確認、ユニットテスト334件、Chrome frontend smokeを確認した

## v0.5.302 (2026-04-20)

### fix(test/e2e): 自動テストとUI E2Eの実行安定性を回復

- **E2E最終アサーションの状態復元を補強**: [gb-e2e-actions.js](app/gb-e2e-actions.js) / [gb-e2e-actions-extra.js](app/gb-e2e-actions-extra.js) / [gb-e2e-actions-dashboard-compare.js](app/gb-e2e-actions-dashboard-compare.js) で、設定モーダル、ダッシュボード、比較ビュー、バージョンパネル、ボード詳細、シナリオテーマ確認がフル順序実行中のタブ切替・再描画遅延でも対象状態を再アクティブ化して検証できるようにした
- **ボード複数選択E2Eを安定化**: 複数カード / 複数ラインの選択状態をE2E側で記憶・復元し、詳細パネルのDOM表示が遅れた場合でもボード状態と保存結果を根拠に検証できるようにした
- **ダッシュボード・比較・エントリ遷移E2Eを補強**: ダッシュボードファイルのウィジェット設定を保存済みJSONからも検証し、一覧クリック後のエントリ表示、比較ビュー直開き、バージョンパネル表示の最終確認で直前操作の対象へ復帰するようにした
- **シナリオテーマ確認の巻き戻りを防止**: シナリオテーマE2Eの確認処理がDOM同期で保存済み設定を古い値へ戻さないよう、エディタ状態と保存済みJSONを分けて検証するようにした
- **シート一括編集とリレーション同期を修正**: [gb-db-bulk-edit.js](app/gb-db-bulk-edit.js) で relation / multi-relation の一括置換とカスケードクリアを復元し、[gb-db-relations.js](app/gb-db-relations.js) で削除済み参照先の逆リレーション解除がエラーで止まらないようにした
- **タイマーのカレンダー連動日時を修正**: [gb-tool-timer-advanced.js](app/gb-tool-timer-advanced.js) で当日イベント取得範囲をUTC文字列ではなくローカル日時文字列で送るようにし、タイムゾーン差でイベント自動開始対象が漏れる問題を防いだ
- **仕様定点観測を更新**: [タイマー_設計意図.md](app/docs/タイマー_設計意図.md) と `spec-snapshot-v5.3-20260420-timer/` を追加し、タイマー設計意図・スナップショット・照合レポートを現在実装に合わせた
- **全体チェックを実施**: `app/` 配下のスクリプト351ファイルを読み込み監査し、Python 106ファイルとJS/MJS 148ファイルの構文確認、ユニットテスト333件、Chrome UI E2E一式が通る状態にした

## v0.5.301 (2026-04-20)

### feat(timer): カレンダー連動と実行リストを追加

- **カレンダーイベント連動を追加**: [gb-tool-timer-advanced.js](app/gb-tool-timer-advanced.js) を追加し、タイマーパネルから対象カレンダーを指定すると、開始時刻になったイベントの終了時刻まで自動でカウントダウンを開始できるようにした
- **アラームとカウントダウン通知を拡張**: タイマーパネル内でアラーム音、音量、10分刻み / 5分刻み / ラスト10秒のカウントダウン、音声通知を設定できるようにした
- **保存済みタイマーと実行リストを追加**: 現在のタイマー設定を保存し、保存済みタイマーを実行リストへ追加、D&Dで並び替え、順番に実行、単体リピート / リスト全体リピートできるようにした
- **タイマー拡張UIを組み込み**: [Meldex.html](app/Meldex.html) でタイマー拡張スクリプトを読み込み、[gb-tools.part02.css](app/gb-tools.part02.css) に保存済みタイマー / 実行リスト / カレンダー連動のパネルUIを追加した
- **徹底チェックで実行中切替の不整合を修正**: 実行中にプリセット開始、リストの次項目、カレンダーイベント開始へ切り替えたとき既存 interval が残らないようにし、実行リスト削除時の現在位置補正とプリセットIDの永続化も修正した
- **回帰テストと監査対象を更新**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) にタイマー拡張の読み込み順・主要機能・実行中切替ガードを追加し、[app/AGENTS.md](app/AGENTS.md) の全ファイル全行チェック対象へタイマー関連ファイルを追加した

## v0.5.300 (2026-04-20)

### feat(calendar): タイマーパネルと外部カレンダー同期を拡張

- **タイマーをペイン内ツール化**: [gb-tool-timer.js](app/gb-tool-timer.js) を追加し、カレンダーツールバー / 設定メニューのタイマーボタンから Chrome の別タブではなく Meldex のペインタブとして開くようにした。既にタイマーパネルが開いている場合は既存タブをアクティブ化する
- **カレンダーパネルで注釈を使用可能に**: [gb-annotations.js](app/gb-annotations.js) / [gb-pane-bridge.part02.js](app/gb-pane-bridge.part02.js) でカレンダーを注釈対象に追加し、カレンダー内のコンテンツ / サイドバーに対して注釈UIをマウントできるようにした
- **同期ダイアログを拡張**: [gb-tool-calendar-sync.js](app/gb-tool-calendar-sync.js) を追加し、Google Calendar に加えて Microsoft Calendar のデバイスコード認証、Microsoftからの取得 / 送信、iCal URL取得、.icsインポート / エクスポートを同じ同期ダイアログから扱えるようにした
- **同期APIを追加**: [meldex_api_calendar_sync.part01.py](app/meldex_api_calendar_sync.part01.py) / [meldex_api_calendar_sync.part02.py](app/meldex_api_calendar_sync.part02.py) に Microsoft Graph 同期と iCal URL取得APIを追加し、Google / Microsoft / iCal の外部IDが混ざらないよう `calendar_source` ごとに更新対象を分離した
- **徹底チェックで同期の不整合を修正**: タイマーのリサイズ監視解除漏れ、Microsoft認証ポーリングの多重化、Microsoftリフレッシュトークン保持、iCalプロパティパラメータ付き行の取り込み、同期件数の更新カウントを修正した
- **回帰テストを補強**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) / [test_meldex_router_modules.py](app/tests/test_meldex_router_modules.py) / [test_meldex_api_smoke.py](app/tests/test_meldex_api_smoke.py) などに、タイマーパネル化、同期スクリプト読込、Microsoft / iCal ルート、カレンダー注釈対象、全テスト実行時のテスト順依存を検証するガードを追加した

## v0.5.299 (2026-04-20)

### perf(panel): パネルタブ切替の全体再描画を削減

- **タブ切替を単一ペイン更新へ高速化**: [gb-tabs.js](app/gb-tabs.js) で、既にDOMがあるペイン内のタブ切替時は `GBLayout.render()` による全レイアウト再描画を避け、タブ見出しと対象ペインだけを更新する高速経路を追加した
- **ペイン再同期APIを追加**: [gb-pane-bridge.part03.js](app/gb-pane-bridge.part03.js) に `refreshPaneAfterTabSwitch()` を追加し、タブ切替後のコンポーネント状態保存、対象ペイン再マウント、注釈UI・詳細/ビューワー・ツールボタン同期を必要範囲だけで行うようにした
- **フォールバック条件を保持**: 折り畳みペインの展開、モバイルレイアウト、DOM未マウント時は従来どおり全体再描画へ戻し、レイアウト構造変更を伴う追加・移動・削除は既存処理を維持した
- **回帰テスト追加**: [test_meldex_pane_tab_performance.py](app/tests/test_meldex_pane_tab_performance.py) を追加し、通常タブ切替、折り畳み時フォールバック、`preserveActivePane`、既存アクティブタブの別ペインフォーカスで全体再描画が走らないことを検証するようにした

## v0.5.298 (2026-04-20)

### feat(toolbar): 各アプリの検索置換とオプション導線を統一

- **ツールバー右端の検索置換 / オプションボタンを整備**: [Meldex.html](app/Meldex.html) / [gb-board-presets.js](app/gb-board-presets.js) で、ノート、フォルダ、シート、ボードのツールバー右端に検索と置換ボタンを配置し、オプションボタンが無いツールバーには `panelRight` アイコンのオプションボタンを追加した
- **検索置換の起動経路をアプリ別に接続**: [gb-outliner-search.part02.js](app/gb-outliner-search.part02.js) でツールバー用の検索置換ヘルパーを追加し、ノートはファイル内検索置換、フォルダ / シートは対象パスにスコープした全文検索置換、ボードはボード内検索置換バーを開くようにした。通常検索を開いたときは前回のツールバースコープをクリアし、置換後のシート再読込にも対応した
- **シナリオツールバーのアイコンを統一**: [gb-tool-scriptnote.js](app/gb-tool-scriptnote.js) / [meldex-core.part01.js](app/meldex-core.part01.js) で、「まとめて表示」を行表示に近い `rows3` アイコンへ変更し、テンプレート登録 / フィルタ登録など登録系ボタンを `save` アイコンへ統一した
- **回帰テスト追加**: [test_meldex_toolbar_regressions.py](app/tests/test_meldex_toolbar_regressions.py) を追加し、ツールバー上の検索置換 / オプション配置、ボードの操作ハンドラ、シナリオ登録系アイコン、検索スコープのクリアを静的に検証するようにした

## v0.5.297 (2026-04-20)

### feat(theme): 設定ダイアログから各アプリ固有テーマを編集可能に

- **選択中テーマ設定をアプリ別タブ化**: [gb-settings.part04.js](app/gb-settings.part04.js) で、設定ダイアログのテーマタブ「選択中テーマの設定」を `共通 / フォルダ / ノート / シナリオ / シート / ボード` のタブ切替にし、各アプリ固有のテーマ変数を同じ場所で編集できるようにした
- **アプリ固有の書式項目を追加**: [gb-settings.part02.js](app/gb-settings.part02.js) に、フォルダカード、ノート本文/見出し、シナリオ基本テキスト、シートセル、ボード背景/選択/カーソルなどのテーマ項目を追加し、シナリオ基本テキストのフォントサイズも書式ポップアップから変更できるようにした
- **書式ポップアップの誤接続を修正**: [gb-format-style-ui.js](app/gb-format-style-ui.js) で、同名ラベルの設定行を行IDで識別し、選択文字色・カーソル太さ・行間などを正しいコントロールへ分類するようにした
- **各アプリのテーマタブ項目を整理**: [gb-detail-panel.part01.js](app/gb-detail-panel.part01.js) でフォルダのテーマタブをカード/枠線/ホバー/選択/メタ情報/アイコン/パネル背景の行に整理し、ノートとシートの編集操作項目も選択・カーソルなど個別行で表示するようにした
- **テーマ未設定時の初期値を補完**: [gb-theme-manager.js](app/gb-theme-manager.js) で、フォルダ、シナリオ、ボード、ノート/シート編集操作のCSS変数に派生デフォルトを追加し、テーマ未設定でもプレビューと実表示が安定するようにした
- **回帰テスト追加**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) に、アプリ別テーマタブ、書式ポップアップID、フォントサイズ対応、ファイルテーマ側の行構成と分類条件を確認する静的ガードを追加した

### fix(theme): 各アプリのテーマ項目を実表示へ接続

- **ノート系テーマの反映漏れを修正**: [gb-note-enhance.part01.js](app/gb-note-enhance.part01.js) / [gb-note-enhance.part02.js](app/gb-note-enhance.part02.js) で、ファイルテーマの `__themeId` と個別CSS変数を共通展開し、ノートタイトル / 本文 / 見出し / DB表示へ即時反映するようにした
- **右パネルとボード内ノートへ適用範囲を拡張**: [gb-detail-panel.part02.js](app/gb-detail-panel.part02.js) で、リンクカードやエントリ自由記述、ボード内ノートを右パネルで開いた場合も、ノート本文・タイトルのテーマ設定を適用するようにした
- **既存テーマ項目の接続を補強**: [gb-tools.part01.css](app/gb-tools.part01.css) / [gb-tools.part02.css](app/gb-tools.part02.css) / [gb-tools.part03.css](app/gb-tools.part03.css) で、DB選択色、エントリ列、ノート本文、見出し、区切り線、詳細パネル本文、ボード内ノート本文が対応するテーマ変数を参照するようにした
- **回帰テスト追加**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) に、テーマタブ項目が実際のCSS変数・右パネル適用経路へ接続され続けることを確認する静的ガードを追加した

## v0.5.296 (2026-04-20)

### refactor(settings): アプリ固有設定を各アプリ側UIへ整理

- **設定ダイアログを共通設定中心に整理**: [gb-settings.part01.js](app/gb-settings.part01.js) から、ノートフォント / ノート既定縦書き / カレンダー開始曜日 / シナリオ用ホイール速度 / チャット既定プロバイダ・モデルなど、各アプリ固有の項目を削除した
- **保存経路の混線を解消**: [gb-settings.part02.js](app/gb-settings.part02.js) / [gb-settings.part03.js](app/gb-settings.part03.js) から削除済み項目の保存処理を外し、設定ダイアログ保存時にノート・カレンダー・チャット・シナリオ設定を意図せず上書きしないようにした
- **シナリオのホイール速度を表示タブへ移設**: [gb-scriptnote-detail.part02.js](app/gb-scriptnote-detail.part02.js) のシナリオオプションパネル「表示」タブへホイール速度スライダーを追加し、既存の `meldex-wheel-speed` 読み取り経路と接続した
- **復元性と仕様メモを補強**: [gb-settings.part03.js](app/gb-settings.part03.js) のUI設定バックアップ対象に `meldex-wheel-speed` を追加し、[設定ダイアログ_設計意図.md](app/docs/設定ダイアログ_設計意図.md) を「共通設定は設定ダイアログ、アプリ固有設定は各アプリ側UI」の現行方針へ更新した
- **回帰テスト追加**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) に、設定ダイアログへアプリ固有コントロールが戻らないことと、移設先UIが維持されることの静的ガードを追加した

## v0.5.295 (2026-04-19)

### fix(theme/board): テーマカラー適用の重さとボード誤保存を修正

- **テーマカラー適用の過剰再実行を抑止**: [gb-theme-manager.js](app/gb-theme-manager.js) でテーマカラー適用先のDOM監視を `document.body` の関連UI追加・削除に限定し、再バインドを `requestAnimationFrame` で集約。ボード描画DOMの高頻度更新やCSSが変わらない `style` 更新で全体操作が重くなる経路を止めた
- **ボード保存先を形式検証**: [gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) / [gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) / [gb-canvas-engine.part03.js](app/gb-canvas-engine.part03.js) で、`.md` かつ `type: board` として正常に開いたファイルだけへボードMarkdownを保存するようにし、シナリオJSONや通常ノートへの混入を防止した
- **終了時・切替時保存にもガードを適用**: [gb-app.part03.js](app/gb-app.part03.js) / [gb-pane-bridge.part02.js](app/gb-pane-bridge.part02.js) の `beforeunload` / パネル切替保存経路でも、ボードとして開いた実体以外へ `bdToMd()` を書き込まないようにした
- **回帰テスト追加**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) にテーマカラー再バインドの集約・フィルタリングと、ボード保存先ガードの静的チェックを追加した

### feat(board): ボードフォント設定を追加

- **ボード標準フォントを追加**: [gb-detail-panel.part01.js](app/gb-detail-panel.part01.js) のボード「スタイル」タブに「ボード標準フォント」セレクトとプレビューを追加し、`style:` の `--bd-default-font-family` として保存 / 復元できるようにした
- **カード / ライン / 階層別スタイルへ対応**: [gb-board-style-manager.js](app/gb-board-style-manager.js) / [gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) でカードスタイル、ラインスタイル、階層別カード / ラインスタイルに `fontFamily` を追加し、保存・複製・デフォルト保存・Undo / Redo で保持されるようにした
- **CSS変数方式で反映**: [gb-board-background.js](app/gb-board-background.js) / [gb-board-render-node.js](app/gb-board-render-node.js) / [gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) / [gb-tools.part03.css](app/gb-tools.part03.css) で、ファイル標準フォントはボード単位の `--bd-default-font-family`、スタイル別フォントは `#bd-style-fontmap` のCSSルールで反映し、カード数に比例したDOM書き換えを避けるようにした
- **ボード以外への波及を抑止**: [gb-note-enhance.part01.js](app/gb-note-enhance.part01.js) のファイルスタイル適用経路では `bd-canvas` / `state.view === 'board'` の場合だけボードフォント変数を同期するようにした
- **回帰テスト追加**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) にボードフォント設定、CSS変数反映、スタイルマップ、階層別スタイル、ノート / シート不変の静的ガードを追加した

### fix(theme): パレット調整と組み込みテーマ編集を改善

- **標準色調整ポップアップを画面内に収める**: [gb-color-palette.js](app/gb-color-palette.js) の共通カラーパレットで標準色調整を展開した際に、ポップアップとスライダー数値が領域外へはみ出さないよう再クランプとレイアウトを調整した
- **組み込みテーマを編集可能に変更**: [gb-settings.part04.js](app/gb-settings.part04.js) で組み込みテーマの値を編集できるようにしつつ、組み込みテーマIDへの上書き保存は禁止し、カスタムテーマとして保存する経路に限定した
- **回帰テスト追加**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) に標準色調整ポップアップと組み込みテーマ保存制御のガードを追加した

### feat(annotation): 付箋注釈とツールバー操作を拡張

- **注釈ツールバーを整理**: [gb-annotations.js](app/gb-annotations.js) の注釈ツールバーから閉じるボタンを削除し、ペン / マーカー / 消しゴムごとの太さスライダーを追加した
- **ボード操作との併用に対応**: ボード上で注釈ツール使用中もズーム / 回転 / パンが使えるよう、ボード操作側の注釈モード抑止を見直した
- **付箋のユーザー表示を改善**: 設定済みユーザー名を `anonymous` より優先して表示し、付箋ヘッダーへユーザーアイコンを追加した
- **付箋本文の書式を追加**: 太字 / 色 / 下線 / 取り消し線の操作を付箋内ツールバーに追加し、本文をリッチテキスト編集できるようにした
- **付箋の見た目とサイズ操作を改善**: 付箋色に合わせたスクロールバー色と、辺 / 四隅ドラッグによるリサイズに対応した
- **付箋のしっぽ機能を追加**: [gb-annotation-tails.js](app/gb-annotation-tails.js) を追加し、Alt+ドラッグで付箋から対象要素へ三角形のしっぽを作成、始点 / 終点ハンドルをドラッグ調整できるようにした
- **しっぽの追従と削除時の履歴保持に対応**: しっぽ終点のカードや行が移動した場合は付箋が追従し、対象要素が削除された場合はしっぽだけを外す。アプリ上で付箋を削除しても注釈パネルには削除済み履歴として残す
- **注釈パネル削除権限を制限**: [gb-right-panel.js](app/gb-right-panel.js) / [gb-annotations.js](app/gb-annotations.js) の注釈削除UIを、ソースフォルダの管理者だけが実行できるようにした

### fix(theme/app): アプリ切替とテーマ設定の混線を修正

- **アプリ切替時の表示混線を抑止**: アプリバーやフォルダツリーから別アプリへ切り替えた際、作業パネル / ビューワー / オプションに直前アプリの内容が残らないよう、各ペインのリセットと再同期経路を整理した
- **インポート / エクスポートアイコンを統一**: テーマJSONのインポート / エクスポートアイコンを入れ替え、共通UIルールとして各インポート / エクスポート操作へ同じ向きを適用した
- **旧テーマ復元操作を撤去**: 旧ボードテーマ色を復元する操作を削除し、現行テーマシステムの設定導線へ統一した
- **テーマカラー適用先を拡張**: アプリバーのボタン、パネルタブ、パネル内タブ、折りたたみバーのボタン、フォルダツリー、フォルダ、パネル内セクション左バーについて、通常 / ホバー / 選択時のアイコン＆テキスト、背景色、タブ下線へのテーマカラー適用を設定可能にした

### fix(board): リンクカード表示とファイルオープン中の状態表示を改善

- **重いファイルのオープン中表示を追加**: ファイルを開いている間、処理中であることが分かるローディング表示を出すようにした
- **シナリオリンクカードを修正**: ボード内のシナリオファイルリンクカードでシナリオアプリのアイコンを使い、ツールチップにはソースではなくシナリオ冒頭を表示するようにした
- **リンクカードのレイアウトを調整**: アプリアイコンがファイル名へ重ならないよう、アイコン領域とファイル名開始位置を見直した
- **右パネルで開く操作を追加**: リンクカードのメニューへ「右のパネルで開く」を追加し、右パネル表示中はカードクリックだけで対応アプリの右パネルへリンク先を開くようにした

## v0.5.294 (2026-04-19)

### perf(board): ボードカード追加後の全体描画復元を撤廃

カード数が増えたボードで `Shift+Enter` / `Ctrl+Enter` / `Tab` などの連続追加後、数秒後に全カードDOM・全ライン・補助UIをまとめて同期して停止する問題に対し、通常操作を差分更新基盤へ移行した。

- **カードDOM生成を単体化**: [gb-board-render-node.js](app/gb-board-render-node.js) を追加し、`bdRender()` 内のカード1枚生成処理を `bdRenderNode()` / `bdReplaceNodeElement()` / `bdUpdateNodePosition()` として分離した。HUD、アンカー、リンクバッジ、自動リンク、画像、複雑形状、コンテナ内カード、最小化カードを単体描画でも生成する
- **dirty更新基盤を追加**: [gb-board-dirty.js](app/gb-board-dirty.js) を追加し、カード・移動カード・接続線・選択UI・コメントHUD・ミニマップ・ボードUIを dirty set と `bdFlushBoardUpdates()` で差分同期するようにした
- **軽量DOMから通常DOMへの全体復元を撤廃**: [gb-board-performance.js](app/gb-board-performance.js) の `bdAppendFastNode()` を通常カードDOMの単体生成ラッパーに変更し、追加後に `bdFlushFullBoardRender()` / 全体 `bdRender()` へ戻すホットパスを外した
- **追加・移動・編集経路を差分化**: [gb-board-ui.part01.js](app/gb-board-ui.part01.js) / [gb-canvas-interact.js](app/gb-canvas-interact.js) / [gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) の子カード追加、同階層追加、アンカー追加、D&D追加、編集確定、ドラッグ、リサイズをカード単位・接続線単位の更新へ移行した
- **ライン部分再描画を補強**: [gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) で `bdDrawConns({ connIds })` / `bdDrawConns({ nodeIds })` を選択中ラインと Free Bezier 編集UIまで安全に再描画できるようにし、選択ハンドルやラベルの残骸が残らないよう `connId` を同期した
- **補助UI更新を差分化**: [gb-comment-badges.js](app/gb-comment-badges.js) のコメントHUDを対象カード更新に対応させ、[gb-canvas-minimap.js](app/gb-canvas-minimap.js) / [gb-board-ui.part02.js](app/gb-board-ui.part02.js) / [gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) に性能計測を追加した
- **読み込み順と監査対象を更新**: [Meldex.html](app/Meldex.html) に `gb-board-render-node.js` / `gb-board-dirty.js` を追加し、[AGENTS.md](app/AGENTS.md) / [CLAUDE.md](app/CLAUDE.md) / [GEMINI.md](app/GEMINI.md) の全ファイル全行チェック対象にも追加した
- **回帰テスト追加**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) に差分描画基盤とライン選択ハンドルの静的ガードを追加した

## v0.5.293 (2026-04-19)

### feat(board): 背景画像設定と同階層カード追加位置を改善

- **Shift+Enter の同階層追加位置を変更**: [gb-board-ui.part01.js](app/gb-board-ui.part01.js) `bdAddSiblingToSelected()` が同じ親の末尾ではなく、選択中カードの直後へカードを挿入するようにした。見た目の追加位置も選択中カードの直下 / 右隣を基準にする
- **ボード背景画像を追加**: [gb-board-background.js](app/gb-board-background.js) を追加し、背景画像の選択 / クリア / 表示方法（全体・余白なし・原寸・タイル）を扱う専用モジュールとして分離した
- **背景画像UIを追加**: [gb-board-presets.js](app/gb-board-presets.js) の下端バーに背景画像ボタンと背景クリアボタンを追加し、[gb-board-ui.part02.js](app/gb-board-ui.part02.js) のボード詳細パネルに背景画像設定欄を追加した
- **背景画像の保存・復元に対応**: ボードの `style:` に `--bd-bg-image` / `--bd-bg-image-fit` を保存し、[gb-canvas-engine.part03.js](app/gb-canvas-engine.part03.js) のボード読込 / スナップショット復元 / 状態復元、および [gb-tool-canvas.js](app/gb-tool-canvas.js) / [gb-theme-manager.js](app/gb-theme-manager.js) の背景反映経路で復元するようにした
- **回帰テスト追加**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) に同階層カードの挿入位置、背景画像の保存・復元・UI接続の静的ガードを追加した

### fix(config): テスト用設定ファイルを実設定から分離

- **設定ファイルの上書き防止**: [Meldex.part01.py](app/Meldex.part01.py) に `MELDEX_CONFIG_FILE` による設定ファイルパス override を追加し、テストや検証時に実運用の `editor-config.json` を書き換えないようにした
- **テスト側の隔離を追加**: [test_support.py](app/tests/test_support.py) で専用の一時設定ファイルを指定し、[test_meldex_batch33_regressions.py](app/tests/test_meldex_batch33_regressions.py) にガードを追加した

## v0.5.292 (2026-04-19)

### feat(theme): テーマ内統一カラーセットを追加

- **テーマカラーを独立管理**: [gb-theme-manager.js](app/gb-theme-manager.js) に `ui.colorSet` を追加し、アプリバー / タブ / ボード配色用のカラーセットを、アクセント色や各種UI要素色から独立して保持できるようにした
- **設定UIを追加**: [gb-settings.part02.js](app/gb-settings.part02.js) のカスタムテーマ「共通」タブに「テーマカラー」編集行を追加し、8色の統一カラーセットを個別に編集・リセットできるようにした
- **テーマ保存に対応**: 統一カラーセットを `meldex-theme-color-set` / `_theme-color-set` として保存し、テーマ保存 / 読込 / エクスポート / インポート / カスタムテーマ保存に含めるようにした
- **ボード配色へ適用**: [gb-theme-coloring-rule.js](app/gb-theme-coloring-rule.js) と [gb-canvas-features.part00.js](app/gb-canvas-features.part00.js) / [gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) のレインボーブランチ / 階層別既定スタイルが、テーマ内統一カラーセットを参照するようにした
- **回帰テスト追加**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) にテーマカラーセットの保存・同期・適用経路の静的ガードを追加した

## v0.5.291 (2026-04-19)

### feat(theme): 標準カラーパレットの一括調整を追加

- **標準スウォッチ全体を調整**: [gb-color-palette.js](app/gb-color-palette.js) の 8×4 標準カラースウォッチに、色相 / 彩度 / 明度をまとめて変更する調整UIを追加した
- **明度コントラスト調整を追加**: 標準スウォッチ専用の「コントラスト」スライダーを追加し、4行全体の明度差をまとめて広げる / 詰める操作に対応した
- **テーマ保存に対応**: 標準パレット調整値を `meldex-standard-palette-adjust` として保持し、エディタテーマ保存 / 読込 / エクスポート / インポート / カスタムテーマ保存に含めるようにした
- **設定同期対象に追加**: [gb-settings.part03.js](app/gb-settings.part03.js) のUI設定バックアップ対象に標準パレット調整値を追加した
- **回帰テスト追加**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) に調整値の正規化・保存・色変換・テーマ保存ガードを追加した

### fix(board): ズームバーと保存ボタン文言を調整

- **下端ズームバーを並び替え**: [gb-board-presets.js](app/gb-board-presets.js) の下端バーで縮小ボタンを拡大ボタンの左へ移動し、拡大ボタンの隣に 100% ボタンを追加した
- **100%ボタンを追加**: [gb-board-ui.part01.js](app/gb-board-ui.part01.js) に `zoom-100` アクションを追加し、Lucide の四角アイコンで倍率を 100% に戻せるようにした
- **保存ボタン文言を統一**: [gb-board-ui.part02.js](app/gb-board-ui.part02.js) / [gb-board-style-manager.js](app/gb-board-style-manager.js) のスタイル保存系 tooltip と完了ステータスを「デフォルトとして保存」に統一した
- **回帰テスト追加**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) にズームバー順序、100%ボタン、保存文言の静的ガードを追加した

## v0.5.290 (2026-04-19)

### feat(settings): ショートカット設定をアプリ別に整理

- **ショートカットタブを強化**: [gb-shortcuts.js](app/gb-shortcuts.js) の設定UIにアプリ別フィルタを追加し、全体 / ノート / シート / ボード / カレンダー / CSV / フォルダ / パネルセットごとに一覧できるようにした
- **カスタム状態を明示**: ショートカット行に「既定 / カスタム」を表示し、既存のクリック変更・個別リセット・全リセットの保存形式は維持した
- **検索表示を安定化**: 検索・アプリ絞り込み時は `hidden` 属性で表示制御し、行の flex レイアウトが崩れないようにした
- **CSS分割上限を維持**: [gb-tools.part04.css](app/gb-tools.part04.css) を追加して `gb-tools.part03.css` を 1000 行未満へ戻し、分割資産テストの上限を満たすようにした
- **回帰テスト追加**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) にショートカットタブ、アプリ別フィルタ、カスタム保存導線の静的ガードを追加した

### fix(board): 注釈機能を CanvasComponent 経路へ復旧

- **旧 board-view 経路を撤去**: 空の `#board-view` と `openBoard()` 内の旧キャンバス生成/D&D経路を削除し、ボードDOM生成を [gb-tool-canvas.js](app/gb-tool-canvas.js) の `CanvasComponent` に一本化した
- **ボードDOMに role 属性を付与**: [gb-board-presets.js](app/gb-board-presets.js) の `bdBuildBoardShellMarkup()` に `data-bd-role` を追加し、注釈 bridge / インタラクション初期化がアクティブなキャンバスを取得できるようにした
- **注釈 bridge 復旧**: [gb-annotations.js](app/gb-annotations.js) と [meldex-core.part01.js](app/meldex-core.part01.js) で `bd-canvas` / `bd-world` の固定ID前提を緩和し、CanvasComponent 内の embedded overlay に `ann-set-state` / `ann-load` が届くようにした
- **注釈モード中の通常操作を抑止**: [gb-canvas-interact.js](app/gb-canvas-interact.js) に `bdIsAnnotationModeActive()` を追加し、注釈ツールバーON時の pointer / wheel / keyboard 操作をボード側で処理しないようにした
- **回帰テスト追加**: [test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) に board-view 撤去、CanvasComponent 初期化、注釈 bridge 参照の静的ガードを追加した

### feat(theme): テーマシステム統合 Phase 1-6

- **統合テーマ基盤を追加**: [gb-theme-manager.js](app/gb-theme-manager.js) / [gb-theme-coloring-rule.js](app/gb-theme-coloring-rule.js) / [gb-theme-migration.js](app/gb-theme-migration.js) を追加し、アプリUIテーマ・ボードテーマ・親子/階層配色ルールを同一テーマ定義で扱うようにした
- **組み込みテーマ10種を定義**: ダーク / ライト / ボード系6種 / レインボー / グレースケールを組み込みテーマとして登録し、ボード右クリックメニューのテーマ選択から適用可能にした
- **旧ボードテーマAPIを撤去**: `BD_THEMES` / `bdApplyTheme` / `BD_RAINBOW` / `bdApplyRainbow` / `_bdGrayscale` / `bdToggleGrayscale` を廃止し、既存のグレースケール状態はテーマIDへ移行する
- **ボードテーマ永続化**: frontmatter `boardUi.themeId` と Undo/Redo / 複数ボード状態 dump/load にテーマIDを保存・復元する
- **UIパレット順適用**: アプリバー、ペインタブ、詳細タブ、パネルセットタブへテーマパレットの順番を data 属性で適用する初期エンジンを追加
- **カスタムテーマの保存/削除/インポート/エクスポートを拡張**: カスタムテーマJSONを `_version: 3` に更新し、カスタムテーマ一覧と既定テーマIDも保存対象にした
- **ダークグレーを無彩色化**: 既定ダークテーマの `#252526` / `#2d2d30` / `#3e3e42` / `#333338` 系を中立グレーに置き換え、青みを排除した

## v0.5.289 (2026-04-19)

### perf / fix(board): v0.5.288 軽量化の追従調整

v0.5.288 のカード追加軽量化に対する継続調整。選択中カードのアンカーDOMを復活させて Hover 操作と整合させ、追加経路を高速描画パスへ揃えた。

- **選択中カードのアンカーDOMを復活**: [gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) `bdRender` の anchorPositions 判定から `bd.selected.has(n.id)` を外し、選択カードでも anchorDOM を生成する。CSS 側 (`gb-tools.part03.css`) で表示制御を担当
- **fastCardRender 経路の anchor 補完**: [gb-board-performance.js](app/gb-board-performance.js) に `bdAppendFastNodeAnchors` を新設し、軽量描画で生成したカードDOMにも 4 方向 anchor を後付けする。クリックは `pointerdown` + `pointermove` + `pointerup` のドラッグ判定で発火
- **追加遅延とレイアウト遅延を分離**: `BD_FAST_AUTO_LAYOUT_DELAY_MS` / `BD_FAST_SIBLING_AUTO_LAYOUT_DELAY_MS` / `BD_FAST_FULL_RENDER_DELAY_MS` を導入し、`bdScheduleAutoLayouts` に `delayMs` を渡して同階層追加と子追加で別ディレイにできるようにした
- **bdSelect の DOM 走査を差分化**: [gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) `bdSelect` で `deferExtras` 中は変更があったノードだけ `.bd-selected` トグルを行い、全 `.bd-node` 走査を回避
- **bdAddAt も fast-mutation 区間に**: [gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) `bdAddAt` を `bdBeginFastBoardMutation` / `bdEndFastBoardMutation` で囲み、追加経路を統一
- **方向判定の構造優先**: [gb-board-ui.part01.js](app/gb-board-ui.part01.js) `bdAddChildToSelected` / `bdAddSiblingToSelected` でルート構造 (mindmap / flowchart 系) を直接見て方向決定する高速パスを追加。子なし root の `bdTreeDirection` 呼び出しを節約
- **同階層追加の lastSib 検索を逆順走査に**: 末尾の兄弟を `bd.nodes` 末尾から逆順 1 回スキャンで取得し、`bdChildren` の全件フィルタを回避
- **重複ライン生成チェックを撤廃**: 子追加直後は同一接続が存在しないことが保証できるため `some(c => c.from === ... && c.to === ...)` チェックを削除
- **回帰テスト追加**: [tests/test_meldex_frontend_regressions.py](app/tests/test_meldex_frontend_regressions.py) に `test_board_selected_cards_keep_anchor_dom_for_hover_after_deselect` を追加

## v0.5.288 (2026-04-19)

### perf(board): カード連続追加時の体感遅延を軽減

カード数が増えたボードで `Ctrl+Enter` / `Tab` / `Shift+Enter` によるカード追加が一瞬止まって見える問題に対し、追加直後の同期処理を削減。

- **連続追加のUndoを集約**: [gb-board-performance.js](app/gb-board-performance.js) を追加し、短時間の子カード追加 / 同階層カード追加ではUndoスナップショットを連続追加単位にまとめるようにした。`localStorage.boardPerfDebug === "1"` のときだけ主要処理時間をコンソール出力
- **カード追加時の全体再描画を回避**: [gb-board-ui.part01.js](app/gb-board-ui.part01.js) `bdAddChildToSelected()` / `bdAddSiblingToSelected()` で、追加直後は `bdRender()` ではなく `bdAppendFastNode()` により新規カード1枚だけを軽量DOMとして追加。停止後の通常描画でHUD・アンカー・リンク表示を復元
- **付帯更新を遅延集約**: [gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) / [gb-board-performance.js](app/gb-board-performance.js) で、ライン再描画・選択枠全体同期・ミニマップ・詳細パネル・コメントHUD更新を連続追加中は後続に回すようにした
- **選択ハイライトだけ即時同期**: 新規カードの `.bd-selection-rect` は `bdSyncSelectionRectForNode()` で1枚分だけ即時配置し、カード本体と選択状態の表示遅延を軽減
- **自動整列を遅延し、内部走査をMap化**: [gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) `bdAutoLayout()` は連続追加停止後にまとめて実行し、レイアウト単位の `Map` / 親子Mapで `bd.nodes.find()` / `bdChildren()` の反復を削減
- **選択中カードの `+` アンカーを抑制**: [gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) / [gb-tools.part03.css](app/gb-tools.part03.css) で、選択中カードには上下左右アンカーDOMを作らず、既存アンカーもCSSで非表示にした
- **監査バッチ定義を更新**: [AGENTS.md](app/AGENTS.md) / [CLAUDE.md](app/CLAUDE.md) / [GEMINI.md](app/GEMINI.md) バッチ14に `gb-board-performance.js` を追加
- **計画書を追加**: [board-performance-lightweight-plan-2026-04-19.md](app/docs/board-performance-lightweight-plan-2026-04-19.md) に原因分析・実施済み範囲・残件を記録

## v0.5.287 (2026-04-19)

### refactor(board): 検索・置換バーを gb-board-find.js に分離

v0.5.286 の機能追加で [gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) が 1707 行 (グローバル品質ルールの 1500 行上限を逸脱) になっていたため、同ファイルから検索・置換バーの実装一式 (`bdOpenFindBar` / `bdCloseFindBar` / `_bdCreateFindBar` / `_bdFindUpdateMatches` / `_bdFindShowCurrent` / `_bdFindScrollToCurrent` / `_bdApplyFindHighlight` / `_bdRenderTextWithHighlight` / `_bdFindPrev` / `_bdFindNext` / `_bdFindUpdateUI` / `_bdFindReplaceOne` / `_bdFindReplaceAll` / `bdFindReplace`) を独立モジュール [gb-board-find.js](app/gb-board-find.js) に移動。

- [app/Meldex.html](app/Meldex.html) `gb-canvas-features.js` の直後に `gb-board-find.js` を読み込む
- [app/CLAUDE.md](app/CLAUDE.md) (AGENTS.md) バッチ14に `gb-board-find.js` を追加
- 機能挙動は変更なし。関数はすべてグローバル名前空間に維持しているため呼び出し側 (`gb-shortcuts.js` / `gb-canvas-interact.js` / `gb-canvas-features.part02.js` / `gb-editor.part04.js`) の修正は不要
- 結果: [gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) は 1707 行 → 1482 行に縮小

## v0.5.286 (2026-04-19)

### feat(board): カード選択時のショートカットを構造ベースで再設計

カード選択中の Enter / Shift+Enter / Ctrl+Enter / Ctrl+矢印 / Ctrl+Shift+クリック を、ツリーの展開方向に追従する形に再設計。

- **Enter は常にテキスト編集**: 旧仕様 (ルートは編集 / 子は同階層追加に分岐) を廃止して常にテキスト編集を開始する。F2 と等価
  - [gb-shortcuts.js](app/gb-shortcuts.js) `board.editOrSibling` を撤去し、`board.editEnter` (key: enter) を新設
- **Shift+Enter で同階層カード追加**: 親と同じスタイル / 同じ構造を継承して同階層末尾に追加。ルートカードでは追加できない旨をステータス表示
  - [gb-shortcuts.js](app/gb-shortcuts.js) `board.addSibling` (key: shift+enter) を新設
- **Ctrl+Enter で子カード追加**: 親と同じスタイルを継承して子階層に追加。Tab キーは互換のため `board.addChildTab` として残置
  - [gb-shortcuts.js](app/gb-shortcuts.js) `board.addChild` のキーを `tab` → `ctrl+enter` に変更
- **追加カードに親のスタイルを継承**: `bdInheritStyleOpts(srcNode)` を新設し、`cardStyle` (スタイル ID) と個別 override (`bgColor` / `textColor` / `borderColor` / `borderWidth` / `borderRadius` / `fontSize` / `fontBold` / `fontItalic` / `textStrokeColor` / `textStrokeWidth` / `shape` / cloud-* 各キー / 幅) を継承する。状態系 (collapsed / locked 等) や内容系 (text / img / link) は継承しない
  - [gb-board-ui.part01.js](app/gb-board-ui.part01.js) `bdInheritStyleOpts` / `bdAddChildToSelected` / `bdAddSiblingToSelected` を追加。カード追加位置はツリー展開方向 (`bdTreeDirection`) に追従して右 / 下 / 左 / 上 から自動選択
- **Ctrl+矢印で兄弟入替 / 子階層の折りたたみ・展開**: ツリー展開方向に応じて自動でキー割り当てを切り替える
  - 右展開 (mindmap / logic / timeline): Ctrl+↑↓ で兄弟入替、Ctrl+← で折りたたみ、Ctrl+→ で展開
  - 下展開 (flowchart / orgchart): Ctrl+←→ で兄弟入替、Ctrl+↑ で折りたたみ、Ctrl+↓ で展開
  - 左展開 (構造未指定で左に展開): Ctrl+↑↓ で兄弟入替、Ctrl+→ で折りたたみ、Ctrl+← で展開
  - [gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) `bdTreeDirection` / `bdSwapSibling` / `bdSetCollapsed` / `bdHandleCtrlArrow` を追加
  - [gb-shortcuts.js](app/gb-shortcuts.js) `board.ctrlArrowUp` / `Down` / `Left` / `Right` 4 エントリを追加
  - [gb-canvas-interact.js](app/gb-canvas-interact.js) `onCanvasKeydown` の旧 「Ctrl+矢印で 10px 移動」ブロックを削除し、Ctrl+矢印 (Shift / Alt 修飾なし) は preventDefault せず中央ハンドラに委譲
- **Ctrl+Shift+クリックでツリー全体を選択**: クリックしたカードまたはラインから親子関係にあるツリー全体 (ルート + 全子孫) を選択集合に追加する
  - [gb-canvas-interact.js](app/gb-canvas-interact.js) ノードクリック / ライン左クリックの両経路で Ctrl+Shift 修飾を最優先分岐として追加。既存の Ctrl のみ (トグル選択) / Shift のみ (方向選択) は維持
- **編集中の Tab / Ctrl+Enter は確定→子カード追加**: 既存挙動 (Tab) を維持しつつ Ctrl+Enter にも同じ動作を割り当て
  - [gb-canvas-interact.js](app/gb-canvas-interact.js) `bd.editing` 分岐の Tab ハンドラを `bdAddChildToSelected` 直接呼び出しに変更し、合成 `KeyboardEvent('Tab')` 経由のループを廃止
- **コンテキストメニューのラベルとアクションを更新**: 「テキスト編集 (F2 / Enter)」「同階層カード追加 (Shift+Enter)」「子カード追加 (Ctrl+Enter)」に書き換え。合成キーボードイベントの代わりに `bdAddSiblingToSelected` / `bdAddChildToSelected` を直接呼び出す
  - [gb-canvas-features.part02.js](app/gb-canvas-features.part02.js) `bdContextMenu` の該当エントリを更新

## v0.5.285 (2026-04-19)

### refactor(board): ボード設定からフローティングミニマップとフォーカスモードを削除

- **フローティングミニマップを削除**: ビューワーパネル側のミニマップ (`gb-canvas-minimap.js` の `_bdDrawPreviewMinimap`) で同じ目的を達成しているため、フローティング版は不要と判断
  - [gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) `bdToggleMinimap` / `_bdDrawFloatingMinimap` / `_bdMinimapVisible` 変数をすべて削除
  - [gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) `bdRender` 内のフローティングミニマップ更新呼び出しを削除。ビューワーペインの `bdUpdateMinimap` は維持
  - [gb-canvas-features.part02.js](app/gb-canvas-features.part02.js) 「ボード設定」サブメニューからミニマップ項目を削除
- **フォーカスモードを削除（Space キーでのフォーカスは維持）**: Space キーを押すだけで選択中カードにフォーカス / 復元できるため、トグル状態は不要と判断
  - [gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) `_bdFocusMode` 変数と `bdToggleFocusMode` 関数を削除。`_bdFocusSaved` と `bdFocusSelected` は Space キー用に残す
  - [gb-canvas-features.part02.js](app/gb-canvas-features.part02.js) 「ボード設定」サブメニューからフォーカスモード項目を削除
  - [gb-canvas-interact.js](app/gb-canvas-interact.js) カードクリック時のフォーカスモード起動処理 (`if (_bdFocusMode && ...) bdFocusSelected()`) を削除。Space キー (`if (e.key === ' ')`) はそのまま残す
  - [gb-canvas-engine.part03.js](app/gb-canvas-engine.part03.js) `bdDumpState` / `bdLoadState` / `bdOpenBoard` リセットから `_bdFocusMode` を除去。`_bdFocusSaved` のみ保持

## v0.5.284 (2026-04-19)

### feat / fix(board): Ctrl+ドラッグをトグル選択に + ラインの矢印をアンカー方向に固定

- **Ctrl+左ドラッグで矩形選択をトグル（追加 / 削除両対応）**: [gb-canvas-interact.js](app/gb-canvas-interact.js) `onDocPointermove` の矩形選択判定を、ドラッグ開始時に保存した `baseSelection` / `baseSelConnIds` と、現在矩形に入っているカード / ライン集合の XOR (対称差) に変更。矩形に入った「もともと選択中のもの」は外れ、「選択されていなかったもの」は加わる。ノードとラインの両方に適用
- **矢印の向きをアンカー方向に固定（直線以外）**: [gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) `bdDrawConns` の矢印マーカー適用を、pathType が `'straight'` 以外 (curve / orthogonal / orthogonal-curve / free-bezier) のとき `(x1,y1)→(cx1,cy1)` / `(x2,y2)→(cx2,cy2)` のベクトルから `atan2` で orient 角度を計算し、`_bdEnsureArrowMarker` に固定角度を渡すように変更。左右アンカーのラインは横向き、上下アンカーのラインは縦向きの矢印になる。直線は従来通り `orient="auto-start-reverse"` （path 接線方向）で維持
- **`_bdEnsureArrowMarker` に `orientDeg` オプションを追加**: 数値を渡すと固定角度、未指定なら自動 (`auto-start-reverse`)。非直線は start/end で別マーカー (`bd-arrow-start-${id}` / `bd-arrow-end-${id}`) を生成、直線は従来通り単一マーカーを共有

## v0.5.283 (2026-04-19)

### refactor(board): 配置タブの数値入力を UI 共通ルールに統一 + クロップ機能を削除

- **配置タブの数値入力を `gb-fmt-num` に統一**: [gb-board-ui.part02.js](app/gb-board-ui.part02.js) 配置タブの X / Y / 幅 / 高さ の `<input type="number">` に `class="gb-fmt-num"` を付与し、スタイル編集や階層別ダイアログと同じ 70px 固定幅・hover/focus でスピンボックス表示の UI 共通ルールに揃える。[gb-tools.part03.css](app/gb-tools.part03.css) に `.bd-detail-field input.gb-fmt-num { flex: 0 0 auto; width: 70px; }` を追加して、既存の `.bd-detail-field input { flex: 1 }` を打ち消す
- **クロップ機能を削除**: ポップアップメニュー「画像操作」サブの「クロップ...」エントリと、オプションパネル「変形」セクションの「クロップ」テキスト入力を削除
  - [gb-canvas-features.part02.js](app/gb-canvas-features.part02.js) `imgSub.item('クロップ...', () => bdCrop())` 行を削除
  - [gb-board-ui.part02.js](app/gb-board-ui.part02.js) 変形セクションの `<label>...クロップ...</label>` を削除。`_bdUpdateNodeFromField` から `case 'clip'` ハンドラも削除
  - [gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) `bdCrop()` 関数定義を削除
  - [gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) `bdRender` の `n.clip → div.style.clipPath` 適用を削除。`bdFromMd` の `clip:` パース、`bdToMd` の `clip` 書き出しと `hasTransforms` 条件からも `n.clip` を除去。古いファイルに残る `n.clip` は読み込み時に無視され、次回保存で落ちる

## v0.5.282 (2026-04-19)

### fix(board): カード追加直後のインライン編集を全経路で廃止 + 複数選択時の構造一括適用 + 初期高さ 1 行分

過去の指示で一部しか対応できていなかった 3 件を横断的に修正。

- **親子関係を含む複数選択で構造を一括適用**: [gb-canvas-features.part02.js](app/gb-canvas-features.part02.js) `bdContextMenu` の構造サブメニューから `if (!nd.parent)` ガードを削除。構造選択は常に表示し、選択中のすべてのカード (`targetNodeIds`) に対して `n.structure = value` を一括適用。構造の副作用 (欠けた親子接続の補充 + `bdAutoLayout`) は、各ノードのルート (親なし) に対してのみ実行してレイアウトの整合性を保つ
- **追加直後のインライン編集を全経路で廃止**: 以下の全経路から `bdEditNode(newNode.id)` を削除。以降は F2 / カードダブルクリックで明示的に編集モードへ入る
  - [gb-shortcuts.js](app/gb-shortcuts.js) `board.addChild` (Tab キー)
  - [gb-shortcuts.js](app/gb-shortcuts.js) `board.editOrSibling` の同階層追加 (Enter キー)
  - [gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) `_bdAnchorAddCard` (カード HUD の上下左右 + ボタン)
  - [gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) `bdAddSummaryLines` (集約カード作成)
  - [gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) アンカードラッグ → 空白ドロップ (接続 + 新規カード)
  - [gb-canvas-interact.js](app/gb-canvas-interact.js) 接続ドラッグ → 空白ドロップ
  - ダブルクリックで空白に追加する `bdAddAt` は元から編集モードに入らない仕様
- **カード初期高さを 1 行分に統一**: Tab / Enter / 集約カードの 3 経路で、`bdNode('', ..., 160, 0, opts)` から `bdCreateNodeWithStyle('', ..., opts)` に切り替え。`bdCreateNodeWithStyle` は `Math.ceil(fontSize * 1.5) + 16` を標準高さに設定するため、空カードでも 1 行分の高さが確保される。既にこの関数を経由していた HUD + ボタン / アンカードラッグ / 接続ドラッグ / `bdAddAt` は影響なし。画像ドロップなど `h=0` で自然サイズにしたい経路は従来通り

## v0.5.281 (2026-04-19)

### feat / fix(board): スタイル管理ダイアログとラインテキスト周りの改善

- **スタイル管理ダイアログのボタンをアイコンに統一 + 保存系ボタンを追加**: [gb-board-style-manager.js](app/gb-board-style-manager.js) `_bdRenderStyleManager` の編集アクション列を、オプションパネル基本タブと同じ Lucide アイコン (plus / save / copy / rotateCcw / trash2) のアイコンボタン (`bd-detail-style-action`) に変更。削除は `bd-detail-style-action--danger` で hover 時のみ赤く。新規に「現在の設定を新しいスタイルとして保存」(plus)、「選択中スタイルに上書き保存」(save) の 2 ボタンを追加 (`_default` スナップショット更新)
- **スタイルリスト / 編集領域のスクロール位置を保持**: カードスタイル / ラインスタイル管理ダイアログ、階層別スタイルダイアログのいずれでも、リスト項目クリックやフィールド編集での再描画時に `body.innerHTML` 書き換え前に `.bd-style-list-items` / `.bd-style-editor-fields--fmt` / `.bd-depth-fields` の `scrollTop` を保存、書き換え後に復元
- **ラインテキストの太字 / 斜体**: [gb-board-ui.part01.js](app/gb-board-ui.part01.js) `bdGetConnectionStyle` に `fontBold` / `fontItalic` を追加。[gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) の SVG `<text>` (沿線回転) と HTML `<div>` 両経路でラベルに `font-weight: bold` / `font-style: italic` を適用。`_BD_LINE_OVERRIDE_KEYS` / `bdClearConnectionStyleOverrides` / bdToMd / bdFromMd / `bdNormalizeLineStyles` も更新してラウンドトリップ対応
- **テキスト表示 / 沿線回転 / 自動反転をチェックボックス化**: [gb-board-style-manager.js](app/gb-board-style-manager.js) `_bdBuildStyleFields` のライン lrow4 で `fmt.makeToggle` から `fmt.makeCheckbox` に差し替え
- **沿線回転 ON のときテキスト背景色 / 枠線色を無効化**: `textAlongPath === true` のとき lrow3 の `labelBgColor` / `labelBorderColor` スウォッチに `disabled` 属性、`opacity: 0.4`、`cursor: not-allowed` を付与し、title にも「沿線回転 ON のときは使用されません」と表示。SVG textPath では背景・枠線が描画できないため誤設定を抑止
- **ラインスタイル正規化でラベル / テキスト系フィールドを保持**: `bdNormalizeLineStyles` が id/name/color/width/style/arrow/pathType しか保存していなかったため、`labelTextColor`, `labelBgColor`, `labelBorderColor`, `fontBold`, `fontItalic`, `textVisible`, `textAlongPath`, `textAutoFlip`, `textShadowWidth`, `textShadowColor` も保持するように修正 (管理ダイアログ経由の編集が保存時に消えるバグを解消)
- **階層別ダイアログのプレビューにラインを追加**: [gb-board-style-manager.js](app/gb-board-style-manager.js) `_bdDepthStylePreviewHtml` をカード + ラインの横並び組に変更 (`bd-depth-preview-group`)。右カラムの大きなプレビュー領域も `bd-depth-editor-preview` コンテナで上段カード / 下段ラインの 2 段構成に。CSS は [gb-tools.part03.css](app/gb-tools.part03.css) に新規クラス追加
- **文字色 / 文字フチ色のスウォッチは既に共通ルール準拠**: `makeSwatchText({ iconName: 'type' })` / `makeSwatchText({ iconName: 'typeOutline' })` で背景色 + Lucide アイコン表示済みのため実装変更なし

### 徹底チェック修正

- **詳細パネル displayStyle に fontBold / fontItalic を追加**: [gb-board-ui.part02.js](app/gb-board-ui.part02.js) の単一ライン詳細パネル (`_bdBindConnectionDetailPanel`) および複数選択パネル (`_bdBindSelectionDetailPanel`) で effective→displayStyle にコピーするキー一覧に `fontBold` / `fontItalic` を追加 (追加なしでは個別オーバーライドが UI のトグルに反映されなかった)
- **スタイルスナップショット関数に fontBold / fontItalic を追加**: [gb-board-style-manager.js](app/gb-board-style-manager.js) `_bdLineStyleSnapshotFromConnection` にも fontBold / fontItalic を追加。「現在の設定を新しいスタイルとして保存」(save-as-new) したときに bold/italic の効果値が新スタイルに引き継がれるように
- **階層別プレビューの大型ラインプレビューがエディタ列幅を超える問題を修正**: [gb-tools.part03.css](app/gb-tools.part03.css) `.bd-depth-editor-preview-line > svg.bd-style-line-preview-large { max-width: 100%; width: auto; }` で SVG を列幅に合わせて縮める。`.bd-depth-editor-preview` に `overflow: hidden` も追加

## v0.5.280 (2026-04-19)

### fix / feat(board): オプションパネル整理

- **タブ切り替えが基本タブに戻る不具合を修正**: [gb-board-ui.part01.js](app/gb-board-ui.part01.js) `_bdCurrentNodeBoardTab`。`bdRefreshSelectionDetails` → `clearBoardDetailTabs` → `showBoardTabs(false)` → `switchDetailTab(null)` で `_currentDetailTab` が null に戻される経路で、フォールバック条件が `typeof === 'undefined'` だけだったため `_bdNodeDetailTab` の救済が発動せず、必ず基本タブに戻っていた。null / undefined の両方でフォールバックするように修正
- **自動整列をチェックボックスに**: [gb-detail-panel.part01.js](app/gb-detail-panel.part01.js) 自動整列フィールドの type を `toggle` から新設の `checkbox` に変更。`_fsBuildControl` に `<input type="checkbox">` + ラベルの分岐を追加。`_fsBuildFieldInline` でも toggle と同じくラッパーを付けずに直接返す
- **拡張タブから進捗 / チェック / ノートを削除**: 既に機能としては使われていないため、[gb-board-ui.part02.js](app/gb-board-ui.part02.js) の拡張タブ HTML から該当 3 フィールドの UI を除去
- **優先度 / フラグの項目名を日本語化**: [gb-board-ui.part01.js](app/gb-board-ui.part01.js) `_bdMarkerSelectHtml` に `_BD_MARKER_CATEGORY_LABELS` マップを追加し、`priority` → 優先度、`flag` → フラグ に表示
- **配置タブに「カードサイズ」セクションを新設**: [gb-board-ui.part02.js](app/gb-board-ui.part02.js) 配置タブの先頭に「カードサイズ」セクション（幅・高さ）を追加し、既存の「配置」セクションからは幅・高さを除去。X/Y/親カード/構造/フラグ類は「配置」セクションに残す
- **配置タブ / 拡張タブ先頭のカード名見出しを排除**: `<div class="bd-detail-heading">${title}</div>` の表示を配置タブと拡張タブから除去（基本タブのみ表示）
- **スタイルタブの項目名の太字を解除**: [gb-format-popup.css](app/gb-format-popup.css) `.gb-fmt-label--group` から `font-weight: 600` を削除し、色も `var(--fg2)` に揃える。他タブの項目名（`bd-detail-field > span`）と同じ見た目に統一

### 徹底チェック修正

- **checkbox ラベルで rowLabel 重複を短縮**: [gb-detail-panel.part01.js](app/gb-detail-panel.part01.js) `_fsBuildControl` の checkbox 分岐で、ラベル生成に `_fsShortLabel(field, rowLabel)` を使用して toggle 類と挙動を揃える (現在の「整列 / 自動整列」ケースでは見た目は変わらないが、将来的に prefix が重複したフィールドが追加されたとき二重表示されない)

## v0.5.279 (2026-04-19)

### feat(board): ライン一括編集 / 30% 分岐 / 直角曲線 / 階層別スタイル拡張

- **複数選択ラインへの一括フィールド編集**: オプションパネル基本タブの「ライン一括変更」セクションに、色・太さ・形状・矢印・ラベル色・テキスト関連の全フィールドを追加。変更は選択中のすべてのラインに対して個別 override として書き込む。[gb-board-style-manager.js](app/gb-board-style-manager.js) `_bdBuildStyleFields` に `editTargets` オプションを追加し、配列で渡すと全ターゲットに `_bdApplyStyleFieldChange` を適用するように。複数ターゲット時は名前行を非表示にする `hideName` オプションも新設
- **直角線の分岐位置を 30% 比率に変更**: [gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) `_bdBuildConnectionPathData` の直角線分岐を、固定 28px から親→子ベクトルの 30% 相当 `(end - start) * 0.3` に変更。親から子までの距離に応じて分岐位置がスケールする
- **形状に「直角曲線」を追加**: 直角線の角を丸めた新しい pathType `orthogonal-curve` を追加。ポリライン点は直角線と共通、各コーナーを 2 次ベジェ (Q) で丸める。半径は隣接辺の半分以下で自動クランプ。関連するパース / シリアライズ / effective スタイル取得 / プレビュー / ラベル表示 / スナップショットのすべてで `orthogonal-curve` を認識するように更新
- **階層別スタイルダイアログの大幅拡張**: [gb-board-style-manager.js](app/gb-board-style-manager.js) `bdOpenDepthStyleManager` のフィールドセクションを `_bdBuildStyleFields` 経由に差し替え、基本タブのカードスタイル・ラインスタイルと同じ項目 (色 / 文字 / フチ / 枠線 / 形状 / 標準幅 / 雲型パラメータ / ラベル / 沿線回転 etc.) をすべて設定可能に。カード項目 (`bdApplyAutoStyle`) も従来の 4 フィールドから全カードフィールドに拡張
- **階層間のラインも階層別に設定**: 各 depth エントリに `line` サブオブジェクトを持たせ、depth のカードから子カードへ伸びるラインのスタイル (色 / 太さ / 種類 / 矢印 / 形状 / ラベル色 / 沿線回転 etc.) を設定可能に。`bdApplyAutoStyle` で `_autoStyle` ルート配下のラインに対して、親ノードの深さに応じてラインスタイルを自動適用 (`styleRef` を明示設定したラインと `_userLineStyle` フラグ立ちのラインはスキップ)
- **既定値 / 正規化の拡張**: `BD_DEFAULT_DEPTH_STYLES` に全カードフィールド + line サブオブジェクトを追加。`bdNormalizeDepthStyles` を書き直して全フィールドを安全にクランプ / フォールバック。`_bdNormalizeDepthLine` を新設

### 徹底チェック修正

- **階層別カードの新規フィールドを空文字列で上書きしない**: 従来の 4 フィールド (fontSize/fontBold/bgColor/width) は互換性のため常に上書きするが、新規フィールド (textColor, borderColor, borderRadius, borderWidth, fontItalic, textStrokeColor, textStrokeWidth, shape) は depth 側が空文字列 / undefined なら上書きをスキップ。既存のカード個別設定が消えるリグレッションを回避
- **階層間ラインの既定値を「指定なし」に**: `BD_DEFAULT_DEPTH_STYLES.line` を `color=''`, `width=0`, `arrow=''`, `pathType=''` 等にして、値が明示されたフィールドだけツリー内のラインに上書き適用する仕様に変更。これがないと `_autoStyle` を有効化した瞬間にツリー内の全ラインが `width=2, arrow=end, pathType=curve` に強制書き換えされてしまう
- **階層間ラインの適用対象を限定**: `bdApplyAutoStyle` のラインループで `nodeDepth.has(c.to)` も確認し、ツリー内カード同士を結ぶ「階層間のライン」だけを対象に。外部参照ラインはスキップ。また `c.styleRef` でスキップしていた条件を外し、プリセット参照ラインにも個別 override として書き込むようにして、実運用で機能するように (既存のすべてのラインは `bdCreateConnection` で `styleRef` がセットされるため、スキップ条件があるとほぼ何も適用されなかった)
- **`editTargets` が空配列のときにフォールバックしない**: `_bdBuildStyleFields` の `setField` で、複数ターゲット指定があれば単一ターゲットにフォールバックせず早期 return。誤って displayStyle (捨てるコピー) に書き込むのを防ぐ
- **雲型パラメータの条件付き適用**: `_BD_DEPTH_CLOUD_FIELDS` と `_BD_CLOUD_SHAPES` を新設。depth.shape が雲型系 (cloud / thorn / thorn-curve / fluffy) のときのみ、cloudBumpWidth などの雲型パラメータを適用。それ以外のシェイプでは触らない
- **直角曲線の select プレビューを修正**: `_bdLineStylePreviewHtml` のプレビュー SVG パスを、L-Q-Q-L の 2 コーナー丸め形状に修正

## v0.5.278 (2026-04-19)

### feat(board): ビューワーのミニマップをパネルにフィット + ボード色を反映

- **アスペクト比を維持してパネルにフィット**: [gb-canvas-minimap.js](app/gb-canvas-minimap.js) `_bdMinimapScale(paneW, paneH, bounds)` を固定倍率 0.12 からフィット倍率 `Math.min(paneW/b.w, paneH/b.h) * 0.92` に変更。余白係数 0.92 でパネル端のクリッピングを避ける。ボードが細長くても正方形でも同じ比率で収まる
- **ボードキャンバスの実背景色を反映**: `_bdMinimapBackgroundColor(fallback)` を新設。`#bd-canvas` の `getComputedStyle().backgroundColor` を優先採用し、transparent のときは `bd._bgColor`、最終フォールバックが `--bg2`。スタイルタブで `--bd-bg` を変えた結果やツールバーのスウォッチで設定した色がミニマップに同期する
- **カード色をスタイル継承込みで反映**: `_bdMinimapNodeFillColor(node, statusDef)` を新設。`bdGetNodeStyle(node).bgColor` → `node.bgColor` → ステータス色 → `#555` の順にフォールバック。カードスタイルマネージャで設定した背景色がミニマップにも反映される。`bdStatusDef` の結果を呼び出し元で 1 回だけ取得して共有し、null 安全に
- **ミニマップクリックによるパン位置も同じ倍率で計算**: `panTo` 内の scale 計算もフィット倍率に切り替え、ドラッグ位置が描画と一致する

### 徹底チェック修正

- **パネルリサイズ追従**: フィット倍率がパネルサイズに依存するため、ビューワーパネルをリサイズしても再描画トリガがなく縦横比がくずれて見える問題を修正。[gb-canvas-minimap.js](app/gb-canvas-minimap.js) にパネルの `ResizeObserver` を追加し、サイズ変化時に `bdUpdateMinimap()` を呼ぶ。一度だけ設置（`pane._bdMinimapRO` ガード）
- **ボード背景色変更の即時反映**: ボード背景を変える 5 経路（[gb-app.part03.js](app/gb-app.part03.js) ツールバースウォッチ、[gb-board-ui.part01.js](app/gb-board-ui.part01.js) `bg-color` アクション、[gb-board-ui.part02.js](app/gb-board-ui.part02.js) のカラーフィールド / リセット、[gb-detail-panel.part01.js](app/gb-detail-panel.part01.js) `applyCss` の `bgColor` 分岐）が `bdRender` を通らないため、それぞれに `bdUpdateMinimap()` 呼び出しを追加。ミニマップが即座に新しい背景色に追従する

## v0.5.277 (2026-04-19)

### feat(board): HUD 表示仕様の見直し (サイズ統一 / 常時表示 / テキスト背面)

- **マーカーのサイズをステータスドットに揃える**: [gb-board-ui.part01.js](app/gb-board-ui.part01.js) `bdMarkerIconHtml` で描画 shape を `<g transform="matrix(1.6 0 0 1.6 -7.2 -7.2)">` で中心 (12,12) 基準に 1.6 倍にスケール。`r=7` の円や `14×14` のパスが viewBox ほぼ一杯まで広がり、14×14 のステータスドットと視覚サイズがほぼ同等になる。`helpCircle` は `r=9` で元々広いため、スケール無しで `r=11` の塗り円として個別に再調整
- **ある場合は常に表示**: [gb-tools.part03.css](app/gb-tools.part03.css) の `.bd-node:not(:hover):not(.bd-selected) .bd-hud { opacity:0 }` セレクタを `.bd-anchor-hud` に限定。ステータス/マーカー/コメントの HUD はカード非選択時でもそのまま表示される（空状態は JS 側で描画しない or CSS の `display:none` で非表示のため、ノイズにならない）。アンカーは従来どおりホバー/選択時のみ露出
- **カードテキストの背面へ**: ステータス/マーカー/コメントの `z-index` を `11` → `8` に下げ、`.bd-text` (`z-index:9`) より背面に回した。テキストが長くなっても HUD が前面に被らず、テキスト側は `pointer-events:none` なのでクリックはそのまま HUD に通る。アンカーは `z-index` 11 のまま（カード追加ボタンなので最前面維持）

## v0.5.276 (2026-04-19)

### fix(board): カードメニューボタン位置調整 + ベジェラインの上下方向対応

- **カードメニューボタン**: v0.5.274 で `right: 16px` にしたが寄せすぎだったので `right: 12px` に戻した。右アンカーは内側 9px に張り出すため、3px の余白で重ならない配置
- **ベジェラインが縦方向に対応**: [gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) `_bdResolveControlPoints` の初期化ロジックを「始点-終点ベクトルの優勢軸に沿って配置」に変更
  - 水平方向 (`|dx| >= |dy|`): 制御点を x 軸上に配置（従来通り）
  - 垂直方向 (`|dy| > |dx|`): 制御点を y 軸上に配置（新規）
  - 矢印マーカーは SVG の `orient="auto-start-reverse"` で path の接線方向に自動回転するため、制御点を縦にするだけで矢印も縦を向く
  - 既に `controlPoints` が保存されているラインは従来値を維持（ユーザーがハンドルで調整済みの向きを壊さない）

## v0.5.275 (2026-04-19)

### feat(board): ホイールズームの 100% スナップ + 下部ツールバー倍率ラベルのドロップダウン

- **ホイールズーム 100% スナップ**: [gb-canvas-interact.js](app/gb-canvas-interact.js) `onCanvasWheel` で、ホイール操作が `1.0 (100%)` を跨ぐ場合は一旦 `1.0` で停止させる。丁度 100% を確実に経由させて倍率位置を合わせやすくする。既に 100% のときは通常通り隣の倍率へ進む (停止ループに入らない)
- **ズーム倍率ラベルのドロップダウン**: 下部ツールバーの `#bd-zoom-label` をクリックしてプリセット倍率を選択できるようにした
  - [gb-board-presets.js](app/gb-board-presets.js) ラベルに `data-bd-action="zoom-select"` を追加
  - [gb-board-ui.part01.js](app/gb-board-ui.part01.js) クリック時に `bdShowZoomMenu(btn)` を呼び出す分岐を追加
  - [gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) `bdShowZoomMenu(anchor)` を新設。`_bdCreateHudMenu` の共通スタイルで吹き出しを生成し、以下の項目を表示
    - 倍率プリセット: 500% / 400% / 300% / 200% / 150% / 120% / 100% / 80% / 50% / 20% / 10% (現在値にチェック)
    - セパレータ
    - フィットマップ (`bdFitAll` を呼ぶ)
  - 倍率を選択するとビューポート中心を軸にズームしてパン位置も補正
  - [gb-tools.part03.css](app/gb-tools.part03.css) `#bd-zoom-label` に `cursor: pointer` + hover 時 `color: var(--fg)` を付与

## v0.5.274 (2026-04-19)

### feat(board): カードメニューボタン左寄せ + アンカー「+」塗りつぶし常時表示

- [gb-tools.part03.css](app/gb-tools.part03.css) `.bd-card-menu-btn` の `right: 4px` → `right: 16px`。右アンカーの「+」(カード右辺から内側へ 9px 突き出す) とメニューボタンが重ならないようにズラした
- 同: アンカー「+」の塗りつぶし＋白抜きを**デフォルト状態**化（旧: hover 時のみ塗りつぶし）。UX 上ボタンであることを一目で示すため。ホバー時は `filter: brightness(1.2)` で明度変化のフィードバックのみ
- ライン選択中 (`#bd-canvas.bd-has-conn-selection`) のときは、円の塗りつぶしを外し、`+` も隠して「枠線のみ」表示に戻す（旧ライン作成モードの見た目を維持）

## v0.5.273 (2026-04-19)

### feat(board): カード初期高さを「1 行入力された状態」に + 空状態 HUD ボタン廃止

- **新規カードの初期高さ**: [gb-board-ui.part01.js](app/gb-board-ui.part01.js) `bdCreateNodeWithStyle` で、h 未指定時に `Math.ceil(fontSize * 1.5) + 16`（padding 8+8 + line-height 1.5）を既定値として設定。画像カード (`img` opt あり) は画像サイズ追従のため従来通り h=0
- **カード HUD の空状態ボタンを廃止**:
  - [gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) 「ステータス」HUD はステータス設定済みのときのみ描画（旧: 破線の「ステータスを設定」空ボタン削除）
  - 同: 「マーカー」HUD はマーカー設定済みのときのみ描画（旧: 破線の「+」マーカー追加ボタン削除）
  - [gb-tools.part03.css](app/gb-tools.part03.css) `.bd-comment-hud.empty { display: none }` で「コメントを追加」空ボタン非表示化（件数 > 0 のときは従来通り表示）。HUD 要素自体は DOM に残す（`CommentBadges.refreshBoard` が find して件数更新するため）
- **ポップアップメニュー「構造」の次に 3 項目を追加**: [gb-canvas-features.part02.js](app/gb-canvas-features.part02.js) `bdContextMenu` 内に「ステータス」「マーカー」「コメント」サブメニューを新設。各 HUD と同等の操作をここから行える
  - ステータス: 「なし」+ 登録済みステータス一覧 (radioMark) + 「ステータスを管理...」
  - マーカー: 優先度 / フラグ カテゴリごとのマーカー選択 + 「すべてクリア」
  - コメント: 「コメントを追加」「コメント一覧を開く」

## v0.5.272 (2026-04-18)

### feat(board): アンカーの「◯+」を lucide circle-plus に統一

v0.5.271 の CSS 疑似要素による「+」描画では 1px 未満のずれが目立っていたので、lucide アイコン `circle-plus` で描画するよう差し替えた。円と「+」が同じ SVG viewBox 内で数学的に整列するため、subpixel ずれが発生しない。

- [meldex-core.part01.js](app/meldex-core.part01.js) LUCIDE レジストリに `circlePlus`（`<circle cx="12" cy="12" r="10"/> + <path d="M8 12h8"/> + <path d="M12 8v8"/>`）を追加
- [gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) 各アンカー div 生成時に `anchor.innerHTML = lucide('circlePlus', 18)` を注入
- [gb-tools.part03.css](app/gb-tools.part03.css) `.bd-anchor-hud` は枠線・背景を廃止し、`color: var(--bd-anchor-color)` を currentColor として SVG stroke に渡す flex コンテナ化。サイズは 14×14 → **18×18**
  - ホバー時: `svg circle { fill: currentColor }` で内側を塗りつぶし、`svg path { stroke: #fff }` で「+」だけ白反転
  - ライン選択中: `svg path { display: none }` で「+」だけ消して円は残す（既存のライン作成用途を維持）
  - SVG には `pointer-events: none` を付けて div 側の click/hover/title を阻害しない

## v0.5.271 (2026-04-18)

### feat(board): アンカーの「+」を拡大 + ホバー塗りつぶし

- アンカーサイズを 10×10 → 14×14 に拡大し、「+」の視認性とクリック判定を両方向上
- 「+」棒の太さを 1.5px → 2px、長さを 6px → 10px に拡張 (内側余白 2px、両端に 1px 丸みを追加して角を柔らかく)
- カーソルオーバー時にアンカー内側を `--bd-anchor-color` で塗りつぶし、「+」色を白に切り替えて反転コントラスト表示 (背景 transition 0.1s)
- ホバー時の枠線太さは 2.5px → 2px に調整 (塗りつぶしと重なって二重縁になるのを緩和)

## v0.5.270 (2026-04-18)

### fix(board): アンカー +クリック機能の徹底チェックで発見した 4 件を修正

v0.5.269 追加の `_bdAnchorAddCard` に対して、想定ユーザー操作を列挙した徹底チェックを実施。以下 4 件を修正。

- **`between` 挿入で兄弟順が崩れる (高)**: 親 X の子が `[n, A, B]` のとき n の左アンカーをクリックすると `bdChildren(X) = [A, B, newNode]` になっていた (newNode を末尾 push していたため)。`bd.nodes.findIndex(v => v.id === fromNid)` の位置に splice するよう変更し `bdChildren(X) = [newNode, A, B]`（newNode が n の兄弟スロットを引き継ぐ）を保証
- **`bd.connecting` のステール状態 (中)**: 前の操作で二段階接続モード (`bd.connecting = ...`) が残ったまま次のアンカークリックが発生すると、副作用的に既存モードに混線する可能性があった。`_bdAnchorAddCard` 冒頭で `bd.connecting` / `_connLabel` / `_connOrigin` / `_connFromAnchor` をクリア
- **コンテナ内包カードでの座標破損 (中)**: `n.contained === true` のカードは親相対座標を使うため、絶対座標前提のツリー操作で位置がずれる。冒頭で no-op return を追加
- **`between` 時の重複接続残留 (低)**: `oldParent → fromNid` 接続が複数存在する場合、従来は 1 本目のみ書き換わり残りが孤児接続として残っていた。`filter` で 2 本目以降を削除するよう変更 (最初の一致のみ `to` を `newNode.id` に書き換え、残りは drop)

## v0.5.269 (2026-04-18)

### feat(board): カードアンカーのクリックでツリー構造に応じたカード追加

ボードで、**ラインを選択していない状態**では、カードの 4 点アンカー◯の中に「+」を表示し、クリックすると展開方向に合わせてカードを追加する。ドラッグ時の既存動作 (ライン作成 / 空白ドロップで新規カード+接続) は変更なし。

- 水平展開 (mindmap / logic / timeline / 未設定):
  - 右アンカー → 子カード追加
  - 左アンカー → 親カードとの間に新規カードを挿入
  - 上アンカー → 同階層カードを「前」に追加
  - 下アンカー → 同階層カードを「後ろ」に追加
- 垂直展開 (flowchart / orgchart):
  - 下アンカー → 子カード追加
  - 上アンカー → 親カードとの間に新規カードを挿入
  - 左アンカー → 同階層カードを「前」に追加
  - 右アンカー → 同階層カードを「後ろ」に追加
- ライン選択中は canvas に `.bd-has-conn-selection` が付与され、「+」は非表示に戻る (ライン操作と干渉しない)
- 親が存在しないカード (ルート / 孤立) での「親との間」「同階層」クリックは無視 (黙って no-op)
- 「+」は `::before`/`::after` pseudo-element で構成し、色は既存の `--bd-anchor-color` を共有

実装:
- [gb-tools.part03.css](app/gb-tools.part03.css) `.bd-anchor-hud::before/::after` で「+」を描画。`#bd-canvas.bd-has-conn-selection` で非表示切り替え
- [gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) `bd-line-endpoint` 付与ルーチンに、canvas への `bd-has-conn-selection` 付与を追加
- [gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) 追加: `_bdAnchorAddCard(fromNid, hudPos)`。structure 判定 → action 決定 → カード生成 → 接続設定 → 自動レイアウト適用 → 編集開始
- [gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) アンカー click (非ドラッグ) 分岐を「ライン選択中のみ従来二段階モード、それ以外は `_bdAnchorAddCard` 呼び出し」に変更。title も "クリックでカード追加 / ドラッグでライン作成" に更新

## v0.5.268 (2026-04-18)

### fix(ui): 詳細パネル → オプションパネル 改名の取りこぼし 3 箇所を修正

v0.5.255 / v0.5.263 で進めた「詳細パネル → オプションパネル」リネームのうち、以下 3 箇所がアイコンまたはラベルの更新漏れだった。

- [meldex-core.part01.js:453](app/meldex-core.part01.js#L453) `UI_TYPE_ICONS.detail` が `'info'` のまま残り、`GBTabs.createTab('オプション', 'detail', ...)` → `uiTypeIconName('detail')` 経由でオプションタブのアイコンが旧 ℹ のままだった。`'panelRight'` に変更し、アクティビティバー・ツールバー・タブ すべてで同じアイコンに統一
- [gb-db-props.part02.js:40](app/gb-db-props.part02.js#L40) シート行コンテキストメニュー「オプションで開く」項目のアイコンが `info` のまま → `panelRight` に統一
- [gb-tool-menu.part02.js:105](app/gb-tool-menu.part02.js#L105) ページ用ツールメニューの「情報を表示」→「オプションを表示」に変更 (`toggleDetailPanel()` を呼び出す項目のため)
- [meldex-core.part01.js:505](app/meldex-core.part01.js#L505) `replaceIcons()` のクラス → 名前マップも `ico-detail` → `panelRight` に分離 (`ico-info` のみ `info` を維持)
- [gb-layout.part01.js](app/gb-layout.part01.js) `_normalizePaneNode()` にマイグレーションを追加。保存済みレイアウト (`localStorage['gb:layout']` と `gb:app-layouts`) に焼き付いている `type: 'detail'` タブの旧ラベル `'詳細'` / 旧アイコン `'info'` を、ロード時に `'オプション'` / `'panelRight'` に書き換える。既存ユーザーの画面にも反映されるよう対応

## v0.5.267 (2026-04-18)

### fix(board): 徹底チェックで発見した保存タイミング不整合を修正

v0.5.266 で隙間変更 / 自動整列 on に合わせて既存ツリーを再レイアウトするようにしたが、保存フローに不整合があった。

**問題**: `adapter.set()` が先に `_saveFileTheme` → `bdSave()` を走らせ、`bdSave` の同期部分 `bdToMd()` が**再レイアウト前**のノード位置を捕捉する。その後に `adapter.applyCss()` 内で `_bdRelayoutAllStructureTrees` が走ってノード位置を変えるが、すでに完了した `bdSave` はその変更を含まない。結果として:

- ファイルには新しい隙間設定 + 古いノード位置が書き込まれる
- `bd.dirty` は true のままになるが、次のユーザー操作まで保存されない
- リロード時に古い位置が表示される (隙間設定と視覚がずれて見える)

**修正**: 3 箇所 (gapSiblings / gapLevels / autoAlign) の `_bdRelayoutAllStructureTrees` 呼び出しの直後に `bdSave()` を再実行するヘルパー `_ensureSaveAfterRelayout` を追加。新しいノード位置を確実に保存する。2 回 PUT が並行するがサーバーの受信順で 2 回目が勝つので実運用上は問題なし。

### その他確認事項 (問題なし)

- contained カード + 構造ツリー: `_bdRelayoutAllStructureTrees` は `!n.contained` で除外済み
- 複数ツリーが同時に存在: 各ルートで `bdAutoLayout` が独立に走り干渉しない
- `bdSave` が保存パスなしで呼ばれる (新規ボード等): 内部で `!savePath` 早期 return
- 自動整列オフ時: 再レイアウトも再保存も走らない (意図通り)

## v0.5.266 (2026-04-18)

### feat(board): 隙間設定変更 / 自動整列オンで既存ツリーを即再レイアウト

v0.5.264 で追加した設定は新規レイアウト時にのみ反映されていたが、既存のカード群にも即反映するように拡張。

- `_bdRelayoutAllStructureTrees()` ヘルパーを新設。`bd.nodes` を走査して `structure` が設定された全ルートに対して `bdAutoLayout` を実行する (contained カードはスキップ)
- スタイルタブで「同階層カード間の隙間」「階層間の隙間」を変更すると、`bd.autoAlign` が on なら即座に既存の全構造ツリー (マインドマップ / フローチャート / ロジック図 / 組織図 / タイムライン) が新しい隙間で再配置される
- 「自動整列」をオフ → オン に切り替えた時も、同じく既存ツリーに現在の隙間設定を適用する
- ルート位置は `bdAutoLayout` 内の `origRootX/Y` 復元ロジックで保持されるため、ツリーは現在の場所に留まり内部だけ整列する
- 自動整列がオフのときは従来通り再レイアウトされないので、隙間は「将来の新規レイアウト用のデフォルト」として保存される

## v0.5.265 (2026-04-18)

### fix(board): 徹底チェックで発見した 2 件の後退を修正

v0.5.264 で `BD_LAYOUT_GAP_H` / `BD_LAYOUT_GAP_V` 定数を廃止した副作用と、contained カードへの誤適用を修正。

- **Tab / Enter のカード追加が undefined 参照で壊れていた** (Critical): 旧定数 `BD_LAYOUT_GAP_H` / `BD_LAYOUT_GAP_V` は `const` 宣言で外部からアクセス可能だったが、v0.5.264 で廃止。`gb-shortcuts.js` の [board.childCard](app/gb-shortcuts.js#L582) と [board.editOrSibling](app/gb-shortcuts.js#L603) がこれらを参照し続けており、Tab で子カード追加 / Enter で同階層カード追加時に `ReferenceError` が発生していた。`bdLayoutGaps().level` / `.sibling` に置換して解消
- **contained カードのリサイズで親ツリーが誤再レイアウト**: autoAlign オン時、コンテナ内のカード (`n.contained === true`) をリサイズすると `_bdFindStructureRoot` が親コンテナの構造ツリールートを辿り `bdAutoLayout` を実行する可能性があった。contained はワールド座標でなく親相対座標で動くため layout と相性が悪い。`_resizedNode.contained` を判定して auto-layout 対象から除外

## v0.5.264 (2026-04-18)

### feat(board): 隙間設定と自動整列 (スタイルタブ) を追加

ボードのオプションパネル「スタイル」タブの編集操作エリアに 3 件の設定項目を追加。

- **同階層カード間の隙間 (`--bd-gap-siblings`)**: `bd.gapSiblings` に同期。bdLayout* (Mindmap / Flowchart / Timeline / OrgChart / Logic) が同階層カード間のオフセットとして参照。未設定時は 40px
- **階層間の隙間 (`--bd-gap-levels`)**: `bd.gapLevels` に同期。親→子へ伸びる軸のオフセット。未設定時は 60px。`bdLayoutGaps()` ヘルパーを新設して全レイアウト関数が共通参照
- **自動整列 (`--bd-auto-align`)**: トグル、初期値オン (defaultOn: true)。`bd.autoAlign` に同期
  - **リサイズ時の周囲カード追従**: リサイズ終了時に `_bdFindStructureRoot()` で構造ツリーのルートを辿り、見つかれば `bdAutoLayout(rootId)` を呼び出して隙間を維持したまま再配置する
  - **ドラッグ時の近接吸着**: ドラッグ量が 40px 未満の小さな移動の場合、`_bdSnapNodeToNeighbors()` が 8px の閾値で他カードの辺 (上/下/左/右) と自身の辺の 4 組み合わせから最近接のスナップ delta を算出して x / y を吸着。複数選択ドラッグは全員に同じ delta を適用
  - 1px 以下の微小な移動 (= ほぼクリック) では吸着しない
- **toggle フィールドに `defaultOn` オプションを新設** (`_fsBuildControl`): 値未設定時にデフォルト ON として UI 表示できるようにした。autoAlign のように「既定オン、明示オフだけ保存したい」項目用
- **bdLoadState でフィールド復元**: `--bd-gap-siblings` / `--bd-gap-levels` / `--bd-auto-align` を `bd._fileStyle` から読んで bd 側プロパティに同期。autoAlign は `'0'` のときのみ off として扱う
- **layout 関数の定数命名を semantic に統一**: `BD_LAYOUT_GAP_H` / `BD_LAYOUT_GAP_V` を廃止し、`BD_LAYOUT_GAP_SIBLINGS_DEFAULT` / `BD_LAYOUT_GAP_LEVELS_DEFAULT` に置換。既存レイアウト関数内のコメントも mindmap は「水平ツリー」、flowchart は「垂直ツリー」の違いを明記

## v0.5.263 (2026-04-18)

### fix(board): ハンドル⇄アンカー重なり再修正 / 沿線回転のテキスト色反映 / テキスト縁取り色設定

- **ハンドルとアンカー HUD の重なり再修正**: 端点ドラッグ中、ポインタが移動先カードに入るとその
  カードの `.bd-anchor-hud` が `:hover` で可視化され、ドラッグ中のハンドル円と重なって二重丸に
  見えていた。ドラッグ中は `#bd-canvas` に `.bd-endpoint-dragging` クラスを付与し、CSS で
  `#bd-canvas.bd-endpoint-dragging .bd-anchor-hud { display: none !important; }` として全カードの
  アンカー HUD を一時的に非表示化。pointerup / pointercancel でクラスを外して通常状態に戻す
- **沿線回転でテキスト色が反映されない問題を修正**: `.bd-conn-label-path` の CSS ルール
  `fill: var(--fg)` が SVG の `setAttribute('fill', ...)` を上書きしていた。ブラウザ仕様で
  CSS は SVG presentation attribute に勝つため、inline 属性では色指定が効かない。`style.fill`
  (inline style) で設定するように変更。stroke も同様に `style.stroke` / `style.strokeWidth` に
  変更
- **テキスト縁取り色を設定可能に**: `textShadowColor` プロパティを新設し、ラインスタイル管理
  ダイアログ「縁取」行にカラースウォッチを追加。SVG textPath モード / HTML div モード /
  ツールバーのスタイルプレビュー 3 箇所で色指定を反映。未指定時は従来通り白
  (`rgba(255,255,255,0.85)` / `#fff`) にフォールバック。ファイル保存/読込みとクリップボード
  ペースト、conn-override キー一覧、スタイル継承すべてに対応

## v0.5.262 (2026-04-18)

### fix(board): 徹底チェックで発見した BD_ANCHOR_NAMES 後方互換問題を修正

v0.5.261 で `BD_ANCHOR_NAMES` を 8 → 4 に縮小した際、`_bdConnAnchorName` がこの配列を使って保存済みアンカー名を検証していたため、旧データに残っている `top-left` / `top-right` / `bottom-left` / `bottom-right` が無効と判定されて null を返し、ラインの端点位置が自動計算にフォールバックしていた（既存ラインの見た目が崩れる後退）。

- 後方互換性検証用の `BD_ANCHOR_NAMES_ALL` を新設（従来通りの 8 名）
- `_bdConnAnchorName` の検証を `BD_ANCHOR_NAMES_ALL.includes(v)` に変更し、旧 8 名のアンカーを引き続き受理する
- `_bdGetAnchorPointByShape` の switch は元から 8 名対応のため、既存ラインの端点座標は崩れない
- 新規ライン作成 / 最近接算出 / Free Bezier 編集オーバーレイは `BD_ANCHOR_NAMES` (4 名) を使うので、ユーザーから見える新規動作は 4 点

## v0.5.261 (2026-04-18)

### feat(board): アンカーを 4 点化 + 内側透明化 + 色設定 + 詳細パネル→オプションパネルのリネーム残件対応

ユーザー要望 4 件をまとめて対応。

- **アンカーを 8 点 → 4 点に**: カード HUD アンカー `.bd-anchor-hud` とマルチ選択グループアンカー `.bd-group-anchor` から四隅 (tl/tr/bl/br) を削除し、辺中央 (top/bottom/left/right) のみ 4 点に統一。楕円形状の四隅補正ロジック (14.64%/85.36%) は不要になったため削除。`BD_ANCHOR_NAMES` も 4 項目 (top-center/left-center/right-center/bottom-center) に縮小。既存データの古い角アンカー (top-left 等) は `_bdGetAnchorPointByShape` の switch が引き続き処理するため、保存済みラインの描画は壊れない
- **アンカーの内側を常に透明化**: `.bd-anchor-hud` / `.bd-group-anchor` の `background` を `var(--bg2)` から `transparent` に変更。ホバー時も塗りつぶさず、`border-width` を 1.5px → 2.5px にしてフィードバックする
- **アンカー色をオプションパネル スタイルタブで設定可能に**: `--bd-anchor-color` カスタムプロパティを新設し、CSS で `var(--bd-anchor-color, var(--accent))` をアンカー枠線色に使用。オプションパネル「スタイル」タブ（ボードコンテキスト）の editOps に `--bd-anchor-color` を追加、UI グループに「アンカー」行を挿入
- **詳細パネル → オプションパネル のリネーム残件**:
  - [gb-scriptnote-detail.part02.js](app/gb-scriptnote-detail.part02.js): 挙動タブの移行バナー文言「詳細パネルの『スタイル』タブに移行しました」→「オプションパネルの『スタイル』タブに移行しました」
  - [gb-detail-panel.part02.js](app/gb-detail-panel.part02.js): エラー文言「詳細の保存に失敗しました」→「オプションの保存に失敗しました」
  - [gb-db-props.part02.js](app/gb-db-props.part02.js): コンテキストメニュー項目「詳細で開く」→「オプションで開く」
  - [gb-board-ui.part01.js](app/gb-board-ui.part01.js) / [gb-board-ui.part02.js](app/gb-board-ui.part02.js): セクション見出し「スタイル詳細」「カードスタイル詳細」「ラインスタイル詳細」から「詳細」を除き「スタイル」「カードスタイル」「ラインスタイル」に統一

## v0.5.260 (2026-04-18)

### fix(board): 選択中ラインの端点カードのアンカー HUD を完全に非表示化

前バージョンでは `pointer-events: none` にしてクリックだけ通過させていたが、ライン選択中にハンドルをドラッグすると反対側 (固定側) のカードで「アンカー HUD」と「端点ハンドル円」が同じ位置に重なって二重丸に見える視覚問題があった。

- CSS ルールを `pointer-events: none` から `display: none` に変更し、端点カードのアンカー HUD を視覚的にも完全に隠す
- これにより固定側も移動側も端点ハンドル (SVG の `.bd-conn-selection-end`) だけが残り、視覚的にクリーンになる。クリック通過の効果はそのまま維持される (display:none 要素はイベントも受けない)

## v0.5.259 (2026-04-18)

### fix(board): 選択中ラインの端点カードのアンカーを非活性化

選択中ラインの始点/終点となっているカードの 8 点アンカー HUD が、端点ハンドルのドラッグ操作と競合して新規ライン作成を誘発していた問題を解消。

- `bdDrawConns` 末尾で `bd.selectedConnIds` の from/to カードに `.bd-line-endpoint` クラスを同期付与する（選択が外れたカードからは除去）
- CSS `.bd-node.bd-line-endpoint .bd-anchor-hud { pointer-events: none; }` でアンカー HUD のクリックを無効化。視覚的にはそのまま HUD として表示されるが、クリックは下位の SVG 端点ハンドルへ通過する
- 結果、ラインを選択した状態で端点をドラッグしようとした際、アンカーが先にイベントを奪って新規ライン作成モードに入る誤動作が起きなくなる

## v0.5.258 (2026-04-18)

### fix(board): ライン始点/終点ドラッグの操作性改善

- **始点ドラッグの不発を解消**: `.bd-conn-hit` (16px 透明ヒットストローク) が端点ハンドルより後に append されていたため、SVG の描画後勝ちルールでハンドルが覆われて始点側のポインターイベントを奪われていた。選択時の端点ハンドル + 中央ハンドルを `hit` の append 後に re-append して最前面へ移すように変更
- **ドラッグ中にライン本体を追従**: 旧実装は `handleEl` の cx/cy だけ更新していてライン本体とアーク/直角/Free Bezier 形状はそのままだった。ドラッグ開始時に反対端のアンカー座標を固定点として取得し、pointermove ごとに main path / 選択前景/背景パスの `d` を `M固定点 L ポインタ位置` の直線プレビューに更新する。ドロップ時は bdDrawConns で本来のパス形状で再描画される
- **選択パスに id を付与**: `bd-sel-back-<connId>` / `bd-sel-front-<connId>` を設定し、ドラッグ中の追従更新でパス要素を直接取得できるようにした

## v0.5.257 (2026-04-18)

### fix(board): 8 点アンカーの左ドラッグ接続 + 楕円対応 + 端点ドラッグ対応

ユーザー報告の 3 件を修正。合わせて v0.5.256 以降の機能停止（`gb-canvas-features.part03.js` に残っていた孤立コードによる SyntaxError）も別途修正済み。

- **8 点アンカーから左ドラッグでラインを引けるように**: アンカーの `pointerdown` を自己完結型のドラッグハンドラに再設計。ドラッグで `bd-conn-preview` を表示、ドロップ先がカードなら接続作成、空白なら新規カード＋接続を 1 操作（bdPushUndo 1 回）で作成。単クリック時は従来の二段階モード（`bd.connecting` セット → 次クリックで接続）を維持
- **8 点アンカーを楕円ベース形状に対応**: 楕円 / 雲 / もやもや / トゲ（直線/曲線）の四隅アンカー（tl/tr/bl/br）を矩形の角 0%/100% ではなく楕円曲線上の 45° 点（14.6447% / 85.3553%）に配置するようインラインスタイルで上書き。上下左右中央は辺中点で矩形・楕円とも共通
- **ラインの始点/終点を 8 点アンカーにドロップで接続できるように**: `_bdBindConnectionEndpointDrag` のドロップ判定を拡張し、落下先が `.bd-anchor-hud` ならそのクラス名（tl/tr/bl/br/top/bottom/left/right）から内部アンカー名（BD_ANCHOR_NAMES）を直接決定する。HUD 以外は従来通り `_bdFindNearestAnchor` の最近接計算
- **HUD 短縮名と内部アンカー名の橋渡しヘルパー `_bdHudPosToAnchorName` を追加**: カード HUD（短縮名 tl/tr/bl/br/top/bottom/left/right）とライン端点（BD_ANCHOR_NAMES = top-left/top-center/top-right/... など）の命名規約差を一元管理

## v0.5.256 (2026-04-18)

### feat(board): カード追加とグループ移動、プレビュー表示の改善

ユーザー要望に基づくボードの UX 改善 5 点。

- **カード追加直後の自動インライン入力を停止**: `bdAddAt` 末尾の `bdEditNode` 呼出を削除。追加直後のカードは選択状態で止まり、編集はダブルクリックで入る方式に統一
- **新規カードに自動でテキストを入れる**: 階層別スタイル（`depthStyles`）に `defaultText` プロパティを追加。`BD_DEFAULT_DEPTH_STYLES` の深さ 0 〜 4 に「カード / サブカード / 項目 / 詳細 / メモ」を既定値として設定し、`bdNormalizeDepthStyles` で永続化対象に含める
- **階層別スタイル管理ダイアログに「デフォルトテキスト」フィールドを追加**: 同ダイアログで各階層ごとのテキストをユーザーが編集できる（`bdOpenDepthStyleManager` の `depthFieldsEl` に input 追加）
- **グループをドラッグで移動可能に**: `.bd-frame-label` に pointerdown ハンドラを追加し、閾値（4px）超えでグループ内の全カード（`contained` 以外）を一括移動。`bdPushUndo` を 1 回だけ積んで Ctrl+Z で一括戻せる。ラベル cursor を `move` に変更。ラベル編集中は cursor を `text` にしてドラッグを無効化
- **ツールバーのスタイルプレビューの横幅を半分に**: `.bd-style-picker-trigger` の `min-width` を `180px` → `90px`。`.bd-style-preview > svg` を `width: 100%` にして内部 SVG も親幅に追従
- **カードプレビューと実体の見た目を揃える**:
  - `_bdCardPreviewShapeStyle` の雲型・もやもや・トゲ（直線/曲線）の polygon を再調整。実カードの動的 clip-path（Catmull-Rom 曲線）に視覚的に近づくよう、山の数と位置、底面の膨らみを整える
  - `_bdCardStylePreviewHtml` でカードスタイルの `fontSize` と `textStrokeWidth` をプレビューサイズ（約 28×20）に合わせてリニア圧縮して表示。文字フチは実カードと同じ `_bdTextOutlineShadow` を使用

## v0.5.255 (2026-04-18)

### feat: 注釈ツール監査対応 (Audit-P1 + Audit-P2) + ボードカード HUD/ポップアップ Gap 修正

`app/docs/annotation-audit-fix-plan-2026-04-18.md` と `app/docs/board-card-popup-audit-fix-plan-2026-04-18.md` に基づく監査対応を実施。

**注釈ツール Audit-P1 (H-1〜H-6)**

- ノートの保存完了フックで `CommentBadges.invalidate` + `refreshNote` を呼び、自動保存後のバッジ反映遅延を解消 (H-1)
- ノート右クリックの「コメントを追加」で右クリック起点要素から明示コンテキストを構築して `addCommentHere(override)` に渡す。`detectCommentContext(scanOverride)` に引数を追加 (H-2)
- シナリオの保存完了フックでも同様にバッジキャッシュを無効化 + 再描画 (H-3)
- ボードライン右クリックメニューに「コメントを追加」を追加。`target_ref` にラインIDを格納し、既存のカードコメントと同じストアで管理 (H-4)
- コメント追加をインライン textarea に刷新。`_promptInlineComment` を追加し、`addCommentHere(override, { anchorEl })` で anchor 近傍に入力欄を表示。ボードカード HUD のダブルクリックで直接インライン入力を開く。click と競合しないよう 250ms 遅延実行で衝突回避 (H-5)
- カレンダーイベント向けに target_kind `calendar_event` を新設。イベント編集パネルに「コメントを追加」ボタン、削除時に `/api/annotations/orphan-by-target` で孤児化。右パネル一覧のグルーピング・ラベル・ジャンプも対応。`.gb-cal-day-event` に `data-calendar-id` を付与し、`CommentBadges.refreshCalendar` + `_ensureCalendarBadge` でイベント右上にバッジ表示 (H-6)

**注釈ツール Audit-P2 (H-7 view_lock 完備)**

- `GET /api/view-lock` / `PUT /api/view-lock` を新設（`view_key = target_path#pane_id` 形式、`state` は JSON で永続化）
- `gb-view-lock.js` を新規作成。`window.ViewLock` モジュールとして engage（一筆目誘導）/ guardAction（解除ダイアログ）/ toggle / bindHudIcon / guardScrollContainer / installInteractionInterceptor / subscribe を公開
- `registerToolComponent` に `requiresViewLock` オプションを追加。ノート / シナリオ / シート / カレンダー / メディア / フォルダ / 比較 / ダッシュボードを `true` に設定（ボードは計画書 §8.2 通り `false`）
- ストローク/付箋の描画前に一筆目誘導ダイアログ（計画書 §8.5）を表示。各ビュールートの右上にロックアイコンを配置（bindHudIcon）
- スクロールコンテナの wheel / keydown / touchmove を capture ガード、scrollbar 経由の scroll も savedTop/Left に戻す
- document の capture フェーズで scroll container 内のインタラクティブ要素への click/change を一括ガード（計画書 §8.4）。解除「はい」で元要素を再 click

**ボードカード HUD/ポップアップ Gap-1〜3**

- マルチ選択時は個別カードの 8 点アンカーを CSS で非表示化し、選択グループ bbox 上の統合アンカー（`.bd-selection-group-anchors`）を `#bd-resize-layer` に描画。ロック混在時はグループアンカー自体を非表示 (Gap-1 §9.6)
- 8 点アンカーを空白ドロップした際に新規カード作成 + 接続ラインを 1 回の Undo にまとめる（案α）。`bd._connOrigin` フラグで由来（`anchor` / `toolbar`）を判別し、ツールバー「ラインを引く」由来は従来通りキャンセル（案β）(Gap-2 §9.7)
- `.bd-anchor-hud` の `cursor: crosshair` は既存実装で満たしていることを確認 (Gap-3 §9.3)

**周辺**

- `app/docs/annotation_unification_plan.md` §16 を実装実態に合わせて書き換え、Audit-P1 / Audit-P2 実装状況を追記 (L-4)
- `app/docs/board-card-popup-redesign-plan.md` §9.3 / §9.6 / §9.7 末尾に 2026-04-18 実装完了の注記を追加

## v0.5.254 (2026-04-18)

### chore: 長期間の未コミット作業を一括取り込み

これまで複数のサブセッションで進められていた大量の変更 (196 ファイル変更 / 40 ファイル新規) を単一コミットにまとめて取り込んだ。個別のフィーチャーや修正の記録は本エントリからは割愛し、git の diff / blame で追えるようにする。主なカテゴリ:

- **大規模ファイル分割 (split loader 方式への移行)**:
  - `gb-note-enhance.js` → `gb-note-enhance.part01.js` / `part02.js` (旧 1251 行を分割)
  - `gb-tool-menu.js` → `gb-tool-menu.part01.js` / `part02.js` (旧 1087 行を分割)
  - 既存の `part01/02/03` 分割と同じ split loader スタブで置換え。

- **exe 版のホームフォルダ初期化強化** (`Meldex.part01.py` 他):
  - 初回起動時に `MeldexHome/` 同梱テンプレートを `~/MeldexHome` へ展開。
  - `マニュアル` / `サンプル` を「組み込みロック付きフォルダ」として扱う機構を追加。

- **DB / Scriptnote / Editor 系の改善多数** (`gb-db-*.js`, `gb-scriptnote-*.js`, `gb-editor.*` 等、35 + 17 + 4 ファイル):
  - 行単位での挙動改善、バリデーション強化、UI 統一等。
  - 個別内容は `git log` / `git blame` 参照。

- **Notion 連携・認証・API 周辺強化** (`meldex_notion_service.py`, `meldex_api_auth.py`, `meldex_api_version.py`, `meldex_calendar_utils.py`, `meldex_api_outliner.py`, `gb-notion-sync.js`):
  - サービス層の整理、API バージョニング、カレンダーユーティリティ追加。

- **RPA 系リファクタ** (`rpa.html`, `rpa.py`, `rpa-panels.js`, `rpa-render.js`, `rpa-dialogs.js`).

- **テスト大量追加** (`app/tests/` に +25 ファイル):
  - `test_meldex_batch24` 〜 `test_meldex_batch48` のバッチ回帰テスト。
  - `test_meldex_api_smoke.py` / `test_meldex_calendar_utils.py` / `test_meldex_migrate_db.py` / `test_meldex_native_dialogs.py` / `test_meldex_split_assets.py` 等の単体テスト。

- **新規ドキュメント**:
  - `docs/board-card-popup-redesign-plan.md` / `board-free-bezier-plan.md` / `board-line-text-along-path-demo.html` / `board-thorn-anchor-demo.html` / `board-free-bezier-demo.html` / `board-card-popup-redesign-demo.html`。

- **`.gitignore` 更新**:
  - `/app/MeldexHome/無題 6.scriptnote.json` と `/app/MeldexHome/無題/` を追加 (既存の「無題 5」等と同パターン)。
  - `/app/_*.concat.js` を追加 (デバッグ用結合成果物)。

- **`AGENTS.md`** 運用ドキュメント更新。

**注記**: これは過去の開発結果を失わないための単発の取り込みコミット。今後の変更は機能単位でコミットする。

## v0.5.253 (2026-04-18)

### fix(board): Free Bezier 徹底チェックで発見した 6 件の不具合を修正

v0.5.252 の Free Bezier 実装に対する徹底チェックで発見した問題を修正。

- **アンカー opposite 自動補正が非自己ループでも適用されていた不具合**: `_bdRenderFreeBezierEditOverlay` の `applyAnchorClick` で、`from.anchor === to.anchor` になった時に片側を `_bdOppositeAnchor` へ強制変更する処理が、`conn.from !== conn.to` (別カード接続) でも実行されていた。別カードなら同名アンカーでも座標が異なるため縮退しない。自動補正を `conn.from === conn.to` (自己ループ) 時のみ適用するよう修正。
- **ハンドルドラッグ pointerdown で即 undo push される不具合**: `_bdBindCurveHandleDrag` がクリックしただけで `bdPushUndo` を呼び、余計な undo がスタックに積まれていた。最初の `pointermove` で `moved=true` になった時だけ push するよう修正。
- **ハンドルドラッグ座標変換の不整合**: `_bdBindCurveHandleDrag` / `_bdBindConnectionEndpointDrag` が `(mv.clientX - rect.left) / bd.zoom` のみで変換しており、`bd.panX/Y` / 回転 / アプリ zoom を無視していた。`bdScreenToWorld` を使う正しい変換に修正。
- **端点ドラッグでカード外ドロップ時の意図しない undo 実行**: `_bdBindConnectionEndpointDrag` が最初の `pointermove` で `bdPushUndo` を呼び、カード外ドロップで `bdUndo` を実行していた。これにより conn を変更していないのに直前の無関係な操作が巻き戻っていた。undo push を「カードへのドロップで実際に変更を適用する直前」に移動し、カード外ドロップ時は `bdDrawConns` で handle 位置を元に戻すだけに変更。同じカード・同じアンカーへ戻す場合も変更不要として早期リターン。
- **textAlongPath ラベルの重複描画**: `bdDrawConns` の svg クリア処理に `.bd-conn-label-path` (SVG textPath テキスト要素) が含まれておらず、再描画ごとに古いラベルが残って重複していた。クリア対象に追加。

変更ファイル:
- [app/gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) `_bdRenderFreeBezierEditOverlay` / `_bdBindCurveHandleDrag` / `_bdBindConnectionEndpointDrag` / `bdDrawConns` の svg クリア。

## v0.5.252 (2026-04-18)

### feat(board): Free Bezier ライン + 8 アンカー編集 UI + 沿線テキスト + 複数選択 + 端点ドラッグ

ボードのライン機能を相関図向けに全面強化した。新しい `pathType='free-bezier'` を追加し、両端のアンカー位置と 2 本の制御ハンドルで単一の CubicBezier を自由編集できる。ラインの複数選択、端点ドラッグでの接続先変更、テキストの沿線回転・縁取り太さ可変なども追加した。

**1. Free Bezier ライン (pathType='free-bezier')**

- 8 アンカー (4 辺中点 + 4 隅) からライン端点を固定指定できる。楕円ベース形状 (ellipse / cloud / fluffy / thorn / thorn-curve) は内接楕円の周上 8 点、矩形系 / 多角形系は bounding box の 8 点で座標計算。
- 両端アンカーから 2 本の相対オフセットハンドルで単一 CubicBezier を描く。`controlPoints` は conn 個別に保存され、カード移動時も曲線形が自然に追従する。
- 自己ループ (from === to) を許可し、両端アンカーを独立指定可。既定は top-center → bottom-center。両端が同一アンカーに縮退しそうな場合は反対側へ自動スナップする。
- 新プリセット `line-free-bezier` (名前「ベジェ」) を `BD_DEFAULT_LINE_STYLES` に追加。詳細パネル / コンテキストメニュー / スタイル管理モーダルの pathType 選択肢に「ベジェ」を追加。

**2. 単一ライン編集 UI (選択中のみ表示)**

- ライン選択中、両端カードに 8 アンカー候補点 (`.bd-anchor-candidate`) を描画。現アンカーは色違いでハイライト。クリックで from / to のアンカーを切り替え。自己ループ時は 1 セットのみ表示し、Shift+クリックで to 側を変更できる。
- 2 本のベジェハンドル (`.bd-curve-handle`) とアンカーへの破線接続線 (`.bd-curve-handle-line`) を描画。ハンドルドラッグで `controlPoints[0/1]` の相対オフセットを更新する。ドラッグハンドラは `document` にバインドし、`bdDrawConns` による handle 再生成中もイベントが途切れないようにした。
- 誤タップ防止のため、これらの編集要素はライン選択中のみ表示する。

**3. ラインテキスト関連 (Phase 5)**

- ラベルの**ダブルクリックインライン編集**: Ctrl / Shift / Meta + ダブルクリックは複数選択モード維持のため編集に入らない。
- **沿線回転 (textAlongPath)**: SVG `<textPath href="#bd-path-{id}" startOffset="50%">` でラインの曲線に沿わせてテキスト描画。
- **自動反転 (textAutoFlip)**: ライン中央接線角度が 90〜270° の時、`textPath side="right"` で逆向きに配置して常に読みやすい向きにする (デフォルト true)。
- **白縁取り (textShadowWidth)**: SVG の `paint-order="stroke" stroke="rgba(255,255,255,0.85)" stroke-width="textShadowWidth*2"` で縁取り可変。デフォルト 1px。HTML 絶対配置モードでも同等の `text-shadow` 4 方向ドットで反映。
- **テキスト表示トグル (textVisible)**: false ならラベル自体を描画しない。ダブルクリックでも編集モードに入らない。
- **スタイル管理モーダル**: ライン UI に「テキスト表示 / 沿線 / 反転 / 縁取」行を追加。`textVisible=false` 時は他のテキスト系項目を半透明化。connection 個別 override 可能。
- **インライン編集中の切替**: textPath モードで contentEditable は SVG text に直接効かないため、ダブルクリックで `conn._editingInline=true` → HTML 絶対配置モードに一時切替 → 編集 → blur で `_editingInline` を削除して textPath モードへ戻す。

**4. 端点ドラッグで接続先カード変更 (Phase 3)**

- `.bd-conn-selection-end` を `pointer-events: all; cursor: grab` にしてドラッグ可能化。
- ドラッグ中は `.bd-drop-target` でカードをハイライト、handle 自体をマウス位置に追従。
- カードにドロップすると `from.cardId` / `to.cardId` を更新し、ドロップ位置から最近接の 8 アンカーを算出して `fromAnchor` / `toAnchor` に設定。自己ループで両端同一アンカーになる場合は反対側へ自動補正。
- カード外へドロップすると `bdUndo` で元に戻す。

**5. 複数選択 (Phase 4)**

- `.bd-conn-hit` クリックで Ctrl / Meta + クリックなら選択追加 / 削除。
- 矩形選択 (カード / ライン混在可): ラインは path の `getPointAtLength` で 24 点サンプリングし、矩形内に 1 点でも入れば選択。矩形選択開始時のライン選択状態 (`baseSelConnIds`) も Ctrl+ドラッグで加算できる。
- 削除 (Delete / Backspace / 右クリック): `bdDeleteSelected` を拡張し、カード+ラインの混在選択時も単独選択されたラインを明示的に `bd.connections` から除外する。

**6. ラインスタイルプレビュー拡張 (Phase 6)**

- `_bdLineStylePreviewHtml` のプレビュー SVG に path id を付与し、`textVisible=true` の場合「Aa」サンプル文字を表示。`textAlongPath=true` なら曲線に沿って (textPath)、false なら水平に配置。`textShadowWidth` に応じて白縁取りも反映。

**7. 保存 / 読み込み / undo / コピー対応**

- `bdToMd` / `bdFromMd` に `fromAnchor` / `toAnchor` / `controlPoints` / `textVisible` / `textAlongPath` / `textAutoFlip` / `textShadowWidth` を追加。controlPoints はフラット配列 `[dx1,dy1,dx2,dy2]` 形式で保存。
- JSON board 形式 (`\`\`\`board` ブロック) の読み込みにも対応。
- undo/redo は `_bdSnapshot` が JSON.stringify で全フィールドを拾うため自動対応。
- コピー&ペースト (`gb-canvas-engine.part03.js`) の push 側にも新フィールドを追加。

**変更ファイル**:

- [app/gb-board-presets.js](app/gb-board-presets.js) `line-free-bezier` プリセット追加 / `_bdLineStylePreviewHtml` にパス id + Aa ラベル + textPath 対応 / free-bezier プレビュー path (`M6,14 C14,2 42,18 50,6`) 追加。
- [app/gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) `_bdLinePathType` に `'free-bezier'` 追加。`BD_ANCHOR_NAMES` / `_bdGetAnchorPointByShape` / `_bdGetCardAnchorPoint` / `_bdConnAnchorName` / `_bdResolveControlPoints` / `_bdDefaultSelfLoopAnchors` / `_bdOppositeAnchor` / `_bdCubicBezierPoint` / `_bdFindNearestAnchor` / `_bdRenderFreeBezierEditOverlay` / `_bdBindCurveHandleDrag` / `_bdBindConnectionEndpointDrag` を追加。`_bdBuildConnectionPathData` に free-bezier 分岐を追加し、`bdDrawConns` 内でアンカー固定座標を解決 (未指定時は curve と同じ辺中央 + gap フォールバック)。兄弟オフセットは free-bezier では無効化。選択表示で free-bezier は 8 アンカー + 2 ハンドルオーバーレイを描画。ラベル描画は textVisible / textAlongPath で HTML ↔ SVG textPath を切替。ダブルクリックに修飾キー判定 + textVisible=false ラインは編集不可。path 要素に `id="bd-path-{connId}"` を付与。アクティブアンカーは `pointer-events: none` で端点ドラッグと共存。
- [app/gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) `bdFromMd` / `bdToMd` に fromAnchor / toAnchor / controlPoints / textVisible / textAlongPath / textAutoFlip / textShadowWidth を追加。pathType='free-bezier' の書き出し対応。JSON board 形式の読み込みにも新フィールド対応。
- [app/gb-canvas-engine.part03.js](app/gb-canvas-engine.part03.js) コピー&ペースト時の新フィールド追加。
- [app/gb-canvas-interact.js](app/gb-canvas-interact.js) 矩形選択にライン (connection) 混在対応。`selStart` に `baseSelConnIds` 追加。path サンプリングで矩形内判定。
- [app/gb-board-ui.part01.js](app/gb-board-ui.part01.js) `bdGetConnectionStyle` に textVisible / textAlongPath / textAutoFlip / textShadowWidth 追加。pathType 解決に `'free-bezier'` を加算。
- [app/gb-board-ui.part02.js](app/gb-board-ui.part02.js) `bdClearConnectionStyleOverrides` / `_bdUpdateConnectionFromField` / 詳細パネル displayStyle キーリストに新フィールド追加。pathType='free-bezier' への書き込み対応。
- [app/gb-board-style-manager.js](app/gb-board-style-manager.js) `_bdApplyStyleFieldChange` に free-bezier / textVisible / textAlongPath / textAutoFlip / textShadowWidth 処理追加。`_bdLineStyleSnapshotFromConnection` / customLineStyle 生成にも新フィールド追加。スタイル管理モーダルの line UI に Row4「テキスト表示 / 沿線 / 反転 / 縁取」追加。textVisible=false で他のテキスト系項目を半透明化。pathType セレクタに「ベジェ」追加。
- [app/gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) コンテキストメニュー「ライン形状」に「ベジェ」を追加。
- [app/gb-canvas-features.part02.js](app/gb-canvas-features.part02.js) コンテキストメニュー「ライン形状」/ 「子ライン形状」に「ベジェ」を追加。`bdEditConnLabel` の finish を拡張し、`conn._editingInline` をクリアして textPath モードに再描画。
- [app/gb-e2e-actions.js](app/gb-e2e-actions.js) pathType='free-bezier' 対応。
- [app/gb-tools.part03.css](app/gb-tools.part03.css) `.bd-anchor-candidate` / `.bd-anchor-candidate.bd-active` / `.bd-anchor-candidate.bd-active-to` / `.bd-curve-handle` / `.bd-curve-handle-line` / `.bd-conn-label-path` を追加。`.bd-conn-selection-end` を `pointer-events: all; cursor: grab` に変更。

**注記**: 計画書 Phase 0 の「詳細パネル基本タブでラインの文字色/背景色/枠線色変更が元に戻る」問題は、コード解析では再現経路が特定できなかったため保留中 (実機再確認待ち)。`docs/board-free-bezier-plan.md` も参照。

## v0.5.251 (2026-04-18)

### feat(board): 詳細パネルの編集を「カード/ライン個別のオーバーライド」に変更 + 上書き保存で共通スタイルに伝播

ボード上でカード/ラインを選択したとき、詳細パネルで変更した値が**そのカード/ライン単体にだけ**適用されるようにした。従来は共通スタイルを直接編集していたため、1 つのカードの色を変えると同じスタイルの全カードが変わってしまっていた。

- 詳細パネルの編集は node/conn の個別プロパティ (override) に書き込む。同じスタイルを使う他のカード/ラインには影響しない。
- 「選択中スタイルに上書き保存」ボタン (save アイコン) を押すと、個別オーバーライドを共通スタイルに伝播 + その node/conn のオーバーライドをクリア → 同じ共通スタイルを参照する他のカード/ラインにも反映される。
- リセットボタンは「このカード/ラインの個別設定をクリア」する動作に変更。共通スタイル自体はリセットしない (他のカード/ラインに影響させないため)。
- `shape` 空文字列 (`''` = rect) オーバーライドが効かないバグを修正: `bdGetNodeStyle` で `|| ''` falsy コアレスから `!== undefined` チェックに変更。

変更ファイル:

- [app/gb-board-ui.part02.js](app/gb-board-ui.part02.js) `_bdBindNodeDetailPanel` / `_bdBindConnectionDetailPanel`: `beforeEdit` コールバックを `() => node` / `() => conn` に変更 (個別オーバーライドに書き込む)。名前フィールドは `nameEditTarget` 経由で共通スタイルに書き込むよう分岐。リセットボタンは `bdClearCardStyleOverrides` / `bdClearConnectionStyleOverrides` のみ呼び出し、共通スタイル側は変更しない。
- [app/gb-board-style-manager.js](app/gb-board-style-manager.js) `_bdBuildStyleFields` に `nameEditTarget` オプション追加 (名前は常に共通スタイルに書き込む)。`_bdSaveCurrentNodeCardStyle` / `_bdSaveCurrentConnectionLineStyle` を刷新: node/conn の override キー (`_BD_CARD_OVERRIDE_KEYS` / `_BD_LINE_OVERRIDE_KEYS`) を共通スタイルにコピーしてから削除 → bdRender で全カード/ラインに反映。`_bdCardStyleSnapshotFromNode` に cloud パラメータ (`cloudBumpWidth` 等) を追加し、もやもや/雲/トゲ型のサイズ・ズラし量も「新スタイルとして保存」で正しく保存されるようにした。
- [app/gb-board-ui.part01.js](app/gb-board-ui.part01.js) `bdGetNodeStyle` の `shape` フィールドを `|| ''` から `!== undefined` チェックに変更 (空文字列 = rect も override として有効にする)。
- [app/gb-board-style-manager.js](app/gb-board-style-manager.js) `_bdApplyStyleFieldChange` の shape 処理で `value === 'rect' ? '' : value` 変換を撤去。node override の shape='rect' が `bdToMd` の `if (n.shape)` 真偽値チェックで永続化されない問題を解消 (徹底チェックで発見)。'rect' をそのまま格納し、レンダリング側は「if 分岐マッチなし = 既定 (rect) 扱い」という既存挙動で解釈する。

## v0.5.250 (2026-04-18)

### feat(board): 同じカード間の複数ライン対応 (相関図用) + 「もやもや」カード形状追加

**1. 同じカードペア間に複数ラインを許可**

相関図を描くため、同じカード A↔B 間に複数のラインを追加できるようにした。複数ライン時は各ラインを中心線から平行にズラして描画し、重ならないようにする。

- [app/gb-board-ui.part01.js](app/gb-board-ui.part01.js) `bdCreateConnection` の「既存ラインがあれば null を返す」制約を撤去 (自己ループ禁止は維持)。
- [app/gb-canvas-interact.js](app/gb-canvas-interact.js) `bdCanCreateConnection` の重複チェックも撤去し、`fromId === toId` のみ弾く。
- [app/gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) `_bdConnSiblingInfo()` を新設し、同じカードペア間 (方向違いも束ねる) のライン数 / インデックスを算出。`bdDrawConns()` の端点計算後に `(index - (count-1)/2) * SIBLING_GAP` 分だけ 分離オフセットを適用する。直線はライン方向に垂直な単位ベクトルで両端をシフト、L 字 (orthogonal) はカード出口方向の垂直軸に端点をシフト。曲線 (curve) は端点はカード側で固定し、`_bdBuildConnectionPathData` に `bulgeOffset` パラメータを追加して制御点 c1/c2 を垂直方向にシフトし、反対向きの同ペア接続が対向するカーブとして膨らむようにする (これで A→B と B→A の 2 本が「双方向矢印のように重なる」見た目にならず、レンズ型に分岐する)。
- [app/gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) 直線の perpendicular 符号を世界座標で canonical 化 (`perpY > 0` または `perpY === 0 && perpX > 0`)。逆方向接続 (from/to 反転) でも両端点が反対側にオフセットされるようにした。バグ修正: A→B と B→A の直線が offset と perpendicular の符号が両方反転して相殺し、同じ位置に重なる問題を解消。
- [app/gb-canvas-features.part02.js](app/gb-canvas-features.part02.js) 「親から切り離す」アクション 2 箇所を修正: 複数ラインが存在する場合、`bdRemoveConnection` が siblings を理由に親子 detach をスキップするため、`bdDetachParentChildRelation` を明示的に (idempotent に) 呼ぶようにした。徹底チェックで発見。

**3. テキスト編集中のカスタムキャレット表示を堅牢化**

v0.5.236 で導入したカスタムキャレット (`.bd-custom-caret`) が、条件によって表示されないケースがあったため、CSS にフォールバックを追加して必ず見えるようにした。

- [app/gb-tools.part03.css](app/gb-tools.part03.css) `.bd-custom-caret` に `min-width: 2px` (--bd-caret-width が 0 / 空でも必ず見える保証)、`height: 1em` (JS 未セット時のフォールバック)、`background` の `var()` フォールバックを `var(--fg, #000)` に二重化。
- [app/gb-tools.part03.css](app/gb-tools.part03.css) `.bd-node.bd-editing { overflow: visible; }` を追加。編集中のカードは `overflow: hidden` を解除してキャレットが clip されないようにする (雲 / トゲ / もやもや 以外の rect / 八角形カードでも有効)。

**4. カードメニューボタンの押下判定を修正**

矩形カードでカードメニューボタン (`.bd-card-menu-btn`) が「右端しか押せない」不具合を修正。

- [app/gb-tools.part03.css](app/gb-tools.part03.css) `.bd-card-menu-btn` の `z-index` を 8 → 11 に引き上げ、`.bd-text` (z-index: 9) の上に出す。カード選択中は `.bd-text` の `pointer-events: auto` が有効になるため、z-index が下だと bd-text にイベントを奪われてボタン左側が押せなくなっていた。

**5. 確認ダイアログ (cfConfirm / cfAlert / cfPrompt) を UI 共通ルールに統一**

`.modal` (大型ダイアログ殻、header/body/footer 分割の flex 構造) を流用していた cf 系ダイアログを、短い問いかけ用のコンパクト構造 `.gb-confirm` に統一。以下のルールを全 Meldex で徹底:

- ヘッダー / フッター分割を持たない 1 ブロック構造 (メッセージ + アクション行)
- 破壊的操作を示すキーワード (`削除` / `破棄` / `除去` / `消去` / `初期化` / `リセット` / `を空に` / `デフォルト.*戻`) を含むメッセージは自動で OK ボタンを `.gb-btn-danger` (赤系) に変更し、ラベルも「削除」/「実行」に切替。誤操作防止のため初期フォーカスはキャンセル側 + danger モードでは Enter ショートカットを無効化 (フォーカスされたボタンの自然な Enter 起動に任せる)。
- 既存の約 60 箇所の `cfConfirm` 呼び出しは引数を変えずに新スタイルへ反映される (後方互換)。
- `cfConfirm(msg, { danger, okLabel, cancelLabel })` で個別上書きも可能。

変更点:

- [app/gb-ui.part03.css](app/gb-ui.part03.css) に `.gb-confirm` / `.gb-confirm-message` / `.gb-confirm-actions` / `.gb-confirm-input` を新設。padding 16px + gap 12px のコンパクトレイアウト。
- [app/gb-app.part02.js](app/gb-app.part02.js) の `cfAlert` / `cfConfirm` / `cfPrompt` を `.gb-confirm` 構造に書き換え、ボタンを `.gb-btn gb-btn-sm + variant` に統一。`_buildCfDialogBody` も `.gb-confirm-message` クラス出力に変更。
- [app/gb-app.part02.js](app/gb-app.part02.js) `_cfIsDeleteMessage()` で削除系キーワードを自動検知。
- 見本ページ `D:\TM Dropbox\Share\ShareDevelop\Meldex開発\テスト\削除確認ダイアログサンプル\削除確認ダイアログ見本.html` で新旧比較可能。

**2. 「もやもや」カード形状を追加**

雲型の輪郭波々を「谷も山も滑らか」にした形状を追加。雲型が cubic Bezier を valley アンカーで接続して谷を尖らせるのに対し、もやもやは Catmull-Rom → Cubic Bezier 変換で全アンカー接線連続 (C1) にして谷まで丸くした。

- [app/gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) `BD_SHAPES` に `'fluffy'` を追加、`BD_SHAPE_LABELS['fluffy']='もやもや'`。
- [app/gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) `_bdFluffyClipPath()` を新規追加。半径 `r(θ) = rBase + amp * cos(numBumps * θ)` で角度方向に波打たせ、周上を `numBumps * 6` サンプルし、閉じた Catmull-Rom (張力 1/6) で C1 連続な Cubic Bezier パスを生成。小山設定時は主波の 2 倍周波の副波を重ねて輪郭をさらに揺らす。clip-path 判定・padding 判定・drop-shadow 判定すべてに `'fluffy'` を追加。
- [app/gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) / [app/gb-canvas-interact.js](app/gb-canvas-interact.js) clip-path 動的計算の dispatch で `_bdFluffyClipPath` を分岐。
- [app/gb-board-style-manager.js](app/gb-board-style-manager.js) カード形状パラメータ UI (山の幅 / 山の高さ / ズラし量 / 小山幅 / 小山高) の表示条件に `'fluffy'` を追加。雲型のパラメータを共有する。
- [app/gb-board-presets.js](app/gb-board-presets.js) `_bdCardPreviewShapeStyle` にプレビュー用 CSS (28 点の丸みを帯びたポリゴン + 楕円 border-radius) を追加。

## v0.5.249 (2026-04-18)

### feat(board): カード折りたたみボタン廃止 + 矩形選択色 / グループ色をスタイルタブで設定可能に

**1. カード上の折りたたみ・展開ボタンを廃止**

カードに常駐していた `.bd-collapse-btn` (右端の +/− 円形ボタン) を廃止し、コンテキストメニューの「構造」サブメニューに「子ノードを折りたたむ / 展開」項目を追加した。

- [app/gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) `colBtn` の生成ブロックを削除。`bd-collapsed` クラス自体は CSS の opacity 効果に使うため継続付与。
- [app/gb-canvas-features.part02.js](app/gb-canvas-features.part02.js) 構造サブメニューの先頭 (子ノード関連グループ) に `bdPushUndo()` 付きトグル項目を追加。
- [app/gb-tools.part03.css](app/gb-tools.part03.css) `.bd-collapse-btn` の CSS と各ツールモード時のカーソル指定を削除。

**2. 矩形選択色 / グループ色をスタイルタブで設定可能に**

これまで矩形選択 (.bd-select-rect) とグループ枠 (.bd-frame / .bd-frame-label) は `var(--accent)` 固定だったが、ボードのスタイルタブ「編集操作」セクションから個別に色を設定できるようにした。

- [app/gb-tools.part03.css](app/gb-tools.part03.css) `.bd-select-rect` を `--bd-select-rect-color` (border + 12% bg)、`.bd-frame` / `.bd-frame-label` を `--bd-group-color` (border + 6% bg + label color) に変更。両者ともデフォルトは `var(--accent)`。
- [app/gb-detail-panel.part01.js](app/gb-detail-panel.part01.js) `_FS_FIELDS.board.editOps` に `--bd-select-rect-color` (label: 矩形選択色) と `--bd-group-color` (label: グループ色) を追加。`_fsResolveSections.board` の編集操作 rows に「矩形選択 / グループ」の 2 行を追加。

CSS 変数は `#board-view` にセットされ、`#bd-world` 内の `.bd-select-rect` / `.bd-frame` に CSS 継承で届く。

## v0.5.248 (2026-04-18)

### fix(board): 楕円カードのメニューボタンを雲・トゲと同様にカード外右上に配置

v0.5.247 では楕円カードのメニューボタンを「右中央」に再配置して可視化したが、ユーザー要望により雲・トゲ等と同じ「カード外右上」に配置する方式に変更。

- [app/gb-tools.part03.css](app/gb-tools.part03.css) `.bd-node[data-shape="ellipse"] { overflow: visible; }` で楕円カードのクリッピングを解除。これでメニューボタンが標準位置 (top: 4px; right: 4px;) のまま、ellipse の外側 (bbox 右上角) に表示される。
- 画像カードでは `.bd-node[data-shape="ellipse"] .bd-img { border-radius: 50%; }` を追加し、image 自体を楕円形に切り抜くことで `overflow: visible` でも image が楕円外にはみ出さないようにする。
- 背景色は `background-clip: border-box` 既定 + `border-radius: 50%` により引き続き楕円形に保たれる。

## v0.5.247 (2026-04-18)

### fix(board): 楕円カードでカードメニューボタンが表示されない不具合を修正

楕円カードは `border-radius: 50%` + `overflow: hidden` により bbox 角の領域が visible 領域外でクリップされる。標準の `top: 4px; right: 4px;` 配置だとボタンが ellipse の外側に位置してほぼ全体が消えていた。

- [app/gb-tools.part03.css](app/gb-tools.part03.css) `.bd-node[data-shape="ellipse"] .bd-card-menu-btn` を追加し、`top: 50%; right: 8px; transform: translateY(-50%);` で右中央に再配置。これで小さい楕円 (40×28) でも大きい楕円 (200×80) でもボタン全体が ellipse の内接領域に収まる。

## v0.5.246 (2026-04-18)

### feat(board): 詳細パネルの共通タブ保持 + カード hover の矩形非表示 + メニューボタン明度調整

3 件のユーザー要望をまとめて反映。

**1. ボード要素クリック時に共通タブを保持**

これまでカード/接続/背景をクリックするたびに「基本」タブに戻されていた。例: 「スタイル」タブを開いていても、別のカードをクリックすると「基本」に戻ってしまう。

- [app/gb-board-ui.part01.js](app/gb-board-ui.part01.js) `_bdResolveCurrentBoardTab(supportable)` を新設。`file-style` / `backlinks` (ボード横断で常に利用可能な共通タブ) は選択タイプに関わらず保持し、その他の board-* タブは supportable に渡されたものだけ保持する。
- `_bdCurrentNodeBoardTab` は新ヘルパー経由で `['board-basic', 'board-layout', 'board-advanced', 'board-note']` を保持。
- [app/gb-board-ui.part02.js](app/gb-board-ui.part02.js) `_bdSetBoardPrimaryTab` (接続線 / 複数選択 / 背景クリックで使用) も `switchDetailTab('board-basic')` 強制をやめ、新ヘルパーで `['board-basic']` を渡す。これで `file-style` / `backlinks` を開いた状態で接続線をクリックしてもタブは保持される。

**2. カード hover 時の矩形 (box-shadow) を非表示**

カードにカーソルオーバーした際、カードメニューボタン (.bd-card-menu-btn) の表示と同時に 1px 枠線 (`.bd-node:hover { box-shadow: 0 0 0 1px var(--fg2); }`) も出ていた。これを除去し、hover フィードバックはメニューボタンの表示のみに統一。`z-index: 5` (前面化) は維持。

- [app/gb-tools.part03.css](app/gb-tools.part03.css) `.bd-node:hover` から `box-shadow` を削除。

**3. カードメニューボタン「...」の明度をラインメニューボタンと揃える**

カードメニューボタンが `color: var(--fg2)` (薄い灰) でラインメニューボタン (`.bd-conn-action-btn` の `color: var(--fg)`) より暗かったので明度を揃えた。背景色も `--bg2 92%` から `--bg2 96%` に揃えた。

- [app/gb-tools.part03.css](app/gb-tools.part03.css) `.bd-card-menu-btn` の `color` を `var(--fg2)` → `var(--fg)`、background を 92% → 96% に変更。

## v0.5.245 (2026-04-18)

### fix(board): 選択ハイライト矩形の同期漏れ 2 件を修正（徹底チェック）

v0.5.244 で導入した `.bd-selection-rect` (選択ハイライト矩形) が、編集開始/終了時と階層レイアウト後に同期されない問題を修正。

- **`bdEditNode` / `bdFinishEdit` で `bdSyncResizeHandles` を呼ぶように変更** ([app/gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js))。これまでは `bd-editing` クラスを付け外ししても `.bd-selection-rect.is-editing` クラスが同期されず、編集中も矩形が表示されたままだった。
- **`bdAutoLayout` の最後に `bdSyncResizeHandles` を呼ぶように変更** ([app/gb-canvas-features.part01.js](app/gb-canvas-features.part01.js))。マインドマップ等の階層レイアウト後、選択中カードが動いても矩形が古い位置に残る問題を修正。

その他、徹底チェックで確認済みの観点（regression なし）:
- `bdSyncResizeHandles` の他の呼び出し元（drag / resize / box-select / bdSelect / bdClearSelection / 各 bdRender 経由）はすべて意図通り動作
- 旧 `.bd-selection-ring` 参照は CHANGELOG 内のみ。アクティブコードはコメントとコンテナ内ノード用 `chRing` のみで意図通り
- コンテナ内ノードは `.bd-inner-nodes .bd-node.bd-selected .bd-selection-ring` で内側オーバーレイを維持
- `#bd-resize-layer` の `pointer-events: none` + `.bd-selection-rect { pointer-events: none }` で card クリックに干渉しない
- `BD_SHAPES` に含まれる形状 (rect / ellipse / cloud / thorn / thorn-curve / octagon) すべてで矩形枠が bbox + 4px に表示される

## v0.5.244 (2026-04-18)

### feat(board): カード選択ハイライトを形状非依存の矩形枠に変更

これまでカードの選択ハイライト (`.bd-selection-ring`) は、カード本体の `clip-path` を継承して形状内側に選択色を 70% 不透明度で重ねるオーバーレイだった。雲・トゲ・八角・ダイヤなど形状を持つカードでも形状に追従していたが、輪郭が複雑なときに選択範囲が分かりにくかった。

形状に関わらずカード bbox の周囲に余白付きの矩形枠を出す方式に変更した。

- [app/gb-tools.part03.css](app/gb-tools.part03.css) `.bd-selection-rect { border: 2px solid var(--bd-selection-color, #4a90d9); border-radius: 4px; ... }` を新設。`.bd-selection-ring` は `display: none !important;` でデフォルト非表示。
- [app/gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) `bdSyncResizeHandles` を拡張し、リサイズハンドルと並行して `.bd-selection-rect` を `#bd-resize-layer` 内に兄弟要素として描画。`node.x - 4 / node.y - 4 / offsetWidth + 8 / offsetHeight + 8` で bbox の外側 4px に矩形枠を配置する。`node.contained` (コンテナ内ノード) はワールド座標と関係しないため対象外。`bd-editing` 中の cardは `is-editing` で非表示にする。
- 旧 `.bd-selection-ring` の埋め込みコードと、`_bdApplyCloudShape` での ring 用 clipPath 同期を削除（不要になったため）。
- コンテナ内ノード (`node.contained`) のみ、従来の `.bd-selection-ring` インライン埋め込みを維持し、`.bd-inner-nodes .bd-node.bd-selected .bd-selection-ring` セレクタで内側オーバーレイ表示を復活。

## v0.5.243 (2026-04-18)

### fix(board): カードスタイル管理ダイアログの閉じるボタンが消えていなかった不具合を修正

v0.5.241 で `.bd-style-modal-close { display: none; }` を追加したが、`.gb-btn { display: inline-flex; }` (gb-ui.part01.css、同じ specificity 0,1,0 で後ロード) に上書きされて非表示にならず、画面右上に小さなボタンが残っていた。

- [app/gb-tools.part03.css](app/gb-tools.part03.css) `.bd-style-modal-close { display: none !important; }` に修正。CSS 設計上は specificity を上げる方が望ましいが、ボタン要素を完全に隠す意図を明確化するため `!important` を採用した。

## v0.5.242 (2026-04-18)

### feat(board): カードのコンテキストメニューから「階層別スタイルに戻す」を選択可能に

これまでは、カードに個別カードスタイルを適用すると、ルートに `_autoStyle: true` が付いていても階層別スタイルに戻す手段がコンテキストメニュー側になく、構造サブメニューでルートを操作する必要があった。これを解消。

- [app/gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) `_bdRestoreCardToHierarchy(nodeIds)` を新設。`bdPushUndo` の後、対象ノードの `cardStyle` を空にし、`bdClearCardStyleOverrides` で per-node 視覚 override を削除、さらに `_userBgColor` / `_userFontSize` / `_userFontBold` / `_userW` フラグを削除して `bdApplyAutoStyle` の上書き対象になるようにする。対象ノードからルートまで遡り、ルートに `_autoStyle: true` があれば `bdApplyAutoStyle` を再実行して深さ別スタイルを反映する。
- [app/gb-canvas-features.part02.js](app/gb-canvas-features.part02.js) カードスタイル サブメニューの先頭に「階層別スタイルに戻す」項目を追加。`nd.cardStyle` が空のとき active 表示。クリックで `_bdRestoreCardToHierarchy(targetNodeIds)` を呼ぶ。区切り線を挟んだ後に既存のカードスタイル一覧を続ける。
- 同サブメニュー一覧から、ノード/接続単位の自動 fork (`card-custom*`) を `_bdIsCustomStyleId('card', id)` で除外し、スタイル管理ダイアログの一覧と整合させた。
- ルートに `_autoStyle: true` が付いていない場合は「個別スタイルを解除しました（ルートカードで「階層別スタイル」を有効にすると深さ別スタイルが反映されます）」と showStatus で誘導する。

## v0.5.241 (2026-04-18)

### fix(board): カードスタイル管理ダイアログの右上の閉じるボタンを非表示に

ユーザー要望により、右上の閉じるボタン (`.bd-style-modal-close`) を `display: none` で視覚的に非表示にした。閉じる操作は ESC キー / overlay (背景) クリックで行う。E2E テスト ([app/gb-e2e-actions-extra.js](app/gb-e2e-actions-extra.js)) が `[data-bd-modal-close]` を querySelector + click() しているため、DOM 要素自体は残す (display: none でも JS 経由の click() は動く)。

## v0.5.240 (2026-04-18)

### fix(board): カードスタイル管理ダイアログのサイズと一覧プレビューを再調整

v0.5.239 のレビューで挙がった「ダイアログがまだ大きい」「リストプレビューの中身が枠いっぱいでない」を反映。

- **ダイアログ全体を縮小** — `min/max-width` を `960px` から `620px` に、`min-height` を `560px` から `380px` に、`max-height` を `88vh` から `50vh` に短縮。左カラム 240px + 右カラム 320px + 内側余白で 620px に収まる。
- **左カラムを必要サイズに** — `grid-template-columns` を `1fr 360px` から `240px 320px` に変更。リスト幅は 96px プレビュー枠 + 名前 + ハンドル + 余白で必要十分な幅に。
- **リストプレビューの中身を枠いっぱいに** — `.bd-style-list-preview .bd-style-card-preview` を `width: 100%; height: 100%; min-width: 0; min-height: 0; font-size: 16px;` に変更。`bd-style-card-preview` の既定 26×16px が 84×42px の枠内で小さく見えていた問題を解消。SVG ライン プレビューも同様に枠いっぱいまで広げる。

CSS のみ・E2E 互換セレクタは維持。

## v0.5.239 (2026-04-18)

### fix(board): カードスタイル/ラインスタイル管理ダイアログのレイアウトを調整

v0.5.238 のレビューで挙がった見た目・操作性の指摘 5 件を反映。

- **閉じるボタンの × アイコンを確実に表示** — `_bdIcon('x', 14)` が `LUCIDE.x` 未定義時に空文字を返すケースを避け、`lucide('x', 16)` を優先・SVG 直埋めをフォールバックに変更。`.bd-style-modal-close { padding: 0; color: var(--ui-fg-default); }` でアイコン色も明示。
- **左カラムをリスト内スクロールに** — body に `modal-body` クラスを追加し、`gb-modal-shell` の自動 wrap を回避。`.modal.bd-style-modal .bd-style-modal-body` から `.bd-style-list-items` まで flex column + min-height: 0 の伝播チェーンを構築し、リストだけがスクロール、エディタの操作ボタンは常に見える状態にした。
- **右カラムの幅を半分くらいに** — `.bd-style-manager` の `grid-template-columns` を `240px minmax(0, 1fr)` から `minmax(0, 1fr) 360px` に変更。左カラムが広く、右カラム (編集パネル) が 360px で固定される。
- **右カラムプレビューを枠いっぱいに拡大** — `.bd-style-editor-preview` を `min-height: 120px` + `align-items: stretch` にし、内部の `.bd-style-card-preview` を `width: 100%; height: auto; min-height: 96px; font-size: 32px;` に拡大。ライン SVG も同様に枠いっぱいまで広げる。`bd-depth-style-manager` は :not() で除外し、固定サイズの large preview を温存。
- **リストプレビューを詳細パネル基本タブと同サイズ (96×54px) に** — `.bd-style-list-preview` を `width: 96px; height: 54px; padding: 6px;` + 枠線・背景色を付与し、`.bd-detail-style-trigger-preview` (詳細パネル) と完全一致させた。

CSS のみの変更 + JS は閉じるボタンと body クラスの 2 箇所のみ。E2E 互換のセレクタは維持。

## v0.5.238 (2026-04-18)

### feat(board): カードスタイル / ラインスタイル管理ダイアログの動作・UI・UXを改善

カードスタイル管理ダイアログ ([app/gb-board-style-manager.js](app/gb-board-style-manager.js)) の精査で発見された問題を一括修正。ラインスタイル管理ダイアログも同じ実装を共有しているため両方に反映される。

**A. 重大な動作問題**
- **削除確認ダイアログを追加** — `削除` ボタンが即座にスタイルを削除していた問題を修正。`cfConfirm` で確認ダイアログを表示し、使用中のカード / ライン数も併せて告知する。
- **ESCキーで閉じる対応** — `_bdModalShell` に capture フェーズの ESC ハンドラを追加。子モーダル (cfPrompt 等) が開いている時は最上位のもののみが閉じるようにし、複数モーダルの取り違えを防ぐ。
- **Undo/Redo を全操作に拡大** — 追加・複製・削除・並べ替え・適用・名前変更・各フィールド変更すべての commit 時点で `bdPushUndo()` を呼ぶように変更。これまでは「デフォルトに戻す」だけが undo 対象だった。

**B. 一覧の不具合**
- **「カスタム」スタイルを一覧から除外** — `card-custom` / `card-custom-<nodeId>` / `line-custom` / `line-custom-<connId>` (詳細パネル編集時に内部的に作られる fork) を一覧から除外。同名「カスタム」が複数並んで識別不能になる問題を解消。`_bdIsCustomStyleId(kind, id)` / `_bdDisplayedManagedStyles(kind)` を新設。
- **適用中スタイルの視覚マーカーを追加** — 一覧の各項目に「適用中」バッジ + 左端アクセント帯 (`is-applied` クラス) を表示。「このスタイルを選択」ボタン (旧名) を「適用する」に変更し、既に適用中なら「適用中」表示で disabled。
- **詳細パネルとの同期** — 追加・複製・削除・並べ替え・適用・リセット・各フィールド変更後に `bdRefreshSelectionDetails(true)` を呼び、詳細パネル側のスタイルピッカー表示を最新化。

**C. 名前検証 / 連番**
- **名前は trim 必須・空文字不可** — 空白だけ・空文字を入力した場合はコミットせず、再描画で旧値に戻す。
- **追加 / 複製時の自動連番** — `_bdMakeUniqueStyleName` で既存スタイルと衝突しない名前 (`新しいカードスタイル 2`、`標準 コピー 3` 等) を生成。

**D. UI 統一**
- **ボタンクラスを `gb-btn` 系に統一** — 「複製」「削除」「リセット」「適用」「追加」を `gb-btn gb-btn-sm` ベース + variant (`gb-btn-primary` / `gb-btn-danger`) で統一。
- **ヘッダー閉じるボタンを × アイコン化** — `gb-btn-ghost gb-btn-icon` で他モーダルの慣例に揃える。
- **D&D 並べ替え対応** — 階層別スタイルマネージャと同じパターンで、リスト項目をドラッグして順序入れ替え可能に。順序はツールバーのスタイルピッカーの並びにも反映される。
- **使用状況メタ表示** — エディタ右側に「<スタイル名> ・ N 個のカードが使用中 ・ 現在使用中」を表示し、削除前の影響範囲を即座に確認できるようにした。
- **プレビュー領域を拡大** — `min-height: 76px` → `96px` + `padding: 12px`。形状 (雲 / 八角 / トゲ) の差異を視認しやすくした。

**E. キーボード操作**
- リスト項目を `tabindex="0"` + `role="button"` にし、Enter / Space で選択可能に。

**E2E 互換**
- `[data-bd-modal-close]` / `[data-bd-style-duplicate]` / `[data-bd-style-use]` / `[data-bd-style-delete]` / `[data-bd-style-add]` / `[data-bd-style-reset]` / `[data-bd-style-editor]` / `[data-bd-style-field]` / `.bd-style-modal` / `.bd-style-modal-body` の各セレクタは維持。

## v0.5.237 (2026-04-18)

### fix(board): スタイルタブのボード背景色がキャンバスに反映されない不具合を修正

既存実装で `canvas.style.background = bd._bgColor` の inline style が CSS 変数 `--bd-bg` より優先されるため、スタイルタブで `--bd-bg` を変更してもキャンバスに反映されなかった。

- [gb-detail-panel.part01.js:402](app/gb-detail-panel.part01.js#L402) `--bd-bg` に `applyCustom: 'bgColor'` を追加。
- [gb-detail-panel.part01.js:516-527](app/gb-detail-panel.part01.js#L516) frontmatter adapter の `applyCss` に `bgColor` 分岐を追加。CSS 変数セットに加えて `bd._bgColor` と `canvas.style.background` と `#bd-bg-swatch` (ツールバーのスウォッチ) を同期し、`localStorage` にも保存。ツールバーのスウォッチ経路とスタイルタブ経路が双方向に整合。

## v0.5.236 (2026-04-18)

### feat(board): スタイルタブに影トグル/影の色/カーソル太さを追加、ラインにも影を適用

4 項目の要望を対応:

**1. 影のオン・オフをスタイルタブで切替**
- [gb-detail-panel.part01.js:402-404](app/gb-detail-panel.part01.js#L402) `_FS_FIELDS.board.display` に `{ key: '--bd-shadow', type: 'toggle', on: '1', off: '', applyCustom: 'shadow' }` を追加。
- [gb-detail-panel.part01.js:506-515](app/gb-detail-panel.part01.js#L506) `_fsGetFrontmatterAdapter` の `applyCss` に `applyCustom === 'shadow'` 分岐を追加。CSS 変数セットに加えて `bd._showShadow` と同期 + `bdRender` / `bdDrawConns` を呼ぶ。

**2. 影がオンの時にラインにも影**
- [gb-canvas-engine.part01.js:1091-1094](app/gb-canvas-engine.part01.js#L1091) `bdRender` で `#bd-canvas` に `bd-shadow-on` クラスをトグル。
- [gb-tools.part03.css:99](app/gb-tools.part03.css#L99) `#bd-canvas.bd-shadow-on #bd-svg { filter: drop-shadow(...) }` で SVG 全体に影を適用 (ライン・矢印・選択ハンドルすべてに影がつく)。

**3. 影の色を設定可能に**
- `_FS_FIELDS.board.display` に `--bd-shadow-color` を追加。
- [gb-canvas-engine.part01.js:1185-1198](app/gb-canvas-engine.part01.js#L1185) カードの drop-shadow / box-shadow の色を `var(--bd-shadow-color, rgba(0,0,0,0.25))` 参照に変更。ラインの drop-shadow も同じ変数を参照。

**4. カーソル太さ (カスタムキャレット)**
- ブラウザ contentEditable のネイティブキャレットは太さ変更不可のため、カスタムキャレット (`.bd-custom-caret`) をカード要素内に absolute で配置する実装に切替。
- [gb-canvas-engine.part02.js:648-675](app/gb-canvas-engine.part02.js#L648) `bdEditNode` で selection の rect からキャレット位置を計算、`selectionchange` で追従更新。`bdFinishEdit` でリスナー解除 + 要素削除。
- [gb-tools.part03.css:87-98](app/gb-tools.part03.css#L87) `.bd-custom-caret` の CSS を追加 (幅 `var(--bd-caret-width, 2px)`、色 `var(--bd-caret-color, var(--fg))`、blink animation)。編集中のテキスト `caret-color: transparent` でネイティブキャレットは非表示。
- [gb-detail-panel.part01.js:409](app/gb-detail-panel.part01.js#L409) `_FS_FIELDS.board.editOps` に `--bd-caret-width` (pxtext) を追加。

**読込時同期**
- [gb-canvas-engine.part03.js:154-158](app/gb-canvas-engine.part03.js#L154) ボード読込後、`bd._fileStyle['--bd-shadow']` があれば `bd._showShadow` を同期。

## v0.5.235 (2026-04-18)

### refactor(board): スタイルタブの「ノード背景」フィールドを削除

`--bd-node-bg` は `.bd-node` の background のフォールバック値 ( `var(--bd-node-bg, var(--bg4))` ) として使われていたが、実際の各カードはカードスタイル / ノード個別値 (inline style) が優先されるため、このフィールドが実効する機会がほぼなく、UI の混乱要因になっていたので削除。

- [gb-detail-panel.part01.js:400-404](app/gb-detail-panel.part01.js#L400) `_FS_FIELDS.board.display` から `--bd-node-bg` を削除。
- [gb-detail-panel.part01.js:720-727](app/gb-detail-panel.part01.js#L720) `_fsResolveSections.board` の 表示 rows の pick から `--bd-node-bg` を削除。label `'全体'` も項目が 1 つのみになったので外す。

## v0.5.234 (2026-04-18)

### fix(board): スタイルタブのカーソル色 / ライン色整理 / 背景色の一本化

ユーザーからの 3 点指摘を対応:

**1. スタイルタブにカーソル色が表示されない**
- v0.5.232 で `_FS_FIELDS.board.editOps` に `--bd-caret-color` を追加したが、セクション定義側 `_fsResolveSections.board` の `pick([...])` に入れ忘れていたため UI に現れていなかった。
- [gb-detail-panel.part01.js:729-732](app/gb-detail-panel.part01.js#L729) `編集操作` rows に `--bd-caret-color` を追加。

**2. スタイルタブの「ライン色」の意味**
- `--bd-line-color` は `_FS_FIELDS.board.display` に定義されていたが、Meldex のスタイルシート / JS のどこからも参照されていないデッドフィールドだった。
- [gb-detail-panel.part01.js:400-405](app/gb-detail-panel.part01.js#L400) `_FS_FIELDS.board.display` から削除。`_fsResolveSections.board` の `pick([...])` からも削除。

**3. 基本タブの「共通設定」(背景色) をスタイルタブに移動**
- [gb-board-ui.part02.js:199-203](app/gb-board-ui.part02.js#L199) ボード詳細パネル (基本タブ) の HTML から「共通設定」セクションを削除。
- [gb-tools.part03.css:39](app/gb-tools.part03.css#L39) `#bd-canvas` の CSS に `background: var(--bd-bg, var(--bg))` を追加。スタイルタブの「ボード背景」(`--bd-bg`) が直接反映されるように。

## v0.5.233 (2026-04-18)

### fix(board): 雲/トゲ型カードの背面に矩形の影が見える不具合を修正

`box-shadow` は box の bounding box に沿って描画されるため、clip-path で形状を切り抜いた雲/トゲ型カードでは形状の外側に矩形の影が漏れて見えていた。 clip-path 系形状では box-shadow の代わりに `drop-shadow` フィルターを使用し、形状追従の影に変更。

- [gb-canvas-engine.part01.js:1181-1195](app/gb-canvas-engine.part01.js#L1181) `nodeStyle.shadow` 適用時、shape が clip-path 系 (`cloud` / `thorn` / `thorn-curve` / `hexagon` / `octagon` / `star` / `diamond`) の場合は `--bd-base-filter` に `drop-shadow(0 8px 16px rgba(0,0,0,0.25))` を追加。それ以外の形状は従来通り `box-shadow`。

## v0.5.232 (2026-04-18)

### fix(board): 選択色 / カーソル色を本来の「スタイルタブ」で設定可能に

ユーザーから「スタイルタブに入れてと言っている」との指摘を受け、詳細パネル内のタブ UI である `detail-tab-file-style` (スタイルタブ) に項目を追加するのが正しいことを確認。v0.5.231 で「共通設定」セクションに独立フィールドとして追加した実装を撤回し、既存の `_FS_FIELDS.board.editOps` 経路に統一。

- [gb-detail-panel.part01.js:406-409](app/gb-detail-panel.part01.js#L406) `_FS_FIELDS.board.editOps` に `{ key: '--bd-caret-color', label: 'カーソル色', type: 'color' }` を追加。既存の `--bd-selection-color` (選択色) と並んでスタイルタブに表示される。
- [gb-board-ui.part02.js:203-206](app/gb-board-ui.part02.js#L203) 共通設定セクションから選択色 / カーソル色項目を削除 (背景色のみに戻す)。
- [gb-board-ui.part02.js:697-724](app/gb-board-ui.part02.js#L697) ハンドラーを bgColor 専用に戻す。
- [gb-canvas-engine.part01.js:12-19](app/gb-canvas-engine.part01.js#L12) `bd.selectionColor` / `bd.caretColor` の独立プロパティを撤回。`_bdApplyBoardSelectionVars()` ヘルパーも削除。
- [gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) / [gb-canvas-engine.part03.js](app/gb-canvas-engine.part03.js) boardUi: ブロックからの `selectionColor` / `caretColor` 読み書きを削除。
- [gb-canvas-engine.part02.js:498-501](app/gb-canvas-engine.part02.js#L498) ライン描画で `bd._fileStyle['--bd-selection-color']` を参照するよう変更。

スタイルタブの既存フレームワーク (`_FS_FIELDS[ctx]` + `_fsGetFrontmatterAdapter`) が `bd._fileStyle` に保存 / `#board-view` 要素に CSS 変数セットを行うため、カード選択ハイライト (`.bd-selection-ring`) も編集時 `::selection` / `caret-color` も継承経由で反映される。

## v0.5.231 (2026-04-18)

### refactor(board): 選択色 / カーソル色をボード全体の設定に統一

カードスタイル / ラインスタイル単位で持っていた `selectionColor` / `caretColor` を廃止し、ボードファイル単位の `bd.selectionColor` / `bd.caretColor` に統一。ボード全体で 1 つの選択色 / カーソル色という設計に整理。

- [gb-canvas-engine.part01.js:12-29](app/gb-canvas-engine.part01.js#L12) `bd` の初期化に `selectionColor` / `caretColor` を追加。`_bdApplyBoardSelectionVars()` ヘルパーで `#bd-canvas` の CSS 変数 `--bd-selection-color` / `--bd-caret-color` にセット (子孫カードに継承)。
- [gb-canvas-engine.part01.js:313-316, 585-587](app/gb-canvas-engine.part01.js#L313) マークダウン FM の `boardUi:` ブロックに `selectionColor` / `caretColor` の読み書きを追加。
- [gb-canvas-engine.part03.js:151-154](app/gb-canvas-engine.part03.js#L151) ボード読込時に bd に流し込み、`_bdApplyBoardSelectionVars()` を呼ぶ。
- [gb-board-ui.part02.js:204-206](app/gb-board-ui.part02.js#L204) 詳細パネル (選択なし時) の「共通設定」セクションに `_bdColorFieldHtml` で選択色・カーソル色の項目を追加。
- [gb-board-ui.part02.js:697-742](app/gb-board-ui.part02.js#L697) 既存の `data-bd-board-color-field` / `data-bd-board-reset-field` ハンドラを field 名で分岐するよう拡張 (bgColor / selectionColor / caretColor)。
- [gb-board-style-manager.js:467-469](app/gb-board-style-manager.js#L467) カードスタイル UI から「選択」「編集時」Row を削除。ラインスタイルからも「選択色」を削除。
- [gb-board-ui.part01.js:189-191, 244-246](app/gb-board-ui.part01.js#L189) `bdNormalizeCardStyles` / `bdNormalizeLineStyles` から `selectionColor` / `caretColor` を削除。旧データに残っていても無視。
- [gb-board-ui.part01.js:282-300](app/gb-board-ui.part01.js#L282) `bdGetNodeStyle` から両フィールドを削除。
- [gb-canvas-engine.part01.js:1121-1123, 1315](app/gb-canvas-engine.part01.js#L1121) bdRender のカード div / 内包子ノード への CSS 変数セットを削除 (bd-canvas 継承に一本化)。
- [gb-canvas-engine.part02.js:645-648](app/gb-canvas-engine.part02.js#L645) `bdEditNode` 内の CSS 変数セットを削除。
- [gb-canvas-engine.part02.js:498-500](app/gb-canvas-engine.part02.js#L498) ライン選択表示は `connStyle.selectionColor` 参照から `bd.selectionColor` 参照に変更。

## v0.5.230 (2026-04-18)

### revert(board): カード選択ハイライトを内側半透明オーバーレイ方式に戻す

v0.5.229 で採用した drop-shadow 8 方向の外枠方式が意図と合わなかったため、v0.5.228 までの「形状内側に半透明色を重ねる」方式に戻す。編集中の選択ハイライト非表示ルールと雲/トゲの `--bd-base-filter` 管理は維持。

- [gb-tools.part03.css:67-85](app/gb-tools.part03.css#L67) `.bd-selection-ring` を復活させ (`display: none` を削除)、`.bd-node.bd-selected` の drop-shadow 8 方向ルールを除去。`background: color-mix(... 70%, transparent)` で選択色を 70% 不透明度で内側に重ねる表示に戻す。
- `.bd-node.bd-editing .bd-selection-ring { opacity: 0 !important; }` で編集中は非表示。

## v0.5.229 (2026-04-18)

### feat/fix(board): 選択ハイライトを外枠化、カーソル設定 UI 明確化、検索バー改善

**1. スタイル UI の「編集時」セクションを独立化**:
- ユーザーに見えていなかった `caretColor` 設定を独立 Row + 「編集時」ラベルで明示。
- [gb-board-style-manager.js:476-484](app/gb-board-style-manager.js#L476) `_bdBuildStyleFields` の UI を再構成。選択色は「選択」行、カーソル色は「編集時」行に分離。

**2. 選択ハイライトを外枠方式に変更**:
- [gb-tools.part03.css:67-92](app/gb-tools.part03.css#L67) `.bd-selection-ring` を廃止し、`.bd-node.bd-selected` に `filter: drop-shadow × 8 方向` で形状追従の外枠を描く。枠色は選択色を 70% 不透明度で color-mix。
- 雲/トゲ形状などの既存 inline `filter` を CSS 変数 `--bd-base-filter` 経由で管理することで CSS の選択時 filter と合成可能に。[gb-canvas-engine.part01.js:1162-1167](app/gb-canvas-engine.part01.js#L1162)

**3. 編集中はカード選択ハイライトを非表示**:
- [gb-tools.part03.css:91-92](app/gb-tools.part03.css#L91) `.bd-node.bd-editing.bd-selected` は base filter のみで描画するルールを追加。編集中の視覚ノイズを除去。

**4. 検索バー位置を少し上に**:
- [gb-tools.part03.css:101](app/gb-tools.part03.css#L101) `.bd-find-bar` の `top` を 50px → 8px に変更。ツールバー直下へ。

**5. ボード pane アクティブ時の Ctrl+F は bd 検索バーを開く**:
- 以前は詳細パネル等でも Ctrl+F で `file-search-bar` (ノート用検索) が開き、対象コンテンツがないため機能しなかった。
- [gb-shortcuts.js:339-354](app/gb-shortcuts.js#L339) `global.search` でアクティブ pane のタブ種別をチェック。ボードなら `bdOpenFindBar('find')` を優先、それ以外は従来の `openFileSearch()`。

## v0.5.228 (2026-04-18)

### fix(board): 検索バーが操作できない/ハイライトがフチに隠れる不具合を修正

v0.5.223 で導入した検索バーで 2 件の致命的な不具合を修正。ユーザーが「操作できない / 機能していないように見える」と指摘していた問題の根本原因。

**操作不能の原因**:
- 検索バーは `bd-canvas` の子要素として appendChild されているため、入力欄やボタンのクリック/pointerdown がバブリングで canvas の `onCanvasPointerdown` ハンドラーに届いてしまい、フォーカスが入力欄に移らず範囲選択などが始まっていた。
- [gb-canvas-features.part01.js:443-446](app/gb-canvas-features.part01.js#L443) 検索バーのルート要素で `pointerdown` / `mousedown` / `click` / `dblclick` / `wheel` / `contextmenu` を `stopPropagation` するよう追加。

**ハイライトがテキストフチに隠れる**:
- カードのテキストフチ (text-shadow 多方向重ね) がハイライトの `<mark>` 背景を視覚的に覆っていた。
- [gb-tools.part03.css:109-111](app/gb-tools.part03.css#L109) `.bd-find-hl` / `.bd-find-hl-current` に `text-shadow: none !important` を追加して、マッチ部分のフチだけ抑止。ハイライトが明確に見えるように。

## v0.5.227 (2026-04-18)

### fix(board): 選択色の 70% 不透明度化 / カーソル色が反映されない不具合を修正

**選択色を 70% 不透明度で表示**:
- [gb-tools.part03.css:67-82](app/gb-tools.part03.css#L67) `.bd-selection-ring` と編集時 `::selection` の background を `color-mix(in srgb, var(--bd-selection-color, #4a90d9) 70%, transparent)` に変更。純色を保存しつつ表示時に 70% 不透明度に落とす。
- [gb-canvas-engine.part02.js:261-274](app/gb-canvas-engine.part02.js#L261) `_bdToSelectionRgba(color)` を追加。SVG の stroke 属性は `color-mix` を確実にサポートしないため、ライン選択色は JS 側で `#RGB/#RRGGBB → rgba(r,g,b,0.7)` に変換して適用。

**カーソル色がスタイル変更後に反映されない不具合を修正**:
- 原因: スタイル UI の `onChange` は詳細パネル再描画のみで `bdRender` は呼ばれないため、カード div の CSS 変数 (`--bd-caret-color`) が更新されないまま編集が始まっていた。
- [gb-canvas-engine.part02.js:645-655](app/gb-canvas-engine.part02.js#L645) `bdEditNode` で編集開始時に必ず `--bd-selection-color` / `--bd-caret-color` を再セットするよう復元 (v0.5.225 で一旦削除したが、bdRender 未経由のケースで古い値が残る問題を解消)。
- [gb-canvas-engine.part01.js:1310-1313](app/gb-canvas-engine.part01.js#L1310) 内包子ノード (`chDiv`) にも選択色・カーソル色の CSS 変数をセット。従来抜けていたため、コンテナ内カードで設定色が反映されなかった。

## v0.5.226 (2026-04-18)

### feat(board): 検索ワードだけをハイライトするよう変更 (カード全体選択を廃止)

従来はマッチしたカードを `bd.selected` に設定してカード全体を選択ハイライトしていたが、ユーザーが元から選択していた状態を壊してしまう点 / ワード単位で見つけにくい点が不自然だったため、マッチしたワード部分だけを `<mark>` でハイライトする方式に変更。

- [gb-canvas-features.part01.js:493-513](app/gb-canvas-features.part01.js#L493) `_bdFindShowCurrent` / `_bdFindClearSelection` を廃止し、`_bdApplyFindHighlight(q)` + `_bdFindScrollToCurrent()` に分離。
  - `_bdApplyFindHighlight(q)` はカード `.bd-text` とライン `.bd-conn-label` の innerHTML を書き換え、マッチ部分を `<mark class="bd-find-hl">` で囲む。現在フォーカス位置は `.bd-find-hl-current` を追加して別色で強調。q が空のとき `applyAutoLinks` 経由で通常表示に戻す (auto-link も復活)。
  - `_bdFindScrollToCurrent()` は `bdFocusSelected` のようにズームしない。現在マッチのノードがビュー外なら panX/Y のみ調整してビュー内に収める。
  - カード / ラインの `bd.selected` / 接続選択には手を出さない (検索中もユーザーの既存選択を保持)。
- [gb-canvas-features.part01.js:436-440](app/gb-canvas-features.part01.js#L436) `bdCloseFindBar` でハイライトを `_bdApplyFindHighlight('')` で解除するよう追加。
- [gb-tools.part03.css:108-110](app/gb-tools.part03.css#L108) `.bd-find-hl` (薄黄色) と `.bd-find-hl-current` (オレンジ色) の CSS を追加。

## v0.5.225 (2026-04-18)

### fix(board): 選択色 / カーソル色をスタイルタブから正しく反映

v0.5.221 で追加した `editSelectionColor` / `editCaretColor` はフィールド名・意味づけが曖昧で、「カード選択ハイライトがスタイルから変えられない」「編集時色の変更が反映されない」という問題を起こしていた。概念を整理し直し、1 つの `selectionColor` がカード選択ハイライトと編集時テキスト選択背景の両方に反映される形に修正。

**変更点**:
- フィールド名を `editSelectionColor` → `selectionColor`、`editCaretColor` → `caretColor` にリネーム。`bdNormalizeCardStyles` / `bdNormalizeLineStyles` で旧名からの自動 migrate を実装。
- カード描画時 (bdRender) に `nodeStyle.selectionColor` / `nodeStyle.caretColor` をカード div の CSS 変数 `--bd-selection-color` / `--bd-caret-color` にセット。
- `.bd-selection-ring { background: var(--bd-selection-color, rgba(86,156,214,0.35)); }` に変更。カード選択時に指定色が反映される。
- 編集時の `::selection` と `caret-color` も同じ CSS 変数を参照するよう統一 (`--bd-selection-color` / `--bd-caret-color`)。
- スタイル管理 UI のラベルを「編集時 選択色 / カーソル色」→「選択色 / カーソル色」に変更 (カード選択ハイライトにも効く意味を反映)。
- ラインスタイルにも `selectionColor` フィールドを追加。ライン選択時の強調色を lineStyle で指定可能に。未指定時は既定色 (アクセント半透明)。
- `bdEditNode` 内の独自 CSS 変数セットを削除 (bdRender で設定済み)。

- [gb-board-ui.part01.js:189-195](app/gb-board-ui.part01.js#L189) カードスタイル正規化: `selectionColor` / `caretColor` に。旧フィールドから migration。
- [gb-board-ui.part01.js:244-246](app/gb-board-ui.part01.js#L244) ラインスタイルに `selectionColor` 追加。
- [gb-board-ui.part01.js:297-298](app/gb-board-ui.part01.js#L297) `bdGetNodeStyle` に新フィールド追加。
- [gb-board-style-manager.js:466-477](app/gb-board-style-manager.js#L466) カードスタイル UI を「選択色 / カーソル色」Row に変更。
- [gb-board-style-manager.js:552-565](app/gb-board-style-manager.js#L552) ラインスタイル UI に「選択色」スウォッチ追加。
- [gb-board-style-manager.js:76-83](app/gb-board-style-manager.js#L76) `_bdResetCardColors` で新フィールド名を使用。
- [gb-canvas-engine.part01.js:1121-1131](app/gb-canvas-engine.part01.js#L1121) bdRender でカード div に CSS 変数をセット。
- [gb-canvas-engine.part02.js:645-651](app/gb-canvas-engine.part02.js#L645) `bdEditNode` 内の重複 CSS 変数セットを削除。
- [gb-canvas-engine.part02.js:493-508](app/gb-canvas-engine.part02.js#L493) ライン選択時、lineStyle.selectionColor を SVG path の stroke 属性に直接設定。
- [gb-tools.part03.css:67-86](app/gb-tools.part03.css#L67) `.bd-selection-ring` background、`::selection` background、`caret-color` の 3 箇所を `--bd-selection-color` / `--bd-caret-color` 変数参照に統一。

## v0.5.224 (2026-04-18)

### fix(board): 検索バーの徹底チェックで発見した 2 件の UX 問題を修正

v0.5.223 の検索バー導入に対する徹底チェックで発見した問題を修正。

- **検索バー入力中に Ctrl+F / Ctrl+H を押すとブラウザのネイティブ検索ダイアログが開く**:
  - [gb-canvas-features.part01.js:442-465](app/gb-canvas-features.part01.js#L442) `stopPropagation()` は canvas への伝播を止めるが、ブラウザの Ctrl+F デフォルト動作は window レベルのため `preventDefault` が必要。検索バー入力 (検索欄 / 置換欄) の keydown でも Ctrl+F / Ctrl+H を捕捉し、`preventDefault` + `bdOpenFindBar(mode)` でモード切替するように変更。
- **置換実行後にマッチインデックスが 0 にリセットされる**:
  - [gb-canvas-features.part01.js:513-535](app/gb-canvas-features.part01.js#L513) `_bdFindReplaceOne` で置換前のインデックスを保存し、再検索後も同じ位置に復帰。件数が減っていた場合は循環して先頭に戻す。置換→次のマッチへの自然な流れに。

## v0.5.223 (2026-04-18)

### feat(board): ボードの検索・置換をモーダル廃止、Ctrl+F / Ctrl+H の検索バーに変更

従来のモーダル `bdFindReplace` を廃止し、キャンバス上にフロート表示される非モーダルの検索バーに置き換え。キャンバスにフォーカスがあるとき Ctrl+F で検索、Ctrl+H で置換行も展開して表示。

- [gb-canvas-features.part01.js:400-528](app/gb-canvas-features.part01.js#L400) `bdFindReplace` を廃止し、検索バー関連関数 (`bdOpenFindBar`/`bdCloseFindBar`/`_bdCreateFindBar`/`_bdFindUpdateMatches`/`_bdFindShowCurrent`/`_bdFindPrev`/`_bdFindNext`/`_bdFindUpdateUI`/`_bdFindReplaceOne`/`_bdFindReplaceAll`) に再構成。
  - カードテキストとラインラベルの両方を検索対象に。
  - Enter で次へ / Shift+Enter で前へ / Esc で閉じる / 置換欄 Enter で現在項目を置換。
  - カウント表示 (例: "3 / 12")、現在のマッチをカードなら `bdSelect` + `bdFocusSelected` でビュー中央に、ラインなら `bdSelectConnection` で選択表示。
  - 右クリックメニューの「検索と置換...」は `bdFindReplace` → `bdOpenFindBar('replace')` への互換ラッパーで維持。
- [gb-canvas-interact.js:989-994](app/gb-canvas-interact.js#L989) `onCanvasKeydown` に Ctrl+F / Ctrl+H のハンドラを追加 (非編集時のみ発動、編集中は従来通り Enter/Esc で編集終了)。
- [gb-tools.part03.css:99-106](app/gb-tools.part03.css#L99) `.bd-find-bar` の CSS を追加。`#bd-canvas` (position:relative) に `position: absolute` で右上にフロート表示、入力欄・件数表示・前後/閉じるボタン・置換欄/置換ボタンの 2 行構成。

## v0.5.222 (2026-04-18)

### fix(board): 雲/トゲ型カードの選択ハイライトが矩形で表示される不具合を修正

v0.5.221 で選択ハイライトを半透明オーバーレイに簡素化した際、雲・トゲ系カードでは `_bdApplyCloudShape` 内で `div.style.clipPath = ''` とクリアするため、子の `.bd-selection-ring` の `clip-path: inherit` が効かず、矩形の半透明色がカード外にもはみ出していた。

- [gb-canvas-engine.part01.js:1073-1080](app/gb-canvas-engine.part01.js#L1073) `_bdApplyCloudShape` で SVG オーバーレイの path と同じ `clipPath` を `.bd-selection-ring` にも設定し、選択色オーバーレイをカード形状に沿わせる。SVG 削除パスではクリップもクリア。

## v0.5.221 (2026-04-18)

### refactor(board): 選択ハイライトを半透明オーバーレイに簡素化

カード・ラインの選択表示を、対象全体に半透明のアクセント色を重ねるだけのシンプルな方式に変更。

- [gb-tools.part03.css:67-75](app/gb-tools.part03.css#L67) `.bd-selection-ring` から白い枠線・外側 box-shadow・ぼかし影を廃止。`inset: 0` + `background: rgba(86,156,214,0.35)` のみに簡素化。形状継承 (`border-radius: inherit; clip-path: inherit;`) は維持。
- [gb-tools.part03.css:115-120](app/gb-tools.part03.css#L115) ライン選択表示の裏白線 (`bd-conn-selection-back`) を透明に、前景 (`bd-conn-selection-front`) は半透明アクセント色の単純な stroke に変更。drop-shadow 類は全廃止。
- [gb-tools.part03.css:125](app/gb-tools.part03.css#L125) 矢印選択時の drop-shadow も廃止。

### feat(board): カード編集時のテキスト選択色・カーソル色をスタイル設定可能に

シナリオアプリのノートエディタと同様に、カード編集中 (contentEditable) のテキスト選択背景色とカーソル色をカードスタイルから設定可能に。

- [gb-board-ui.part01.js:189-191](app/gb-board-ui.part01.js#L189) `bdNormalizeCardStyles` に `editSelectionColor` / `editCaretColor` フィールドを追加 (default `''`)。
- [gb-board-ui.part01.js:297-298](app/gb-board-ui.part01.js#L297) `bdGetNodeStyle` でノード個別値も含めて返す。
- [gb-board-style-manager.js:466-476](app/gb-board-style-manager.js#L466) `_bdBuildStyleFields` に「編集時」Row を追加し、テキスト選択色・カーソル色のスウォッチを配置。
- [gb-board-style-manager.js:76-83](app/gb-board-style-manager.js#L76) `_bdResetCardColors` で編集時色もリセット対象に。
- [gb-canvas-engine.part02.js:645-654](app/gb-canvas-engine.part02.js#L645) `bdEditNode` で編集開始時にカード要素の CSS 変数 (`--bd-edit-selection-color` / `--bd-edit-caret-color`) に値を流す (未指定なら removeProperty)。
- [gb-tools.part03.css:82-84](app/gb-tools.part03.css#L82) 編集中カードのテキストに `caret-color: var(--bd-edit-caret-color, auto)`、`::selection { background: var(--bd-edit-selection-color, rgba(86,156,214,0.4)) }` を適用。未指定時はアクセント色半透明でフォールバック。

## v0.5.220 (2026-04-18)

### fix(board): カード/フレームラベル編集時に黒い入力欄がかぶさる見た目を廃止

編集中のテキスト要素に `background: var(--bg)` が設定されており、ダークテーマでは真っ黒の「入力欄」がカード/ラベル上にかぶさって見えていた。カード・ラベル上のテキストをそのまま直接編集できるよう、編集中の背景・枠線・padding・border-radius・min-width を削除。

- [gb-tools.part03.css:83](app/gb-tools.part03.css#L83) `.bd-node.bd-editing .bd-text` から `background`、`border-radius`、`padding`、`min-width` を削除。`outline: none`、`cursor: text`、`user-select: text` のみ残す。
- [gb-tools.part03.css:130](app/gb-tools.part03.css#L130) `.bd-frame-label[contenteditable="true"]` から `background`、`border` を削除。
- ラインラベル (`.bd-conn-label`) は元から背景が半透明のままで、編集中は枠線だけアクセント色に変わる既存動作 (変更なし)。

## v0.5.219 (2026-04-18)

### fix(board): コンテナ内包のドロップ時、多段ネスト/contained 同士で座標が崩れる不具合を修正

v0.5.217〜0.5.218 導入の徹底チェックで発見した座標計算バグを修正。

**問題**: ドロップ先 (parentNode) が自身 contained (別コンテナの中のカード) の場合、`parentNode.x` は祖父コンテナ相対の座標なのに絶対座標として扱われ、ドロップしたカードの新相対座標が間違った値になる。多段ネストの dragOffsets 初期化も 1 段しか対応しておらず、深くネストされた contained のドラッグで同様の破損が発生。

**修正**:
- [gb-canvas-interact.js:338-350](app/gb-canvas-interact.js#L338) `dragOffsetEntry` で contained 連鎖を最深部まで辿って絶対座標を算出するよう変更 (多段ネスト対応)。
- [gb-canvas-interact.js:576-606](app/gb-canvas-interact.js#L576) ドロップ処理にヘルパー `getAbs(n)` を導入し、parentNode と drag 中のノード双方を絶対座標に揃えてから差分で新相対座標を計算。ネストコンテナやコンテナ間移動でも座標が崩れない。

## v0.5.218 (2026-04-18)

### feat(board): コンテナ内カードを D&D でコンテナ外へ取り出せるように

- [gb-canvas-interact.js:333-357](app/gb-canvas-interact.js#L333) ドラッグ開始時の `dragOffsets` 初期化を見直し。contained ノードは親ノードの絶対座標を足して **絶対座標で ox/oy を保存**するよう変更 (ドラッグ閾値超過で contained を解除しても座標の整合が崩れない)。
- [gb-canvas-interact.js:411-431](app/gb-canvas-interact.js#L411) pointermove の drag 処理の直前に、ドラッグ閾値 (dx/dy ≥ 8px) を超えた時点で contained ノードを「取り出す」ロジックを追加:
  - `n.contained = false`、`n.parent = ''` にしてカードを独立化
  - `bdRender()` で再描画。DOM 配置が innerDiv (inline-block) から独立座標配置に切り替わり、以降ポインタに追従する。
  - フラグ `bd._dragExtracted` で 1 度目の閾値超過時のみ処理が走るようガード。
- クリック (ドラッグしない) ではコンテナから取り出されない (閾値 8px 未満なら何も起きない)。
- ドロップ時は既存ロジックに合流: 別カード上なら新コンテナに内包、空白ならその位置に独立カードとして配置。

## v0.5.217 (2026-04-18)

### feat(board): カードをカードに重ねたらコンテナに内包 (旧: 親子関係 + ライン自動生成)

- **廃止**: ドロップ時の接続線自動生成 (`bdCreateConnectionWithStyle` 呼び出し)
- **新規**: ドロップ時にドロップ先を `container = true` 化し、ドラッグしたカードを `contained = true` + 親相対座標に変換して取り込む
- [gb-canvas-interact.js:566-602](app/gb-canvas-interact.js#L566) ドロップ処理を書き換え:
  - `parentNode.container = true` でドロップ先をコンテナ化
  - 既に他コンテナの contained だった場合は旧親の絶対座標を足し戻してから新親相対に変換 (2 段ネストの移動対応)
  - 循環防止条件 (自分自身 / 子孫へのドロップを拒否) は維持
  - ステータス表示を "親子関係を設定しました" → "コンテナに内包しました" に変更

## v0.5.216 (2026-04-18)

### fix(board): 右ドラッグ/Alt+左ドラッグ導入で生じた UX 退行を修正

v0.5.213〜0.5.215 の右ドラッグ/Alt+左ドラッグ導入に伴う徹底チェックで発見した 2 件の問題を修正。

- **Alt+左クリック (ドラッグなし) でノードメニューが開く**:
  - [gb-canvas-interact.js:527-540](app/gb-canvas-interact.js#L527) 旧「右ドラッグ無しで離した時は右クリックメニュー」という `_rightDragNode` ロジックを撤去。現在 `_rightDragNode` は Alt+左ドラッグ由来のみになったため、ドラッグしない場合は選択だけで終わるのが自然。
- **右クリック (静止) 時に未選択カードが自動選択されない**:
  - [gb-canvas-interact.js:546-562](app/gb-canvas-interact.js#L546) 旧実装では右ボタン mousedown で `bdSelect(nid)` を呼んでいたが、右ドラッグをパン化した際に脱落していた。pointerup で dx/dy < 15 のとき、未選択カードなら `bdSelect(nid)` を呼んでからメニューを開くよう修正 (Shift 併用時は複数選択を維持)。

## v0.5.215 (2026-04-18)

### refactor(board): カードスタイル設定 UI を整理

- [gb-board-style-manager.js:448-475](app/gb-board-style-manager.js#L448) Row 2 の並びを「フチ幅 / 枠線 / 標準幅 / 形状 / 角丸」に変更 (角丸を形状のあとに移動)。
- 角丸は形状が矩形 (`shape === 'rect'` or 空) のときだけ表示。楕円・雲・トゲ・ピル・八角形など、形状が独自に輪郭を決める形状では無意味なので非表示。
- 「張り出し」(`cloudSideWidth`) の UI 入力を削除。楕円ベース化 (v0.5.190) 以降、`_bdCloudClipPath` / `_bdThornClipPath` の実装で完全に未使用のデッドコード (データフィールドは互換性のため温存)。
- [gb-board-style-manager.js:395-415](app/gb-board-style-manager.js#L395) `_bdBuildStyleFields` の options に `hideDefaultWidth` を追加。true のとき「標準幅」入力を非表示に。
- [gb-board-ui.part02.js:519-526](app/gb-board-ui.part02.js#L519) 既存カード選択時の詳細パネルから「標準幅」を排除 (新規カードの初期幅に関わる値なので、既に作成済みのカードには無関係)。スタイル管理モーダル / ボード詳細パネル (新規カード設定) では従来通り表示。

## v0.5.214 (2026-04-18)

### fix(board): カード / ライン上で右ドラッグするとパン後にメニューが開く不具合を修正

v0.5.213 で右ドラッグを常にパンにしたが、カードとラインには個別の `oncontextmenu` ハンドラー (`bdContextMenu` / `bdConnContextMenu`) が設定されており、右ドラッグ終了直後にブラウザが発火する `contextmenu` イベントでメニューが開いてしまっていた。

- [gb-canvas-interact.js:269-277](app/gb-canvas-interact.js#L269) 右ボタン mousedown 時に `bd._rightPanMoved = false` 初期化。
- [gb-canvas-interact.js:378-383](app/gb-canvas-interact.js#L378) 右ドラッグパン中、移動量が 4px 以上になったら `bd._rightPanMoved = true` をセット。
- [gb-canvas-engine.part01.js:1108-1114](app/gb-canvas-engine.part01.js#L1108) / [gb-canvas-engine.part01.js:1291-1296](app/gb-canvas-engine.part01.js#L1291) カード (および内包子ノード) の `oncontextmenu` で `_rightPanMoved === true` ならメニュー発火を抑止。フラグは 1 回使ったら false に戻す。
- [gb-canvas-engine.part02.js:527-531](app/gb-canvas-engine.part02.js#L527) ライン hit 領域の contextmenu ハンドラーも同様にガード。

## v0.5.213 (2026-04-18)

### feat(board): 右ドラッグを常にパン、ライン作成は Alt + 左ドラッグに変更

従来の「ノード上の右ドラッグ = 接続線作成」「空白の右ドラッグ = パン」の使い分けを廃止し、右ドラッグはどこでも常にパンに統一。接続線作成は Alt + 左ドラッグに割り当て。

- [gb-canvas-interact.js:265-290](app/gb-canvas-interact.js#L265) 右ボタン処理を「常にパン」に統一。静止時のメニュー表示判定には nid を持たせ、ノード上で右クリックすればノードメニュー、空白で右クリックすれば空白メニューが出るように維持。
- [gb-canvas-interact.js:292-300](app/gb-canvas-interact.js#L292) `e.altKey && nodeEl` のとき、旧・右ドラッグと同じ `_rightDragNode` ロジック (ドラッグ後に接続先ノード上で離すと接続線作成) を起動。
- [gb-canvas-interact.js:546-553](app/gb-canvas-interact.js#L546) パン終了時のメニュー呼び出しを `bdContextMenu(e, nid || null)` に変更。

## v0.5.212 (2026-04-18)

### fix(board): テキストフチのボケを軽減し、シャープな輪郭に

v0.5.211 の blur=0.4px はボケが強く見えていたため、blur を 0.15px に減らしてシャープに。減らした blur の代わりに方向数と環の数を増やして離散感を抑える。

- [gb-board-presets.js:314-336](app/gb-board-presets.js#L314) `_bdTextOutlineShadow` を 3 環構成に変更:
  - 外周 60 方向 (半径 w)
  - 中周 24 方向 (半径 w × 0.66、w ≧ 2 のとき)
  - 内周 12 方向 (半径 w × 0.33、w ≧ 3 のとき)
- blur を 0.4px → 0.15px に減らし、ピクセル境界だけ吸収する極小量に。

## v0.5.211 (2026-04-18)

### fix(board): テキストフチを高密度 text-shadow + 微量 blur 方式に変更

v0.5.208〜0.5.210 の SVG filter 方式 (feMorphology / feGaussianBlur + feComponentTransfer) は ブラウザ実装差が大きく、フチが消えたり角が四角くなったり不安定だったため、text-shadow を高密度に重ねる方式に回帰。

**新方式の工夫**:
- 外周 36 方向 (半径 w) で滑らかな円形輪郭を作る
- 内周 12 方向 (半径 w/2) で太いフチの中抜けを防ぐ (w ≧ 2 のとき)
- 各影に微量 blur (0.4px) を付与して離散的なドット感を吸収

- [gb-board-presets.js:314-336](app/gb-board-presets.js#L314) `_bdTextOutlineShadow` を 2 段配置 + 微 blur 版に書き直し。
- [gb-canvas-engine.part01.js:1236-1246](app/gb-canvas-engine.part01.js#L1236) カード本体のテキストフチを SVG filter から `textShadow = _bdTextOutlineShadow(...)` に戻す。旧 filter は `_bdApplyTextOutline(txt, 0, '', id)` で除去。
- [gb-canvas-engine.part01.js:1278-1281](app/gb-canvas-engine.part01.js#L1278) 内包子ノードも同様に text-shadow 方式に戻す。
- `_bdApplyTextOutline` 関数自体は残存 filter 除去に使うため残す。

## v0.5.210 (2026-04-18)

### fix(board): テキストフチが消失する不具合を修正

v0.5.209 で SVG filter 方式に切り替えた際、細いテキスト (線幅 1〜2px) では `stdDeviation=w×0.85` のガウシアンブラー後に中心アルファすら 0.5 未満になり、閾値 0.5 の 2 値化でマスクが全て 0 → フチが完全に消える不具合を修正。

- [gb-canvas-engine.part01.js:967-984](app/gb-canvas-engine.part01.js#L967) `_bdApplyTextOutline` のパラメータ調整:
  - `stdDeviation` を `w × 0.85` → `w × 0.6` に縮小 (中心アルファが残るように)
  - `feFuncA tableValues` を `"0 1"` (閾値 0.5) → `"0 1 1 1"` (閾値 0.25) に変更 (4 セグメントで先頭のみ 0)
  - この組み合わせでブラー + 閾値から求まるフチ厚さが概ね `w` になる

## v0.5.209 (2026-04-18)

### fix(board): テキストフチの角が斜めに切り落とされる / 太いと四角くなる不具合を修正

v0.5.208 で `feMorphology (dilate)` でフチを描画したが、SVG 仕様上この operator は矩形カーネルでの膨張になるため、フチが太くなるにつれ角が斜めカットされ、さらに太くすると四角く見える不具合を修正。

**解決**: `feGaussianBlur` + `feComponentTransfer` (discrete 閾値) の組合せで円形膨張に置換。ガウシアンブラーのカーネルは等方 (円形) なので、2 値化後のマスクも真の円形膨張となり、角が完全に丸く仕上がる。

- [gb-canvas-engine.part01.js:928-1002](app/gb-canvas-engine.part01.js#L928) `_bdApplyTextOutline` のフィルタチェーンを再設計:
  - `feGaussianBlur` (stdDeviation = w × 0.85) で SourceAlpha を円形にぼかす
  - `feComponentTransfer` + `feFuncA type="discrete" tableValues="0 1"` で α ≧ 0.5 を 1、未満を 0 に 2 値化
  - `feFlood` + `feComposite (operator="in")` でマスクに指定色を塗る
  - `feMerge` で元のテキストを前景に重ねる
- filter region を `-50% / 200%` に拡大し、太いフチでも見切れないように。

## v0.5.208 (2026-04-18)

### fix(board): テキストフチのガタつきを SVG filter で完全に滑らかに描画

v0.5.207 で `text-shadow` による擬似フチに切り替えたが、離散的な影の重ね合わせのため影同士の間にガタつきが残る問題を修正。

**解決**: SVG filter の `feMorphology (operator="dilate")` を使った方式に切り替え。テキストアウトラインを数学的に外側へ膨張させるため、完全に滑らかな丸フチになる。

- [gb-canvas-engine.part01.js:928-995](app/gb-canvas-engine.part01.js#L928) `_bdApplyTextOutline(txt, width, color, nodeKey)` を追加。テキスト要素の親に `<svg><defs><filter>` を埋め込み、`feMorphology` + `feFlood` + `feComposite` + `feMerge` のフィルタチェーンで膨張アウトラインと元テキストを合成。
- [gb-canvas-engine.part01.js:1245-1254](app/gb-canvas-engine.part01.js#L1245) カード本体のテキストフチ描画を text-shadow から `_bdApplyTextOutline` に変更。
- [gb-canvas-engine.part01.js:1285-1299](app/gb-canvas-engine.part01.js#L1285) 内包子ノードは DOM 追加前は親要素が存在しないため、dataset にパラメータを退避し、DOM 追加後に filter を適用するパターンに変更。
- プレビュー (`_bdCardStylePreviewHtml`) は小サイズ (26×16) のため従来の `text-shadow` 方式を維持。

## v0.5.207 (2026-04-18)

### feat(board): カードのテキストフチの角を丸く描画

`-webkit-text-stroke` はフォントの輪郭に沿って miter で描画されるため角が鋭くなり、CSS にフチの linejoin を制御する手段がない。text-shadow を円周上の多方向に重ねる擬似フチ方式に切り替え、各影が丸いため角が自然に丸く描画されるように変更。

- [gb-board-presets.js:314-328](app/gb-board-presets.js#L314) `_bdTextOutlineShadow(width, color)` を追加。幅に応じて 8〜24 方向の影を円周上に配置し `text-shadow` 文字列を生成する。
- [gb-canvas-engine.part01.js:1169-1182](app/gb-canvas-engine.part01.js#L1169) カード本体のテキストフチを `webkitTextStroke` から `text-shadow`（`_bdTextOutlineShadow`）に変更。
- [gb-canvas-engine.part01.js:1212-1217](app/gb-canvas-engine.part01.js#L1212) 内包子ノードのテキストフチも同様に変更。
- [gb-board-presets.js:336](app/gb-board-presets.js#L336) スタイル管理モーダルのプレビュー `_bdCardStylePreviewHtml` も `text-shadow` 方式に変更。

## v0.5.206 (2026-04-18)

### fix(board): トゲ（曲線）の枠線の peak も miter で尖らせる

- [gb-canvas-engine.part01.js:979-988](app/gb-canvas-engine.part01.js#L979) `_bdApplyCloudShape` の miter 判定条件を `shape === 'thorn'` から `shape === 'thorn' || shape === 'thorn-curve'` に拡張。曲線トゲも peak がアンカーで接線不連続なので miter が効く。

## v0.5.205 (2026-04-18)

### fix(board): トゲ（直線）の枠線の peak を尖らせる

path は直線でも、枠線 SVG の `stroke-linejoin` が `round` 固定だったため、peak の角が丸く描画されていた不具合を修正。形状ごとに linejoin を切り替える。

- [gb-canvas-engine.part01.js:936-983](app/gb-canvas-engine.part01.js#L936) `_bdApplyCloudShape` で `div.dataset.shape` を見て stroke 接続方式を切り替え。
  - `shape === 'thorn'`: `stroke-linejoin="miter"` + `stroke-miterlimit="40"` (鋭角でも自動 bevel にフォールバックしないよう大きめの miterlimit)。
  - その他 (`cloud` / `thorn-curve`): 従来通り `stroke-linejoin="round"`。
- stroke-linejoin 属性を SVG path 初期化時の 1 回限りの設定から、毎回の属性更新に移動。

## v0.5.204 (2026-04-18)

### fix(board): トゲ（直線）の輪郭を完全な直線に修正

トゲ（直線）が cubic Bezier で描かれていたため、山の側面がわずかに曲線になり、peak も丸く見える不具合を修正。valley と peak をアンカーにした直線 (L) の連続に変更し、完全な polygon 相当に。

- [gb-canvas-engine.part01.js:744-810](app/gb-canvas-engine.part01.js#L744) `_bdThornClipPath` の path 生成ループを `C` (cubic Bezier) から `L L` (valley 直前の peak + valley) に変更。定数 `K = 4/3` を削除。peak は従来通り拡大楕円上の `peakAt(tMid, hMul)`、valley は楕円上の `ellipsePoint(tEnd)`。

## v0.5.203 (2026-04-18)

### refactor(board): トゲ（曲線）を peak アンカー方式に全面刷新

v0.5.196〜0.5.202 で続いていた「valley 付近に小山・コブ・小さな尖りが出る」不具合を、描画方式の全面変更で根本解決。

**従来**: valley と peak の両方を path 上のアンカーポイントとして配置し、 valley-peak 間を Bezier で結んでいた。valley での接線を連続にすると楕円周方向の外向きコブが発生し、不連続にすると制御点の内向きオフセットが隣接 Bezier と干渉して自己交差する、という構造的問題を抱えていた。

**新方式 (peak アンカー方式)**: path のアンカーポイントは peak のみに置き、valley は隣接 peak 間を結ぶ cubic Bezier の「谷」として暗黙的に発生させる。これにより valley 近辺の接線問題が構造的に発生せず、peak では必然的に接線不連続 (= 鋭い尖り) になる。

- [gb-canvas-engine.part01.js:817-907](app/gb-canvas-engine.part01.js#L817) `_bdThornCurveClipPath` を再設計:
  - path 構造: `M peak0 → C ... peak1 → C ... peak2 → ... → peak0 Z`
  - 隣接 peak 間 cubic の制御点を `p0 + (p1 - p0) × TPULL + centerDir × depth` と `p1 + (p0 - p1) × TPULL + centerDir × depth` で配置。両制御点を中心方向に同量オフセットすることで、Bezier 中間が凹み「谷」を自然に作る。
  - TPULL = 0.33、DEPTH_RATIO = 0.9 (谷の深さは effBumpH × 0.9)。
  - valley の位置・楕円接線・弦長などの複雑な計算は不要になり、コードもシンプル化。

## v0.5.202 (2026-04-18)

### fix(board): トゲ（曲線）の valley 付近に「小山」のように見える外向きコブが出る不具合を緩和

v0.5.201 で楕円接線方式にしたところ、valley 付近で Bezier が楕円周方向に沿って動き、楕円の曲率で**外向きに膨らんで見える**不具合が発生。接線方向の制御点距離比 `TAN_RATIO` を下げて valley 付近の周方向成分を弱める。

- [gb-canvas-engine.part01.js:825-830](app/gb-canvas-engine.part01.js#L825) `TAN_RATIO` を 0.35 → 0.18 に、`SIDE_CURL` を 0.55 → 0.40 に調整。
  - `TAN_RATIO` を減らすと valley 近辺で曲線が楕円周方向に膨らむ量が減り、外向きコブが目立たなくなる。
  - `SIDE_CURL` も合わせて減らすことで、隣接山 Bezier との干渉による自己交差リスクを下げる。

## v0.5.201 (2026-04-18)

### fix(board): トゲ（曲線）の側面凹みが浅く直線トゲと見分けがつかない不具合を修正

前 v0.5.200 で cubic 1 本方式に切り替えたが、制御点を平行シフトしても Bezier 全体が平行移動するだけで、側面の凹みが視覚的に出ず直線トゲと同等の見た目になっていた不具合を修正。

**解決**: quadratic 2 本方式 (valley → cL → peak, peak → cR → valley) に戻し、制御点 cL, cR の位置を「弦中点 + 弦垂直内向き」から「**valley の楕円接線方向 × distTan + 弦垂直内向き × distPerp**」に変更。valley 近辺で Bezier の接線が楕円接線に近づくことで、path が楕円の弧に沿って滑らかに接続し、隣接山同士の自己交差が起きない。peak では接線不連続で鋭い尖りを維持、さらに弦垂直内向きの shift で側面が明確に凹む。

- [gb-canvas-engine.part01.js:818-925](app/gb-canvas-engine.part01.js#L818) `_bdThornCurveClipPath` を再設計:
  - 描画を cubic 1 本 → quadratic 2 本に戻す。
  - `ellipseTangent(t)` を追加。楕円上の点での接線単位ベクトルを返す。
  - 左側面制御点 cL = vStart + tanS × (chordLen × TAN_RATIO) + perpIn × (effBumpH × hMul × SIDE_CURL)
  - 右側面制御点 cR = vEnd − tanE × (chordLen × TAN_RATIO) + perpIn × (effBumpH × hMul × SIDE_CURL)
  - TAN_RATIO = 0.35、SIDE_CURL = 0.55。

## v0.5.200 (2026-04-18)

### fix(board): トゲ（曲線）の描画方式を cubic Bezier 1 本方式に変更し自己交差を解消

v0.5.196〜0.5.199 で続いていた「山が葉っぱ状に分離する」「山と山の間に小さな突起が出る」不具合を、描画方式の構造変更で根本解決。

**原因**: quadratic Bezier 2 本 (valley→peak, peak→valley) の接続方式では、制御点を弦垂直内向きにオフセットすると、valley 近辺で Bezier が楕円内側に大きく潜り込み、隣接山の Bezier と path が自己交差して「葉っぱ型」の閉じた領域ができていた。valley での接線不連続と制御点の内向きシフトが構造的に干渉する。

**解決**: 直線トゲ (_bdThornClipPath) と同じ cubic Bezier 1 本方式に切り替え、制御点を弦垂直内向きにシフトすることで側面を凹ませる。1 本の cubic Bezier は自己交差しない。

- [gb-canvas-engine.part01.js:818-912](app/gb-canvas-engine.part01.js#L818) `_bdThornCurveClipPath` を全面書き換え:
  - 2 本 quadratic Bezier を 1 本 cubic Bezier に変更。
  - peak は拡大楕円 (rx+h, ry+h) 上の `peakAt(tMid, hMul)` に戻す (直線トゲと同じ)。
  - 制御点は `valley + (peak - valley) * K` (K=4/3, peak を通過させる) の位置から、弦 valley-valley に垂直な内向きに `effBumpH * hMul * SIDE_CURL` (SIDE_CURL=0.45) だけシフト。
  - SIDE_CURL=0 なら直線トゲと同じ見た目、大きくするほど側面の凹みが深くなる。

## v0.5.199 (2026-04-18)

### fix(board): トゲ（曲線）の山が自己交差して「葉っぱ状」に分離する不具合を修正

前 v0.5.198 で peak 位置を「valley 弦中点 + effBumpH」に変更したが、山が広い (角度幅が大きい) 場合に弦中点が楕円内側に深く位置するため、peak が本来の楕円外側輪郭より内側に引っ込み、Bezier が自己交差して山が「アーモンド/葉っぱ型」の閉じた領域に分離する不具合を修正。

- [gb-canvas-engine.part01.js:843-866](app/gb-canvas-engine.part01.js#L843) `makePeak` を修正:
  - peak の放射外向き距離を `effBumpH * hMul` 固定から `sagitta + effBumpH * hMul` に変更。
  - `sagitta` = 楕円の弧中点 (ellipsePoint(tMid)) の放射方向投影 − 弦中点の放射距離 = 弦と弧のたわみ分。
  - これで peak は常に「楕円外側 + effBumpH」相当の位置にある。山の幅 (角度) に依存せず自己交差が起きない。
  - 山の高さ均一性も維持 (放射方向への外側突出量は全山で effBumpH)。

## v0.5.198 (2026-04-18)

### fix(board): トゲ（曲線）で隣接山の間に「小山」のような突起が見える不具合を修正

小山を無効 (幅・高さ 0%) にしても、山同士の間に小さな突起や山の大小ムラが発生する不具合を修正。

原因: (1) peak を「拡大楕円 (rx+h, ry+h) 上の点」として計算していたため、非正方形楕円では曲率依存で「valley → peak の実距離」が山ごとに変動し、視覚的な高さが不均一になっていた。(2) 制御点オフセットを弦長比例にしていたため、弦長自体が楕円上の角度で変動し、凹み量も不均一になっていた。

- [gb-canvas-engine.part01.js:824-907](app/gb-canvas-engine.part01.js#L824) `_bdThornCurveClipPath` を修正:
  - peak の計算を `makePeak(vStart, vEnd, hMul)` に変更。valley 弦の中点から中心逆方向 (= 放射外向き) に `effBumpH * hMul` 進んだ点を peak にする。これで全ての山の高さが一定になる。
  - `sideCtrl` の制御点オフセットを弦長比例から `effBumpH * hMul * CURVE_RATIO` (effBumpH 比例) に変更。山ごとの弦長変動に影響されず、全ての山で凹み量が揃う。
  - `CURVE_RATIO` を 0.30 → 0.8 に変更 (式の基準量変更に伴う見た目合わせ)。

## v0.5.197 (2026-04-18)

### fix(board): トゲ（曲線）の山の高さを均一化

前 v0.5.196 で追加したトゲ（曲線）で、小山を無効 (幅・高さ 0%) にしても横方向と縦方向で山の高さが違って見える不具合を修正。

- [gb-canvas-engine.part01.js:824-894](app/gb-canvas-engine.part01.js#L824) `_bdThornCurveClipPath` の Bezier 制御点の計算を、「弦中点 → 中心 (cx, cy)」方向の比例寄せから、「弦に垂直な内向き方向 × 弦長比例」のオフセットに変更。中心方向ベクトルの長さは楕円上の山の位置 (角度) に依存するため、横長/縦長カードで山ごとの凹み量が不均一になっていた。弦垂直方向 + 弦長比例なら、山のサイズと一致する均一な凹み量になる。
- `CURVE_RATIO` を 0.35 → 0.30 に調整 (計算式変更に伴う見た目合わせ)。

## v0.5.196 (2026-04-18)

### feat(board): カード形状「トゲ（曲線）」を追加、既存トゲを「トゲ（直線）」に改名

既存のトゲ型 (valley → peak → valley を Bezier で描き、側面がほぼ直線に見える) を「トゲ（直線）」にリネームし、各トゲの左右側面が内側に凹む爆発フキダシ型「トゲ（曲線）」を新規追加。

- [gb-canvas-features.part01.js:636-637](app/gb-canvas-features.part01.js#L636) `BD_SHAPES` に `'thorn-curve'` を追加。`BD_SHAPE_LABELS` のラベルを `thorn:'トゲ（直線）'`, `'thorn-curve':'トゲ（曲線）'` に変更。内部値 `'thorn'` は既存ボードデータ互換のため維持。
- [gb-canvas-engine.part01.js:816-893](app/gb-canvas-engine.part01.js#L816) `_bdThornCurveClipPath` を新規追加。各山を 2 本の quadratic Bezier (`valley → peak`, `peak → valley`) で描き、各制御点を「弦の中点から中心方向」に `CURVE_RATIO=0.35` で寄せて側面を凹ませる。パラメータ (山の幅・高さ・ズラし量・小山サイズ) は既存トゲと共有。
- [gb-canvas-engine.part02.js:8-13](app/gb-canvas-engine.part02.js#L8) / [gb-canvas-interact.js:171-176](app/gb-canvas-interact.js#L171) 動的 clip-path 生成分岐に `'thorn-curve'` を追加し、対応する描画関数を呼び分け。
- [gb-canvas-engine.part01.js:1049](app/gb-canvas-engine.part01.js#L1049) / [gb-canvas-engine.part01.js:905](app/gb-canvas-engine.part01.js#L905) / [gb-canvas-engine.part02.js:25](app/gb-canvas-engine.part02.js#L25) / [gb-canvas-interact.js:188](app/gb-canvas-interact.js#L188) drop-shadow 適用 / テキスト安全域パディング計算 / パディング再計算の各分岐に `'thorn-curve'` を追加。
- [gb-board-presets.js:306](app/gb-board-presets.js#L306) スタイル管理モーダルの小サイズ (26×16) プレビュー用に、各 peak-valley 間に内側凹み点を挿入した 16 点 polygon を追加。
- [gb-board-style-manager.js:468](app/gb-board-style-manager.js#L468) 詳細パネル Row 3 (山の幅 / 山の高さ / ズラし量 / 小山サイズ) の表示条件に `'thorn-curve'` を追加。

## v0.5.195 (2026-04-18)

### feat(board): 雲/トゲ型の小山サイズを幅・高さ別設定に分離

前 v0.5.193 で追加した単一の `cloudSubBumpRatio` を、幅と高さを独立して設定できる 2 プロパティに分離。

- [gb-board-ui.part01.js:303-305](app/gb-board-ui.part01.js#L303) / [gb-board-ui.part01.js:195-200](app/gb-board-ui.part01.js#L195) `cloudSubWidthRatio` と `cloudSubHeightRatio` を追加。`bdNormalizeCardStyles` で旧 `cloudSubBumpRatio` を自動 migrate。
- [gb-canvas-engine.part01.js:672-676](app/gb-canvas-engine.part01.js#L672) / [gb-canvas-engine.part01.js:748-753](app/gb-canvas-engine.part01.js#L748) `_bdCloudClipPath` / `_bdThornClipPath` の opts を `subWidth` / `subHeight` の 2 つに分離。両方 > 0 のとき小山が現れる。
- [gb-board-style-manager.js:471-478](app/gb-board-style-manager.js#L471) 詳細パネル Row 3 に「小山幅 %」「小山高 %」の 2 入力を追加。

## v0.5.194 (2026-04-18)

### feat(board): 雲/トゲ型の枠線を外側に描画

カード本体の CSS `clip-path` を撤廃し、SVG オーバーレイで fill (背景色) と stroke (枠線) の両方を描画する方式に変更。`paint-order="stroke fill"` と `stroke-width = 2×borderWidth` により、stroke の外側半分 (= borderWidth px) だけが可視となり、雲/トゲの輪郭に沿った外側枠線になる。

- [gb-canvas-engine.part01.js:839-895](app/gb-canvas-engine.part01.js#L839) `_bdApplyCloudShape` を書き直し。カード div の `clipPath` / `background` / `border` を透明化し、`overflow: visible` に。SVG 1 つで fill + 外側 stroke をまとめて描画。
- [gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) / [gb-canvas-interact.js](app/gb-canvas-interact.js) bgColor を `_bdApplyCloudShape` に渡すよう呼び出しを更新。

## v0.5.193 (2026-04-18)

### feat(board): 雲/トゲ型の小山サイズを数値設定可能に

旧 v0.5.192 のトグル (on/off 固定 50%) を 0–100% の数値入力 `cloudSubBumpRatio` に置き換え。

- 1 スロット = メイン山 + 小山 の構成に変更。スロット幅 = `BUMP_W × (1 + SUB_RATIO)`、メイン山の角度幅 = `periodAngle / (1 + SUB_RATIO)`、小山の角度幅と高さがそれぞれ SUB_RATIO 倍。
- 0%: 小山なし、50%: メインの半分サイズ、100%: ほぼ同サイズの山が倍の数に。
- 後の v0.5.195 で幅/高さを独立設定に分離。

## v0.5.192 (2026-04-18)

### feat(board): 雲/トゲ型に「小山」トグル追加 (試作)

メイン山の間に半分の高さの小さい山を挿入する on/off トグルを追加。後の v0.5.193 で数値入力に置き換え。

## v0.5.191 (2026-04-18)

### fix(board): 雲型の Bezier 制御点を弦に垂直外向きに修正 (トゲ化回避)

v0.5.190 の楕円ベース雲型で山がトゲのように尖って見える不具合を修正。

- 旧: 制御点を `V + K*(peak - V)` の方向に伸ばしていたため、`peak - V` の水平成分が制御点を対角方向に引き、Bezier が peak に向かって収束する尖った形になっていた。
- 新: 制御点を弦に対して純粋に垂直外向きに伸ばす方式に変更 (`mLen = (4/3) × effBumpH`, `P1 = V_start + mLen × perp`, `P2 = V_end + mLen × perp`)。両制御点が同じ垂直方向に伸びるため Bezier は弦に対して対称な半円状の膨らみを形成し、t=0.5 がちょうど peak を通る。
- 副次効果として隣接する山の境界で接線が反転 (cusp) し、自然な V 字の谷ができて本物の雲の「もこもこ感」になる。

[gb-canvas-engine.part01.js:682-725](app/gb-canvas-engine.part01.js#L682)

## v0.5.190 (2026-04-18)

### feat(board): 雲型を楕円ベースに変更 + トゲ型追加 + テキスト安全域パディング

雲型のベース形状を矩形から楕円に変更し、同じく楕円ベースで放射状に尖ったトゲ型を新設。楕円・雲・トゲ型のテキストが形状からはみ出ないようパディングを自動調整。

- [gb-canvas-features.part01.js:636-637](app/gb-canvas-features.part01.js#L636) `BD_SHAPES` に `'thorn'` 追加、`BD_SHAPE_LABELS` に「トゲ」追加。
- [gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) `_bdCloudClipPath` を楕円ベースに書き直し、`_bdThornClipPath` 新設 (peak を radial 方向に配置して尖り化)、`_bdComputeShapePadding` 新設 (楕円の内接矩形 × 1/√2 に基づく padding)。
- v0.5.191 で雲型の Bezier 制御点を再修正。

## v0.5.189 (2026-04-18)

### feat(board): 雲/トゲ型のプレビュー用ポリゴンを整理

スタイル管理モーダルのプレビュー用 (26×16 小サイズ) の静的ポリゴンを、実カードの動的 clip-path とは別に 16 点 (雲) / 8 点 (トゲ) で簡素化。

## v0.5.188 (2026-04-18)

### feat(board): 雲型の左右辺にも固定サイズ山を適用 (中間版)

左右辺に `SIDE_H = BUMP_W` で複数山を配置するよう拡張。v0.5.190 で楕円ベースに置き換えたため中間版。

## v0.5.187 (2026-04-18)

### feat(board): 雲型カードの山サイズ/ズラし量/角丸/枠線対応

矩形ベース雲型に以下のパラメータを追加 (詳細パネルで設定可):

- 山の幅 (`cloudBumpWidth`, 8–200px, default 40)
- 山の高さ (`cloudBumpHeight`, 2–100px, default 16)
- 張り出し (`cloudSideWidth`, 2–100px, default 12)
- ズラし量 (`cloudOffset`, 0–100%, default 50)

角丸 (既存 `borderRadius`) を雲型の四隅にも適用し、tangent 連続な cubic Bezier 四分円で接続。枠線を SVG オーバーレイで 2×borderWidth 描画し、clip-path で外側半分をカットして内側 borderWidth を可視化 (後 v0.5.194 で外側描画に変更)。

[gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) / [gb-board-style-manager.js](app/gb-board-style-manager.js) / [gb-board-ui.part01.js](app/gb-board-ui.part01.js) / [gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) / [gb-canvas-interact.js](app/gb-canvas-interact.js)

## v0.5.186 (2026-04-18)

### feat(board): 雲型に角丸とズラし量の数値設定を追加 (中間版)

ズラし量を 0–100% 可変化、角丸 (border-radius) を雲型にも適用。v0.5.187 で本実装化。

## v0.5.185 (2026-04-18)

### feat(board): 雲型パラメータを詳細パネルで設定可能に (中間版)

山の幅・高さ・張り出し量を詳細パネルから設定可能に。v0.5.187 で全パラメータ統合。

## v0.5.184 (2026-04-18)

### feat(board): 雲型の左右辺にも山を配置

左右辺も単一のふくらみから固定サイズの山の繰り返しに拡張。

## v0.5.183 (2026-04-18)

### feat(board): 雲型を clip-path: path() + 動的 Bezier に変更

静的ポリゴンを撤廃し、カードサイズから固定サイズの山を動的に計算する方式に変更。山の個数は `max(2, round((width-2*SIDE)/40))` で決定。cubic Bezier でなめらかな曲線に。

## v0.5.182 (2026-04-18)

### feat(board): 雲型の輪郭を見直し (56 点ポリゴン化)

旧 36 点 (うねりの幅 2–3%) から 56 点 (山の高さ 20–25%、谷の凹み 6%) の典型的な積雲形状に変更。v0.5.183 で動的計算に置き換え。

## v0.5.181 (2026-04-18)

### feat(board): 詳細パネルの色スウォッチ並び順・プレビュー改善

カードスタイル詳細の色スウォッチ配置と表示を改善。

- カード Row 1 の並び順: 背景色 → 文字色 → 文字フチ色 → 枠線色 → 背景色 → **枠線色** → 文字色 → 文字フチ色 (枠線色を背景色の次に配置)
- ラインラベル Row 3 の並び順: テキスト文字色 → テキスト背景色 → テキスト枠線色 → **テキスト背景色 → テキスト枠線色 → テキスト文字色**
- 文字色・文字フチ色スウォッチに新 `bgColor` オプション追加。現在の bgColor に背景を揃え、実際の見え方をプレビュー。
- [gb-format-popup.js:92-111](app/gb-format-popup.js#L92) / [gb-board-style-manager.js:412-493](app/gb-board-style-manager.js#L412)

## v0.5.180 (2026-04-18)

### feat(board): ラインラベルのカラースタイル対応 + 徹底チェック修正

ラインのラベル (テキスト) にカードと同様の色カスタマイズを追加。`labelTextColor` / `labelBgColor` / `labelBorderColor` の 3 プロパティ。

- conn 個別上書きとラインスタイルプリセットの両方で設定可能。
- ラインスタイル編集モーダル・コンテキストメニューの「テキスト」サブメニューに色スウォッチを追加。
- ファイル保存・読込、Undo/Redo、プリセット再適用時の個別値クリア等に対応。
- 徹底チェックで発見した 3 件を同時修正: paste 時のラベル色欠落、「新スタイルとして保存」での個別上書き値消失、リセット時のクリア対象追加。
- [gb-board-ui.part01.js](app/gb-board-ui.part01.js) / [gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) / [gb-canvas-engine.part02.js](app/gb-canvas-engine.part02.js) / [gb-canvas-engine.part03.js](app/gb-canvas-engine.part03.js) / [gb-canvas-features.part01.js](app/gb-canvas-features.part01.js) / [gb-board-style-manager.js](app/gb-board-style-manager.js) / [gb-board-ui.part02.js](app/gb-board-ui.part02.js)

## v0.5.179 (2026-04-18)

### fix(board): ラインの「…」ボタンをクリック可能に修正

ラインホバー時に表示される「…」(メニュー) ボタンのクリックが反応しない不具合を修正。

- **原因**: ボタン上で pointerdown するとキャンバスまでバブルし、キャンバスの pointerdown ハンドラが空白クリック扱いで `bdSelect(null)` → `_removeConnActionBtn()` を呼ぶため、click 発火前にボタンが DOM から消えていた。
- **修正**: [gb-canvas-engine.part02.js:73-98](app/gb-canvas-engine.part02.js#L73) ボタンに `pointerdown` でメニューを開く方式に変更 (click まで待たない)。加えて pointer 系イベントすべてで `stopPropagation` し、hit path の `mouseleave` でも `relatedTarget` がボタンの場合は hide タイマーを起動しないようにした。

## v0.5.178 (2026-04-18)

### fix(board): 直角線の兄弟ライン重なりとカード距離による長さズレを修正

直角線で同一カードから出る兄弟ラインの水平/垂直区間が微妙にズレる問題を修正。gap 適用方向を pathType ごとに分離 (直線: 中心ベクトル沿い, L 字系: 軸方向のみ)。

## v0.5.177 (2026-04-18)

### refactor(board): 接続線の曲げポイント機能を全削除

新規追加する UI がないレガシー機能「曲げポイント」を一式削除。`c.bends` フィールド、`bdDrawConns` の pts 配列への組み込み、SVG 曲げハンドル (`.bd-curve-handle`)、曲げハンドルのドラッグ/右クリック削除、コンテキストメニュー「曲げをリセット」等を撤廃。旧データの bends は読み込み時に無視。

## v0.5.176 (2026-04-18)

### fix(board): ラインの方向を構造ベースで決定、curve の強制 L 字化を削除

構造タイプ (logic/timeline/mindmap: 左右方向、orgchart/flowchart: 上下方向) からライン方向を決定するよう変更。`_bdBuildConnectionPathData` の curve 側で L 字分岐を強制する処理を削除し、素直な曲線/直線にフォールバックするよう修正。

## v0.5.175 (2026-04-18)

### feat(board): 曲線/直角線では辺の中央を指すよう変更

曲線・直角線でエンドポイントがカード端点の中央 (辺の中点) を指すよう変更。v0.5.174 のカード中心基準クリップ方式をベースに、curve/orthogonal では左右または上下の辺中央から始まるよう `effectiveStructure` で制御。

## v0.5.174 (2026-04-18)

### feat(board): ラインをカード中心基準のクリップ方式に変更

接続線の始点・終点をカード中心を結ぶ直線上で両端のカード矩形でクリップする方式に変更。旧来の「構造に応じて上下端/左右端を選択」方式から移行。

## v0.5.173 (2026-04-18)

### refactor(board): 詳細パネル基本タブの重複タイトル削除

詳細パネル基本タブで重複していたタイトル表示を整理。

## v0.5.172 (2026-04-18)

### feat(board): スタイル保存時に現在値をデフォルト化 + リセットをアイコンボタン化

スタイル保存/上書き保存で `_default` に現在値をスナップショットし、以降のリセットで保存時状態に戻るよう変更。詳細パネルのリセットボタンをアイコン化。

## v0.5.171 (2026-04-18)

### fix(board): スタイル保存ボタンと詳細パネル編集の stale-reference 修正

`bdEnsureBoardUiState` が `bd.cardStyles` を新配列に差し替えるタイミングで bind 時に取得した style 参照が古くなる問題を修正。`beforeEdit` で毎回現在の参照を取り直すよう変更。

## v0.5.170 (2026-04-18)

### feat(ui): 詳細パネルセクション境界の統一 + 設定ダイアログ修正

詳細パネル各セクションの境界スタイルを統一。設定ダイアログの表示不具合も修正。

## v0.5.169 (2026-04-17)

### docs: 2026-04-17 調査セッション成果物と UI サンプル配置ルール追加

調査セッションで作成した計画書・現行機能リスト・UI サンプル一式を `app/docs/` に追加し、UI サンプル HTML の配置場所ルールを `app/AGENTS.md` に明文化。

- [app/AGENTS.md](app/AGENTS.md) に「UIサンプル / プレビュー用HTMLの配置場所」セクションを追加。UI サンプル / モックアップ / プレビュー用 HTML は必ず `app/docs/` 配下に配置し、`C:\tmp\` 等の一時フォルダには作成しない運用を明記
- [app/docs/deferred-bugs-fix-plan-2026-04-17.md](app/docs/deferred-bugs-fix-plan-2026-04-17.md) — 2026-04-17 全体チェック残課題の修正計画 (v0.5.165〜v0.5.168 の元計画)
- [app/docs/source-folder-chatbot-plan.md](app/docs/source-folder-chatbot-plan.md) — ソースフォルダ別チャットボット化の実装計画
- [app/docs/tab-pin-removal-plan.md](app/docs/tab-pin-removal-plan.md) — タブピン留め廃止計画 (v0.5.160 元計画)
- [app/docs/terminology-cleanup-plan-2026-04-17.md](app/docs/terminology-cleanup-plan-2026-04-17.md) — ユーザー向け文書での用語統一クリーンアップ計画
- [app/docs/meldex-current-features.md](app/docs/meldex-current-features.md) — Meldex 現行機能リスト (単一ソース)
- [app/docs/chatbot-ui-samples.html](app/docs/chatbot-ui-samples.html) — ソースフォルダ別チャットボット UI 案サンプル

コード変更なし。

## v0.5.168 (2026-04-17)

### 徹底チェック: 内包カード相対座標化の波及修正 6 件

v0.5.166 D-6 で contained カードの座標系を相対座標に統一したが、絶対座標前提で書かれていた周辺処理を洗い出して修正。全体チェック (18 バッチ) では該当ファイル以外に実バグは検出されず、問題はすべて v0.5.166 の波及範囲に限定されていた。

#### P-1. bdAlign: contained カードを整列対象から除外
[gb-canvas-engine.part02.js:656-661](app/gb-canvas-engine.part02.js#L656) 左/右/上/下/中央/分散の min/max 計算に contained カード (相対座標) を混入させると他のカードの位置が大きく歪むため、フィルタで除外。

#### P-2. _bdArrangeItemsForSelection: contained をパッキング対象から除外
[gb-canvas-engine.part02.js:749](app/gb-canvas-engine.part02.js#L749) `bdArrangeByWidth` / `bdArrangeByHeight` が相対座標を絶対値として min/max するのを防ぐ。

#### P-3. bdCloneNodesWithOffset: 親未コピー時の相対→絶対変換
[gb-canvas-engine.part02.js:961-966](app/gb-canvas-engine.part02.js#L961) 貼り付け時、source.contained=true だがコピー元に親が含まれない場合、元親の絶対座標を足して絶対値に戻す。これにより独立カードとして正しい位置に配置される。

#### P-4. bdFitAll: contained を bounds 計算から除外
[gb-canvas-engine.part02.js:895-898](app/gb-canvas-engine.part02.js#L895) 相対座標 (小さい値) が混ざって zoom 計算が歪むのを防ぐ。すべて contained だった場合のフォールバックも追加。

#### P-5. bdDrawFrames: グループ範囲計算から contained を除外
[gb-canvas-engine.part02.js:29](app/gb-canvas-engine.part02.js#L29) グループ枠の bounds 計算で contained を混入させない。

#### P-6. bdExportImage / bdCreateSummary / floating minimap / minimap
[gb-canvas-features.part01.js:310-316](app/gb-canvas-features.part01.js#L310) / [gb-canvas-features.part01.js:529-540](app/gb-canvas-features.part01.js#L529) / [gb-canvas-features.part01.js:591-597](app/gb-canvas-features.part01.js#L591) / [gb-canvas-features.part01.js:610-621](app/gb-canvas-features.part01.js#L610) / [gb-canvas-minimap.js:111-122](app/gb-canvas-minimap.js#L111) / [gb-canvas-minimap.js:69-78](app/gb-canvas-minimap.js#L69) 画像エクスポート、集約カード作成、フローティングミニマップ、ボード右下ミニマップの bounds 計算・描画から contained を除外。

#### P-7. captureResizeSelection: contained をリサイズ対象から除外
[gb-canvas-interact.js:118](app/gb-canvas-interact.js#L118) 複数選択リサイズで contained (inline-block で親の中に配置) を座標スケール対象に含めないよう修正。

#### P-8. ドラッグ中の contained カード DOM 更新を抑止
[gb-canvas-interact.js:414-423](app/gb-canvas-interact.js#L414) contained カードは親の inline フローで配置されるため、ドラッグ中に `el.style.left/top` を直接書くとインライン配置と競合する。`n.x/n.y` の更新は行うが DOM 直書きはスキップ。

## v0.5.167 (2026-04-17)

### 残課題 修正バッチ 3: UI 系 7 件

計画書 [deferred-bugs-fix-plan-2026-04-17.md](docs/deferred-bugs-fix-plan-2026-04-17.md) の §3.3〜§3.4 相当。D-11 は計画書の誤検出 (ループ命名番号は `(2), (3), ...` で既に正しい) のため修正対象外。

#### D-12. 詳細パネル: フォルダ選択時にノートエディタタブが表示される
[gb-detail-panel.part01.js:852](app/gb-detail-panel.part01.js#L852) `noteEditorTypes` から `'folder'` を除外。フォルダを選択しても「エディタ」タブが表示されなくなった (フォルダは中身編集不可のため)。

#### D-13. openScenarioInScriptNote の fromExplorer 経路で初期化失敗
[gb-tool-menu.js:299-301](app/gb-tool-menu.js#L299) 既存 activeTab を scriptnote に置き換える経路で、`GBLayout.render()` が再マウントのタイミング次第で走らず 40 回リトライ後「初期化に失敗しました」となる可能性があった。loadIntoTab 起動直前に `GBLayout.render()` を明示呼び出しするよう修正。

#### D-14. activateTab の navNavigating フラグが async を待たない
[gb-app.part01.js:591-618](app/gb-app.part01.js#L591) / [gb-app.part02.js:80-100](app/gb-app.part02.js#L80) `navNavigating = true → navOpen() → false` が navOpen 内部の非同期処理を待たず、openPage/openBoard 等が完了する前にフラグが倒れて navPush が重複積載される可能性があった。`navOpen` がプロミスを返すようにし、`_withNavFlag` ヘルパーで `finally` 後にフラグを倒すよう修正。

#### D-15. dragover 中の getData が信頼できない
[gb-layout.part01.js:431-442](app/gb-layout.part01.js#L431) / [gb-layout.part01.js:488-494](app/gb-layout.part01.js#L488) 多くのブラウザで dragover 中は `getData` が空文字を返すため、locked ペインの pane 判定が効かないケースがあった。dragstart で `window._gbTabDragSrcPaneId` に送信元ペイン ID を保持し、dragover はこれを参照するよう変更。

#### D-16. closeTab 単一ペイン空状態で activeTabIndex が残存
[gb-tabs.js:141-150](app/gb-tabs.js#L141) 単一ペインで最後のタブを閉じると `pane.activeTabIndex = 0` の古い値が残り、他ロジックで `tabs[0]` が undefined になる可能性。`tabs.length === 0` 時に `activeTabIndex = -1` を明示。

#### D-17. _appendCollapsedIcons panelset の click 二重ハンドラ
[gb-layout.part01.js:644-657](app/gb-layout.part01.js#L644) `renderGroupIconButton` 内の click (switchGroup → render) と `_appendCollapsedIcons` 内の追加 click (collapsed=false → render) で render が 2 回走っていた。capture 段階で `stopImmediatePropagation` し、activeGroupId 更新と onClick を 1 回の render にまとめるよう修正。

#### D-18. showSettingsModal キャンセル時に最前面モーダルを閉じる
[gb-settings.part01.js:375](app/gb-settings.part01.js#L375) キャンセルボタンが `document.querySelector('.modal-overlay')?.remove()` で最前面モーダルを閉じるため、設定モーダルから別モーダルを開いた後の戻りで意図しないモーダルを閉じる可能性があった。`this.closest('.modal-overlay')` に変更して設定モーダル自身だけを閉じるよう修正。

## v0.5.166 (2026-04-17)

### 残課題 修正バッチ 2: ボード機能系 6 件

計画書 [deferred-bugs-fix-plan-2026-04-17.md](docs/deferred-bugs-fix-plan-2026-04-17.md) の §3.2 相当。

#### D-5. bdCloneNodesWithOffset の contained 強制上書き
[gb-canvas-engine.part02.js:956-961](app/gb-canvas-engine.part02.js#L956) 貼り付け時の parent リマップで `nn.contained = !!mappedParent` と元ノードの `contained` 値を無視して強制設定していた。マインドマップ構造の親子 (`contained=false`) がコピー時に contained 化されてレイアウトが破壊される問題を修正。`nn.contained = !!(source?.contained && mappedParent)` に変更。

#### D-6. 内包カードの座標系不整合 (設計判断: 相対座標へ統一)
[gb-canvas-features.part02.js:248-262](app/gb-canvas-features.part02.js#L248) / [gb-canvas-features.part02.js:436-450](app/gb-canvas-features.part02.js#L436) / [gb-canvas-engine.part01.js:151-162](app/gb-canvas-engine.part01.js#L151) / [gb-canvas-features.part02.js:262-277](app/gb-canvas-features.part02.js#L262) 「内包」パスが子カードの絶対座標のまま `contained=true` にしていた一方、release 側は `ch.x += parent.x` と相対座標扱いしていたため、解除時にカードが遠くへ飛んでいた。互換性を無視して**相対座標統一**の方針 (案 B) を採用: 内包時に `ch.x -= nd.x; ch.y -= nd.y` で相対化。`bdDetachParentChildRelation` と「親から切り離す」メニューも release 経路に揃えた。既存 `.board.md` に contained=true が残っているデータは次回解除時に位置ズレするため手動 D&D で回復が必要。

#### D-7. bdAreAllLinesSelected の読み取り副作用
[gb-canvas-engine.part01.js:68-82](app/gb-canvas-engine.part01.js#L68) 読み取り専用の is-check 関数 `bdAreAllLinesSelected()` が内部で `bdGetSelectedConnectionIds()` → `bdPruneConnectionSelection()` → `bdSetConnectionSelection()` と副作用を生んでいた。副作用なしの `_bdPeekSelectedConnectionIds()` を追加し、check 関数はこちらを使うよう変更。

#### D-8. bdOpenLinkedPath の過剰分割
[gb-board-links.js:108-113](app/gb-board-links.js#L108) page → pivot → scriptnote のように異なるタイプのリンクを連続クリックすると毎回 `splitPane` で新規ペインが増えていた。`_bdFindSidePane` 再利用を `opts.ctrlKey` 有無に関わらず有効化し、既存の非ツールペインを優先再利用するよう修正。

#### D-9. `.board.json` / `.canvas.json` の解決タイプ不整合
[gb-board-links.js:59](app/gb-board-links.js#L59) `_bdFileIcon` は `.board.json` / `.canvas.json` を board アイコンにマップする一方、`_bdResolveLinkedEntry` は複合拡張子判定がなく `json → page` に解決していた。`.board.json` / `.canvas.json` を board タイプとして解決する分岐を追加。

#### D-10. `.jpg + linkType=page` のとき media 解決になる
[gb-board-links.js:54](app/gb-board-links.js#L54) `explicitType='page'` の分岐が無いため、`.jpg` に linkType=page を明示しても media として開かれていた。explicitType=page の分岐を拡張子判定より前に追加。

## v0.5.165 (2026-04-17)

### 残課題 修正バッチ 1: データ永続化・保存系 4 件

計画書 [deferred-bugs-fix-plan-2026-04-17.md](docs/deferred-bugs-fix-plan-2026-04-17.md) の §3.1 相当。

#### D-1. ボード本文の `#` エスケープ欠落
[gb-canvas-engine.part01.js:376-392](app/gb-canvas-engine.part01.js#L376) / [gb-canvas-engine.part01.js:570-584](app/gb-canvas-engine.part01.js#L570) カード本文の 2 行目以降に `# 見出し` が含まれると再読込時にカードが分裂していた。保存時に行頭 `#\s` を `\#\s` へエスケープ、読込時に逆変換。既存の `\` 付き文字列も bijection で保全 (`\#` → `\\#`)。互換性無視の方針のため既存 `.board.md` の 2 行目以降 `#` を含むファイルは一度開き直して保存すると正常化する。

#### D-2. bdSave の `_lastSavedNodeIds` 更新漏れ
[gb-canvas-engine.part02.js:911-918](app/gb-canvas-engine.part02.js#L911) 保存成功後 `unchanged=false` のとき `_lastSavedNodeIds` が更新されないため、連続保存で孤児化 API (`/annotations/orphan-by-target`) が同じカード ID に対し重複呼び出しされ得た。保存成功後は `unchanged` に関わらず必ず更新するよう修正。

#### D-3. file_id キャッシュの rename/delete 同期漏れ
[gb-app.part01.js:252-283](app/gb-app.part01.js#L252) / [gb-app.part01.js:378-392](app/gb-app.part01.js#L378) リネーム時に `_fileIdCache[oldPath]` が残り、`_pathToFileId(newPath)` が空文字を返して `addRecent()` などが file_id を付けられなかった。削除時も古いキャッシュエントリが残留。子孫パスも prefix マッチで remap する。

#### D-4. localStorage マイグレーション走査中のキー変化
[gb-app.part01.js:76-92](app/gb-app.part01.js#L76) 初回マイグレーションで `localStorage.key(i)` と `removeItem`/`setItem` を同じループ内で行っていたため、キーインデックス変化によって特定のキー群で変換漏れが発生し得た。先に全キーを配列化してから走査するよう修正。

## v0.5.164 (2026-04-17)

### 全体チェックで発見した上位バグ修正（バッチ 1）

#### A. ミニマップの関数名ミス（v0.5.161 で混入）
[gb-canvas-minimap.js:86](app/gb-canvas-minimap.js#L86) `_bdConnStyle` という関数は存在せず、正しくは `bdGetConnectionStyle`。このためラインスタイル反映が常に失敗し、styleRef で色/太さ指定しても `c.color` が無いラインは accent 色の単色で描画されていた。正しい関数名に修正。

#### B. bdTransform の zoom clamp 未使用
[gb-canvas-engine.part02.js:845-852](app/gb-canvas-engine.part02.js#L845) 事前に `const zoom = Math.max(0.1, bd.zoom || 1)` で clamp していたのに `transform: scale(...)` には未 clamp の `bd.zoom` を使っていた。`bdLoadState`/undo で異常値 (0/負/NaN) が混入した際の非表示/反転を防止。

#### C. bdFitAll の null チェック欠落
[gb-canvas-engine.part02.js:885-888](app/gb-canvas-engine.part02.js#L885) `document.getElementById('bd-canvas')` が null のまま `clientWidth` を参照していたため、ボード DOM 未生成のタイミングで TypeError の可能性があった。null チェックで return するように修正。

#### D. 空ラベルを dblclick → ESC で「テキスト」が残る
[gb-canvas-engine.part02.js:457](app/gb-canvas-engine.part02.js#L457) / [gb-canvas-features.part02.js:98](app/gb-canvas-features.part02.js#L98) `conn._labelWasEmpty` で編集前の原値を記録し、ESC 時は空に戻す。ダブルクリック側の即 `bdDirty()` も撤廃し、編集確定時にのみ永続化。

## v0.5.163 (2026-04-17)

### 全体チェックで発見した中優先度バグ 3 件（バッチ 2）

#### A. moveTab クロスペイン activeTabIndex 補正漏れ
[gb-tabs.js:191](app/gb-tabs.js#L191) `idx < fromPane.activeTabIndex` のとき `activeTabIndex--` が無かった問題を修正。

#### B. _dropToolToPane の分割失敗通知欠落
[gb-docking.js:126](app/gb-docking.js#L126) `splitPane` が null のとき `showStatus('分割上限に達しました', true)` を追加。

#### C. renderPanelSetTabbar drop の source/target 操作順序
[gb-panelset.js:334](app/gb-panelset.js#L334) `insertFreeNodeAsColumn` を source 整理より先に呼ぶよう順序を入れ替え、最後に明示 render/saveLayout。

## v0.5.162 (2026-04-17)

### cf ダイアログの単一行メッセージを従来スタイルに戻す
v0.5.159 で `_buildCfDialogBody` が全メッセージを「先頭行=タイトル」扱いにしていたため、単一行 (`このダッシュボードを削除しますか？` など) が h3 見出しサイズで表示される UX 変化が発生。単一行は従来の 14px 本文 div、複数行のみタイトル化する分岐を追加。

## v0.5.161 (2026-04-17)

### ボード: リンクアイコン/矢印可視性/ミニマップ/直角線/永続化 5 件修正

#### A. リンクアイコンをアプリ種別で表示
[gb-board-links.js:158](app/gb-board-links.js#L158) `_bdFileIcon(ext)` を `_bdFileIcon(ext, path)` に拡張。複合拡張子 `.scriptnote.json` / `.board.json` / `.canvas.json` / `.smart-db.json` / `.dashboard.json` を優先判定し、既存の `uiTypeIconName()` マッピングに委譲してシナリオ/ボード/スマートシート/ダッシュボードの正式アイコンを表示。`.md` もノートエディタのアイコンに揃う。`gb-canvas-engine.part01.js` の呼び出し側に `n.link` を追加渡し。

#### B. 矢印がカードに隠れないよう常に前面
[gb-tools.css:1450](app/gb-tools.css#L1450) `#bd-svg { z-index: 0 }` → `z-index: 11`。`#bd-nodes` の子ノード (z-index: 2+depth) より前面、`#bd-resize-layer` (z-index: 12) より背面に配置。SVG 側は `pointer-events: none` なのでカードのクリック・操作は阻害しない。

#### C. ミニマップのフィットを廃止 + ライン色反映
[gb-canvas-minimap.js](app/gb-canvas-minimap.js) `Math.min(W/bounds.w, H/bounds.h) * 0.9` → 固定倍率 `_bdMinimapScale() = 0.12`。ボード全体の重心を canvas 中心にアライン。接続線の `ctx.strokeStyle` を `'#888'` 固定 → `c.color || style.color || accentColor` に変更し、カードの塗色と同じ扱いで反映。

#### D. 再ロードで矢印が消える問題
[gb-canvas-engine.part01.js](app/gb-canvas-engine.part01.js) 保存・読込で `arrow` フィールドを扱う際、`c.arrow = ''` でプロパティが存在すると `bdGetConnectionStyle` の `hasOwnProperty` チェックで style 側 arrow のフォールバックが無効化されていた。修正:
- 保存: `arrow: ${c.arrow || 'none'}` 固定出力 → 明示値 (`end`/`both`/`start`) のときだけ書き出し
- 読込 (frontmatter): 初期値 `arrow:true` を削除。`arrow: none`/`false` の場合はプロパティを設定せず style の矢印に委ねる
- 読込 (JSON board block): 明示指定時のみ `conn.arrow` を設定

#### E. 「直角線」ライン形状を 2 段階（コの字）化
[gb-canvas-engine.part02.js:246](app/gb-canvas-engine.part02.js#L246) `_bdBuildConnectionPathData` の `orthogonal` 分岐で、structure 未指定の場合も `pts.length === 2` なら必ず 2 段階折れ曲がり (`start → branch → branch → end`) を採用。start-end の優勢ベクトル方向に応じて水平中継 (logic 流) / 垂直中継 (flowchart/orgchart 流) を自動選択。Why ツリーのような「縦→横→縦」が実現可能になる。

## v0.5.160 (2026-04-17)

### タブピン留め機能を完全撤去

計画書: [docs/tab-pin-removal-plan.md](app/docs/tab-pin-removal-plan.md)

パネルタブ右クリック →「ピン留め」サブメニューから設定できたタブピン留め機能を廃止。誤クローズ防止は「他のタブをすべて閉じる」「ペインロック」で代替可能で、ラベルが非表示になる副作用が UX に悪影響を与えていたため。DB 列ピン留め / フォルダツリーのクイックアクセスは別機能のため保持。

#### フェーズA (fd189ca): UI エントリポイント除去
- [gb-layout.part02.js](app/gb-layout.part02.js) の「ピン留めサブメニュー」ブロックを削除
- 「タブを閉じる」の `if (!tab.pinned)` 条件を外し常に表示

#### フェーズB (0ca03d1): 表示ロジック除去
- [gb-layout.part01.js](app/gb-layout.part01.js) の `sortedTabs` (pinned 優先ソート) を削除、`node.tabs.forEach((tab, i) => ...)` に戻す
- `tabEl.className` の `' pinned'` クラス付与を削除
- [gb-ui.js](app/gb-ui.js) `createTab()` の `opts.pinned` オプションと JSDoc 記述を削除

#### フェーズC (a5abfc0): 書き込み系・ガード除去
- [gb-tabs.js](app/gb-tabs.js) `closeTab()` のピン留めガード (`if (pane.tabs[idx].pinned) return;`) を削除
- [gb-app.part01.js](app/gb-app.part01.js) / [gb-tool-menu.js](app/gb-tool-menu.js) の `activeTab.pinned = false` 強制解除を削除
- [gb-layout.part01.js](app/gb-layout.part01.js) `_normalizePaneNode` に `delete tab.pinned` を追加 → ロード時に既存 JSON の残存プロパティが一掃され、次回 `saveLayout()` で JSON から消える

#### フェーズD (c3376bf): CSS 除去
- [gb-layout.css](app/gb-layout.css) の `.gb-tab.pinned` 3 ルールを削除
- [gb-ui.css](app/gb-ui.css) の `.gb-panel-tab.pinned` ルールを削除

#### 副次解消 (§3.7)
- **新しいウィンドウで開くでピン留めタブが残る既存バグ**: `closeTab` ガード削除により正常動作
- **D&D でウィンドウ外にドロップした際の同種バグ**: 同上
- **`global.closeTab` ショートカットがピン留めタブで無反応だった問題**: 解消

#### フェーズF: ドキュメント更新
- [ui-design-system-spec.md](app/docs/ui-design-system-spec.md) / [ui-spec-audit-report.md](app/docs/ui-spec-audit-report.md) / [submenu-popup-migration-plan.md](app/docs/submenu-popup-migration-plan.md) / [icon-implementation-plan.md](app/docs/icon-implementation-plan.md) / [icon-proposal-sample.html](app/docs/icon-proposal-sample.html) から `.pinned` 記述を除去・廃止を明記
- `pin` アイコンは DB 列固定で使用するため残置

## v0.5.159 (2026-04-17)

### 確認系ダイアログのタイトル表示を追加

`cfAlert` / `cfConfirm` / `cfPrompt`（[gb-app.part02.js](app/gb-app.part02.js#L665)）はタイトル要素 `<h3>` を持たず、本文 div だけで構成されていた。そのためメッセージ全体が左寄せの地の文として表示され、`.modal` の `padding 16px` + `.btn-row` の `margin-top 12px` という枠取りだけが目立つ結果、上下に大きな空白があるダイアログに見えていた。「レイアウトを初期化しますか？\nページをリロードします。」のような 2 行メッセージでも、1 行目をタイトル扱いにする仕組みがなかった。

修正:
- ヘルパー `_buildCfDialogBody(message)` を追加。改行で先頭行をタイトルとして `<h3>`、残りを本文 div に分離
- `cfAlert` / `cfConfirm` / `cfPrompt` すべてこのヘルパーに統一
- 既存の呼び出し（単一行/複数行）どちらでも自然に見えるよう fallback あり
- タイトルは既存 `.modal h3` の CSS（`margin-bottom: 12px; color: var(--ui-accent);`）をそのまま使うため、他のモーダルとの統一感も維持

## v0.5.158 (2026-04-17)

### 徹底チェックで発見した 2 件を修正

#### A. panelset-icon の drop で preventDefault 忘れ
[gb-panelset.js:94](app/gb-panelset.js#L94) v0.5.157 で親タブバーに委譲する経路（`parentSide === 'left'/'right'` のとき）を追加したが、`e.preventDefault()` を呼ばずに `return` していたため、ブラウザのデフォルト drop 動作（テキスト/URL ナビゲーション等）が走る可能性があった。preventDefault を追加し、stopPropagation は呼ばず親 tabbar へ bubble を維持。

#### B. renderAsColumn で panelset に data-split-id を誤設定
[gb-layout.part01.js](app/gb-layout.part01.js) v0.5.156 の panelset collapsed ブランチで `bar.dataset.splitId = node.id` を設定していたが、対象は panelset であって split ではない。`_updateColumnIndicator` のフォールバック検索で `[data-split-id="..."]` に hit すると同一 ID の要素が複数扱われる懸念があるため削除。`data-column-node-id` のみに統一。

## v0.5.157 (2026-04-17)

### 折りたたみアイコン全表示 + panelset タブバーのカラム間インジケータ

#### A. panelset 折りたたみ縦バーで全アイコンを表示
v0.5.156 の折りたたみ縦バー化では、各グループの先頭 type しかアイコン化していなかった（`_appendCollapsedIcons` の panelset 分岐）。ユーザー要望に応えて、通常タブバーと同じ `renderGroupIconButton` を流用し、各グループの全 type アイコンを横並び（1 ボタン内）でまとめつつ、ボタン自体を縦に並べる形に変更。折りたたみ時のクリックで展開も行われるよう追加 listener を差し込み。

CSS: [gb-layout.css](app/gb-layout.css) に `.gb-split-collapsed-horizontal .gb-panelset-icon { min-width: 24px; max-width: 28px; padding: 0 2px; }` を追加して 32px 幅に収める。

#### B. panelset タブバー上でカラム間インジケータが全く出ない問題
panelset の各グループアイコンと tabbar 自身が `x-gb-panelset-group` を dragover で `stopPropagation` していたため、親のペイン `.gb-pane` に dragover が届かず、カラム間（left/right）のエッジインジケータが出なかった。加えて、パネルセットタブバー DOM はペインの外側にあるため、もともとペインの dragover は発火しない構造だった。

修正:
- [gb-docking.js](app/gb-docking.js) に `showEdgeIndicator(el, side)` を public API として公開。任意の要素の左/右辺に 6px の縦バーを描画
- [gb-panelset.js:211](app/gb-panelset.js#L211) `renderPanelSetTabbar` の dragover で、マウス X がタブバーの左右 15% 内なら `side='left'/'right'` を判定し、`bar.closest('.gb-column')` を参照して `GBDocking.showEdgeIndicator` を呼び出し、カラム全高の縦バーインジケータを描画
- drop 時に `side='left'/'right'` なら `insertColumnAround` / `insertFreeNodeAsColumn` で新カラム挿入、`center` なら従来通り panelset マージ
- `renderGroupIconButton` の btn.dragover から `stopPropagation` を削除して親タブバーに bubble 到達させ、親側の side 判定が優先される場合は btn の drop 処理をバイパス

## v0.5.156 (2026-04-17)

### パネルセット D&D 強化 + 折りたたみ表示修正 3 件

#### A. パネルセットカラム折りたたみ時、タブが横並びで見切れていた
[gb-layout.part01.js:182](app/gb-layout.part01.js#L182) 水平分割の子として panelset が折りたたまれたとき（`node.collapsed` が付与される経路）、`renderAsColumn` が通常通りタブバーを描画していたため、32px 幅のカラムに対してタブバーが横方向にはみ出していた。

修正: `renderAsColumn` で panelset かつ collapsed なら、split 折りたたみと同じ `.gb-split-collapsed-horizontal` 縦バーで描画（ドラッグハンドル + `_appendCollapsedIcons` による各グループの縦並びアイコン）。

#### B. パネルセットタブの並び替えができなかった
[gb-panelset.js:73](app/gb-panelset.js#L73) グループアイコンボタンには dragstart/dragend はあったが dragover/drop がなく、タブ同士の間にドロップしても並び替えできなかった。

修正:
- グループアイコンボタンに `dragover`/`dragleave`/`drop` を追加。左右半分で前後挿入位置を判定
- 新関数 `reorderOrMoveGroup(srcPsId, grpId, tgtPsId, tgtGrpId, pos)` を追加し、同一 panelset 内では並び替え、別 panelset なら指定位置へ移動
- CSS で `.gb-panelset-drop-before/after` の挿入線インジケータを追加

#### C. パネルセットタブ D&D / フォルダツリー隣接時にカラム間インジケータが出ない
[gb-docking.js:294](app/gb-docking.js#L294) カラムドラッグ（`x-gb-column`）の時しか left/right ゾーン判定していなかったため、パネルセットタブ（`x-gb-panelset-group`）のドラッグでは新カラム挿入のインジケータが出ず、常に中央マージ扱いだった。

修正:
- dragover での zone 判定を panelset-group の場合にも適用
- drop 時、edge（left/right）なら新関数 `GBPanelSet.insertGroupAsColumn(...)` を呼んでグループを新カラムとして挿入
- `insertFreeNodeAsColumn(newNode, targetId, pos)` を [gb-layout.part02.js](app/gb-layout.part02.js) に追加（未アタッチ自由ノードを新カラムでラップ挿入）

残課題: フォルダツリーに隣接する領域でインジケータが出ない件は、フォルダツリー DOM がペイン外にあるのが原因と推測。要再現確認の上、次回対応。

## v0.5.155 (2026-04-17)

### カラム D&D バグ 2 件修正

#### A. 複数パネルタブがあるカラム（panelset）にドロップできない
[gb-panelset.js:211](app/gb-panelset.js#L211) panelset のタブバー（グループアイコン列）が `application/x-gb-column` を dragover で受け付けず `preventDefault` を呼ばなかったため、そのタブバー上にドロップ操作をしても drop イベントが発火せずカラムマージが成立しないケースがあった（タブバー外の領域なら成立するが、ユーザーの D&D 経路によっては常にタブバーに吸われる）。

修正: `renderPanelSetTabbar` の dragover/drop で `x-gb-column` も受理し、`applyColumnDrop(sourceNodeId, panelsetNode.id)` に流す。

#### B. カラムとカラムの間（新カラム挿入）にドロップできない
従来の `_setupColumnDropOnPanes` はペイン内のどこに落としても zone 判定せず `applyColumnDrop`（中央マージ）に一本化していたため、**新しいカラムとして挿入**する経路が存在しなかった。

修正:
- [gb-layout.part02.js](app/gb-layout.part02.js) に新関数 `insertColumnAround(sourceId, targetId, position)` を追加。target を新しい水平 split でラップして source と並置する（position=left/right）
- [gb-docking.js:290](app/gb-docking.js#L290) の `_setupColumnDropOnPanes` で `getDropZone` を呼び、zone が `left`/`right` なら `insertColumnAround`、それ以外は従来通り `applyColumnDrop`
- 同ファイルに `_updateColumnEdgeIndicator` を追加し、left/right ドロップ時はカラムの該当辺に 6px の縦バーをハイライト表示して「新カラム挿入位置」を可視化

## v0.5.154 (2026-04-17)

### 折りたたみバー（split ノード）にドラッグハンドル追加

カラム内に垂直分割で複数パネルが積まれた状態で折りたたむと、`.gb-split-collapsed` バーに**ドラッグハンドルが無く、カラム全体の移動ができなかった**。単独パネルの折りたたみは pane 側の dragHandle が残るので移動できるが、複数段（= split ノードが collapsed）のケースだけ移動手段が欠けていた。

修正:
- [gb-layout.part01.js:639](app/gb-layout.part01.js#L639) `renderSplit` の collapsed ブランチで、`_appendCollapsedIcons` の前にドラッグハンドル（`.gb-split-collapsed-drag-handle`）を先頭に差し込み
- `dragstart` で `_findColumnAncestorId(node.id) || node.id` を使ってカラム ID を解決し、`application/x-gb-column` ペイロードで発火（既存の `applyColumnDrop` 経路に乗る）
- `_findColumnAncestorId` を pane 限定から任意ノード ID 対応に一般化（`node.type === 'pane'` チェックを `node.id === nodeId` に置換）
- [gb-layout.css:692](app/gb-layout.css#L692) ハンドルのスタイルを追加。縦長バーでは `transform: rotate(90deg)` で横向き、横長バーでは縦向きに

## v0.5.153 (2026-04-17)

### 縦長バー時のドラッグハンドルを横向きに

パネルを水平分割で折りたたんだ時（縦長バー）、ドラッグハンドルアイコンが `gripVertical`（縦棒2本）のままでアイコンの向きが縦並びと合わず不自然だった。

修正: [gb-layout.css:684](app/gb-layout.css#L684) に `.gb-pane-collapsed-horizontal > .gb-pane-tabs .gb-pane-drag-handle svg { transform: rotate(90deg); }` を追加し、縦長バー時のみグリップアイコンを横向きに回転。垂直分割での折りたたみ（横長バー）は縦棒のまま。

## v0.5.152 (2026-04-17)

### パネル境界線の掴みやすさ向上

[gb-layout.css:765](app/gb-layout.css#L765) パネル境界線（`.gb-split-handle`）の当たり判定が 4px しかなく、カーソルで正確に合わせにくかった。

修正:
- 擬似要素 `::before` で当たり判定を両側 2px ずつ拡張し、見た目を変えずに hit area を 8px に
- ホバー時は拡張領域も薄くアクセント色で光らせ、掴める範囲を可視化
- `@media (pointer: coarse)`（タッチデバイス）では拡張を両側 4px（合計 12px）に増やす

境界線の見た目の太さは 4px のまま。隣接パネルの `.gb-pane-ctrls`（右 padding 4px）等と干渉しないよう拡張幅を調整済み。

## v0.5.151 (2026-04-17)

### 折りたたみバー 全タブアイコン表示 + 画面内収納

#### A. 折りたたみ時にすべてのタブアイコンを順番通りに表示
[gb-layout.part01.js:593-635](app/gb-layout.part01.js#L593) `_appendCollapsedIcons` は、pane 処理で `activeTabIndex` のタブアイコンしか表示していなかった。タブが 3 つあっても 1 つしか見えず、どのパネルに何が入っているか折りたたみ時に識別できない問題。

修正: `n.tabs.forEach` で全タブを順番通りにアイコン化。クリックでそのタブをアクティブ化して展開。アクティブタブには `.active` クラスを付与して視認性を確保。

#### B. pane 折りたたみ時にタブが横並びで幅からはみ出していた（本命修正）
[gb-layout.css:628](app/gb-layout.css#L628) pane 単独の折りたたみ（`.gb-pane-collapsed`）では `.gb-pane-tabs` を `flex-direction: column` に切り替えていたが、**中間コンテナ `.gb-pane-tabs-scroll` は `flex-direction: row` のまま**だったため、タブが横並びに詰め込まれ、32px 幅のペインから右へはみ出して見切れていた。

修正:
- `.gb-pane-collapsed-horizontal > .gb-pane-tabs > .gb-pane-tabs-scroll` に `flex-direction: column` + `max-width: 32px` + `overflow-x: hidden` を指定し、タブを縦並びに
- `.gb-pane-collapsed-horizontal > .gb-pane-tabs` に `width: 32px` + `overflow-y: auto` を指定し、タブ数が多い場合は縦スクロール
- 垂直分割側（`.gb-pane-collapsed-vertical`）も同様に横並びの幅制約 + スクロール対応
- 両方に `box-sizing: border-box` を追加してはみ出し防止

#### C. split 折りたたみバーのアイコンが右にズレていた副次修正
[gb-layout.css:666](app/gb-layout.css#L666) `.gb-split-collapsed` に `box-sizing` 指定がなく、`width: 32px` + `border: 1px` + `padding: 2px` で実幅が 38px になり、親 split の 32px 固定幅からはみ出していた。

修正:
- `box-sizing: border-box` + `max-width: 32px` / `max-height: 32px` を追加
- `overflow` 設定でアイコン多数時のスクロール対応
- `justify-content: center` → `flex-start` に変更（多数のアイコンで中央あふれを防ぐ）

## v0.5.150 (2026-04-17)

### パネル D&D 追加修正

#### A. ボードキャンバスがパネル D&D を呑み込んでいた
[gb-canvas-interact.js:605](app/gb-canvas-interact.js#L605) `onCanvasDragover` / `onCanvasDrop` がインタラクティブキャンバス上で `e.preventDefault(); e.stopPropagation();` を無条件で呼んでいたため、ボード ペインを target にしてパネル/タブを D&D したとき、キャンバスがイベントを消費して**ペインの drop ハンドラが発火しない**問題があった。これにより panelset 化されず、indicator も解除されない。

修正: `MeldexDnD.isPanelDnD(types, ctrlKey)` をチェックし、パネル/タブ D&D (x-gb-pane / x-gb-tab / meldex-tool / Ctrl+meldex-node) はキャンバス側で処理せずペイン側へ委譲。

#### B. グローバル安全網
[gb-docking.js](app/gb-docking.js#L347) の `setupDropTargets` 内で `_ensureGlobalDragEnd()` を追加。`document` レベルで `dragend` と `drop` を capture phase で捕捉し、いずれの経路でも `hideIndicator()` を確実に実行することでインジケータ残留を防止。

## v0.5.149 (2026-04-17)

### パネル/カラム D&D 復活

#### A. パネルセットグループ移動が無効化されていたバグ
[gb-panelset.js:236, 272](app/gb-panelset.js#L236) で `window.GBLayout?._layoutInternal` を参照していたが、`GBLayout` は classic script の top-level `const` のため `window` 経由では取得できない（`undefined`）。`internal` が常に undefined となり、`moveGroupToPanelset` / `dropGroupOnPane` が早期 return して何も起きていなかった。

修正: 直接参照 `(typeof GBLayout !== 'undefined') ? GBLayout?._layoutInternal : null` に変更。

#### B. パネルハンドルの通常ドラッグが「将来実装」のまま
[gb-docking.js:205](app/gb-docking.js#L205) の `handleDrop` で、ペイン単独 D&D（`x-gb-pane` ペイロード）の処理が `// ペイン全体の移動（将来実装）` のまま空実装だった。

修正: `applyColumnDrop(data.paneId, targetColumnId)` を呼んで対象カラムへマージ（panelset 化）。
- 元ペインのカラムが単独ならそのカラムごと移動
- 元ペインのカラムに複数ペインある場合は該当ペインのみ detach して移動

#### C. ドロップ未成立時のインジケータ残留
ESC キャンセルや無効ゾーンへのドロップで `drop` イベントが発火せず、`hideIndicator()` が呼ばれないケースに備えて、`dragHandle` ([gb-layout.part01.js:281](app/gb-layout.part01.js#L281)) と panelset グループアイコン ([gb-panelset.js](app/gb-panelset.js#L72)) に `dragend` ハンドラを追加して `GBDocking.hideIndicator()` を確実に呼び出す。

## v0.5.148 (2026-04-17)

### 徹底チェックの副次発見 2 件 修正

#### A. パネルセットドックの右クリックメニュー close handler バグ
v0.5.146 で修正したパネル「…」メニューと同じ stale-closure バグが [`gb-panelset.js:_showGroupContextMenu`](app/gb-panelset.js#L138-L153) にも残っていた。パネルセットアイコン右クリック→「このドックを閉じる」を選んだあと別のコンテキストメニューを開くと、古い close handler が新メニューを破壊して項目クリックが効かなくなる可能性。

修正: `menu.isConnected` 自己破棄 + `inAny` 判定の同パターンを適用。

#### B. シナリオパネルでのファイル切替時 `ann.targetPath` 古いまま
シナリオパネルでアクティブのまま別シナリオファイルを `_loadScenario` した場合、`showView` がコンポーネントタイプで早期 return するため `_mountFloatingAnnotationUi` が再呼び出しされず、注釈の対象パスが旧ファイルのまま残っていた（注釈ボタン自体は表示されるが、注釈データが古いファイルを参照）。

修正:
- `GBPaneBridge.mountFloatingAnnotationUi` を public API に追加
- `ScriptNoteComponent._loadScenario` の最後で明示的に呼び出し、`ann.targetPath` と注釈オーバーレイを新ファイルに同期

## v0.5.147 (2026-04-17)

### 注釈フローティングボタン復活＋ツールバーアイコン差し替え

#### 注釈ボタン (FAB) 表示バグ修正
注釈フローティングボタン (`#btn-tb-annotation`) が、ノート / シナリオ / シート / ボード等のアプリパネル内に表示されなくなっていた問題を修正。
- `_mountFloatingAnnotationUi` がボタン/オーバーレイを再配置する際、`_retractLegacyFromPane` が付与した `display:none` を引き継いでしまっていた
- 配置先が表示先 (contentEl) のときは `display=''` で表示、ストレージ退避時のみ `display='none'` に明示分岐

これでアクティブパネルのアプリ種別が注釈ホスト (page / folder / board / scriptnote / database 系) かつファイルが開かれているとき、各パネル右下にフロートボタンが現れる。

#### Lucide アイコン追加・差し替え
書字方向 / インデント / 引用の専用 Lucide アイコンを採用:

| 用途 | 旧アイコン | 新アイコン |
|---|---|---|
| 横書き | `alignLeft` | `text-align-start` |
| 縦書き | `bookOpenText` | `kanban` |
| 見出しインデント | `alignLeft` | `indent-increase` |
| 引用 | `quote` | `text-quote` |

対象:
- ノートツールバー (`btn-note-vertical` / `btn-heading-indent` / 引用ボタン)
- シナリオツールバー (`btn-horizontal` / `btn-vertical`)

副次修正: ノートの縦書き切替ボタン (`toggleNoteVertical`) が `textContent` でアイコンを上書きしていたため、クリック後にアイコンが消える問題があった。`innerHTML` でアイコン span を入れ替え、`replaceIcons()` で SVG 化する形に修正。状態に応じてアイコンと `title` も切り替わる。

## v0.5.146 (2026-04-16)

### パネルロック解除不能バグ修正

パネルをロックすると、その後「…」メニューを開いて「ロックを解除」をクリックしても無反応になる問題を修正。

#### 原因
`_showPaneActionsMenu` の外側クリック検知 close handler が、メニュー DOM 削除後も `document` に残留していた。次回メニューを開くと、古い handler が新メニュー内のクリックを「自分のメニュー（既に削除済み）の外側」と誤判定し、新メニューを `pointerdown` 段階で破壊。結果 `click` イベントが届かず、「ロックを解除」が無反応になっていた。

#### 修正
- close handler 冒頭で `menu.isConnected` を確認し、自分のメニューが既に外されている場合はリスナー自身を破棄して終了
- 残存メニューの判定を `_showTabContextMenu` と同じく「現在開いている全 `.gb-context-menu` のいずれかに該当するか」に変更し、複数メニューが同時にあっても誤動作しないように

## v0.5.145 (2026-04-16)

### 詳細パネル スタイルタブ UI 統一・見出しグループ別行分割

ノート・シート・シナリオ・ボードの詳細パネル「スタイル」タブを、書式設定ポップアップ基準実装（`gb-fmt-popup` 系）の見た目に統一。

#### 主な変更
- 旧 `_fsChunkFields(spec.display, 3)` による「3 フィールド毎の機械的折返し」を廃止し、見出しグループごとに行を分割
- ノートのスタイルタブで、タイトル / H1〜H6 / 本文 / リンク / 引用 / 区切り線がそれぞれ独立した行に配置（行頭に「タイトル」「H1」等の見出しラベルを bold で表示）
- シートも同様にヘッダー / エントリ列 / セル / アクティブセル枠 / 罫線で行分割
- シナリオも枠線 / 見開き区切り / ホバー / カーソル / ドラッグ選択 / テキスト選択 / ドロップで行分割
- 行レベルの見出しラベルは新規 `.gb-fmt-label--group`（`min-width: 5em` の bold ラベル）で実装

#### コントロール統一
- 色スウォッチ: `.gb-color-swatch--field` → `.gb-fmt-swatch-bg`（22×22px）
- 太字/斜体ボタン: `B` / `I` を `<b>` `<i>` でラップして書体差を強調
- 数値入力: `.gb-input-sm`(68px) → `.gb-fmt-num`(70px、スピンボックスは hover/focus でフェード表示)
- テキスト入力: `.gb-input-sm` → `.gb-fmt-text`（フォント名は 120px、px 値は 60px）
- 行コンテナ: `.gb-field-row` → `.gb-fmt-popup-row gb-fmt-popup-row--wrap`
- ラベル+入力ペア: `.gb-field-inline` → `.gb-fmt-popup-group`

参照仕様: [app/docs/format-popup-ui-preview.html](app/docs/format-popup-ui-preview.html)

## v0.5.144 (2026-04-16)

### パネルアクティブ化ワンクッション完全解消（C 案 後続修正）

v0.5 系で導入した「pointerdown 経由の `setActivePane` を queueMicrotask で非同期化」だけでは、非アクティブパネル内の項目をクリックすると 1 回目のクリックがアクティブ化に消費される現象が残っていた。

#### 原因
`_mountAllPanes` → `_mountLegacyLikeTab` 内の `_retractLegacyFromPane(contentEl)` が、これから載せ直す予定のレガシーコンテナ（`db-view-container` や `page-view` など）も含めて一旦 `legacy-views` ストレージへ退避し、直後に同じコンテナを再 `appendChild` していたため、microtask 内で「detach → reattach」が必ず発生。クリック先パネルの祖先要素が瞬間的に DOM から外れることでブラウザ側の click 配信が阻害されていた。

#### 修正
- `_retractLegacyFromPane(contentEl, exceptId = null)` に「退避対象から外す ID」オプションを追加
- `_mountLegacyLikeTab` から「これから載せる `containerId`」を `exceptId` として渡し、同一コンテンツ維持時の DOM 動揺を排除
- `showView` 経由の直接マウント経路（パネルブリッジ part02 line 470 付近）も同様に対応し、`_retractLegacyFromPane` を完全 idempotent 化

これで非アクティブパネルの項目（シートのセル、ノートのリンク、ボードのカード等）が 1 クリックで反応するようになる。

## v0.5.143 (2026-04-16)

### ボード スタイル詳細パネル UI 統一・カスタム化マイグレーション

ボード詳細パネル（空白クリック時）と、カード/接続線選択時の詳細パネルで「スタイル詳細」UIを完全統一。スタイルの編集仕様を見直し、過去の override 持ちデータを「カスタム」スタイルへ自動マイグレーション。

#### UI 統一
- 詳細パネル `.bd-detail-section` の grid 内で `bd-detail-style-row` と `bd-style-summary-card` を `grid-column: 1/-1` で全幅化（横並び崩れを解消）
- カード/ライン選択時のスタイル詳細を、ボード詳細と同じ「プレビュー＋＋＋保存ボタン＋編集フィールド」構造に統一
- 編集フィールドの中央寄せを左揃えに修正（`align-items: stretch` + `justify-content: flex-start`）
- トリガープレビューを picker (`bd-style-picker-preview`) と完全同じ visual に統一（96×54、padding 6px、`bd-style-card-preview-large` 使用）

#### スタイル編集仕様の変更
- **ボード詳細パネル**: 編集 = 既存スタイル直接反映（カスタム化なし）。リセットボタンで初期設定（BD_DEFAULT）に戻す
- **カード/接続線詳細パネル**: widget 表示は effective style（override + base）、編集時は `beforeEdit` で個別 override をクリア後 base style を直接編集
- リセットボタンの動作を「色のみ初期化」→「スタイル全フィールドを BD_DEFAULT に戻す」に変更（`_bdResetStyleToDefault`）

#### バグ修正
- **「標準に戻る」現象**: `_bdBindBoardDetailPanel` で `cardStyle = bdGetCardStyleById(...)` の直後に `lineStyle = bdGetLineStyleById(...)` を呼ぶと、2回目の `bdEnsureBoardUiState` で `bd.cardStyles` が新配列に置換され、cardStyle が古い配列内の参照になる問題を修正。`bdEnsureBoardUiState` を1回明示的に呼んで `bd.cardStyles.find(...)` で直接取得するパターンに変更
- 同様の参照乖離を防ぐため、`_bdBindNodeDetailPanel` / `_bdBindConnectionDetailPanel` も `bdEnsureBoardUiState` 1回 + 直接 find に変更
- `bdNormalizeLineStyles` で保存値に欠けたキーを id 一致する `BD_DEFAULT_LINE_STYLES` から補完（旧形式ファイルで `pathType` 等が抜けている場合の対策）

#### マイグレーション
- **v5**: ビルトインスタイル（`BD_DEFAULT_*_STYLES` に id 一致するもの）の色を BD_DEFAULT で強制同期。途中で BD_DEFAULT 色を共通カラーパレット準拠に変更したが、保存ファイル側に旧色が残る問題を解消
- **v6**: 各 node/connection の個別 override を「カスタム」スタイルに変換
  - 旧仕様で `node.bgColor` 等の override で見た目を変えていたデータを `card-custom-{node.id}` / `line-custom-{conn.id}` というスタイルに昇格
  - `node.cardStyle` / `conn.styleRef` に新 ID を割り当て、override は削除
  - これでドロップダウン/トリガープレビューに「カスタム」として反映され、widget 表示と base style の乖離が解消
- **再帰呼び出し対策**: `bdApplyStylePresetMigration` 内で `bdGetNodeStyle/bdGetConnectionStyle` を使わず override + base を直接合成（`bdEnsureBoardUiState` の再帰を排除）。`bd._migrating` フラグ + try-finally で fail-safe

#### ＋ボタン / 保存ボタン
- 各スタイルトリガー右側に「＋」（新規スタイル保存）と「保存」（永続化）ボタンを追加（v0.5.139 で追加）

## v0.5.141 (2026-04-16)

### カラム/パネルセットタブ D&D 強化

- Alt+ドラッグでカラム移動中、対象カラム全体に半透明オーバーレイを表示
- パネルセットタブを Alt+ドラッグで移動可能に（他カラムのパネルセットタブバーへドロップで panelset 取り込み、通常ペインへドロップで mergeColumns で新 panelset 化）
- カラム折りたたみ時、panelset がある場合は各グループのアイコンも縦並び表示

## v0.5.140 (2026-04-16)

### ノートエディタ 5件改修

- 表セル空欄でも一行分の縦幅を保持（`td:empty` に ZWS、行高さ確保）
- ポップアップメニュー「コメントを追加」の絵文字アイコン削除
- 「ルビを設定」を cfPrompt から小ポップアップに変更
- Alt+下キーでルビポップアップを直接表示（ブロック移動は Alt+Shift+下/上 に再割り当て）
- ノート設定タブの見出しプレビューを実サイズで表示、クリックで openFormatPopup を呼び書式設定可能に

## v0.5.139 (2026-04-16)

### ボード 空白クリック時の詳細パネル スタイル編集機能

- 詳細パネルでの編集を有効化、＋ボタン（新規スタイル保存）と保存ボタン（上書き）を追加
- 「カードスタイル詳細」「ラインスタイル詳細」を段落タイトルに昇格

## v0.5.138 (2026-04-16)

### 非表示機能 廃止 (hidden-feature-removal-plan)

「フォルダ内のファイルを非表示にする」機能を全廃。計画書: `app/docs/hidden-feature-removal-plan.md`。

- **背景**: 旧機能はローカル localStorage 専用でマルチデバイス同期に未対応。ワークスペース化を検討したが労力に対する効果が釣り合わないと判断。グローバルフィルタとツリー検索で代替可能なため全廃する方向に決定
- **削除**:
  - フォルダツリー右クリックメニューの「ファイル表示」サブメニュー（フォルダ/シート対象）
  - `getFolderFilesHidden` / `setFolderFilesHidden` / `propagateFolderFilesHidden` / `applyFolderFilesHidden` / `_getFolderHiddenMap` / `_setFolderHiddenMap`
  - `applyGlobalFilter` 内の非表示マップ参照（フィルタ単独でツリー表示制御）
  - `folder-files-hidden` を file_id マイグレーション対象とエクスポート対象から除外
- **クリーンアップ**: 起動時に `folder-files-hidden` を一度だけ削除（フラグ `_folder-hidden-removed` で二重実行防止）
- **race condition 対策**: `_restoreUiConfigFromServer` でサーバーから旧 `folder-files-hidden` を書き戻さないようガード（fetch 遅延でクリーンアップ後に解決した場合のゾンビキー残存を防止）
- **コメント整理**: 設定モーダル保存処理の旧称コメント「フォルダツリー全ファイル表示設定」を除去
- **スナップショット仕様書整理**: `spec-snapshot-v5.3-20260412/folder-tree.md` の 15.2 「ファイル表示」セクション削除と後続番号繰り上げ
- **代替手段**: グローバルフィルタ（ファイル形式・最終更新日）／ツリー検索／物理的なフォルダ整理

## v0.5.137 (2026-04-16)

### ツールバー統一 (toolbar-unification-plan)

全アプリのツールバーを共通仕様に統一。計画書: `app/docs/toolbar-unification-plan.md`、§13 徹底チェック反映済み。

- **共通CSS基盤 (.tb-* クラス群)**: `gb-ui.css` に追加
  - `.tb-icon-btn` (22×22) / `.tb-text-btn` (高さ22+アイコン+テキスト) / `.tb-swatch` (22×22) / `.tb-title` / `.tb-spacer` / `.tb-range` / `.tb-range-label` / `.tb-badge` / `.tb-input` / `.tb-select`
  - 縦幅 32px (min-height)、フォント 12px、SVG 16×16 中央配置で統一
  - `.gb-toolbar button` の legacy デフォルトスタイルは `.tb-*` を `:not()` で除外
- **Lucide アイコン追加**: `meldex-core.part01.js`
  - `bold` `italic` `underline` `strikethrough` `list` `listOrdered` `quote` `heading` `wrapText`
  - `calendarPlus` `calendarDays` `calendarRange` `listChecks` `timer` `bookmarkPlus` `clipboardCheck`
  - `zoomIn` `zoomOut` `maximize`
  - `replaceIcons()` にもそれぞれ分岐を追加。ツールバー内では 16px 表示に統一
- **ノートツールバー (T1)**: 見出しインデント/余白スライダー/コールアウト/表/水平線を維持してアイコン化。書式系 B/I/U/S をアイコン化
- **自由記述ツールバー (T2)**: `#rt-toolbar` をアイコン化 (書式系・リスト系)。`#entity-rt-toolbar` に複製される既存経路はそのまま動作
- **シナリオツールバー (T3)**: 横書き/縦書き 2ボタン維持 (§13-C-9)、タイトル input 維持 (§13-B-5)、プリセット保存 `bookmarkPlus` で維持 (§13-B-6)、各操作 (フィルタ/検索/更新/詳細/列追加) をアイコン化
- **ボードツールバー (T4/T5)**: 「管理」テキストを `settings2` アイコン化、消しゴム/選択/追加は既存アイコン、ボトム: ズーム -/+ を `zoomOut/zoomIn`、全体を `maximize`、戻すを `disc`、既存の `.bd-toolbar-*` クラスと `.tb-*` を併記
- **カレンダーツールバー (T6)**: 月/週/日/タスク/シフトは個別ボタン維持 (§13-C-8)、アイコン化 (`calendarDays/calendarRange/calendar/listChecks/clock`)、サイドバーは別アイコン `panelLeft` で維持 (§13-B-4)、+イベント `calendarPlus` / +タスク `listChecks` / テンプレート `layoutTemplate` / タイマー `timer`
- **DBツールバー (T7)**: 絵文字/テキスト併記をアイコンのみに。カテゴリ表示 span はそのまま `.tb-title` として残す (§13-D-11)、架空のビュー select は追加しない (§13-C-10)
- **HTMLビューア (T8) / CSVビューア (T9)**: 統一仕様に合わせる。`+ 行` `+ 列` `シートに変換` は `.tb-text-btn` で曖昧さ回避 (§13-E-2)

### 仕様固定

- 既存機能を一切削除しない (§14-8)
- ビュー切替（排他選択）は個別ボタン+active で残す (§14-9)
- アイコンだけで曖昧なボタンは `.tb-text-btn` で文字併記 (§14-10)
- カテゴリ表示・タイトル入力等の表示・入力要素はそのまま残す (§14-11)

## v0.5.136 (2026-04-16)

### 書式設定UI統一 フェーズC（ノート右クリック）完了

- ノート / 自由記述 / 詳細パネル editable の右クリックメニューを整理
  - 旧: `太字` `斜体` `下線` `取り消し線` `文字色` の5項目が縦リストで並ぶ
  - 新: `書式…` 1項目に集約。クリックで統一書式ポップアップ (`gb-fmt-popup`) が開く
- ポップアップ内: `[文字色T] [B] [I] [U] [S]` の横並び1行
- 内部は従来通り `document.execCommand('bold' / 'italic' / 'underline' / 'strikeThrough' / 'foreColor')` を呼ぶため、保存形式は不変
- 長押しメニュー（タッチ・ペン対応、2026-04-16 リファクタ済）経由でも同じ動作
- 「コメントを追加」「ルビを設定…」「リンクを挿入…」「切り取り/コピー/貼り付け」「元に戻す/やり直し/すべて選択」「テーブル行列操作」等は変わらず動作

### 書式設定UI統一 フェーズD-1（ボード色スウォッチのクラス統一）

- カードスタイル管理 / ラインスタイル管理 / ノードプロパティ編集の色スウォッチを共通クラス `gb-fmt-swatch` + `gb-fmt-swatch-bg` / `gb-fmt-swatch-fg` に統一
- 文字色は Lucide `type` アイコン、文字フチ色は Lucide `typeOutline` アイコンで表示（計画書 §2-2-C）
- リセットボタンに `gb-fmt-reset` クラス併記。既存の `bd-color-swatch` / `bd-detail-reset-btn` クラスは互換維持
- 影響: 22px 高さ・スウォッチ見た目がシナリオと揃う

### 残タスク（別計画書として管理）

以下は規模・影響範囲が大きいため本 v0.5.136 では実装せず、個別タスクに切り出し:

- **フェーズ D-2**: 太字/斜体/影 のチェックボックスを `.gb-fmt-btn` トグルに刷新
- **フェーズ D-3**: カード/ラインスタイル管理ダイアログ内を `.gb-fmt-popup-row` 構造で横並び化
- **フェーズ D-4**: 「影」「テキストをラインに合わせる」を個別スタイル設定からボードツールバーの表示モードへ移動（データマイグレーション必要、ユーザー確認後に着手）
- **フェーズ E**: `.sn2-style-*` / `.bd-detail-reset-btn` 等の旧クラス削除、`openPopupPalette` を `openColorPalette` エイリアスとして廃止

## v0.5.135 (2026-04-16)

### 詳細パネルのフォントサイズを 12px に統一

- 詳細パネル（`#rp-detail`）全体を `var(--ui-font-12)` (12px) に統一
- 影響範囲:
  - ヘッダ（タイトル・閉じるボタン）: 11px → 12px
  - フィールドラベル: 10px → 12px（読みやすさ向上）
  - 入力欄（input/textarea/select/button）: 11px → 12px
  - DB プロパティの派生値（modified/formula/rollup）: 13px → 12px
  - ボード詳細パネルのタブボタン (`.bd-detail-tab-btn`): 11px → 12px
  - エンティティ親 DB リンクボックス: 11px → 12px
- 直値ではなく `var(--ui-font-12)` トークンで記述することを原則化

### 書式設定ポップアップ共通ヘルパー追加

- 新規 [gb-format-popup.js](app/gb-format-popup.js) / [gb-format-popup.css](app/gb-format-popup.css) を追加
- 共通ヘルパー `openFormatPopup(anchorEl, options)` を公開
  - フィールドのホワイトリスト指定（`bgColor` / `textColor` / `borderColor` / `textStrokeColor` / `bold` / `italic` / `underline` / `strike` / `fontSize` / `textBefore` / `textAfter` / `textAlign` / `textValign` / `textOverflow`）
  - 全インタラクティブ要素の高さを 22px に統一（計画書 §2-2-A）
  - ラベル+入力欄をペア化、ペア内 2px／ペア間 8px
  - 数値入力欄は 70px 幅、スピンボックスは hover/focus でのみ表示（`input.gb-fmt-num` の詳細度で指定）
  - リセット・全行適用（bulk）・外クリック close を内包
  - 旧 `.sn2-style-*` クラスは CSS 併記で互換維持（段階的移行）
- Lucide アイコン `type` を新規登録、`typeOutline` の誤登録値を正しいフチ付き "T" パスへ差し替え（参照コード側は副作用ゼロ）

### シナリオのセル書式設定をヘルパー経由に移行

- `_showCellStylePopup` / `_showColBulkPopup` を `openFormatPopup` 呼び出しに置換
- 既存の undo ラベル（「タイプ色変更」「タイプ書式変更」「タイプサイズ変更」「タイプ設定変更」「タイプ配置変更」「書式リセット」「タイプ一括変更」「全行適用ON/OFF」「一括書式リセット」）は維持
- `_text` / `_role` 列のレガシーフォールバック（`chara.fontWeight` 等）同期、自動配色クリア（`chara.autoColor`）の特殊ロジックは呼び出し側の onChange に集約
- 旧実装約370行 → 新実装約160行に短縮

### 挙動変更の注意

- 背景色スウォッチは「未設定時はチェック柄表示」方針に統一（旧実装ではタイプ由来の fallback 色で塗っていた）。文字色スウォッチ T は引き続きタイプ色 fallback あり
- 一括設定ポップアップの「N 件」件数表示は省略。チェックボックスの ON/OFF で「全行適用」か「選択行適用」か判断する運用に

## v0.5.134 (2026-04-16)

### E2Eテスト基盤の強化（内部）

- 1ケース実行・複数ケースバッチ実行・失敗ケース再実行の PowerShell スクリプトを追加（`run_frontend_e2e_case.ps1` / `run_frontend_e2e_case_batch.ps1` / `run_frontend_e2e_failed_only.ps1`）
- ヘッドレスブラウザ起動のタイムアウトを 60→150 秒へ延長し、`--virtual-time-budget` を 30→90 秒に拡大
- ブラウザ起動環境を隔離するランタイム環境変数（LOCALAPPDATA / APPDATA / TEMP / USERPROFILE）を自動構築
- 失敗時に `gb-smoke-result` の内容、DOM 抜粋、stderr 末尾をまとめた詳細ログを自動出力
- E2E アクション `gb-e2e-actions-extra.js` / `gb-e2e-actions-dashboard-compare.js` にバージョン管理パネル・ダッシュボード比較系の検証アクションを追加
- 既存テストケースのセレクタ・値を現行 UI に合わせて補正（メインメニューのクラス名、状態値 `完了`、リレーションの対象エントリ 等）

### 計画書の追加

- 折り畳みペイン ポップアップ展開機能 計画書（`dock-palette-system-plan.md`）
- 書式設定UI 統一計画書（`format-popup-ui-unification-plan.md`）
- 非表示機能 廃止 計画書（`hidden-feature-removal-plan.md`、採用）
- 非表示設定のワークスペース化計画書（`hidden-items-workspace-plan.md`、不採用の経緯を記録）

## v0.5.133 (2026-04-16)

### タッチ長押しメニューの展開

- タッチ・ペン入力での長押しで右クリックメニュー相当を開ける箇所を大幅に拡大
- 対応箇所: フォルダツリー行、シート（行・エントリ名列ヘッダ・プロパティ列ヘッダ・ギャラリーカード・カンバンカード）、ダッシュボード項目、ボードの接続線、注釈、チームチャット（ルーム行・メッセージ）、タブバー、パネルセットボタン、ワークスペースタブ、シェルメニュー、カラーパレット、シナリオのテキスト列
- マウスの右クリック動作はそのまま維持。タッチ／ペン入力のみ長押しで発火
- ボードの線の曲げポイント削除は誤タップ事故のリスクから今回は対象外として保留

### パネルタブに「＋」ボタン追加

- 各パネルセットのタブバー末尾に「＋」ボタンを配置
- クリックでアプリバーの「パネル」ボタンと同じメニューが開き、そのパネルセットに新しいタブとしてパネルを追加できる
- パネルメニュー経由の追加動作を「常に現在アクティブなパネルセットに新規タブ追加」に変更。既に他のパネルセットに同種のパネル（フォルダツリー／ビューワー／詳細／チャット／ヒストリー／注釈など）があっても、別のパネルセットに並べて追加できる
- フォルダツリーのパネルセットからの追加でも、そのパネルセットにそのまま追加されるよう統一

### バージョン管理パネルをいつでも開けるように

- 対象アプリが未指定でも、空状態のバージョン管理パネルを開けるよう改修
- 従来出ていた「バージョン管理を表示するアプリがありません」の赤字ステータスによりパネルが開かない問題を解消
- 空状態で開いたタブは、対象アプリを開いた状態で再度「バージョン管理」を選ぶと、その対象に切り替わって中身が表示される（新しいタブが増えずに既存タブを再利用）

## v0.5.132 (2026-04-15)

### アプリバー再構成

- アプリバーをアプリレイアウト中心に再構成
- アプリバー計画書と関連索引を追加
- アプリバーのメニューが外側クリックで閉じるよう修正
- アプリバー文言と自動レイアウト切替を調整
- アプリバーとペイン操作の挙動を安定化

## v0.5.131 (2026-04-14)

### ボード改善

- ラインと関連カードの描画を改善
- ボードプリセット / スタイルマネージャの調整
- キャンバスエンジン（part02 中心）の大幅機能追加とインタラクション改善
- 注釈（annotation）表示の改修

### シナリオ（scriptnote）機能拡充

- シナリオ検索モジュール `gb-scriptnote-search.js` を新規追加
- シナリオステータス機能 `gb-scriptnote-status.js` を新規追加
- フィルタ / フォーマット / セレクション / カラム / ドキュメントの挙動改善
- 詳細パネル (`gb-scriptnote-detail.part03.js`) とエディタ本体 (part01〜04) の修正
- シナリオ専用CSS (`gb-scriptnote-editor.css`) を整備

### エクスポート整理

- 共通保存ヘルパー `gb-export-save.js` を新規追加
- HTML / Image エクスポートを新ヘルパーへ統合

### UI統一

- メインツールバーのアイコンボタンを統一
- モーダルシェル / レイアウト / ツールメニュー / 設定等を微調整
- ノート強化 (`gb-note-enhance.js`) の機能拡張

## v0.5.130 (2026-04-14)

### 用語リネーム（UI統一）

- `database` → **シート**、`smart-db` → **スマートシート**、`annotation` → **注釈**、`scriptnote` → **シナリオ** にUI表記を統一（内部コード名は維持）
- 「旧シナリオエディタ」と現行「シナリオ（内部: scriptnote）」の区別ルールを CLAUDE.md / AGENTS.md / GEMINI.md に明記
- 用語統一表（`MeldexHome/マニュアル/99_制作ルール/用語統一表.md`）を更新

### ドキュメント再編

- 設計意図ドキュメントを新命名へ再編
  - 新設: `シート_設計意図.md`、`スマートシート_設計意図.md`、`シナリオエディタ_設計意図.md`、`注釈_設計意図.md`
  - リネーム/整理: `データベース_設計意図.md` → `シート_設計意図.md`、`メモ_設計意図.md` → `注釈_設計意図.md`、`台本エディタ_設計意図.md` → `シナリオエディタ_設計意図.md`
  - 廃止: `スマートDB_設計意図.md`
- 仕様スナップショット `spec-snapshot-v5.3-20260414/` および `spec-snapshot-v5.3-20260414-scriptnote/` を新規追加
- 深部回帰テスト計画 `meldex-deep-regression-test-plan-2026-04-14.md` を追加
- 仕様観察ルール `spec-observation-rules.md`、TyranoScript エクスポート Phase1 計画、ノベルエディタ設計書を追加

### デバッグリスト運用基盤（Phase 0〜3 + §12）

- Phase 0+1: デバッグリスト運用基盤の実装
- Phase 2: 削除連動 + 逆方向同期 + ループガード
- Phase 3: プロパティ型設定ダイアログに calendarSync / autoFillOnCreate UI 追加
- §12.2/§12.3: AI対応待ちAPI + バグ報告連携
- §12.5/§12.6: バックリンク + AIチャット連携

### アイコン整備（Lucide）

- Lucide アイコン整備 Step A〜D
- Phase E/F/G/H 完走（CDN/ライセンス/Twemoji/パネル）
- モバイル/タッチ media query を新 `.gb-pane-ctrls` クラスへ対応

### バックエンドAPIルーター化（大規模リファクタ）

FastAPI モノリスをドメイン別ルーターへ分離:

- workspace / file-index / outliner / trash
- notion / annotations / backlinks / duplicate / extension
- search / replace / link-dict
- calendar db / calendar sync / collab
- version / clip export
- chat（ストリーミング / 非ストリーミング分離、共通ヘルパー抽出）
- 各ルーター追加時に回帰テスト・スモークテストを同梱
- phase0 groundwork / phase2 router inventory / remaining-scope チェック記録を docs に残す

### フロントエンド大規模分割（1000行未満化）

- 肥大化していた JS/Python を `*.partNN.js` / `*.partNN.py` へ分割
- 分割対象: `Meldex.py`、scriptnote インタラクション / セレクション / フィルタ / ドキュメント / ヒストリー、Meldex core / DB モジュール、フロント・バックエンドの超過スクリプト全般
- 分割ローダ基盤 (`gb-split-loader.js`) を導入
- scriptnote の監査指摘を修正（scriptnote audit hardening）
- ペイン状態境界の整理 + スモークカバレッジ追加
- フロントエンドスモーク実行時のブラウザ起動をゲート化

### 機能改善・不具合修正

- `Meldex.py` の改行混在を正規化（以後の編集を通常作業で行えるように）
- 条件付きカラー・条件フィルタモーダルの挙動を安定化
- チャット + バージョンパネルのワークフロー改善
- パネルレイアウトの安定化
- app shell / editor ワークフローの改善
- ペイン単位のナビゲーション、シナリオ スタイルタブ追加
- ビジョンチャット機能追加、ボードエディタ修正

### テスト追加

- `test_meldex_api_database_entity_create.py`
- `test_meldex_auth_state_service.py`
- `test_meldex_chat_common.py`
- `test_meldex_notion_service.py`
- 既存 `test_meldex_chat_stream.py` / `test_meldex_frontmatter.py` を拡充

## v0.5.123 (2026-04-13)

### フォルダツリー 不具合修正・アーキテクチャ改善

- フォルダツリーのセクション（ソースフォルダ・ホーム等）の開閉が効かなくなる不具合を修正
  - ラッソ選択の pointerdown ハンドラがセクションヘッダーのクリックを横取りしていた
  - お気に入り・最近使った項目のクリックも同様に影響を受けていたのを修正
- **フォルダツリーパネルをクリック時にアクティブ化しないように変更（アーキテクチャ改善）**
  - ナビペイン（outliner）のクリックでアクティブペインを変更しない
  - ファイルは元のアクティブペイン（コンテンツペイン）でそのまま開かれる
  - setActivePane → _mountAllPanes の副作用が構造的に排除され、スクロールリセット・shake・新タブ追加が根本解消
- フォルダツリーのスクロール位置が先頭に戻る不具合を修正
  - render() 全体をスクロール位置保存/復元で保護
  - setActivePane 内でも全ペインのスクロール位置を保護
  - tree-scroll-container に overflow-anchor: none を設定
  - updateRecentItems のスクロール復元がガードと競合する問題を修正
- フォルダツリーからファイルを選択した時に新タブが追加される不具合を修正
  - openScenarioInScriptNote が addTab で常に新タブを作成していた → fromExplorer 時はアクティブタブを置換
  - navPush のフォルダ専用パスで既存 folder タブがない場合にアクティブタブを置換
  - 同タイプ内ナビゲーション（page→page 等）で render() をスキップしタブラベルのみ更新

### パネルレイアウト 不具合修正・機能追加

- パネルタブをドラッグして並び替える際に、パネル分割用のドロップインジケータが誤表示される不具合を修正
  - ルートエッジ検出（capture phase）、ペイン dragover、ルート drop の3箇所でタブバーを除外
  - タブバーの dragover で既存のドッキングインジケータを即クリアするよう修正
- パネル最大化時にアプリバーとステータスバーも非表示にするよう改善
  - サイドバーが表示中の場合は一時的に隠し、復元時に元に戻す

### 修正対象ファイル

- `gb-outliner.js` — ラッソ選択の除外リスト追加、スクロール保護ガード簡素化、updateRecentItems のロック
- `gb-layout.js` — ナビペインのアクティブ化スキップ、render()/setActivePane のスクロール保護、最大化時のクローム非表示
- `gb-pane-bridge.js` — isNavPaneType コールバック登録、同タイプナビゲーション軽量化、フォルダタブ置換
- `gb-docking.js` — ルートエッジ dragover/drop、ペイン dragover でタブバー上のドラッグを除外
- `gb-tool-menu.js` — openScenarioInScriptNote の fromExplorer 時アクティブタブ置換
- `Meldex.html` — tree-scroll-container に overflow-anchor: none

## v0.5.122 (2026-04-11)

### フォルダツリー / ノートエディタ 不具合修正・機能追加

#### フォルダツリー
- **🚧 クリック時のスクロールジャンプ修正 (再修正・未確認)** ([app/gb-outliner.js](app/gb-outliner.js)): v0.5.121 で `updateRecentItems` の innerHTML 再構築側を保護したが、ジャンプは依然として再現していた。仮説として open* 関数内の `highlightOutlinerNode(path)` → `_findAndHighlight` → `row.scrollIntoView({ block: 'nearest' })` が候補。クリックハンドラ側で既に `row.classList.add('active')` を実行しているため、open* 内の再ハイライトと scrollIntoView は冗長と判断
  - クリックハンドラの `_expOpts` に `skipHighlight: true` を追加 ([app/gb-outliner.js:508](app/gb-outliner.js#L508))。各 open* 関数は既に `if (!openOpts.skipHighlight)` ガード済みなので scrollIntoView 経路が切られるはず
  - `gb-csv-viewer.js` の `openCsvFile` だけ `skipHighlight` ガードがなかったため追加 ([app/gb-csv-viewer.js:33](app/gb-csv-viewer.js#L33))
  - **ユーザー検証ではこの修正後も依然として再現するとの報告あり。本コミットでは保留扱いとし、別セッションで根本原因を調査予定**

#### ノートエディタ
- **ツールバーに「表」作成ボタンを追加** ([app/Meldex.html:323](app/Meldex.html#L323), [app/gb-editor.js](app/gb-editor.js)): コールアウトボタンの隣に「表」ボタンを追加。新規関数 `insertNoteTable(rows=3, cols=3)` で 3x3 の表 (1 行目は `<th>`) をキャレット位置に挿入する。execCommand `insertHTML` 経由で undo 履歴にも乗る
- **表セルの右クリックメニューで列/行の追加・削除を可能に** ([app/gb-note-enhance.js](app/gb-note-enhance.js) `_initTableOperations`): 既存の `+ 列` / `+ 行` ボタンに加え、表セルを右クリックすると以下のメニューを表示
  - この行を削除 / この列を削除
  - 行を上に追加 / 行を下に追加
  - 列を左に追加 / 列を右に追加
  - 表を削除 (確認ダイアログあり)

  最後の行・列を削除する場合は表ごと除去する。位置決めには共通 `positionPopup()` を使用
- **D&D ブロック移動が `HierarchyRequestError` で失敗する問題を修正** ([app/gb-note-enhance.js](app/gb-note-enhance.js) `_initBlockDragHandle` drop ハンドラ): 見出しセクション (`heading-section`) などの内側にネストされたブロックをドロップ先にすると、`_findBlock` がネストされた子ブロックを返し、`target.before(dragBlock)` で `dragBlock.contains(target)` が真になって `HierarchyRequestError ("The new child element contains the parent")` を投げていた。
  - ドロップ時に `dragBlock.contains(target)` を判定し、真ならノーオペで終了
  - `target.before/after` を `try/catch` でガードし、想定外の階層エラーが出ても `dragging` クラスは必ず解除する

### 修正対象ファイル
- [app/gb-outliner.js](app/gb-outliner.js)
- [app/gb-csv-viewer.js](app/gb-csv-viewer.js)
- [app/gb-note-enhance.js](app/gb-note-enhance.js)
- [app/gb-editor.js](app/gb-editor.js)
- [app/Meldex.html](app/Meldex.html)

## v0.5.121 (2026-04-11)

### フォルダツリー / ノートエディタ 不具合修正

#### フォルダツリー
- **クリック時にツリーが先頭へジャンプする問題を修正** ([app/gb-outliner.js](app/gb-outliner.js) `updateRecentItems`): ファイル / フォルダをクリックすると `addRecent()` → `updateRecentItems()` が走り、`#body-recent` の `innerHTML` を空文字で再構築していた。`#body-recent` は `#tree-scroll-container` 内にあるため、innerHTML 空化の瞬間にブラウザのスクロールアンカリングがアンカーを失い、ツリー全体が先頭へ吸い寄せられていた。`updateRecentItems` 内で `#tree-scroll-container.scrollTop` を保存し、再構築直後と次フレームで復元するように修正

#### ノートエディタ
- **表の「+列」ボタンが押せない問題を修正** ([app/gb-note-enhance.js](app/gb-note-enhance.js) `_initTableOperations`): `.table-add-col` は CSS で `right: -40px` の絶対配置になっており、container (table.parentElement) とボタンの間に「無人地帯」がある。container から右へカーソルを動かしてボタンへ向かう途中で container の `mouseleave` が走り、関連先 (relatedTarget) がボタンでもコンテナでもないため即座にボタンが除去されていた。`mouseleave` 即除去をやめ、350ms の遅延 + container / ボタン双方の `mouseenter` でキャンセルする方式に変更
- **ブロック D&D が実際に移動しない / インジケーターがズレる問題を修正** ([app/gb-note-enhance.js](app/gb-note-enhance.js) `_initBlockDragHandle`): `#page-content` には [setupEditableDropHandler](app/gb-editor.js) の `dragover` / `drop` が bubble フェーズで先に登録されており、
  - dragover 内で `caretRangeFromPoint` を使い `.drop-caret` (テキストキャレット位置の青線) を表示するため、ブロック境界を表す `drag-guide-top/bottom` とは別のインジケーターが微妙にズレた位置に出ていた
  - drop 内で `sel.addRange(range)` がカーソル位置を書き換え、execCommand 周辺の挙動と相まってブロック移動の DOM 操作が破綻していた

  ブロックドラッグ用の `dragover` / `drop` リスナーを **capture フェーズ** で登録し、`handle._dragBlock` がセットされている場合 (= ブロックドラッグ中) は `e.stopPropagation()` + `e.stopImmediatePropagation()` で bubble 側を完全に止める方式に変更。残存していた `.drop-caret` はブロックドラッグ中に検出して除去する。

### 修正対象ファイル
- [app/gb-outliner.js](app/gb-outliner.js)
- [app/gb-note-enhance.js](app/gb-note-enhance.js)

## v0.5.120 (2026-04-11)

### 台本: 末尾 Shift+Enter / ノートエディタ: ハンドル追従修正

#### 台本エディタ
- **セル末尾で Shift+Enter による改行ができない問題を修正** ([app/gb-scriptnote-editor.js](app/gb-scriptnote-editor.js)): `dataset.after` (`」` などの affix 表示用) が設定された行で末尾に Shift+Enter を押すと、改行が無視されていた。`dataset.before` / `dataset.after` は CSS の `::before` / `::after` 疑似要素 ([app/gb-scriptnote-editor.css:696-702](app/gb-scriptnote-editor.css#L696-L702)) で描画されるため `<br>` を挿入しても視覚順序は変わらない (改行は常に affix の内側で発生する)。先頭・末尾位置での早期 return を撤廃し、あらゆる位置で改行を許可するように変更

#### ノートエディタ
- **ハンドルが上方へ逃げて掴めない問題を修正** ([app/gb-note-enhance.js](app/gb-note-enhance.js) `_initBlockDragHandle`): v0.5.119 でハンドルを `position:fixed` 化した際、ハンドルへ向かってマウスを動かすと連鎖的に上方へ逃げる挙動になっていた。原因はハンドルを `blockRect.top` に固定していたため、ユーザーがハンドルへ近づこうと斜め上方向に動かすと上のブロックへ侵入し、`mouseover` で再配置される連鎖が発生していた
  - イベントを `mouseover` から `mousemove` に変更し、ハンドル top をマウス Y に追従させる (ブロック範囲内にクランプ)。これによりユーザーは左方向に動かすだけでハンドルを掴める
  - 同一ブロック内では Y が 16px 以上動いたときだけ再配置するヒステリシスを追加し、微小ジッタを抑制
  - `_lastBlock` / `_lastMouseY` を `mouseleave` / `handle.mouseleave` 時にリセット
  - `handle._dragBlock` (ドラッグ中フラグ) が立っている間は再配置をスキップ

### 修正対象ファイル
- [app/gb-scriptnote-editor.js](app/gb-scriptnote-editor.js)
- [app/gb-note-enhance.js](app/gb-note-enhance.js)

## v0.5.119 (2026-04-11)

### 台本エディタ・ノートエディタ キーボード操作とハンドル位置の修正

ユーザー要望に基づくキーボードショートカット追加とノートエディタのレイアウト不具合修正。

#### 台本エディタ
- **タイプ列ドロップダウンの Shift+↑/↓ で 5 つ飛ばし** ([app/gb-scriptnote-menu.js](app/gb-scriptnote-menu.js)): `_showRoleMenu` の `menuKeyHandler` 内で `ArrowUp` / `ArrowDown` の処理に `step = e.shiftKey ? 5 : 1` を導入。フラットリスト・カテゴリリスト・サブメニュー表示それぞれに反映。範囲外は `Math.max(0, ...)` / `Math.min(length-1, ...)` でクランプ
- **編集中の Alt+↑/↓ で 5 行スキップ** ([app/gb-scriptnote-editor.js](app/gb-scriptnote-editor.js)): host の `keydown` ハンドラで `ArrowUp` / `ArrowDown` を処理する分岐 (1727 行付近) の手前に Alt 修飾分岐を追加。フィルタで非表示の行はスキップしながら 5 ステップ進める。縦書きモード (`viewMode === 'vertical'`) でも `prevKey` / `nextKey` の選び分けはそのまま流用
- **Undo/Redo 後のカーソル位置を元行近傍に復元** ([app/gb-scriptnote-editor.js](app/gb-scriptnote-editor.js) `_applySnapshot`): 旧実装は元行が見つからないと無条件で先頭行に飛ばしていたため、テキスト編集直後の Ctrl+Z でカーソルが最初の行へ飛ぶケースがあった。`focusRowId` に加えて旧インデックス `focusRowIdx` を保持し、行が消えていたら「直前の行 → 同インデックス → 最終行へクランプ」の順で近傍を探索する。selection 取得も `activeElement` だけでなく `anchorNode` / `focusNode` 経由でも .sn2-row をたどるよう強化。focus 位置は `'start'` から `'end'` に変更し、復元後に `scrollIntoView` で可視化
- **Ctrl+Enter は既に実装済み** を確認 ([app/gb-scriptnote-editor.js:1659-1663](app/gb-scriptnote-editor.js#L1659-L1663)): `_splitRow(text, { keepRole: true })` で現在行のタイプを引き継いだ新行が追加される。挙動を改めて確認しコード変更なし

#### ノートエディタ
- **行ドラッグハンドルが目次エリアに被る不具合修正** ([app/gb-note-enhance.js](app/gb-note-enhance.js) `_initBlockDragHandle`): 旧実装は handle を `page-content` の親 (フレックスコンテナ) に append し、`pcRect.left - parentRect.left - 22` で left を計算していた。目次 (`#note-toc` 200px + リサイザ 6px) が表示されているとこの計算結果 (約 184px) が目次領域内に着地する。修正では handle を `page-content` 直下に append し、`position:relative` を `page-content` 自身に付与。`getComputedStyle(pc).paddingLeft - 22` を新ヘルパー `_handleLeftPx` で取って handle を padding-left の内側 22px に常に配置するように変更。スクロール考慮で `pc.scrollTop` / `pc.scrollLeft` を加算。`_findBlock` は handle 自身を block と誤認しないよう `node === handle` を除外

### 修正対象ファイル
- [app/gb-scriptnote-menu.js](app/gb-scriptnote-menu.js)
- [app/gb-scriptnote-editor.js](app/gb-scriptnote-editor.js)
- [app/gb-note-enhance.js](app/gb-note-enhance.js)

## v0.5.118 (2026-04-11)

### DB高速化v2 Step3 完了: 派生値 (formula / rollup / source) の依存更新 + 取りこぼし型確認

DB 高速化計画 v2 (`app/docs/plan-db-perf-v2.md`) の Step 3 第3陣を実装し、計画書の残課題を解消した。

- **対象**: `app/gb-db-props.js`
- **背景**: v0.5.111 / v0.5.116 で第1陣 (text / number / select / checkbox / date / url / status) と第2陣 (multi-select / user / multi-user) の partial update 化は完了していたが、派生値型 (formula / rollup / source 系) は計画書リスク #3 に従って意図的に未対応のまま残っていた。「他セル編集後に formula / rollup / source 列が古い表示のまま残る」という既知制約を解消する
- **新規ヘルパー**:
  - `_refreshDerivedCellsInRow(editedTd, entityPath)`: 編集された td から tr を取り、行内の派生プロパティ (`ptc.source` / `formula` / `rollup`) のセルだけ再評価する。`tr.querySelector('td[data-prop-name="..."]')` で対象 td を引き、`.cell-values` の中身だけを差し替える
  - `_renderDerivedCellContent(container, ptc, entityName, propName, entityData)`: gb-database.js の `renderEntityCell` 内 source / formula / rollup 分岐をミラーした薄いコンテンツ生成関数。同じ DOM 構造・同じスタイルを生成する
  - `_cssEscapeAttr(s)`: `CSS.escape` の薄ラッパー (未対応環境向けにバックスラッシュエスケープへフォールバック)
- **source 系プロパティの自動更新**:
  - `_refreshDerivedCellsInRow` 呼び出し時に `entityData._modified = new Date().toISOString()` と `entityData._modified_by = getUsername()` をクライアント側で書き換える
  - サーバー側は `_touch_modified` で frontmatter を既に更新済みなので、ローカル `pivotData` を同じタイミングで合わせるだけ
  - これにより `ptc.source === 'modified' / 'modified_by'` 列が partial update 後に新しい値で表示される
- **rollup の partial 更新**:
  - 自己参照ロールアップ等の取りこぼしを防ぐため、行内に rollup プロパティが 1 個でもあれば `clearRollupCache(state.currentDbPath)` で当該 DB の `_rollupCache` をパージ
  - その後、`calcRollupValue` を改めて実行して非同期で結果を表示
  - cross-DB の rollup キャッシュには触れない (過剰パージを避ける)
- **formula の partial 更新**:
  - `formulaEvalForEntity(ptc.formula, entityData)` を即座に再実行
  - `entityData` は編集された値が既に反映されたローカル `pivotData` の参照そのものなので、`prop("X")` の解決は最新値で行われる
- **組み込み箇所**:
  - `_tryRefreshPivotCellLocal`: `_refreshPivotRelationCell` が成功した直後に `_refreshDerivedCellsInRow` を呼ぶように変更
  - `_finalizeRelationCellUpdate`: relation 編集の partial パスでも `_refreshDerivedCellsInRow` を呼ぶように変更 (`setActiveCell` の直前)
- **計画書 第1陣 / 第2陣の取りこぼし確認**:
  - 計画書原文に登場する `email / phone / file` 型は Meldex のプロパティ型レジストリ ([gb-db-props.js:1735](app/gb-db-props.js) / [768](app/gb-db-props.js)) に**そもそも存在しない** (Notion の型名を参考にした原文の名残)
  - `person` 型は `user` / `multi-user` として実装され v0.5.111 で対応済み
  - 結論: Step 3 第1陣・第2陣の取りこぼしは無し
- **スコープ外 (計画書記載の既知制約として残置)**:
  - **クロス行 / クロス DB の rollup 連鎖更新**: 例えば DB-X のエントリ A を編集したとき、別 DB-Y の rollup (DB-X を参照) のセルは更新されない。逆引きインデックスがないため最小実装では困難。`_rollupCache` の 30 秒 TTL に任せる
- **徹底チェック (検証観点)**:
  - tr.querySelector の data-prop-name 引き → `renderEntityCell` ([gb-database.js:1069](app/gb-database.js)) で `td.dataset.propName = propName;` が設定される ✓
  - `_getPivotEntityName` は新形式 (.md) / 旧形式 (フォルダ) の両方に対応 ✓
  - 同じ td.cell-values が連続編集で複数回再描画される競合 → `container.innerHTML = ''` で古い span は detach、async resolve は detach 済み span に setText するだけで visible には影響しない ✓
  - グループヘッダー行 / 新規エントリ行 → `data-prop-name` 付き td がないため querySelector が null → 安全にスキップ ✓
  - 列フィルタで非表示の派生列 → 同様に querySelector が null → スキップ ✓
  - source 系 frontmatter 上書きの整合 → サーバー成功後に呼ばれるためタイミング OK。失敗時は呼び出し元で `selectDatabase` フォールバックして fresh データを取得 ✓
  - Undo / Redo → 既存の `_dbUndoValue` callback は silent な `selectDatabase` を呼ぶため、Undo 後はサーバーから fresh データを取得 → 派生セルも正しい値で再描画 ✓
  - 連続編集での rollup 競合 → `_rollupCache` を毎回パージ → 最新値が反映 ✓
  - `_executeButtonActions` ([gb-db-props.js:2142](app/gb-db-props.js)) は selectDatabase で全部再構築するため、本変更とは独立 (今回スコープ外)
- **関連計画書**: `app/docs/plan-db-perf-v2.md` Step 3。本コミットで Step 3 完了 → DB 高速化計画 v2 全 Step 完了

## v0.5.117 (2026-04-11)

### 全体チェックで発見した既存バグ 3 件の修正

全体監査 (DB パイプライン中心の line-by-line 精読 + 残りファイルの Grep スキャン) で発見した、ユーザー操作で実際に再現しうる既存バグを修正。

- **`_addEntityBelow` (`gb-db-props.js`)**: エントリ名列ヘッダーメニュー「エントリを追加」アクションが、存在しないエンドポイント `apiPut('/entity', ...)` を空文字列のエントリ名で呼び出していたため常に失敗していた。`_handleNewEntryClick` と同じパターンに置き換え、`apiPost('/entity/create', ...)` で `無題N` を生成 → silent な `selectDatabase` で再描画 → `_waitForEntityRow` 経由でインラインリネームを起動するように修正。
- **`_executeDbAction` (`gb-db-actions.js`)**: DB アクション (`auto_copy` / `input_fields`) のプロパティ保存が、`/api/value` に対して未対応のキー `path` を渡していたため値が保存されなかった。新形式の `.md` パスを使うので `entry_path` キーに修正。`note: ''` も明示。
- **`_autoFillOnStatusChange` (`gb-database.js`)**: ステータス変更に連動した日付プロパティ自動入力後、ローカル `pivotData` の `existing.value` を更新していなかった。Step 3 の partial update 経路 (`showStatusDropdown` の `_tryRefreshPivotCellLocal`) では編集対象セルだけ再描画するため、自動入力された日付セルが古い値のまま表示される問題があった。`_apiPutValue` 成功直後に `existing.value = now` を設定して同期させる。

### 監査済みファイル
- 精読: `gb-database.js` / `gb-db-props.js` / `gb-db-views.js` 主要部 / `gb-db-date.js` / `gb-db-formula.js` / `gb-db-rollup.js` / `gb-db-aggregate.js` / `gb-db-relations.js` / `gb-db-validate.js` / `gb-db-actions.js` / `gb-pane-context.js` / `gb-tools.css` (Step1 部) / `meldex-core.js` 主要部 / `gb-history.js` / `gb-app.js` 前半
- Grep スキャン: 全 67 個の `gb-*.js` + Python (`Meldex.py` の `/api/value` 系エンドポイント等)
- 重点パターン: `apiPost/apiPut` の API キー整合性 / `selectDatabase` 経路と `state.pivotData` 直接参照 / `_renderToken` 中断トークン整合性 / 双方向リレーション同期 / Undo/Redo callback / フォーミュラ・ロールアップ・集計の境界値

### 監査メモ (本セッションでは未修正)
- **ピボットのソート比較式**: `gb-database.js` の `renderPivot` 内ソート比較器がプロパティ値の配列をそのまま `String(...)` に渡しており、数値・日付列での昇降順が正しく動かない。プロパティ型を見て採用値の `value` を引き出す形に直すべきだが、影響範囲が広く別作業として計画したほうが安全。
- **自己参照ロールアップ**: `gb-db-rollup.js` / `gb-db-validate.js` の `!ptc.relationDb` ガードが空文字列 (自己参照) を「未設定」として弾いてしまい、自己参照リレーションを使ったロールアップ・参照存在チェックが機能しない。プロジェクトで使用例が少ないため deferred。
- **ポップアップ位置補正**: 一部のポップアップ (`showColHeaderMenu` 等) が `positionPopup` / `clampPopupToViewport` を経由せず手動で zoom 補正している。グローバルメモリの「ポップアップ位置ズレ問題」(pywebview 環境) と同根のため、専用の調査セッションで一括対応する想定。
- **`gb-app.js` の `apiFetch` 重複定義**: `meldex-core.js` で定義済みの `apiFetch` を `gb-app.js` 内で再定義した直後に user タグ付けラッパーで上書きしている。動作上は問題ないが、行 724-733 のローカル定義は dead code (削除可)。

## v0.5.116 (2026-04-11)

### DB高速化v2 Step3 仕上げ: checkbox / コンテキストメニュー値変更の部分更新化

- **対象**: `app/gb-db-props.js`, `app/gb-database.js`
- **背景**: v0.5.111 で date / select / multi-select / user 等の主要型は `_refreshAfterCellEdit` 経由の部分更新化が完了していたが、checkbox 型のセルクリックと値の右クリック (⋯) → コンテキストメニュー → リネーム入力の 2 経路が `selectDatabase` (フルリロード) のままだった。これを部分更新へ統一する
- **checkbox 型 (`createTypedValueElement`)**:
  - クリックでトグル後に `_refreshAfterCellEdit(cb, entityPath, propName)` を追加
  - 条件付き書式の再適用 / フィルタ・グループ・ソート再評価が partial 経路で走るように
  - 既存の `cb.textContent` 即時更新 + `_dbUndoValue` 登録は維持
- **コンテキストメニュー値変更 (`_showValueContextMenu`)**:
  - リネーム入力 commit 後の `selectDatabase(silent)` を `_refreshAfterCellEdit(_menuAnchor, entityPath, propName)` に置換
  - クロージャ先頭で `_menuAnchor = e?.target || null` を捕捉し、callback 実行時にも有効な anchor として渡す
  - `_refreshAfterCellEdit` 未定義環境向けのフォールバックも残置
- **第3陣 (派生値) は引き続き未対応**: formula / rollup の依存更新、created-time / updated-time の自動更新は計画書 (`app/docs/plan-db-perf-v2.md`) リスク #3 に従い deferred。formula / rollup 列を含むテーブルでは、他セル編集後にこれらの列が古い表示のまま残る点は既知の制約
- **徹底チェック (検証観点)**:
  - 条件付き書式の再適用 → `_refreshPivotRelationCell` 内 `getCellColor` で td 背景再計算 OK
  - groupBy / advFilter / ソート対象列 → `_tryRefreshPivotCellLocal` の既存ガードでフォールバック OK
  - Undo/Redo 整合 → `_dbUndoValue` callback は silent な `selectDatabase` で full reload を維持
  - エラー時ロールバック → try/catch 内で val.value も partial も更新せず state 維持
  - Step 2 チャンク分割中の競合 → partial refresh は既存 td の `.cell-values` のみ書き換え、チャンク append と無干渉。フォールバック時は renderToken 更新で進行中チャンク中断
  - 連打 → 2 回目以降の click は detached cb に紐付くため `closest('td')` が null となり silent reload にフォールバック
- **計画書**: `app/docs/plan-db-perf-v2.md` Step 3

## v0.5.115 (2026-04-11)

### 台本エディタ: タイプ管理パネルとオプション設定ポップアップの仕上げ

- **対象**: `app/gb-scriptnote-detail.js`
- **タイプ管理表の列幅と表示調整**:
  - 「操作」列を「オプション」列に改名（ヘッダーラベル + ボタンラベル）
  - オプションセル/ボタンに `white-space: nowrap` を付与し、「オプション」が二文字ごとに改行される問題を修正
  - 列幅: テキスト列を 54px → 90px、オプション列を 90px → 80px に調整。タイプ列(100px) に対して少し狭いテキスト列バランス
- **オプション設定ポップアップ**:
  - 幅を `min-width: 240px / max-width: 320px` から `min-width: 340px / max-width: 480px` に拡張
  - 区切りチェックボックス行とプロットチェックボックス行を別の `<div>` に分離して、必ず別行で表示されるように
- **テーマタブ「列間枠線」の右に列名+チェックボックスをインライン表示**: ラベル「列間枠線」が幅100%で別行に置かれていたのを削除し、ラベルとチェックボックスUIを同じ行 (display:inline-flex) に並ぶように調整

## v0.5.114 (2026-04-11)

### 台本エディタ: タイプ名による暗黙判定を完全撤廃（厳格な設定駆動設計）

- **対象**: `app/gb-scriptnote-editor.js`, `app/gb-scriptnote-detail.js`, `app/gb-scriptnote-menu.js`, `app/gb-scriptnote-editor.css`
- **背景**: v0.5.113 でタイプ管理表のドロップダウンを削除したが、`_detectKind` が依然としてタイプ名から `heading` / `action` / `break` / `summary` を自動推論しており、`MODE_COUNT_DEFS.pageBreakRoles` 名前判定や CSS `[data-kind="heading"]` 装飾でユーザー設定なしに見た目や採番が変わってしまっていた。これを完全撤廃する
- **`_detectKind` の簡素化**: 戻り値を `'blank'` (空ロール) と `'dialogue'` のみに限定。タイプ名による heading/action/break/summary の推論を撤廃。旧データ移行用には `_legacyDetectKindByName` を別関数として残す（loadDoc 内の一回だけ参照）
- **`MODE_COUNT_DEFS` の簡素化**: `pageBreakRoles` / `pageResetRoles` / `summaryRoles` を撤廃し、`primaryLabel` / `secondaryLabel` のみに削減
- **`_calcPagePanel` の刷新**: `_getRoleFlagSets()` で `chara.isBreak` / `chara.isSummary` から区切り/プロット名を集めて採番判定。kind 概念は内部で使わない
- **「プロット」をオプション設定フラグに**: オプション設定パネルに「プロット」チェックボックスを追加（`chara.isSummary`）。コマ番号がリセットされ、ガターからコマ番号が消える
- **`_buildRowEl` の `dataset.kind` ロジック修正**: 名前推論ではなく `chara.isBreak` / `chara.isSummary` で `'break'` / `'summary'` を決定。それ以外は `'dialogue'`、空ロールは `'blank'`
- **CSS `data-kind="heading"` 装飾を全廃**: 見出し用枠線を付けたいタイプは、オプション設定の「見出し枠線」(`chara.outline`) で明示的に有効化する
- **自動配色 UI を列ごとの単一行に簡素化**: テーマタブの自動配色セクションは、5種類×4列のマトリクスから 4列×1行に変更。`editor.autoColorRule = { _gutter: 'bg', ... }` で全タイプ共通のルールを保存。タイプ種別ごとの個別配色はできなくなる
- **コンテキストメニュー（ロール選択）の刷新**: タイプ選択メニューを「タイプ / プロット / 区切り / なし」の3+1グルーピングに変更。kind 推論ベースの「キャラ / ト書き / 見出し / 区切り」5分類を廃止
- **SEP 送信の単純化**: kind ベースの分岐を撤廃し、`chara.isBreak` の場合のみ pageSetting としてマーク、それ以外は character として送信。クリップボード出力では区切り行で `←` を出すロジックは維持（`chara.isBreak` ベース）
- **旧データ移行**:
  - `editor.autoColorConfig`（旧kind別マトリクス）→ `editor.autoColorRule`（列別単一行）にヒューリスティック変換（dialogue キーを優先採用）
  - レイアウトモードに応じた旧 `pageBreakRoles` 名前リスト（マンガなら「めくり」「改ページ」「柱」など）に該当するタイプには `chara.isBreak = true` を自動付与
  - 旧 `_legacyDetectKindByName` で `summary` 判定されるタイプ（プロット）には `chara.isSummary = true` を自動付与

## v0.5.113 (2026-04-11)

### 台本エディタ: タイプ種別の概念を撤廃 + オプション設定への機能集約

- **対象**: `app/gb-scriptnote-detail.js`, `app/gb-scriptnote-editor.js`, `app/gb-scriptnote-editor.css`
- **変更内容**:
  - **タイプ管理表からタイプ種別ドロップダウンを削除**: タイプ管理表のすべての行は基本機能なしで統一。種別による特殊機能はオプション設定で個別に付与する設計に変更
  - **「区切り」フラグをオプション設定に移動**: タイプ種別の `break` を撤廃し、オプション設定に「区切り」チェックボックスを追加。`chara.isBreak` フラグで管理。レイアウトモード別の `pageBreakRoles` 名前一致 OR `chara.isBreak` で区切り判定する
  - **「インデント」をテーマタブからオプション設定に移動**: `editor.actionIndent` を撤廃し、オプション設定に「インデント」入力欄を追加。`chara.indent` プロパティで個別タイプに値を持つ。ト書き判定とは独立して、設定された行にだけ適用される
  - **テーマタブ「列間枠線」のレイアウト修正**: ラベルと UI を別行から同じ行のインライン配置に変更
  - **オプション設定パネルの細かい調整**:
    - 「テキスト位置」→「テキスト表示位置」に改名
    - 「枠線」セクションのタイトルを削除（中身は維持）
    - 「枠線表示」→「見出し枠線」に改名
    - 「このタイプを複製」→「複製」に改名
    - 「このタイプを削除」→「削除」に改名
- **旧データ移行（loadDoc 直後）**:
  - `chara.kind === 'break'` を `chara.isBreak = true` に自動変換（kind 自体は破壊しない）
  - `editor.actionIndent` を、`chara.kind === 'action'` または名前ベースで ト書き判定されたタイプの `chara.indent` に自動転写
- **CSS 変更**: `[data-kind="action"] .sn2-text { padding-left }` を撤廃。インデントは `chara.indent` を読んで `_buildRowEl` でインライン適用（横書き= padding-left, 縦書き= padding-top）
- **`_calcPagePanel` の単純化**: kind 概念を内部で使わず、レイアウトモード名前判定 + `chara.isBreak` + `summaryRoles` 名前判定だけで採番する
- **SEP 送信**: クリスタへの送信ロジックでも `chara.isBreak` を `kind === 'break'` 同等扱いに加味

## v0.5.112 (2026-04-11)

### 台本エディタ: タイプ管理の機能強化 + 右ドラッグパン

- **対象**: `app/gb-scriptnote-detail.js`, `app/gb-scriptnote-editor.js`, `app/gb-scriptnote-editor.css`
- **追加機能**:
  - **列ヘッダーメニュー「全行に適用」チェックボックス**: タイプ管理表のヘッダー列メニュー (`_showColBulkPopup`) に「全行に適用（新規行にも反映）」チェックボックスを追加。ON にすると変更が選択状態に関係なく全タイプに適用され、`doc.editor.columnAllRules[colId]` にルールとして永続化される。新規タイプ追加・複製・DBインポート時にもルールが自動適用される
  - **オプション設定からタイプ複製**: タイプ詳細パネルのオプション設定ポップアップ (`_showRoleOptionsPopup`) に「このタイプを複製」ボタンを追加。`_cloneChara` ヘルパーで列スタイルもディープコピー
  - **右ドラッグパン**: ボードエディタと同じ感覚で、台本エディタのスクロール領域上の空白を右ドラッグするとパンできる。`.sn2-editor` に `padding-right: 60vw; padding-bottom: 60vh` を追加して表の端より外側にもパン可能。縦書きモードは `padding-left: 60vw` で左方向も拡張。pywebview の右クリックメニューを抑制
- **ヒストリー徹底チェック**: 詳細パネル全体の `_pushUndo` カバレッジを精査。`columnAllRules` は `doc.editor` 配下のため `_takeSnapshot()` に自動的に含まれ、Undo/Redo で復元される
- **新規ヘルパー (gb-scriptnote-detail.js)**:
  - `_cloneChara(src)`: textStyle/gutterStyle/gutter2Style/roleStyle/customStyles/autoColorTarget をディープコピー
  - `_applyColumnAllRules(chara)`: `doc.editor.columnAllRules` の各列ルールを chara の対応スタイルに適用

## v0.5.111 (2026-04-11)

### DB高速化v2 Step3: インクリメンタル更新の横展開 (date / select / multi-select / user)

- **対象**: `app/gb-db-props.js`, `app/gb-database.js`
- **目的**: セル編集後の `selectDatabase()` → 全件再取得 → 全 tbody 再構築を、対象セルだけの差分更新に置き換える。1セル編集ごとのフリーズを解消する
- **既存実装**: text / number / url / status (ステータスドット) / 新規候補値追加 (`startCellInlineAdd`) はすでに `_tryRefreshPivotCellLocal` 経由の部分更新化済み
- **第1陣 + 第2陣の追加**:
  - **date 型**: date inline editor の finish 経路を `_refreshAfterCellEdit` に置換
  - **select 型**: `showSelectDropdown` の値変更/解除を `_refreshAfterCellEdit` に置換。解除時は `_removeLocalPivotValue` で削除済み val をローカル pivotData から除去
  - **multi-select 型**: multi-select inline edit の finish 経路を `_refreshAfterCellEdit` に置換
  - **user / multi-user 型**: `_saveUserValue` に `anchorEl` パラメータを追加し、`_refreshAfterCellEdit` に置換。新規追加 (val が null) ケースのみフルリロード
  - **text 型 (空文字 delete)**: `startInlineEdit` の空文字 commit 経路を `_removeLocalPivotValue` + `_tryRefreshPivotCellLocal` に置換
- **新規ヘルパー (gb-db-props.js)**:
  - `_refreshAfterCellEdit(anchorEl, entityPath, propName)`: anchor から td を取得 → `_tryRefreshPivotCellLocal` を試行 → 失敗時は silent な `selectDatabase` にフォールバック。エンティティビューでは `selectEntity` にフォールバック
  - `_removeLocalPivotValue(val, entityPath, propName)`: 削除済み val をローカル `pivotData.entities` の配列から splice 除去
- **`_tryRefreshPivotCellLocal` の判定強化**: ソート対象列 (`sortCfg.key === propName`) の編集はフォールバックに切り替え (計画書 リスク 4 対応)
- **対応外 (フォールバック動作)**: formula / rollup / button / chat / multi-source-relation / groupBy 対象列 / advFilter 対象列 / ソート対象列 — `_tryRefreshPivotCellLocal` 内で false を返してフルリロード
- **Undo/Redo 整合**: 既存の `_dbUndoValue` / `_dbUndoStatus` の Undo callback は `selectDatabase` 経由でフルリロードするため、Undo 後の整合性は保たれる
- **計画書**: `app/docs/plan-db-perf-v2.md` Step 3

## v0.5.110 (2026-04-11)

### DB高速化v2 Step2: ピボットレンダリングのチャンク分割

- **対象**: `app/gb-database.js` の `renderPivot`、`app/gb-pane-context.js` の `_currentPaneState` / `destroyPaneContext`
- **目的**: 1万行 × 20列 = 20万 DOM 要素を一度に `appendChild` する初回ロードフリーズを解消する。`requestIdleCallback` で 100 行ずつチャンク化する
- **実装**:
  - `renderPivot` 内で行生成タスク (グループヘッダー + エントリ行) を `rowTasks` 配列にフラット化
  - 「＋新規エントリ」行を最初に `appendChild` してアンカーにし、チャンクは `tbody.insertBefore(frag, newEntryRow)` で挿入 → thead/tfoot/新規行/フッターの順序を常に確定させる
  - 最初の 100 行は同期生成 → 即座に表示。残りは `requestIdleCallback` (フォールバック `setTimeout`) で 100 行ずつ追加
  - **中断トークン**: `ctx._renderToken = Symbol('renderPivot')` を採番。各チャンクの先頭で `ctx._renderToken !== renderToken` をチェックして bail。同じ ctx の `renderPivot` 再呼び出し / `destroyPaneContext` で進行中チャンクを即座に破棄
  - **進行フラグ**: `ctx._renderInProgress` で進行中状態を保持。位置復元系ヘルパーがリトライの判断に使う
- **`_currentPaneState()` のシングルトン化**: フォールバック経路 (デフォルトの単一ペインモード) で呼び出しごとに新しいプロキシオブジェクトを返していたため、`ctx._renderToken = ...` で書き込んでも次の呼び出しでは別オブジェクトになり中断トークンが機能しなかった。フォールバックプロキシをモジュール内で 1 度だけ生成して memoize するように修正
- **`destroyPaneContext`**: 当該ペインの `_renderToken` を null にして進行中チャンクを停止
- **位置復元系ヘルパーのリトライ対応**:
  - `_restoreCellPos`: 対象行が DOM に未生成かつ `_renderInProgress` の場合、最大 20 回リトライ (約1秒)
  - `restoreActiveCellByEntityName`: 同上、`_waitForEntityRow` 経由で待機
  - `restoreActiveCellByRow`: rowIdx が現在の dataRows.length を超える場合、最大 30 回リトライ
  - `_waitForEntityRow` (新設): 新規エントリ作成後の rename / focus 用の汎用待機ヘルパー
- **新規エントリ作成系のリトライ対応**: `_handleNewEntryClick` / `_handleInsertRowRelative` / `triggerNewEntity` を `_waitForEntityRow` 経由に書き換え。`selectDatabase` 完了後 50ms で行を探していたが、チャンク分割で行が後追いの場合に取得失敗するのを防ぐ
- **D&D manualOrder 初期化のフォールバック**: `_handleTbodyDrop` で `cfg.manualOrder` が未設定のとき DOM から順序を組んでいたが、チャンク中は不完全な順序になるため、`renderPivot` で `ctx._lastEntityNames = entityNames` を保存し、進行中なら `_lastEntityNames` を優先利用するよう修正
- **tbody イベント委譲との互換性**: `_installTbodyDelegation` は tbody 自身に listener を登録しているため、後追いで insertBefore された行にもイベント bubbling で自動的に効く。確認のみ
- **計画書**: `app/docs/plan-db-perf-v2.md` Step 2

## v0.5.109 (2026-04-11)

### DB高速化v2 Step1: ピボットテーブルに content-visibility 導入

- **対象**: `app/gb-tools.css` の `#pivot-table tbody tr`
- **目的**: スクロール時に画面外行のレイアウト/スタイル計算/ペイントを Chromium ネイティブ機能でスキップさせ、1万行クラス DB のスクロール負荷を解消する
- **追加 CSS**:
  - `#pivot-table tbody tr` に `content-visibility: auto` + `contain-intrinsic-size: auto 36px`
  - `tr.group-header-row` / `tr.new-entity-row` は `content-visibility: visible` で強制表示 (数が少なく、見出しが消えるリスクを避ける)
- **行高推定値**: 36px (実測 padding 3px + font-size 13px ベース)。`auto` キーワード付きのため Chromium が一度実測した行サイズをキャッシュする
- **印刷経路**: アプリに `window.print()` / `@media print` は存在しないため対象外
- **DOM 操作との互換性**: `setActiveCell` は `classList.add('active-cell')` のみで `scrollIntoView` を呼ばない。`_restoreCellPos` は `tbody.querySelector` で行を取得するが、`content-visibility: hidden` の行も DOM 上に存在するため問題なし
- **計画書**: `app/docs/plan-db-perf-v2.md` Step 1

## v0.5.108 (2026-04-11)

### 台本エディタ フィルタプリセットに組み込み「すべて表示」を追加

- **背景**: フィルタプリセットの初期状態がプレースホルダ `（プリセット）` だったため、プリセット選択中から「全行表示に戻す」を 1 操作で行えなかった
- **対応**: 組み込みプリセット「すべて表示」を 1 件常時保持し、ドロップダウンの先頭・初期選択にする
  - HTML (gb-tool-scriptnote.js): `<option value="__all__">すべて表示</option>` を初期オプションとして配置
  - 選択時挙動: `_filterRoles = null` でフィルタ解除し、`#btn-filter` のアクティブ表示も外す。`_activeFilterPreset = '__all__'` を記録
  - `_refreshFilterPresets()` の `while (sel.options.length > 1) sel.remove(1);` ループは先頭オプションを残すため、ユーザープリセットを再構築しても「すべて表示」は維持される
- **削除不可**: 削除メニュー (`_showDeleteFilterPresetMenu`) は localStorage の `sn2-filter-presets` のみを反復するため、組み込みは出現しない。ドロップダウンから削除する手段なし
- **予約名チェック**: プリセット保存ダイアログ (`saveFilter` action) で、`__delete__` / `__all__` / `すべて表示` の名前は登録不可。表示メッセージで通知
- **削除メニューを開いた際のドロップダウン挙動**: 「── プリセット削除 ──」を選択したとき、ドロップダウンの選択を `_activeFilterPreset || '__all__'` に戻してから削除メニューをポップアップ表示（以前は空文字に戻していた）
- **フィルタチェックボックスポップアップとの同期** (gb-scriptnote-editor.js `_showFilterMenu`): 「すべて表示」チェックを押したときのドロップダウン同期を `value = ''` から `value = '__all__'` に変更

## v0.5.107 (2026-04-11)

### 台本エディタ 役割空行用 デフォルトタイプ + 一括書式の上書き化

#### 役割が空の行用 デフォルトタイプの追加

- **背景**: 役割が空（タイプ列が空 = メニューの「（なし）」を選んだ状態）の行には、これまでスタイルを設定する手段がなかった。タイプ管理に通常エントリを作っても役割名でしかマッチしないため、空役割行には適用されなかった
- **対応**: タイプ管理に **末尾固定の特殊エントリ「（デフォルト）」を 1 件常時保持** し、これが役割が空の行のスタイル供給元となるよう実装
  - データ表現: `chara.isDefault === true` の特殊フラグを持つ 1 件。`name: ''` で正規化（識別は `isDefault` フラグのみで行う）
  - `_ensureDefaultChara()` ヘルパー (gb-scriptnote-menu.js): 必ず末尾に 1 件のみ存在することを保証（重複削除 + 末尾移動 + 不在時新規作成）。コンストラクタと `_applySnapshot()` の末尾、`renderDetailPanel()` 冒頭で呼ぶ
  - `_applyRowStyle` / `_buildRowEl` / `_getCharaStyle` / `_sn2GetTextAffix` (gb-scriptnote-editor.js): 役割が空のときは `c.isDefault` でデフォルトタイプを引く。役割が非空のときは `!c.isDefault && c.name === role` で通常エントリだけを引く（防御的）
- **詳細パネルでの表示**: 通常エントリの末尾に「（デフォルト）」行として描画
  - 名前列はイタリック固定ラベル「（デフォルト）」（編集不可）
  - ドラッグハンドル・種別セレクト・オプションボタンを抑制（チェックボックスは表示）
  - 各列セル（区切り / 小区切り / タイプ / テキスト / カスタム列）の書式ポップアップ（背景色・文字色・太字・斜体・サイズ・テキスト前後・配置）はそのまま使用可能
  - 上部に区切り線 + 薄い背景で視覚的に分離（CSS: `.sn2-detail-default`）
- **保護対象**: 削除 / 複製 / D&D / 名前編集 / 種別変更 / 自動配色割当 / 行のタイプ選択メニュー / `_getCharacterList` / `byKind` グルーピング / `charaKindMap` の構築 / `gb-scriptnote-rules.js` のキャラ一覧描画 — いずれもデフォルトタイプを除外
- **一括設定への参加**: チェックボックスでデフォルト行を選択でき、全選択 / 一括設定ポップアップ / 列ヘッダー一括設定 / 一括色変更にデフォルトを含めて適用できる。ただし種別 (`kind`) 一括変更だけはデフォルト対象外
- **D&D 不変条件**: ドラッグ元が default なら preventDefault、ドロップ先が default の位置より後なら直前位置に強制。並び替え後に `_ensureDefaultChara()` を呼んで末尾固定を維持
- **使い方**: タイプ管理タブを開き、最下部の「（デフォルト）」行のセルをクリックして書式ポップアップで色等を設定する。台本エディタの役割空行（メニューで「（なし）」を選んだ行）にスタイルが反映される

#### 列ヘッダー一括設定の太字・斜体を上書き化

- **問題**: 列ヘッダー一括設定 (`_showColBulkPopup`) の太字・斜体ボタンは、選択中の各タイプの fontWeight / fontStyle を個別にトグルしていたため、選択前の状態に応じて結果が混在した
- **対応**: 選択全体に対する上書きに変更。「全員すでに bold なら解除、それ以外なら全員 bold」というロジックで、クリック後は必ず選択全体が揃う。斜体も同じパターン

#### 徹底チェックで修正した派生バグ

- **`_setRowRole` の新規キャラ追加が default を末尾から外していた問題** (gb-scriptnote-menu.js): 行のタイプメニューから新しい役割名を入力したとき、`characters.push(newChara)` で末尾追加していたためデフォルトタイプの後ろに新キャラが入っていた。デフォルトの直前に splice して末尾不変条件を維持
- **DB インポートでも同じ問題** (gb-scriptnote-detail.js `_showDbImportModal`): 同様にデフォルトの直前に挿入するよう修正
- **`_setRowRole` の outline 検索が空 role で default を拾えていなかった問題** (gb-scriptnote-menu.js): 役割が空のときも default の outline 設定が適用されるよう、空 role 時は `c.isDefault` で引くよう修正（`_applyRowStyle` 等と挙動を統一）
- **`_detectKind` のキャラ検索を防御的に default 除外** (gb-scriptnote-editor.js): 非空 role の探索で `!c.isDefault && c.name === r` を使うようにし、role が空白文字のみのときは早期 'blank' 返却
- **複製ボタンで default のみ選択時に無駄な undo エントリが残る問題** (gb-scriptnote-detail.js): `_pushUndo('タイプ複製')` を空チェックの後ろに移動

## v0.5.106 (2026-04-11)

### 台本エディタのカーソル / キーバインド + パネル UI 大幅改善

#### 台本エディタ カーソル系 (gb-scriptnote-editor.js / gb-scriptnote-editor.css)

- **カーソル消失バグ (致命的) を修正**: `caretEl` を contenteditable な `.sn2-text` の子要素として appendChild していたため、(1) `.sn2-text` に `position: relative` がなく座標がずれる、(2) caretEl が editable content として扱われ入力文字が caretEl 内に紛れ込む、(3) caretEl の `animation: sn2-caret-blink` を入った文字も継承して点滅、という三重バグが発生していた。caretEl の親を `.sn2-text` ではなく `.sn2-row` (既に position:relative) に変更し、editable content から完全に切り離した
- **`computeFallbackRect`** を新設。`range.getClientRects()[0]` が 0 サイズを返すケース (空セル / 改行直後 / DOM ミューテーション直後) で DOM 構造から推定で rect を組み立てる。Case A: テキストノード内オフセット → 1 文字戻し / 先頭文字位置の rect 利用。Case B: 直前要素が text node → 末尾文字の右側、BR → 次の行の先頭、空セル → textEl の左上 (縦書きなら右上)。`caretAfter` / `caretBefore` ヘルパーで縦横両モード対応
- **focusout 遅延化**: `setTimeout(..., 0)` で次フレームに `document.activeElement` を再確認。セル間フォーカス移動中に `relatedTarget` が瞬時に null になっても、host 内に戻れば caret を隠さない
- **Ctrl+矢印 行入れ替え後**: 二重 rAF で layout 確定後に `_caretSelChangeHandler()` を強制呼び出し
- **Shift+Enter 末尾改行**: trailing `<br>` を追加して空行を可視化。空テキストノードが残る分割ケースにも対応 (ループで可視コンテンツの有無を判定)。改行後の rAF 内でもキャレット強制更新
- **undo/redo 後のカーソル復元**: `_applySnapshot` が `_render()` で DOM を完全再構築する際にフォーカスが消えていた問題。snapshot 適用前にフォーカス中の rowId を保存し、render 後に rAF で同 rowId のテキストにフォーカスを戻して二重 rAF でキャレット再描画

#### 台本エディタ キーバインド (gb-scriptnote-editor.js)

- **Shift+矢印** (縦書きモードでは Shift+左右): セル境界に達した時のみ行選択を 1 行ずつ拡張。アンカーは `_lastSelectedIdx` を共用 (Shift+クリックと同じ起点)。フィルタ非表示行はスキップ
- **Ctrl+D**: 行選択・テキスト選択を一括解除。`_clearRowSelection` 内でアンカーもリセット
- **上下矢印 3 段階移動**: (1) 視覚的境界行 (Up=最上行 / Down=最下行) でなければブラウザのデフォルト (1 行移動)、(2) 境界行で絶対先頭/末尾でなければセルの絶対先頭/末尾へジャンプ、(3) 既に絶対先頭/末尾なら隣のセルへ移動。`compareBoundaryPoints` で textEl の絶対端を判定。複数行セルの中間行はブラウザに任せ、最上行/最下行で適切にセル先頭/末尾ジャンプを行う

#### 台本エディタ D&D / 詳細パネル (gb-scriptnote-editor.js / gb-scriptnote-editor.css / gb-scriptnote-rowset.js)

- **行ハンドル D&D 後の矩形選択誤発火**: pointerdown の除外リストに `.sn2-handle` を追加
- **複数行 D&D**: 開始行が `_rowSelection` に含まれる場合は選択中の全行を `dragRowIds` 配列に格納し、まとめて移動 (1 回の undo にまとめる、ラベル「行移動（複数）」)
- **詳細パネル全タブのスクロール対応**: `.sn2-detail` / `.sn2-ruby-panel` を `overflow: auto` に変更、`.sn2-rowset-panel` の inline style に `flex:1; overflow:auto` を追加。`.sn2-detail-list` に `min-height:0; min-width:0` を追加して flex item の自然サイズが flex 親をはみ出さないようにする。タイプ管理タブの toolbar は `position: sticky; left: 0` で横スクロール時も左端固定

#### パネル境界線 折りたたみボタン (gb-layout.js / gb-layout.css)

- **`gb-pane-collapse` ボタン削除**: 各パネル左上の折りたたみボタンを削除し、境界線上の `split-collapse-btn` 2 つに集約
- **境界線上に ◀▶ (縦は ▲▼) の 2 ボタン**: ハンドルにホバー時のみ表示。`pointer-events: auto` で常時 hit-test、`opacity: 0/1` で表示切替
- **双方向ボタン挙動**: 通常モードは「自分側を閉じる」、反対側が既に折り畳まれている場合は「反対側を展開する」に切り替わる (= ボタンの矢印方向 = 「ハンドルが動く方向」)
- **`_findAdjacentTarget`**: ハンドルから視覚的に最も近いパネル/サブツリーを再帰的に探索。親 split と同じ方向の split は更に潜り、異なる方向の split はそれ自体を 1 単位として返す。「フォルダツリー + 台本エディタ」のような横分割サブツリーで ◀ クリック時、台本エディタだけ (1 列分) を折りたたむ
- **外側 ratio 調整 (深いネスト対応)**: 深い target を折りたたむ場合、target の現在ピクセル幅に基づいて外側 split (=node) の `ratio` も縮める。これにより freed space が反対側のサブツリー (= ハンドル直反対側) に伝達される。`target._outerCollapseSaved` に外側 ratio を保存して展開時に復元
- **不要な「展開」ボタン削除**: `gb-split-expand-btn` を削除 (新ボタンが展開も処理するため不要)

#### パネルタブ D&D 並び替え (gb-layout.js / gb-layout.css)

- **同ペイン内タブを D&D で並び替え**: tabBar に `dragover` / `dragleave` / `drop` ハンドラを追加し、ピン留めによる視覚順と内部順の差を吸収するため ID ベースで `insertIndex` を計算して `GBTabs.moveTab` を呼ぶ。docking システムの zone 表示は `stopPropagation` で抑止
- **drop インジケータの flex-wrap 不具合修正**: 当初 `<div class="gb-tab-drop-marker">` を tabBar に DOM 挿入していたが、tabBar の `flex-wrap: wrap` で marker 分の幅が追加されてタブが折り返し、tab bar の高さが伸びていた。target タブ自身の `box-shadow: inset` (`.gb-tab-drop-before` / `.gb-tab-drop-after`) で描画する方式に変更、layout に一切影響しない。`tabEl.dragend` で全 marker を念のためクリアして Esc キャンセル等のリーク対策

## v0.5.105 (2026-04-11)

### DB 操作系バグ修正 9 件 + エントリ画面リライト 7 件 + 既存バグ 2 件解消

#### DB 操作系 (gb-database.js / gb-db-props.js / gb-tools.css)

- **DB1**: Shift+クリック範囲選択で 2 回目以降に anchor が失われる問題を修正。`_handleTbodyPointerdown` で `_pendingShiftAnchor` に anchor を保存し、change イベントが先に発火して `_lastSelectedCb` を上書きするブラウザ挙動への対策とした。
- **DB2 / 既存バグ #1**: 複数行 D&D が 1 行しか動かない問題を修正。tr.dragstart の `e.target !== tr` 早期 return を削除し、nameSpan ドラッグでも複数選択時は `text/x-meldex-rows` ペイロードを乗せる。
- **DB3**: セル更新の体感速度を改善。新規候補追加・インライン編集・ステータス変更後の全テーブル再描画を、ローカル `pivotData` 更新 + 当該セル DOM のみ書き換える増分パスに切り替え。`_tryRefreshPivotCellLocal` (gb-db-props.js) を新設し、groupBy / formula / rollup / chat / multi-source-relation / button はフルリロードへフォールバック。
- **DB4**: 列ヘッダー D&D の並び替え結果がバラバラ・2 回目以降効かない問題を修正。古い `colOrder` に存在しない列で `arr.indexOf(p) === -1` から `splice(-1, ...)` に陥っていた。`visibleProps` を起点に並び替え、hidden 列を末尾保持して `colOrder` を再構築。`renderPivot` に `ctx` を渡すよう修正。
- **DB5**: 水平スクロール時に固定列テキストが二重に重なる問題を修正。`td.col-entity` / `td.col-pinned` / `th.col-pinned` に `linear-gradient` を下敷きとして敷き、行ホバーや条件付き書式の半透明 background でも透けないようにする。
- **DB6**: 行追加 ＋ ボタンと grip ハンドルをエントリ名列の最左に配置 (チェックボックスはその右に移動)。＋ ボタンクリックで下に新規行追加、Alt+クリックで上に追加。`_handleInsertRowRelative` を追加し、`manualOrder` を更新して位置指定挿入を実現。
- **DB7**: 一括編集バーに「下に N 行追加」ボタンを追加。選択中の最後のエントリの下に、選択数と同じだけ新規行を一気に追加。
- **DB8**: 列固定中に解除手段がなくなる問題に対応。エントリ名列のメニューに「固定中の列を解除 (n)」サブメニューを追加し、個別解除と一括解除をサポート。
- **DB9**: エントリ名列自体の固定を切替できるようにする。`entityColumnPinned` 設定 (デフォルト true) を導入し、エントリ名列メニューから ON/OFF 切替可能。CSS で `entity-col-unpinned` クラス時に `col-entity` の `position:sticky` を解除。

#### エントリ画面 (gb-editor.js / gb-detail-panel.js / Meldex.html / gb-tools.css)

`renderEntityPage` を全面リライトし、`renderEntityPropsGridInto` 共通関数を新設。

- **エントリ1**: プロパティ値の編集 UI を DB と統一。`createTypedValueElement` に `propTypes` を渡し、select / status / multi-select / relation 等の型別ドロップダウンを利用可能にする。
- **エントリ2**: プロパティ D&D 並べ替え。HTML5 drag を card 単位で実装し、`entryPropOrder` を `dbViewConfig` に保存。同 DB 内の全エントリで順序を共有 (DB のプロパティ並び順とは独立)。
- **エントリ3**: プロパティ複数段表示。テーブルから CSS Grid (`auto-fill` / `minmax(260px, 1fr)`) に切り替え、パネル幅に応じた段数を実現。
- **エントリ4**: 詳細パネルのエントリ表示 (`_loadEntityInto`) を `renderEntityPropsGridInto` 共通関数に統合。詳細パネルでも entity-view と同じ編集 UI / D&D 並べ替え / 親 DB リンクが使えるようになる。
- **エントリ5**: 自由入力欄を「ノートエディタと同等」に。`entity-rt-toolbar` は既に `rt-toolbar` から複製されていたため、表示制御を整え contenteditable + Markdown 変換 + ツールバーで page-view と同等の編集体験を提供。
- **エントリ6**: 「ノートを作成」ボタン制。ノート未作成 (`page_content` 空) のときは `entity-freetext` / `entity-rt-toolbar` を非表示にし、`+ ノートを作成` ボタンを表示。クリックで空ノートを作成して編集開始。
- **エントリ7**: ページタイトル上に親 DB へのリンク (`← DB名`) を追加。クリックで親 DB を開く。

#### 既存バグ (app/docs/existing-bugs-20260410-d3.md)

- **#2**: 条件付きカラーモーダル「+ ルール追加」が動かない問題を修正。`data-action` 委譲経路の不具合を回避するため、ボタンに id を付与して直接 `addEventListener` でバインド。関数を `window` にも明示的に公開し、削除ボタンも直接バインドに変更。
- **#3**: グループ間 D&D を実装。`_handleGroupDrop` を追加し、ドラッグされた各エントリの `groupBy` プロパティ「採用 / 掲載済み」値を新グループ key に書き換える。formula / rollup / multi-source-relation はドロップ不可。`_handleTbodyDragover` / `_handleTbodyDrop` / `_handleTbodyDragleave` に `group-header-row` 対応を追加し、`group-drop-target` クラスにハイライトスタイル。

## v0.5.104 (2026-04-11)

### 仮想スクロール Phase 0 D-9: 受け入れ基準チェック + Phase 0 完了

D-2 から D-7 までの全サブステップを完了し、D-9 の硬い条件をすべて満たした状態に到達。Phase 1 (仮想スクロール本体) の前提が整った。

**追加のクリーンアップ:**
- `_setupCellValueDrag` を document レベル委譲化: per-element の `addEventListener` を廃し、`row.dataset.cvDragValue` / `cvDragEntityPath` / `cvDragPropName` で値を渡す。document に登録した `dragstart` / `dragend` リスナー (1 回のみ) が `.cell-value[draggable]` を closest で拾って処理。これにより値チップ毎の event handler を 0 に
- `_ensureCellValueDragDelegate()` をモジュールロード時に即時呼び出し

### Phase 0 D-9 受け入れ基準達成状況

**硬い条件 (Phase 1 着手の前提):**
- ✓ 全サブステップ D-2 〜 D-7 完了
- ✓ `renderEntityRow` / `renderEntityCell` / `renderGroupHeaderRow` / `renderNewEntryRow` の 4 関数で `state.currentDbPath` / `state.pivotData` 直接参照 0 件
- ✓ 4 render 関数で `addEventListener(...)` 0 件 (tbody 委譲のみ)
- ✓ `_setupCellValueDrag` も document 委譲化済み (Phase 0 強化)
- ✓ `ctx._selectedEntities` が Set として存在し、DOM ベースの選択取得が廃止
- ✓ `_restoreCellPos` が entityName / propName ベースで動作
- ✓ `destroyPaneContext` で `db-bulk-edit-bar[data-pane-id]` の cleanup
- ✓ 4 関数が分離されており、閉包に依存せず引数だけで動く
- ✓ renderPivot 本体の forEach ループが分離関数の呼び出しだけ
- ✓ renderEntityCell が型分岐 15 種以上を内部処理 (source / rollup / button / multi-source-relation / chat / formula / その他 default branch)

**柔らかい条件 (目安):**
- ✓ renderPivot 本体: 360 行 (500 行以下の目安をクリア)
- ✓ renderEntityCell: 111 行 (200 行以下の目安。型分岐 15 種を考慮しても妥当)
- ✓ renderEntityRow: 94 行 (D-3 時点 293 行 → D-4 で 240 行 → D-4-g で 91 行 → 現状 94 行。68% 削減)
- ✓ renderNewEntryRow: 23 行
- ✓ renderGroupHeaderRow: 11 行

**残課題 (Phase 2 以降):**
- `renderPivot` 内の 2 件の state 参照 (`ctx.X || state.X` フォールバック) はスプリットビュー対応時に整理
- `_upsertLocalPivotValue` / `_getPivotEntityData` の `state.pivotData` 参照は Phase 2 で ctx 引数追加
- `createValueElement` の per-chip ハンドラ (`status-dot click` / `value-text click` 等) は leaf 関数の per-value handler として残置。多数のチップを持つ DB では Phase 1 仮想スクロール導入後の負荷次第で再検討

**既存バグ (D-3 検証中に発見、D-9 とは独立):**
- 詳細は [app/docs/existing-bugs-20260410-d3.md](app/docs/existing-bugs-20260410-d3.md)
- 複数行 D&D / 条件付きカラーの「+ ルール追加」/ グループ間 D&D の 3 件
- いずれも Phase 0 のリファクタとは独立。Phase 1 着手前または Phase 1 と並行で修正予定

## v0.5.103 (2026-04-11)

### 仮想スクロール Phase 0 D-7: saveAndRestore + Undo の state 整理

`startCellInlineAdd` の冒頭で `_currentPaneState()` を取得し、`state.currentDbPath` 直接参照を ctx 経由に置換。Undo callback は既に dbPath を closure に保持する設計だったため追加修正は最小限。

- **`startCellInlineAdd`**: `const ctx = _currentPaneState(); const dbPath = ctx.dbPath || state.currentDbPath` を冒頭で実行。`checkColumnEditable` / `getStatusEnabled` / リレーション型自己参照の `relDb` 解決 / カスケード参照元データの取得をすべて ctx 経由に置換
- **`_dbUndoValue` / `_dbUndoStatus` / `_dbUndoPairValue` / `_dbUndoRename` / `_dbUndoManualOrder`**: 既に dbPath を関数エントリ時 (registration time) に closure に閉じ込めており、Undo 時には state を再読しない設計のため D-7 の意図にそのまま合致。追加修正なし
- **`saveAndRestore` 内部**: 全ての state 参照は closure 変数 (dbPath / entityPath / propName / ptc) 経由のため、すでに ctx-safe。スプリットビュー破棄後でも Undo callback 内で API 呼び出しが完結する設計

**残課題:**
- `_upsertLocalPivotValue` / `_getPivotEntityData` (gb-db-props.js) は内部で `state.pivotData` を読む。Phase 2 のスプリットビュー対応時に `ctx` 引数を追加する想定 (現状の単一ペインモードでは機能的に問題なし)

## v0.5.102 (2026-04-11)

### 仮想スクロール Phase 0 D-6: セル位置復元の entityName / propName 化

セル位置の保持型を `{rowIdx, colIdx}` から `{entityName, propName}` に変更。仮想スクロールで行が再生成されても entityName で位置復元できる前提を整える。

- **`_cellPos(td)`**: `tr.dataset.entityName` と `td.dataset.propName` から位置オブジェクトを生成
- **`_restoreCellPos(pos, moveTo)`**:
  - visibleProps を再計算 (`hiddenCols` / `colOrder` / `propTypes` から)
  - flatRows を DOM の `tr[data-entity-name]` 列挙で取得 (現在の表示順)
  - moveTo 'right' / 'left' は visibleProps インデックスで、'down' / 'up' は flatRows インデックスで隣接セルへ移動 (旧版に無かった 'up' も追加)
  - tr 検索に CSS.escape を使用 (entityName に特殊文字があっても安全)
  - 該当 tr が DOM にない場合のフォールバック経路を Phase 1 仮想スクロール導入用にコメントで残置 (`scrollToRow(entityName)` → `setActiveCell`)
- D-3 で renderEntityCell が `td.dataset.propName` を、renderEntityRow が `tr.dataset.entityName` をそれぞれ既に設定済みのため、追加の DOM 修正は不要

## v0.5.101 (2026-04-11)

### 仮想スクロール Phase 0 D-5: 選択状態の Set 化と db-bulk-edit-bar クリーンアップ

ピボット行の選択状態を DOM (`querySelectorAll('.row-select-cb:checked')`) ベースから Set (`ctx._selectedEntities`) ベースに移行。仮想スクロールで行が再生成されても entityName で選択状態を維持できる前提を整える。

**gb-pane-context.js の変更:**
- `PaneContext` クラスに `this._selectedEntities = new Set()` を追加
- フォールバック `_currentPaneState()` に `_selectedEntities` getter を追加 (state にシングルトン Set を保持)
- `destroyPaneContext(paneId)` で `db-bulk-edit-bar[data-pane-id]` を document.body から除去するクリーンアップを追加

**gb-database.js の変更:**
- `_getSelectedEntities(ctx)`: DOM クエリを廃止し `[...ctx._selectedEntities]` を返す。pivotData に存在しない stale エントリは self-clean
- `_updateBulkEditBar(ctx)`: バー id ベースから `data-pane-id` ベースに変更 (D-5-g: 複数ペイン対応の前準備)
- `clearBtn` ハンドラ: Set クリア + DOM 同期
- `_restoreSelectionByEntityNames`: Set にも反映 (render 跨ぎ用)
- `renderEntityRow`: cb 生成時に `ctx._selectedEntities.has(entityName)` で初期 checked 状態を設定
- `_handleCheckboxClick` / `_handleTbodyChange` / `_handleTbodyPointerdown` / `_handleTbodyPointerover`: いずれも Set add/delete を併走
- `_handleTbodyDragstart` の複数行 D&D ペイロード: `paneRoot.querySelectorAll('.row-select-cb:checked')` から `[...ctx._selectedEntities]` に置換
- `selectDatabase`: 別 DB への切替時に Set をクリア
- `_bulkDeleteEntities`: 削除した entityName を Set から明示除去

**残課題 (D-9 前に検討):**
- `_setupCellValueDrag` (値チップの drag) は依然として個別 addEventListener。leaf 関数で stopPropagation するため tbody まで届かない
- 旧 `id="db-bulk-edit-bar"` の参照箇所が他ファイルに残っている可能性 (後続で grep 確認)

## v0.5.100 (2026-04-11)

### 仮想スクロール Phase 0 D-4-g: tbody pointer 系イベント委譲化 + D-4 完了

renderEntityRow 内の `cb.pointerdown` / `cb.pointerenter` を tbody 委譲に移行。pointerenter は非バブリングのため `pointerover` で代替する。これにより D-4 (Phase 0-1: イベント委譲化) のサブステップ a〜g がすべて完了。

- **新規ヘルパー**: `_handleTbodyPointerdown(e)` (pointerdown ロジック)、`_handleTbodyPointerover(e)` (ドラッグ範囲選択拡張)
- **削除した個別ハンドラ**: cb.pointerdown / cb.pointerenter (renderEntityRow)
- **`_handleTbodyPointerdown` の挙動**: tr.draggable=false 一時無効 + suppressClick 1 回登録 + ステイル anchor チェック + Shift+クリックは早期 return + 通常クリックは押下時即トグル & dragSelectState 開始
- **`_handleTbodyPointerover` の挙動**: dragSelectState がアクティブで cb.checked が状態と異なる場合のみ更新 (差分のみ反映で重複処理回避)
- **document level pointerup**: `paneRoot._dragSelectPointerUp` は renderPivot 内に残置 (ペイン単位のクリーンアップが必要なため、tbody 委譲とは独立)
- **`renderEntityRow` のサイズ**: 293 → 240 → 91 行 (D-4 全体で 69% 削減)

### Phase 0 D-4 完了状況

- ✓ click (D-4-a)、dblclick (D-4-b)、change (D-4-c)、contextmenu (D-4-d)、dragstart/dragend (D-4-e)、dragover/dragleave/drop (D-4-f)、pointerdown/pointerover (D-4-g)
- ✓ 4 render 関数 (renderGroupHeaderRow / renderEntityCell / renderEntityRow / renderNewEntryRow) は addEventListener フリー
- △ `_setupCellValueDrag` (値チップの drag) は依然として個別 addEventListener。leaf 関数で stopPropagation するため tbody まで届かない。D-9 の strict compliance のため後続検討

## v0.5.99 (2026-04-11)

### 仮想スクロール Phase 0 D-4-f: tbody dragover / dragleave / drop イベント委譲化

renderEntityRow 内の `tr.dragover` / `tr.dragleave` / `tr.drop` を tbody 委譲に移行。

- **新規ヘルパー**: `_handleTbodyDragover` / `_handleTbodyDragleave` / `_handleTbodyDrop`
- **削除した個別ハンドラ**: tr × 3 (dragover / dragleave / drop)
- **dragover の前 hover 追跡**: `tbody._lastDragoverTr` で直前にホバーしていた tr を追跡し、移動時に boxShadow をクリア
- **dragleave の境界判定**: `e.relatedTarget` が tbody 内なら無視、tbody から完全離脱した場合のみ boxShadow クリア
- **drop の manualOrder 初期化**: 元コードの `cfg.manualOrder = [...entityNames]` (closure 変数) を、DOM の `tr[data-entity-name]` 列挙で代替 (現在の表示順を保持)

## v0.5.98 (2026-04-11)

### 仮想スクロール Phase 0 D-4-e: tbody dragstart / dragend イベント委譲化

renderEntityRow 内の `tr.dragstart` / `tr.dragend` (行の並び替え) と `nameSpan.dragstart` / `nameSpan.dragend` (DB間 D&D) を tbody 委譲に移行。draggable 属性は維持。

- **新規ヘルパー**: `_handleTbodyDragstart(e)` / `_handleTbodyDragend(e)` — `.entity-name-label` を nameSpan ドラッグ、`tr[data-entity-name]` をエントリ行ドラッグとして判別
- **削除した個別 dragstart/dragend ハンドラ**: tr 用 2 件、nameSpan 用 2 件
- **値チップ (`.cell-value`) のドラッグ**: `_setupCellValueDrag` 内で `e.stopPropagation()` するため tbody まで届かず、委譲対象外。leaf 関数扱いで残置
- **既知バグの保持**: tr.dragstart の `if (target !== tr) return` 早期 return は元コードのまま (existing-bugs-20260410-d3.md 記載の複数行 D&D バグ)。本サブステップでは挙動を変えない方針

## v0.5.97 (2026-04-11)

### 仮想スクロール Phase 0 D-4-d: tbody contextmenu イベント委譲化

renderEntityRow 内の `tr.addEventListener('contextmenu', ...)` を tbody 委譲に移行。

- **新規ヘルパー**: `_handleTbodyContextmenu(e)` — `tr[data-entity-name]` (new-entity-row / group-header-row 以外) で `showDbCardContextMenu` を呼ぶ
- **削除した個別 contextmenu ハンドラ**: tr.contextmenu (renderEntityRow)
- thead の contextmenu (列ヘッダー) は行数に依存しないため対象外で残置

## v0.5.96 (2026-04-11)

### 仮想スクロール Phase 0 D-4-c: tbody change イベント委譲化

renderEntityRow 内の `cb.addEventListener('change', ...)` (キーボード/Space 等の checkbox 変更フォールバック) を tbody 委譲に移行。

- **新規ヘルパー**: `_handleTbodyChange(e)` — `.row-select-cb` の change で tr の `row-selected` クラス、`paneRoot._lastSelectedCb`、`_updateBulkEditBar` を更新
- **削除した個別 change ハンドラ**: cb.change (renderEntityRow)
- 選択状態の保持メカニズムは DOM (cb.checked) のまま (D-5 で Set 化に置換予定)

## v0.5.95 (2026-04-11)

### 仮想スクロール Phase 0 D-4-b: tbody dblclick イベント委譲化

renderEntityRow 内の `tdName.addEventListener('dblclick', ...)` (インラインリネーム) を tbody 委譲に移行。

- **新規ヘルパー**: `_handleTbodyDblclick(e)` — `td.col-entity` (new-entity-row 以外) で `startEntityInlineRename` を呼ぶ
- **削除した個別 dblclick ハンドラ**: tdName.dblclick (renderEntityRow)
- 渡す要素: `tdName` (col-entity td)、`nameSpan` (`.entity-name-label`)、`entityName` (tr.dataset)、`dbPath` (ctx.dbPath) — すべて DOM/dataset/ctx から逆引き

**徹底チェック**: data cell には dblclick 処理が無いため対象は tdName 1 種のみ。toolbar / thead / 列リサイズハンドルの dblclick は行数に依存しないため対象外で残置。

## v0.5.94 (2026-04-10)

### 仮想スクロール Phase 0 D-4-a: tbody click イベント委譲化

renderEntityRow / renderEntityCell / renderGroupHeaderRow / renderNewEntryRow 内の個別 click 用 addEventListener を全廃し、tbody 1 つに委譲。`e.target.closest()` で 9 種類のターゲットを順次判別。

- **新規ヘルパー**: `_handleTbodyClick` (オーケストレーター 106 行)、`_handleNewEntryClick` (22 行)、`_handleRelationLinkClick` (30 行)、`_handleCheckboxClick` (Shift+クリック範囲選択 20 行)、`_installTbodyDelegation` (べき等な委譲登録 7 行)
- **削除した個別 click ハンドラ**: gtd / btn (button型) / addBtn / td (空セル) / cb (Shift範囲) / tdName (詳細パネル) / moreBtn / relation-link / 新規エントリ tdName の計 9 種
- **追加した dataset**: `td.dataset.propName` (renderEntityCell) と `gtr.dataset.groupKey` (renderGroupHeaderRow)。これで委譲ハンドラから entityName / propName / groupKey を逆引き
- **render 関数のサイズ**: renderEntityRow 293 → 240 行、renderEntityCell 122 → 111 行、renderNewEntryRow 43 → 23 行、renderGroupHeaderRow 14 → 11 行
- **判別優先順位** (`_handleTbodyClick` 内): group-header-row → new-entity-row (col-entity 限定) → relation-link → entity-row-more-btn → row-select-cb → cell-add-btn → db-action-btn → col-entity td → 一般データセル

**保持した個別ハンドラ (D-4-a 対象外、後続サブステップで処理):**
- cb.pointerdown / pointerenter / pointerup → D-4-g
- cb.change → D-4-c
- tdName.dblclick → D-4-b
- tr.contextmenu → D-4-d
- tr.dragstart / dragend / dragover / dragleave / drop → D-4-e/f
- nameSpan.dragstart / dragend → D-4-e
- 列ヘッダー周辺の click (thead 内、行数に依存しない)

**徹底チェック**: 新規エントリ行の空データセルで誤って作成が走らないよう、check 2 で `target.closest('td.col-entity')` ガードを追加した。

## v0.5.93 (2026-04-10)

### 仮想スクロール Phase 0 D-3: renderPivot から行・セル生成関数を分離

renderPivot 内の `groupedEntities.forEach` 〜 `names.forEach` 〜 `visibleProps.forEach` の3重ループを4関数に切り出した。閉包依存を排し、すべての必要な値を `options` 経由で受け取る構造に変更。Phase 1 仮想スクロール導入時に `VirtualScrollManager.onRangeChange` から任意インデックスの行を単独生成できる前提を整える。

- **新規関数 4 つ** (gb-database.js):
  - `renderGroupHeaderRow(groupKey, names, visibleProps, groupByProp, ctx) → tr` (14 行) — グループヘッダー行 + 折りたたみ click ハンドラ
  - `renderEntityCell(entityName, propName, ctx, options) → td` (122 行) — セル生成 + 型分岐 15 種 (source / rollup / button / multi-source-relation / chat / formula / relation / select / multi-select / status / date / url / text / number / checkbox / note 等) + 条件付き書式 + 空セルクリック
  - `renderEntityRow(entityName, ctx, options) → tr` (293 行) — エントリ行全体 (D&D / 条件付きクラス / チェックボックス / nameSpan / moreBtn / リレーションリンク / セルループ / +プロパティ列)
  - `renderNewEntryRow(ctx, options) → tr` (43 行) — 末尾の新規エントリ行
- **renderPivot 本体は 813 行から 358 行へ縮小** (約 56% 削減)。残るのは thead 構築、列 D&D、グループ化 Map 構築、フッター呼び出しのみ
- **閉包依存ゼロ:** 4 関数すべて、必要な値は引数 (`entityName` / `ctx`) と `options` オブジェクト経由で受け取る。`pLeftOffset` は `cellOpts` を mutable 共有することで行内で累積
- **意図的な制約:** D-3 はコード移動だけ。個別 `addEventListener` のまま移植 (イベント委譲化は D-4 で実施)

### Phase 0 受け入れ基準への適合状況

- ✓ 4 関数が分離されており、閉包に依存せず引数だけで動く
- ✓ renderPivot 本体の forEach ループが分離関数の呼び出しだけになっている
- ✓ renderEntityCell が型分岐 15 種以上を内部で処理
- ✓ renderPivot 本体 358 行で「500 行以下」の柔らかい目安をクリア
- △ renderEntityRow が 293 行で「200 行以下」の柔らかい目安を超過。理由は D&D / チェックボックス / nameSpan / リレーション / セルループの 5 系統を必然的に抱えるため。D-4 のイベント委譲化で大幅に縮む見込み

## v0.5.92 (2026-04-10)

### 徹底チェックで追加発見した不具合の修正

- **ステイル `_lastSelectedCb` で Shift+クリック範囲選択が壊れる問題**: renderPivot で DOM が再生成された後、`paneRoot._lastSelectedCb` が detached なエレメントを指したまま残る。次の Shift+クリック時に pointerdown がこのステイル参照を「有効な anchor」とみなして range 経路に入ってしまい、click ハンドラ側でも `allCbs.indexOf(staleCb) === -1` で範囲選択が無音で skip され、結局 cb が一切トグルされない症状を発生させていた。pointerdown で `anchorCb.isConnected` をチェックし、ステイルなら `paneRoot._lastSelectedCb = null` してから通常クリック扱いにフォールバックするように修正
- **`_restoreSelectionByEntityNames` 後に一括編集バーの件数が更新されない問題**: ヘルパー内で復元対象が 1 件以上あれば末尾で `_updateBulkEditBar(ctx)` を呼ぶよう変更。D&D / Undo / Redo 後に常に件数表示が追従する
- **行 D&D の Undo で選択状態が消える問題**: `_dbUndoManualOrder` が単一の `movedName` しか保持していなかったため、Undo 後に新しい DOM がレンダーされた際にチェックボックスが全てリセットされていた。シグネチャを `movedNames`（配列）に変更し、apply 内で `_restoreSelectionByEntityNames(undefined, names)` を呼んで全ての移動エントリのチェックを復元。ヒストリー label も「行移動: foo 他 N 件」表示に変更
- **`showColHeaderMenu` のリネーム入力で `_setSelectedColumns` の追従が漏れていた問題**: 列名変更後、列選択状態が古い名前のままになっていた。`startHeaderInlineRename` と同じ追従処理を追加
- **テキスト型セルの空文字列削除パスで入力要素がそのまま残る問題**: `selectDatabase` 待ちの間、span 内に空の input が表示されたまま。await 直前に `span.textContent = ''` でクリア
- **テキスト型セル空文字列削除の Undo/Redo でファイル不一致**: 削除直後の `val` を redo 用にキャプチャしていたが、Undo で再作成された新ファイルとは別の `candidate_index` になるため Redo が失敗する。`currentVal` を closure 変数にして undo 時に新ファイルの metadata で更新するように変更
- **値変更系 Undo/Redo でローディングオーバーレイが点滅する**: `_dbUndoValue` / `_dbUndoPairValue` / `_dbUndoStatus` の selectDatabase 呼び出し、および値追加 Undo/Redo (startCellInlineAdd 内の historyPush) を `silent: true` に変更し、Undo/Redo の体感速度を向上

### データベース: 選択・D&D・メニュー・編集体験の改善

### データベース: 選択・D&D・メニュー・編集体験の改善

#### チェックボックス選択

- **Shift+クリック範囲選択を修正**: pointerdown が無条件に `_lastSelectedCb = cb` を上書きしていたため、続く click ハンドラが範囲の起点を読めず常に「単独 cb」とみなされていた。Shift+クリック時かつ範囲起点が存在する場合のみ pointerdown は何もせず click ハンドラに任せる。範囲起点がない場合は通常クリックと同じ動作にフォールバックして cb をトグルする
- **複数選択時の D&D で選択行を全て移動**: 行 dragstart 時に「自身がチェック中」かつ複数選択中なら、選択中エントリ名のリストを `text/x-meldex-rows` で運ぶ。drop 時は元の並び順を保ったまま連続ブロックとして挿入位置に投入
- **D&D 後にチェック状態を保持**: drop 内で `_restoreSelectionByEntityNames(ctx, orderedDragged)` を呼び、新しい DOM の cb を再 check + tr に row-selected を再付与

#### セル値の編集

- **値の追加・削除でローディングオーバーレイが出ないように改善**: `selectDatabase()` に `silent: true` オプションを追加。saveAndRestore / 値削除 / ステータス変更 / インラインテキスト編集の各経路で silent モードで呼ぶようにし、ローディング overlay の点滅を消した
- **テキスト型セルで空文字列 + Enter で候補値を削除**: `startInlineEdit` の finish 関数で `nv === '' && old !== ''` の場合は `_apiPutValue(val, { _delete: true })` を呼ぶように変更。専用の `historyPush('値削除: ...')` も登録し Undo で復元可能

#### 列ヘッダーメニューの整理

- **エントリ名列ヘッダーを左クリックでもメニュー表示**: th0 に click リスナーを追加し、`showEntityColMenu(e)` を呼ぶ。リサイズハンドル上のクリックは除外
- **「ソート」→「並び替え」にリネーム**: `showEntityColMenu` 内のラベルとプロパティ別並び替えセクションのコメントを置換
- **すべての列ヘッダーメニューに「並び替え」サブメニューを追加**: `showColHeaderMenu`（列名変更の下）に「この列で昇順/降順」「名前 昇順/降順」「マニュアル」を含む並び替えサブメニューを追加。`getDbViewConfig().sortConfig` の現在値にラジオマークを表示

#### メニュー上部のリネーム入力欄

- **すべてのヘッダー・セルのコンテキストメニュー上部にリネーム入力欄を追加**:
  - 共通ヘルパー `_addMenuRenameInput(menu, currentValue, onRename, opts)` を `gb-db-props.js` に追加
  - 入力欄は menu の最上部に挿入され、即フォーカス + 全選択状態に。Enter で確定（コールバック呼び出し + メニュー閉じ）、Escape で破棄
  - keydown は `e.stopPropagation()` でグローバルショートカットへ伝搬させない
  - 適用先:
    - **`showColHeaderMenu`** （列ヘッダー）→ 列名を即時リネーム（colOrder / propertyTypes / colWidths も同期）
    - **`showEntityColMenu`** （エントリ名列ヘッダー）→ DB 名を即時リネーム（`apiPut('/rename')`）
    - **`showDbCardContextMenu`** （エントリ右クリック / ⋯）→ エントリ名を即時リネーム（`_dbUndoRename` 経由）
    - **`_showValueContextMenu`** （セル値 ⋯）→ 値テキストを即時リネーム（`_dbUndoValue` 経由）

#### 影響ファイル

- `app/gb-database.js`: 行 dragstart / drop の複数行対応、`_restoreSelectionByEntityNames` 呼び出し、`selectDatabase` の silent オプション、`startInlineEdit` の空文字列削除、cb pointerdown の Shift+クリック分岐、エントリ名列ヘッダー click、`_showValueContextMenu` のリネーム入力欄
- `app/gb-db-props.js`: `_addMenuRenameInput` ヘルパー追加、`showColHeaderMenu` / `showEntityColMenu` / `showDbCardContextMenu` でリネーム入力欄表示、ソート→並び替え rename、列ヘッダーメニューに並び替えサブメニュー追加

## v0.5.91 (2026-04-10)

### 徹底チェックで発見した不具合の修正

- **行 D&D の Undo で sortConfig が誤って `manual` に固定される問題を修正**
  - D&D 前に sortConfig が未設定（`null`、= 既定の名前順）だった場合、Undo 後も sortConfig が `manual` のままになり、本来の名前順に戻らなかった
  - `_dbUndoManualOrder` の apply 関数を `sortConfig === null` のとき `delete c.sortConfig` するように変更
- **チェックボックスの Shift+クリック範囲選択が壊れていた問題を修正（pre-existing）**
  - `pointerdown` ハンドラが `_lastSelectedCb = cb` を無条件に上書きしていたため、続く click ハンドラが範囲の起点を読めず常に「cb 単独」とみなされて範囲選択が動作しなかった
  - `pointerdown` 内で Shift キーが押されている場合は手動トグル・`_lastSelectedCb` 更新・`dragSelectState` セットをすべてスキップし、既存の click ハンドラに範囲選択を任せる
  - Shift+クリック経路でも click のネイティブトグル抑止と行 HTML5 ドラッグ抑止は引き続き行う
- **ステータス機能 OFF の DB で「既存の値を置き換える」を外して一括編集すると複数候補値ができる問題を修正**
  - 一括編集ダイアログで `statusOn === false` の場合、`bulk-edit-replace` チェックボックス行を丸ごと省略し、適用時は常に `replace = true` を強制
- **キーボードショートカット経由で `startCellInlineAdd` が status OFF + 既存値ありセルを通り抜ける問題を修正**
  - `startCellInlineAdd` の冒頭にガードを追加。`getStatusEnabled === false && container.querySelector('.cell-value')` の場合は警告を出して return
  - `gb-db-props.js` の `_refreshPivotRelationCell` も同様に「ステータス OFF + 既存値あり」では add ボタンを描画しない条件を追加

### データベース: 一括編集の改善 + ステータス機能の DB 単位トグル

#### 一括編集

- 一括編集適用後、編集対象だったエントリの選択状態を再適用するように変更
  - 従来は `selectDatabase` の再描画でチェックボックスが全てリセットされ、続けて別のプロパティを編集するたびに選択し直す必要があった
  - 新規ヘルパー `_restoreSelectionByEntityNames(ctx, entityNames)` を追加し、editName ベースで row-select-cb を再 check + tr に row-selected を再付与
- 一括編集ダイアログの「既存の値を置き換える」チェックボックスの初期値を ON に変更
  - これまでは未チェックがデフォルトで「候補値として追加」される動作。実際のユースケースでは置き換え動作が一般的なため初期値を反転

#### ステータス機能の DB 単位トグル（既定オフ）

- DB 単位で「ステータス機能」をオン/オフ切り替え可能にした。**初期値はオフ**（既存 DB も含めて undefined → false 扱い）
- `gb-app.js` に `getStatusEnabled(dbPath)` / `setStatusEnabled(dbPath, on)` ヘルパーを追加（`dbViewConfig.statusEnabled` フラグで永続化）
- オフ時の挙動:
  - **セルに候補値追加 +ボタンを非表示**（既に1件以上値がある場合のみ。空セルの初回入力は許可）
  - **ステータスドット（カラー●）を非表示**（採用状況フィルタの状態と独立）
  - **一括編集ダイアログの「ステータス」ドロップダウンを非表示**（適用時は内部既定値 `'採用'` を使用）
- オン/オフ切り替え UI: エントリ列ヘッダの右クリックメニュー → 表示設定 → ステータス機能（オン/オフのラジオ）

#### 影響ファイル

- `app/gb-app.js`: `getStatusEnabled` / `setStatusEnabled` 追加
- `app/gb-database.js`: `_restoreSelectionByEntityNames` 追加、bulk-edit-replace の checked 初期化、bulk-edit-status のトグル、cell-add-btn / status-dot のトグル
- `app/gb-db-props.js`: status-dot のトグル、`showEntityColMenu` の表示設定サブメニューに「ステータス機能」追加

## v0.5.90 (2026-04-10)

### データベース: D&D 行移動・インラインリネームの改善

#### 行移動

- D&D で行を並べ替えた直後、移動したエントリ行を自動でアクティブセルにする
- 行 D&D を Undo / Redo / ヒストリーパネルに対応（`historyPush('行移動: <name>', ...)` 経由、スコープは `db:<dbId>`）
  - Undo 時は移動前の `manualOrder` と `sortConfig` を完全復元（マニュアルソートに切り替わる前の状態にも戻せる）
  - Redo 時は移動後の `manualOrder` を再適用しマニュアルソートへ
  - 新規ヘルパー `_dbUndoManualOrder(dbPath, movedName, oldOrder, oldSortConfig, newOrder)` を追加

#### インラインリネーム

- エントリ名のダブルクリック時、入力欄が「⋯」ボタンの下に折り返して一時的に二行になる現象を修正
  - `td.appendChild(inp)` を `td.insertBefore(inp, nameSpan)` に変更し、入力欄を nameSpan の位置に直接差し込む
  - リネーム中は `.entity-row-more-btn` と `.relation-links` を `display:none` で退避（commit 時は renderPivot で再構築されるため復元処理は不要）
- 右クリックメニュー / ⋯ ボタンメニューの「リネーム」がダイアログを開いていた挙動を、インラインリネーム起動に変更
  - ピボットテーブル上に該当エントリ名 span が存在すればインラインに切り替え、なければ従来の `cfPrompt` ダイアログにフォールバック
  - これによりピボットビューでは一貫してインライン編集、ギャラリー / カンバンビューではダイアログという動作になる

#### 影響ファイル

- `app/gb-database.js`: 行 D&D drop ハンドラ、`startEntityInlineRename`、新規 `_dbUndoManualOrder`
- `app/gb-db-props.js`: `showDbCardContextMenu` のリネーム項目

## v0.5.89 (2026-04-10)

### 仮想スクロール計画 Phase 0 D-2: renderPivot 本体の state 参照を ctx 経由へ置換

大規模 DB 対応の仮想スクロール導入（計画書 `app/docs/plan-virtual-scroll.md`）に向けた前提リファクタリング Phase 0 に着手。最初のサブステップ D-2 として、renderPivot 本体からグローバル state への直接参照を除去する作業を実施。

#### 変更
- `gb-database.js`: `renderPivot(ctx)` 本体（426〜1154 行）内で `state.currentDbPath` / `state.pivotData` を直接参照していた 14 箇所を、先頭でローカル変数化済みの `dbPath` / `entitiesMap` に置換
  - エントリ名セルの dragstart / click / dblclick / moreボタン / tr contextmenu
  - リレーションリンクメニューのビューワープレビュー
  - ボタン型 / マルチソースリレーション型 / チャット型セルの entityPath 構築
  - 通常セルの createTypedValueElement / createValueElement / startCellInlineAdd
  - 新規エントリ行クリック時の既存エントリ名取得と DB パス参照
- renderPivot 先頭（428, 431 行）のフォールバック `ctx.pivotData || state.pivotData` / `ctx.dbPath || state.currentDbPath` は引数なし呼び出しとの互換性のため維持
- 仮想スクロール下で任意インデックスの行生成がグローバル state に依存しない状態に向けた第一歩

#### 計画書更新
- `app/docs/plan-virtual-scroll.md`: D-9「Phase 0 完了の受け入れ基準」を書き換え
  - renderPivot 本体の行数基準（旧「400 行以下」）を硬い条件から外し、柔らかい条件「500 行以下目安」に移行
  - 硬い条件として「4 つの行生成関数が閉包に依存せず引数だけで動くこと」「forEach ループが分離関数呼び出しだけになっていること」「renderEntityCell が型分岐 15 種を内部処理していること」を明記
  - 行数基準を厳守しない理由（徹底チェックで判明した残留処理量の多さ、薄い補助関数の量産回避）を明文化

### 列が 2 つずつ表示されるバグの防御

renderPivot で `colOrder`（localStorage の `dbViewConfig:<fileId>` に永続化）に重複が混入した状態で DB を開くと、ヘッダー・セルの列が二重に表示される現象への防御修正。

#### 原因
- `gb-database.js` の renderPivot 先頭で `let props = colOrder ? [...colOrder] : [...data.properties];` と展開する際、`colOrder` 自体に重複があった場合にそれがそのまま props 配列に残る
- 以降の列操作（列 D&D・プロパティ追加・ピン留め変更等）で `setColOrder(dbPath, getColOrder(dbPath) || [...props])` にフォールバックする経路が複数あり、重複した props を再保存することで重複が永続化する悪循環が成立

#### 修正
- `gb-database.js`: renderPivot 先頭の props 計算に `props = [...new Set(props)];` の 1 行を追加。colOrder 展開直後に重複を除去することで、過去に壊れた colOrder が読み込まれても画面上の重複表示が解消される
- 以降の列操作時には重複除去済みの props が setColOrder 経由で保存されるため、localStorage 側の colOrder も自然とクリーンアップされる

### テスト用 DB を新規追加

- `app/MeldexHome/サンプル/テストDB/`: Phase 0 リファクタ動作確認用の新形式 DB（120 エントリ）
  - フォルダノートで `type: settings-db` と property_types を定義
  - 各エントリは `type: settings-entry` + `properties` dict 形式で書き込み済み
  - text / select / multi-select / date / number / checkbox / url / note の 8 型を網羅
  - D-8 主要操作一覧の手動確認に使用

## v0.5.88 (2026-04-10)

### Phase 1 残作業: ツールメニューの「最近使ったファイル」を localStorage 経由に切り替え + コメント整理

`topbar-cleanup-plan.md` の Risk 節「ツールメニューの『最近使用』」が Phase 1 で取りこぼしていた件を修正。

#### 修正
- `gb-tool-menu.js`: `_appendRecentSection()` が `navHistory[]` を直接読んでいたが、Phase 1 で `navHistory[]` がエクスプローラー履歴限定になり、ツールタブメニューの「最近使ったファイル」が事実上空になっていた
- `localStorage` の `meldex-recent`（`addRecent()` が更新するキー）から最大 8 件を取得するよう変更
- 元の挙動と互換: 全ツール横断の最新ファイルを表示、クリックで `navOpen()` 経由でオープン

#### コメント整理
- `gb-pane-bridge.js`: `_overrideNavPush` 内のコメントを「履歴管理はレガシー版に委譲」→「パネルタブ更新が責務。Phase 1 以降、履歴管理は explorerHistoryPush に分離済み」に更新（Phase 1 以前の挙動を指していた古いコメント）

## v0.5.87 (2026-04-10)

### Phase 6: アプリバー整理 — ツールタブ整理（案C: 現状維持）

`app/docs/topbar-cleanup-plan.md` の Phase 6 について、3 案（A: 全メニュー統合 / B: 主要 5 つのみ残す / C: 現状維持）から **案 C（現状維持）** を採用。

#### 判断理由
- Phase 5 でアプリバーが左配置（縦並び）も選べるようになり、縦配置時はツールタブ 8 個が縦に並ぶため視覚的な圧迫感が少ない
- ツールタブを直接アクセスできるアプリバー上の利便性を維持
- 必要なら将来的に再検討可能（コード変更なしで決定を覆せる）

#### 結果のアプリバー構成（Phase 1-6 完了時、15 ボタン）
```
[メニュー]
[ツリー] [エクスプ][ノート][台本][DB][スマートDB][ボード][ダッシュ][アクション]
[ビューワー][詳細]
(ワークスペースタブ)
[メモツール]
[ユーザー][設定]
```

#### Phase 1-6 全体の削減効果
- 開始時: 24 ボタン
- Phase 1（戻る/進む 移動）: −2 → 22
- Phase 2（インポート/エクスポート 統合）: −2 → 20
- Phase 3（オーバーレイ 移動）: −1 → 19
- Phase 4（チャット/カレンダー/ヒストリー/メモ 統合）: −4 → 15
- Phase 5（リネーム + 配置設定）: 0
- Phase 6（ツールタブ整理）: 0（案 C 採用）
- **合計: 24 → 15 ボタン（−9, −37.5%）**

## v0.5.86 (2026-04-10)

### Phase 5: アプリバー整理 — `#top-bar` → `#app-bar` リネーム + 配置方向設定

`app/docs/topbar-cleanup-plan.md` の Phase 5 を実装。

#### Phase 5-1: ID リネーム
- `Meldex.html`: `id="top-bar"` → `id="app-bar"`
- `gb-layout.css`: 全 14 箇所の `#top-bar` セレクタを `#app-bar` に置換
- `gb-pane-bridge.js`: `_initToolButtonDrag()` のセレクタ更新
- `meldex-core.js`: `el.closest('#top-bar')` を `el.closest('#app-bar')` に更新

#### Phase 5-2: 配置方向対応 CSS（gb-layout.css）
- `body[data-appbar-position="top"]`（デフォルト）: 既存の挙動
- `body[data-appbar-position="bottom"]`: `order: 99` で center-area の最下部に移動。border が下→上に切り替わる
- `body[data-appbar-position="left"]`: `position: fixed; left: 0` で画面左端に縦並び（48px 幅）。`#center-area { margin-left: 48px }` で本文を押し出す
- `body[data-appbar-position="right"]`: 同様に画面右端に縦並び
- 縦並び時はワークスペースタブ（`#workspace-tabs`）を非表示、セパレータを横線に切替

#### Phase 5-3: 設定ダイアログ
- `gb-settings.js`: 「全般」タブの「表示サイズ」セクションの直下に「アプリバーの位置」セクションを追加
- 選択肢: 上 / 下 / 左 / 右
- localStorage キー: `meldex-appbar-position`
- `applyAppbarPosition(pos)` ヘルパー関数を新設し `body.dataset.appbarPosition` を切替

#### Phase 5-4: 起動時適用 + モバイル例外
- `gb-app.js`: 起動時に `localStorage.getItem('meldex-appbar-position')` を読み、`applyAppbarPosition()` を呼ぶ
- `gb-layout.css` の `@media (max-width: 768px)` ブロック内で配置設定を `!important` で上書き、強制的に「上」配置 + 折り畳みメニューに（`responsive-ui-plan.md` 準拠）

#### 設計判断
- HTML 構造の restructure を避け CSS のみ（`position: fixed` + `margin`）で実装。リスクを最小化
- 既存の `#center-area` / `#sidebar` / `#right-panel` のフレックス構造を一切変更しない

## v0.5.85 (2026-04-10)

### Phase 4: アプリバー整理 — チャット/カレンダー/ヒストリー/メモをメニュー統合

`app/docs/topbar-cleanup-plan.md` の Phase 4 を実装。

#### Phase 4-A: トップバーから 4 ボタンを除去
- `Meldex.html`: `.tb-rpanel-group` から「チャット」「カレンダー」「ヒストリー」「メモ」ボタンを除去（ビューワー / 詳細のみ残す）
- `gb-app.js`: モバイルツールメニュー（`showMobileToolMenu`）から該当 4 項目を除去

#### Phase 4-B: メニュー（☰）にパネルセクションを追加
- `gb-outliner.js`: `toggleActivityMenu()` 内に「パネル」セクションを追加。フォルダツリー / ビューワー / 詳細 / チャット / カレンダー / ヒストリー / メモの 7 パネルを開閉可能
- 各項目は `GBTabs.findPaneWithTab(toolType, '')` で開閉状態を判定し、開いているものにチェックマーク（`lucide('check', 14)`）を表示
- メニューは都度生成されるため、リアルタイム同期は不要（開いた瞬間の状態を反映）

#### Phase 4-C: 死にコード除去
- `gb-pane-bridge.js`: `_toolActionToType` マップから chat/calendar/history/annotation の 4 エントリを削除（参照元のボタンが消えたため）

#### 既存の初期レイアウト（変更なし）
- `_buildDefaultLayout()` は既に「ツリー(15%) | メイン(82%) | ビューワー+詳細(縦分割) | ツール(折り畳み, 4タブ)」の構造を持っており、Phase 4-C の要求と一致するため変更不要
- 既存ユーザーの保存レイアウトは現状維持（マイグレーションは行わない）。チャット/カレンダー/ヒストリー/メモを開くにはメニュー経由で対応する

#### 削減効果
- アプリバー 19 → 15 ボタン（−4）

## v0.5.84 (2026-04-10)

### Phase 3: アプリバー整理 — オーバーレイをメモツールフロートバーへ移動

`app/docs/topbar-cleanup-plan.md` の Phase 3 を実装。

- `Meldex.html`: トップバーから `btn-overlay-toggle` を除去
- `Meldex.html`: `#ann-toolbar`（メモツールフロートバー）の末尾付近（メモ管理/スクリーンショット の後、全削除/閉じる の前）に新 `btn-overlay-toggle` を配置
- ID は同一のまま維持したため、`gb-annotations.js` の `toggleOverlayVisibility()` の DOM 参照変更は不要
- 削減効果: アプリバー 20 → 19 ボタン（−1）

## v0.5.83 (2026-04-10)

### Phase 2: アプリバー整理 — インポート/エクスポートを各エディタメニューに移動

`app/docs/topbar-cleanup-plan.md` の Phase 2 を実装。トップバーのインポート/エクスポートボタンを除去し、各エディタのツールメニュー（☰）に統合。

#### 不足項目の補填
- ノート（page）のツールメニューに「Markdownをコピー」を追加（旧 `showExportMenu` の page 限定項目）
- ボード（board）のツールメニューに「PNG画像として保存」を追加（`bdExportImage()` を呼び出し）

#### 旧 UI/コードの除去
- `Meldex.html`: トップバーから「インポート」「エクスポート」ボタンを除去
- `gb-app.js`: モバイルツールメニュー（`showMobileToolMenu`）から「インポート」「エクスポート」項目を除去
- `gb-outliner.js`: `showImportMenu()` / `showExportMenu()` 関数本体を削除（参照元なし）

#### テーマ読込/保存の扱い
- 既に `openColorSettings()` モーダル内に「テーマ保存」「テーマ読込」ボタンが存在するため、設定経由でアクセス可能
- トップバー除去で唯一の参照経路がなくなるため、設定モーダル経由のみとなる

#### 削減効果
- アプリバー 22 → 20 ボタン（−2）

## v0.5.82 (2026-04-10)

### Phase 1: アプリバー整理 — 戻る/進むをエクスプローラー専用に変更

`app/docs/topbar-cleanup-plan.md` の Phase 1 を実装。戻る/進むを全ツール横断のナビゲーションから、エクスプローラー（フォルダツリー / フォルダビュー）経由のフォルダ・ファイル移動履歴限定に変更。

#### コア改修
- `gb-app.js`: `navPush()` の責務を「パネルタブ自動更新」のみに分離。履歴記録は新設の `explorerHistoryPush()` に移動
- `gb-app.js`: `history.pushState()` / `popstate` リスナー / `history.back()` / `history.forward()` / `history.replaceState()` を全廃止（ブラウザ履歴 API への依存をゼロ化）
- `gb-app.js`: `navBack()` / `navForward()` をエクスプローラー履歴のみで動作するよう変更

#### open○○ 関数の `fromExplorer` 対応
- `openPage` (gb-editor.js)、`openFolder` (gb-folder.js)、`selectDatabase` / `selectEntity` (gb-database.js)、`openBoard` / `openMedia` / `openHtmlFile` / `openCalendarFile` (gb-app.js)、`openCsvFile` (gb-csv-viewer.js)、`openSmartDbFile` / `selectSmartDb` (gb-db-smart.js)、`openScenarioInScriptNote` (gb-tool-menu.js) に `openOpts.fromExplorer` を追加
- 各関数は `fromExplorer: true` のときのみ `explorerHistoryPush()` を呼び履歴に記録

#### エクスプローラー経路への `fromExplorer:true` 付与
- `gb-outliner.js`: ツリー単一クリック、検索結果ジャンプ、最近使った項目クリック、お気に入りクリック、新規ノード作成後のオープン、台本コンテキストメニュー
- `gb-folder.js`: フォルダビュー内のアイテムオープン、画像のキャンバス化、フォルダタグクリック
- `gb-app.js`: 起動時の last view 復元

#### UI 移動
- `Meldex.html`: `btn-back` / `btn-forward` をトップバー（`#top-bar` 内）から `#folder-view` ヘッダーに移動
- ボタンタイトルを「フォルダ履歴を戻る/進む (Alt+←/→)」に変更

#### ショートカット
- `gb-shortcuts.js`: `global.navBack` (`alt+arrowleft`) / `global.navForward` (`alt+arrowright`) を新規追加（ブラウザネイティブの戻る/進むに依存しない直接実装）
- `_shortcutHandlers` に navBack / navForward を登録
- `btn-back` / `btn-forward` のツールチップをエクスプローラー履歴文言に変更

#### 副作用 / 注意点
- マウスサイドボタンによる戻る/進むは無効化される（ブラウザ履歴 API に依存しないため）
- `showNavHistoryDropdown()` はそのままでエクスプローラー履歴のみ表示するようになる（`navHistory[]` がエクスプローラー専用化されたため）
- 削減効果: アプリバー 24 → 22 ボタン

## v0.5.81 (2026-04-10)

### シナリオエディタの完全削除（台本エディタへ移行）

旧シナリオエディタを完全に削除し、台本エディタ（gb-scriptnote-*）に一本化した。既存の `.scenario.json` ファイルは台本エディタへ自動変換して開く経路を残す。

#### 削除したファイル（9ファイル, 約15,500行）
- `gb-scenario-engine.js`, `gb-scenario-edit.js`, `gb-scenario-features.js`, `gb-scenario-rules.js`, `gb-scenario-rules-core.js`, `gb-scenario-note.js`, `gb-scenario-runtime.js`, `gb-tool-scenario.js`, `gb-scenario.css`

#### 共有コードの移設・抽出
- `switchDetailTab` / `_currentDetailTab` / `_normalizeDetailTab` を `gb-scenario-rules.js` から `gb-detail-panel.js` へ移設し、シナリオ専用タブ（rules/colors/scenario-notes/scenario-version）を validTabs および forEach から除去
- 台本エディタが依存していた `.gb-se-root` / `#se-toolbar` / `.title-input` のスタイルを `gb-scenario.css` から `gb-scriptnote-editor.css` へ抽出
- `gb-theme.css` から消費者のいないシナリオ用 CSS 変数（`--c-mekuri-*`, `--c-kaipage-*`, `--c-hakushi-*`, `--c-tobaki-*`, `--c-gion-*`, `--c-kokoro-*`, `--c-narration-*`, `--c-komagai-*`, `--c-meta-fg`, `--c-panel-*`, `--c-warn-*`, `--c-drop-indicator`, `--show-mekuri-border`, `--show-drop-indicator` 等）を削除

#### 参照箇所の除去（22ファイル）
- Meldex.html: link/script タグ8個、ツールバーボタン、`#scenario-view` コンテナ、シナリオ用 detail-tab マークアップを削除
- gb-app.js: `openScenario()` 関数本体、ビュー切替・D&D・XLSXインポート・モバイルメニュー等のシナリオ分岐を除去（XLSXインポートは台本作成へ移行）
- gb-shortcuts.js: `scenario.*` ショートカット定義13個＋ハンドラ15個、スコープマップ・ラベルを完全削除
- gb-settings.js: シナリオフォント設定UI、`getScenarioFontOptions()`, `SCENARIO_COLOR_KEY`, `syncScenarioColors()`, カスタムテーマ「シナリオ」セクション、テーマプリセット8種からシナリオ用 CSS 変数行32本を削除
- gb-pane-bridge.js: VIEW_IDS / validViewTypes / detailScrollSelectors / showScenarioTabs 呼び出し / ツールタブ「シナリオ」を除去
- gb-history.js: バージョンタブ・openScenario・スコープアイコン等の scenario 参照を除去
- gb-detail-panel.js: シナリオタブHTML、`_syncDetailPanel` のシナリオ分岐、`preserveScenarioTab` 判定を削除
- gb-outliner.js: 新規作成メニュー・エクスポート・リセント・D&D・検索結果のシナリオ分岐を除去（アイコン・ラベル・GF_TYPE_ORDER は既存ファイル表示用に維持）
- gb-tool-menu.js: シナリオビュー用エクスポートメニュー、新規リンクカード「シナリオ」、未使用 `getCurrentScenarioLikeFileFormat()` / `isSavedScriptNoteFile` を削除
- gb-annotations.js / gb-board-ui.js / gb-calendar.js / gb-component.js / gb-folder.js / gb-split.js / gb-canvas-features.js / gb-color-palette.js / gb-right-panel.js / meldex-core.js / gb-editor.js / gb-pane-context.js / gb-tool-components.js / gb-tool-scriptnote.js / gb-docking.js / gb-note-enhance.js: state.currentScenarioPath、`scenarioPath` プロパティ、`showScenarioTabs` 呼び出し、`view === 'scenario'` 分岐、ScenarioComponent 関連コメント等を除去
- gb-activity-bar-v2.js: 「新規シナリオ」ボタンとアクションを削除

#### Python サーバー (Meldex.py)
- シナリオ新規作成ブロック（`elif item_type == "scenario"` 約1500行）を削除
- ファイルタイプ判定・リネーム・検索結果の `scenario` タイプは既存ファイル互換のため維持

#### ビルドスクリプト
- `build.py` / `build_exe.py` のファイルリストから削除済み 9 ファイルを除去

#### 既存 `.scenario.json` ファイルの扱い
- フォルダツリー / 検索結果 / リセント / お気に入り / D&D 等あらゆる経路から、`.scenario.json` ファイルをクリック・ダブルクリックすると自動的に台本エディタへインポートして開く（`openScenarioInScriptNote` 経由）
- 注釈ジャンプ機能（`gb-annotations.js`）の `.json` 判定を `.scriptnote.json` / `.scenario.json` に厳密化
- 新規作成は不可（フォルダツリー追加メニュー・アクティビティバーから「シナリオ」ボタンを削除）

#### LLMチャット
- `gb-right-panel.js` のシステムプロンプトから旧拡張子 `.scenario.json` への言及を削除し、`.scriptnote.json` のみに統一
- マニュアル名を「シナリオエディタ マニュアル.md」→「台本エディタ マニュアル.md」に修正

#### ドキュメント
- ルート `CLAUDE.md`: ファイル構成、iframe 通信規約表、テスト手順から scenario-editor 関連を除去
- `app/CLAUDE.md`: 全ファイル全行チェックのバッチ定義から削除済みファイルを除外

## v0.5.80 (2026-04-10)

### カラーパレットのライブ適用化 + レイアウト再構成

#### カラーパレット
- スウォッチクリック・スライダー操作・カラーピッカー操作すべてで、即座に色が反映されるよう変更（従来は「適用」ボタン押下時のみ反映）
- 「適用」ボタンを「閉じる」ボタンに変更（色の反映は担わず、パレットを閉じる役割のみ）
- スライダーのラベルを `H/S/B` → `色相/彩度/明度` に変更（ラベル幅 12px → 28px）
- 不透明度（α）スライダーを削除
- カラーピッカーを開くスウォッチを 24×24 → 48×48 に拡大
- ピッカー＋「+保存」ボタンをフッターからスライダー上部に移動（`.gb-palette-picker-row`）
- 閉じるボタン行を独立化（`.gb-palette-close-row`）
- `_buildPaletteElement` のコールバックを `onApply` → `onChange`/`onClose` の2つに分離

#### テーマ設定（半透明色）
- `openCsPaletteRgba` をパレットの純hex出力前提に書き換え — パレット操作で外部αスライダーがリセットされる回帰を修正
- 透明スウォッチ選択時のみαを0に落とす挙動に整理

## v0.5.79 (2026-04-10)

### ボード詳細パネルのコンパクト化 + ツールメニュー機能追加

#### ボード
- 詳細パネルのスタイルサマリーからプレビュー表示を削除（カード・ライン共通）
- 詳細パネル全体をコンパクト化（フォントサイズ・余白・スウォッチサイズ縮小）
- フィールドレイアウトを縦並びから横並び（ラベル+入力）に変更

#### ツールメニュー
- DBツールメニューに「辞書ファイルを出力」を追加（IME辞書形式、ふりがな付きエントリを出力）
- 共通メニュー末尾に「パスをコピー」を追加

#### 台本エディタ
- destroy()でdocument.body上のフロートバー・一括編集ポップアップを除去（DOM残留リーク修正）

#### ドキュメント
- CLAUDE.md / GEMINI.md の旧名称（GelBoard / GelBoardHome）をMeldex / MeldexHomeにリネーム

## v0.5.78 (2026-04-10)

### 台本エディタ: 縦書きモード改善・複数選択・テーマ拡張

#### 縦書きモード修正
- ヘッダーの高さ（縦幅）を行と一致させる（ヘッダー高さ測定→全行に適用）
- テキスト折り返し時のセル幅拡張（scrollWidth検出）
- 折り返しモードでもヘッダー高さ同期・テキスト幅拡張を適用
- セル幅を横書き行高に合わせるパディング調整
- ハンドルを縦並び（チェックボックス上・グリップ回転）に変更
- 縦中横の幅圧縮なしモード（sn2-tcy-wide）をガターに適用
- ヘッダーセルに縦中横を適用
- 枠線表示（outline）の上辺（横書きの左辺）が消える問題を修正
- ヘッダーの列間境界線の色を視認性向上
- ヘッダーが垂直スクロールで固定される問題を修正
- 列リサイズ時のヘッダー→行高さ再同期

#### 複数選択・一括操作
- チェックボックスをトグル動作に修正（ラジオボタン動作を解消）
- ドラッグ矩形選択（閾値4px、Ctrl/Shift対応、段の隙間対応、選択矩形表示）
- フロートバーを上部表示、アクセントカラー左ボーダー、幅fit-content
- フロートバーに「複製」「選択反転」ボタンを追加
- ハンドル列ヘッダーに全選択/全解除ポップアップメニュー
- 選択開始領域クリックで選択解除

#### テーマタブ拡張
- 見開き区切り線: 二重線→実線に変更、色・太さをテーマタブで設定可能
- カスタムキャレット: 太い縦線（横書き）/横線（縦書き）、zoom補正、色・太さをテーマ設定可
- 選択矩形の色をテーマタブで設定可能（フチ=実色、内側=半透明）
- テキスト選択ハイライト色（::selection）をテーマタブで設定可能
- 枠線モード「すべて」で横書き時に縦線を表示、縦書き時はborder-bottomに変換

#### その他
- クリスタ送信: テキストの前後設定（「」等）を含めるか選択可能に
- ロールメニュー: フラットモードでEnterキー選択、背景色付き項目のホバーハイライト、幅縮小
- フィルタプリセット: 選択中のプリセット名をドロップダウンに表示
- PageUp/PageDownで折り返しOFF時にスクロール（縦書き対応）

### シナリオエディタ: 縦書きヘッダー高さ修正

- vtransposeの各行thにgetScenarioColumnWidthExpr()の値をheightとして設定
- テキスト行tdの高さをcalc(var(--text-chars) * 1em + 8px)に統一

## v0.5.77 (2026-04-10)

### 台本エディタ: ルビ位置・字間修正

- ルビのセンタリング方式を `top:50%/transform` から CSS auto margin 方式に変更（横書き・縦書き共通）
- 縦書きルビのテキストからの距離の符号が逆だった問題を修正
- ルビ字間の CSS zoom 二重スケーリング問題を修正（`getBoundingClientRect()` の値を `_getZoom()` で割ってCSS座標系に変換）
- ルビ字間の分布幅を対象文字列の90%に変更、`text-indent` でトレーリングスペースを均等化
- ルビ距離の初期値を 2px → 3.5px に変更

## v0.5.76 (2026-04-09)

### 台本エディタ: 多数の機能追加・改善

#### UI改善
- ロール管理タブのプレビューセルで透明色背景をページ背景色で表示
- テーマタブの基本テキストセクションに左右余白を追加
- ルビタブを表形式に変更、表示設定をルビ一覧の上に配置、サイズと距離を横並び
- ロール列ドロップダウンの種類分類を20個超の場合のみに変更（20以下はフラットリスト）
- ロール列ドロップダウンのactiveハイライトを左ボーダー+太字に変更
- ルビ対象文字列の破線下線を削除
- ステータスバーに主要なショートカットキーを表示

#### 新機能
- 詳細パネルに「行セット」タブを追加（行の組み合わせを指定回数分追加、プリセット保存）
- フィルタプリセット機能（ツールバーのドロップダウンからプリセット選択・登録・削除）
- 台本形式として保存でフォルダ選択モーダルに変更（サブフォルダ対応）
- ルビ設定ポップアップに「自動ルビルールにも追加」チェックボックスを追加

#### 操作改善
- Enter: ロールなしの空白行を追加、Ctrl+Enter: 同じロールの空白行を追加
- Shift+矢印キーで複数行（複数セル）にわたるテキスト選択が可能に
- 更新ボタンで詳細パネルのタブ状態を維持
- フラットリストモードでのキーボードナビゲーション対応

#### 縦書きモード
- 連続半角英数字を縦中横（tcy）で横組みブロック表示
- 折り返しオン時に両端の段を超えてスクロール可能に
- 縦書き時のテキスト見切れ・ヘッダー不一致の改善（未完了: 次セッションで対応）
- ルビ距離の調整 → v0.5.77 で対応済み

## v0.5.75 (2026-04-09)

### 台本エディタ: ツールバー更新・スクロール位置維持・フォーカス修正

- ツールバーに「更新」ボタンを追加（ファイルを再読み込み）
- 詳細パネルのロール管理タブから「適用」ボタンを削除（各操作で即時反映されるため不要）
- _render()でのDOM再構築時にスクロール位置を保存・復元（undo/redo、フィルタ時のCtrl+矢印等でスクロールが先頭に戻る問題を修正）
- フィルタ有効時のCtrl+矢印による行入れ替え後にカーソルが消える問題を修正（DOM再構築後にrowIdで新要素を検索）

## v0.5.74 (2026-04-09)

### 台本エディタ: 縦書きモード改善・操作拡張

#### 縦書きモード対応
- 半角英数字を正立表示（text-orientation: upright）
- 各行の幅をテキスト内容に合わせて縮小（横書き用min-width: 60pxを無効化）
- ルビを対象文字列の右側に配置（日本語組版標準）
- 枠線モード（all/horizontal/vertical/none）を縦書きに正しくマッピング
- heading行の太い枠線をborder-leftに変換
- ト書きインデントをpadding-topに変換
- ページ区切り・空行のmin-heightをmin-widthに変換
- ドロップインジケーターを左右の縦線に変換
- 列間枠線をborder-bottomに変換
- カスタム列セルのサイズ指定を高さ基準に変更
- テキスト位置シフト（前にズラす/後にズラす）をtranslateYで縦書き対応
- Ctrl+行入れ替えをCtrl+Right/Leftに変更
- _isAtBoundary()を左右方向の境界判定に対応
- _adjustRubySpacing()を高さ基準の字間計算に対応

#### 操作拡張
- Shift+Delete: 現在行を削除（最後の1行は保護、undo対応）
- Shift+矢印キー: セルを跨いだテキスト選択（境界到達時にselection.extendで拡張）
- Ctrl+Shift+V: セル内に改行を保持して貼り付け（行分割しない）
- 右クリックメニューに「セル内に貼り付け」を追加

#### 見開き区切り線
- テーマパネルに「見開き区切り」設定を追加（チェックボックス + 開始ページ + 間隔）
- 指定ページ番号の最後の行に二重線で区切りを表示（フィルタ時にも対応）
- 縦書き時はborder-leftに変換

#### その他修正
- プロット行の大区切り列にページ番号を表示
- セル内複数行でのカーソル上下移動が正しく動作するよう_isAtBoundary改善
  - rect取得失敗時のフォールバック（テキスト先頭/末尾判定）を追加

### ノートエディタ: リストネスト対応

#### Markdown→HTML変換（mdToHtml）
- インデント深度に応じたネストされた`<ul>`/`<ol>`構造を生成
- adjustListDepth()でスタックベースの深度管理を導入（深度変化に応じて自動開閉）

#### HTML→Markdown変換（htmlToMd）
- LI要素の親UL/OLのネスト深度を辿り、正しいインデントを付与
- LI内のテキスト部分とネストされたリスト部分を分離して変換
- walk()のchildren事前計算からLIを除外（二重走査防止）

## v0.5.73 (2026-04-09)

### 台本エディタ: ルビ表示の修正・改善

#### バグ修正
- ルビが二重に表示される問題を修正（gb-tools.cssのグローバル`::before`ルビと台本エディタの`::after`ルビが同時適用されていた。`.sn2-text`内で`::before`を`content:none`で無効化）

#### ルビ表示設定
- ルビタブに「サイズ」（em単位）と「距離」（px単位）の設定UIを追加
- 設定はファイルごとに保存（`editor.rubyFontSize`, `editor.rubyOffset`）
- CSS変数 `--sn2-ruby-size`, `--sn2-ruby-offset` でリアルタイム反映

#### ルビ字間調整（自動）
- ルビが対象文字列の幅に収まる場合、字間を広げて幅内に分布表示（両端に7.5%ずつ余白を確保）
- ルビが収まらない場合は中央揃えで両側にはみ出し表示
- `_adjustRubySpacing()` メソッドで描画後に自動計算、ルビサイズ変更時も即時再計算

## v0.5.72 (2026-04-09)

### 台本エディタ: グローバルundo/redo・ヒストリー統合

#### undo/redoシステム統合
- 台本エディタの内部undo/redoをグローバルヒストリーシステム（historyPush/historyUndo/historyRedo）に統合
- スコープ `scriptnote:フルパス` でヒストリーパネルに操作履歴が表示される
- Ctrl+Z / Ctrl+Y（Ctrl+Shift+Z）がグローバルシステム経由で動作
- ヒストリーパネルからのクリックundo/redo対応
- エディタレジストリによるマルチインスタンス安全性（同名ファイル衝突防止）

#### スナップショット拡張
- rows のみ → rows + characters + editor + rubyRules + title + layoutMode に拡張
- undo/redoで詳細パネル（ロール管理/テーマ/ルビ）も自動再描画

#### 全操作のundo対応（最深部まで）
- **ロール管理パネル**: 追加/複製/削除/名前変更/種別変更/色変更/書式変更/配置変更/サイズ変更/テキスト前後/リセット/一括変更/一括リセット/並び替え(D&D)/オプション設定(枠線/テキスト位置)/DBインポート/適用ボタン
- **テーマパネル**: 基本テキスト(色/太字/斜体/サイズ)/枠線(モード/色/幅)/余白/まとめ表示/ページ背景色/ホバー背景色/ドロップインジケーター(色/幅)/ト書きインデント/カウント設定(大区切り/小区切り/書式)/列間枠線/自動配色
- **ルビパネル**: ルール追加/テキスト編集/ルビ編集/削除/並び替え(D&D)
- **列管理**: 列幅変更/列名変更/配置変更/表示切替/列スタイル(色/書式/サイズ/リセット/ロール優先)/列追加/複製/削除/選択肢編集/単位設定/列順序D&D/列値変更(number/select/text)
- **ツールバー**: 表示方向(横/縦)/折り返し切替/レイアウト変更/テンプレート適用/タイトル変更
- **エディタ本体**: テキスト編集/行分割/行結合/行入れ替え/行移動D&D/複数行ペースト/一括ロール変更/一括行削除/ルビ設定

#### ヒストリーシステム拡張（gb-history.js）
- scriptnote:スコープの動的redo/undoキャプチャ（scenario:と同じパターン）
- スコープ表示名のヘルパー関数追加（フルパスからファイル名のみ抽出）
- scriptnoteスコープアイコン追加

## v0.5.71 (2026-04-09)

### 台本エディタ: スタイル整合性・UI改善・行選択

#### ロール管理タブ（詳細パネル）
- ロール列セルにホバー時「…」ボタン追加（他列と同じ書式ポップアップを表示）
- セル書式ポップアップとヘッダー一括ポップアップに「リセット」ボタン追加
- プレビューとエディタのスタイル整合性修正（_text列のみcharaフォールバック、他列は独立）
- エディタ側: textBefore/After/Align等をtextStyleから優先読み込み（??で空文字リセット対応）
- 透明色スウォッチを市松模様で表示（透明設定の視認性向上）
- チェックボックス・ハンドル列を中央揃え
- 「全選択」ボタンを常時表示
- 新規ロール追加を表の先頭に配置
- 自動配色: 設定変更を全キャラに適用（autoColor未設定キャラもautoColorTarget更新）

#### メインパネル
- テキスト前後（「」）がdata-valign設定時に改行される問題を修正（CSS align-content対応）
- セル背景未設定時にページ背景色にフォールバック（hover/focusのグレー透け防止）
- テキスト位置: marginRight+textIndent → marginLeft方式に変更（右端の突起解消）
- 非表示セルの背景も透明化（背景色だけ残る問題修正）
- ドロップインジケーターをbox-shadow → ::after疑似要素に変更（セル背景に隠れない）
- 折り返し時の矢印キーで段を跨いで行移動可能に
- PageUp/PageDownで前後の段に移動する機能追加
- 折り返しモードのヘッダーポップアップメニュー表示修正
- 行チェックボックス追加（ハンドル左、ホバー時表示）
- 行選択をrowIdベースに変更（並び替え後のずれ防止）
- フロート一括編集バー（画面下固定、リサイズ追従）

#### カラーパレット
- カラーピッカーのchangeイベント対応（保存せずに適用可能に）

## v0.5.70 (2026-04-09)

### 全体チェック修正

#### 高優先度修正
- gb-database.js: ピン留め列のpLeftOffsetをエントリ列幅に動的対応（列幅変更時のずれ修正）
- gb-canvas-engine.js: bdToMd()にバルーン情報の書き出しを追加（保存時消失防止）
- gb-db-rollup.js: 同一DBリレーション（空文字列）がロールアップ・グラフで無視される問題を修正
- gb-tool-menu.js: 「別名で保存」のフォルダツリーが展開できない問題を修正（APIレスポンス形式対応）
- gb-shortcuts.js: Ctrl+Tab/Ctrl+Shift+TabをGBTabs/GBLayout v5.0ペインシステムに対応
- gb-stamp.js: スタンプ判定の正規表現lastIndex問題を修正

#### 中優先度修正
- gb-right-panel.js: チャット検索をTreeWalker+1パスに改善（短い検索語での フリーズ防止）
- gb-settings.js: モバイル時の設定モーダルでdisplay:none/flex競合を修正
- gb-calendar.js: シフト表のインラインイベントハンドラでパス特殊文字をエスケープ
- gb-tool-calendar-views.js: イベント/タスク/シフト削除に確認ダイアログ追加
- gb-db-dashboard.js: ダッシュボード/ウィジェット削除に確認ダイアログ追加
- gb-db-smart.js: スマートDB削除に確認ダイアログ追加
- gb-canvas-features.js: 空白エリア右クリックメニューの座標計算をbdScreenToWorldに統一（回転時の位置ずれ修正）
- gb-canvas-interact.js: ラインツール/右ドラッグのプレビュー座標をbdScreenToWorldに統一
- gb-db-views.js: タイムライン依存矢印のscrollリスナー蓄積を防止

## v0.5.69 (2026-04-09)

### 台本エディタ: フィルタ改善・ロール設定表化・テーマ基本設定・undo拡充

#### フィルター
- Alt+クリックでクリックしたロール以外を全オフにする機能を追加
- 1ロールのみ表示中にEnterで行追加すると、そのロールが自動設定されるように

#### ロール設定（詳細パネル）
- ロール一覧を列×ロールの表レイアウトに再構成（各列のプレビュー表示）
- セルクリックで個別の色・書式設定ポップアップ（bg/text色、B/I、フォントサイズ）
- 列ヘッダーから選択ロールへの一括設定に対応

#### テーマ
- 基本テキスト色・太字/斜体・フォントサイズ設定を追加（CSS変数でテーブルに反映）
- ファイル読込時にCSS変数を自動復元

#### undo/redo
- 列スタイル変更・列幅変更にundo対応を追加

#### テキスト位置
- テキスト位置シフト先の既存値を非表示に（前回コミットで実装済み）

## v0.5.68 (2026-04-09)

### シナリオエディタ: undo/redo全操作対応・カラーパレット全面改修・UI改善

#### undo/redo対応
- スナップショットにsettings・ランタイム変数を追加し、全設定変更がundoで完全に復元されるように
- withUndoGroup()でモード切替等の複合操作を1つのundoエントリにまとめる仕組みを導入
- 入力設定タブ: キャラD&D・条件付き書式全操作・行タイプ/特殊キャラD&DにpushUndo追加
- 色設定タブ: 個別色変更・テーマインポート/リセット・線表示切替にpushUndo追加
- ツールバー設定: 表示モード・テキスト幅・行間・字間・フォント・折り返し・枠線にpushUndo追加

#### カラーパレット (v6.0)
- クリックで即適用→パレット内選択のみに変更、「適用」ボタンで確定
- H/S/B/Alphaスライダー追加（色の微調整が可能に）
- カスタムカラーのD&D並べ替え・右クリック削除対応
- 「+保存」で選択中のカスタム色上書き対応
- プリセット先頭行を透明・白・黒・グレー段階に変更

#### フィルター機能
- Alt+クリックでクリックしたキャラ以外を全オフにする機能を追加
- 1ロールのみ表示中に行追加すると、そのロールが自動設定されるように

#### ロール設定タブ
- キャラ設定を列×ロールの表レイアウトに再構成（各列のプレビュー表示）
- セルにカーソルを合わせて個別の色・書式設定が可能（ポップアップUI）
- ヘッダーから列ごとの一括設定に対応

#### テーマタブ
- 基本テキスト色・太字/斜体・フォントサイズ設定を追加（CSS変数でテーブルに反映）

#### スクリプトノート
- テキスト位置で隣接セルの既存値がシフト先と重複する場合、非表示にする対応

#### 自動配色
- autoColorTarget全パターン（bg/text/none + 切替 + 色ピック + レンダリング）を徹底チェック。重大バグなし

## v0.5.67 (2026-04-08)

### CSS zoom二重スケーリング問題の修正

UIスケール（`document.documentElement.style.zoom`）適用時に、全ポップアップ・ツールチップ・ドロップインジケーターの表示位置がズレる問題を修正。

- **根本原因**: `getBoundingClientRect()` / `e.clientX/Y` はビューポートピクセルを返すが、`style.left/top` はCSS座標系（zoom適用後）で解釈されるため、zoom倍率分だけ位置がズレていた
- `positionPopup()` にzoom補正を追加（9箇所の呼び出し元すべてに反映）
- `clampPopupToViewport()` 呼び出し元の初期位置にzoom補正を追加
- ドロップインジケーター（`gb-docking.js`）にzoom補正を追加
- カスタムツールチップ（`gb-events.js`）にzoom補正を追加
- 全27ファイル、約50箇所のポップアップ位置パターンを修正
- AGENTS.md のポップアップルールを更新（zoom補正パターンを明記）

## v0.5.66 (2026-04-08)

### 多機能バグ修正・機能追加

#### カレンダー
- フォルダツリーからカレンダーを開いた時にDBテーブルが表示されるバグ修正（ビューモードを強制的にtimelineに設定）
- イベントカードの上下端ドラッグリサイズを15分刻みに変更（従来: 自由サイズ）

#### データベース
- ツールバーのDB名をダブルクリックでインライン編集・リネーム可能に
- エントリのソート機能追加（名前昇順/降順、マニュアル、プロパティ別）
- エントリをD&Dで並べ替え可能に（自動的にマニュアルソートに切り替え）
- 「列の表示/非表示」を「プロパティ管理」に改名、D&Dで列順序変更に対応
- エントリ名列の「...」メニューに「エントリを追加」「ソート」を追加

#### ビューワー
- 画像切替時の白フラッシュ修正（画像ロード完了後にレイヤー切り替え）
- 「フィット」モードを長辺フィットに変更（パネルに収まりつつ画像全体を表示）

#### ボード
- 詳細パネルのプレビューセクションを削除
- 詳細パネル項目をパネル幅に応じた自動複数列レイアウトに変更
- カードスタイル選択の横幅を倍に拡大（70px→140px）
- 八角形カードを四隅カット型に変更（clip-path調整）
- clip-path使用形状（八角形・六角形・星等）の選択ハイライトをfilter drop-shadowで対応
- 雲形カードをより雲らしいclip-pathに変更

#### フォルダツリー
- 展開済みフォルダのクリックで折りたたみ可能に

#### パネル
- 非アクティブパネル内の項目クリック時にアクティブ移動と項目選択が同時に動作するように修正

## v0.5.65 (2026-04-08)

### 台本エディタ v2 — 次期開発（ファイル分割 + 16機能追加）

#### アーキテクチャ
- gb-scriptnote-editor.js (2004行) を4ファイルに分割
  - gb-scriptnote-editor.js (本体 ~1100行)
  - gb-scriptnote-detail.js (詳細パネル ~680行)
  - gb-scriptnote-menu.js (ロール選択メニュー ~225行)
  - gb-scriptnote-columns.js (列管理・ヘッダーメニュー ~480行)
- 分割方法: Object.assign(ScriptNoteEditor.prototype, { ... }) によるprototype拡張

#### ヘッダー機能
- ダブルクリックで列名をインライン編集（標準列はcolumnLabels、カスタム列はlabelに保存）
- 「...」ホバーメニュー: 水平配置/垂直配置/テキスト折り返し/列操作
- ヘッダーメニューから列追加（右に）/複製/削除
- ドロップダウン列の選択肢編集モーダル
- 数値列の単位設定ポップアップ（セル内にunit表示）
- ヘッダーは常にsticky表示（折り返しモードでも段内で固定）

#### 標準列の管理
- 番号/ロール/テキスト列の個別表示/非表示（visibleStandardColumns）
- 最後の標準列は削除不可ガード付き
- ヘッダーメニューから非表示列の復元

#### 編集機能
- Ctrl+上下キーで行入れ替え（フィルタで非表示の行をスキップ）
- Ctrl+Rでルビ入力（{漢字|ルビ}形式、DOM往復変換対応、自動ルビボタン付き）
- ロール列「なし」は空表示に変更

#### フィルタ
- ツールバーにフィルタボタン追加
- ロール種別でチェックボックス選択
- フィルタ適用中はボタンがactive表示

#### 詳細パネル（ロール管理）
- 「適用」ボタン追加（未登録ロールの同期 + 全行スタイル再適用）
- 一括処理ボタン（背景色/文字色）は複数選択時のみ表示
- カラースウォッチは共通カラーパレット（openColorPalette）を使用
- ト書きインデント深さの設定追加（CSS変数 --sn2-action-indent）
- 大区切り・小区切りの書式にフォントサイズ設定追加
- 単位入力欄の幅を2文字分拡大（32px→56px）
- 書式ボタン・テキスト書式アイコンをLucide typeOutlineに変更

#### UI改善
- ツールバー: 「縦」→「縦書き」、「横」→「横書き」に改名
- 詳細ボタン: toggleではなくopen動作に修正（真っ黒バグ修正）
- 新規作成時の背景色をテーマの背景色（--bg）に
- ヘッダーと行の右端ズレ修正（box-sizing統一）
- 列追加ダイアログのサイズ拡大（480px幅、min-height:300px）
- ポップアップの位置ズレ対策（contain: none）
- 配置設定のCSS（data-align/valign/overflow属性）

## v0.5.64 (2026-04-08)

### 台本エディタ v2 — 軽量段落エディタに全面再設計

#### アーキテクチャ
- シナリオエンジン（表計算型）への依存を完全に除去し、Word方式の段落ベースエディタに再設計
- 全行が常時contenteditable（クリックでDOM再構築しない）
- 1行あたりのDOM要素: 20個→3個（ハンドル+ロール+テキスト）

#### 基本編集
- Enter: 行分割、Shift+Enter: セル内改行（`<br>`明示挿入）
- ArrowUp/Down: セル間移動（単一行は常に移動、複数行は境界判定）
- Tab: ロールメニューを開く
- Backspace/Delete: 行結合
- Undo/Redo: Ctrl+Z/Y（スナップショットベース、500msデバウンス）

#### 行のドラッグ&ドロップ
- Notionスタイルのドラッグハンドル（ホバーで表示）
- ハンドルからのドラッグのみ有効、ドロップ位置にインジケーター表示

#### ロールメニュー（ドロップダウン）
- カテゴリベースのサブメニュー構成（キャラ/ト書き/見出し/区切り/なし）
- サブメニューは隣にポップアップ表示
- キーボードナビゲーション対応（ArrowUp/Down/Enter/Escape/Tab）
- Delete/Backspaceでロールを「なし」に

#### ページ/コマ番号
- モード別カウント定義（manga/drama/afureko/stage）
- 大区切り/小区切りそれぞれに単位・桁数・位置（単位+数/数+単位/前+数+後）を設定可能
- 0桁で単位テキストのみ表示
- カウント書式（背景色/文字色/太字）設定

#### ロール管理（詳細パネル）
- 台本上の全ロールを自動登録
- ドラッグ&ドロップで並べ替え
- 追加/複製/削除（ヘッダーのボタンで選択対象に実行）
- チェックボックスによる複数選択（ホバー表示、Shift+クリックで範囲選択）
- 一括操作（背景色/テキスト色）
- キャラ個別スタイル（背景色/テキスト色/太字/斜体）
- テキスト列書式のオプション設定（ポップアップ）
- テキスト前後文字列オプション（CSS ::before/::after で表示）
- ロール種別（台詞/ト書き/見出し/区切り/プロット）
- DBからキャラ読み込みモーダル

#### 表示設定
- 枠線モード（すべて/横線のみ/縦線のみ/ロール列間のみ/非表示）
- 枠線の色・太さカスタマイズ（CSS変数で統一制御）
- 列間枠線（番号|ロール間/ロール|テキスト間/全列間）
- 見出し枠線囲みオプション
- まとめ表示（前行と同じガター値/ロール値を非表示）
- ト書きのインデント

#### 縦書き・折り返し
- 横書き/縦書き切替（ツールバーボタン）
- 折り返しON: flexbox wrapで段組み（段幅=ヘッダー幅、隙間なし）
- 縦書き: テキスト/ガター/ロールにwriting-mode:vertical-rl適用
- 縦書きヘッダー: 右側縦並びに配置

#### カスタム列
- ツールバーから列追加（テキスト/数値/ドロップダウン）
- ヘッダーに全列のリサイズハンドル（ドラッグで幅調整）
- テキスト列も固定幅に変更可能
- 列幅をCSS変数で制御、doc.editor.columnWidthsに保存

#### 台本ファイル読込修正
- navPushのpane-bridgeオーバーライドによるコンポーネント破壊を防止
- フォルダツリーから台本ファイルを直接開く機能

---

## v0.5.63 (2026-04-08)

### 台本エディタ読込修正

- **台本ファイルの読込失敗を修正**: 台本を開く・新規作成・シナリオからインポートのすべての経路で、ファイル読込後に「台本ファイル未読込」のまま表示されない不具合を解消
  - 原因: `navPush` のpane-bridgeオーバーライドが、台本コンポーネント自身の `navPush` 呼び出しに対してもコンポーネントを破壊・再生成していた
  - `navPush` オーバーライドにコンポーネント型タブの自己ナビゲーション検知ガードを追加（calendar/search/scriptnote共通）
  - `loadIntoTab` を確実なフォールバックとして機能させ、既存タブ再利用時のstate上書き問題にも対応
- **フォルダツリーから台本ファイルを直接開く機能を追加**: 新規作成時にフォルダツリーの台本ノードをクリックして台本エディタで開けるように改善

---

## v0.5.62 (2026-04-06)

### ルール設定拡張・列順対応・表示不具合修正

- **行ドラッグ表示を修正**: 上方向へ移動中にドロップ位置の行が横にずれて見える不具合を解消
- **本文編集中の高さ自動調整を追加**: 改行に合わせて入力欄の高さをリアルタイム更新し、12行分で上限を設定
- **モード切替確認を廃止**: 執筆モード切替時の「現在の設定を登録するか」確認ダイアログを削除
- **選択セル設定を追加**: セル単位で水平方向・垂直方向の配置と、はみ出す/折り返す/切り詰める表示形式を一括設定可能に改善
- **列設定を強化**: 列ラベル・表示/非表示・縦位置に加えて、ドラッグで表示順を並べ替え可能に改善
- **特殊キャラロールを拡張**: 「左セルにはみ出し表示」を追加し、現在の列順に合わせて1列左の可視セルへ本文を表示可能に改善
- **透明色の再編集を修正**: 特殊キャラ設定・行タイプ設定で透明を選んだ後も別色へ変更できるよう修正
- **設定表記を整理**: キャラ設定/特殊キャラ設定/行タイプ設定へ名称を統一し、リセットはモード既定へ戻す動作に整理
- **列順変更との整合性を補強**: 編集対象セルの解決、コピー/貼り付け、選択セル書式が並べ替え後の表示順でも崩れないよう修正

---

## v0.5.61 (2026-03-24)

### 完成版 — UI設定・フォント管理・テーマ・モード別設定

#### UI色設定システム
- **全セルタイプの装飾設定**: 文字色・太字・斜体・背景色をUI色設定ダイアログから個別に設定可能
- **CSS変数ベース**: 57個のCSS変数で全色・太字・斜体・線幅を管理
- **カラーパレットポップアップ**: スウォッチクリックで24色プリセット+カスタムカラーから選択
- **カスタムカラー保存**: カラーピッカーで作成した色をパレットに追加（localStorage永続化、右クリックで削除）
- **線の表示/非表示切替**: 枠線・見開き区切り線のオン/オフトグル
- **線の太さ設定**: 枠線・見開き区切り線・ドロップインジケーターの太さをpx単位で設定
- **警告色カスタマイズ**: めくり/改ページ交互違反の警告色（デフォルト赤）を設定可能
- **カスタムテーマ**: 全UI設定をJSONファイルに保存/読込（テーマ保存/テーマ読込）

#### フォント管理
- **12種のプリセットフォント**: BIZ UDGothic, Yu Gothic, Noto Sans JP, Zen Maru Gothic, Klee One 等
- **Google Fonts自動読込**: 選択時にCDNから自動ロード
- **カスタムフォント追加**: フォント名・font-family・Google Fonts名を指定して追加
- **フォントプレビュー**: フォント管理ダイアログはオーバーレイ非表示で背景がリアルタイムプレビュー

#### モード別設定
- **横書き/縦書きで独立した設定**: 幅・P幅・行間・字間・フォントがモードごとに保存
- **モード切替時に自動反映**: ツールバーの入力欄・フォント選択がモードに応じて更新

#### 縦書きモード改善
- **全セル縦書き**: ヘッダー含む全セルにwriting-mode: vertical-rl適用
- **縦中横**: 半角英数を自動検出して横書き表示（text-combine-upright）
- **font-feature-settings: "vert"**: 縦書き専用グリフ有効化
- **見開き区切り線**: 全行（ページ設定・頁・コマ・キャラ名・テキスト）に表示
- **ドロップインジケーター**: 縦書き/プロットモード両方で正しく表示
- **キャラ名背景色**: tdセル全体に背景色適用（横書きと統一）
- **ドロップダウン左方向展開**: 縦書きサイドバーでの操作性向上

#### その他の改善
- **ボタン順序**: プロット→折返し→横書き→縦書き
- **モード切替時のスクロール**: 選択セルが表示されるように自動スクロール
- **縦書き折り返しオフ**: 初期表示が右端（先頭行）になるように修正
- **ステータスバーセパレーター**: 全角「 ｜ 」で区切り（JS動的挿入）
- **マウスホイール**: 折り返しオフ時、横書きは縦スクロール、縦書きは左スクロール

---

## v0.5.60 (2026-03-24)

### 縦書きモード完全リライト — `e835f0c`

- 転置テーブル方式で再実装（行=フィールド、列=データ行、右→左配置）
- ヘッダー含む全セル縦書き化
- 横書きと同じCSSクラスを共有（色ずれ完全解消）
- キーボード操作を論理方向変換で横縦統一
- 全セル中央揃え（テキスト列のみ左寄せ）

---

## v0.5.59 (2026-03-24)

### 大幅強化 — `ae46437`

- SEP内蔵（WebSocket連携）
- 段組みレイアウト（横書きでも段組み、実測ベース段分割）
- プロットモード改善（非表示行列スキップ、ページ単位操作）
- キャラ設定サイドバー化（24色パレット、並替え、名前編集）
- ドラッグ操作刷新（列ごとに移動単位変更）
- 複数行選択、セル単位ナビゲーション、自動保存

---

## v0.5.58 (2026-03-24)

### 初回リリース — `2fcd853`

ブラウザベースのマンガシナリオ執筆ツール。
横書き/縦書き切替、段組みレイアウト、ページ/コマ自動計算、条件付き書式、
プロットモード、キャラ管理、メモ、バージョン管理、JSON/xlsx/HTML入出力、
CLIP STUDIO PAINT連携（SEP内蔵）。

---

## 今後の実装予定

- 列の表示/非表示のカスタマイズUI
- カスタム列の追加機能
- 媒体プリセット（マンガ/小説/脚本で列構成を一括切替）
- 文字数カウント（リアルタイム表示）
- シーンヘッダー（脚本用: INT/EXT、場所、時間帯）
