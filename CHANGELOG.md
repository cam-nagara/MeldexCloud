
# シナリオエディタ チェンジログ

[過去の履歴（v0.7.348〜v0.5.58）](CHANGELOG-archive-v0.5.58-v0.7.348.md)

## Unreleased

## v0.7.366 (2026-09-04)

### RenderListを共有フォルダ経由で直接連携できるようにしました

- ownerだけが設定できるRenderList connectorを追加しました。pairing secretはprofile単位のOS資格情報保管へ保存し、平文設定やLAN待受portを作らず、選択したSMB/NAS上のbridgeとAnalytics Libraryだけを使います
- RenderListのPCを仮想スタッフ、render taskをタスクと固定時間の予定へ冪等投影します。予定の移動はoptimistic revision付きcommandとしてRenderListへ返し、予定時間のresize、source-managed予定の通常編集、Undoによる複製を禁止します
- Analytics Libraryのhash・size・path・展開量・record identityを検証し、月別分析sheetへ増分投影します。Meldexのメモとタグはworkspace別overlayとしてrevision管理し、RenderListの正本recordを変更しません
- HMAC署名、message IDとfile名の一致、共通entity revision、receipt、期限、stale lock回収、quarantineを実装しました。署名済みでも意味的に不正なpayloadは投影せず、rejected receiptを返します

検証: RenderList connector／staff／calendar／履歴の対象40件中39件に合格し、現行HEADと矛盾する旧Timer期待1件だけを明示除外しました。版情報2件、Python分割bundleと対象JavaScriptの構文も確認しました。実SMB/NAS複数PC、実Windows資格情報保管、配布版、Cloud公開は実施していません。

### Windowsのフォルダ／ファイル選択を操作中に打ち切らないようにしました

- ホームフォルダの「変更」でWindowsのフォルダ選択を開き、15秒以上かけて移動してから選択すると、成功した選択結果より先に画面側が通信を打ち切り、手入力ダイアログへ移ってしまう問題を修正しました
- フォルダ選択はサーバー側の120秒待機より長い135秒、ファイル選択は300秒待機より長い315秒まで画面側も待つ共通契約へ揃えました。ホームフォルダ変更はソースフォルダ追加APIを流用せず、現在のホームを初期位置にした専用フォルダ選択APIを使います
- OSダイアログ自体が規定時間を超えた場合は、キャンセルと誤認せず504のタイムアウトとして扱います。対象の全フォルダ／ファイル選択入口と、ホームフォルダの保存までを回帰テストで固定しました

検証: ネイティブダイアログ契約6件、関連API／画面回帰52件、Chromeでホームフォルダ選択・保存を通すmicro E2E 1件に合格しました。実Windowsダイアログで15秒以上操作する確認はDebuggerの実機テストへ登録します。実運用フォルダ、実Dropbox、Windows配布版は変更していません。

### 管理者PCのCLI返答をユーザー単位で制御できるようにしました

- 共有ワークスペースの各ユーザーに「管理者PCのCLI返答を許可」を追加しました。所有者だけが変更でき、新規メンバーと旧データで値が無い場合は拒否として扱います
- 許可状態を共有メタデータと所有者署名済み端末台帳の両方で検証し、登録、依頼受付、キュー処理、CLI起動直前の各段階で再確認します。途中で権限を取り消した場合もCLIを起動しません
- 許可の保存に失敗した場合は有効化せず、拒否時は署名台帳を先に閉じるfail-closedの更新順序にしました。設定画面とマニュアルには、信頼できるユーザーだけを許可する注意を追加しました

検証: 共有ワークスペース、署名済み登録／依頼、relay、使用量集計、設定画面の変更済み5スイート154件に合格しました。実アカウント、実Dropbox共有、管理者PCでの実CLI起動は行っていません。

## v0.7.365 (2026-08-29)

### 公開検証を致命的安全ゲートと任意台数分散へ再構成しました

- Release Validationの公開停止条件を、データ損失・保存破損、安全でない同期／上書き、認証・秘密情報漏洩、起動不能・強制終了へ限定しました。該当するunittestとブラウザ操作を先に実行し、失敗時は残るPCの処理も早期停止します
- full unittestと高リスクE2Eは同じSHAの全件診断として1回だけ収集し、非致命的な失敗・既知失敗・retry-passは公開記録へ警告として残します。テスト欠落、不完全実行、dirty source、SHA／カタログ不一致、証拠破損は引き続き公開を停止します
- 自前runnerを3台固定からfleet定義の任意台数へ変更し、PCごとのunittest／E2E性能と過去の実測時間を使って負荷を均等化しました。RTX 5090搭載PCを含む現在の3台を登録し、PCを追加しても同じ分割処理で全台を同時利用します
- 同一PC内の書換えテストと対話型Chromeは直列に保ちながらPC間では最大並列化し、失敗後の根拠のない再実行を無効化しました。課金対象のGitHub-hosted runnerとActions artifactを使わず、各nodeのハッシュ済み証拠をjobログから集約します

検証: 分散・fleet・致命ゲート・証拠縮約・workflow契約の対象73件、保存・復元・競合・起動・秘密情報を扱う致命unittest 149件に合格しました。隔離Chromeで保存失敗、復元、競合、読取ロック、タイムアウト時の未保存保持、再読込を含む致命E2E 16件も失敗・retry-passなしで合格しました。実Dropbox、実作品、Windows配布版、Cloud公開はこの変更では扱っていません。

### 公開前の横断検証で現行操作の回帰を修正しました

- テーマ設定を変更した時に詳細パネルを含む表示先へ即時反映し、タグタブの選択下線、エントリ詳細の「チャットを作成」、モバイル検索欄のEscape閉じを現行UIへ復元しました
- 常駐付箋のメニューと閉じる操作をホバー前から利用できるようにし、シナリオの列追加ダイアログを閉じた後は元の列ヘッダーへフォーカスが戻るようにしました
- 3台の自前runnerで公開検証できるよう、退役済みTimerや旧プロパティ画面を参照していたE2Eを現行のタスクリスト、アノテート、設定、ボード、チャット、シート仕様へ揃えました
- 補助ペインのリンクプロパティ、アノテートメニューのフォーカス復帰、ノートの目次・コールアウト、Viewerの旧アノテート変換を安定化し、Windows間で改行設定が異なっても公開マニュアルの資産revisionが一致するようにしました
- 旧 `.env` に残るNotionトークンは、OS資格情報庫への保存と読戻しが成功した後だけ移行元から削除するようにし、認証情報を失わず平文保存を解消できるようにしました
- モバイルの再描画直後でもアイディア一覧の再試行操作を取り違えず、古い一覧要求の完了が新しい要求の進行表示へ干渉しないようにしました。短時間に閉じて開き直した場合も古い画面の処理を現在の進行中件数へ混ぜません。入れ子ダイアログを閉じた後は親ダイアログ内の操作位置へフォーカスを戻すようにしました。SQLiteシートは既にWAL方式なら接続ごとの再設定を避け、別処理の通常書込み中でもエントリを読み込めるようにしました
- シナリオの列追加ダイアログは、閉じるボタン後のブラウザ既定動作や列ヘッダー再描画が重なっても元の列へフォーカスを戻し、利用者が別の操作を始めた場合はその操作を優先するようにしました
- 同一シートのSQLiteを複数要求が初めて開く場合は、WAL切替とスキーマ初期化だけをシート単位で直列化し、Windows上の一時的な `database is locked` で読み込みが失敗しないようにしました

### 開発版が空白ウィンドウのまま起動できない問題を修正しました

- `start-editor.bat` の隔離ランチャーがWindows版Python 3.13でアクセス違反になる問題を修正し、隔離環境と絶対パスを引き継いだ子プロセスとしてMeldex本体を起動するようにしました。本体の終了コードも起動バッチへ返します
- 旧起動方式の `python -u Meldex.py` が親プロセスを失ったまま8001番ポートを占有した場合も、Pythonプロセス・相対エントリポイント・Meldex既知ポートのlisten状態をすべて満たす対象だけを終了できるようにしました。この旧プロセス回収では、他のPythonプロセスや別パスのMeldexを対象にしません
- 外部通信を遮断する隔離開発版では、ベクトル索引用モデルのダウンロードを起動時に繰り返し試行しないようにしました。通常版のベクトル索引設定は変更していません
- 隔離された新規ワークスペースなど、管理者AI用の所有者公開鍵pinがまだない状態で稼働確認を行っても、未処理のサーバー例外を繰り返さないようにしました。未検証の稼働情報は従来どおり採用せず、未設定状態として扱います

検証: Windows上の旧プロセス残留状態でポート競合と空白ウィンドウを再現し、修正後に同じ `start-editor.bat` から起動しました。`/api/version` とルートのHTTP 200、Chromeアプリのタイトル、フォルダツリー・カレンダー・プロパティの実描画を確認しました。隔離ランチャー、終了対象の限定、ソース版／配布版プロセス終了、所有者公開鍵pin未設定時のfail-closed応答の対象テストに合格しています。実作品、実運用Dropbox、Cloud公開、Windows配布版は変更・検証していません。

### 残っていた横断計画を完了し、検索・補助パネル・Timerを整理しました

- チャットへクリップボード画像を貼り付ける既存経路を再確認し、添付アップロードの成功件数を正しく扱うようにしました。オプション、タグ、バックリンク、アノテート、テーマ、バージョン管理、ヒストリー等の対象依存パネルには、現在参照しているファイル・フォルダ・複数選択・全件スコープが分かる共通ヘッダーを追加しました
- Timer機能を本体、右レール、スケジュール導線、Windows単独アプリ、Cloud PWA、配布ページ、CI、マニュアルから退役させました。既存の `.mel-timer` は削除・変換・上書きせず非対応として保護し、旧Cloud URLには編集機能を持たない退役案内を残しています
- クイックメモのタグをプロパティと同じ共通タグチップへ揃え、選択状態を実際の操作ボタンへ通知するようにしました。ボードの利用者入力を表示する箇所もDOMテキスト描画へ変更し、HTMLとして解釈しません
- フォルダツリーの表示設定をボタン直下へ表示し、連続変更時の再読込を1件の進行表示へ集約しました。ツリー上の `Ctrl+F` は名前・スコープ・タグ絞り込みへ、虫眼鏡、`Ctrl+Shift+F`、コマンドパレットは全文検索・置換へ送り、2つの検索語入力を同時表示しないようにしました
- 未実行計画と状態一覧を現行実装へ再照合し、実装・自動検証済みの項目を完了へ更新しました。Timer退役後の標準Release資産数も、本体3資産と現役単独アプリ6資産の計9資産へ揃えています

検証: 関連unit／静的契約、JavaScript／Python構文、分割bundle・公開マニュアル・セキュリティmanifest・Cloud静的成果物の生成整合を確認しました。Chromeではフォルダ表示・検索、パネルリサイズ、390×844 Phone相当の右パネル操作を確認しました。実Windowsクリップボード、実Dropbox複数端末、実iOS／Android、署名済みWindows配布版、Cloud公開は実施していません。

### テーマ設定のプレビューをMeldex本体のUIへ揃えました

- テーマ設定のプレビューを、フォルダ、ノート、シナリオ、シート、ボード、スケジュールで本体と同じDOMクラス・Lucideアイコン・レール・タブバー・ツールバー・ツリー・補助パネルを使う構成へ変更しました。左右レールで選んだ対象は対応するツリー／補助パネルへ反映され、サイドバー開閉アイコンも本体とプレビューで共通化しています
- プレビュー中央には本体に実在する要素だけを表示し、見出し、本文、リスト、表、カード、予定、レールボタン、タブ、ツールバー、ツリー行などをクリックして実際の書式設定ポップアップを開けるようにしました。通常・ホバー・選択ごとの文字色、背景色、枠線、左アクセント、下線、太字、斜体、フォント、フォントサイズ、行間、テーマカラーの自動適用を同じ画面から編集できます
- ノート見出しには見出し前／本文までの間隔を追加し、ボタンやリストを含む数値書式ではpx／%の単位選択と実プレビューへの反映を揃えました。カードの選択ハイライトやツリー行の点線アウトラインも状態別に設定できます。空白面をクリックした場合は、不要な状態設定を出さず背景色とその自動配色だけを表示します
- 「テーマカラーの自動適用設定」は、対象名と編集状態を明示したコンパクトな幅へ整理しました。背景・文字・線／アクセントの適用先、明暗強度、個別書式を1つのポップアップで編集し、取消時はテーマ値・選択状態・フォーカスを確実に元へ戻します。親の設定ダイアログよりこのポップアップが先にEscを処理するようにしました
- 共通カラーパレットの右へ色相・彩度・明度・明暗比を配置し、初期幅でも8色×3行を維持するレスポンシブ配置へ変更しました。色相範囲は360度の循環を扱い、両端を丸いアクセント色ハンドルと白い選択範囲で表示します。「始点」「終点」表記、範囲を変えず両端の役割だけを交換する反転、数値スピンボックス、各スライダーのアクセント色ハンドルを追加しました
- アクセントカラーのスウォッチを選択欄の右へ配置し、OS／既定アクセントでも実色を表示します。すべての組み込みテーマでポップアップ外周の初期色にアクセントカラーを使い、設定ダイアログ外のグレーアウトも他の設定画面へ揃えました
- デスクトップの設定ダイアログ初期サイズを、テーマプレビューまで見渡せる1070×920へ拡大しました。スマホ版のテーマタブではプレビュー本体を表示せず、詳細設定を開く導線だけを44px以上の操作面で残しています
- テーマプレビューから開く書式・配色ポップアップをモーダル内の操作として扱い、各項目の説明をクリック後の標準タイトル表示に頼らず、他の画面と同じくカーソルオーバーまたはキーボードフォーカスで表示するようにしました
- ショートカット設定で「すべて」を選んだ時は全カテゴリを2列で表示し、「この環境の変更を他のMeldexと連動」の文字サイズを周辺の設定項目へ揃えました

検証: テーマ関連unit／静的回帰、JavaScript／Python構文、分割bundle生成を確認しました。Chromeでは8種類のプレビュー各20状態について、実イベント、計算済み書式、ポップアップ編集、取消時の値・フォーカス復元を確認し、テーマ保存後の再読込も確認しました。Desktop／Tablet／Phoneのプレビューmanifestを確認し、アプリ内ブラウザでは本体とプレビューを同一画面で比較して、パレット、レール、タブ、ツールバー、見出しの全書式、空白面の背景専用ポップアップを目視確認しました。今回の追補では設定寸法7件、テーマ回帰16件、ツールチップ4件、分割JavaScript構文、版情報整合を確認し、アプリ内ブラウザでデスクトップの初期寸法、プレビュー表示、クリック不要のツールチップを確認しました。実スマホ、実iOS／Android端末、Windows配布版、Cloud公開は実施していません。

### 公開版ごとのWebマニュアルへ切り替えられるようにしました

- Webマニュアルの目次上部に「マニュアルのバージョン」を追加し、検索欄の前で公開済みの版を選べるようにしました。通常の `manual.html` は常に最新版を表示し、選択中の版を明示します
- 公開時の本文HTMLを `manual-versions/v<バージョン>/manual.html` へ版別保存する契約と一覧manifestを追加しました。既存の公開版 v0.7.363 を初回履歴として保持し、以後の公開版を上書きせず追加します
- 版切替では現在開いているページ位置を引き継ぎ、戻る／進む操作に対応します。一覧を読み込めない場合は表示中の版を保ち、誤った版へ移動しません

検証: 版別HTMLと一覧manifestの生成・Cloud配布物への収録・CSP・公開manifestのハッシュ対象・最新版の既定表示・版切替・検索・目次・戻る／進むを対象テストとDesktop／スマホ／タブレット表示で確認しました。旧公開版の本文は v0.7.363 の公開物を基準に固定しています。

## v0.7.364 (2026-08-29)

### タグパネルの連続切替を止まらず操作できるようにしました

- タグツリーで通常のタグを付け外しした時は画面へ即座に反映し、短時間の連続操作を1回の一括保存へまとめるようにしました。タグごとに割当ファイルを読み書きしていた待ち時間と、保存直後の不要な再読込を削減しました
- Desktop版、Cloud／Dropbox版、単独版で同じ一括更新契約を使います。自動付与されたタグの由来、別端末との競合検出、保存失敗時の表示復元を維持し、画面を閉じる時は保存待ちの変更を先に送信します

検証: タグパネルの即時表示・連続切替の1回保存・保存失敗時の復元、Desktop／Cloud／単独版の保存、CAS競合、自動タグ由来の保持、JavaScript／Python構文を対象テストで確認しました。Chromeのタグ管理micro E2Eで実際の連続クリックと保存完了を確認しました。実Dropbox複数端末、Windows配布版、実データ規模の速度計測は実施していません。

### Webマニュアルを現行仕様へ全面更新しました

- `app/manual` のMarkdown 210原稿を現行のDesktop版・Cloud版と照合し、廃止済み2原稿を除く208文書を公開対象として再生成しました。現行ファイル形式、画面構成、右レール、設定経路、ワークスペースチャット、バージョン管理、操作履歴、Dropbox共有、保存・復旧手順を現在の実装へ合わせました
- 利用者向け表記を実際のUIへ統一し、**オプション**、**サブパネル**、**プロパティ**、**テーマ**、**バージョン管理** などの独立した項目と役割を明確にしました。旧形式・旧モデル名・廃止機能・実装より強い保証表現は、互換説明または注意事項として必要な箇所だけに限定しました
- AI事業者のモデル名・料金・データ取扱条件を固定値で断定せず、現行の公式案内へのリンクと、Meldexの実画面で確認する手順へ更新しました。Claude Codeの公式手順も現在のQuickstart URLへ揃えました
- 配布ページと総合マニュアルの注意文を「AIを利用して作成・更新」へ統一し、記載内容が実際のMeldexの画面や動作と異なる場合があることを明記しました。存在しない別の公式情報・配布者案内との照合は求めず、重要な操作や費用・外部サービス設定では実際の画面表示を確認する案内へ変更しました
- 利用できない非公開GitHub Issuesへの案内を削除し、マニュアル、About画面、配布物README、利用規約、プライバシーポリシーの公式URLと問い合わせを、Meldex BETA配布ページと実際の送信フォームへ統一しました
- 公開生成処理は廃止状態の原稿と制作中のスクリーンショット指示をHTML・検索コーパスから除外し、機能分類9区分、文書内見出しへの直接移動、個別ショートカットの目次集約を維持する検査を追加しました

検証: 210原稿・208公開文書・1,933検索チャンクの構造、内部リンク、公開除外、旧UI語、外部公式リンクを確認しました。マニュアル、配布ページ、用語、アクセシビリティ、法務・連絡先、Chrome拡張、操作継続性、Cloud公開契約の対象テスト116件が合格しました。公開マニュアル生成、Python／JavaScript構文、Cloud静的生成、版情報整合、配布物検査を確認しました。

## v0.7.363 (2026-08-29)

### Cloud版と配布ページから開発者へ報告できるようにしました

- Meldex Cloud内で手動送信した不具合・要望・質問を、Debuggerの開発者受信箱へ直接届けるようにしました。自動クラッシュ報告の同意とは分離し、氏名と連絡先はMeldex内だけに保存して外部へ送りません
- 一時的な通信失敗や送信間隔制限ではブラウザ内の送信待ちへ保存し、オンライン復帰、再試行時刻、次回起動時に同じ受付番号で再送します。送信待ち、送信済み、送信失敗、Debugger受付番号を同じ履歴行へ反映し、同時送信でも報告を失わないようにしました
- 配布ページの「利用上の注意と連絡」へ、不具合・要望・質問を送れるフォームを追加しました。必須入力、送信内容の確認、Bot用入力欄、44px以上の操作面、狭幅1列表示、送信中・成功・再送待ち・失敗の状態表示に対応しています
- Cloud公開時はDebuggerのHTTPS送信先とMeldex識別名を必須設定とし、未設定・不正値のまま外部へ公開しない検査を追加しました

検証: Meldexの手動報告クライアント、Debugger連携、送信履歴、フォーム完了表示、Cloud公開準備の対象64件、JavaScript／Python構文、Cloud静的生成、Desktop／390px Phoneの設定フィードバックmicro E2Eを確認しました。Debugger側は公開報告・CORS 18件、OpenAPI契約1件、API／契約typecheckを確認しました。配布ページはDesktop／390×844で必須入力、日本語入力、44px操作面、横はみ出しなし、ブラウザエラーなしを確認しました。実Debuggerへ有効な報告を作る送信、公開Cloudへの反映、実iOS／Android、Windows配布版は実施していません。

### Webマニュアルの目次と本文表示を整理しました

- 目次を6分類の折りたたみツリーへ変更し、分類ごとのページ数を表示しました。検索中は該当する枝だけを展開し、検索終了時に元の開閉状態へ戻します
- 全210ページの見出し階層を自動インデントとガイド線で表示し、背景、カード、境界線、文字色、緑のアクセントを配布ページと同じ外観へ統一しました
- 個別ショートカットページはチャット参照と直接リンク用に維持しながら通常の目次から除外し、ショートカット一覧1ページへ集約しました。CSSとJavaScriptは内容ハッシュ付きURLで読み込み、更新後に旧デザインがキャッシュへ残ることを防ぎます

検証: 公開マニュアルの生成・対象unit 7件、Python／JavaScript構文、デスクトップ表示、390px幅の目次ドロワー、検索と開閉状態の復元、横はみ出しなし、ブラウザエラーなしを確認しました。

### 仮ユーザーを制作管理に追加し、ユーザー／ワークスペース設定を整理しました

- 人物の総称を「ユーザー」へ統一し、ログインできる「アカウントユーザー」と、実在しない試験用の人物にも使える「仮ユーザー」を区別しました。設定の「ユーザー」から仮ユーザーを追加し、シフト、稼働時間、休日、単価、担当者、自動割り当てに利用できます
- ワークスペースごとの所属と権限を「ワークスペース」へ統合しました。仮ユーザーは `制作管理のみ・ログイン不可` と表示し、認可用メンバーには保存せず、ログイン、共有ファイルアクセス、管理者／所有者ロール、外部カレンダー送信を許可しません。ユーザー管理シートを確認できない場合もアクセスユーザー追加を停止します
- 既存のユーザー管理データへ安定ID・種別・制作対象ワークスペースを後方互換メタデータとして追加しました。表示名だけの旧行も仮ユーザーとして読み取り、Desktop／Cloudで保存・再読込できます

検証: Desktop／Cloudのユーザー管理契約、安定ID・旧データ読取・所属再読込、仮ユーザーのアクセス権限拒否、判定失敗時のフェイルクローズ、制作管理と外部カレンダーの境界を対象unitで確認しました。設定画面はDesktop／390×844 Phone／768×1024 Tabletのmicro E2Eとスクリーンショットで確認しました。実Dropbox複数端末、実iOS／Android、Windows配布版、Cloud公開は実施していません。

### 昨日・今日の未実装計画を完了し、エクスポート・復元ポイント・検索を補強しました

- Cloudのファイル保存はBlobやURL、8MB超の出力もブラウザの保存先選択／ダウンロードへ統一し、Desktop専用保存ダイアログへ誤って送らないようにしました。設定ZIPはCloudでも許可済みの表示・操作設定だけを書き出し、秘密情報を除外し、取込前に端末内バックアップを残します。チャットの出力メニューもキーボード、Escape、フォーカス復帰に対応しました
- 復元ポイントは通常保存のたびに暗黙作成せず、設定した周期に変更があった対象だけを保存します。時間／日／平日／週／月、間隔、曜日、日、時刻、タイムゾーンを選べ、既定保持は無期限、必要な場合だけ日数を指定できます。件数上限と段階的間引きは廃止し、削除予定を件数・容量・最古日時で確認してから適用します
- 個人の復元ポイント方針をDesktop／Cloudで共有し、共有ワークスペースではowner／adminだけが変更、member／viewerは読取専用となるCAS契約へ統一しました。ファイル、フォルダ、シートDBの復元・削除・削除取消も同じlock・権限境界で扱います
- ノート、シナリオ、シート、ボード、フォルダのツールメニューとコマンドパレットへ検索・置換入口を追加しました。検索結果は一致位置を保持し、各編集面の本文、行、セル、トピック、項目へ移動します。別ワークスペースや削除済み対象の古いコマンド履歴は候補から除外します
- 2026-08-27〜28作成の計画書と作成元セッションの利用者依頼を再照合し、競合、履歴、注釈、D&D、設定、Viewer等の既存Wave実装を対象回帰で再認定しました。古い計画状態と、復元ポイント／検索／設定引き継ぎのマニュアルを現行仕様へ更新しました

検証: 20対象moduleの317 unique tests、復元ポイント／履歴の集中回帰、Desktop／Cloud source syncと1047ファイルのCloud manifestを確認しました。隔離ブラウザではDesktopと390×844 Phoneの「履歴・復元」、Phoneメニュー、コマンドパレットの検索入口を確認しました。実Dropbox二端末、実iOS／Android、B-MANGA／CLIP STUDIO、署名済みWindows配布、Cloud公開は未実施です。

### 右サイドバー、クイックメモ、シート／スケジュール、ボードの操作を拡張しました

- 右サイドバー内で切り替えたパネルを戻る／進む履歴へ追加し、マウスの戻る・進むサイドボタンにも対応しました。共通の数値入力欄はドラッグで増減でき、Ctrlで10倍刻みのスナップ、Shiftで10分の1刻みの微調整、Escで開始値へ復帰できます。無効なボタンの枠線も全体で非表示にしました
- 設定の「ワークスペース」はワークスペース名・対象フォルダ・通常使用・削除へ、「メンバー・権限」は所属メンバーとワークスペース内役割へ責務を分離し、重複表示を整理しました
- クイックメモの不要な履歴・右サイドバー・バージョン管理ボタンに加え、重複して残っていたタグ辞書・復元ポイントの入口を撤去しました。保存待ち／自動保存中はヘッダーから外し、Undo／Redo、入力方法、ペンツールなどのボタンを本体ツールバーと同じ28pxへ小型化し、タグ追加の＋を中央へ揃えました。「音声」は「ボイス」へ統一しています
- クイックメモのタグは再クリックと全解除で空配列を明示保存し、保存中の古い応答で解除前へ巻き戻らないようにしました。タグツリーでは見切れた項目を含む全件を確認できます。ペンは本体のアノテートと同じ合成入力点と0.5px閾値を使い、細かな軌跡を保持します。Meldex内パネルから単独アプリへ切り替えると、現在のメモを保存して同じ大きさで開き、起動成功後に元のパネルを閉じます。初期保存先はホームフォルダ内の「クイックメモ」シートです
- シートとスケジュールへガントビューを追加しました。シートでは開始・終了・進捗列、期間バー、マイルストーン、未スケジュールを扱い、スケジュールでは予定・ToDoの期間表示、日／週／月目盛、グループ化、移動・期間変更に対応しました
- シートの列タイプ設定をオプションパネルへ一本化し、日時列の通常／作成日時／更新日時も列設定タブで選ぶようにしました。セレクト列の選択肢は1件1行へ分け、追加、色、複製、削除を各行から操作できます
- ボードの利用者向け「カード」を「トピック」へ統一し、三点メニューを中央へ揃えて点を拡大しました。トピック、ライン、グループ、階層別スタイルへ、背景・枠線・ライン色それぞれの不透明度をスタイル設定として追加しました

検証: 対象unit／DOM契約、分割bundle整合、JavaScript／Python構文を確認し、実ブラウザで右サイドバー履歴、無効ボタン枠線、クイックメモのヘッダー・タグツリー・保存先、実データ3件を使ったスケジュールのガント、ボードの用語・三点メニュー座標・不透明度欄をAI目視しました。今回のクイックメモ修正は対象テスト65件、保存中にタグを解除する競合E2E、CloudのPC／タブレット／スマートフォン、420×620の単独画面でボタン寸法・＋の中心・ペン軌跡を確認しました。実Dropbox複数端末、実タッチ端末、Windows配布版、Cloud公開は実施していません。

### パネル、ボード／シート、常駐注釈、テーマ設定のUIを統一しました

- テーマ、バックリンク、プロパティのパネルタブへ対応アイコンを表示しました。ボードのグループスタイルはツールバーから外し、カード、ライン、階層別スタイルと同じオプションパネルのタブへ移しました
- ボードのビュー切替をシートと同じタブ、ドロップダウン、追加・名称変更・複製・並べ替え・削除メニューへ揃えました。シートのギャラリー、ツリー、チャートは、0件時の「トピックがありません」とビューアイコンを各タブと一致させました
- 付箋の「…」はPCでホバーまたはキーボードフォーカス時だけ表示し、ホバーできないタッチ環境では44pxの操作面を維持しました。常駐アプリの付箋はMeldex本体と同じ構造・外観へ揃え、注釈フロートメニューは常駐アプリで撮影した対象スクリーンショットにだけ書き込めるようにし、付箋・撮影ボタンを除外しました
- 設定ダイアログと右サイドバーのテーマプリセット操作を、選択欄右の「…」メニュー1個へ統合しました。追加、複製、リネーム、更新、保存、削除を同じ順序で提供し、組み込みテーマの見出し・階層・系列色がアクセント1色へ潰れず、設定済みパレットを保持するようにしました

検証: 対象unit／DOM契約、JavaScript／Python構文、生成テーマbundleの整合、Chromeのボード・シート・設定ダイアログ・右サイドバー・常駐スクリーンショット注釈5経路を確認しました。ボードとシートは同一viewportの画面をAI目視で比較しました。Windows常駐アプリの配布EXE、実タッチ端末、実Dropbox複数端末、Cloud公開は確認・実施していません。

### 注釈機能の名称とアイコンを全環境で統一しました

- 利用者向けの「注釈」機能を「アノテート」、「情報」パネルを「プロパティ」パネルへ改名し、本体、Cloud版、単独アプリ、常駐アプリ、配布ページ、マニュアル、ヘルプ、操作テストで同じ表記に統一しました
- アノテートパネル、フロートメニュー、固定レール、モバイル編集バーなど、アノテート機能を開くLucideアイコンを `square-pen` へ統一しました。互換性のため、内部識別子、保存形式、APIの `annotation` / `information` は変更していません

検証: Desktop／Cloud／単独アプリのソース同期、配布コピーの鮮度、生成監査台帳、用語・アイコン残存検査、対象unit／DOM契約、実ブラウザの固定レール操作を確認しました。実Dropbox複数端末、Windows配布EXE、実iOS／Android端末、Cloud公開は実施していません。

### 仕様台帳の漏れを埋め、公開・競合・シナリオ・検索置換のデータ保全を強化しました

- 52要求・85受入ケースをrev2要求台帳へ移し、各要求を単一workstreamへ割り当てました。実Dropbox、実端末、配布版、公開環境の未確認項目は合格へ繰り上げず、外部証拠待ちとして維持します
- 大容量資産を含むHTML公開は資産を先に保存し、browser pickerでも保存後のsizeとSHA-256を再確認してからHTMLを確定します。途中失敗では旧HTMLを維持します
- Desktop／Cloudの競合解消を比較済みrevisionとSHA-256へ固定し、backupも比較済みsnapshotそのものを保存します。原本適用後に競合退役が失敗した場合は元世代へ補償し、補償失敗を成功扱いにしません
- シナリオのEnter行分割、IME中の誤操作防止、read-only、競合選択肢を実状態へ接続しました。検索置換は入力中に一致数を更新し、結果移動で不要な書式ダイアログを開かないようにしました。スマートフォンでもタイプメニューが共通キー処理と競合せず、Escで確実に閉じます
- 構造化ファイルの検索置換を対応形式へ限定し、確認対象の全pathと世代を固定してから一括保存します。途中失敗時は先行保存をCASで補償します
- 旧シナリオ／ボード単独入口はpathと表示名を保ったまま現役Meldex本体へ引き継ぐ非編集shimへ整理しました

検証: 元変更61ファイルを個別にAIレビューし、監査追補も再確認しました。対象unit、Desktop／Phoneのシナリオmicro E2E、Desktop 1440×900・Phone 390×844・Tablet 820×1180のAI目視、生成bundle／Cloud／Desktop source syncを確認しました。実Dropbox二端末、実iOS／Android端末、Windows配布版、公開Cloudは未確認で、公開・版更新は行っていません。

### テーマ設定とシート／ボード共通ビューを完成させました

- テーマ入口を Lucide のパレットアイコンへ統一し、選択中アイコンを4組み込みテーマで識別できる配色にしました。固定右レールのテーマは、現在のファイルまたはフォルダに追従し、未対応・読み取り専用・不一致・エラーを空白にせず表示します
- 設定の色相開始／終了を1本の二つまみトラックへまとめ、開始値が終了値より大きい色相環の折返しもそのまま保存します。色相・彩度・明度のトラックと実UIプレビュも同じテーマ値へ接続しました
- ボードで、選択トピックの親系統／子系統の一括選択、0〜200pxのライン幅、線幅で線方向へ伸びない矢印、名前付きグループスタイルを追加しました
- 同じTopicデータにテーブル、ツリー、ボードを混在追加できる共通ビュータブを追加しました。名前変更、複製、並べ替え、削除、0件の空状態、読み取り専用の表示切替をPC・スマートフォン・タブレットで同じ操作体系にしました

検証: テーマ・固定レール・TopicViewDocument・ボード選択／ライン／グループの対象unit／DOM契約、構文検査、PC・390×844・834×1194の実ブラウザE2Eを確認しました。実Dropbox複数端末の同時更新、実iOS／Android端末、配布版、Cloud公開は実施していません。

## v0.7.362 (2026-08-25)

### 仕様定点観測の要求台帳を回復しました

- 2026-08-10以後のClaude Code／Codex／Gemini CLIの利用者起点ログを全件分類し、8月25日の台帳から漏れていた色相・彩度・明度・実UIプレビュー、Viewer、フォルダ、チャット、設定などの具体条件を要求・判断・受入ケースへ追加しました
- 出典ログの完全性と製品実装の完成度を別ゲートにしました。既存の合格証拠は保持し、新たに回復した条件と実端末・実Dropbox・公開確認は、証拠がないまま合格へ昇格させていません
- 現役設計意図と修正計画へ要求IDを接続し、未計画・部分対応を機械可読なcoverage matrixで確認できるようにしました。今回の更新は仕様・監査基盤のみで、製品コード、版番号、公開状態は変更していません

検証: source inventoryの全メッセージ分類・ハッシュ・件数等式、要求台帳の出典完全性、欠落セッション／メッセージ／statement／全称coverage／fork重複／raw本文混入の回帰を対象テストで確認しました。製品側は未確認要求を含むため、完成判定を`incomplete`に維持しています。

### ボードのライン作成とキーボード操作を更新しました

- トピック周囲に常設していた上下左右の **＋** を廃止し、PCではAlt＋ドラッグ、スマートフォン／タブレットでは下部編集バーの **ライン** を主な作成経路にしました。ボード空白上の自由点同士も、トピックに属さない単独ラインとして作成できます
- トピック上からラインを作成した場合は、ドラッグの開始位置と終了位置を結ぶ方向から輪郭との交点を求め、上下左右の中心へ強制せず保存します。中心付近だけは従来どおり軽くスナップし、旧4方向アンカーを持つボードも引き続き開けます
- 複数選択したトピックを **Ctrl＋G** でグループ化できるようにしました。Tab／Enterで追加したトピックと、矢印キーで切り替えたアクティブトピックは、ズームを変えず必要な分だけ画面が追従します

検証: 対象ユニットで自由端、輪郭位置、旧アンカー互換、保存／再読込を確認し、実ブラウザE2Eで常設 **＋** の不在、Alt＋ドラッグ、単独ライン、Undo、Ctrl＋G、画面追従、PC／390×844／768×1024の下部ライン操作を確認しました。AI目視ではPC、スマートフォン、タブレットの現在画面を確認しました。実iOS／Android端末、実Dropbox複数端末、Cloud共有状態、配布版、公開は確認していません。

### ボード作成とライン端点の操作を修正しました

- フォルダツリーの **＋** からボードを作成した際、同名の別種項目があっても作成結果を種類と名前の組み合わせで確認し、不要な待機や誤った通信失敗表示を起こさないようにしました。ツリー上ではボードを1項目として扱い、内部データを同名フォルダとして重ねて表示しません
- ラインの始点と終点を、カードの上下左右の **＋** だけでなく輪郭上の任意位置へ移動できるようにしました。カードの移動・変形と保存後の再読込でも位置を維持し、従来の4方向アンカーを持つボードも引き続き開けます
- 新しく追加したカードへ初回保存前に安定した参照を割り当て、ラインが一度保存した後に消える問題を修正しました。旧一時IDで保存された端点も再読込時に復元します

検証: 隔離した開発版で、ホームフォルダへのボード作成、ツリー上の単一表示、ライン両端の任意輪郭位置への移動、自動保存後の再読込を手動相当操作で確認しました。対象ユニット36件と対象E2E 2件が合格しました。実作品データ、実Dropbox複数端末、公開Cloud、配布版、公開は確認していません。

### シートとボードのトピック統合後の保存回帰を修正しました

- セレクト／マルチセレクト列の候補色と将来の列設定を省略せず保存し、保護列・権限拒否・保存失敗では画面だけに変更を残さず元へ戻すようにしました。候補名の変更・削除では対応する色も同じ履歴で移動・削除します
- 旧ボードに保存済みの複数ビュー、座標、ライン、グループ、関係、スタイルをDesktop／Cloud移行で維持し、旧原本と現在の編集が同じ項目で競合した場合は上書きせず停止します
- ボード上部へビューの追加・複製・名称変更・並べ替え・削除を追加し、狭幅ではオーバーフローへまとめました。読み取り専用時は変更だけを無効化し、ロックしたカードをシャッフル対象から除外します
- プレビュー再表示後のミニマップとカード詳細の即時反映を修正し、旧原本・最新状態・検証済みチェックポイントから縮退候補を非破壊で検出できるようにしました。実データ修復は隔離コピー確認と明示承認が必要です

検証: 対象ユニット／契約111件、テーマコントラスト検査、シート列表示順、ボードのミニマップ・カード詳細・PC／スマートフォン／タブレットのビュー操作を実ブラウザで確認しました。実作品データ、実Dropbox複数端末、公開Cloud、配布版、実データ一括修復、full公開E2Eは実施していません。

### 開発版を実Dropboxデータから分離しました

- `start-editor.bat` から起動する開発版は、ソースや製品版を増やさず、開発専用データをリポジトリ内の `_verify/start-editor-development` に保存するようにしました。DropboxがPC上で動作中でも実同期先を自動検出せず、アクセストークンと通常のブラウザプロファイルも引き継ぎません
- 開発版でスケジュールのUIと細かな挙動を確認し、固まった同一バージョンをデスクトップβ版とCloudβ版へ公開した後、Dropboxのテスト専用フォルダで共有確認する運用へ統一しました。共有確認専用の第三の版は追加しません
- シフト勤務テンプレートのメニューをEscキーで閉じられない場合がある問題を修正しました

検証: 開発版起動の対象テスト3件、隔離基盤14件、Dropboxデスクトップ連携62件、実バッチの隔離確認、Meldex本体の隔離importを確認しました。スケジュール微細挙動E2EはPC／スマートフォン／タブレット／Cloud静的画面を含む対象63件すべて合格しました。公開、配布版ビルド、実Dropbox共有は実施していません。

### テストと公開ゲートを現役製品面へ統一しました

- Meldex本体、Viewer／Timer／Quick Memo、常駐機能、デスクトップWeb Clipper、CloudのPC／タブレット／スマートフォン、iOS／Androidを一つの製品面台帳へまとめ、現役面の必須操作に自動証拠または実機必須証拠が無い場合は公開判定を閉じるようにしました。旧Note／旧シナリオ／旧ボード／旧シート単独版は、Meldex本体への引き継ぎ互換だけを残しました
- 自前Windows runnerの公開workflowをPowerShellへ統一し、unittestと公開必須E2Eを実測時間に基づいて2論理ノードへ分割しました。同一ソース、同一ケース台帳、割当と完了の重複・欠落、成果物、失敗、未完了、再試行合格を機械集約し、1台だけの場合も全件を直列化して網羅性を保ちます
- Windows公開では本体と現役3単独アプリのEXE／ZIP、隔離起動、常駐経路、Chrome拡張とnative host、版、ハッシュ、法務、秘密情報を同じソースで確認します。Cloud公開は実運用先`cam-nagara/MeldexCloud`だけを更新し、限定tokenをGit remoteへ保存せず、公開後にソース、版、manifest記載ファイル、主要経路、Windowsダウンロード先を再検査します
- 現役3単独Cloud画面は起動・主要操作に加えて各画面のオフライン再起動を公開必須workflowへ登録しました。AndroidはGradle unit／compileを独立したモバイル配布ゲートへ追加し、iOS／iPadOSはMac・実機のDebugger証拠が無ければモバイル配布だけを止めます
- Cloud公開準備テストの設定検査と生成検査を分離し、同じ検査内容のローカル所要時間を約575秒から約94秒へ短縮しました

検証: 変更候補`259951290449e3ead22f5f8311f88c65d9abca67`でfull unittest 8,990件を実行し、未登録skip 0・timeout 0・予期しない失敗3件でした。公開必須E2Eは699件を重複・欠落なく完了し、573件合格・126件失敗・未完了0・再試行合格0でした。実MV3拡張、content script、service worker、native hostの成功／未登録エラー契約は合格しましたが、Web Clipper popupは設計上の384×640枠を縦に3px超過して失敗しました。失敗を既知扱いへ弱体化せず、Desktop／Cloud／モバイルの公開ゲートは閉じたままです。この全量結果は版更新前の候補に対する不具合収集証拠であり、v0.7.362の公開認定には使用していません。GitHub Release、Cloud公開、実Dropbox二端末、Android実compile、iPhone／iPad／Android実機は実施していません。

## v0.7.361 (2026-08-25)

### スケジュールと制作管理の操作を一つの流れへ統合しました

- スケジュール中央面を「カレンダー／タスクリスト／プロジェクト一覧」の3つへ整理し、制作設定を作業テンプレート親子構造と現役シート編集面を持つ一つのダイアログへ統合しました。PCの狭幅では操作群をまとまり単位で折り返し、スマートフォン／タブレットではデスクトップ用ツールバーを重複表示しません
- 制作管理の12シート契約と既存データ移行を更新し、`表示名`列や同名の別ワークスペースを安全に識別します。新規シートを既存テンプレートで上書きする経路を止め、未知列・本文・将来Schemaを失わず扱います
- 制作タスクを共通TopicRefへ接続し、右サイドバーからリンク済みノート／チャットの一覧、作成、再表示、解除を行えるようにしました。作成・リンク・再試行は部分成功を残さず、Desktop／Cloudで同じ安定IDを使います
- イベントカードのチェックON／OFFを同一予定の複数表示へ同期し、予定色は明示した個別色を除いてカレンダー色へ追随します。カレンダー表示設定は利用者ごとに分離し、owner／adminは許可された全ワークスペースのアクティブメンバー、一般利用者は本人と共有分だけを確認できます
- 出退勤、シフト、制作予定の自動生成予定を元データと安定IDで結び、ワークスペースをまたぐ同名メンバーを混同しないようにしました。自動生成予定は予定画面から直接変更・削除せず、元データへ案内します
- 初回起動で複数のカレンダー読込が同時に走った場合のSchema移行競合と、イベント保存失敗時に理由だけが表示される問題を修正しました

検証: 保存・再読込・移行・権限・競合・TopicRef・自動割り当て・10人3作品の計算シナリオを対象unit／integrationで確認し、PC・768×1024・390×844の実ブラウザで中央面、制作タスクリスト、管理入口、イベント編集、出退勤を確認しました。実Dropbox複数端末の同時更新と、全Stage・全画面を並べた最終シナリオ証拠ビューは未確認です。公開・配布版ビルドは行っていません。

## v0.7.360 (2026-08-24)

### トピックをシート／ボード間で移動・リンク複製できるようにしました

- トピック自身が列タイプ付きの全値を保持し、対応列がない場所では値を失わず非表示、値がない列は空欄になる共通プロパティ基盤を追加しました
- シート、ボード、メイン／サブパネル、別ウィンドウ、フォルダツリーの間で、同じトピックの「移動」と「リンク複製」を行えるようにしました。移動は追加先の保存成功後に元の配置だけを外します
- 同じ共有列IDを優先して値を表示し、同名同タイプ列への初回対応付けと列名編集確定時には、共通化するか別列のままにするかを確認します。確認済み対応は保存し、曖昧な複数候補や既存値を自動上書きしません
- トピック詳細に「この場所の値」「すべての値」「登録先とリンク」を追加し、非表示の値を含む全プロパティと、登録中のシート／ボード、リンク済みノート／チャットを確認して開けるようにしました
- 複数画面の更新通知と短時間のドラッグ受け渡しを追加しました。受け渡し用ストレージには不透明な一時IDと有効期限だけを保存し、値や本文は永続化しません

### 旧スマートシートと専用ダッシュボードを廃止しました

- 旧スマートシートの作成入口、画面、横断クエリ、ダッシュボード、保存／公開／LLM操作、専用モジュールとテストデータを削除しました。旧`.smart-db.json`は互換読込せず、通常シートの利用を案内します
- チャット履歴、全ファイル一覧、クイックメモ、デバッグリストなどの内部用途は、利用者が編集する横断クエリではなく、通常シート互換のシステムシートproviderへ置き換えました
- スケジュールのタスクリスト「すべて」は既存の専用集約経路を維持し、旧スマートシートへ依存しないまま検索・表示・編集を継続します
- マニュアル、ヘルプ、機能テスト用サンプル、UI/E2E監査台帳を新しいトピック配置仕様へ更新しました

検証: トピックの型付き値・配置トランザクション・遅延移行・システムシートprovider、シート／ボード／フォルダツリー／パネル間操作、旧機能残存防止、スケジュール「すべて」、Desktop／Cloud生成物の対象テストと静的検査を実施しました。実Dropbox複数端末、実iPad／Android、Windows配布版の手動操作は未確認です。

## v0.7.359 (2026-08-24)

### マニュアルを配布ページから読めるWeb版へ移しました

- Meldex Home内のマニュアルを廃止し、配布ページから開く検索・目次付きWebマニュアルへ移しました。Meldexの画面とマニュアルを別タブや別ウィンドウで並べて確認できます
- Meldex内のヘルプメニューと設定画面は、該当するWebマニュアルの項目を直接開くようにしました。起動時にHome内のクイックスタートを自動表示する処理も廃止しました
- Meldexの使い方をチャットで質問したときは、Webマニュアルと同じ原稿から生成した内容を自動参照し、該当する公式マニュアルURLを添えて回答するようにしました。創作相談などMeldexの操作質問ではない会話には追加しません
- シート／ボードの「トピック」統一、複数ソース、レイアウトテンプレート、複数リンク、ノート埋め込み、ボードの複数ビューなど、v0.7.358の仕様へ説明を更新しました

### iPhone・iPad・Android版Web Clipperの初期実装を追加しました

- iPhone・iPadではSafariの表示中ページや選択範囲を共有でき、Chromeなどから共有されたURLはMeldex側でページを開き直して本文を取得する構成を追加しました
- AndroidではChromeなどの共有からURL、テキスト、画像、PDFを受け取り、ページ本文の取得結果を確認してDropboxの`Web Clipper`へ保存できる構成を追加しました
- 全文、一部、URLのみを区別し、オフライン保存待ち、再試行、取消、認証切れからの再接続、同一共有の重複防止を両OSへ追加しました
- 既存デスクトップ拡張と共通の本文抽出・Markdown変換・取得品質判定を使い、公開記事内のログインリンクを購読制限と誤判定する問題や、取得品質を過大表示する問題を防ぎました
- Meldex設定へモバイル版の説明を追加し、スマートフォンとタブレットではWeb Clipperへ直接移動できる一列表示にしました。PCでは従来のデスクトップ拡張設定も維持します
- Debuggerの既存送信専用契約へ接続するモバイル用クライアントを追加しました。事前同意、伏せ字、件数上限と30日の保存期限を持つ送信待ち、重複防止を備え、Web本文、共有URL、画像、認証情報は自動送信しません

検証: 共通抽出・ネイティブ契約・既存デスクトップWeb Clipper・設定ナビゲーションの対象テスト57件、独立JavaScriptの構文、JSON Schema、plist、Android Manifest、YAML、秘密情報、ファイル行数を確認しました。実ブラウザでは390×844、768×1024、1280×900で設定導線、横はみ出し、表示対象、コンソールエラー0件を確認しました。Xcode / Swift / Android SDK / Gradle / adbが無いWindows環境のため、ネイティブコンパイル、実共有メニュー、実機アクセシビリティ、署名済み内部ビルドは未確認です。ストア公開と既存データ移行は行っていません。

- モバイル保存先にソースフォルダ／作品フォルダの安定ID、Dropbox名前空間、Owner／Editor／Viewer、ロック、容量を持つ共通契約を追加しました。曖昧な同名パスや権限を確認できない共有先には保存せず、個人用保存先を案内します
- 共有先への書き込みは所有者鍵を確認できる場合だけ許可し、鍵を未設定の端末では共有保存を表示しないfail-closed契約にしました。所有者鍵の配布方式は実端末登録時に選択します
- ロック台帳はファイルの存在だけで保存を止めず、保存先またはその親を実際にロックしている項目がある場合だけ拒否するようにしました

### トピックのリンクファイル、Xブックマーク、重複画像表示を修正しました

- トピック上部の作成ボタンを整理し、**＋リンクを追加**から通常ノートと保存済みチャットを新規作成してリンクできるようにしました。作成または登録に失敗した場合は部分成功を残さず、読み取り専用・容量不足・競合の理由を表示します
- 列一覧の並べ替えを追加ボタンの直前へ移し、列順・非表示列・グループを保存ビューとペインごとに分離しました。旧シート共通値は初回だけ安全に引き継ぎます
- トピック画面の「添付」を「リンク」へ統一し、新規ボードには実在する共通アイコンを表示します。チャット画像など一般の添付機能は変更していません
- 重複画像は現役サムネイルAPIから実画像を表示します。Xブックマーク本文の管理用文言を除き、所属フォルダは未取得・所属なし・失敗を区別して保存／補完します。追加料金が発生する定期フォルダ取得は既定オフです
- Xフォルダ同期は編集ロック中のファイルを書き換えず、重複整理も参照やアノテートの更新失敗、または整理対象のロックを検出した場合は元ファイルを残して再実行できるようにしました

### 画像読込中の表示を共通リングへ統一しました

- シート、ノート、フォルダ、チャット、ボード、ビューワーなどの画像読込中表示を、画像ごとに中央の分割リング一つへ統一しました。画像本体と枠へ二重表示される状態や、矩形いっぱいの集中線状表示を解消しました
- Desktop、390px、768px、dark/light、動きを減らす設定、成功・失敗・再試行、複数同時読込で、画像枠とスクロール位置を変えないことを対象E2Eで確認しました

### 4アプリ以外の履歴とバージョン管理を補強しました

- クイックメモと単独タイマーに、変更前の自動版、版の一覧、現在との差分比較、確認付き復元を追加しました。同じ内容を保存した場合は不要な版を作りません
- ビューワーのアノテート・付箋を、Meldex本体と単独Viewerの両方から対象別に比較・復元できるようにしました。子ビューワーでの作成・編集・削除も親画面のUndo/Redoへ反映します
- チャットの保存先表記差によって版一覧が空になる問題を修正し、現在のチャットだけを比較・復元できるようにしました
- アノテート・付箋の復元前退避と同時更新検知を追加し、別の対象へ移動済みの同じアノテートIDを旧版復元で複製しないようにしました。競合時は上書きせず停止します
- 新形式のシート版比較で、SQLiteに保存された本文や同名項目が欠落し、すべて追加扱いになる問題を修正しました
- 単独タイマーの設定、テーマ色、時・分・秒、表示状態と主要操作をUndo/Redoへ接続し、スマートフォンの履歴・設定・タグ操作を44pxのまま画面内へ収めました。版復元の直後に保存待ちの古い内容が復元結果を上書きする競合も防ぎました
- 一般の予定、ToDo、シフトへ変更前の自動版、対象別の一覧・比較・確認付き復元を追加しました。復元前の版を残し、表示後に別画面で変更された場合は上書きせず停止します。版履歴も予定本体と同じ共有スナップショットで端末間同期します
- 共通UI設定ファイルへ変更前の自動版、一覧・項目別比較・確認付き復元を追加しました。設定保存は永続化の成功を待ってから完了表示し、容量不足や保存失敗を成功扱いしません
- ノートの版復元直後も通常の表示更新経路を使い、frontmatter、自動リンク、ファイル固有表示、目次、スクロール位置が再読込後と一致するようにしました
- Web Locksを利用できないCloud環境でも、予定表のleaseをheartbeatと保存直前検証で保護し、期限切れの画面が新しい編集を上書きしないようにしました。版復元は本文・監査・応答を同じcommit結果へ揃え、後続の編集者が取得したleaseを旧処理が解放しません
- Cloud設定ではデスクトップ専用の自動起動項目を表示・保存対象から外し、容量不足時は設定ダイアログと利用者向けエラーを維持します

検証: 保存・一覧・比較・復元・競合を含む既存対象契約87件に加え、一般スケジュール／設定の関連契約62件を合格確認しました。Meldex本体3経路と、単独クイックメモ／Viewer／タイマーのデスクトップ・モバイル計6経路を実ブラウザで確認し、タイマーは復元前の保存待ち破棄と復元後UI更新まで固定しました。一般スケジュール／設定はDesktop、Cloud、390×844、768×1024で、保存、履歴差分、競合拒否、復元、再読込、容量不足時の失敗表示まで確認しました。実Dropboxの2端末同時操作だけは実環境確認待ちです。

### 既知E2E失敗の現役台帳を0件にしました

- 過去142件の記録はSHA-256固定の不変fixtureへ保全し、runnerが読む台帳には現在再現する未解決ケースだけを置く構成へ変更しました
- 142件を一件ずつ再実行し、現役経路への追随と局所修正を行いました。最終的なactive known outcomeは0件です

### LLM／CLIの機能理解と外部返答境界を安全化しています

- 現役18機能について環境別の能力台帳を追加し、ChatとMCPが同じSchema取得・payload検証・読解adapterを使うようにしました。ノート、ボード、予定、トピック、シート、スマートシート、シナリオ等は上限、正本状態、保存形式、権限、未解決参照、出典を構造化して返します
- 外部ワークスペースチャット用CLIは、shell、MCP、plugin、hook、browser、Computer Use、sub-agent、秘密環境を渡さない固定answer-only実行器へ分離しました。端末／nodeのEd25519署名、所有者署名済み許可台帳、期限・改ざん・replay拒否を追加しています
- 添付・状態機械・端末承認UI・実機ゲートがそろうまではproduction gateを閉じ、旧version 1や未署名Cloud直結を互換動作へ戻しません
- 返答用AIの状態をAIタブ内だけへ追加し、署名済みの所有者鍵・管理者PC・安全policy・CLI能力・待機件数が一致した場合だけ準備完了と表示します。依頼状態、取消、再試行、未読、アプリ内通知、明示許可後のOS通知、端末登録と所有者による承認・失効を追加しました。実CLI・実Dropbox・実スマートフォン確認前のため外部経路は引き続き安全停止中です
- 外部依頼の取消・再試行もブラウザー端末鍵の署名と単回nonceを必須にし、旧入口、改ざん、replay、失効端末、別利用者からの操作を拒否します。AIによるファイル版復元は単純な確認フラグを廃止しました。認証済み画面から確認tokenを発行する導線が未接続のため現在は実行不可とし、LLM/MCP自身が確認を発行する入口は公開していません
- 「管理者AIに依頼」は、現在のワークスペースで署名・安全設定・オンライン状態を確認できた管理者PCが準備完了の場合だけ表示して操作可能にしました

検証: 最終の全量unittestは8,895件を実行し、既知11件、skip 12件、timeout 0件でした。unexpected 33件のうち、生成監査4件は正規生成後の関連72件、全量負荷時だけのファイル索引／二重writer 3件は隔離実行で合格しました。残る26件は、今回はビルド対象外とした `Meldex`／`MeldexCloud` 配布コピーの未同期だけです。対象ブラウザーE2E、LLM受け入れ20件、高リスク修正後の独立レビュー47件、JSON・構文・秘密情報検査を実施しました。ネイティブ実機、実Dropbox二端末、実CLI、配布物のビルドと公開は未実施です。

## v0.7.358 (2026-08-23)

### シートとボードのデータをトピックとして共通化しました

- シートの行とボード上の項目を同じトピックとして扱い、複数ソースの内容を手動・検索条件・両方の方法で一つのビューへ集められる基盤を追加しました。旧シート、スマートシート、ボードは原本を変更せず、開いた時に必要な範囲だけを移行します
- 端末内の軽量索引と送信待ちキュー、差分チェックポイント付きの可搬ファイルを追加しました。ソース未接続時は最後の完全スナップショットを読み取り専用で開き、競合・容量不足・中断時も旧原本と直前の完全状態を残します
- Cloudのトピック索引・送信待ち・受領記録を同じIndexedDBトランザクションで更新し、古い応答と新しい編集が重なっても新しい編集を失わないようにしました。可搬ファイルは保存直前の別アプリ／同期サービスによる更新世代を原子的に退避・照合し、安全な置換ができない環境では上書きせず停止します
- シートの「エントリ」とボードの保存対象を、画面上では「トピック」に統一しました。内部の旧IDや保存キーは互換性のため維持します

### シート・ノート・ボードの編集機能を拡張しました

- トピックレイアウトへ枠線、背景、角丸、影、余白、列名表示を追加し、値を含まないレイアウトテンプレートとしてファイルまたは共有ライブラリへ保存・読込できるようにしました。ギャラリー／カンバンでも型に合った編集を行えます
- 一つのセルへ複数のWeb・Meldex・PC内リンクを追加し、並べ替え、個別解除、既存項目の選択、新規ノート／シート／ボード／チャット作成を行えるようにしました
- ノート本文へシート／ボードを参照のまま遅延表示する埋め込みブロックを追加しました。引用はツールバー、`/`メニュー、行種変換、貼り付け、Markdown往復で同じ保存形式を使います
- 同じ埋め込みを複数のノート面へ同時表示しても、表示面ごとの購読・再接続・破棄を分離しました。競合再読込に失敗した場合は埋め込みを含むMarkdownへ戻し、次の保存でも参照を保持します
- ボードへ複数ビュー、KJ法用シャッフル、安定ID付きグループ、グループテンプレート、トピック／グループ／自由点へ接続できるライン端点を追加しました。座標や作図状態はビューごとに保存し、トピック本文は共有します

### ビューワー連携と単独アプリを整理しました

- チャット添付を元資産のビューワー表示へ統一し、アノテートの追加・編集・削除後に対象サムネイルだけを再生成します。合成できない場合も元画像と「アノテートあり」を表示します
- WindowsとCloud／PWAの単独アプリをビューワー、タイマー、クイックメモの3種類へ整理しました。旧ノート／シナリオ／シート／ボードのURLと既存ファイルは削除せず、Meldex本体の該当画面へ案内します
- Windows Releaseは3アプリそれぞれの単独EXE、ZIP、ZIPのSHA-256（計9資産）を必須化し、起動確認に失敗した配布物を公開しないようにしました

検証: トピック契約・保存・索引・移行・可搬アーカイブの対象契約102件、Note埋め込み／保存競合52件、関連するボード・アノテート・単独アプリ契約、実ChromeのDesktop／mobile 9操作を確認しました。10,000トピックのシート、2,000トピックのボード、5ソース・50,000件の索引、20埋め込みの性能モデルはv0.7.357比1.008倍で10%悪化ゲート内、最大メインスレッド処理は0.089msでした。Cloud初回操作可能時間は人工遅延下で5.539〜5.720秒、横はみ出しとブラウザエラーは0件です。全量unittest 8,600件（timeout 0）の初回unexpected 24件は、今回の用語・生成台帳・配布コピーに追随していなかった15件を同期しました。変更後全量のunexpectedは9件で、全量負荷時だけの3件を隔離実行で合格、sandbox権限1件を同じテストの権限付き実行で合格と確認しました。残る5件は開始基準からある指示書ハードリンク／不変E2E台帳3件と、この環境のTcl不足です。
## v0.7.357 (2026-08-23)

### 進捗表示と設定ツリーを修正しました

- 画面右下の処理パネルを右レールの左側へ移し、クイックメモボタンを覆わないようにしました。進捗のステータスバーへの重複表示は廃止し、Xブックマーク、Xアカウント投稿、Notionなど総件数が分かる処理は、バーと文字に実際の進捗率を表示します
- 設定ダイアログの左ツリーで、親項目を再度クリックして閉じられるようにしました。子項目を選んだ場合は子だけを強調し、親より一段深い位置に表示します

### ノートとシートの開く操作を修正しました

- ノートの空行で `/` メニューから「区切り線」を選び、その行を区切り線へ変換できるようにしました。メニュー内の文字が押しつぶされて縦書き状になる表示も修正しました
- シートのエントリ列にある開くボタンは、設定した「メインパネル」または「右サイドバー」の一方だけへ開くようにしました。新規・未設定時の初期値は「右サイドバー」です
- Xブックマークなど、シート内だけに保存され実体の `.md` ファイルを持たないエントリも、「ファイルが見つかりません」にならず右サイドバーで開けるようにしました

検証: 進捗・設定ツリー・ノート・シートの対象unittest、JavaScript／Python構文検査、PC・タブレット・スマートフォンの実Chrome E2Eで、進捗率、右レールとの非重複、設定ツリーの開閉・選択、区切り線変換、シートの開き先とSQLite論理エントリ表示を確認しました。

## v0.7.356 (2026-08-23)

### フォルダツリーの作成・表示とフィルタを修正しました

- フォルダツリーの行内 `+` からシートを作成しても、作成位置へ強制スクロールせず、ほかのセクションと現在の表示位置を保つようにしました。新しいシートの選択とメインパネルで開く動作は維持します
- フォルダツリーの「表示」で、リスト／グリッドが実際の一列／複数列へ切り替わり、サムネイルの「なし／小／中／大」が描画へ反映されるようにしました。選択は再起動後も復元します
- 不要な「フィルタ」補助行を廃止し、ポップアップをヘッダーのフィルタアイコン直下へ統一しました。フィルタOFF時を含め、テーマによって種類ボタンの文字が読めなくならない配色にしました
- 動画をメインパネルと右サイドバーで開いた場合の自動再生、動画クリックによる一時停止／再生が共通ビューワー経路で動くことを実動画の回帰テストへ固定しました

検証: 関連unittest 54件、生成監査契約72件、JavaScript／Python構文検査、実Chromeで新規シート作成後のスクロール維持、フォルダツリーのグリッド／サムネイル、PC・タブレット・スマートフォンのフィルタ表示、実MP4の自動再生とクリック切替を確認しました。全量unittest 8,441件の一度の実行では14件が失敗し、今回の生成監査差分4件は再生成後の対象契約で合格、残る10件は不変E2E台帳・一時DB／再試行・公開フォームキュー・Windows最前面状態の既存失敗として分離しています。

## v0.7.355 (2026-08-23)

### Cloud初回起動時の日次記録エラーを防ぎました

- Dropbox未接続の新規ブラウザでは、自動日次記録が保存先の準備完了を待つようにし、接続案内の上へ「Unexpected end of input」エラーが重なる問題を修正しました
- Dropbox接続後または端末内保存の準備完了後は日次記録を再評価し、閲覧専用・オフライン・接続解除中は書き込まず静かに次回へ持ち越します。手動操作のエラー通知は従来どおりです

検証: 自動日次記録の対象契約、Cloud静的生成と整合検査、保存状態のない実ChromeによるPC・タブレット・スマートフォン幅の初回起動を確認しました。公開中の版から変更されていない主要CRUD E2Eの既存失敗1件は今回の回帰ではないことを履歴で分離し、実Dropbox上の複数端末CRUDはDebuggerの実機テストへ登録します。

### 初回起動を端末内保存で始めるようにしました

- デスクトップ版とCloud版の初回起動から保存先の選択を外し、Dropboxへ接続せず、この端末内への保存で開始するようにしました。Dropboxは必要になったときに設定から接続できます
- 初回案内を「ようこそ」「同意」の2ステップへ整理し、利用条件への同意・ベータ版・バックアップ確認の項目だけに、赤字の「必須」を表示するようにしました。クラッシュ報告、利用統計、更新確認は引き続き任意です
- 既に選択済みの保存モードとDropbox設定は維持します。旧共有サーバー設定が残る場合も、既存データを黙って切り替えないための移行確認を残しています

### エントリの列一覧をシート単位で開閉できるようにしました

- エントリ詳細の列一覧を見出しの矢印で閉じ、同じ場所からすぐ再表示できるようにしました。初期状態は表示です
- 開閉状態は同じシート内の全エントリ、メイン表示、サブパネル、モバイルドロワーで共有します。カスタムのエントリレイアウトにも同じ開閉操作を適用しました
- 配布ページの「作る」の説明を、設定資料の作成と企画・構成の検討を幅広く表す文言へ更新しました
- スマートフォンでUI表示を拡大した場合も、書式設定のカラーパレットが画面内に収まり、全項目をスクロールして操作できるようにしました。キーボード操作時の選択位置も枠線で確認できます
- 長時間処理の進捗トレイがメニューや確認ダイアログを覆わず、処理中も各操作を続けられるようにしました

検証: 初回起動、同意、端末内保存、列一覧、カスタムレイアウトをPC・スマートフォン・タブレットの実Chromeで確認しました。公開候補の実Chrome E2Eは700件を1回完走し、失敗20件を一括分類しました。公開停止条件（データ消失、保存破損、認証・安全性、起動不能、主要操作不能）に該当する失敗はなく、現行実装へ追随していなかった6件を修正して直接テストへ合格、残る古い期待値・軽微UI・限定機能14件は根拠付き既知台帳へ記録しました。全量E2Eは反復せず、配布物の起動・法的表示・ハッシュ・静的生成を個別に確認しています。

## v0.7.354 (2026-08-22)

### 保存・競合・索引の残課題を解消しました

- ノートの置換後保存、フロントマターの原子的な保存、設定の同時更新、Dropbox／端末内ファイルの安全な置換を見直し、失敗・競合・再試行・再読込でも原本や別設定を失わないようにしました
- ファイル名変更時の検索索引をSQLiteへ一本化し、Windowsで大文字・小文字だけを変えた場合や一時的なロックが起きた場合も、古い名前が検索結果へ残らないようにしました
- バックリンクのUnicode表記、オートリンク辞書の更新、Xアカウント取り込みの再開位置、共有カレンダーの非同期更新を修正しました

### 削除・表示・操作の信頼性を改善しました

- フォルダツリーとシートの削除確認へ安定IDを引き継ぎ、取り消し後にやり直す場合も最新の参照関係を再確認するようにしました
- 直接開いた画面と比較タブの復帰、ノートの置換直後の追加入力、シートの日本語を含む数字順、モバイルの削除操作、メニューのEscape操作と画面内配置を修正しました
- 回復用のバックアップファイルを通常の一覧やリンク候補から除外し、検証設定が実際の作品フォルダを参照しないように隔離しました

### 共通基盤と診断を整理しました

- HTML、属性、CSS識別子、URL、XMLの文字処理を共通化し、危険な文字列を扱う表示箇所と例外的なHTML生成箇所を機械検査できるようにしました
- Desktop、常駐アプリ、付箋、ノート、シナリオ、シート、タイマー、ビューワーへ、同意・伏せ字・重複防止付きのクラッシュ回収を接続しました。作品本文、画像、ローカル変数は自動送信しません
- 単独アプリの共通ランタイム、検索索引の保存処理、ノート／シナリオ／共通画面の実ブラウザ操作を責務別ファイルへ分割し、ビルド時の作業フォルダ外への自動同期を明示操作だけに限定しました
- タグ管理とシートのフォルダツリー表示のマニュアルを追加し、長大化した変更履歴は直近5版と履歴アーカイブへ分けました

検証: 全量ユニット8,428件で予期しない失敗0件・timeout 0。条件付き11件はブラウザ有効化またはWindowsのシンボリックリンク権限がある環境で実行する設計どおりのスキップです。実ChromeでDesktop、Cloud、360px／390pxスマートフォン、タブレットの初回起動、固定レイアウト、フォルダツリー削除、シート削除、メニュー操作を確認しました。実Dropbox／Google／Microsoft／給与サービス、配布版の強制終了、タッチ／高DPI、公開Cloudは未確認です。

## v0.7.353 (2026-08-22)

### Google Chatの履歴をTakeoutから読み込めるようにしました

- Google Takeoutに含まれるプロフィール、DM、グループ、スペース、スレッド、メッセージ、リアクション、添付を自動検出し、原本を残したまま月別の読みやすい会話ファイルへ変換します。旧Googleハングアウト形式の会話イベントも同じ保存先へ取り込みます
- 同じアーカイブ、分割アーカイブ、後日作成したTakeoutは会話ID・メッセージID・内容指紋で照合し、変更のない履歴を重複作成しません。展開済みフォルダを同じ場所で更新した場合も内容の変更を検出し、上書き前の原本を内容指紋付きの版として残します
- Google側で削除された履歴をMeldexから自動削除せず、編集済みメッセージと欠損添付を明示します。未対応形式は黙って破棄せず原本として保持し、メッセージ本文や参加者を実行結果・監査ログへ出しません

### Dropbox・Debugger統合後の不整合を修正しました

- 開発版でもCloud版と同じクラッシュ報告クライアントを読み込み、実行経路の差をなくしました
- フィードバックの送信履歴で「対応状況」を固定列として確認できるようにしました
- Meldex標準のDebugger送信先を使う場合も、チャットからの不具合報告を実通信せず端末内の送信待ちへ安全に積むテストへ更新しました

検証: Google Chat、Google取り込み、版整合の対象27件、Dropbox・Cloud・Debugger統合の対象テスト、実Chromeの保存先選択・旧設定移行・フィードバック設定5経路を合格。広域ユニット8,347件では統合に直結する失敗を解消し、正規チェックアウトでのみ成立するハードリンク検査を除いて再確認しました。実Google Takeoutによる件数照合と代表会話確認は未実施です。

## v0.7.352 (2026-08-22)

### 自分が送った報告をMeldex内で確認できるようにしました

- 利用者のホームにある `Meldexフィードバック` シートを起動時に自動準備し、フィードバックフォーム、サポート画面、常駐アプリ、同意済みクラッシュ報告の内容と送達状態を一覧できるようにしました
- 送信待ち、送信済み、送信失敗、送信先未設定、端末内保存を区別し、受付日時、Debuggerの受付番号、対象バージョン、失敗理由を同じ行へ反映します。オフライン再送や次回起動回収でも固定の端末内受付番号を使うため、履歴行は重複しません
- フィードバックフォームは、端末内保存だけで「開発者へ送信済み」と表示しなくなりました。Cloud版から送れない手動フィードバックも端末内保存と明示し、Cloudの自動クラッシュ報告は既存の標準クライアントを置き換えず送信結果だけを履歴へ反映します
- 遅れて到着した古い同期結果で送信済みの行が送信待ちへ戻らないようにし、フォームから渡された行パスは既存の回答行として確認できた場合だけ使用します。自由記入や入れ子の診断情報に含まれるローカルパス、メール、トークン形式も、端末内の送信待ち保存より前に伏せるようにしました

### クラッシュ診断を次回起動とCloudから送れるようにしました

- デスクトップ版が正常終了を記録できずに止まった場合、次回起動時に匿名の異常終了報告をDebuggerの送信待ちへ入れるようにしました。FastAPIの未処理500も、元の応答を変えず同じ送信待ちへ入ります
- Cloud/PWAの未処理例外も、Cookieやリファラーを送らない送信専用クライアントで直接報告できます。通信できない間は端末内へ保持し、同じ報告を二重登録せずオンライン復帰後に再送します
- 自動送信はクラッシュ報告への明示同意がある場合だけ有効です。同意を外すと未送信診断とセッション痕跡を削除します。作品、画像、文書本文、ソース本文、ローカル変数は送らず、Meldex専用ログも安全な項目だけを伏せ字化して扱います

検証: 送信履歴、Debugger送信、次回起動クラッシュ回収、未処理500、同意、伏せ字、フォームUIの関連テスト133件、SQLiteシート58件、追加プライバシー12件（合計203件）と、実Chromeのフィードバック設定E2E、JavaScript/Python構文検査を合格。実際に配布版プロセスを強制終了する確認、実Debugger、公開Cloudからの送信は未実施です。

## v0.7.351 (2026-08-22)

### 共同ワークスペースをDropboxへ一本化しました

- 初回起動と設定の保存先選択からMeldex共有サーバーを外し、複数メンバー、権限、カレンダー、スケジュールを含む共同ワークスペースはDropboxで共有する形へ統一しました。このPC／この端末への保存と、デスクトップ版のソースフォルダは引き続き利用できます
- 旧共有サーバー設定が残っている場合は自動接続せず、既存ファイルを移動・削除しないことを明示して、Dropboxまたは端末内保存を選び直す案内を表示します。選択を取り消した場合は旧設定を保持し、確定後にだけ旧接続先を削除します
- 共有サーバー向けのAPI転送、リソース取得、旧認証トークン転送を停止しました。デスクトップ版のローカルAPIはループバック待受けに固定し、配布版ではトークン検証を無効化できないようにしました
- NASはデスクトップ版のソースフォルダとして引き続き参照できます。ただし、NASだけではメンバー、権限、スケジュール、端末固有のロック・履歴・アノテートを共有せず、同じファイルの安全な同時編集も保証しません

検証: Dropbox一本化・旧設定移行・ループバック／認証固定の対象単体・契約テスト、Cloud起動ハーネスと保存先選択・旧設定移行E2EのDesktop・360px・390px・430px・tablet表示を合格。全件ユニット8,320件ではDropbox一本化に関する未分類失敗0件（今回と無関係のWindows最前面化テスト1件のみ、長時間シャード内で失敗）。NAS実機と実Dropbox複数アカウントによる確認は未実施です。
## v0.7.350 (2026-08-21)

### 画像読み込みと長時間処理の進み具合を統一しました

- ノート、シート、フォルダー、ボード、チャット、画像ビューアーなどで画像の読み込みに時間がかかる場合、画像の中央に分割リングを表示するようにしました。短時間で表示できる画像ではリングを出さず、失敗時は再読み込みできます
- インポート、ファイルのコピー・移動、シートの連続削除、自動タグ付け、同期、書き出しなどを、共通の進捗トレイとバーで確認できるようにしました。件数が分かる処理は割合を表示し、総量が分からない処理も進行中であることを示します
- 複数処理の同時実行、成功、部分失敗、失敗、取消、再試行、画面移動後の継続表示を同じ仕組みへまとめました。従来のステータスバー表示は補助表示として残し、スマートフォンや単独画面でも処理を見失わないようにしています

### GoogleデータをDesktopとCloudへ安全に取り込めるようにしました

- Googleドキュメント、スプレッドシート、スライド、フォーム、Gmail、連絡先を、ファイルを1件ずつ選ばず一括取得し、2回目以降は変更分を更新できるようにしました。共有アイテムは既定で除外し、共有ドライブはドライブ単位で選べます
- スプレッドシートはタブIDと型付きの値を保ち、ローカル編集との衝突は競合コピーへ退避します。Google側で削除・権限喪失になってもMeldex内のバックアップは消さず、利用不可状態として残します
- Google TakeoutのZIP／TAR／展開済みフォルダを安全に検査し、同じ内容の再投入をスキップします。Keep、ロケーション履歴、Chromeブックマークを変換し、未知形式も原本として保持します。Cloudでは大容量Takeoutをデスクトップ版へ案内します
- OAuthトークンは端末秘密ストアだけへ保存し、対象を追加した時だけ必要な読取権限を求めます。公開に必要な設定がない環境は接続能力を有効にしません

### 版履歴の改変検出を追加しました

- owner端末は共有変更レコードをHMAC署名し、editor端末の保存は「管理者の署名待ち」として継続できるようにしました。ownerが版一覧を開くと追認し、改変、従来の未署名版、署名待ちを区別して表示します
- owner鍵が失われた状態で新しい鍵を自動生成して過去の正常署名を改変扱いにすることはありません。復旧が必要な状態として停止し、版は隠したり削除したりしません

### サンプル配布とシート切替時のエラーを整理しました

- 製品に同梱していたサンプルデータ、導入画面、導入API、Cloud初回投入、独立サンプルパックを撤去しました。すでに利用者が保存・編集しているサンプルや任意のファイルを削除する処理は追加していません
- スマートフォンでシートを開いた直後に版管理へ切り替えると、閉じたシートの応答が後から描画されて赤い読込エラーになる問題を修正しました。シートへ戻った際は最新内容を表示します

検証: 最終差分の対象単体・契約・操作監査52件、Cloud公開準備・静的生成27件、Desktop・tablet・390px・CloudのGoogle設定実Chrome4件、390pxのシート→版管理→シート復帰E2Eを合格。画像・進捗表示は候補78経路の分類、関連回帰、Desktop・tablet・phoneの進行中／失敗／再試行／完了／動きを減らす設定を合格。JavaScript／Python構文、生成bundle、差分検査も合格。実Google／DropboxアカウントとDebugger送信は保留しています。

## v0.7.349 (2026-08-21)

### タスクごとの実績作業時間と制作進行の履歴を正確に残せるようにしました

- タスクを作業中・完了へ変更した時の実績区間と実際の出退勤打刻を照合し、退勤中・離席中を除いた「実績作業時間」をタスク別・参加者別に自動計算します。従来の予定シフトは実績計算に使わず、打刻不足や順序競合は確定値として扱わず理由を記録します
- 応援参加者、別タスクへの作業切替、締切延長、日をまたぐ作業と徹夜、打刻の追加・修正・削除に追随して再計算します。保存途中の失敗は対象タスクを元へ戻し、同じ打刻操作の再送でも失敗した再計算だけを安全にやり直せます
- 予定と実績の日次記録を営業日単位で自動保存し、バージョン管理パネルから過去の制作状況を確認できるようにしました。管理者向けには作品・担当者・品質別の履歴照会、業務改善指標、JSON／CSV出力、確認付きの明示削除をデスクトップ版とCloud版へ追加しました
- 最大10人・作品A／B／C・重複する制作期間・応援参加・日曜夜から月曜朝までの徹夜・締切延長・翌朝7時再開を含む決定的な回帰シナリオを追加しました
